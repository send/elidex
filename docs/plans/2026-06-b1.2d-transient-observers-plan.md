# B1.2d-i — MutationObserver transient registered observers (DOM §4.3)

**Status:** IMPLEMENTED (see §9 As-built deltas). plan-reviewed clean (0C/2I-fixed/~6MIN-fixed). Per-PR slice of the approved B1.2 umbrella → own plan-review (umbrella base-case clause = narrowly-scoped per-PR slice of an approved umbrella).
**Branch / worktree:** `b1.2d-range-transient` @ `/Users/kazuaki/repos/send.sh/elidex-b1.2d`, off `origin/main` `8b614682` (#409 B1.2b-3).
**Parent program:** Program B (F3 mutation-path) → B1.2 umbrella → **B1.2d**. Predecessors: B1 (#379 MO mechanism + single-node childList), B1.2a #384 (move-record), B1.2-frag #387, B1.2b-1 #393 / b-2 #399 / b-2-select #402, B1.2c #405 (textContent), B1.2b-3 #409 (validity fold).

## §0 Scope decomposition (anchor: ideal, not incremental)

The handoff item "**B1.2d = Range mutations + transient observers + live-range**" — re-derived against the code this session — resolves to **two real gaps + one already-done**:

1. **Transient registered observers** (DOM §4.3) — **NOT implemented** (grep `transient` across `crates` = 0 observer hits). The §4.3 MutationObserver registration model is incomplete: a node removed from an observed subtree stops being observed by the ancestor's `subtree:true` observer.
2. **Range mutation methods produce no `MutationRecord`** — `deleteContents`/`extractContents`/`insertNode` mutate via raw `EcsDom` (`range/mutation.rs`), bypassing the record-producing `apply_*`. (`cloneContents`/`surroundContents`/`createContextualFragment` are `NotSupportedError` stubs = separate slot `#11-range-full-impl`, OUT.)
3. **Live-range boundary fixup (DOM §5.5) — ALREADY DONE.** `LiveRangeBridge` (`range/live.rs`) synchronously handles `MutationEvent::{Remove,Insert,TextChange,ReplaceData,SplitText,NormalizeMerge}` and adjusts boundary points (`adjust_ranges_for_*`). Only the narrow shadow edge `#11-shadow-tree-live-range-tracking` (MINOR) remains. **Not B1.2d scope.**

**Decompose B1.2d (edge-dense → multi-PR per CLAUDE.md):** gaps (1) and (2) are **different subsystems touching disjoint layers** and each carries ≥3 intersecting invariant axes, so they are **NOT bundled** (One-issue-one-way / "単一 PR に束ねない"):

- **B1.2d-i (THIS slice) = transient registered observers.** Layers: **VM** (record-drain orchestration) + **`elidex-api-observers`** (the `MutationObservedBy` component + registry). The foundational MO-machinery completion.
- **B1.2d-ii (carved, own plan-review) = Range mutation records.** Layers: **`elidex-dom-api`** (Range handlers) + **`elidex-script-session`** (`range/mutation.rs` → route through record-producing `apply_*`). Routes through the (by then transient-aware) removal chokepoint, so the §4.3-transient × Range-removal coupling is handled **by construction**.

**Why transient-first (the ordering call, not asked — lens converges):** (a) it is the *chokepoint* feature — once the removal record-drain creates transients, every removal path (removeChild / move-adopt / replace / replaceChildren / **and** the future Range removals) gets it by construction; (b) the two slices touch **disjoint files/layers** (B1.2d-i = `vm_api.rs`/`natives_promise.rs`/`api-observers/mutation.rs`; B1.2d-ii = `dom-api/range/*`/`script-session/range/*`) so order is additive with no strangler; (c) foundational-machinery-then-API-surface mirrors how B1 was built (mechanism → coverage). *Alternative considered:* Range-first (continues the B1.2a–c API-surface sweep) — rejected as ordering because it leaves the MO machinery incomplete longer and gains nothing (Range records don't depend on transients). **Surfaced for plan-review challenge.**

**Explicitly OUT of B1.2d-i:** (a) Range mutation records (= B1.2d-ii). (b) `cloneContents`/`surroundContents`/`createContextualFragment` (`#11-range-full-impl`). (c) live-range fixup (DONE, §0.3). (d) ~~`source`-keyed observe clear~~ **IMPLEMENTED with per-registration identity (final, Codex R4-H1, §9-delta-11)**: each `MutationObservation` carries `reg_id: u64` (the spec's registered-observer identity) and `source: Option<u64>` (the originating registration's `reg_id`, `None` for permanent); observe() step 7.1 clears by source reg-id, notify step 6.3 stays observer-keyed, unbind clears all. The earlier "over-clears in a contrived corner" residual (`#11-mutation-transient-source-fidelity`) is **CLOSED** — the reg-id key distinguishes multiple registrations of one observer on a single node (which the R3 ancestor-entity intermediate could not). (e) boa (production runs boa but MO delivery is VM-side per B1; boa deletion-bound, [[feedback_boa-findings-light-touch]]) — **Codex R3-G3 confirmed boa's delivery path lacks transient create/clear; scoped out per the program-wide boa-deletion boundary (D-26 PR7 / S5), the transient lifecycle becomes the only path when boa is removed.**

## §1 Problem

DOM §4.3: each node has a **registered observer list** (defined in §4.3 "Mutation observers"; §4.3.1 is the `MutationObserver` interface). A **transient registered observer** is appended to a *removed* node so an ancestor's `subtree:true` observer keeps receiving records for mutations in the removed subtree until the next microtask delivery, after which it is cleared. elidex implements neither half:

- **Creation gap (DOM §4.2.3 "remove" step 15):** *"For each inclusive ancestor inclusiveAncestor of **parent**, and then for each registered of inclusiveAncestor's registered observer list, if registered's options['subtree'] is true, then append a new transient registered observer whose observer is registered's observer, options is registered's options, and source is registered to **node's** registered observer list."* — **not** gated by `suppressObservers` (only step 16's record is). elidex's removal paths (`apply_remove_child` + the move-adopt source-removal in `apply_append_child`/`apply_insert_before`/`apply_replace_child` + `apply_replace_all`) do none of this.
- **Clearing gap (DOM §4.3 "notify mutation observers" step 6.3):** *"For each node of mo's node list: remove all transient registered observers whose observer is mo from node's registered observer list."* — elidex's microtask delivery (`vm_api.rs` `clear_pending_records` / the `NotifyMutationObservers` microtask) drains record queues but has no transient housekeeping.

**Observable effect today:** `obs.observe(ancestor, {childList:true, subtree:true})`, then `ancestor.removeChild(mid)` where `mid` has a subtree, then `mid.appendChild(x)` (mutating the now-detached subtree before microtask) → the spec delivers the `mid.appendChild` childList record to `obs` (via the transient observer on `mid`); elidex delivers **nothing** (no transient on `mid`, and `mid`'s ancestor walk hits null).

## §2 Coupled invariants at this corner (the design surface / edge matrix)

1. **Creation timing: eager per-mutation `notify` (spec-aligned with "during remove"), NOT a deferred microtask drain.** Spec runs step-15 creation *during* `remove` (synchronous). **Verified mechanism (plan-review R1, corrected):** elidex's ancestor-walk `notify` runs **eagerly/synchronously per record** — `queue_mutation_record` (`mutation_observer.rs:459`) calls `notify_one` (`:521`) immediately, drained at each `invoke_dom_api` boundary in JS-execution order. The `NotifyMutationObservers` microtask (`deliver_pending_mutation_records:469`) does **no walk** — it only flushes already-queued per-observer record queues to callbacks. (`natives_promise.rs:279` "FIFO drain" is *callback* delivery order, irrelevant to creation.) So hooking creation inside `notify_one` runs it at the mutation, before `node` can be re-mutated — exactly the spec's "during remove", correct **by construction** (no deferred-drain race; the original "microtask-FIFO" framing here was a mis-read of the timing model). The only ordering that matters is *within one call emitting multiple records* (move-adopt §2.5): the records `Vec` iterates source-removal-first (`move_record_list`), so the removal's transient precedes the destination record's walk. **Invariant to verify:** `notify_one` is the single eager per-record walk site (both callers `:437` batch loop + `:459` route through it); the per-call records `Vec` is source-removal-first.
2. **Layering: creation cannot live in `apply_*`.** `elidex-api-observers` **depends on** `elidex-script-session` (Cargo.toml), so `apply_remove_child` (script-session) **must not** reach `MutationObservedBy` (api-observers) — that would be a dependency cycle. → transient creation is owned by **`elidex-api-observers`** (a new registry method) and **orchestrated by the VM** at the drain (VM depends on both). This is also why B1.2d-i and B1.2d-ii are layer-disjoint.
3. **Walk PARENT's inclusive ancestors; append to the removed NODE.** Not node's own observers (those stay on node, still observing it directly). Source ≔ the ancestor's registered observer.
4. **`suppressObservers` does NOT gate creation — and every removal surfaces its removed set on a `ChildList` record (plan-review R1 correction).** Spec step 15 (creation) has no `suppressObservers` guard (only step 16's *record* does). **Verified:** every elidex removal path carries the removed set on a `ChildList` record reaching `notify_one` — standalone (`apply_remove_child` = 1 removal record; move-adopt = a source-removal record) **or coalesced** (replace = `removed_nodes=[old_child]`; replace-all = removed children in one record, queued iff added∪removed non-empty). So the §5.2 **record-keyed** hook (`record.kind==ChildList && !removed_nodes.is_empty()`, keyed off `record.target` + `record.removed_nodes`) covers **all** of them — there is **no** record-less removal path that loses the removed set (the earlier "suppressed records → need a non-record path" framing was inverted). Invariant to confirm at impl: no `apply_*` path detaches a non-empty removed set while emitting zero `ChildList` record.
5. **Move-adopt removals create transients too.** `el.appendChild(alreadyParented)` adopts → source-parent removal (§4.5 step 2 / remove). That removed-from-old-parent step is a `remove` → step 15 applies. So the move-adopt source-removal record drain must also create transients on the moved node (w.r.t. its *old* parent's ancestors). Edge: the node is immediately re-inserted elsewhere — but the transient (keyed to the old-ancestor observer) still belongs until microtask.
6. **Clearing needs "mo's node list"; elidex has no observer→nodes index.** ECS-native resolution: transient observers ARE `MutationObservedBy` entries (component), so clearing = **one ECS query** over all `MutationObservedBy` removing entries where `transient && observer ∈ notifySet` (the cleared observers). No reverse index, no side-store (mirrors the existing `disconnect()` retain-query `mutation.rs:150`). Preserves the side-store→component rule + Sub-check 2b (no new SoT to reconcile).
7. **`subtree:true` preserved on the transient ⇒ deep-subtree mutations caught by the upward walk.** Spec appends the transient to `node` only (not descendants), carrying the *original options* (`subtree:true`). A mutation deep in the removed subtree produces a record whose `notify` walk climbs target → … → `node` (transient, subtree:true) → matches. So append-to-node-only is correct (no per-descendant transients). **Invariant:** the transient entry carries the original `MutationObserverInit` verbatim.
8. **ECS-native by construction (Sub-check 2a/2b).** Transient = a `MutationObservedBy` entry with a `transient` marker; it participates in the existing `notify` ancestor-walk with **zero** walk changes (the walk already reads `MutationObservedBy`). The ONLY new code = creation (append entries at drain) + clearing (query-remove at microtask end) + the marker field. No new registry data structure, no OO observer-list object.
9. **Dual-runtime / VM-scoped.** MO delivery is VM-side (B1); transient creation/clearing piggyback the VM drain. boa = known-differ, scope-out.

## §3 Spec coverage map (webref-verified this session)

| Spec section | Step | Branch | Touch (compile/dispatch site) | Full enum? | User-input flow |
|---|---|---|---|---|---|
| DOM §4.2.3 remove (`#concept-node-remove`) | step 15 | for each `subtree:true` registered observer on an inclusive ancestor of `parent` → append transient (observer+options+source) to `node`'s list | NEW `MutationObserverRegistry::add_transient_observers(dom, parent, &removed)` (NEW), called inside `notify_one` (`mutation_observer.rs:521`) per ChildList removal record | ✓ | yes (observer `init` the page set via `observe()`) |
| DOM §4.2.3 remove (`#concept-node-remove`) | step 16 | `suppressObservers == false` → queue removal record | — (existing, done by B1/B1.2a; OUT of this slice — listed to show step-15 is NOT gated by it) | n/a | n/a |
| DOM §4.3 notify mutation observers (`#notify-mutation-observers`) | step 6.3 | for each node of mo's node list, remove transient registered observers whose observer is mo | NEW `clear_transient_observers(dom, &notifySet)` (NEW) ECS-query in `deliver_pending_mutation_records` (`mutation_observer.rs:469`), `notifySet = observers_with_records()` | ✓ | no (MO machinery) |
| DOM §4.3 registered observer / transient registered observer (`#registered-observer`) | def | transient registered observer = registered observer + source (§4.3 defs; §4.3.1 = the `MutationObserver` interface) | `MutationObservation` (`mutation.rs:76`) += `transient: bool` (source collapses to observer-id — §2.6/§4) | ✓ | no |
| DOM §4.3 `observe()` (`#dom-mutationobserver-observe`) | re-observe | "remove all transient registered observers whose source is registered" | observe-time source-clear on the target node (`mutation.rs` observe path), keyed on observer-id | ✓ | yes (observe target + `init`) |

**Breadth:** K=1 spec (dom), M=5 entries → single-PR scope (K<4, M<20). Citations webref-verified this session (`body dom concept-node-remove` steps 15/16; `body dom notify-mutation-observers` step 6.3; `body dom #registered-observer` def). All touch sites in **VM** (`vm/host/mutation_observer.rs`) + **`elidex-api-observers`** (`mutation.rs`); **no** `elidex-dom-api`/`elidex-script-session` algorithm change (= the carved B1.2d-ii Range-records slice).

### §3.1 User-input touch audit
- **No new untrusted surface.** Transient creation reads existing `MutationObservedBy` (observer options the page set via `observe()`) + the removed-node set (already computed by `apply_*`). Clearing removes component entries. The change can only **add** mutation-record deliveries that the spec mandates (a conformance gain) + bounded transient-entry growth cleared every microtask.
- **DoS/growth bound:** transients are appended per removed-subtree-root per matching ancestor observer, cleared every microtask delivery (step 6.3). No unbounded accumulation (a never-delivering observer is the pre-existing record-queue concern, not new).

## §4 Design decision (ideal: transient = a marked component entry, VM-orchestrated, cleared by ECS query)

The ideal is **not** a new "transient observer list" structure (OO direct-translation, Axis 2a) nor an observer→nodes reverse index (side-store, Axis 2b). A transient registered observer **is** a `MutationObservedBy` entry that happens to be marked transient — it then participates in the existing notify-walk for free.

- **Data:** `MutationObservation` (`mutation.rs:76`) gains `transient: bool` (default false). The spec's `source` (a registered observer) collapses to the entry's `observer` id in elidex: a node has at most one registration per observer (observe() replaces), so both clearings — notify step 6.3 ("observer is mo") and observe() step 7.1 ("source is registered") — key on **observer id**. No `source` field needed; §4.3 fidelity preserved by construction (documented, not deferred).
- **Creation seam (api-observers, VM-invoked):** new `MutationObserverRegistry::add_transient_observers(&self, dom: &mut EcsDom, parent: Entity, removed: &[Entity])` = remove-step-15: walk `parent`'s inclusive ancestors (reuse the same bounded walk as `notify`, `MAX_ANCESTOR_DEPTH`), collect each `subtree:true` `MutationObservation`, append a `transient:true` copy to each `removed` node's `MutationObservedBy` (insert the component if absent; dedup if an identical (observer,transient) entry exists). Invoked by the VM **at the record-drain**, for each drained ChildList record with non-empty `removed_nodes`, using `record.target` (= old parent) + `record.removed_nodes`, **before** `notify`-ing subsequent records (FIFO order, invariant §2.1).
- **Clearing seam (api-observers, VM-invoked):** in the VM microtask delivery, after building the delivered-observer set (notifySet) and firing callbacks, run one ECS query over `MutationObservedBy` (mirror `disconnect()`'s `retain`-query `mutation.rs:150`) removing entries where `transient && notifySet.contains(observer)`; drop the component if it becomes empty. (Also: `observe()` on a node removes that node's existing `transient` entries for the same observer — re-observe path.)
- **Why VM-orchestrated, not a ConsumerDispatcher consumer:** a `MutationEvent::Remove` consumer (like `LiveRangeBridge`) was considered — rejected because (a) it splits transient lifecycle across two seams (creation at ConsumerDispatcher, clearing at microtask) on different timing models, and (b) the record-drain already iterates exactly the removal records in FIFO order with `parent`+`removed_nodes` in hand, so creation co-locates cleanly with the existing `notify` call (one orchestration point, single-writer). ECS-native + One-issue-one-way.

**Crate-API / layering mapping:** algorithm home = `elidex-api-observers/src/mutation.rs` (owns `MutationObservedBy` + registry); orchestration = VM drain (`vm/vm_api.rs` + `vm/host/mutation_observer.rs`/`natives_promise.rs`). No `elidex-script-session`/`elidex-dom-api` change. No `vm/host/` *algorithm* (the walk/append/clear live in api-observers; VM only calls them at the drain = marshalling/orchestration, Layering-mandate-clean).

## §5 Implementation

### 5.1 `elidex-api-observers/src/mutation.rs`
- `MutationObservation` += `transient: bool`. All existing constructors set `false` (write-site audit: `mutation.rs:137` append + `:143` observe-insert-one — both permanent). Also fix the pre-existing `§4.3.1`→`§4.3` docstring drift on `mutation.rs:81` (registered-observer-list) + `disconnect()` while touching the file.
- `add_transient_observers(&self, dom, parent, removed: &[Entity])`: bounded inclusive-ancestor walk of `parent` (factor the walk shared with `notify`); for each `subtree:true` observation, for each `removed` node, append `MutationObservation { observer, init: init.clone(), transient: true }` (skip dup). Insert `MutationObservedBy` if absent.
- `clear_transient_observers(&self, dom, delivered: &BTreeSet<MutationObserverId>)`: ECS query over `MutationObservedBy`; retain entries `!(transient && delivered.contains(observer))`; remove emptied components. (Mirror `disconnect()`'s `retain`-query shape, `mutation.rs:150`.)
- `observe()`: before inserting the new registration, remove existing `transient` entries on that node whose `observer == id` (re-observe source-clear).

### 5.2 VM orchestration (`vm/host/mutation_observer.rs`)
**Creation — inside `notify_one` (`mutation_observer.rs:521`), the single per-record notify chokepoint.** Both notify-phase callers route through it (`:437` batch `deliver_mutation_records` loop + `:459` `queue_mutation_record`), so hooking `notify_one` covers both by construction (One-issue-one-way; no per-call-site edit). At the top of `notify_one`, before the ancestor-walk `notify`, if `record.kind == ChildList && !record.removed_nodes.is_empty()`, call `host.mutation_observers.add_transient_observers(dom, record.target, &record.removed_nodes)` (record.target = the old parent — §2.5 move-adopt uses the source-removal record whose target is the old parent). `notify_one` runs **eagerly/synchronously per record** (not microtask-deferred), so creation happens at the mutation in JS-execution order — correct by construction (§2.1). **Borrow:** `notify_one` currently takes the immutable `&EcsDom` split (for `notify`); `add_transient_observers` mutates `MutationObservedBy`, so switch to the `&mut EcsDom` split (`split_dom_mut_and_observers`, `host_data.rs:1423`) and reborrow `&*dom` for the subsequent `notify`.
**Clearing — in `deliver_pending_mutation_records` (`mutation_observer.rs:469`), the §4.3 notify-mutation-observers microtask.** It already collects `observer_ids = observers_with_records()` (= the spec's `notifySet`, step 2). Per step 6.3 (clear transients before invoking callbacks, step 6.4), call `host.mutation_observers.clear_transient_observers(dom, &observer_ids_set)` **before** the `deliver_to_observer_callbacks` loop — so callback-created transients (re-entrant mutation during 6.4) survive to the next microtask. (NOT the `vm_api.rs:693` `clear_pending_records` site — that is `Vm::unbind` rebind-scrub, unrelated; transient lists need no unbind scrub since they are `MutationObservedBy` components despawned with the world.)

### 5.3 No `apply_*` / dom-api change
Confirmed by §2.2 layering — creation/clearing are api-observers + VM only. (B1.2d-ii will add the Range→`apply_*` routing; that automatically flows through this drain.)

## §6 Tests (engine-independent unit in `elidex-api-observers` + VM integration)
- **api-observers unit** (drive registry directly with a hand-built `EcsDom`): (a) remove of a subtree-observed mid-node appends a transient to mid; (b) the transient carries the original `subtree:true` init; (c) `clear_transient_observers(delivered)` removes it iff observer ∈ delivered; (d) a `childList`-only (no subtree) ancestor observer creates **no** transient; (e) node's OWN observers are not made transient; (f) **coalesced-record creation**: a subtree-observed child removed by `replaceChildren`/`replaceChild` (whose removal rides a *coalesced* `ChildList` record, not a standalone one) still gets a transient via the record-keyed hook (drive `notify_one` with a coalesced record carrying `removed_nodes`, or `add_transient_observers` with the coalesced record's `target`+`removed_nodes`) — locks §2.4.
- **VM integration** (real JS, the headline §1 scenario): `observe(ancestor,{childList,subtree})` → `removeChild(mid)` → `mid.appendChild(x)` → microtask → the `mid.appendChild` childList record IS delivered; a second microtask round (no re-observe) delivers nothing (transient cleared). Move-adopt variant (§2.5). Re-observe clears prior transients (§5.1 observe path).
- **Regression:** existing MO childList/attr/characterData delivery + `disconnect()` despawn cleanup unchanged.

## §7 Risk / review-tail pre-emption
- **Eager-notify ordering (§2.1 — corrected):** creation is eager (synchronous in `notify_one` at the mutation), so cross-statement removal-then-subtree-mutation ordering is correct **by construction** (JS-execution order) — there is NO deferred-drain race to guard (the original "highest-care microtask-FIFO" worry was a mis-read; `notify_one` is not microtask-deferred). The only residual ordering = the within-call multi-record `Vec` order (move-adopt source-removal-first), already guaranteed by `move_record_list`. Still add the §6 VM integration test exercising removal-then-detached-subtree-mutation to lock the eager behavior.
- **Move-adopt double-context (§2.5):** the moved node leaves old parent (transient w.r.t. old ancestors) then joins new parent. Ensure the transient is created against the OLD parent's ancestors (the source-removal record's `target` = old parent), not the destination. The record shape already carries the source removal separately (B1.2a `move_record_list`).
- **Clearing scope:** clear only `delivered` observers' transients (step 6.3 is per-mo-in-notifySet), NOT all transients — an observer that didn't deliver this microtask keeps its transients. Query filter must AND `transient && delivered.contains(observer)`.
- **File size:** `mutation.rs` 618 → +~60 (well under 1000). VM orchestration lands in `vm/host/mutation_observer.rs` (720 → ~735, under 1000), NOT `vm_api.rs` (1197, already >1000) — §5.2 routes clearing to `deliver_pending_mutation_records` in `mutation_observer.rs`, so no substantive add lands in the 1197-line file. `range/live.rs` (1021) + `tree.rs` (1019) are **untouched** by B1.2d-i.
- **Edge-density:** moderate (creation × clearing × move-adopt × suppressObservers × subtree-gating) but **reuses** the existing `MutationObservedBy` component + notify-walk (no new structure) → bounded. Umbrella mandates plan-review; blast = MO machinery (VM) → `/external-converge` after the pre-push 6-stage gate.

## §8 Carve / defer ledger
(Umbrella-tracked B1.2 sub-slice, own plan-review — not a fresh defer slot.)
- **CLOSES `#11-mutation-transient-observers`** at landing (register the close in `project_open-defer-slots.md` SoT — it is currently listed open in the DOM/parser/observer cluster).
- **B1.2d-ii (Range mutation records) carved → own plan-review.** `deleteContents`/`extractContents`/`insertNode` route through record-producing `apply_*` (+ the engine-indep `range/mutation.rs` returning records); flows through this slice's transient-aware drain by construction. Not a fresh slot (B1.2 umbrella sub-slice). `cloneContents`/`surroundContents`/`createContextualFragment` remain `#11-range-full-impl`.
- **`#11-shadow-tree-live-range-tracking`** (MINOR, pre-existing) — untouched; live-range fixup is otherwise DONE (§0.3).
- ~~**`#11-mutation-transient-source-fidelity`**~~ — **NO LONGER CARVED (fixed in Codex R4-H1, §9-delta-11).** Per-registration identity (`reg_id` + `source: Option<u64>`) makes observe() step 7.1 source-keyed and distinguishes multiple registrations of one observer on a single node; the over-clear residual is fully gone. Do **not** register this slot at landing (superseded — it was provisional in the pre-R4 commits).
- **Remaining B1.2:** B1.2d-i (this) → B1.2d-ii (Range records) → B1.3 (characterData incl. Comment + oldValue) / B2 (attribute-write convergence, B0 §4.5).

## §9 As-built deltas (impl-time spec re-verification, #409 discipline)

Four deviations from the pre-impl plan, each from re-verifying a premise against webref/code at implementation time (per [[feedback_plan-review-verify-preserve-existing-spec-claims]]):

1. **`notify` §4.3.2 `interestedObservers` map-collapse added — the plan's §2.8 "zero walk changes" premise did NOT survive spec scrutiny.** §4.3.2 "queue a mutation record" step 1 builds `interestedObservers` as a **map keyed by observer**, collapsing multiple matching registered-observer entries along the inclusive-ancestor walk into **one** record per observer (step 4). elidex's `notify` instead pushed one record per matching `MutationObservedBy` entry — a **latent dup bug** (already wrong for one observer registered on ≥2 inclusive ancestors) that transients newly make reachable (a removed node can hold a permanent + transient entry, or multiple transients, for the same observer). Refactored `notify` to accumulate an `interestedObservers: BTreeMap<observer, Option<oldValue>>` during the walk and enqueue once per observer (step 3.2.2 default-null / 3.2.3 set-old-value / step 4). This is the canonical spec fix at the right altitude (One-issue-one-way) and incidentally fixes the pre-existing multi-ancestor dup. Locked by `notify_collapses_{permanent_and_transient_same_observer,observer_on_two_ancestors}`.
2. **`add_transient_observers` / `clear_transient_observers` are free `pub fn` in `mutation.rs`, NOT registry methods (plan §4/§5.1 framing).** Transient registered observers live **entirely** in the per-node `MutationObservedBy` component; the registry owns only record queues. A registry `&self` method that never reads `self` would trip `clippy::unused_self` (pedantic, on) and is an OO-translation smell. Free functions co-located with the component definition are the honest ECS-native form (Sub-check 2a). `observe()` re-observe (step 7.1) reuses `clear_transient_observers`.
3. **Clearing is per-observer *interleaved* inside the delivery closure, NOT one upfront set pass (plan §5.2).** §4.3 "notify mutation observers" step 6.3 runs **inside** the per-`mo` loop — after emptying that observer's queue (6.2), before invoking its callback (6.4). `deliver_to_observer_callbacks` calls `prepare(vm, id)` per observer before each callback, so `clear_transient_observers(host.dom(), mo_id)` lives in that closure (before the `records.is_empty()` short-circuit). This is strictly more faithful than the upfront `BTreeSet` clear: a transient created by an *earlier* observer's callback for a *later* observer still in `notifySet` is cleared at that later observer's turn, while a transient created by *this* observer's own callback survives to the next microtask. Single id, no set — simpler too.
4. **Citation-fix scope corrected from the plan's "fix §4.3.1→§4.3 on `mutation.rs:81` + `disconnect()`".** webref-verified the DOM §4.3 structure: §4.3 "Mutation observers" (defs: registered observer / registered observer list / transient registered observer / notify mutation observers) vs §4.3.1 "Interface MutationObserver" (`observe`/`disconnect`/`takeRecords` methods) vs §4.3.2 "Queuing a mutation record". Fixed the **definition** citations (module docstring + `MutationObservation` + `MutationObservedBy`, the §4.3.1→§4.3 drift). **Kept** `observe()` (`:118`) and `disconnect()` (`:149`) at §4.3.1 — they are interface methods, so §4.3.1 is correct; the plan's "+`disconnect()`" instruction would have *introduced* drift (followed the spec, not the plan, per #409).

All 45 api-observers unit tests + 109 `tests_mutation_observer` VM tests + full 6024 elidex-js suite green; boa runtime compiles (transients VM-only, boa known-differ + deletion-bound).

5. **File-size: `mutation.rs` crossed 1000 (618 → 1179), correcting the §7 estimate (predicted "+~60, well under 1000").** The growth is the §9-delta-#1 `notify` map-collapse refactor + the new test scenarios; **source is only 494 lines** (test module = 495–1179). Per CLAUDE.md "1000-line debt = touch-time split" the inline test module is a prime scenario-seamed split candidate, but the split is a **standalone PR/commit, NOT bundled with this feature** (mirroring the tree.rs-1019 → `task_2d979473` precedent from #409) → carried as a follow-up task. Recorded in the landing memo.

### Codex /external-converge deltas (post-push R-loop)

6. **R1-F1 (P2): the §4.3 notifySet was record-queue-derived (`observers_with_records`)** → `takeRecords()` before the microtask dropped the observer, skipping its step-6.3 transient clear (leak). Added the spec's "pending mutation observers" set (`pending` / `take_pending_observers`, populated in `notify`); both VM + boa delivery switched to it; dead `observers_with_records` removed.
7. **R1-F3 (P2): the observe step-7.1 predicate counted transient entries** → observing a transient-only node wiped unrelated removed-subtree transients. Gated the predicate + ensure-permanent find on `!transient`.
8. **R3-G1 (P2): unbind dropped the pending delivery but left transient `MutationObservedBy` entries** (`clear_pending_records` clears only registry state) → a same-DOM rebind could deliver through a stale transient. Added `clear_all_transient_observers(dom)`, called at the bound `Vm::unbind` block (before `hd.unbind()` zeros `dom_ptr`). Permanent registrations untouched.
9. **R3-G4 (P2): observe step-7.1 was observer-keyed, over-clearing other-source transients** → first fixed with an **ancestor-entity** `source` key (`source == target`); **superseded in R4 — see delta 11** (the entity key could not distinguish multiple registrations of one observer on a single node). The notify clear (step 6.3) is observer-keyed (spec-correct); `clear_all` (unbind) drops all.
10. **R3-G3 (P2): boa delivery lacks the transient lifecycle** → scoped out (boa deletion-bound, [[feedback_boa-findings-light-touch]] + §0.OUT(e); transients become the sole path at boa removal). **R1-F2 / R3-G2 / R5-I2 (P3, test-module split)** → deferred to the standalone split task (`task_b3cd18da`), CLAUDE.md "split は単独 commit" + #409 precedent.
11. **R4-H1 (P2): the R3 ancestor-**entity** `source` key could not distinguish multiple registrations of one observer on a single node** (a permanent + a chained transient → same `source == entity`), over-clearing a foreign-sourced transient on re-observe. **Final model:** `MutationObservation` carries `reg_id: u64` (the spec's registered-observer identity, allocated from a registry counter) and `source: Option<u64>` (the originating registration's `reg_id`, `None` for permanent). `observe()` step 7.1 = `clear_transient_observers_by_source(dom, source_reg_id)`; notify step 6.3 = `clear_transient_observers(dom, observer)` (observer-keyed); unbind = `clear_all_transient_observers(dom)` — three spec-distinct clears, all via `retain_observations` (One-issue-one-way). `add_transient_observers` is now a registry method (it allocates `reg_id`s) and walks permanent **and** transient ancestor entries (spec step-15 chained transients). **This is the final source model — fully closes `#11-mutation-transient-source-fidelity` (the R3 entity-source intermediate in delta 9 is obsolete).**
12. **R4-H2 (P2): `pending` delivered callbacks in observer-id order** (`BTreeSet`); §4.3 iterates the pending **ordered set** in append order. Made `pending` an append-ordered `Vec` (dedup on insert) and notify's §4.3.2 `interestedObservers` an insertion-ordered Vec (walk order) — spec append order end-to-end (sibling-sweep). Preserves the B1/#213 determinism goal with the spec-correct order.
13. **R5-I1 (P2): this as-built section still described the R3 entity-source model** → corrected (deltas 9/11 now point at the reg_id final model). **R5-I2** = the recurring test-split MIN (delta 10, `task_b3cd18da`).
