# B1.2a — Move-record childList delivery for `MutationObserver` (plan-memo)

Plan date: 2026-06-21 JST
Status: **PLAN / DESIGN — pre-implementation. No `.rs` change in this PR-of-record yet.**
Program: Program B (F3) → B1 follow-on. Parent: `docs/plans/2026-06-b1-mutationobserver-plan.md` (B1, merged
PR #379 `8cb96457`). B1.2 = the follow-on family the B1 plan §5 "Follow-on slices" table names (direct tree
ops + coalescing + Range + move-record + transient observers); **B1.2a is its move-record slice**, decomposed
into its own terminal slice here (the a/b/c/d split is this memo's refinement of that table, gated per-slice by
`/elidex-plan-review` — the operative edge-dense discipline, no separate umbrella memo of record).
Gate: edge-dense (move = 2-record × sibling-timing × same/cross-parent × shadow-delivery) → this memo must pass
`/elidex-plan-review` **before** any implementation commit (CLAUDE.md "Edge-dense work = multi-PR + 実装前
plan-review 必須").

> **Terminology.** "move" in this memo = the **legacy non-atomic reinsertion** path — `appendChild`/
> `insertBefore`/`replaceChild` applied to an already-parented node, which runs adopt→remove→insert (DOM §4.5
> step 2 + §4.2.3). It is **NOT** the spec's atomic "move a node" algorithm (`#concept-node-move`, used by
> `moveBefore()`), which deliberately does *not* remove-then-insert and queues different records. `moveBefore`
> atomic-move is out of scope (its own future slice).

> **What B1.2a builds.** B1 covers `MutationObserver` childList records for **fresh single-node** inserts and
> **defers moves** (`record_deferred_to_b1_2`, `element/tree.rs:29`) — a node that already has a parent is
> applied but its record withheld. B1.2a **completes the move case**: `appendChild`/`insertBefore`/
> `replaceChild` of an already-parented node emits the **two** WHATWG DOM records a move owes — a
> **source-parent removal** record (from `adopt`) + a **destination-parent insertion** record — with correct
> pre/post siblings, instead of dropping them. It reuses B1's mechanism verbatim (`queue_mutation_record` +
> §4.3 microtask + `MutationObserverRegistry`); only the record **production** at the three insert handlers
> changes.

> **Explicitly NOT in B1.2a (separate slices).**
> - **DocumentFragment expansion** — `EcsDom::append_child` (`tree/mutation.rs:22`) does **not** expand a
>   fragment: it would link the fragment *entity* as a child (an **apply-layer** bug, latent because B1 defers
>   the fragment record). Fixing it needs an `elidex-ecs` apply change (expand the fragment's children) +
>   multi-node record — a different layer/scope. **Stays deferred** (the B1 guard keeps emitting no malformed
>   record). → own slice (B1.2-fragment).
> - **direct tree ops** (ParentNode/ChildNode/insertAdjacent/select) → B1.2b. **replace-all coalescing** →
>   B1.2c. **Range / transient observers** → B1.2d. **CE-reaction timing across moves** = Mechanism A
>   (`ConsumerDispatcher`), untouched by B1.2a (MO-record-only).
>
> These five (B1.2-fragment / B1.2b / B1.2c / B1.2d / Mechanism A) are **sibling implementation slices of the
> active B1.2 umbrella program** (refining the B1 plan §5 "Follow-on slices" table), **not** open-ended
> `#11-*` defer slots — so the defer-ledger 3-element form (Why / Re-evaluation trigger / date) is N/A; each
> is a tracked next-PR slice under the umbrella, each with its own `/elidex-plan-review`.

> **Runtime note.** As with B1, the VM MutationObserver path is production-dormant (the shell runs boa) until
> S5; B1.2a is validated by VM tests.

---

## §A. Spec coverage map (preflight hard-gate)

All anchors webref-verified (see §8). B1.2a realizes the **move** path of the insert/remove tree algorithms.

| Spec section | Step | Branch | Touch (site) | Full enum? | User-input flow |
|---|---|---|---|---|---|
| DOM §4.2.3 "insert" (`#concept-node-insert`) | step 7.1 `adopt` (move) + step 8 dest record | already-parented node → 2 records | `elidex-dom-api/element/tree.rs` AppendChild/InsertBefore + `apply_*` | ✓ (move branch) | no |
| DOM §4.5 "adopt" (`#concept-node-adopt`) | step 2 "remove node" (source removal, NOT suppressed) | source-parent removal record | `apply_*` move path (new) | ✓ | no |
| DOM §4.2.3 "remove" (`#concept-node-remove`) | step 16 queue removal record (suppressObservers=false) | the removal record shape | `apply_*` move path | ✓ | no |
| DOM §4.2.3 "replace" (`#concept-node-replace`) | steps 7–9 (ref/prev capture) + 11.2 remove(old, suppress=true) + 13 insert(adopt, suppress NOT threaded) + 14 record | replaceChild w/ already-parented newChild = move of newChild → 2 records (source-removal + coalesced) | ReplaceChild handler + `apply_replace_child` | ✓ — §2.4 enumerates source-removal (NEW), coalesced prev=step9 (may==newChild), next=step7+step-8 adjust | no |
| DOM §4.3.2 "queue a (tree) mutation record" (`#queue-a-tree-mutation-record`) | record shape per target | source(removed) + dest(added) | `queue_mutation_record` (reused) | ✓ | no |

**Breadth**: K=1 spec (DOM); the observable surface = the move branch of appendChild/insertBefore (+ the
newChild-move half of replaceChild). Single-PR scope = correct (one coherent terminal slice under the B1.2
umbrella, edge-dense base-case).

**User-input flow**: none new. Records are engine-built from already-stored DOM state.

---

## §0. Decisions this memo commits to (lens-collapsed; §7 for the gate to falsify)

1. **A move emits TWO records, not one (§2).** Per spec: `appendChild`/`insertBefore` of a parented node runs
   `adopt` (§4.5 step 2 "remove node", default `suppressObservers=false`) → §4.2.3 remove step 16 queues a
   **removal record on the source parent**, then insert step 8 queues the **insertion record on the
   destination parent**. Same-parent reorder included (adopt removes from the same parent first). This is the
   spec-correct shape Codex PR379 R2/R3 pointed at; B1's single-dest-record reading was rejected as wrong, and
   "emit nothing" (B1's deferral) is now replaced by the correct two-record emission.

2. **Siblings captured at the right instants (§2.2).** Source-removal record's `previousSibling`/`nextSibling`
   = the node's siblings in the **old** parent, captured **before** the move. Destination-insertion record's
   siblings = the node's siblings in the **new** parent, captured **after** the relink (this also fixes B1's
   R1 degenerate `previousSibling==self` case by construction — post-apply the node is never its own sibling).

3. **Records produced at the engine-independent algorithm seam; `vm/host/` stays marshalling-only (Layering).**
   The two records are built in `elidex-script-session::apply_*` (or a move-aware builder) and pushed onto
   `session.notify_records`; the existing `dom_bridge` drain + `queue_mutation_record` deliver them unchanged.
   No `vm/host/` algorithm.

4. **Reuse B1's delivery mechanism verbatim.** `queue_mutation_record` already handles N independent records
   targeting different parents (each does its own §4.3.2 ancestor walk via `registry.notify`). No delivery,
   microtask, or registry change. Only the producer (the `record_deferred_to_b1_2` guard → move-record
   emission) changes.

5. **Fragment + non-move scope unchanged.** Fresh single-node inserts keep B1's behavior. Fragments stay
   deferred (apply-layer fix is a separate slice — scope note above). The `record_deferred_to_b1_2` guard is
   **narrowed** from "move OR fragment → defer" to "fragment → defer". After narrowing, the guard is
   **orthogonal to move dispatch** — it no longer carries move-ness; the move/fresh decision is the builder's
   `old_parent.is_some()` (§2.3), not this guard.

---

## §1. Verified anchors (re-grepped at HEAD `8cb96457`, 2026-06-21)

| Symbol / site | Verified location | Note |
|---|---|---|
| `record_deferred_to_b1_2` (guard to narrow) | `crates/dom/elidex-dom-api/src/element/tree.rs:29` (callers `:61`/`:110`/`:259`) | currently defers move **and** fragment; B1.2a flips move → emit |
| `apply_append_child` / `apply_insert_before` / `apply_replace_child` | `crates/script/elidex-script-session/src/mutation/mod.rs` (`pub`, B1) | build the dest record; extend to also build the source-removal record on a move |
| `EcsDom::append_child` | `crates/core/elidex-ecs/src/dom/tree/mutation.rs:22` | move path = `detach_with_hook(child, parent)` (`:40`) then relink — apply already correct for moves; **fragment NOT expanded** (scope note) |
| `EcsDom::get_parent` / `prev_exposed_sibling` / `next_exposed_sibling` | `elidex-ecs` (used by B1 `apply_*`) | for old-parent + old-sibling capture (source record) |
| `SessionCore::push_notify_record` / `take_notify_records` | `elidex-script-session/src/session.rs` (B1) | push BOTH records (source then dest) |
| `VmInner::queue_mutation_record` + `NotifyMutationObservers` microtask | `elidex-js` (B1) | unchanged — drains each record independently |
| `MutationObserverRegistry::notify` | `crates/api/elidex-api-observers/src/mutation.rs:206` | unchanged — per-record ancestor walk handles different targets + shadow gating |

Re-grep all at PR-open (line numbers drift).

---

## §2. Design — the two-record move

### 2.1 Order + targets (§4.5 + §4.2.3)
A move (`node` already parented) = remove-from-source **then** insert-into-dest:
1. **Source-removal record** — `{type: childList, target: oldParent, removedNodes:[node], previousSibling:
   <node's old prev>, nextSibling: <node's old next>}` (adopt §4.5 step 2 → remove §4.2.3 step 16).
2. **Destination-insertion record** — `{type: childList, target: newParent, addedNodes:[node],
   previousSibling/nextSibling: <node's NEW siblings>}` (insert §4.2.3 step 8).
Pushed in that order onto `session.notify_records`; delivered in order by the existing microtask.
Same-parent reorder: `oldParent == newParent` but still two records (removal then insertion) — spec-faithful.

### 2.2 Sibling capture timing — **node-relative, dest = post-write (uniform for fresh+move)**

The move-aware builder, given `(parent, node)`:
1. **Before** the EcsDom write — *move source context only* (the write destroys it): `old_parent =
   get_parent(node)` (this `is_some()` is also the **move-vs-fresh SoT** — see §2.3); on a move also
   `old_prev = prev_exposed_sibling(node)`, `old_next = next_exposed_sibling(node)` for the source-removal
   record.
2. Apply via `EcsDom::append_child` / `insert_before` (`detach_with_hook` + relink).
3. **After** the write — *dest siblings, captured **node-relative**: `prev_exposed_sibling(node)` /
   `next_exposed_sibling(node)`*. This is correct for **both** fresh and move (append → prev = new
   last-but-one, next = None; insertBefore → prev = node's new prev, next = refChild) and fixes B1's R1
   degenerate `previousSibling == node` by construction (post-apply the node is never its own sibling).
4. Build + return the record list (1 fresh / 2 move).

⚠ **Capture-shape change from the existing helpers (the refactor is NOT a return-type widening).** The
current `apply_append_child` captures the dest prev **pre-write, parent-relative**
(`children_iter_rev(parent).next()`, `mutation/mod.rs:230`) and `apply_insert_before` captures it **pre-write,
ref-relative** (`prev_exposed_sibling(ref_child)`, `:254`). For a **fresh** node these pre-write captures
coincide with the post-write node-relative value (the node is not yet in the tree), which is why B1 was
correct; for a **move** they snapshot the *stale* pre-detach chain (→ self-sibling). The builder therefore
**removes the pre-write dest capture entirely** and uses the uniform post-write node-relative capture above —
One-issue-one-way (one capture rule for both cases), not a parallel move-only branch.

### 2.3 Builder shape + move-vs-fresh dispatch signal
`apply_append_child` / `apply_insert_before` currently return `Option<MutationRecord>` (one). Chosen shape:
**extend the existing `apply_*` to return a record list** (`SmallVec<[MutationRecord; 2]>` / `Vec`; one record
source, fresh = 1-elem, move = 2-elem) — One-issue-one-way, the flush path (`apply_mutation`) and the boa-flush
path inherit correct move records for free. (Rejected: a dedicated `apply_move_*` — it forks the producer into
two sites, the decision-tax shape CLAUDE.md warns against.)

**Move-vs-fresh dispatch is decided *inside the builder*, by `old_parent = get_parent(node)` captured
pre-write (§2.2 step 1): `Some` → move (2 records), `None` → fresh (1 record).** This is the move-ness SoT.
The handler does **not** dispatch on move-ness — after §4 narrows `record_deferred_to_b1_2` to **fragment-only**
(§0.5 decision 5), that guard reports fragment-ness, not move-ness, and is orthogonal to the move/fresh split.
The handler's only job is: if the (fragment-only) guard says defer, withhold; otherwise push the whole
returned list. (A prior draft said "the handler computes `is_move` from the narrowed guard" — that was wrong:
the narrowed guard is `false` for every move, so branching the handler on it would silently drop all move
records. Corrected here.)

### 2.4 replaceChild — full record set (resolves §7 Q2, derived from `#concept-node-replace`)

**replace does NOT use §2.2's append/insertBefore capture rule** for its coalesced record — replace has its own
spec capture discipline (steps 7–9, all read **pre-removal, oldChild-relative**). §2.2's uniform post-write
node-relative rule applies to `apply_append_child` / `apply_insert_before` only.

For `replaceChild(newChild, oldChild)` with **`newChild` already parented** (a move into the replace slot),
B1.2a emits **two** records (B1 deferred the whole replace record for any move; B1.2a un-defers it):

1. **Source-removal record** on `newChild`'s OLD parent — **fires iff `newChild`'s parent is non-null AND
   `newChild != oldChild`**. The `!= oldChild` exclusion is the **self-replace** degenerate (`replaceChild(X,X)`):
   step 11 removes X first, so at step 13's adopt X's parent is already null → adopt's remove is a no-op → **no
   source-removal record** (only the single coalesced `{added:[X],removed:[X]}` record fires). The move SoT for
   the replace path is therefore `get_parent(new_child).is_some() && new_child != old_child` — NOT the bare
   `is_some()` that §2.3 uses for append/insertBefore (which have no oldChild). Step 13 inserts `newChild` with
   `suppressObservers=true`, but the insert's internal **adopt** (`#concept-node-insert` step 7.1) calls "remove
   node" on `newChild` from its old parent with the **default** `suppressObservers=false` (suppression is NOT
   threaded into adopt's remove) → a removal record fires on `newChild`'s old parent. Shape: `{removed:[newChild],
   previousSibling/nextSibling = newChild's OLD exposed siblings}`, captured **pre-write** (the forthcoming
   `dom.replace_child` → `detach_with_hook(new_child, parent)` at `tree/mutation.rs:164` destroys this — so
   `apply_replace_child` must read `get_parent(new_child)` + `prev/next_exposed_sibling(new_child)` **before** it,
   exactly as §2.2 step 1 does for append/insert). **This record is NEW in B1.2a** — `apply_replace_child`
   currently captures nothing about `new_child`'s old position.
2. **Coalesced replace record** on `parent` (step 14): `{added:[newChild], removed:[oldChild],
   previousSibling, nextSibling = referenceChild}`. Capture per spec, **pre-removal, oldChild-relative**:
   - `previousSibling` = step 9 = oldChild's previous sibling. **No step-9 adjustment exists** → this MAY legitimately
     equal `newChild` (e.g. `[A,B,C].replaceChild(A,B)` → `previousSibling = A = newChild`). Spec-faithful — the
     existing `prev_exposed_sibling(old_child)` pre-write capture is **correct; do NOT "fix" it** to post-write.
   - `nextSibling` = `referenceChild` = step 7 (oldChild's next sibling) **with the step-8 adjustment: if that next
     sibling IS `newChild` (newChild was oldChild's next sibling, e.g. `[A,B,C].replaceChild(C,B)` → next(B)=C=newChild),
     set referenceChild to `newChild`'s next sibling instead.** Step 8 fires only on a **move** (a fresh newChild can't be
     oldChild's sibling), so it was unobservable in B1 (move-replace deferred) and **first becomes live in B1.2a**.
     The existing `next_exposed_sibling(old_child)` capture lacks this adjustment → B1.2a must add it.

**Same-parent move** (`newChild`'s old parent == `parent`): both records target `parent`; order = source-removal
(from step 13's adopt) **then** coalesced (step 14 queues after). Two independent §4.3.2 records, same target — no
coalescing between them.

⚠ **Same-parent source-removal siblings must exclude `oldChild` (spec step ordering vs. EcsDom primitive
ordering).** Per spec, `oldChild` is removed at **step 11**, *before* `newChild`'s adopt at **step 13** — so at
adopt time `newChild`'s siblings no longer include `oldChild`. Example `[A,B,C].replaceChild(C,B)`: step 11
removes B → tree `[A,C]`; step 13's adopt-removal of C sees `previousSibling = A` (NOT B). But the EcsDom
primitive `replace_child` detaches `new_child` at `tree/mutation.rs:164` **before** detaching `old_child` at
`:186` (reverse order), so a naive pre-write `prev_exposed_sibling(new_child)` would capture **B**. Therefore the
source-removal record's siblings = `newChild`'s exposed prev/next **stepping over `oldChild`** (if a captured
sibling == `oldChild`, use `oldChild`'s sibling on that side). For a **cross-parent** move `oldChild` is not in
`newChild`'s parent, so the exclusion is a no-op — uniform rule, correct for both.

When `newChild` is **fresh** (not parented), B1 behavior is unchanged: one coalesced record, step-8 never fires,
no source record.

### 2.5 Component data-flow (read / write balance)

Every read the new code performs and the relink write-path that populates it — Sub-check 2b ledger:

| Read | When | Anchor | Populated by |
|---|---|---|---|
| `get_parent(node)` (move SoT + source target) | pre-write | node | existing `Relations` parent component (relink-maintained) |
| `prev_exposed_sibling(node)` / `next_exposed_sibling(node)` (append/insert source-removal siblings) | pre-write | node | existing exposed-sibling helpers (skip `ShadowRoot` per §4.8) |
| `prev_exposed_sibling(node)` / `next_exposed_sibling(node)` (append/insert dest siblings) | **post-write** | node | same helpers, read after `link_node` + `rev_version` complete |
| `get_parent(new_child)` (replace move SoT) — record (1) fires iff `is_some() && new_child != old_child` (self-replace exclusion, §2.4-1) | **pre-write** | new_child | existing `Relations` parent component |
| `prev/next_exposed_sibling(new_child)` **stepping over `old_child`** (replace source-removal siblings, §2.4-1) | **pre-write** | new_child | same; MUST precede `dom.replace_child` (`detach_with_hook` at `tree/mutation.rs:164`); old_child-exclusion = spec step-11-before-13 ordering |
| `prev_exposed_sibling(old_child)` / `next_exposed_sibling(old_child)` (replace coalesced record, §2.4-2; +step-8 adjust on next) | pre-write | old_child | existing (B1) — spec steps 7–9 read pre-removal oldChild-relative |

All reads consume pre-existing EcsDom relink state — **no new write-path** is introduced (the apply primitives
`EcsDom::append_child` / `insert_before` / `replace_child` already do `detach_with_hook` + `link_node` +
`rev_version`). B1.2a changes only *when* / *relative to what* siblings are read (§2.2 append/insert) and adds the
replace source-removal capture + step-8 referenceChild adjustment (§2.4).

---

## §3. §4.2/§4.3 named invariants — disposition

| Invariant | B1.2a handling |
|---|---|
| two-record move (source removal + dest insertion) | §2.1 — the core of B1.2a |
| sibling correctness (incl. B1 R1 degenerate prev=self) | §2.2 — post-apply node-relative dest siblings fix it by construction (append/insert) |
| replaceChild move record set (source-removal + coalesced; step-8 referenceChild adjust) | §2.4 — derived from `#concept-node-replace`; replace keeps its own pre-removal oldChild-relative capture |
| same-parent reorder | §2.1 — two records on the same parent |
| shadow-root delivery gating | reuse `registry.notify` ancestor walk + exposed-sibling helpers (B1) — unchanged |
| CE-reaction timing across moves | **out of scope** — Mechanism A (`ConsumerDispatcher`), untouched |
| DocumentFragment expansion | **out of scope** — apply-layer fix, separate slice; stays deferred |
| no double-delivery | each record independently `queue_mutation_record`'d; no overlap with B1's fresh path |

---

## §4. File-level change plan (post-review)

1. `crates/script/elidex-script-session/src/mutation/mod.rs`:
   - `apply_append_child` / `apply_insert_before` return a record **list**; dispatch on `old_parent =
     get_parent(node)` pre-write (§2.3); dest siblings captured **post-write node-relative** (§2.2); on a move
     emit the source-removal record before the dest-insertion record. `apply_mutation` + boa-flush path inherit.
   - `apply_replace_child` (§2.4): also return a list; on a moved `newChild` capture `get_parent(new_child)` +
     `new_child`'s old siblings **pre-write** (before `dom.replace_child`) and emit the NEW source-removal record;
     add the **step-8 referenceChild adjustment** to the coalesced record's `nextSibling` (if
     `next_exposed_sibling(old_child) == new_child` → use `new_child`'s next). Keep the coalesced record's
     `previousSibling` (step 9) pre-removal oldChild-relative capture **unchanged** (may == newChild, spec-faithful).
2. `crates/dom/elidex-dom-api/src/element/tree.rs` — narrow `record_deferred_to_b1_2` to **fragment-only**;
   the three handlers push the returned record list (1 fresh / 2 move) onto `session.notify_records`.
3. **Tests** — `tests_mutation_observer/integration.rs`: same-parent reorder → 2 records (removal on parent +
   insertion on parent, correct siblings); cross-parent move → removal on source-observer + insertion on
   dest-observer; `insertBefore` move; **replaceChild move** = source-removal + coalesced record, incl. the
   step-8 case (`[A,B,C].replaceChild(C,B)` → coalesced `nextSibling` = C's next, not C) and the
   `previousSibling==newChild` spec-faithful case (`[A,B,C].replaceChild(A,B)`); the B1
   `real_same_parent_move_defers_record_no_malformed_delivery` test is **replaced** by a move-emits-correct-records
   test; fragment still-deferred test stays.

Files NOT touched: `elidex-ecs` primitives (apply already correct for moves), `queue_mutation_record` /
microtask / registry (B1, unchanged), `elidex-js-boa`.

---

## §5. Testing / AC
1. Same-parent reorder (`parent.appendChild(parent.firstChild)`): exactly 2 childList records (removal then
   insertion) on `parent`, siblings correct, no `previousSibling==node`.
2. Cross-parent move: source observer gets removal record (target=oldParent, removedNodes=[node]); dest
   observer gets insertion record (target=newParent, addedNodes=[node]).
3. `insertBefore` move: insertion record `nextSibling==refChild`.
4. **replaceChild move**: (a) cross-parent — source-removal on newChild's old parent + coalesced record on
   parent; (b) `[A,B,C].replaceChild(C,B)` (same-parent, newChild adjacent-after) — coalesced `nextSibling` =
   C's original next (step-8 adjust), NOT C itself, **and** the source-removal record's `previousSibling` = A
   (steps over the removed B), NOT B; (c) `[A,B,C].replaceChild(A,B)` (same-parent, newChild adjacent-before) —
   coalesced `previousSibling == A == newChild` (spec-faithful, no step-9 adjust); (d) **self-replace**
   `replaceChild(X,X)` — exactly ONE coalesced record `{added:[X],removed:[X]}`, NO source-removal record (the
   `!= oldChild` gate); fresh-newChild replace unchanged (1 record).
5. Fresh single-node insert: B1 behavior unchanged (1 record) — regression guard.
6. Fragment: still deferred (no malformed record) — unchanged.
7. Delivery on microtask, order source-before-dest. `mise run ci` green.

---

## §6. Collision / sequencing
- Builds on merged B1 (`8cb96457`). A1 (#376) is **OPEN** (paused at external-converge R6) but touches **none**
  of B1.2a's files (verified vs A1's merge-base) — no collision; same for the media slices. B1.2a touches only
  `mutation/mod.rs` + `element/tree.rs` + MO tests. Re-confirm at PR-open.
- Worktree-isolated (`b1.2a-move-fragment-records`). main-direct commit forbidden.

## §7. Open questions (for the 5-agent gate)
1. Builder shape (§2.3): record-list return on `apply_*` vs dedicated `apply_move_*`? (lens: record-list.)
2. ~~replaceChild-with-moved-newChild exact record set~~ — **RESOLVED in §2.4** (derived from
   `#concept-node-replace`): 2 records = source-removal on newChild's old parent (adopt, suppress NOT threaded) +
   coalesced step-14 record; previousSibling = step 9 (may == newChild, spec-faithful); nextSibling = step 7 + the
   **step-8 adjustment** (referenceChild==newChild → newChild's next), which first goes live in B1.2a.
3. Does any observer-walk subtlety arise when source==dest parent (two records, same target, same microtask)?
   (Expected fine — two independent §4.3.2 records.)
4. Same-parent reorder: confirm `detach_with_hook` + relink yields the spec's removal-then-insertion (not a
   no-op) for `appendChild(lastChild)` (degenerate) — may legitimately be a no-op move (0 records) if the node
   is already last; derive at impl.

## §8. Citation appendix (webref-verified 2026-06-21, carried from B1)
| Concept | Source | Anchor |
|---|---|---|
| insert (adopt at 7.1, record at 8) | DOM §4.2.3 | `#concept-node-insert` |
| adopt (step 2 remove node) | DOM §4.5 | `#concept-node-adopt` |
| remove (step 16 queue record, not suppressed) | DOM §4.2.3 | `#concept-node-remove` |
| replace (step 11.2 / 14) | DOM §4.2.3 | `#concept-node-replace` |
| queue a tree mutation record | DOM §4.3.2 | `#queue-a-tree-mutation-record` |
