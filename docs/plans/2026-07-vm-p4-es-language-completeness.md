# VM P4 — ES language completeness: discharging the stubbed-emit inventory

**Status**: umbrella plan (multi-PR program). Per-slice `/elidex-plan-review` mandatory.
**Baseline**: `f7d9b5ce` (= `origin/main` at authoring time, commit date 2026-07-26).
**Slots**: `project_open-defer-slots.md` §"VM P4 ES-language + builtin gaps"; **plus** the two
pre-existing slots this umbrella adopts (§8).
**Corrects**: `project_vm-p4-es-language-gaps.md` §2/§4 (see §1.1).
**Revision**: R2 — restructured after `/elidex-plan-review` round 1 (2 CRIT / 11 IMP), which
established that the defect class sits in the **compiler emit layer**, not the dispatch layer.

---

## §0.5 Spec citation table

All rows verified via `.claude/tools/webref` against `ecma262` — the original 25 on 2026-07-26,
[C43] on 2026-08-03.

⚠ **This table is not the document's citation chokepoint, and reading it as one is how two
fabricated anchors survived under a blanket header before ([§10](2026-07-vm-p4-umbrella-review-rounds.md)).** It covers the ECMA-262 sections
carrying an ID; the document also cites ECMA-262 sections inline without IDs, and cites **WebIDL**
and **WHATWG HTML** — specs this header has never
covered. Verify those at their use sites with `webref heading webidl <n>` / `webref dfn html <t>`.
⚠ **And a *planned* function has no use site yet, which is the half of that obligation nothing in
this document owned.** Measured over this table's rows rather than over the file — the rows are the
lines this table starts, and the read is pipe-free for the reason §5's census is:
`python3 -c "import sys;C=chr(124);R=[l for l in open(sys.argv[1]) if l.startswith(C+' [C')];print(len(R),*[sum(t in l for l in R) for t in ('Reflect','Promise','RegExp','asyncIterator','AsyncIterator','String.raw')])" <this file>`
prints `29 0 0 0 0 0 1` at this revision — 29 rows, none of them naming `Reflect`, `Promise`,
`RegExp` or either spelling of the async-iterator symbol, against the control `String.raw` → **1**,
so the read discriminates rather than returning zero for everything — while §5 assigns the
thirteen `Reflect` statics, the `Promise` combinators, the `RegExp` prototype members and the
async-iterator methods to children **no derivation has minted yet**. A terminal child can therefore
implement its assigned function and cite nothing, and the umbrella's inline evidence for the
governing clause sits somewhere else entirely. The use-site rule above is **extended, not
duplicated**: where the function does not exist yet, the use site is the child's own plan-memo, and
**the derivation that mints a child records there, per function or handler it assigns to that child,
the verified section and the `webref` invocation that returned it** — a mandatory output of the
derivation alongside the obligations, edges and acceptance conditions §5 already places on it, read
at that child's own mandatory `/elidex-plan-review`. Expanding *this* table instead would put the
contract in a header whose first sentence says it is not the chokepoint, and would restate at the
umbrella a rule the child is the one able to discharge. No row and no derivation restates this
rule.

| ID | Citation | Anchor | Used by |
|---|---|---|---|
| [C19] | ECMA-262 §13.3.8.1 Runtime Semantics: ArgumentListEvaluation | `#sec-runtime-semantics-argumentlistevaluation` | the child umbrella **1b**'s derivation mints for the argument-emit helper · the child of umbrella 4a that owns the tagged-call lowering |
| [C20] | ECMA-262 §13.3.6.2 EvaluateCall | `#sec-evaluatecall` | the child umbrella **1b**'s derivation mints for the argument-emit helper · the child of umbrella 4a that owns the tagged-call lowering |
| [C33] | ECMA-262 §13.3.5.1.1 EvaluateNew | `#sec-evaluatenew` | the child umbrella **1b**'s derivation mints for the argument-emit helper |
| [C34] | ECMA-262 §7.3.14 Construct | `#sec-construct` | the child umbrella **1b**'s derivation mints for the argument-emit helper |
| [C35] | ECMA-262 §7.4.10 IteratorStepValue | `#sec-iteratorstepvalue` | the child of umbrella 1a that removes `op_array_spread`'s `return()`, via that umbrella's derivation, which mints it |
| [C45] | ECMA-262 §13.15.5.5 Runtime Semantics: IteratorDestructuringAssignmentEvaluation | `#sec-runtime-semantics-iteratordestructuringassignmentevaluation` | the child of umbrella 0bc that owns §13.15.5.5 IteratorDestructuringAssignmentEvaluation (the `AssignmentRestElement` no-close rule — step 4 repeats only while `[[Done]] is false`) |
| [C36] | ECMA-262 §7.4.11 IteratorClose | `#sec-iteratorclose` | the child of umbrella 0bc that owns the [C39] `IteratorClose` conformance / the child of umbrella 1a that removes `op_array_spread`'s `return()` (dec. 13a), via that umbrella's derivation, which mints it / `#11-vm-iteratorclose-precedence-convention` (dec. 13b) — **not** the argument-emit helper the child umbrella **1b**'s derivation mints (§2.5 C×F) |
| [C37] | ECMA-262 §13.3.9.1 Runtime Semantics: Evaluation (optional chain) | `#sec-optional-chaining-evaluation` | the child umbrella **1b**'s derivation mints for the argument-emit helper |
| [C38] | ECMA-262 §13.3.9.2 Runtime Semantics: ChainEvaluation | `#sec-optional-chaining-chain-evaluation` | the child umbrella **1b**'s derivation mints for the argument-emit helper |
| [C11] | ECMA-262 §10.2.2 `[[Construct]] ( argList, newTarget )` | `#sec-ecmascript-function-objects-construct-argumentslist-newtarget` | the child of umbrella 2ab that owns the base-path field-initialisation timing · the child of umbrella 3 that owns both call entry points |
| [C13] | ECMA-262 §13.3.7.1 Runtime Semantics: Evaluation (`super`) | `#sec-super-keyword-runtime-semantics-evaluation` | the child of umbrella 1a that owns the argument-layout and inline-cache contract, via that umbrella's derivation, which mints it (the `op_super_call_spread` handler) + the emit the child umbrella 1b's derivation mints, the child of umbrella 3 that owns the super-property emit shape |
| [C21] | ECMA-262 §7.4.4 GetIterator ( obj, kind ) | `#sec-getiterator` | the child of umbrella 1a that removes `op_array_spread`'s `return()`, via that umbrella's derivation, which mints it, 6b |
| [C22] | ECMA-262 §13.2.4.1 Runtime Semantics: ArrayAccumulation | `#sec-runtime-semantics-arrayaccumulation` | the child of umbrella 1a that removes `op_array_spread`'s `return()`, via that umbrella's derivation, which mints it (the `SpreadElement` drain) + contrast for dec. 3 |
| [C39] | ECMA-262 §13.15.5.2 Runtime Semantics: DestructuringAssignmentEvaluation | `#sec-runtime-semantics-destructuringassignmentevaluation` | the child of umbrella 0bc that owns §13.15.5.2 DestructuringAssignmentEvaluation |
| [C23] | ECMA-262 §13.2.8.4 GetTemplateObject | `#sec-gettemplateobject` | the child of umbrella 4a that owns GetTemplateObject |
| [C40] | ECMA-262 §13.3.11.1 Runtime Semantics: Evaluation (tagged template) | `#sec-tagged-templates-runtime-semantics-evaluation` | the child of umbrella 4a that owns the tagged-call lowering |
| [C44] | ECMA-262 §22.1.2.4 `String.raw ( template, ...substitutions )` | `#sec-string.raw` | Slice 4c |
| [C24] | ECMA-262 §13.3.7.3 MakeSuperPropertyReference | `#sec-makesuperpropertyreference` | the child of umbrella 3 that owns §13.3.7.3 MakeSuperPropertyReference |
| [C25] | ECMA-262 §9.1.1.3.5 GetSuperBase | `#sec-getsuperbase` | the child of umbrella 3 that owns §9.1.1.3.5 GetSuperBase |
| [C26] | ECMA-262 §7.3.32 DefineField | `#sec-definefield` | the child of umbrella 2ab that owns §7.3.32 DefineField · Slice 2ac |
| [C27] | ECMA-262 §7.3.33 InitializeInstanceElements | `#sec-initializeinstanceelements` | the child of umbrella 2ab that owns §7.3.33 InitializeInstanceElements' intra-initialization ordering |
| [C28] | ECMA-262 §15.7.10 ClassFieldDefinitionEvaluation | `#sec-runtime-semantics-classfielddefinitionevaluation` | Slice 2aa |
| [C29] | ECMA-262 §7.3.26 PrivateElementFind | `#sec-privateelementfind` | the child of umbrella 5 that owns Private Name identity + the `GetPrivate`/`SetPrivate`/`PrivateIn` dispatch |
| [C30] | ECMA-262 §7.3.28 PrivateMethodOrAccessorAdd | `#sec-privatemethodoraccessoradd` | the child of umbrella 5 that mints the method/accessor closure |
| [C31] | ECMA-262 §7.3.30 PrivateGet | `#sec-privateget` | the child of umbrella 5 that owns Private Name identity + the `GetPrivate`/`SetPrivate`/`PrivateIn` dispatch |
| [C41] | ECMA-262 §7.3.31 PrivateSet | `#sec-privateset` | the child of umbrella 5 that owns Private Name identity + the `GetPrivate`/`SetPrivate`/`PrivateIn` dispatch |
| [C42] | ECMA-262 §7.3.27 PrivateFieldAdd | `#sec-privatefieldadd` | the child of umbrella 5 that mints the field record |
| [C43] | ECMA-262 §13.15.2 Runtime Semantics: Evaluation (assignment operators) | `#sec-assignment-operators-runtime-semantics-evaluation` | Slice 0a ([§16](2026-07-vm-p4-slice-0a-landing-record.md)) |
| [C32] | ECMA-262 §27.9.3.2 AsyncGeneratorStart | `#sec-asyncgeneratorstart` | umbrella 6a's children |

Existing in-code citations `[C13] §13.3.7.1`, `[C19] §13.3.8.1`, `[C11] §10.2.2`
(`crates/script/elidex-js/src/vm/dispatch_class.rs:6`) re-verified correct — no drift.

---

## §1. Origin and evidence

P4 is the live remaining VM work: [[phase4-plan]]'s **"VM Builtins P4 — 大型 builtins: boa→VM
切替後"** gate opened at the boa deletion (#458 `315ba316`) and was only converted into slots on
2026-07-18 (post-#480).

All behavioural findings are **live-VM probe evidence** — a temporary harness under `vm/tests/`
driving `Vm::eval` at `f7d9b5ce`, run in four rounds, removed afterwards. All structural claims
are grep/read-verified against the same tree.

### §1.0 What this umbrella owns

The unit is the **stubbed-emit class**: a compiler arm that lowers a user-writable production to a
substitute — emitting nothing, an `Op::PushUndefined`, or a wrong constant — so the program runs and
answers wrongly without raising. §2.2's three passes **are** that class's derivation, and that
section's scope statement says, as a property of the commands, what they read.

**§5 sequences the work that discharges that class. A row enters §5 when the derivation produced the
defect.** A conformance defect measured some other way is not thereby unowned, and is never argued
away on that ground: it is recorded where it was measured — §2.2's per-slice-sweep rule is what puts
it there — and it is given an owner of its own, a terminal row, a §8 slot, or the child an umbrella's derivation mints for it, carrying the required
triple. That is already the disposition of the children the derivation of umbrella **M** mints, those the derivation of umbrella **R** mints, those `#11-vm-atomics-global`'s mints and
those `#11-vm-typed-array-family-layering-and-gate`'s mints; until now it was reached once per defect instead of
stated once, which is why §5 grew a row every round while the class it was derived for did not.
`#11-vm-object-spread-source-coercion` and `#11-vm-arrow-lexical-new-target` already have such a
slot, so their §5 rows are rewritten below as pointers to it and nothing they measured is lost.

The document keeps its name. The scope statement above is what bounds the program; a title never
did.

### §1.1 Corrections to the prior memo

The re-probe **contradicted** `project_vm-p4-es-language-gaps.md` §2's scope-precision note and
found five gaps it never listed.

| Prior claim | Re-probe result |
|---|---|
| "`new X(...args)` … **works**" | **FALSE — broken.** `new C(...[1,2])` → `a=[1,2]`, `b=undefined`; `new C(...[1,2,3]).n` → `arguments.length === 1` |
| (not listed) | **`super.x` / `super.m()` / `super[k]` / `super.x=` all throw** `TypeError: Cannot convert undefined or null to object` — total loss of super-property access |
| (not listed) | **public class fields `class A{x=1}` → `undefined`** (silent). `static x=1` *does* work |
| (not listed) | **`obj[k] += v` PANICS the process** (`assert!`, `compiler/expr_assign.rs:170`) |
| (not listed) | **destructuring *assignment* is a silent no-op** at the baseline — see §1.2; 0a made it a scoped throw |
| (not listed) | **async generators non-functional** — `ag().next` is `undefined`; `for await…of ag()` raises an unhandled `TypeError: value is not iterable` |
| `super(...)` grouped with broken | **`super(...args)` works in the directly executed constructor spelling the probe ran** — the one correct spread path, and the reference implementation for the argument-emit helper the child umbrella **1b**'s derivation mints. ⚠ **Narrowed at PR-B** — a `super` spelling reached through an arrow resolves against the arrow's own frame and throws; see §2.2's `parser/arrow.rs:104-119` row |
| "flag accessors missing" | Precise: `.flags` / `.lastIndex` / `.exec` **work**; `.global`/`.ignoreCase`/`.multiline`/`.sticky` and `@@match`/`@@replace` absent |
| `new.target` "probe mis-designed" | Working **in the direct-constructor spelling the probe ran**. ⚠ **Narrowed at PR-B** — lost through an arrow call; see §2.2's `vm/dispatch_class.rs:29-37` row |

### §1.2 ⚠ Retraction — this plan's own round-1 over-claim

Plan revision R1 asserted "Confirmed correct, no action: **all destructuring forms**". That was
generalised from a probe of destructuring **declarations** only. Round-1 review (Axis 3 CRIT)
caught it; the round-1 re-probe read destructuring **assignment** as a **silent no-op**. ⚠ **That reading, and the `Actual` column below, are stale at `658cc302`: all eight rows are now loud.** Re-measured row by row against the pinned tree, every one raises a `TypeError` — `update of this target is not yet supported` for the two `(x)++` / `(a[0])++` spellings, `assignment to this target is not yet supported` for the five plain-assignment spellings, and `this for-in/of assignment target is not yet supported` for the `for ([a,b] of …)` spelling — while the `Spec` column is unchanged and verified against Node row by row. §2.2 already annotates this transition on its own rows; this table never did:

| Spelling | Actual | Spec |
|---|---|---|
| `var x=1; (x)++` | **unchanged `1`** (found by the §2.2 sweep, round 3) | `2` |
| `var x=1; (x)+=1` | **unchanged `1`** (ditto) | `2` |
| `var a=[1]; (a[0])++` | **unchanged `1`** (ditto) | `2` |
| `var a,b; [a,b]=[1,2]` | `a`,`b` both **`undefined`** | `1`, `2` |
| `var x,y; ({x,y}={x:1,y:2})` | both **`undefined`** | `1`, `2` |
| `var a=1,b=2; [a,b]=[b,a]` | **unchanged `1,2`** (the swap idiom silently does nothing) | `2,1` |
| `var o={}; [o.p]=[7]` | `o.p` **`undefined`** | `7` |
| `for ([a,b] of [[1,2]]) {}` | `a`,`b` **unchanged** | `1`, `2` |

Root, re-derived at `658cc302` rather than carried forward with the `Actual` column: the
`AssignTarget::Simple` arm (`compiler/expr_assign.rs:213`) still lowers only `ExprKind::Identifier`
and `ExprKind::Member`, but **every other LHS now reaches a scoped gate** —
`emit_unsupported(fc, "assignment to this target is not yet supported")` at `:266-268`, and
`AssignTarget::Pattern` `emit_unsupported(fc, "destructuring assignment is not yet supported")` at
`:271-280`, both lowering to `Op::ThrowUnsupported` (`:368`). ⚠ **The arms this paragraph described
for several revisions — `_ => { compile_expr(right)?; }` compiling the RHS with no store, and a
`Pattern` arm emitting `Op::Pop` while its comment claimed to "fail explicitly" — do not exist at the
pinned tree: Slice 0a removed them, which is the same transition the sentence above records when it
says all eight rows are now loud.** They are baseline-only evidence. **So what the child umbrella **0bc**'s derivation mints for the assignment-form binding-pattern lowering must replace is
those two `ThrowUnsupported` gates, not a silent fall-through** — a redesign written against the old
arms would re-remove work already landed. (The `Pattern` gate stays unreachable:
`git grep -n 'AssignTarget::Pattern' 658cc302 -- crates/script/elidex-js/src/parser` returns nothing,
rc=1 — all producers use `Simple`.) This is registered as **Slice 0bc** (§5).

Also newly confirmed silent-wrong, previously mis-tiered or unlisted:
- **`import('x')` → `undefined`** (not a Promise). `compiler/expr.rs:200` compiles
  `ExprKind::DynamicImport` to `PushUndefined`, and the parser applies **no module gate** to
  `import(...)` (`parser/primary.rs:382`) — so this is reachable in ordinary script context. **T1,
  not T3.** (`import.meta` *is* correctly module-gated at `parser/primary.rs:390`.)
- **`{1n: 'x'}` → key is the empty string** `{"":"x"}` (`compiler/expr_object.rs:117-121`
  "conservative fallback").
- **`obj.#x++` silently keeps the old value** (`compiler/expr_ops.rs:249`).
- ⚠ **RETRACTED at PR-B — "Computed class *accessor* keys hard-error (`compiler/expr_class.rs:588-590`)"
  was false, at `39bbdb1b` and at baseline `f7d9b5ce` alike.** Measured: `let k='p'; class C { get
  [k]() { return 7 } }; (new C).p` → `7`, and `let n=0; class C { get [++n]() { return 7 } }; n` → `1`.
  Computed class members take an earlier path (`expr_class.rs:523`, identical at `f7d9b5ce:523`) and
  never reach the cited guard, which is unreachable dead code — see the corrected §2.2 row. R1's
  "computed class keys correct" needed no narrowing after all.
- **Object-literal accessor keys with a non-Identifier / non-String-literal key are a silent no-op**
  (`compiler/expr_object.rs:26-39`) — the defect the retracted item was pointing at, one construct
  over. `let k='p'; let o={ get [k]() { return 7 } }; o.p` → `undefined`, and the key expression never
  runs: `let n=0; ({ get [++n]() {} }); n` → `0`. Numeric literal keys too: `({ get 1(){ return 7 } })`
  defines nothing. See §2.2.
- **Object-pattern destructuring with a non-Identifier / non-String-literal / non-computed key is a
  silent no-op** (`compiler/stmt_destructure.rs:117-119` at baseline, `:118-120` at head):
  `const {1: a} = {1: 'x'}; a` → `undefined`. See §2.2.

Newly confirmed absent: `Array.prototype.at` / `String.prototype.at` / `findLast` /
`findLastIndex` / `toSorted` / `Object.hasOwn` / `Object.groupBy` / `String.matchAll`.
⚠ **This is a probe sample, not the sweep boundary (Codex R5).** `Array.prototype.toReversed`,
`toSpliced` and `with` — ECMA-262 §23.1.3, alongside the `toSorted` listed here — appear nowhere in
the repo or this plan (the **install site** is what is enumerated, never the bare name: `git grep -c '"<name>"' 658cc302 -- crates/script/elidex-js/src/vm/globals.rs` → **0** for each of `toReversed`, `toSpliced`, `with` and `toSorted`, against the positive controls `'"lastIndexOf"'` → **1** and `'"sort"'` → **1** on the same file, so the pattern reaches an installed name where one exists. ⚠ A bare `grep -rl <name>` decides none of this: `with` matches **1209** files under `crates/` and its three `elidex-js` hits are all the `with` *statement* keyword, while the other three names match this plan file itself). Since §7.2 derives its
permanent rows from the *enumerated* absences plus one row per later slot, **the children the Slice-9 umbrella's derivation mints
could satisfy this plan with those still missing**. Which sub-slice each name falls to follows from
the sub-slice charters in §5.
Confirmed **correct**, no action: destructuring **declarations** (incl. nested/default/rest, and
parameter destructuring) — ⚠ **narrowed at PR-B to Identifier, string-literal and computed keys**;
other key kinds are a silent no-op (see the retraction bullets above and §2.2) — **and narrowed
again here on a second axis: the entry holds only for a pattern that drains its source.** An array
binding pattern that stops early pops its iterator instead of closing it, so `const [x] = it` never
calls `it.return()`, and the arm is shared by declarations, parameter patterns and for-in/of heads
alike (§2.2's `stmt_destructure.rs:36-81` row) —
`IncElem`/`DecElem` (`a[0]++`), `await` microtask ordering, static
fields/methods — ⚠ **narrowed at PR-B to the *definition*, which is all the probe `static x = 1`
exercises**; the initializer runs with the enclosing function's `this`, so
`class C { static x = this }` gives `C.x === C` → `false` (§2.2's `expr_class.rs:402-427` row) —
computed **class** keys (methods *and* accessors — the retraction above),
object-literal accessors with **Identifier or string-literal** keys only (⚠ likewise narrowed at
PR-B), `flatMap`, `Promise.prototype.finally`. The class and object-literal accessor paths differ in
**failure mode**, not only in coverage: a key kind neither lowers is a `CompileError` in a class
(measured: `class C { get 1n() { return 7 } }`) and a silent `Op::Pop` in an object literal.

The static-field narrowing and §1.1's `new.target` and `super(...args)` narrowings share the shape
this section's absence list already carries: **a probe that exercises one spelling cannot close a
surface.** `static x = 1` cannot distinguish a correct field receiver from an incorrect one, `new F()`
read from `F`'s own body cannot distinguish a frame-local `new.target` from a lexical one, and
`super(...args)` written directly in a constructor cannot distinguish a frame-local `super` from a
lexical one.

---

## §2. Root-cause thesis + coupled-invariant enumeration

### §2.1 Thesis (R2 — corrected layer)

These are not N unrelated feature gaps. They are **one defect class**, and round-1 review
established it sits one layer higher than R1 claimed:

> **Unimplemented syntax is compiled to `Op::PushUndefined` (or to a no-op, or to an `assert!`)
> at the compiler emit layer. The dispatch handlers nominally responsible for those features are
> unreachable dead stubs behind them.**

Verified: `Op::TaggedTemplate`, `Op::DefineField`, `Op::GetSuperProp`, `Op::SetSuperProp`,
`Op::GetSuperElem`, `Op::ImportMeta`, `Op::DynamicImport`, `Op::CreateClass`,
`Op::DestructureElem` **all have zero compiler emit sites** (re-derived at `658cc302`, where
`git diff --stat 658cc302 HEAD -- crates/` is empty, with the **comment-stripped** form — a bare
`grep -rn "Op::$op" … | wc -l` counts a mention inside a comment as an emit site and therefore
returns **17**, silently keeping `SetPrivate`. ⚠ **The loop's input is the enum, never a hand-supplied
list** — a placeholder `for op in …` reproduces the very under-enumeration this form exists to prevent,
since an opcode added after the list was written is skipped while the output still reads consistent.
It therefore feeds from **§2.3's extraction, which is the one place variant names are derived**
(CLAUDE.md *One issue, one way*):
`for op in $(git show <rev>:crates/script/elidex-js/src/bytecode/opcode.rs | awk '/^pub enum Op \{/{f=1;next} f&&/^\}/{f=0} f' | grep -oE '^    [A-Z][A-Za-z0-9]*'); do git grep -h "Op::$op" <rev> -- crates/script/elidex-js/src/compiler/ | grep -vE '^[[:space:]]*(//|\*)' | grep -cE "Op::$op([^A-Za-z0-9_]|$)"; done`
→ run whole at `658cc302` it walks **129** variants and returns 0 for **18** of them, the §2.3 set).
So a fix that targets the dispatch stub touches dead code and changes nothing.

Each stub **silently substitutes `undefined` / `false` / a wrong arity** for the spec value. That
is why a whole tier of these was invisible to the defer-slot ledger: a stub throws no error, fails
no test, and registers no TODO.

**P4 = discharging that inventory**, at the emit layer, with connect-or-delete applied to the dead
opcodes behind it. Directly mandated by CLAUDE.md:

- **「TODO 先送り禁止」** — exactly the deferred-implementation pattern the rule forbids; they never
  got the "理由 + 対処時期を明示して確認を取る" step.
- **「dead code は接続するか削除」** — **18** opcodes with documented stack effects and zero emit sites (§2.3).
- **「One issue, one way」** — converge each syntax form onto one emit path.
- **「Ideal over pragmatic」** — no "minimal v1"; each slice ships the spec-complete form.

### §2.2 Stub inventory — Layer A (compiler emit; the live defects)

**Derivation (R2 round 3 — decision 9 resolved empirically, not stipulated).** The table below is
the output of a documented three-pass sweep over `crates/script/elidex-js/src/compiler/`, run
2026-07-26, **not** probe hits plus targeted greps (the method that produced the §1.2 over-claim):

```
# Pass 1 — LEXICAL marker sweep (concept, not one string)
grep -rniE "not yet|not supported|unsupported|conservative|stub|for now|simplified|\
fall(s)? through to push|just evaluate|side effects only|skip \(|would need" *.rs
# Pass 2 — LEXICAL substitution-class enumeration; every hit read and classified
grep -rn "Op::PushUndefined" *.rs        # class 1  → 24 hits. The hit count is the only
                                         # figure this block carries: which hits are defects
                                         # is a read-every-hit classification step, and a
                                         # defect count written here goes stale silently
                                         # (pass 2 misclassified one — see below).
# Pass 3 — STRUCTURAL production coverage (added R2 round 4; passes 1-2 are
#          comment-keyed and cannot see an arm that skips silently *without* a marker)
awk '/^pub enum ExprKind/,/^}/' ../ast.rs | grep -oE '^    [A-Z][A-Za-z]*'   # 30 variants
awk '/^pub enum StmtKind/,/^}/' ../ast.rs | grep -oE '^    [A-Z][A-Za-z]*'   # 24 variants
#   → map every variant to its compiler arm; flag any arm that neither emits an op
#     nor returns CompileError for a user-writable production.
```

**Pass 3 result (run 2026-07-26; the two counts re-derived at `658cc302` by the same two `awk`
commands above, still **30** and **24**).** All **30** `ExprKind` and all **24** `StmtKind` arms
exist in their compilers, so no *variant* is unhandled — the class-1/2/3 defects are in arm
*content*, tabled below. (Not "no production is unhandled": pass 3's input is the variant list, so a
production with no variant is not among the 54 it checks. That is the scope statement below, not a
separate caveat.)
`compile_stmt` groups **6** `StmtKind` variants into one no-op arm (`stmt.rs:31-39`):
`Empty | Error | Debugger | ImportDeclaration | ExportDeclaration | FunctionDeclaration`. Of these
`Empty`/`Error`/`Debugger` are legitimate. ⚠ **`FunctionDeclaration` was classified
"legitimate-and-documented (hoisted at function/script level)"; that holds only at the top level and
the classification is corrected here.** The arm's own comment is true of the two hoist loops that
exist and of nothing else — `compiler/mod.rs:82` iterates `program.body` and
`compiler/expr_function.rs:167` iterates `func.body`, both immediate-child lists, and those are the
only other sites (`for f in $(git ls-tree -r --name-only 658cc302 crates/script/elidex-js/src/compiler/); do git show 658cc302:$f | grep -n 'StmtKind::FunctionDeclaration'; done`
→ `expr_function.rs:167`, `mod.rs:82`, `stmt.rs:37`). `StmtKind::Block` (`stmt.rs:134-144`) only
recurses back into `compile_stmt`, so a block-nested declaration reaches the no-op arm and is never
instantiated — tabled below. **`ImportDeclaration` / `ExportDeclaration` are silent no-ops** —
and both passes 1 and 2 missed them, because the arm emits nothing (pass 2 blind) and its comment
says "stubs", a word not in pass 1's pattern list.

**Reachability**: not live today — `parse_module` (`lib.rs:94`) has **no production caller**
(`grep -rlw 'parse_module' crates/script/elidex-js/src` → **8** files and **55** non-definition sites, every one inside a `#[cfg(test)]` module or a doc comment — `scope/tests.rs`, `parser/{expr_tests_validation,expr_tests_special,function_tests,stmt_tests,module}.rs`, `compiler/mod.rs`; negative control `grep -rlw 'parse_modulez' crates/script/elidex-js/src` → **0**), so the VM never parses as a module. But the **parser and scope
analysis already support modules** (`ProgramKind::Module`, `ScopeKind::Module`, import/export
parsing, `scope/tests.rs` exercises all of it). So this is a **latent trap for Slice M**: the moment
module parsing is enabled, `export const x = 1` and `import {x} from 'm'` compile to **nothing**,
silently, with the front end reporting success. The child of umbrella M that enables `parse_module`
must fix this arm *before* enabling it, not after. Recorded in §5 as a precondition on the child of umbrella M that enables `parse_module`.

**Granularity caveat (honest scope of pass 3)**: this pass is at *variant* granularity. Two round-3
findings live one level deeper — inside an arm, in a sub-`match`/`if` with no else:
`expr_class.rs:430-447` (`ClassMemberKind::PrivateField` compiled only under `if *is_static` ⇒
`class A{#x=1}` emits nothing) and `expr.rs:186-189` (`ExprKind::Spread` in prefix position compiles
its operand; every such node is an early-SyntaxError position per spec). 0ca must run pass 3 at
**both** granularities — variant-level, then sub-arm-level over the enum-shaped inner matches, which
is where every class-2 defect found so far actually lives. **That inner-enum set is derived, not
listed.** AST-side candidate set:
`grep -oE '^pub enum [A-Za-z]+' crates/script/elidex-js/src/ast.rs` → **26** at `658cc302` and at
HEAD `d49465d0` (`git diff 658cc302 HEAD -- crates/script/elidex-js/src/ast.rs` is empty), plus the
compiler-internal enums the same arms match on (`VarLocation`, `compiler/resolve.rs:13`). An enum is
**in scope** when a compiler arm matches on one of its variants. Swept so far: `ClassMemberKind`,
`MemberProp`, `AssignTarget` and `PropertyKey` (four of the 26) plus `VarLocation`. **`PatternKind`
is one of the 26 and was never in that set**, and the destructured-catch-parameter row below is what
that omission hid. ⚠ **`PropertyKey` was swept and the
sweep still missed four of its arms** (tabled below — three rows added at PR-B, one corrected in
place) — because the sub-arm pass is
still *per file*, and `PropertyKey` lowering is open-coded across **3** of them
(`grep -rln 'PropertyKey::' crates/script/elidex-js/src/compiler/` at `39bbdb1b`:
`expr_class.rs`, `expr_object.rs`, `stmt_destructure.rs`) with catch-alls of four different shapes —
`Op::Pop`, `Op::PushUndefined`, `CompileError` and a bare `_ => {}` — and the `CompileError` one is
invisible to any substitution-class grep. Slot
`#11-vm-property-key-lowering-unification` has no derivation and mints no child — its kind is UNDETERMINED (§8), so the collapse has no owner yet; 0ca's sub-arm pass
should be run **per inner enum across all files**, not per file.

Pass 1 returns 31 hits, pass 2's `PushUndefined` arm returns 24; **the large majority are
legitimate** (`yield` with no argument, optional-chain null path, parameter/field defaults, empty
returns, `ExprKind::Error` after a parse error already reported). Classification is therefore a
read-every-arm step, and it is what makes the count trustworthy rather than the grep itself.

**The sweep found 3 defects no prior inventory (probe, R1, R2, or round-2 review) contained** —
parenthesized assignment/update targets, all probe-confirmed silent no-ops **at `f7d9b5ce`** (0a
made all three loud — see §2.2's ⚠):
`(x)++` → x unchanged · `(x)+=1` → x unchanged · `(a[0])++` → unchanged. Root:
`parser/expr.rs:531-536` `is_valid_assign_target` unwraps `ExprKind::Paren` to *validate* but never
normalises it, and neither `expr_ops.rs` nor `expr_assign.rs` unwraps, so a parenthesized target
falls to the catch-all arms. This is direct evidence that decision 9's concern was correct and that
a stipulated inventory would have shipped incomplete.

Emit-site counts grep-verified 2026-07-26 (command in §2.1).

⚠ **This is what the documented derivation found at `f7d9b5ce`, not the defect surface** — the §1.2
treatment, applied to §2.2 itself. **Scope, stated as what the commands look at**: pass 1 matches
comment text in the Rust sources under `crates/script/elidex-js/src/compiler/`; pass 2 counts
`Op::PushUndefined`
occurrences in those same files; pass 3 walks the `ExprKind` / `StmtKind` variant lists in `ast.rs`
and maps each variant to its arm in those files. So the derivation sees compiler arms, their marker
comments, and productions that have an AST variant. Anything else is beyond its reach **by
construction** — that is a property of the commands, not a list of known holes, and this section
does not carry such a list.

**§2.2 is therefore a seed, not a boundary** — the framing §1.2 already applies to its own probe. It
found the class and justified the slice plan; nothing is out of scope by being absent from it, and
no row may be argued away on that ground.

**One mechanism discovers defects, and it is the per-slice sweep.** §5 already mandates that 0ca
re-run all three passes against **0ca's own parent HEAD**, and §5's `Primary module(s)` demotion
already puts touch-set derivation against a slice's own parent HEAD inside every slice's mandatory
`/elidex-plan-review`. The rule those two share is the canonical one: **the slice that owns a file
reads that file against its own parent HEAD and tables what it finds**, whatever directory the file
sits in. A second mechanism — a section-level inventory that cannot be complete — would be the
duplicated decision surface CLAUDE.md *One issue, one way* forbids, so there is not one. Two
obligations follow, and they are the whole of what this paragraph asks for:

- **0ca reads pass 3 for arm *content*, not only arm presence.** An arm that exists and does the
  wrong thing is invisible to a presence walk. State that in 0ca's memo.
- **A slice whose touch set reaches a file this derivation never reads** — anything under `vm/`,
  `parser/` or `scope/` — reads it against its own parent HEAD and tables what it finds there,
  rather than inheriting §2.3's dead-opcode partition as the dispatch-layer inventory.

Rows below whose Site column names a file outside `compiler/` arrived exactly that way. They are
evidence the mechanism works, not exceptions to a rule.

**Consequence for §7.2.** §9 dec. 9(b) derives the permanent conformance table as one row per §2.2
defect row, plus the §1.1/§1.2 absences — so the conformance table inherits this section's scope.
Round 8 found that on the T3-absence axis and patched it with a second source (§7.2); the general
form is the same, and §7.2 states it: rows for what this derivation does not read arrive from the
slice that owns the code, derived against its own parent HEAD.

⚠ **This table is the probe at baseline `f7d9b5ce`; its Site / Emits / Observable columns are frozen
there.** Read them as "what the sweep found", never as current state. Slice 0a then landed
(`658cc302`) and, beyond its three T0 rows, converted **nine** further rows from a silent no-op into a
*scoped* `Op::ThrowUnsupported` — marked **0a ✅ loud** in the Slice column. The construct is still
unimplemented and the named slice still owns it; what changed is the **failure mode**, which is the
very axis 0ca is scoped by, so 0ca's charter is narrowed accordingly below. **Nine is a count of marked
rows in this table, not of call sites** — `grep -rn 'emit_unsupported(fc' compiler/ | grep -v 'fn '`
gives **10** at `658cc302`, and the two do not correspond: two call sites guard constructs with no row
here, while one row's rejection is reached from two of them. One of the nine (`AssignTarget::Pattern`)
is a **dead** arm the parser never constructs, so eight are observable conversions.

Site line numbers are `f7d9b5ce`-relative and several have moved: `compiler/stmt.rs` went **964→712**
when 0a split out `compiler/stmt_loop.rs`, so the `:877-878` and `:882` rows now live in that new
file. ([§18.2](2026-07-vm-p4-slice-0a-landing-record.md)'s R1 row says 1001→712, which is right at *its own* anchor — a mid-PR commit on the
`vm-p4-slice0a` branch, after earlier 0a commits had grown the file. That commit is reachable from neither HEAD nor `origin/main`, so
it is not cited as a sha here: by the rule above the two figures are themselves the record, and their durable half is checkable at
`658cc302`, where `compiler/stmt.rs` is **712** lines. Do not carry the 1001 back to this baseline.)

⚠ **Three rows were added at PR-B and a fourth was corrected in place, after the freeze, and the
freeze still holds for all four.** Added: the object-literal accessor key row, the object-pattern
destructuring key row, the object-pattern rest-exclusion row. Corrected in place: the
computed-class-accessor row, which *replaces* the row asserting a `CompileError` — measured,
`git show 2a51106c -- docs/plans/2026-07-vm-p4-es-language-completeness.md` deletes that one row and
adds four, so net table growth is **+3**. The arms they cite are unchanged between `f7d9b5ce` and
`39bbdb1b`, so their Observable columns are baseline facts as much as the rest of the table.
*(Inside the table cells, "base" means `f7d9b5ce` and "head" means `39bbdb1b`. Both SHAs are fixed
anchors; the bare word "head" is not, which is why every claim **outside** the table now names the
SHA.)* The
files are not all byte-identical, and the rows say where their line numbers moved:
`git diff f7d9b5ce 39bbdb1b -- crates/script/elidex-js/src/compiler/{expr_object,stmt_destructure,expr_class}.rs`
is empty for `expr_object.rs`; for `stmt_destructure.rs` it is one `use` line plus an
`emit_unsupported` in the `VarLocation::Module` **store** arm (+1 line shift), and for
`expr_class.rs` one comment expansion at `:542` (+2 line shift). Neither touches a `PropertyKey`
arm. **Pass 2 missed one of these four**: the destructuring-key row is a class-1
`Op::PushUndefined` substitution, and pass 2 claims to have read and classified all 24 hits.
`git grep -c 'Op::PushUndefined' f7d9b5ce -- 'crates/script/elidex-js/src/compiler/*.rs'` sums to
**24** and puts **3** of them in `stmt_destructure.rs` — `:51`, `:118`, `:170` — so `:118`, the
catch-all this revision tables as a defect, was inside the set pass 2 read and was classified
legitimate. The count was right; the classification was not — which is why the sweep block above
carries the hit count and no defect count.

| Site | Syntax | Emits | Observable | Tier | Slice |
|---|---|---|---|---|---|
| `compiler/expr_assign.rs:170` | `obj[k] += v` | **`assert!` → panic** | process abort | **T0** | 0a ✅ |
| `compiler/expr_ops.rs:29` | **`obj.p \|\|= v` / `&&=` / `??=`** (named member) | **`unreachable!` → panic** — short-circuit was implemented only for the *identifier* target, so every member logical assignment reached `compound_op_to_opcode` | process abort | **T0** | 0a ✅ |
| `compiler/expr_assign.rs:170` + `expr_ops.rs:29` | **`obj[k] \|\|= v`** (computed logical) | **panic** (both of the above) | process abort | **T0** | 0a ✅ |
| `compiler/expr_assign.rs:210-212` | `[a,b]=…`, `({x}=…)` | RHS only, no store | silent no-op | T1 | the child of umbrella 0bc that owns the assignment-form binding-pattern storage — **0a ✅ loud** |
| `compiler/expr_member.rs:70-76` | `f(...a)` | spread operand as one arg | silent wrong arity | T1 | the child umbrella 1b's derivation mints for the argument-emit helper |
| `compiler/expr_class.rs:428` | `class A{x=1}` | **skipped entirely** | field `undefined` | T1 | **2aa** |
| `compiler/expr.rs:200-207` | `super.x`, `super[k]` | `PushUndefined` | TypeError at use | T2 | the child of umbrella 3 that owns the super-property emit shape |
| `compiler/expr.rs:237-239` | `` t`a${1}` `` | `PushUndefined` | tag never called | T1 | the child of umbrella **4a** that owns the tagged-template emit |
| `compiler/expr_member.rs:44`, `expr.rs:230` | `#x` get / `#x in o` | emits `GetPrivate`/`PrivateIn` → dispatch stub | `undefined` / `false` | T1 | the child of umbrella 5 that owns Private Name identity + the `GetPrivate`/`SetPrivate`/`PrivateIn` dispatch |
| `compiler/expr_assign.rs:202-206` | `o.#x = v` | `Op::Pop` (the `_ =>` arm); **no `SetPrivate` emit site exists at all** | write lost **and** the assignment evaluates to the *object*: `x = (o.#p = 5)` ⇒ `x === o` | T1 | the child of umbrella 5 that owns Private Name identity + the `GetPrivate`/`SetPrivate`/`PrivateIn` dispatch — **0a ✅ loud** |
| `compiler/expr.rs:200-207` | `import('x')` | `PushUndefined` | not a Promise | **T1** | *(see §5)* |
| `compiler/expr_object.rs:117-121` | `{1n: 'x'}` (**literal** key only — `{[1n]:…}` computed is correct, probe-verified). The cited arm is `PropertyKey::Literal(Literal::BigInt(_) \| Literal::RegExp { .. })` **inside** the `PropertyKind::Init` match whose head is `expr_object.rs:83` (arm head `:65`) — the same match the slot below names as the exhaustive copy the canonical lowering collapses the others into; a BigInt key is a key kind that lowering must answer | empty-string key → `{"":"x"}` | wrong key | T1 | **owner undetermined** — `#11-vm-property-key-lowering-unification`'s kind is UNDETERMINED (§8) and it mints no child |
| `compiler/expr_ops.rs:249` | `obj.#x++` | emits nothing, old value retained | silent no-op | T1 | the child of umbrella 5 that owns Private Name identity + the `GetPrivate`/`SetPrivate`/`PrivateIn` dispatch — **0a ✅ loud** |
| **`compiler/expr_ops.rs:261`** | **`(x)++`, `(a[0])++`** — parenthesized update target | operand evaluated only | **silent no-op** | T1 | 0bb — **0a ✅ loud** |
| **`compiler/expr_assign.rs:210-212`** | **`(x)+=1`** — parenthesized assign target (same catch-all as destructuring) | RHS only | **silent no-op** | T1 | 0bb — **0a ✅ loud** |
| `compiler/expr_ops.rs:147-149` + **`parser/`** | `delete this.#x` | `Pop; PushTrue` → `true` | wrong constant; ECMA-262 §13.5.1.1 makes it an **early SyntaxError** ⇒ **parse-time** rejection, so 0c's runtime-throw regime is wrong for it (same layer argument as §9 dec. 15). The sibling half of the *same* spec bullet (`delete <identifier>`) is **already** parser-gated, so the two halves must not land in two layers | T1 | **0ba** |
| `compiler/expr_ops.rs:133-163` (at `658cc302`) | **`delete (o.x)`**, **`delete o?.x`** — the arm tests the **raw** AST for `ExprKind::Member` (`:135`), so an `ExprKind::Paren` wrapper (`ast.rs:299`) or an `ExprKind::OptionalChain` takes the `else` at `:156-161` | `compile_expr`; `Op::Pop`; `Op::PushTrue` — the property **value** is evaluated, discarded, and `true` pushed | **silent success**: `{ let o={x:1}; delete (o.x); return 'x' in o }` → `true`. ECMA-262 **§13.5.1.2** step 4 deletes whenever Evaluation of the UnaryExpression yields a property Reference, and **§13.2.9.2** returns the ParenthesizedExpression's Reference unchanged | T1 | **0bb** |
| `compiler/expr_ops.rs:114-132` (at `658cc302`) | **`typeof (missingName)`** — the unresolvable-reference case runs only when the *immediate* argument is `ExprKind::Identifier` (`:117`), so a `Paren` wrapper defeats it | falls past `:132` to the generic `compile_expr` + `unary_op_to_opcode` (`:164-165`), whose `Op::GetGlobal` miss path raises `ReferenceError` (`vm/dispatch.rs:187-192`) | **throws** instead of returning `"undefined"`. ECMA-262 **§13.5.3.1** step 2.a | T1 | **0bb** |
| `compiler/expr_ops.rs:226` | module-binding **update** (`importedBinding++`) — *not* `delete`; falls to the `:261` catch-all, leaving the current value (the in-code "fall through to push undefined" comment is itself stale) | operand only | silent no-op | T1 | the child of umbrella M that implements module bindings — **0a ✅ loud** |
| `compiler/stmt.rs:877-878` | module-binding `for-in` target | `Pop` | silent no-op | T1 | the child of umbrella M that implements module bindings — **0a ✅ loud** |
| `compiler/stmt.rs:31-39` | `import`/`export` **declarations**, grouped into the `Empty`/`Debugger` no-op arm (found by the pass-3 structural sweep) | nothing | silent no-op — **latent**: unreachable until `parse_module` gains a production caller | T1 | the child of umbrella M that enables `parse_module` (precondition — this arm is fixed before that child enables it) |
| `compiler/stmt.rs:515-630` (at `658cc302`) | **`switch` does not enter its CaseBlock lexical scope** — `StmtKind::Block` (`:134-144`) and the `try` block (`:313-317`) both save `fc.current_scope_idx`, call `find_child_block_scope` and restore; the `StmtKind::Switch` arm does neither (`git show 658cc302:…/compiler/stmt.rs \| awk 'NR>=515 && NR<=632' \| grep -c -E -e current_scope_idx -e find_child_block_scope` → **0**, against **4** for the Block arm), while scope analysis *does* push a child `ScopeKind::Block` for the case block (`scope/visitor.rs:126-141`, `push_scope(ScopeKind::Block, …)` at `:131`) | ops emitted against the **enclosing** scope index, so a case-body declaration resolves to an outer binding | silent wrong **value**. Probe-measured at `658cc302` (`crates/` is byte-identical at `42c80199`): `let x=1; switch(0){case 0: let x=2;} x` → **`2`** where ECMA-262 gives `1`; control `let x=1; { let x=2; } x` → **`1`**; same shape inside a function → **`2`**. ECMA-262 **§14.12.4** Runtime Semantics: Evaluation steps 3-6 take `blockEnv = NewDeclarativeEnvironment(oldEnv)`, run `BlockDeclarationInstantiation(CaseBlock, blockEnv)` and set the running LexicalEnvironment to it, restoring at step 9. ⚠ **Arm-content defect** — the arm exists and emits, so a presence-only walk marks it handled | T1 | the child of umbrella **B** that owns block-scope entry |
| `compiler/stmt.rs:369-371`, `:380-382`, `:476-478`, `:501-503`, and `emit_pending_finally_bodies` (`:678-696`) (at `658cc302`) | **a `finally` body does not enter its own block scope** — `let x=1; try {} finally { let x=2 } x`. Every site that compiles the finalizer's statements compiles them against the enclosing `fc.current_scope_idx`, while scope analysis pushes a child `ScopeKind::Block` for the finalizer at `scope/visitor.rs:166`. Region probe, the same command over sibling regions of the one `Try` arm: `git show 658cc302:…/compiler/stmt.rs \| sed -n '<range>p' \| grep -c -E -e find_child_block_scope -e current_scope_idx` → **0** over `365,383` (no-catch finalizer) and **0** over `466,505` (shared + catch-rethrow finalizer), against **3** over `310,320` (the try block) | the finalizer's statements emit against the enclosing scope index, so a declaration in the finalizer resolves to an outer binding | silent wrong **value**. Probe-measured at `0c10870b` (`git diff --stat 658cc302 0c10870b -- crates/` is empty): `let x=1; try {} finally { let x=2 } x` → **`2`** where ECMA-262 gives `1`, and the `catch`+`finally` spelling `let x=1; try {} catch(e) {} finally { let x=2 } x` → **`2`** as well, so both emission branches carry it. Controls, same harness: `let x=1; { let x=2 } x` → **`1`**, `let x=1; try { let x=2 } finally {} x` → **`1`**, `let x=1; try { throw 0 } catch(e) { let x=2 } x` → **`1`** — the sibling scopes of the same statement are entered. Governing: ECMA-262 **§14.2.3** BlockDeclarationInstantiation (`webref aoid ecma262 BlockDeclarationInstantiation`), invoked by **§14.2.2** Block Runtime Semantics: Evaluation. **A call at the finalizer sites is not the fix.** `scope/visitor.rs:148` and `:166` both push with the `Try` statement's own `span`, so the try and finalizer scopes are siblings under one parent with the same kind *and* the same span, and `find_child_block_scope` (`stmt.rs:639-652`) returns the first child matching kind+span — always the try block. So the lookup key has to distinguish same-span siblings before any entry site can be added | T1 | the child of umbrella **B** that owns block-scope entry |
| `compiler/stmt.rs:31-39` + `compiler/mod.rs:82` + `compiler/expr_function.rs:167` (at `658cc302`) | **block-scoped function declarations are never instantiated** — `{ f(); function f() {} }`. Both hoist loops iterate an immediate-child list (`program.body` / `func.body`) and `StmtKind::Block` only recurses, so the declaration reaches the no-op arm. Scope analysis binds the name in the child block scope (`scope/visitor.rs:189` → `bind_and_visit_function` at `:422`, whose `state.add_binding(prog, state.current_scope(), …)` uses the innermost scope), so the slot exists and nothing ever writes it | nothing emitted at the declaration site, and no hoist reaches it | silent no-op → TypeError at call. Probe-measured at `658cc302`: `(function(){ { try { return 'called:'+f() } catch(e) { return 'ERR:'+e } function f(){ return 7 } } })()` → **`"ERR:TypeError: not a function"`**; `(function(){ { return typeof f; function f(){} } })()` → **`"undefined"`**. ⚠ **A third spelling is a *control*, not a symptom**: `(function(){ { function f(){return 7} } return typeof f })()` → **`"undefined"`** is what a strict-only core must keep — making it `"function"` is §10.2.11 FunctionDeclarationInstantiation step 32's *Normative Optional* web-legacy arm, and step 31 takes the other branch whenever `strict` is true (`webref aoid ecma262 FunctionDeclarationInstantiation`; steps from `webref body ecma262 sec-functiondeclarationinstantiation`), so CLAUDE.md's core-vs-compat split keeps it out of this slice. Control, the function body itself: `(function(){ return typeof g; function g(){} })()` → **`"function"`**. ECMA-262 **§14.2.3** BlockDeclarationInstantiation must instantiate and initialize it on block entry (AO number from `webref aoid ecma262 BlockDeclarationInstantiation`); §14.12.4 step 5 is the same AO for the CaseBlock, which is why the row above shares this row's owner | T1 | the child of umbrella **B** that owns §14.2.3 declaration instantiation |
| `compiler/expr_class.rs:430-447` | `class A{#x=1}` — `PrivateField` compiled only under `if *is_static`, no else | nothing | silent no-op | T1 | the child of umbrella 5 that mints the field record |
| `compiler/expr.rs:186-189` + **`parser/expr.rs:256-263`** | `ExprKind::Spread` in prefix position (`var y = ...x`) — an early-SyntaxError position per spec; the parser's `Ellipsis` arm is **ungated** | operand only | silent no-op | T1 | **0ba** (§9 dec. 15) |
| `compiler/expr_object.rs:26-39` (identical at head) | **object-literal accessor keys** — `compile_accessor` matches only `PropertyKey::Identifier` (`:27`) and `PropertyKey::Literal(Literal::String)` (`:32`); every other key kind falls to `_ => { fc.emit(Op::Pop); }` (`:37-39`). The key expression of a computed accessor is **never compiled at all** | `Op::Pop` — no property defined, and for a computed key **the key expression never runs** | silent no-op (class 2). Probe-measured at `39bbdb1b`: `let n=0; ({ get [++n]() { return 7 } }); n` → **`0`** · `let k='p'; let o={ get [k]() { return 7 } }; o.p` → **`undefined`** · `let n=0; ({ set [++n](v) {} }); n` → **`0`** (setter half) · `let o={ get 1() { return 7 } }; o[1]` → **`undefined`**, so this is **not limited to computed keys** — every non-Identifier / non-String-literal key is swallowed. (`{get true(){}}` **works**: `true` in key position parses as `PropertyKey::Identifier`, not `Literal::Boolean` — do not claim boolean keys are broken.) Contrast, same file: the sibling `PropertyKind::Init` arm (match at `:83`) **is** exhaustive over `PropertyKey` — `let o={1:'x'}; o[1]` → `'x'` (exhaustive is not the same as correct: its BigInt/RegExp arm is the `:117-121` row above). ECMA-262 §13.2.5.6 PropertyDefinitionEvaluation | T1 | **owner undetermined** — `#11-vm-property-key-lowering-unification`'s kind is UNDETERMINED (§8) and it mints no child |
| `compiler/stmt_destructure.rs:91` match, `:117-119` catch-all (`:92` / `:118-120` at head) | **object-pattern destructuring keys** — matches `PropertyKey::Identifier` (`:92` base / `:93` head), `Literal::String` (`:98`/`:99`) and `Computed` (`:104`/`:105`); every other key kind falls to `_ => { fc.emit(Op::PushUndefined); }` | `Op::PushUndefined` instead of a `GetProp`/`GetElem` | silent no-op (class 1 — **missed by pass 2**). Probe-measured at `39bbdb1b`: `const {1: a} = {1: 'x'}; a` → **`undefined`** (spec `'x'`). Identifier, string-literal and computed keys all work (`{p: a}` / `{'s': a}` / `{[k]: a}` → `'x'`), so this narrows — not deletes — §1.2's "destructuring **declarations** confirmed correct". Governing: ECMA-262 **§14.3.3.1** Runtime Semantics: PropertyBindingInitialization, whose step 1 is `Let propertyKey be ? Evaluation of PropertyName` = **§13.2.5.5**, where `LiteralPropertyName : NumericLiteral` is `Let number be the NumericValue…; Return ! ToString(number)` (the assignment-form counterpart on 0bc's path is **§13.15.5.3** PropertyDestructuringAssignmentEvaluation) | T1 | **owner undetermined** — `#11-vm-property-key-lowering-unification`'s kind is UNDETERMINED (§8) and it mints no child |
| `compiler/stmt_destructure.rs:134` match, `:149-150` catch-all (`:135` / `:150-151` at head) | **object-pattern REST exclusion** — the second `PropertyKey` match in the same function, deleting already-destructured keys from the rest object. Matches `Identifier` (`:135` base / `:136` head) and `Literal::String` (`:142`/`:143`); `_ => {}` under a comment reading "Computed/other keys: handled below via temp locals" — which is true of `Computed` (the `computed_key_slots` loop below) and false of every other kind | nothing emitted, so the key is never deleted from the rest object | silent wrong **value**, not a no-op: the destructured key survives into the rest binding. Probe-measured at `39bbdb1b`: `const {1: a, ...r} = {1:'x', z:'y'}` leaves `r` as **`{"1":"x","z":"y"}`**; the Identifier and computed spellings both give `{"z":"y"}`. Governing: ECMA-262 **§14.3.3.2** Runtime Semantics: RestBindingInitialization, which *receives* `excludedNames (a List of property keys)` as an argument — the set is built by **§14.3.3.1**, which returns « propertyKey » (the assignment-form counterpart on 0bc's path is **§13.15.5.4** RestDestructuringAssignmentEvaluation) | T1 | **owner undetermined** — `#11-vm-property-key-lowering-unification`'s kind is UNDETERMINED (§8) and it mints no child |
| `parser/object.rs:200-203` + `compiler/expr_object.rs:84-87` (at `658cc302`) | **`#x` is accepted as an object property key** — `({#x: 1})` and `const {#x: y} = o`. `parse_property_key` (`parser/object.rs:176`) has a `TokenKind::PrivateIdentifier` arm returning `PropertyKey::PrivateIdentifier`, and the arm reads only the token: nothing in it looks at which of the helper's three callers is asking (`git grep -n 'parse_property_key' 658cc302 -- crates/script/elidex-js/src` → the class-member path `parser/function.rs:518`, the object literal `parser/object.rs:51`, the binding pattern `parser/pattern.rs:123`, plus the definition) | object literal: `expr_object.rs:84` groups `PrivateIdentifier` with `Identifier` and emits `Op::DefineProperty`, so an ordinary property is defined; binding pattern: the key kind reaches the `stmt_destructure.rs` catch-all of the row two above | silent wrong **program** — the spelling has no meaning for the lowering to be wrong about. Probe-measured at `0c10870b`: `Object.keys({#x: 1})` → **`"x"`** (the `#` is dropped), `({#x: 1})['x']` → **`1`**, `({#x: 1})['#x']` → **`undefined`**; and `const o = {'#x': 7, x: 9}; const {#x: y} = o; y` → **`undefined`**. Governing grammar: **§13.2.5** Object Initializer derives a key only through `PropertyName : LiteralPropertyName \| ComputedPropertyName` with `LiteralPropertyName : IdentifierName \| StringLiteral \| NumericLiteral`, and **§14.3.3** Destructuring Binding Patterns reaches the same nonterminal through `BindingProperty : PropertyName : BindingElement` (`webref body ecma262 sec-object-initializer` / `sec-destructuring-binding-patterns`) — so neither context can derive `#x` and both are a Syntax Error. `PrivateIdentifier` is an alternative of `ClassElementName`, which is why the arm stays and gains a condition on its calling context rather than being deleted | T1 | **0ba** |
| `compiler/expr_class.rs:588-590` (`:590-592` head) + `:568`, `:621` (`:570`, `:623` head) | ⚠ **the previous row here was FALSE at head *and* at baseline** and is corrected. It claimed `class{get [k](){}}` is a `CompileError` — a "loud reject — not a defect". Measured at `39bbdb1b`: `let k='p'; class C { get [k]() { return 7 } }; (new C).p` → **`7`** · `let n=0; class C { get [++n]() { return 7 } }; n` → **`1`** (the key side effect runs) · `class C { get 1() { return 7 } }; (new C)[1]` → **`7`**. Computed class members never reach the cited guard: they are handled by a different, earlier path — `expr_class.rs:523` `if computed { … MethodKind::Get => Op::DefineComputedGetter, Set => Op::DefineComputedSetter, Method \| Constructor => Op::DefineComputedMethod … }` — which exists identically at `f7d9b5ce:523` | the `if computed { return Err(…) }` guard at `:588-590` is **unreachable dead code**: `grep -rn 'emit_class_member_name_op' crates/script/elidex-js/src/compiler/` gives both call sites passing `computed: false` (`:423` and `:575` at baseline, `:423` and `:577` at head). The two `_ =>` arms at `:568` / `:621` **are** reachable, but only for key kinds neither preceding arm covers — measured, `class C { get 1n() { return 7 } }` → `CompileError "unsupported class member key type"` while `class C { get 1() {…} }` succeeds | the guard is an **I-4 connect-or-delete**; the two `_ =>` arms are a loud **BigInt-literal-key** reject (a RegExp key in class position does not reach them — it is a *parse* error, measured) | I-4 / T2 | **owner undetermined** for the guard — `#11-vm-property-key-lowering-unification`'s kind is UNDETERMINED (§8) and it mints no child · the child of umbrella 2 that owns class-member key lowering (the two reachable `_ =>` arms) |
| `compiler/stmt.rs:882` | `for (obj.prop in …)` | `Pop` | silent no-op | T1 | the child of umbrella 0bc that owns the for-in/of head assignment-target store — **0a ✅ loud** |
| `compiler/expr_member.rs:92` | **`(o.m)()`** — `compile_call_expr` matches `ExprKind::Member` on the **raw** callee, so a parenthesized callee takes the plain-call branch | `Op::Call` (no receiver pushed) | **`this` is `undefined`** instead of `o` ([C20] step 1.a.i) | T1 | **0bb** (the shared `peel_paren` chokepoint — §9 dec. 14) |
| `compiler/expr_member.rs:180-187` (at `658cc302`) | **`o.m?.(…)`** — `prev_is_member` is `i > 0 && matches!(chain[i - 1], OptionalChainPart::Member { .. })`, so an optional call that is the chain's *first* part takes the `else` branch | `Op::Call` at `:186` (no receiver pushed); `o?.m(…)` takes `Op::CallMethod` at `:184` | **`this` is `undefined`** instead of `o` ([C20] step 1.a.i) | T1 | **0bb** — sibling of the `expr_member.rs:92` row above: same observable (a callee that is a member Reference loses its receiver), **two mechanisms, not one chokepoint**. `(o.m)()` is `compile_call_expr`'s `if let ExprKind::Member` test on the raw callee, defeated by the `ExprKind::Paren` wrapper (`ast.rs:299` at `658cc302` and at `6edda6f2`); this row is `compile_optional_chain_expr`'s `prev_is_member`, which reads `chain[i-1]` and never consults the chain's *base*. `peel_paren` does not reach this one — 0bb owns both because the receiver contract is one, and fixing either alone leaves the other |
| `compiler/stmt.rs:79-86` (at `658cc302`; the `is_await: _` discard is `:83`) | **`for await (x of it)`** — the `is_await: _` discard, which Slice 0a moved out of `:99-103` when it split the loop bodies into `stmt_loop.rs` | compiles identically to sync `for-of` | silent **wrong protocol** (sync iterator used for an async iterable) | T1 | **6b** |
| `compiler/expr_member.rs:60-64` | `f(a×256)` | **`assert!` → panic** | process abort | T0 | the child umbrella 1b's derivation mints for the argument-emit helper |
| `compiler/stmt.rs:424-439` — **not** an `f7d9b5ce` row: derived at `658cc302` and re-derived at HEAD `d49465d0`, where `git show <rev>:crates/script/elidex-js/src/compiler/stmt.rs \| grep -n 'if let Some(param_id) = catch.param'` gives **424** at both, and `'Op::Pop); // pop exception'` gives **439** at both | **destructured catch parameter** — `catch ({x})`, `catch ([a])` | the parameter is stored only under `if let PatternKind::Identifier(atom) = &pattern.kind` (`:426`); no other `PatternKind` has an arm, so nothing is emitted for the pattern's bound names, and the arm then runs `fc.emit(Op::Pop)` (`:439`) **unconditionally**, discarding the exception either way | silent no-op: `try { throw {x: 1} } catch ({x}) { … }` never initializes `x`. ECMA-262 **§14.15.2** Runtime Semantics: CatchClauseEvaluation **step 5** — `Let status be Completion(BindingInitialization of CatchParameter with arguments thrownValue and catchEnv)` (§-number from `webref aoid ecma262 CatchClauseEvaluation`; step text from `webref body ecma262 sec-runtime-semantics-catchclauseevaluation`) | T1 | the child of umbrella 0bc that owns the destructured catch parameter (gained here — see §5) |
| `compiler/expr_assign.rs:215-220` | `AssignTarget::Pattern` | `Op::Pop`, claims to "fail explicitly" but does not | **dead** — parser never constructs this variant | I-4 | the child of umbrella 0bc that owns the `AssignTarget::Pattern` connect-or-delete — **0a ✅ loud** |
| `compiler/expr_class.rs:402-427` (at `658cc302`) | **`class C { static x = this }`** — the static initializer is compiled with the **enclosing** `FunctionCompiler` (`compile_expr(fc, …)` at `:411` computed-key branch, `:418` otherwise), so `ExprKind::This` inside it reads the surrounding function's receiver. Contrast the static **block** arm at `:448-471`, which builds a child compiler and invokes it with the ctor as `this` (`:454` `Op::Dup`, `:470` `Op::CallMethod`) | the initializer's value evaluated against the wrong receiver | `C.x === C` → **`false`**. ECMA-262 **§15.7.10** ClassFieldDefinitionEvaluation and **§7.3.32** DefineField call the initializer with the field receiver. ⚠ Narrows §1.2's "static fields/methods, no action" and §8's treatment of that facet of `#11-step9-class-extras` as discharged — the probe `static x = 1` cannot distinguish the receivers | T1 | **2ac** |
| `vm/dispatch_objects.rs:135-186` (at `658cc302`) | **`{...'ab'}`**, **`const {...r} = 'ab'`** — `op_spread_object` copies only when source **and** destination are both `JsValue::Object` (`:137`); any other source falls out of the `if let` and the handler returns `Ok(())` | nothing copied | **empty object** instead of the enumerable index properties. ECMA-262 **§7.3.25** CopyDataProperties returns early only for `undefined`/`null` (step 1) and otherwise applies `ToObject` (step 2). ⚠ **Layer-B live defect in a connected opcode** — outside the scope stated above (the passes read `compiler/`) and outside §2.3's zero-emit partition; it is tabled here because the handler was read directly, which is the mechanism that section names | T1 | **O** |
| `vm/dispatch_class.rs:29-37` + `vm/value.rs:588-599` (at `658cc302`) | **`new.target` read inside an arrow** — `op_new_target` reads only the current frame's `CallMode` (`:32-33`), and `FunctionObject` carries `captured_this` (`value.rs:598`) with no lexical `new.target` counterpart; invoking an arrow pushes a `CallMode::Call` frame (`vm/interpreter.rs:648`, `:683`, `:732`) | `JsValue::Undefined` | `function F(){ this.ok = (() => new.target)() === F } new F().ok` → **`false`**. ECMA-262 **§9.4.5** GetNewTarget resolves the surrounding function environment, which for an arrow is the enclosing function's. ⚠ Narrows §1.1's `new.target` row | T1 | **N** |
| `compiler/stmt_destructure.rs:36-81` (at `658cc302`) | **an array binding pattern never closes its iterator** — `const [x] = it`. The `PatternKind::Array` arm emits `Op::GetIterator` (`:38`), takes one value per element, and where the pattern has no rest element ends at `fc.emit(Op::Pop); // pop iterator` (`:80`). No `Op::IteratorClose` is emitted from this file at all: `git grep -l 'Op::IteratorClose' 658cc302 -- crates/script/elidex-js/src/compiler/` returns `expr_yield_star.rs`, `stmt.rs` and `stmt_loop.rs`, and not this one | `Op::Pop` on an unexhausted iterator | `it.return()` is never called on the **normal early exit**. ECMA-262 **§8.6.2** BindingInitialization, production `BindingPattern : ArrayBindingPattern`, **step 3**: "If iteratorRecord.[[Done]] is false, return ? IteratorClose(iteratorRecord, result)" (AO number from `webref aoid ecma262 BindingInitialization`, step text from `webref body ecma262 sec-runtime-semantics-bindinginitialization`). The arm is `compile_destructure_pattern`, reached from parameter patterns (`expr_function.rs:55`), `var`/`let`/`const` declarations (`stmt.rs:121`), the `for` head (`stmt_loop.rs:251`) and the for-in/of head (`stmt_loop.rs:321`) — `git grep -n compile_destructure_pattern 658cc302 -- crates/script/elidex-js/src`. **Derived from the arm; not probe-measured** (this PR edits no `.rs` file, and the crate has no runner binary) | T1 | the child of umbrella 0bc that owns §8.6.2 step 3's `IteratorClose` across the array-binding consumers |
| `compiler/stmt.rs:284-291` + `:224-256` (at `658cc302`) | **`outer: { break outer; }` does not compile** | the `Labeled` arm records the label as `fc.loop_stack.len()` (`:287-288`) and emits no patch point for the end of an ordinary labelled statement; the `Break` arm reads that index back and rejects it when no loop is open (`:244-249`, message `"label '{label_name}' is not associated with a loop or switch"`) | a labelled statement outside any loop records depth `0` while `fc.loop_stack.len()` is `0`, so the guard `loop_idx >= fc.loop_stack.len()` holds and **valid code is rejected at compile time**; with a loop nested inside the labelled block the same index resolves to `fc.loop_stack[0]` (`:251`), so the patch attaches to the loop rather than to the block. ECMA-262 **§14.13.4** LabelledEvaluation, production `LabelledStatement : LabelIdentifier : LabelledItem` **step 4** — a break completion whose `[[Target]]` is the label is caught at the labelled statement itself, so a labelled statement is a break target whether or not it is a `BreakableStatement` (§-number from `webref aoid ecma262 LabelledEvaluation`, step text from `webref body ecma262 sec-runtime-semantics-labelledevaluation`). **Derived from the arms; not probe-measured** | T2 | the child of umbrella **L** that owns the label→patch-list representation |
| `scope/visitor.rs:79-124` + `compiler/stmt_loop.rs` (at `658cc302`) | **no per-iteration lexical environment for a loop's `let` head** — `let fs=[]; for (let i=0;i<2;i++) fs.push(()=>i)` and `for (let x of [1,2])` alike | `visit_stmt` pushes exactly one `ScopeKind::Block` for the whole `for` (`:85`) and one for the whole `for-in`/`for-of` (`:114`), and the compiler stores the head binding into that one scope's local — `compile_for`'s init at `stmt_loop.rs:245-260`, the for-in/of head at `:321` — with no environment clone between iterations. `git grep -c CreatePerIterationEnvironment 658cc302 -- crates/script/elidex-js/src` exits **1** (no match) | one binding shared by every iteration, so every closure created in the body observes the last value. ECMA-262 **§14.7.4.4** CreatePerIterationEnvironment, reached from **§14.7.4.2** ForLoopEvaluation, builds a fresh `NewDeclarativeEnvironment` per iteration and copies the previous iteration's value in (steps 1.d, 1.e.ii-iii); **§14.7.5.7** ForIn/OfBodyEvaluation **step 8.h.iii** takes `iterationEnv = NewDeclarativeEnvironment(oldEnv)` per iteration for a lexical head (both read via `webref body ecma262`). **Derived from the scope pass and the arms; not probe-measured** | T1 | **row E** (kind UNDETERMINED — no owner until its measurement is made) |
| `vm/dispatch_iter.rs:141-239` + `:243-273` (at `658cc302`) | **`for-in` enumerates a front-loaded snapshot** | `op_for_in_iterator` collects keys once into `ForInState { keys, index: 0 }` (`:231-236`) and inserts a name into `seen` only inside `if attrs.enumerable && seen.insert(sid)` (`:188` own storage, `:220` prototype chain), so a **non-enumerable own key never marks the name visited**; `op_for_in_next` (`:243`) hands back `state.keys[state.index]` (`:255`) with no re-check of existence or enumerability | an inherited enumerable property shadowed by a non-enumerable own property of the same name is still enumerated, and a key deleted before its turn is still yielded. ECMA-262 **§14.7.5.9** EnumerateObjectProperties: "The values of [[Enumerable]] attributes are not considered when determining if a property of a prototype object has already been processed", and "A property that is deleted before it is processed by the iterator's next method is ignored" — and its informative generator adds the key to `visited` **before** testing `desc.enumerable` (`webref body ecma262 sec-enumerate-object-properties`). A live defect in a **connected** handler, so §1.0's derivation did not produce it: it is recorded here under this section's per-slice-sweep rule and owned by its own §8 slot rather than by a §5 slice. **Derived from the handlers; not probe-measured** | T1 | `#11-vm-for-in-enumeration-semantics` |
| `parser/arrow.rs:104-119` + `vm/value.rs:588-599` + `vm/dispatch_class.rs:115-119` (at `658cc302`) | **`super` reached through an arrow** — `(() => super())()` in a derived constructor, `(() => super.x)()` in a method — ⚠ **only the first spelling reaches the observable below**: measured at `658cc302`, `(() => super.x)()` and its non-arrow control `super.x` fail *identically* with `TypeError: Cannot convert undefined or null to object`, because `ExprKind::Super` lowers to `Op::PushUndefined` (`compiler/expr.rs:200-207`) and the crate emits no `Op::GetSuperProp` / `Op::SetSuperProp` anywhere under `compiler/` (`git grep -n -e GetSuperProp -e SetSuperProp 658cc302 -- crates/script/elidex-js/src/compiler` returns two comment lines and no emit), so `dispatch_super_inner` is never reached and the arrow contributes nothing. The `super()` half does discriminate: `class A {} class B extends A { constructor(){ (() => super())(); this.z=1 } }` raises exactly `SyntaxError: 'super' keyword unexpected here` while the non-arrow control answers `1` | the parser deliberately admits it: the arrow parser saves `in_method` / `in_constructor` / `in_static_block` / `in_derived_constructor` and restores them inside `with_function_context`, under the comment "Arrow functions inherit super context". Nothing carries that inheritance past the parser: `git grep -n -e 'in_method' -e 'in_derived_constructor' -e 'in_static_block' 658cc302 -- crates/script/elidex-js/src \| grep -v '/parser/'` returns only `install_body_mixin_methods` substring hits in `vm/globals.rs` and `vm/host/body_mixin.rs`, while the same pattern inside `parser/` matches across six files, so it discriminates. `FunctionObject` (`value.rs:588-599`) has `captured_this` and no home-object counterpart — `git show 658cc302:…/vm/value.rs \| sed -n '588,599p' \| grep -ci home` → **0**, against **3** for `captured` over the same range and **4** for `home` over the whole file, so the pattern discriminates — and a frame's `home_class` is `Some` only when `compiled.is_class_ctor` (`vm/interpreter.rs:802`) or hard-coded `None` on the `call_internal` entry (`:589`, whose own comment says so) | `dispatch_super_inner` (`vm/dispatch_class.rs:115`) reads the **current** frame's `home_class` and raises `VmError::syntax_error("'super' keyword unexpected here")` when it is `None` (`:117-119`); an invoked arrow's frame is not a class-ctor frame, so that is the branch taken. ECMA-262 **§13.3.7.2** GetSuperConstructor step 1 and **§13.3.7.3** MakeSuperPropertyReference step 1 both begin at **§9.4.3** GetThisEnvironment, whose step 2 walks outward until a record's `HasThisBinding()` is true — an arrow's is not — so both spellings resolve against the enclosing method or constructor (§-numbers from `webref aoid ecma262 GetSuperConstructor` / `MakeSuperPropertyReference` / `GetThisEnvironment`; steps from `webref body ecma262 sec-getsuperconstructor` / `sec-makesuperpropertyreference` / `sec-getthisenvironment`). ⚠ Narrows §1.1's `super(...args)` row, and the frame-state audit umbrella **3**'s derivation places on its children is stated over the direct-method entry points, so it does not reach this. **Derived from the arms; not probe-measured** | T2 | **S** |
| `compiler/stmt.rs:203-205` (at `658cc302`; the same pair at `:227` / `:238` in the `Break` arm, under the same comment) | **a `for-of` left by `return` or `break` from inside an enclosing `try`/`finally`** | `emit_pending_finally_bodies` first, `emit_iter_close_range` second, so the finally bodies' ops precede the `Op::IteratorClose` ops in one instruction sequence (`fn emit_iter_close_range` is `stmt.rs:657`; its `fc.emit(Op::IteratorClose)` at `:661` is one of the two statement-lowering emits [§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) partitions, and neither of that section's greps sees this defect — they find the emit, not its position relative to the finally bodies: `git grep -n 'emit(Op::IteratorClose)' 658cc302 -- crates/script/elidex-js/src/compiler` returns four hits (`expr_yield_star.rs:146`/`:157`, `stmt.rs:661`, `stmt_loop.rs:147`), none in `:203-205`, while `git grep -n -e 'emit_iter_close_range(' -e 'emit_pending_finally_bodies(' 658cc302 -- crates/script/elidex-js/src/compiler` returns both call sites, so a discriminating probe exists) | the close and the enclosing `finally` run in the **wrong order**, and because the ops are sequential a throw from the finally body transfers control before the close ops execute at all, so `.return()` is never called. ECMA-262 **§14.7.5.7** ForIn/OfBodyEvaluation performs the close **inside the loop's own evaluation** — step 8.i.vi on an abrupt lhs completion and step 8.m.v when `LoopContinues(result, labelSet)` is false, each `Return ? IteratorClose(iteratorRecord, status)` — so the completion reaches an enclosing `finally` only after the iterator is closed. The order is **nesting-dependent**: for a `try`/`finally` *inside* the loop body the finally is part of step 8.10's "Evaluation of stmt" and therefore precedes the same close (§-number from `webref aoid ecma262 ForIn/OfBodyEvaluation`, steps from `webref body ecma262 sec-runtime-semantics-forin-div-ofbodyevaluation-lhs-stmt-iterator-lhskind-labelset`). ⚠ The in-tree comment above the pair states one nesting's order as unconditional and attributes it to the spec — a code-side observation for the owning slice, not a claim this plan carries. **Derived from the arms; not probe-measured** | T1 | the child of umbrella **Pd** that owns the close-versus-enclosing-finalizer ordering |
| `compiler/expr_function.rs:31-66` + `scope/visitor.rs:442-486` (at `658cc302`; `git diff --stat 658cc302 909a77cb -- crates/` is empty, so both readings hold at this document's HEAD) | **a parameter binding is never uninitialized, and the body shares the parameters' environment** — `function f(a=b,b=1){}` and `function f(a=x){var x}` | `compile_param_prologue` lowers each default as read-the-slot / compare-to-`undefined` / conditionally store (`Op::GetLocal` `:41`, `Op::PushUndefined` `:42`, `Op::StrictEq` `:43`, `Op::JumpIfFalse` `:44`, `Op::SetLocal` `:46`), and the caller has already filled every positional slot before the prologue runs, so a default initializer reads a later parameter's slot as an ordinary value; the arm's own comment at `:52` states it — "`Var` kind selects no `InitLocal` — param bindings carry no TDZ". `visit_function` pushes exactly one `ScopeKind::Function` (`:456`) and runs `visit_params` (`:479`) and the body statements (`:481-483`) inside it, so a body `var` and a parameter default resolve against the same scope | a default initializer reads a later parameter's value instead of raising, and reads a body-local `var` instead of the outer binding of that name. ECMA-262 **§10.2.11** FunctionDeclarationInstantiation orders it otherwise: step 21.3.a creates each parameter binding with "Perform ! envRecord.CreateMutableBinding(paramName, false)" and initializes it only under step 21.3.b's `hasDuplicates` case, so the bindings are uninitialized while the defaults run; step 28 performs "IteratorBindingInitialization of formals", which is what initializes them, left to right; and step 30.2 takes "variableEnv be NewDeclarativeEnvironment(envRecord)" for the body whenever `hasParamExprs` (step 8, ContainsExpression of formals) is true, under step 30.1's note that a separate record "ensure[s] that closures created by expressions in the formal parameter list do not have visibility of declarations in the function body" (§-number from `webref aoid ecma262 FunctionDeclarationInstantiation`, steps from `webref body ecma262 sec-functiondeclarationinstantiation`). A live defect in fully emitted code, so §1.0's derivation did not produce it: recorded here under this section's per-slice-sweep rule and owned by its own §8 slot rather than by a §5 slice — 0bc's parameter coverage is binding-pattern storage and iterator closing, a different mechanism. **Derived from the arms and the scope pass; not probe-measured** | T1 | the child of umbrella `#11-vm-parameter-environment-instantiation` that owns the parameter/body environment separation |
| `vm/dispatch_objects.rs:352-384` (at `658cc302`) | **`instanceof` never tests its right operand for callability** — `({}) instanceof {prototype:{}}` | `op_instanceof` rejects a non-Object right operand (`:353-357`), looks `@@hasInstance` up on it (`:360-361`) and, if that lookup returns anything at all, resolves and calls it (`:362-364`); otherwise it falls straight through to the prototype-chain walk (`:367-383`) with no callability test in between. `coerce::get_property` (`vm/coerce.rs:907`) returns `Some(PropertyResult::Data(v))` for a **present** property whatever its value, `resolve_property` (`vm/ops_property.rs:38-41`) passes a data slot through unchanged, and `call_value` (`vm/ops.rs:665-674`) raises `TypeError "not a function"` for a non-Object callee | a plain object carrying a `prototype` property answers `instanceof` with `false` where the AO throws, and an `@@hasInstance` present with the value `null` throws where the AO ignores it. ECMA-262 **§13.10.2** InstanceofOperator orders the two: step 2 is "Let instOfHandler be ? GetMethod(target, %Symbol.hasInstance%)" — GetMethod yields **undefined** for a `null` or `undefined` value, so step 3's "is not undefined" test routes those to the default path — step 4 is "If IsCallable(target) is false, throw a TypeError exception", and only step 5 reaches **§7.3.21** OrdinaryHasInstance, whose step 1 returns false for a non-callable and whose step 5 throws when `Get(ctor, "prototype")` is not an Object, where `:373`'s `if let` takes the `Ok(false)` path instead (§-numbers from `webref aoid ecma262 InstanceofOperator` / `OrdinaryHasInstance`, steps from `webref body ecma262 sec-instanceofoperator` / `sec-ordinaryhasinstance`). A live defect in a **connected** handler, so §1.0's derivation did not produce it: recorded here under this section's per-slice-sweep rule and owned by its own §8 slot rather than by a §5 slice — it is neither a compiler-arm stub nor a builtin-property absence. Read from the handler, not by grep: `is_callable` returns **0** hits over the whole of `vm/dispatch_objects.rs`, which cannot distinguish "the check is missing" from "the check is spelled otherwise", while `grep -rln 'is_callable' crates/script/elidex-js/src/vm/` reaches `vm/ops.rs` and `vm/coerce.rs` among other siblings, so the name is in use in the tree. **Derived from the handler; not probe-measured** | T1 | `#11-vm-instanceof-callable-check` |
| `compiler/expr_assign.rs:80-88` (at `2039766a`; `git diff --stat 658cc302 2039766a -- crates/` is empty, so the reading holds at `658cc302` too) | **a write to a `const` binding is rejected at compile time** — `try { const x = 1; x = 2 } catch (e) { 42 }` | `compile_identifier_store`'s `VarLocation::Local` arm returns `CompileError { message: "Assignment to constant variable '…'" }` (`:81-88`) before emitting anything; its sibling check two lines below takes the other route for the same lookup, emitting `Op::CheckTdz` (`:90-91`) rather than rejecting | the whole script yields no bytecode, so every unrelated statement is lost and the `catch` is never reached. ECMA-262 puts the failure at run time: **§13.15.2** Runtime Semantics: Evaluation reaches **§6.2.5.6** PutValue, whose Declarative Environment Record path is **§9.1.1.1.5** SetMutableBinding, step 5 — "Assert: This is an attempt to change the value of an immutable binding", then 5.b "If strict is true, throw a TypeError exception" (§-numbers from `webref aoid ecma262 PutValue` / `SetMutableBinding` + `webref heading ecma262 9.1.1.1`, steps from `webref body ecma262 sec-declarative-environment-records-setmutablebinding-n-v-s`). This is the shape [§17](2026-07-vm-p4-slice-0a-landing-record.md)'s private-store CRIT had and §9 dec. 5 resolved against measured evidence — loud but **not scoped** — so the disposition is dec. 5's, not a fresh one: a runtime throw through the `Op::ThrowUnsupported` mechanism (an engine-constructed error raised through `throw_error`, catchable and local), and `CompileError` reserved for what the spec itself rejects before execution. §1.0's derivation does not produce this row — §2.2's pass 3 flags an arm that neither emits an op **nor** returns `CompileError`, and this arm returns one — so it is recorded here under this section's per-slice-sweep rule and owned by its own §8 slot. **Derived from the arm; not probe-measured** | T2 | `#11-vm-const-assignment-runtime-throw` |
| `compiler/stmt.rs:262-282` (at `2039766a`; contrast `:203-205` in the `Return` arm and `:227` / `:238` in the `Break` arm) | **an inner `for-of` left by a labelled `continue` is never closed** — `outer: for (;;) { for (const x of it) { continue outer } }` | the `Continue` arm emits the pending finally bodies (`:263`) and the jump (`:264`) and pushes the patch onto the targeted loop's `continue_patches` (`:273`); it never calls `emit_iter_close_range`, which the `Return` arm calls over `0..loop_stack.len()` (`:205`) and the `Break` arm over `target..loop_stack.len()` (`:238`), and which is what emits `Op::IteratorClose` for the for-of frames in its range (`:657-664`, the emit at `:661`). The absence is probed, not assumed: `grep -rn 'emit_iter_close_range' crates/script/elidex-js/src --include='*.rs'` returns the definition and exactly those two call sites (plus one test comment naming it), so the pattern reaches the helper wherever it is used and reaches nothing in `:262-282`. Nor is the close recovered downstream — the jump is patched onto the **targeted** loop's `continue_patches` and lands at its `loop_start` (`compiler/stmt_loop.rs:123`), bypassing the inner `for-of`'s exit path (`:125-128`, whose `:128` comment reads "normal exhaustion: discard iterator without calling .return()") and its catch handler, the only other for-of `Op::IteratorClose` emit (`:143-147`), which runs on a throw | `it.return()` is never called on the way out. ECMA-262 **§14.7.5.7** ForIn/OfBodyEvaluation step 8.m tests `LoopContinues(result, labelSet)` and on false reaches step 8.m.v, `Return ? IteratorClose(iteratorRecord, status)`; **§14.7.1.1** LoopContinues returns true for a continue completion only when step 3's `[[Target]]` is empty or step 4's labelSet contains it, so the loop the label names continues while the loops inside it close (§-numbers from `webref aoid ecma262 'ForIn/OfBodyEvaluation'` / `LoopContinues`, steps from `webref body ecma262 sec-runtime-semantics-forin-div-ofbodyevaluation-lhs-stmt-iterator-lhskind-labelset` / `sec-loopcontinues`). The range this needs is therefore not the `Break` arm's: `break` leaves the targeted frame and `continue` re-enters it. **Derived from the arms; not probe-measured** | T1 | the child of umbrella **Pd** that owns the labelled-`continue` exit range |
| `compiler/stmt.rs:227` and `:263` (at `b5356098`; `git diff --stat 658cc302 b5356098 -- crates/` is empty, so the reading holds at `658cc302` too) | **a `break` or `continue` whose target lies inside the enclosing `try` still runs the finalizer** — `try { while (true) { break } log.push('after') } finally { log.push('fin') }` | the `Break` arm's first statement is `emit_pending_finally_bodies(fc, prog, analysis, func_scopes)?` (`:227`) and the `Continue` arm's is the same call (`:263`); neither is guarded by any test of where the jump lands, and the helper (`:678-696`) takes no target — it walks the whole of `fc.finally_stack`, `for i in (0..saved.len()).rev()` (`:686`) — so every enclosing finalizer is emitted whatever the target. The arms do compute a target for the *other* obligation on the same path (`target_idx` at `:231-236`, the `emit_iter_close_range` lower bound at `:238`), so the information a guard needs is already in scope at the site that lacks it. Contrast the sibling `Return` arm (`:203`), where emitting all of them is correct because a return does leave every enclosing `try` | the finalizer's ops run at the jump **and** again when the try block completes, so `fin` is logged before `after` and once more after it — silent wrong **order**, plus a duplicated finalizer run. ECMA-262 **§14.15.3** Runtime Semantics: Evaluation, production `TryStatement : try Block Finally`: step 1 is "Let blockResult be Completion(Evaluation of Block)" and step 2 "Let finallyResult be Completion(Evaluation of Finally)"; the `try Block Catch Finally` production orders the same two at steps 1 and 4. So the Finalizer is evaluated only after the Block's evaluation **completes**, and a jump whose target is inside the Block does not complete it (§-number from `webref heading ecma262 14.15.3`, steps from `webref body ecma262 sec-try-statement-runtime-semantics-evaluation`). **Derived from the arms; not probe-measured** | T1 | the child of umbrella **Pd** that owns target-region-aware finalizer emission |
| `compiler/expr_assign.rs:45` + `vm/dispatch.rs:155-160` + `vm/value.rs:952-957` (at `2039766a`) | **a TDZ check is lost through a captured binding** — `let f = () => x; f(); let x = 1` | `compile_identifier_load`'s `VarLocation::Local` arm consults `needs_tdz` and emits `Op::CheckTdz` before `Op::GetLocal` (`:34-42`), while its `VarLocation::Upvalue` arm is a bare `fc.emit_u16(Op::GetUpvalue, idx)` (`:47`; `:45` is the `Op::GetLocal` emit of the Local arm above it). Both `Op::CheckTdz` emit sites in the crate are inside that Local arm (`grep -rn 'Op::CheckTdz' crates/script/elidex-js/src --include='*.rs'` → two emit sites, `compiler/expr_assign.rs:42` and `:91`, both inside that Local arm; every remaining hit is the opcode's own definition, its documentation, its dispatch arm or a test, so the pattern reaches the name's other uses and still finds no emit outside the Local arm). The runtime half cannot recover it: the `Op::GetUpvalue` arm reads `upvalue_ids[idx]` and pushes `read_upvalue` (`vm/dispatch.rs:155-160`), `read_upvalue` returns the stack slot or the closed value (`vm/ops.rs:597-602`), `UpvalueState` carries exactly `Open { frame_base, slot }` and `Closed(JsValue)` (`vm/value.rs:952-957`) with no uninitialized state, and the TDZ bits live on `CallFrame` (`is_in_tdz`, `vm/value.rs:1106`), which an upvalue read never consults | a captured binding read before its declaration yields `undefined` instead of raising. ECMA-262 **§9.1.1.1.6** GetBindingValue step 2 — "If the binding for name in envRecord is an uninitialized binding, throw a ReferenceError exception" — under a preamble that adds "regardless of the value of strict" (§-number from `webref heading ecma262 9.1.1.1`, steps from `webref body ecma262 sec-declarative-environment-records-getbindingvalue-n-s`). Cross-layer (compiler emit + upvalue representation + dispatch), so §1.0's derivation does not produce it: §2.2's sweep is over `compiler/` arms and this arm emits an op. Recorded here under the per-slice-sweep rule and owned by its own §8 slot. **Derived from the arms and the runtime reads; not probe-measured** | T1 | `#11-vm-upvalue-tdz-check` |
| `scope/visitor.rs:458-463` + `compiler/expr.rs:210-214` + `compiler/expr_function.rs:116-133` (at `2039766a`) | **a named function expression has no binding to its own name** — `let f = function g(){ return g }; f() === f` | `visit_function` adds the name as an inner `BindingKind::Function` binding for the expression form (`:458-463`, under the `:439` comment "E13: `is_expression` controls inner name binding"), and `compile_nested_function` then initializes every `BindingKind::Var \| BindingKind::Function` local above the parameter count to `undefined` (`:120` filter; `Op::PushUndefined` / `Op::SetLocal` / `Op::Pop` at `:130-132`). Nothing stores the closure into that slot: the `ExprKind::Function` arm is `compile_nested_function` → `add_constant` → `fc.emit_u16(Op::Closure, idx)` and ends (`compiler/expr.rs:210-214`). Counting `Op::Closure` emits proves nothing — several files emit it — so the probe is what **follows** each: `grep -n -A 3 'emit_u16(Op::Closure, idx)' crates/script/elidex-js/src/compiler/expr.rs crates/script/elidex-js/src/compiler/mod.rs crates/script/elidex-js/src/compiler/expr_function.rs` returns the two *declaration* sites followed by `resolve_identifier` and a store (`compiler/mod.rs:93-96`, `compiler/expr_function.rs:180-183`) and the two *expression* sites followed by the arm's closing brace (`compiler/expr.rs:213-214`, `:219-220`), so it separates the paths rather than counting the opcode | the self-name reads `undefined`, so recursion through it fails and the identity test is false. ⚠ **Both of those pass on a *mutable* binding in the function's own scope, which is not what the AO says** — **§15.2.5** step 5 is `CreateImmutableBinding(name, false)` in a `funcEnv` of the expression's own, so a third regression is required: in strict mode, assigning to the self-name inside the body throws a TypeError (**§9.1.1.1.5** SetMutableBinding step 5.b for an immutable binding under `S`). Without it the slot retires with the binding present and its immutability absent. ECMA-262 **§15.2.5** InstantiateOrdinaryFunctionExpression, production `FunctionExpression : function BindingIdentifier ( FormalParameters ) { FunctionBody }`: step 4 "Let funcEnv be NewDeclarativeEnvironment(outerEnv)", step 5 "Perform ! funcEnv.CreateImmutableBinding(name, false)", step 8 creates the closure against `funcEnv`, and step 11 "Perform ! funcEnv.InitializeBinding(name, closure)" — an **immutable** binding in an environment of the expression's own, against a mutable one in the function's own scope here (§-number from `webref aoid ecma262 InstantiateOrdinaryFunctionExpression`, steps from `webref body ecma262 sec-runtime-semantics-instantiateordinaryfunctionexpression`). The arm emits an op, so §1.0's derivation does not produce it: recorded here under the per-slice-sweep rule and owned by its own §8 slot. **Derived from the arms and the scope pass; not probe-measured** | T1 | `#11-vm-named-function-expression-binding` |
| `parser/primary.rs:200-203` + `parser/function.rs:281-292`, `:370-371` + `scope/visitor.rs` (at `b5356098`; `git diff --stat 658cc302 b5356098 -- crates/` is empty, so the readings hold at `658cc302` too) | **a private reference is never validated against a declared private name** — `({}).#x`, and `class C { m() { return this.#missing } }` | `parse_member_prop`'s `TokenKind::PrivateIdentifier` arm advances and returns `MemberProp::PrivateIdentifier(name)` (`primary.rs:200-203`) without consulting anything about which private names are in scope. The one private-name set the parser builds is for **declarations**, not references: `parse_class_body` allocates `private_names: [HashSet<Atom>; 3]` (`function.rs:370-371`) and fills it through `check_private_name_dup` (`:281-292`), whose `match` inserts only from `ClassMemberKind::PrivateField` and `PrivateMethod` and `return`s for every other member kind; the set is a local of that loop and is dropped at `:390`, and no reference site reads it. Scope analysis carries no declared-private-name environment either: `git grep -in 'private' b5356098 -- crates/script/elidex-js/src/scope/` returns exactly three lines — `visitor.rs:278`, where `ExprKind::PrivateIn` is grouped with the other single-argument expressions and its `right` visited, and `:546` / `:550`, where `PrivateMethod` / `PrivateField` are traversed for their function or initializer — all child traversals, none of which binds a name. Negative control: the same case-insensitive pattern over `crates/script/elidex-js/src/` reaches **48** files (`git grep -iln 'private' b5356098 -- crates/script/elidex-js/src/ \| wc -l`), so it discriminates rather than matching nothing. The reference reaches lowering — `compiler/expr_member.rs:44` emits `Op::GetPrivate` and `compiler/expr.rs:230` `Op::PrivateIn` at `b5356098` (`git grep -n -e 'Op::GetPrivate' -e 'Op::PrivateIn' b5356098 -- crates/script/elidex-js/src/compiler/`), the two emit sites the rows above already table | **no SyntaxError anywhere**. What happens next is Slice 5's business and changes with it — today the dispatch stub answers `undefined` / `false` (the `expr_member.rs:44` row above) — but the reference is never *rejected*, so a `#missing` in a branch that never executes is never diagnosed at all, where the spec requires an early error. ECMA-262 **§15.7.7** Static Semantics: AllPrivateIdentifiersValid returns false for `MemberExpression : MemberExpression . PrivateIdentifier` when `names` does not contain the StringValue of the PrivateIdentifier, and likewise for the `CallExpression . PrivateIdentifier`, both `OptionalChain` and `RelationalExpression : PrivateIdentifier in ShiftExpression` productions; its `ClassBody : ClassElementList` production is what supplies the names — "Let newNames be the list-concatenation of names and the PrivateBoundIdentifiers of ClassBody". The rules that **check** it are **§16.1.1** Static Semantics: Early Errors, `ScriptBody : StatementList` ("It is a Syntax Error if AllPrivateIdentifiersValid of StatementList with argument « » is false unless the source text containing ScriptBody is eval code that is being processed by a direct eval") and **§16.2.1.1** Static Semantics: Early Errors, `ModuleBody : ModuleItemList` (the same rule with no eval carve-out). ⚠ **Not §15.7.1** — the class-body early errors do not mention the SDO at all (`webref body ecma262 sec-class-definitions-static-semantics-early-errors \| grep -c 'AllPrivateIdentifiersValid'` → **0**); §15.7.7's own `ClassBody` production is where a class body enters the algorithm. §-numbers from `webref aoid ecma262 AllPrivateIdentifiersValid` and `webref heading ecma262 16.1.1` / `16.2.1.1`; step text from `webref body ecma262 sec-static-semantics-allprivateidentifiersvalid` / `sec-scripts-static-semantics-early-errors` / `sec-module-semantics-static-semantics-early-errors`. Cross-layer (parser + scope analysis) and outside `compiler/`, so §1.0's derivation does not produce it: recorded here under this section's per-slice-sweep rule and owned by its own §8 slot. **Derived from the arms and the scope pass; not probe-measured** | T1 | `#11-vm-private-name-early-errors` |

**Verified NOT defects** (checked during the sweep, no action): `delete x` **is** correctly gated by
the parser ("Cannot delete an unqualified identifier in strict mode") — so `expr_ops.rs:156-159` is
unreachable for identifiers, contrary to a round-2 review claim; `with` → `CompileError` (arm head `stmt.rs:190` at `f7d9b5ce`, `:88` at `658cc302` — the file was
split; the `:192` this line used to carry was the message-string line, `:90` at `658cc302`)
is correct per I-6/ADR #2; `{[1n]:…}` and `{[/a/]:…}` computed keys are correct.

### §2.3 Stub inventory — Layer B (dispatch; dead code behind Layer A)

These are I-4 connect-or-delete items, **not** live defects. Each is discharged by the slice that
lands its Layer-A emit (connect), or deleted by the dead-opcode sweep (Slice D, adopting
`#11-dead-opcode-removal`).

⚠ **The membership test here is "zero compiler emit sites", so this section is not the dispatch-layer
defect inventory.** A handler that *is* emitted to and is wrong belongs in §2.2 — `op_spread_object`
is tabled there — and is found by reading handlers, not by the `compiler/` sweep. §2.2 states the
obligation that puts: the slice that owns a handler reads it against its own parent HEAD and tables
what it finds.

**⚠ Corrected R2 round 3 — the earlier hand-curated "nine" was wrong in both directions.** Enumerated
mechanically over every `Op` variant in `bytecode/opcode.rs` — **129** at `658cc302`
(`git show <rev>:crates/script/elidex-js/src/bytecode/opcode.rs | awk '/^pub enum Op \{/{f=1;next} f&&/^\}/{f=0} f' | grep -cE '^    [A-Z][A-Za-z0-9]*( = [0-9]+)?,$'`;
the **125** that stood here was last true at `f627a3f6`, i.e. already stale at this document's own
baseline, which is why the figure is anchored to a revision and not to a date): **18** have zero
compiler emit sites, and **all 18 are actionable**. ⚠ **The `17 actionable + 1 reserved` partition
that stood here is withdrawn** — it exempted `Wide` (see the bullet below); the count is flat again,
and every live site states it that way.

- **Previously listed, confirmed dead (9)**: `CallSpread`, `NewSpread` (`dispatch.rs:888`),
  `TaggedTemplate` (`:896`), `DefineField` (`:942`), `GetSuperProp`, `SetSuperProp`, `GetSuperElem`
  (`:1036`), `CreateClass` (`:931`), `DestructureElem` (`:923`).
- **WRONGLY listed as dead — `GetModuleVar` IS emitted** at `compiler/expr_assign.rs:52`
  (`VarLocation::Module(idx) => fc.emit_u16(Op::GetModuleVar, idx)`). Removing it under Slice D would
  have broken module-binding reads. Also `ImportMeta`/`DynamicImport` are dead, but for the *reason*
  in §2.2 (the compiler folds them into `PushUndefined`), so they are connects owned by the child of umbrella M that implements module bindings, not deletes.
- **MISSING from the earlier list (5)**: `DestructureProp`, `ObjectRest`, `DefaultIfUndefined` — all
  three share the same no-op dispatch arms as the listed `DestructureElem` (`dispatch.rs:925-928`) —
  plus `Debugger` and `SwitchJump`.
- `GetPrivate`/`PrivateIn` **do** have emit sites (1 each) so are *not* dead; `SetPrivate` is — and
  it is why §2.1's derivation strips comments before counting. `SetPrivate`'s **only** occurrence
  under `compiler/` is the comment at `expr_assign.rs:330`, so the count-based form classifies it
  live and yields 17, under-deleting by one; `GetPrivate` (`expr_member.rs:44`) and `PrivateIn`
  (`expr.rs:230`) are the controls — real `fc.emit_u16` sites, which the comment-stripped form
  still keeps out of the set. **Re-derive with §2.1's command**, not with a bare `grep … | wc -l`.
- `Wide` raises a loud `VmError` at **`vm/dispatch.rs:1074`** — re-derived with
  `git grep -n 'Op::Wide' -- crates/`, which returns that line and `bytecode/opcode.rs:562` and
  nothing else; the `:1083` this bullet used to cite is wrong at `658cc302` and at HEAD alike, and
  [§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) carried the same wrong offset. ⚠ **It is swept like the other 17, and the "reserved"
  exemption is withdrawn**: it was stated in prose with no slot id, no owner and no expiry, which
  §8's Forward rule forbids, and it is the opposite of what §2.3 did for `ImportMeta` /
  `DynamicImport` in the identical situation — they got a slot id. CLAUDE.md is
  *「dead code は接続するか削除」* and *「後方互換性は維持しない」*, and nothing pins the discriminant
  (`bytecode/opcode.rs` derives only `Debug, Clone, Copy, PartialEq, Eq` under `#[repr(u8)]`, and
  `git grep -ln 'serde' -- crates/script/elidex-js/src/bytecode/` is empty, so there is no
  serialized bytecode format). The option taken is **delete**, in Slice D, whose row carries the
  mechanical consequences.

Slice D's scope is this corrected enumeration, and it must be **re-derived mechanically at
implementation time** rather than inherited from this list (opcodes gain emit sites as slices land).

Stale "stub" labels on now-real implementations (`SetPrototype`, `AssertConstructor`,
`DefineMethod`, `SuperCall`, `NewTarget`): comment-only cleanup, fold into the touching slice.

### §2.4 Severity taxonomy (drives ordering)

- **T0 CRASH** — process abort on valid JS.
- **T1 SILENT-WRONG** — wrong answer, no error. Strictly worse than a missing global, which at
  least throws `ReferenceError`.
- **T2 LOUD-BROKEN** — throws / total feature loss. Debuggable but unusable.
- **T3 ABSENT** — missing globals; loud `ReferenceError`.

### §2.5 Coupled invariants (edge-dense enumeration — the 1a+1b pair)

Six axes; each **pair's intersection** named:

- **A · Arg-emit form** — flat values + `argc`, or one args Array.
- **B · Call shape** — plain / method / `new` / optional / `super(...)` / `super.m(...)`.
- **C · Iterator protocol** — [C19] spreads via `GetIterator(spreadObj, **sync**)` [C21], drained
  by `IteratorStepValue` [C35]; a user iterator can throw, be infinite, or mutate VM state.
- **D · GC safety** — the args Array is a heap object; unpacking crosses an allocation boundary.
- **E · Inline cache** — `Call`/`CallMethod` carry a u16 call-IC slot; spread opcodes carry none;
  `New` has **no IC at all**.
- **F · Exception unwind** — stack-depth restore on a mid-drain throw.

| Pair | Intersection (a decision, not an assumption) |
|---|---|
| A×B | Each call shape needs both forms ⇒ one spread opcode per shape. `CallMethodSpread` does not exist — the gap forcing a new opcode (§9 decision 2). |
| A×C | The Array form *is* where the iterator drain happens. Flat must never run an iterator; Array always must, even for `f(...[])`. |
| A×E | A site switching to the Array form must not allocate an unused IC slot. **Resolved** ([§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md)): `alloc_call_ic_slot` is a monotonic counter and the table is indexed by the bytecode operand — desync is structurally impossible; skip allocation on the Array path. |
| A×F | **Corrected R2r2**: once `lay_out_call_args` succeeds, the Array path leaves the *same* `argc` slots as Flat; the 0-or-1-slot asymmetry exists only for a failure *inside* the helper. The three new handlers must still pick one of the two in-tree error-exit disciplines ([§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md)). |
| B×C | Optional-call short-circuit: `nullish?.m(...a)` must **not** evaluate or drain the operand ([C37] step 3 returns before [C38] ChainEvaluation). Iterator side effects are observable. |
| B×D | **Corrected R2r2**: under `lay_out_call_args` the array is popped *before* `do_new` allocates the instance (`vm/ops.rs:766`), so the two are never simultaneously live-and-needed. The real B×D case is the generator/async callee window ([§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) GC). |
| B×E | `New` has no IC dimension; only `Call`/`CallMethod` do. |
| C×D | **The drain is rooted by construction** (`op_array_spread` `peek`s, so the array stays on `vm.stack`, itself a GC root). The **unrooted window is pop → re-push**: no JS allocation may occur inside it. |
| C×F | [C20] step 3 runs ArgumentListEvaluation **before** the step 4/5 callability checks ⇒ a non-callable callee must still drain the iterator to completion before throwing. **[C19] does NOT call `IteratorClose`** — verified 2026-07-26, `body ecma262 sec-runtime-semantics-argumentlistevaluation \| grep -ci iteratorclose` → **0**; `?` propagates the abrupt completion directly, and the drain's own AO already sets `[[Done]]` on throw: it calls **§7.4.10 IteratorStepValue** [C35], whose **step 4.a** sets it on an `IteratorValue` throw, and whose step 1 delegates to §7.4.9 IteratorStep — which sets it at **step 3.a** for an `IteratorComplete` throw, its own step 1 delegating further to §7.4.6 IteratorNext for the `next()` throw. *(An earlier draft credited §7.4.9 alone, which is not the AO invoked here.)* Calling `return()` here would be an *observable divergence*. (Contrast [C39] DestructuringAssignmentEvaluation → 6 `IteratorClose` call sites, which is why [C36] falls to the child of umbrella 0bc that owns the destructuring-assignment `IteratorClose` conformance, not to Slice 1.) |
| D×E | (none — IC slots are compile-time indices, not heap refs.) |
| D×F | The unwind path must not leave the args Array reachable only from a dropped Rust local. |

≥3 intersecting axes ⇒ **edge-dense**, which is the criterion's own antecedent and the only half this
section states; what follows from it is §5's, at the head of that section and on the row. Applied to
the rows this section had already flagged (0b/2/3/5/6, plus 0c for
the three substitution classes × 6 compiler files × a deliberate user-visible behaviour change per
dec. 5, plus **P** for a convention sweep spanning `compiler/`, core `vm/` and `vm/host/`): each sub-slice §5
mints carries its own coupled-invariant enumeration in its own memo. ⚠ **Which rows are terminal and
which are umbrellas is not restated here** —
§5's rows are that field's single home, and the list that stood in this sentence went stale twice
(first naming 5 terminal after its row became an umbrella, then agreeing with the table in a copy that
would rot on the next re-split). 0a and D are narrow enough
to skip. ⚠ **Withdrawn for both ([§18](2026-07-vm-p4-slice-0a-landing-record.md)).** 0a's is moot — it shipped, and the measured axis count was
**6**. **D is re-adjudicated here rather than left between two contradicting sites: it needs its own
plan-review.** Its charter is to *delete* opcodes on the strength of a re-derived §2.3 set, and §2.3
records that the previous enumeration was wrong in both directions — it would have deleted a live
opcode (`Op::GetModuleVar`). A deletion slice whose input is an enumeration with a known failure
history is not narrow.

---

## §3. Spec coverage map

Scope = **the 1a+1b pair** (per-PR split tagged below). [C19] is an SDO defined piecewise over **8 productions** (prose read via
`.claude/tools/webref body ecma262 sec-runtime-semantics-argumentlistevaluation`, 2026-07-26); 3 are
TemplateLiteral productions that route the *same* SDO into tagged templates and are therefore
listed here as explicit out-of-scope rows with a hand-off to the child of umbrella 4a that owns the tagged-call lowering (I-3 requires the child of umbrella 4a that owns the tagged-call lowering reuse this
slice's helper, and `ArgsForm` as specified cannot express `« siteObj »` ++ substitutions — that
constraint is recorded now rather than discovered in Slice 4a).

| Spec section | Step | Branch | Touch (compile/dispatch site) | Full enum? | User-input flow |
|---|---|---|---|---|---|
| ECMA-262 §13.3.8.1 ArgumentListEvaluation | production | `Arguments : ( )` — empty list | `compile_call_arguments` (NEW) → `Flat(0)` | ✓ | yes |
| ECMA-262 §13.3.8.1 ArgumentListEvaluation | steps 1-3 | `ArgumentList : AssignmentExpression` | `Flat(n)` path | ✓ | yes |
| ECMA-262 §13.3.8.1 ArgumentListEvaluation | steps 1-5 | `ArgumentList : ... AssignmentExpression` — leading spread | `Array` path → `Op::CreateArray` + `Op::ArraySpread` | ✓ | yes |
| ECMA-262 §13.3.8.1 ArgumentListEvaluation | steps 1-4 | `ArgumentList : ArgumentList , AssignmentExpression` | `Op::ArrayPush` / `Flat` | ✓ | yes |
| ECMA-262 §13.3.8.1 ArgumentListEvaluation | steps 1-4 | `ArgumentList : ArgumentList , ... AssignmentExpression` — trailing/multiple spread | `Op::ArraySpread` after prior pushes | ✓ | yes |
| ECMA-262 §13.3.8.1 ArgumentListEvaluation | production | `TemplateLiteral : NoSubstitutionTemplate` | — | n/a (out of scope → the child of umbrella 4a that owns the tagged-call lowering) | yes |
| ECMA-262 §13.3.8.1 ArgumentListEvaluation | production | `TemplateLiteral : SubstitutionTemplate` | — | n/a (out of scope → the child of umbrella 4a that owns the tagged-call lowering) | yes |
| ECMA-262 §13.3.8.1 ArgumentListEvaluation | production | `SubstitutionTemplate : TemplateHead Expression TemplateSpans` | — | n/a (out of scope → the child of umbrella 4a that owns the tagged-call lowering) | yes |
| ECMA-262 §13.3.6.2 EvaluateCall | step 1.a.i | `GetThisValue(thisValueRef)` — property-reference receiver | `Op::CallMethodSpread` (NEW) receiver slot | ✓ | yes |
| ECMA-262 §13.3.6.2 EvaluateCall | step 2.a | not a Reference Record → `thisValue` = `undefined` | `Op::CallSpread` (plain-call shape) | ✓ | yes |
| ECMA-262 §13.3.6.2 EvaluateCall | step 3 | argList = ArgumentListEvaluation — **before** callability checks | the A×C / C×F intersections | ✓ | yes |
| ECMA-262 §13.3.6.2 EvaluateCall | step 4 | `func` not an Object → TypeError | existing `Call` dispatch | ✓ | yes |
| ECMA-262 §13.3.6.2 EvaluateCall | step 5 | `IsCallable(func)` false → TypeError | existing `Call` dispatch | ✓ | yes |
| ECMA-262 §13.3.6.2 EvaluateCall | step 7 | `Call(func, thisValue, argList)` — the invocation | all three new handlers | ✓ | yes |
| ECMA-262 §13.3.6.2 EvaluateCall | step 1.b.iii | `refEnv.WithBaseObject()` — reachable only via `with` | — | n/a (out of scope per I-6) | no |
| ECMA-262 §13.3.5.1.1 EvaluateNew | step 4.a | argList = ArgumentListEvaluation | `Op::NewSpread` | ✓ | yes |
| ECMA-262 §13.3.5.1.1 EvaluateNew | step 5 | `IsConstructor` false → TypeError (**after** step 4.a) | `do_new` | ✓ | yes |
| ECMA-262 §13.3.5.1.1 EvaluateNew | step 6 | `Construct(ctor, argList)` — newTarget omitted | `do_new` | ✓ | yes |
| ECMA-262 §7.3.14 Construct | step 1 | newTarget defaults to `ctor` | `new.target` propagation (edge 13) | ✓ | yes |
| ECMA-262 §7.4.4 GetIterator | steps 1-2 | `kind = sync` selects `GetMethod(obj, %Symbol.iterator%)` | `Op::ArraySpread` (existing) | ✓ | yes |
| ECMA-262 §7.4.4 GetIterator | step 3 | `method` undefined → TypeError (non-iterable) | `Op::ArraySpread` (existing) | ✓ | yes |
| ECMA-262 §7.4.10 IteratorStepValue | steps 1-5 | per-element drain | `spread_iter_loop` (existing) | ✓ | yes |
| ECMA-262 §7.4.11 IteratorClose | steps 1-8 | abrupt completion mid-drain | — | n/a (**not reached from [C19]** → the child of umbrella 0bc that owns the destructuring-assignment `IteratorClose` conformance) | yes |
| ECMA-262 §13.3.9.1 Evaluation (optional chain) | step 3 | nullish → return `undefined` **before** ChainEvaluation | optional-call short-circuit (edge 12) | ✓ | yes |
| ECMA-262 §13.3.9.2 ChainEvaluation | step 3 | `OptionalChain : ?. Arguments` — optional **call** `f?.(...a)` → EvaluateCall. ⚠ **Not universal (Codex R2)**: for `o.m?.(...a)` the callee is still a *property Reference*, so §13.3.6.2 EvaluateCall step 1.a.i must pass `o` as `this`. `compile_optional_chain_expr` (`expr_member.rs:146`) compiles the `Member` base **as a value** and selects `CallMethod` only when a preceding member sits inside `chain`, so this spelling has **already lost its receiver at `658cc302`** — lowering it to `CallSpread` preserves the silent-wrong result. Needs a member-reference-base call shape plus edges for `o.m?.(...a)` and `o?.m?.(...a)` | `Op::CallSpread` (**+ a receiver-preserving form**) | ✓ | yes |
| ECMA-262 §13.3.9.2 ChainEvaluation | step 6 | `OptionalChain : OptionalChain Arguments` — optional **method** call `o?.m(...a)` → EvaluateCall | `Op::CallMethodSpread` (`expr_member.rs:184`) | ✓ | yes |
| ECMA-262 §13.3.7.1 Evaluation (`super`) | `SuperCall` step 4 | ArgumentListEvaluation (spread variant) | `Op::SuperCallSpread` — already correct, folded onto helper | ✓ | yes |
| ECMA-262 §13.2.4.1 ArrayAccumulation | steps 1-4 | `SpreadElement : ... AssignmentExpression` — `GetIterator(…, sync)` + `IteratorStepValue` drain, **no `IteratorClose`** | **[the child of umbrella 1a that removes `op_array_spread`'s `return()`]** `op_array_spread` (dec. 13a removes the non-spec `return()`) | ✓ | yes |
| ECMA-262 §13.2.4.1 ArrayAccumulation | production | `Elision : ,` / `Elision : Elision ,` — array-literal **holes** | `expr.rs:105-119` — contrast only, NOT shared (§9 decision 3) | n/a (out of scope) | yes |
| ECMA-262 §13.3.6.2 EvaluateCall | step 6 | `PrepareForTailCall()` | — | n/a (no tail calls in elidex) | no |
| ECMA-262 §13.3.5.1.1 EvaluateNew | steps 1-2 | constructExpr evaluation + GetValue | `expr.rs:89` callee compile | ✓ | yes |
| ECMA-262 §13.3.5.1.1 EvaluateNew | step 3 | `argumentsNode` empty → new empty List | `ArgsForm::Flat(0)` (`new C`) | ✓ | yes |

**Breadth**: K=1 spec (ecma262), **M=32** (verified by `preflight.py` → "total entries (M): 32"). M≥30 ⇒ **⚠ SPLIT-DEFAULT** — the strongest breadth verdict.

**✅ Answered by splitting, not by override** (2026-07-26, user decision — §9 dec. 6). The work is
now the **Slice 1a** half (VM infrastructure) and the **Slice 1b** half (compiler + opcodes + handlers). This map spans
**both**, so M=32 is the *pair's* depth.

**Per-PR recompute** (R2 round 5 — the gate is a per-PR verdict, so a pair-level answer would be
the same author-altitude override dec. 6 disclaims): tagging each row by its Touch column, **1a
owns 5** ([C22] `SpreadElement`, [C21] GetIterator ×2, [C35] IteratorStepValue, [C13] SuperCall)
and **1b owns 20**, with **7** `n/a` shared. So **1b M=20 < 30 ⇒ clears SPLIT-DEFAULT on its own**, and
the 1a half is trivially clear. ⚠ **`1a` and `1b` are both umbrellas from this revision (§5), so neither half's row count is a
per-PR verdict — `1b M=20 < 30` above is a reading of the Touch column, not a terminality finding,
the criterion having returned umbrella for that half on a different axis; 1a's 5 rows are the same
kind of reading** — and the reading is itself the mechanism
partition that umbrella's cell states: [C22], [C21]×2 and [C35] are all the `op_array_spread` drain,
while [C13] is the `op_super_call_spread` handler. Each child recomputes this gate at its own start,
against a row set that is a subset of the 5, so each clears it a fortiori; that is the only sense in
which the figure survives. The split resolves the gate on both sides, not just on the pair. The points below are retained because they explain why this map is deep rather than broad,
and why a **call-shape** split stays forbidden (I-3) — but the gate itself is satisfied structurally.

**Why the map is deep, not broad.** Three points, in order of weight:

1. **The invariant argument (§9 decision 6) is what carries this, not the ratio.** Every candidate
   seam reintroduces an umbrella invariant — VM-first ships 3 dead opcodes (I-4), compiler-first
   emits into stubs (I-1), shape-split is the strangler form that caused the bug (I-3). A breadth
   metric cannot override a structural "must change together".
2. **M here measures coverage *depth on one algorithm*, not scope breadth.** K=1: every row is
   ECMA-262, and 25 of 32 rows are the single call-argument evaluation path viewed through the five
   call shapes. **7 rows are explicit `n/a` out-of-scope hand-offs** (3 TemplateLiteral → the child of umbrella 4a that owns the tagged-call lowering,
   [C20] steps 1.b.iii and 6, [C36] → the child of umbrella 0bc that owns the destructuring-assignment `IteratorClose` conformance, [C22] contrast) — i.e. rows that document what this
   slice does *not* do.
3. **The metric moved for the right reason and should not be gamed.** M rose 23 → 27 → 31 purely
   because rounds 2-4 *added* honest step-level and `n/a` rows; the implementation scope never grew.
   Deleting the `n/a` rows would drop M below the threshold while making the plan strictly worse, so
   the count stays and the gate is answered on substance.

(R1/R2 stated "19 of 23" and R3's draft "21 of 27"; both were stale the moment rows were added —
corrected here, and the ratio is explicitly *not* the argument.)

### §3.1 User-input touch audit

- `compile_call_arguments` (NEW): argument-list shape is user-controlled (count, spread positions).
- `Op::CallSpread` / `Op::NewSpread` (existing, stub → real): args Array contents and callee are
  user-controlled; the spread operand may be any object with `@@iterator`.
- `Op::CallMethodSpread` (NEW): additionally the **receiver** is user-controlled.
- `Op::ArraySpread` (existing, unchanged): already runs a user iterator — this slice **increases its
  reachability** (previously only array literals and `super(...)`), so its throw/GC behaviour is
  newly on the plain-call path. Exposure change: **increased, deliberately**.
- Adjacent pre-existing lax surface: `compile_arguments`'s `assert!(arguments.len() <= 255)`
  (`expr_member.rs:60-64`) is an I-2 violation on user input (a 256-argument call aborts the
  process). **In scope** — this slice rewrites the function.

---

## §4. Cross-cutting design invariants

**I-1 · No silent stub.** Every unimplemented opcode or compiler emit path either is implemented or
raises a **loud, scoped runtime error**. The banned set is **all three *substitution* classes** (§2.1 also names
`assert!`, which belongs to I-2, and this list adds a fourth, *wrong arity*) — R2 round-2 review caught that scoping it to `PushUndefined` alone would have left out
exactly the class that produced the §1.2 retraction:

1. **`PushUndefined`** for unimplemented syntax (`expr.rs:200-207`, `:237-239`, and
   **`stmt_destructure.rs:117-119`** — the object-pattern key catch-all, added at PR-B);
2. **silent no-op** — emitting nothing, or only the operand's side effects
   (`expr_assign.rs:210-212`, **`expr_assign.rs:202-206`**, `expr_ops.rs:261`, `expr_ops.rs:226`,
   `expr_ops.rs:249`, `expr_class.rs:428`, `expr_class.rs:430-447`, `expr.rs:186-189`,
   **`stmt.rs:99-103`**, `stmt.rs:877-878`, `stmt.rs:882`, the
   `ImportDeclaration`/`ExportDeclaration` half of `stmt.rs:31-39`, the `FunctionDeclaration` half of that same arm (`stmt.rs:31-39` + `mod.rs:82` + `expr_function.rs:167` — a block-nested declaration reaches the no-op arm and neither hoist loop reaches it), `stmt.rs:424-439` (the destructured catch parameter — no `PatternKind` arm but `Identifier`, and an unconditional `Op::Pop` after it), and — both added at PR-B —
   **`expr_object.rs:26-39`** (the accessor-key `_ => { fc.emit(Op::Pop); }`) and
   **`stmt_destructure.rs:149-150`** (the rest-exclusion `_ => {}`, whose *observable* is a wrong
   **value** rather than a missing effect — the destructured key survives into the rest binding.
   Listed here anyway: the banned thing is the emit shape, and typing it by its observable is how it
   dropped out of this list in the first place);
3. **wrong constant** — a plausible-looking substitute value (`expr_object.rs:117-121` empty-string
   key; `expr_ops.rs:147` `Pop; PushTrue` for `delete this.#x`);
4. **wrong arity** — the right *kind* of value in the wrong *count* (`expr_member.rs:70-75`, the
   call-spread defect itself). Named for completeness; owned by the child umbrella 1b's derivation mints for the argument-emit helper, not 0ca, since making it
   loud would break every `f(...a)` site that a working implementation is about to fix.

The class-1 and class-2 lists above are the §2.2 table's class-1 / class-2 rows, kept in sync
deliberately — R2 round 3 caught them as a strict subset, and PR-B's four new rows reproduced
exactly that, which is how an arm silently drops out of 0ca's remit.

**Slice 0ca discharges all three program-wide up front** (§5) — with **three** carve-outs, not one:

- **the two early-SyntaxError sites**, which §2.2 assigns to **0ba** because a runtime throw
  is the wrong remedy for a parse-time rejection (§9 dec. 15): `expr.rs:186-189` prefix `Spread`
  (class 2) and `expr_ops.rs:147` `delete this.#x` (class 3);
- **the five `PropertyKey` rows §2.2 routes to that slot** (`expr_object.rs:26-39`, `expr_object.rs:117-121` (the class-3 `{1n:'x'}` empty-string key, whose arm sits inside the same `PropertyKind::Init` match the collapse absorbs the others into),
  `stmt_destructure.rs:117-119`, `stmt_destructure.rs:149-150` and the `expr_class.rs` dead
  `if computed` guard), which §2.2 routes to **a child `#11-vm-property-key-lowering-unification` cannot mint while its kind is UNDETERMINED (§8)** — and the
  `expr_class.rs` `_ =>` arms to **the child of umbrella 2 that owns class-member key lowering** — because making four divergent catch-alls individually
  loud entrenches the duplication the collapse exists to remove (CLAUDE.md *One issue, one way*);
- **class 4 (wrong arity)**, carved in item 4 above (the child umbrella 1b mints for it, not 0ca).

Its scope is derived by a documented
sweep, not by inspection — see §5 Slice 0ca and §9 decision 9. §9 decision 5 records the
throw-vs-`CompileError` choice.

**I-2 · `assert!` is not rejection** — *for the refusal-of-user-constructs class only*. A compiler
arm that rejects a **user-writable construct** must never `assert!`/`panic!`/`unreachable!`
(`expr_assign.rs:170`, `expr_member.rs:60`). **The remedy is `Op::ThrowUnsupported`, a scoped
runtime throw — NOT `CompileError`** (dec. 5, ratified by evidence in [§17](2026-07-vm-p4-slice-0a-landing-record.md)/[§18](2026-07-vm-p4-slice-0a-landing-record.md)): I-1 asks for loud
*and scoped*, and `CompileError` yields no bytecode for the whole script, so it fails every
unrelated statement too. **That reason is not bound to this invariant's class**: it holds for any arm
whose `CompileError` stands in for a failure ECMA-262 evaluates at run time, whether or not the arm
ever asserted — §2.2's `compiler/expr_assign.rs:80-88` row is the instance, and it is owned by a slot
rather than by I-2. `CompileError` stays correct only where the compiler *already* rejects, and
for parse-time rejections (dec. 15). Earlier drafts of this invariant said "must produce
`CompileError`"; that contradicted dec. 5 and Slice 0a shipped the contradiction before the gate
caught it. **Explicit carve-out**:
asserts that encode **ISA/bytecode-format invariants** (jump-offset and operand-width bounds —
`compiler/function.rs:194/222/239`, `stmt.rs:808` — all genuine i16/u16 range asserts) **stay as
`assert!`** per
[[feedback_compiler_asserts_are_isa_invariants]], which directs that "assert → Result" findings on
those sites be declined with a pointer to that memo. R2 round-2 review flagged that some of these
*are* user-JS-reachable (a >32 KB function body); that is a known, ratified position, not a defect.
⚠ *This invariant used to close by naming `expr_assign.rs:102`
(`unreachable!("assignment to import binding")`) as becoming reachable with Slice M and Slice M's to
convert. **Slice 0a already converted it**: `grep -c 'unreachable!' compiler/expr_assign.rs` → 0 at
`658cc302`, and the site is `emit_unsupported(fc, "assignment to an imported binding is not
supported")` at `:104`. The child of umbrella M that owns this arm inherits no conversion here — only
the implementation.*

**I-3 · One argument-emit path.** All call shapes route through **one** helper that decides
flat-vs-array; the tagged-template arg list of the child of umbrella 4a that owns the tagged-call lowering routes through the same helper. **Carve-out
(spec-mandated, R2 round 5)**: the synthesized default derived constructor
(`compiler/expr_class.rs:145-152`) must NOT be folded — ECMA-262 §15.7.14 step 14.a.iv.1 NOTE requires it
not to observably call `%Array.prototype%[@@iterator]`, which the shared path would. See [§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md). The direct lesson
of the present bug: `super` got a correct hand-written spread branch while the other four
`compile_arguments` callers silently shared a broken one.

**I-4 · Connect or delete.** All **18** zero-emit opcodes (§2.3 — rebuilt mechanically in
round 3; the earlier "nine" was wrong in both directions) are each either connected by the
slice that lands their Layer-A emit, or deleted by the dead-opcode sweep. §5 names an owner for
every one — no unowned violations, and **no named exception**: the `Op::Wide` carve-out that stood
here had no slot id, no owner and no expiry, which §8's Forward rule forbids, so `Wide` is in Slice
D's delete scope like the rest (§2.3).

**I-5 · Layering.** Two directions, and the boundary is the **`engine` feature gate + host-binding
dependency**, not directory name (R2 round-2 correction — `vm/gc/keepalive.rs`, `vm/host_data/`,
`vm/sw_thread.rs`, `vm/worker_thread.rs`, `vm/webidl_sequence.rs`, `vm/wasm_payload.rs` all sit
outside `vm/host/` yet are engine-bound):

- **Outbound**: no slice may add *language semantics* to engine-bound code (`vm/host/` and the
  `#[cfg(feature = "engine")]` surface above).
- **Inbound**: no slice may pull *engine-bound responsibility* (network / HTML fetch / DOM binding)
  into core.
- ⚠ **The inbound rule has no membership test, and core dispatch already carries host knowledge.**
  Measured at `658cc302`: `vm/dispatch_objects.rs:404`/`:415` (the `in` operator special-casing
  `DOMStringMap` / `Storage`) and `vm/dispatch_iter.rs:121`/`:132` (for-in named-property exotic
  keys) call `host::dataset::*` / `host::storage::*` from core. I-5's consequence #2 then directs
  the children of umbrella **10a** (its `Reflect` half) and the children of umbrella **10b** (its `Proxy` half) to
  resolve `Proxy`/`Reflect` × `ObjectKind::HostObject` *inside* core dispatch, i.e. to add more. Whether those are admissible marshalling or inbound violations is undecided here; the
  outbound rule got a Converse precisely because a literal reading stops at the wrong place, and
  the same is available in this direction. **The test is settled once, by the slot
  `#11-vm-inbound-host-call-membership-test` (§8), and both consumers inherit it**: that slot owns the
  membership test for what core `vm/` may call in `vm/host/`, and the audit of the four sites measured
  above under whatever test it adopts. ⚠ **Neither consumer can carry it in a `Deps` cell, both rows now being umbrellas and an umbrella named in a `Deps` cell naming nobody** — placing the edge on the children that resolve `ObjectKind::HostObject` inside core dispatch is a mandatory output of each umbrella's derivation, which both rows record.
  ⚠ *Settling it per surface — 10a's mandatory plan-review for the `Reflect` half, 10b's for the
  `Proxy` half, whichever ran first auditing the four sites and the second inheriting that test — is
  **withdrawn**. The two rows carry no edge between them, so their plan-reviews may run concurrently:
  "whichever runs first" orders nothing, and both could classify the same four host calls differently.
  A core/engine boundary is project-wide, not a per-surface choice.* ⚠ *Both rows becoming umbrellas strengthens that withdrawal rather than reopening it: each half is now several children with several mandatory plan-reviews, so a per-surface settlement would have to converge more reviewers on the same four sites, not two.* Assigning the whole
  question to 10b alone let 10a's children land, add `Reflect` × `ObjectKind::HostObject` special cases to core
  dispatch, and retire with the contract still unmade — the decision is not available at the time the
  work is done, so "coordinate with the other row" cannot supply it.
- ⚠ **The Converse below is contradicted by `vm/host/mod.rs:11-13`'s own layering mandate**, which restricts everything under that directory to engine-bound responsibilities. Carved as `#11-vm-typed-array-family-layering-and-gate` (§8) rather than resolved here — it is a relocation/gating decision, not a wording fix.
- **Converse (added R2 round 5)**: engine-**gated** is not the same as engine-**bound**. Files under
  `vm/host/` that implement pure ECMA-262 language surface (`vm/host/typed_array_*.rs` = ECMA-262
  **§23.2 TypedArray Objects**, the *family* — **not** §23.2.2 "Properties of the %TypedArray%
  Intrinsic Object", the static-method subsection alone, which is what this sentence used to say;
  §-number verified via `webref heading ecma262 23.2`, and §23.2.2.1 `%TypedArray%.from` is the one
  member this clause actually reasons about, at `typed_array_static.rs:798`, an `iter_close` caller;
  and the
  iterator-protocol call sites in `vm/host/{url_search_params,headers/parse_init}.rs` — ⚠ **round-8
  correction: these two are NOT "pure ECMA-262"**; with `vm/webidl_sequence.rs` they implement
  **WebIDL §3.2.21.1** "Creating a sequence from an iterable", which has *no* `IteratorClose` step,
  so they take the same two-layer disposition as their shared helper) are **language-semantics sites the core
  convention owns** — a core-wide convention sweep *must* reach them, and doing so is
  not an outbound violation. ⚠ **The parenthetical here named "the child of umbrella P that owns the `vm/host/` `iter_close` call sites", and it was wrong three ways at once.** It keyed a child's charter on a **directory**, which the paragraph above this one rejects by name — the boundary is the `engine` feature gate plus host-binding dependency, *not* directory name. It resolved to no single child: §8's `#11-vm-typed-array-family-layering-and-gate` partitions these very sites by governing spec, putting `typed_array_static.rs` under **Pa** and `url_search_params.rs` / `headers/parse_init.rs` / `structured_clone.rs` under **Pc**, so the definite description spanned two already-minted children and named neither. And the sites this sentence is about are wider than any `vm/host/` charter — `vm/webidl_sequence.rs` is core `vm/`, which is why the sentence calls the sweep *core-wide*. The parties are named by the membership test this section already states — *which spec governs the algorithm whose completion is being closed?* — so ECMA-262 **§7.4.11** puts the `%TypedArray%.from` site on **Pa**, and WebIDL **§3.2.21.1**, which has no `IteratorClose` step, puts the sequence-creation sites on **Pc** together with their core helper. Without this clause an implementer reading the outbound rule literally
  would stop at the core sites and ship the split-contract One-issue-one-way failure the sweep
  exists to remove. ⚠ **Slice 0a already spent part of this disposition** — it touched
  `vm/host/{headers/parse_init,structured_clone,url_search_params}.rs`, re-attributed their governing
  sections (WebIDL §3.2.27 → §3.2.21 / §3.2.21.1, §3.10.21 → §3.2.15) and registered
  `#11-webidl-sequence-dense-array-fast-path` at three in-code sites. P inherits a partly-made
  decision, not a blank one — see [§16](2026-07-vm-p4-slice-0a-landing-record.md).
  ⚠ *A correction bullet used to sit above this Converse re-pairing each `typed_array_*` file with
  its own §-number. It is **dropped, not re-derived**: `ls vm/host/ | grep typed_array` gives seven
  files, the bullet paired three, and one of the three was wrong —* `typed_array_parts.rs`*'s only
  §23 citation is `:84` and is **§23.2.4.4 ValidateTypedArray**, not the §23.2.3 it was paired with.
  An annotation layered over a normative sentence is the pattern this document is removing; the
  sentence itself now says §23.2.*
  **Two-layer cases needing explicit disposition in Pa's and Pc's memos**: `vm/webidl_sequence.rs`
  (governed by WebIDL §3.2.21) and **`vm/host/structured_clone.rs`** (WHATWG HTML **§2.7.4** StructuredSerialize **fused with §2.7.6
  StructuredDeserialize**, per the entry point's own docstring at `:63-64` and row F's classification
  (§2.7.7 StructuredSerializeWithTransfer for the transfer-list path where the `iter_close` at
  `:1062` sat — `:1064` at `658cc302`. ⚠ **Codex R1 corrected the attribution of that site**: it is
  inside `ensure_empty_transfer_list`, whose own docstring calls it a WebIDL `sequence<object>`
  conversion throwing per **WebIDL §3.2.21 step 3**, and its `iter_close` runs while consuming the
  iterable — *before* HTML StructuredSerializeWithTransfer ever receives the list. So the **site
  stays in the WebIDL half of [§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md)'s 7/5 partition**, and it is the *outer native* that is separately
  HTML §2.7.4 / §2.7.7. Calling the site HTML's own loop would select the wrong `.return()` and
  error-precedence remedy in the P family. The in-code docstring's "§2.9" is drifted; **§5's Slice-Pc
  row owns the retag as a named deliverable** — this clause does not) — ⚠ **the sentence that used to follow here is withdrawn (Codex R3)**: it called the `DataCloneError` an abrupt completion of *HTML's own loop*, contradicting the correction directly above and leaving [§6.2a-2](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) enumerating only four non-ECMA sites. `ensure_empty_transfer_list` is the **WebIDL** conversion; only the outer native is HTML §§2.7.4/2.7.7, and the WebIDL count must follow from that. Superseded text: its abrupt was described as a `DataCloneError` thrown by HTML's own loop body, so a
  step-5 flip changes an HTML-defined error surface, not an ECMA-262 one; round 6 corrected an
  earlier mis-classification of this file as pure ECMA-262).

  ⚠ This clause is currently an **allowlist, not a predicate** — state the membership test
  ("which spec governs the algorithm whose completion is being closed?") and **re-derive the file
  list from it at implementation time**, per [§13](2026-07-vm-p4-umbrella-review-rounds.md)'s enumerate-by-command rule. This binds the children umbrella **M**'s derivation mints — module-script *fetching* is an ECMA-262 host hook and must go
  through a host seam, not the core VM; the renderer holds no direct network access (CLAUDE.md
  "Security by structure"). Slice M's memo must draw that line before implementation.

Two named consequences: the WeakMap/WeakSet ephemeron work of the child of umbrella **7b** that carries it — that row is not a landable slice, so placing this obligation on a specific child is a mandatory output of its own derivation — lands in `vm/gc/trace.rs`
alongside engine-gated wrapper-store rooting and the keepalive predicate
(`vm/gc/collect.rs:1149→1235→1341`), and must define identical semantics for non-`engine` builds;
and the `Proxy`/`Reflect` × `ObjectKind::HostObject` resolution (`vm/object_kind.rs:272`) inside core
dispatch splits by surface, because the row that used to carry both halves became an umbrella. The
`Reflect` half is **umbrella 10a**'s — `Reflect` is that row's surface, but the row became an
umbrella at R77 (thirteen §28.1 functions, independent mechanisms), so the obligation falls to
whichever child its derivation mints for the `HostObject` routing rather than being stated on the
row directly. The `Proxy` half falls to the children of umbrella **10b** that
reach that variant; an umbrella lands no PR, so placing the obligation on the specific child is a
mandatory output of its derivation, exactly as for 7b above. A host-side special case would violate
the outbound rule in either half.

**I-6 · No sloppy/Annex B surface.** No slice introduces sloppy-mode or Annex B behaviour
([[reference_elidex-js-core-strict-only]]). **Note**: this does *not* gate **strict direct `eval`** — per
`docs/design/ja/14-script-engines-webapi.md` §14.1.1 that is **core**. ⚠ **What §14.1.1 states is what
this PR amends it to state** — the reconciliation §8 records as done here rather than deferred: its
criterion was caller-scope injection ("only sloppy `eval`'s caller-scope injection is LegacySemantics",
the reading this note carried), and it is now the strictness of the **evaluation** across all three
dynamic-code forms — one criterion, though the ⚠ sentence below measures that it is *derived* from
`strictCaller` as well as from the source on the direct path and from the source alone on the other
two, so the citation above and the split below name one criterion and not two. ⚠ **But this note read "`Function`/`eval`" and so put the
whole language half in core, which introduces exactly the sloppy surface this invariant forbids.**
⚠ **Superseded — the ⚠ sentence below draws the line, and it is not this one.** Superseded text: two of the three forms were called sloppy *by construction*, not by a directive the caller might omit, on this evidence:
**§19.2.1.1** PerformEval step 1 is `Assert: If direct is false, then strictCaller is also false`, and
steps 12-13 set `strictEval` from `strictCaller` or from the script's own directive — so `(0, eval)("this")`
is **sloppy**, no matter how strict the caller; and **§20.2.1.1.1** CreateDynamicFunction parses the body
and hands it to `OrdinaryFunctionCreate` at step 28 with no step forcing strictness, so
`Function("return this")` is **non-strict** absent a directive in the body it was handed (§-numbers from
`webref aoid ecma262 PerformEval` / `CreateDynamicFunction`; steps from
`webref body ecma262 sec-performeval` / `sec-createdynamicfunction`). **`HostEnsureCanCompileStrings`
does not close this** — the host-defined **§19.2.1.2** hook (§9 decision 7's security/CSP surface)
allows or denies *compilation*; it makes neither body strict. ⚠ **But neither form is sloppy *as a form*, so the split cannot be drawn on the spelling.** **§19.2.1.1** **step 13** — `Else, let strictEval be ScriptIsStrict of script` — is reached exactly when `strictCaller` is false, i.e. on the indirect path, so `(0, eval)('"use strict"; …')` runs **strict**; and **§10.2.3** OrdinaryFunctionCreate **step 7** `Let strict be IsStrict(body)` with **step 10** setting `[[ThisMode]]` to strict makes `Function('"use strict"; return this')()` strict as well (steps from `webref body ecma262 sec-performeval` / `sec-ordinaryfunctioncreate`). Sending the whole of either form to LegacySemantics therefore leaves **core with no owner for valid strict dynamic code**, and draws a wider split than CLAUDE.md's own enumeration does — it names *sloppy direct-eval (caller-env scope injection)*, not the constructor and not the indirect form. So the split is on the **strictness of the evaluation**, not on the spelling — ⚠ **and that is not the same as the strictness of the parsed source, which is what this passage said for one revision and which is right for only two of the three forms.** **§19.2.1.1** **step 12** is `If strictCaller is true, let strictEval be true` and **step 13** — the `ScriptIsStrict of script` read — is its `Else`, so on the **direct** path the two are disjuncts and the source's own directive is consulted only when the caller is sloppy: strict core code evaluating `eval("var x = 1")` evaluates a body carrying no directive of its own **strictly**, and a rule reading source strictness alone would route that conforming case to LegacySemantics. The **indirect** path has no second disjunct — step 1's `Assert: If direct is false, then strictCaller is also false` forces `strictCaller` false there — and neither does `Function`, whose **§10.2.3** **step 7** `Let strict be IsStrict(body)` reads the handed body alone. So: **direct `eval` splits on the *effective* evaluation strictness (`strictCaller` or the source's own directive); indirect `eval` and `Function` split on the strictness of the parsed source, which is what effective strictness reduces to on those two paths; the strict half of each is core and the sloppy half of each LegacySemantics** (§-numbers from `webref aoid ecma262 PerformEval` / `OrdinaryFunctionCreate`, steps from `webref body ecma262 sec-performeval` / `sec-ordinaryfunctioncreate`). Core's dynamic-code surface is then a *subset* of the standard one and conformant on every evaluation it accepts, which is what makes it designable rather than arrived at by calling the half core. §5's `Function`/`eval`
pointer row carries that separation and the home of each part; the umbrella
`#11-vm-function-constructor-global` opens states which forms it mints.
⚠ **And what `CLAUDE.md`'s *Core vs compat split* clause states is what this PR amends it to state**
— the same form the §14.1.1 note above takes, and the reconciliation §8 records as done here rather
than deferred. That clause carried two sentences that the split above puts in disagreement: the
first is **motivation-scoped** (「その motivation が plugin 領域 (= 上記 legacy 機能) なら **core slot
化しない**」) and the second a **flat prohibition** (「`FrameKind` / opcode / dispatch path 等を
direct/indirect eval 区別 … で拡張する案は core scope 外」) — and assigning strict-source indirect
`eval` to core requires the dispatch path to tell direct from indirect, which is literally what the
second forbids. `CLAUDE.md` is the repo SSoT, so this document cannot override it; the amendment
**narrows the second sentence to the first's own motivation test** rather than weakening it. An
extension that exists to serve *legacy* semantics — var hoisting, sloppy-mode flags, sloppy
direct-eval's caller-env scope injection — is still rejected by it, and a reader must still reject a
sloppy-motivated `FrameKind`/opcode/dispatch proposal by reading it; what it no longer rejects is an
extension that exists because ECMA-262 **derives strictness differently on the two paths**
(**§19.2.1.1** step 12's `strictCaller` disjunct against step 1's assert). The reason that is a
narrowing and not a hole: when the clause was written the only reason to distinguish the two forms
was sloppy direct-eval's scope injection, and `#11-eval-direct-vs-indirect-flag` was dropped
pre-implementation on exactly that ground ("core = strict-only + new scope なので direct/indirect
区別不要", [[reference_elidex-js-core-strict-only]]). The split above establishes a second reason
that did not exist then, and the amended clause is the one that can tell the two apart.

**I-7 · A representation change is a collector change.** Where object-valued state *lives* is what
roots it, so a slice that moves such state from a frame onto a closure — or adds it to one — owes the
tracing edge and a regression that forces a collection between the store and the read — **and that scopes every other holder out, so the new edge is the object's *sole* root**. That second half is not a refinement of the first: a test that leaves the surrounding constructor, class or object bound in a global or a live local keeps the moved object marked *through that binding*, so it passes under a no-op arm and measures nothing. The shape that works is to build the holder inside an immediately invoked function and keep only the closure — an arrow, an extracted method — then collect, then read through it. Measured at
`7c7c2c61`: the `ObjectKind::Function` arm marks `fo.upvalue_ids` and `fo.captured_this` and nothing
else (`vm/gc/trace.rs:251-258`), which is complete today because those are `FunctionObject`'s only
object-valued fields — `func_id` is a `compiled_functions` index and `name` a `StringId`
(`vm/value.rs:588-599`) — and the match is *exhaustive, no wildcard* by its own statement at
`vm/gc/trace.rs:244` (`grep -c '^\s*_ =>' crates/script/elidex-js/src/vm/gc/trace.rs` → **0**, against
the positive control `grep -c 'ObjectKind::' crates/script/elidex-js/src/vm/gc/trace.rs` → **99** —
⚠ **both carried a bare `vm/gc/trace.rs` and neither reproduced from the repository root**: that path
does not exist there, so the claim side printed a no-such-file warning rather than a zero, and the
control side named no file at all and read standard input). Frames are the other marked location: `mark_roots` marks
`CallMode::Construct`'s `new_target` and `frame.home_class` (`vm/gc/roots.rs:294-297`) and the
suspended-generator arm marks the same two on `susp.frame` (`vm/gc/trace.rs:353-357`) — **4 sites, 2
fields**, the whole of `grep -rn 'home_class\|new_target' crates/script/elidex-js/src/vm/gc/` less its
two doc-comment hits. **So a move is a net *loss* of a strong edge rather than a missing addition**,
and an unmarked slot is returned to `free_objects` by `sweep_objects` (`vm/gc/sweep.rs:22-31`), which
makes the consequence a **recycled `ObjectId`** — a stale id resolving to an unrelated later object —
rather than a leak. Stated over the **edge**, not over a struct field, so it binds a hidden-property
representation (already reached by `trace_storage`) exactly as it binds a new field, and each slice's
own plan-review derives which it is. ⚠ **Invisible to the functional probes**, which is why it is an
invariant and not a per-row note: a reader that runs at *call* time passes every single-call test with
no collection in between. ⚠ **The population is defined by a **predicate**, and a grep is only its seed.**
The predicate: *does this row's deliverable put an `ObjectId` or a `JsValue` somewhere the collector
does not already walk?* Every row answering yes owes the arm and the regression, and each row answers
it at its own plan-review against its own parent HEAD — that answer, not a list here, is what binds.
⚠ **The seed grep is not the inventory, and this clause said it was for one revision.**
`grep -nE '^\| .*(GcRoots|mark_roots|gc/trace\.rs|gc/roots\.rs|gc/collect\.rs|tracing edge|trace arm|root set)' <this file>`
→ **12** rows, measured on blob `04734642` (this file at `c6b5bc31`) and **anchored to a blob because
the seed now counts this document's own prose**. Row **2aa** is the twelfth, and it matches for a
reason that is this clause's own thesis turned back on it: the note 2aa carries in order to
demonstrate that it sat *outside* the collector vocabulary quotes all eight alternatives verbatim,
which put it inside. That same note also quotes the negative control, so `GcRootsXX|mark_rootsXX`
over the same blob returns **2**, which is neither the 0 this clause first asserted nor the **1** that replaced it: the two hits are 2aa's note and the superseded sentence one screen above it, which quoted the same control literal while asserting 0. A seed that counts this document's own prose reaches the *control* exactly as it reaches the pattern, which is why the control is re-derived against the pinned blob by the same `git show 04734642` pipeline and never against the working tree. A control literal the file does
not contain, `GcRootsNONE|mark_rootsNONE`, same pipeline and same blob, returns **0**, so the pattern
still discriminates. The **11** that stood here was the count at `c26d7148`, before 2aa's note landed;
it is not a miscount but a figure whose population moved under it, which is why re-running is
`git show 04734642 | grep -cE '<the pattern above>'` and not the same pattern against `<this file>` —
the latter measures the working tree at the instant it runs, which is how this figure went stale
twice. The twelve: §5 rows **1a**, **2aa**, **3**, **4a**, **C**, **S**, **7**, **7a**, **7b** — nine —
and §8 slot rows `#11-vm-arrow-lexical-new-target`,
`#11-vm-operand-rooting-by-construction`, `#11-vm-typed-array-family-layering-and-gate` — three;
9 + 3 = 12. **It selects
rows that already use collector vocabulary, so it cannot return the rows whose omission is the very
thing this invariant exists to catch** — the out-of-vocabulary positive control proves it: the child umbrella **10b**'s derivation mints for `Proxy`
mandates a representation strongly holding `[[ProxyTarget]]` and `[[ProxyHandler]]` (**§10.5.15**
ProxyCreate) and the child umbrella **6a**'s derivation mints for async generators mandates an async-generator `ObjectKind` carrying suspended state and a
request queue; both answer the predicate yes, neither matches the grep, and neither appeared in the
twelve. **They are named here as failures of the seed, not as the completion of a list** — a list
completed by hand is the curated shape this clause replaced. ⚠ **A negative control shows a pattern is
not vacuous; only a positive control drawn from *outside the pattern's own vocabulary* shows it is not
self-selecting.** Any command offered as an inventory in this document carries both.
The two the earlier curated list omitted remain the illustration of why a list is the wrong shape:
**4a**, whose cell already owed a root field and a forced-collection regression and wrote that
regression *without* the sole-root half; and **C**, which had to write "so I-7 binds this row too"
into its own cell — the restatement this rule's last sentence forbids. For **S**, the predicate is
answered by whichever answer its plan-review gives about the environment link being object-valued;
for **3** and for **7a**, by the child that owns the representation rather than the umbrella — 7a's `[[MapData]]` / `[[SetData]]` variant and its unconditional marking arm sit on the child its derivation mints for them. ⚠ **And a forced-collection regression asserts that the collection was *placed*, not
merely requested** — arm `vm.inner.force_gc_before_next_alloc` and assert it was consumed, the in-tree
pattern at `vm/tests/tests_member_compound_assign_gc.rs:56-62` whose own docstring names the mode this
guards (a case that stops allocating at all, and so passes vacuously). A test that requests a
collection into a window holding no allocation measures nothing, however the code under it is
written. No row restates this rule; a row states only what it owes under it.

**I-8 · An acceptance condition declares its polarity.** Every acceptance condition a row states carries one of three declared readings **at the tree that row's slice is pinned to** — a **red probe**, which answers wrongly there and answers correctly only once the deliverable lands; a **green guard**, which already answers correctly and is stated so that the remedy may not break it; or a **declared control**, positive or negative, whose job is to bound an over-eager fix. The vocabulary is this document's own — "positive control" and "negative control" are already used throughout §5 and §8, and `#11-vm-named-function-expression-binding` already writes one guard out in full ("green today … a guard on the remedy rather than a red probe") — so what this invariant adds is that the reading is **stated**, not that it is invented per row. ⚠ **A condition written in red-probe voice while green at the pinned tree is worse than none**: a reader cannot tell it from a live defect, and a slice retires it without writing a line of the deliverable. Measured at `658cc302` over the ≈95 acceptance conditions this document's own R-loop delta touched, §5 rows **0bc**, **2ab** and **2ac** and §8 slots `#11-vm-object-spread-source-coercion`, `#11-vm-for-in-enumeration-semantics`, `#11-vm-parameter-environment-instantiation`, `#11-vm-arrow-lexical-new-target` and `#11-vm-object-literal-proto-setter` each state at least one condition the tree already satisfies while introducing it as a subtraction an implementation has to fix. ⚠ **That was one dry-run's output over one delta and a *seed*, not an inventory — and the population it seeded has since been measured in full.** The predicate was applied to **every** terminal row of §5 and §8 — the population being the rows the census program leaves unmarked, **75** of them at `d8e799aa`, the revision the sweep ran against (**33** §5 data rows, **42** §8 slot rows), and **61** at this revision because the same sweep converted fourteen of them: **17** state no acceptance because they are merged, pointer, adopted or spent rows, **58** are assessable, **2** pass — §5 row **6c** and §8's `#11-vm-native-function-length-on-allocator` — and **56** fail, each with a named implementation that satisfies every condition it states while leaving the deliverable wrong. Of the 56, **13** are discharged by becoming umbrella rows at this same revision, which carry no acceptance at all; the other **43** state theirs here. ⚠ **The eight rows this seed named are now four**: **0bc**, **2ab** and `#11-vm-parameter-environment-instantiation` are umbrella rows from this revision and carry no condition to mis-declare, and **2ac** discharged its flag by declaring its ordering regression a guard — while the seed *under*-read **2ab**, whose first regression was green for the same reason as its third and was declared vacuous only for the third. No row inherits its polarity from any list; each states its own, and the figures above are re-derived by re-running the sweep rather than read forward. ⚠ **Polarity is a property of the tree, not of the sentence**, so it is re-derived at each row's own mandatory `/elidex-plan-review` against that row's parent HEAD: a condition that is red today is turned green by whatever lands first, which is §5's prerequisite rule seen from the other side. **And the producer half**: a witness reads whatever it names, so where it reads an artifact no landed row mints — a global binding, a constructor, an intrinsic, a build configuration — the row records that derivation, and the child that will mint it, in its `Deps` cell rather than a blank. §5 already says this over slice *deliverables*; it is stated here over *witnesses*, because a row can carry a correct `Deps` cell for what its deliverable consumes and still spell an acceptance on a producer nobody owns — §5 row **8a** is the instance, every one of whose accessor witnesses reads a `RegExp` global that does not exist. No row restates this rule; a row declares its polarity and names the producer it waits on.

**I-9 · A new `ObjectKind` variant is admissible only against a stated criterion.** Lesson #276's
plan-memo control — *is this variant justified by a unique runtime state shape, or is it routable
through a representation that dispatch and the collector already walk?*
([[feedback_objectkind-resolution-uniformity]], PR #198) — is asked **per variant a derivation
mints**, so each umbrella's derivation answers it rather than whichever reviewer happens to notice.
⚠ **This document does reach for #276 once, and on a different question**: §9 decision 2b applies
the variant-vs-data shape to *opcodes*, where the discriminant is a stack shape the emit site knows
statically. Nothing in it reaches the object representation, and four sites here mandate a variant
with no criterion behind them — the async-generator kind the child umbrella **6a**'s derivation mints carrying suspended state and a request
queue and the representation the child umbrella **10b**'s derivation mints strongly holding `[[ProxyTarget]]`/`[[ProxyHandler]]`
(**§10.5.15** ProxyCreate, from `webref aoid ecma262 ProxyCreate`), both of which **I-7** already
names as its seed's out-of-vocabulary positives; the `[[MapData]]`/`[[SetData]]` variant on the
child umbrella **7a** mints for it; and the child `#11-vm-typed-array-family-layering-and-gate`'s derivation mints for the `TypedArray`/`DataView` variants (§8), whose
**second separating observable is stated over variant existence** — that no configuration carries a
`TypedArray` or `DataView` variant without the `ArrayBuffer` variant they point at — with nothing
behind it saying when a variant may exist at all. ⚠ **And #276's second disjunct is not available to
a core-language variant, so citing it unread would mandate an I-5 violation.** Its remedy is spelled
`HostObject` + an ECS-component brand check, which is engine-bound by construction; proposing it for
a language surface is exactly the outbound violation **I-5** forbids. For a core variant the
alternative is a representation the VM already carries — an object's hidden-property storage, an
existing kind's payload — and a row that can name none has *answered* the question rather than
dodged it. **I-7 covers the collector consequence of a representation change and explicitly not this
question**, which is what invites a reader to think it does: a row can owe a tracing arm under I-7,
write it, and still have minted a variant nothing sanctioned. No row restates this rule; a
derivation records, per variant it mints, which disjunct it answers and against what.

---

## §5. Slice plan

Each slice = own PR + own `/elidex-plan-review`.

**Terminality criterion — the rule, quoted from CLAUDE.md § *Design discipline*, "Edge-dense work":**

> ≥3 intersecting invariant axes を束ねる、または正準アルゴリズムが無い subsystem を触る work は
> (a) **単一 PR に束ねない** — umbrella plan + PR ごとの plan に分割し各 PR を個別に full review、
> (b) **各 PR は実装前に `/elidex-plan-review` 必須** (judgment でなく rule)、(c) **base case
> (再帰の終端)** = 承認済 umbrella 配下で plan-review を通った narrowly-scoped per-PR slice は
> terminal 単位 = 許容される単一 PR

Read (c) in the direction it is written: approval makes a **narrowly-scoped** slice terminal. It does
not make a row narrowly scoped, and clause (b) says the mandatory plan-review does not discharge (a).
So the test a row must pass, and the test to apply to any row added later **before** a reviewer
applies it: *does this row bundle ≥3 intersecting invariant axes, **or touch a subsystem with no canonical
algorithm**?* ⚠ **The second disjunct is CLAUDE.md's own, and a paraphrase of it — "span more than one
governing algorithm across layers" — stood here for several revisions.** They are not the same test: a
row introducing **one** novel algorithm in **one** layer satisfies CLAUDE.md's disjunct and fails the
paraphrase, so the paraphrase let such a row read terminal. Where a row's evidence turns on layers
rather than on the absence of a canonical algorithm, it says so in its own cell; the test itself is
quoted, not restated.

⚠ **Retracting the paraphrase left a class behind, and it is larger than the retraction: rows whose
kind is derived from breadth where the criterion asks for independence.** The first disjunct is not a
count of anything — it asks for **≥3 axes that intersect**, and the only evidence that an axis is an
axis is that it **fails while the others pass**. Two mechanisms, two AOs, three
layers, four files, six call sites: every one of those is breadth, and none of them is a measurement
of independence. The second disjunct is the complementary trap in the other direction — it asks
whether the AO that governs the work is **absent from the specification**, so a cell that cites its
governing clause through `webref` has *disproved* its own second disjunct in the act of grounding
itself, and almost every cell here does.

**Two spellings, both measured in this document rather than imagined.** The first substitutes a
synonym for the retracted phrase — *algorithm families*, *governing families* — and inherits its
reading whole. ⚠ *Written out, that phrase would appear here twice more, and §5's head is where the
sweeps for this class take their population, so the count must keep returning **2** — both of them
the retraction passages — or the discriminator stops discriminating. Two edits to this very
paragraph pushed it to 4 before this sentence existed. The reading is:*

    sed -n "$(grep -n '^## §5. Slice plan' F | cut -d: -f1),$(grep -n '^| # | Slice | Primary' F | cut -d: -f1)p" F \
      | grep -v '^    ' | grep -c 'governing algorithm'          # F = this file

⚠ *Three things about that command are load-bearing and every one of them was wrong in a draft of this
paragraph.* **`grep -v '^    '` drops indented lines, which is how the command stops counting its own
needle**: written without it the command is inside the range it measures and returns 3, and the third
hit is the command. **The needle is the two-word fragment, not the retracted phrase.** The phrase is hard-wrapped across a newline at both of its occurrences and `grep` is line-based, so
searching for it whole returns **0** — a sweep run from the sentence as written would find nothing and
report a clean discriminator. **And the range is derived from the two headings, not written down.**
The upper bound was `1807` when this paragraph was added and is `1847` four commits later, moved by
the paragraph itself; a hard-coded line number is the one kind of figure this document re-derives
everywhere else. The second computes an axis count from a breadth product and joins the two with an
equals sign: `6 sites × 3 files × key kind × syntactic position × failure-mode discipline`
**=** `≥3 intersecting invariant axes`, in `#11-vm-property-key-lowering-unification`'s cell. A site
count and a file count are breadth; the equals sign is where the missing step went. ⚠ *A third needs
no vocabulary at all: row **7** derives its kind from **two** invariant axes, against a threshold this
paragraph states as **≥3**. No sweep keyed on wording can find that one, because its wording is
correct and its arithmetic is not.*

⚠ **The rows carrying it are resolved on the row, and this passage does not list them** — the
obligation is the row's and the list would be the second copy that rots. What this passage owns is
the rule. ⚠ *An earlier revision answered this with a **marker** appended to each affected cell, and
counted them with `grep -c '^|.*criterion does not state'`. Both are gone: the marker left the
declaring field asserting a kind while the note beside it said the ground was missing — prose
contradicting the structured cell, which is worse than the restatement the single-home rule already
forbids — and with the markers removed that grep returns 0, so as a control it could no longer
distinguish a clean document from one whose vocabulary had moved.* Each row now reads as one of three
things: the measurement supplied, the kind corrected, or the kind declared **undetermined in the
declaring field**. The live count is the third of those, and it is a program's output rather than a
grep's: `python3 .claude/tools/plan-memo-umbrella-check.py <this file>` prints it as
`[KIND-UNDETERMINED]`. And
the set is a **seed, not an inventory**: it was assembled by reading, the two spellings above are
what reading found, and a row that states the same defect in a fourth spelling is not in it. If it does, the row is an umbrella; it states its charter, mints terminal
sub-slices, and each sub-slice is its own PR under its own plan-review. Sub-slices take the parent's
id plus a trailing letter, as `9a`-`9d` already do — and a sub-slice that itself fails the test takes a
second letter (`9da` / `9db`), rather than being kept terminal because its id already has one.

This umbrella is the approved parent for every row that passes that test, **wherever the row is
written down** — the test is stated over the *work*, not over the section that records it. §8's slot
rows are the same rows before they are scheduled, so a slot whose *Why* cell declares ≥3 intersecting
axes, **or a subsystem with no canonical algorithm**, has declared itself an **umbrella row** in
the sense above and is read under that entry from the moment it says so. ⚠ **The second disjunct here is
the quoted test and not the paraphrase this section has already rejected once.** "Span more than one
governing algorithm across layers" stood in this sentence, applying to §8's slots the very substitute
withdrawn for §5's rows in the paragraph immediately above, and the two predicates come apart in the
direction that decides scheduling: a slot introducing **one** novel algorithm in **one** layer satisfies
CLAUDE.md's disjunct and fails the paraphrase, so under the paraphrase such a slot read terminal and
could be scheduled as one PR where the repository rule requires an umbrella and terminal children.
One test governs both tables — which is this paragraph's own premise, the test being stated over the
*work* and not over the section that records it — so a substitute stated for one table is a second
test and not a restatement of the first. Both consequences arrive
together when it materialises: at the occasion its `Trigger` names, its derivation runs and mints
terminal children, each its own PR under its own plan-review, and each obligation, each `Deps` edge
and each acceptance condition the slot **records as evidence for that derivation to route** is placed on the specific child that consumes that
artifact. Its `Trigger` cell therefore names that derivation — or the children the derivation mints —
together with the occasion that fires it, never a single PR that would carry the work, since an
umbrella ships none. No row restates this; a row that declares edge-density and then reads as one PR
is repointed here.

**A derivation or an acceptance condition is stated over the algorithms the spec defines for a
surface, not over the members that surface has.** §2.2's accessor row already carries the
distinction in the form that matters here — *exhaustive is not the same as correct*: the sibling
`PropertyKind::Init` match is exhaustive over `PropertyKey` and still lowers a BigInt key to the
empty-string key. A rule written over members inherits that failure and adds a worse one: a member
can be present while the algorithm governing it is unbuilt, so the rule reads satisfied, the slice
retires, and the row that would have caught it is the row that declared itself done. So a slice's
derivation names the spec clauses that define the algorithms for its surface and is run against
those, and its acceptance condition asserts what those algorithms require of an observable
result — never that a name resolves.

**A derivation's output is a grouping, not a slice.** What a derivation returns is what the clauses it
names cover, and nothing about that shape makes it one PR's worth of work. So a group is an
**intermediate** result: the terminality criterion above is applied to each group the derivation
returned, and a group yields as many terminal slices as that criterion requires. A rule that mints one
slice per group defeats the criterion precisely where the derivation *succeeded*, because a group is
well-formed as a spec unit and can still hold mechanisms with no shared lowering chokepoint. `Object`
is the demonstration: **§20.1.2.7** `Object.fromEntries` step 6 performs **§24.1.1.2**
AddEntriesFromIterable, whose step 1 is `GetIterator(iterable, sync)`, and **§20.1.2.13**
`Object.groupBy` step 1 performs **§7.3.35** GroupBy, whose step 4 is the same `GetIterator` — while
**§20.1.2.14** `Object.hasOwn` is `ToObject` → `ToPropertyKey` → `HasOwnProperty` and consumes no
iterator (numbers from `webref aoid ecma262 AddEntriesFromIterable` / `GroupBy` and
`webref heading ecma262 20.1.2`; steps from `webref body ecma262 sec-object.fromentries` /
`sec-add-entries-from-iterable` / `sec-object.groupby` / `sec-groupby` / `sec-object.hasown`). One PR
for "the Object family" therefore bundles an iterator-protocol contract with a member that has none.
This is stated once for the whole table and governs every derivation this document writes — the
builtin one, Slice 7's, Slice 8's, Slice M's, and any later one; no row restates it, and a row that
reads as one slice per group is repointed here.

### What in this table is specification, and what is evidence

**Three things a row records are specification. Everything else in it is evidence.** The three:
**(1) the split** — that the slice exists and where its boundary falls; **(2) the ordering** — the
`Deps` column, **and nothing else** — ⚠ **this definition read "the `Deps` column and the prerequisites
called out per row" for several revisions, which gave ordering two homes and contradicted the
single-home rule stated a few lines below.** A prerequisite named in prose is a *reason*, never an
ordering: what schedules is the cell. (The column's **completeness** is a separate claim, and not one
the cell carries — that boundary is the edge-derivation passage below); and **(3) the owner** of
each obligation the row names. Everything else — the `Primary module(s)` column, the acceptance
condition, the terminality verdict, the spec readings, the measured touch sets, and **the *solution*
to any obligation the row names** (which mechanism, which representation, which key, which lifetime,
which crate) — is **evidence about the code at the revision it cites**. Evidence is a starting point
for the slice's own mandatory `/elidex-plan-review`, which derives the design against **that slice's**
parent HEAD; where the two differ the plan-review wins and this table is **not** amended to match it —
superseded evidence is simply superseded.

⚠ **Each of the three has exactly one home, and it is the structured cell — prose carries reasons, never
a restatement.** The split is the set of rows; the ordering is the `Deps` column; the owner is the slice
id the obligation is written on. Prose that *restates* one of them creates a second copy that nothing
checks, and the second copy is always the one that rots: a `0b` umbrella sentence read "the sub-slices
are ordered and dependent" while all three Deps cells said otherwise; an ordering paragraph named Pa's
dependents as a list while a fourth row sat outside it; a "shared prerequisite" framing recorded
obligations belonging to **no** slice id at all. So prose may say *why* an edge exists, *why* a seam is
admissible, *why* an owner is the owner — and must not be the place any of the three is read from.
⚠ **A "shared prerequisite" is therefore not an owner-free category.** It is outside either slice's
*charter*, which is why it is stated in prose — but "owner" is one of the three, so a prerequisite with
no owner is a specification hole, and precisely the third state **I-4** forbids. **Each such bullet names
its owner**: where both named slices must exist before the obligation can be discharged, the owner is
**whichever lands second**, and it integrates *and* tests it; where only one side can satisfy it, that
side owns it outright.

### What each field means for a row that is not a terminal slice

⚠ **The ordering, the owner and the acceptance condition are read at the row's own landing point — so a
row that never lands carries none of them, and a row that lands before its consumers cannot assert what
they buy.** The terminality criterion above sorts rows into **five** kinds; the kind is what decides how
each field is read, and the defaults above are the *terminal* row's. Stating that once, here, is what
stops the next row of any other kind from being written and read under defaults that do not apply to
it — this passage governs every such row and slot in this document, and none of them restates it.
⚠ *This sentence read "three kinds" against a list of four for many revisions, and a fifth is added
below. The count is the length of the list under it and nothing else —*
`awk '/^### What each field means/,/^⚠ \*\*A derivation/' <this file> | grep -c '^- \*\*'` *— so it is
re-read from the list on any edit that adds a bullet, never carried:*

- **Terminal row** — ships one PR. The three fields and the acceptance condition read exactly as above.
- **Umbrella row** — ships **no implementation PR**. It carries **(1) the split** — its own boundary
  against its siblings, like any row — and states a charter and a derivation from which it mints
  terminal children at **its own start**; each child is its own PR under its own mandatory
  `/elidex-plan-review`, while the umbrella's own charter, derivation and split are what *its* plan-memo
  states and a plan-review of that memo reviews — as this document is one and is being reviewed as one.
  It carries **neither (2) the ordering nor (3) the owner, and no acceptance condition**: naming an
  umbrella as an owner, as a "lands second" party, or in
  a `Deps` cell names *nobody*, and an edge attached to one is either stranded — nothing transfers an
  umbrella's `Deps` to a generated child — or over-orders every child that does not consume it. Placing
  each obligation, each edge and each acceptance condition on the specific child is a **mandatory output
  of that umbrella's derivation**. ⚠ **A consumer whose producer is not yet minted records that
  derivation, not a blank.** The derivation is itself a schedulable event with an owner — the
  umbrella's own start — and minting is one of its outputs, so a `Deps` cell may name **an umbrella's
  derivation together with the child that derivation mints for the artifact**: what the consumer waits
  on is that event having run and produced that child, never the umbrella *landing*, which does not
  occur. Re-pointing the edge at the child, once minted, is a mandatory output of the same derivation,
  alongside the obligations it already places. That is what every consumer of a not-yet-minted producer
  records, so no conversion leaves one holding `—`, and none leaves an edge that lives only in prose.
  Children already listed under it are that derivation's output *so
  far*, never its boundary: a member absent from them is **un-minted, never out of scope**. An umbrella
  **never lands**; it *retires* once every child it minted has landed, and a partial landing is recorded
  against the child, not against the umbrella. ⚠ **A "family" label — *the 0b family*, *the 2a family*,
  *the P family* — reads as a licensed collective and is not one when it stands as a party.** Denoting
  a set descriptively is fine, and so is an ordering *between named members of it* (`Pb → Pa` inside
  the P family orders two terminal rows). What is not: a shared prerequisite *of* a family, edges
  *onto* a family, a family *ordered by its `Deps` column* — a family has no id and no `Deps` cell, so
  an obligation written on one is owned by nobody, which is the third state **I-4** forbids and
  exactly what the rule above forbids for a bare umbrella id. The label is the same defect wearing a
  collective noun, and it survived the first two sweeps of that rule because neither was looking for
  a spelling with no id in it. ⚠ **Every umbrella row states the marker
  `UMBRELLA, not a terminal unit` in its Slice cell** — wherever the row is written down, and whatever
  else that cell says about where the work is scheduled — so umbrellas are *enumerated* rather than read
  out of each row's prose, **and read in the field that declares the kind, which differs per table**:
  §5's rows declare it in the `Slice` cell, §8's slot rows have no `Slice` cell and declare it in
  `Why deferred`. A check over the whole row would certify a slot whose marker had drifted into its
  `Trigger`, leaving the authoritative field unmarked while the count stayed the same. The reading returns **54**, and it is a *program over the declaration field*, not a human read: `python3 -c "import sys;C=chr(124);B=chr(92);d=lambda l:l.replace(B+C,chr(1)).split(C);R=[d(l) for l in open(sys.argv[1]) if l.startswith(C+chr(32))];m=[c for c in R if 'UMBRELLA, not a terminal unit' in c[2]];print(len(m),sum(len(c)==8 for c in m),sum(len(c)==6 for c in m))" <this file>` prints `48 33 15` at this revision — total, §5 half, §8 half — splitting each row on **unescaped** pipes and testing cell 2, which is the declaration field in both tables. ⚠ **It printed `48 33 15` one revision ago and `50 33 17` the revision before that.** Row **7** returned to the umbrella half when its second disjunct was measured, which is the whole of the 48→49 step. The earlier 50→48 step was different in kind, and the two extra were not declarations at all: `#11-vm-per-iteration-loop-environment` and `#11-vm-explicit-resource-management` are pointers into §5 whose cells opened by stating **§5 rows' kinds** in the marker's own words, so the census counted one umbrella twice and the reader could not tell. Cell-scoping had fixed *where* the marker sits and left *whose kind it states* unchecked. **The repair is at the source — the two pointer cells no longer restate what they point at — and the consequence is that the one-liner and the attribution-checking reading now agree.** They did not have to: the reading that found the defect is `.claude/tools/plan-memo-umbrella-check.py <this file>`, which excludes a marker attributed to another row and reports each exclusion by name. ⚠ *Agreement today is a fact about the document, not a property of the one-liner — it cannot see attribution and will silently over-count the next cell that restates another row's kind. Which is why the check stays, and why this passage does not claim the one-liner is sufficient.* ⚠ *The lesson generalises past this figure: each repair to this census fixed the previous one's scope and inherited its blind spot — whole-file grep → row-scoped → cell-scoped → attribution-checked, and the first three each read as the finished answer.* Cell-scoping fixed *where* the marker sits and left *whose kind it states* unchecked: `#11-vm-per-iteration-loop-environment`'s cell opens `Slice **E** — **UMBRELLA, not a terminal unit**` and `#11-vm-explicit-resource-management`'s opens `Slice **R** — …`, each declaring a **§5** row's kind from a §8 cell. Both are pointers into §5, and the rule below says a pointer slot carries no marker of its own, so neither belongs in the count. The one-liner above cannot see this and is kept as the record of what it does measure; the reading that excludes them is `.claude/tools/plan-memo-umbrella-check.py <this file>`, which reports each exclusion by name rather than silently lowering the total. (§5 rows have 8 fields, §8 slot rows 6, so the halves fall out of the same parse and need no line ranges). What the control **cannot be is a whole-line grep for `UMBRELLA`**: that literal is a *substring* of the marker, so such a pipeline returns the marker count by construction — degenerate, not a control, and unable to disagree for any content (measured 32 = 32 while two rows declared the kind in lowercase and were invisible to both). A **cell-scoped parse is not degenerate**, and is what produced the correction above: it can disagree with the marker count whenever a marker sits in a row but outside the declaring field, which is the failure this rule exists to catch. The population is read from each row's **declaration** — all **64** §5 data rows and all 51 §8 slot
  rows (⚠ **50** until this revision minted `#11-vm-float16array-global`, **49** until the revision that minted `#11-vm-structured-clone-layering-seam`, **48** until the one that minted `#11-vm-native-function-length-on-allocator`, and **47** until the one before that — ⚠ *the two were collapsed into a single revision here, which dropped **49** out of the chain; re-derived by replaying this document's own slot-row count over every commit in this PR*, which
  minted `#11-plan-memo-acceptance-falsifiability-check`; the same rule that re-derives the 64 on a row being added re-derives this one, and the population is slot-bearing rows, so the `Intl` row — which mints no slot — is not in it) — ⚠ **64 at this revision, and this figure has been wrong twice for two different reasons**: it read
  **62** for several revisions by excluding the id-less `| **—** |` `Function`/`eval` pointer row at the
  foot of the table, and **63** for one more because minting row **F** added a row and the population was
  not re-derived with it. **Adding a row re-derives this figure in the same edit** — a stale total leaves
  the added row outside the very program this passage substitutes for a whole-line grep. And since that program is defined over the *declaration field of every row in the population*, a row omitted from the population is invisible to the verification and could carry an unmarked declaration without moving the reported figure — and the figures above are that program's output rather than a whole-line grep's. The earlier control agreed
  with the marker count and still
  did not, because two rows spelled the kind in words alone and every reader deriving the kind from the
  marker read them as something else. ⚠ The pipeline is **row-scoped** because this passage states the
  literal itself; an unscoped count over the whole file is off by the number of times the rule is
  written down. ⚠ **And it is scoped to `^| `, not to `^| \*\*`, because §8's slot rows are umbrella
  rows too** — the split is **33** §5 rows and **15** §8 slots (§5 side: ⚠ **28** until Slice **L** converted, **29** until Slice **B** did, **30** until row **10a** did, **31** until the revision before the fourteen-row sweep, **37** after it, **33** from the revision that resolved the breadth-derived kinds, **34** from the revision that grounded row **7** on the second disjunct, and **33** from the one that disproved row **2a**'s — because **0c**, **E**, **10** and **2a** turned out to have no grounded kind at all and are now *kind-undetermined*, which is neither half of this split. ⚠ *This figure has now moved in both directions inside one PR, which is the point of deriving it rather than carrying it: rows leave the count when their ground is disproved and re-enter when it is measured*; §8 side: ⚠ **6** until `#11-vm-ordinary-function-name-length` converted at `d8370865` and **7** until `2d56fa40`, which is this one. ⚠ **The §8 values read 8 and 9 until this revision, each 2 high for the whole chain**, because the two pointer slots named above were counted throughout; re-derived by replaying `.claude/tools/plan-memo-umbrella-check.py` over every commit in this PR rather than by subtracting 2 from what stood here. ⚠ **This revision converts fourteen rows in one pass, and moves both halves** — §5's **0bc**, **1b**, **2ab**, **C**, **A** and **9da**, and §8's `#11-vm-parameter-environment-instantiation`, `#11-vm-typed-array-family-layering-and-gate`, `#11-vm-atomics-global`, `#11-vm-weakref-finalization-registry`, `#11-plan-memo-spec-field-single-home-check`, `#11-plan-memo-acceptance-falsifiability-check`, `#11-vm-webidl-sequence-early-exit-drain` and `#11-vm-structured-clone-layering-seam` — because the criterion was applied to **every** terminal row in both tables in one sweep instead of to whichever row a review round happened to name, which is how a per-round conversion rate of one hid a backlog of fourteen. A §8 slot that is a *pointer* into §5 carries no marker of its own — `#11-vm-iteratorclose-precedence-convention`, `#11-vm-labelled-statement-break-target`, `#11-vm-block-declaration-instantiation` and now `#11-vm-async-generators` are all such pointers and none is in the 15. ⚠ **Two more are, and were in it**: `#11-vm-per-iteration-loop-environment` (→ Slice **E**) and `#11-vm-explicit-resource-management` (→ Slice **R**) are pointers by this same rule, and each carries a marker that states the kind of the §5 row it points at. That is why this rule and the count above disagreed for six revisions without either being re-read against the other: the rule is prose and the count was a program, and nothing ran the rule. ⚠ **And "a pointer *to a §5 umbrella*" was wrong for the first of them**: `#11-vm-iteratorclose-precedence-convention` points at §5's **Pa** and **Pb**, and neither carries the marker — they are *prerequisite* rows. Its exclusion from the count is still right; the reason stated for it was not). The narrower pipeline was what this rule
  first carried, and it repeated at the slot level the same undercount it had just fixed at the row
  level: a rule swept over the population that motivated it rather than over the population it reaches.
- **Prerequisite row** — ships one PR, and lands *before* the consumers that would make its artifact
  reachable. It carries all three, but its **acceptance is bounded to what its own artifact buys at the
  point it lands**: asserting anything that turns true only once a consumer lands either pulls that
  consumer's work into the prerequisite or leaves the row retiring red. Where the artifact has no
  JS-reachable holder yet, the *observable result* rule above is satisfied against the slot from Rust —
  demanding a JS path there would pull the consumer's exposure work in. And where an obligation is a
  prerequisite **of** both named slices, rather than something both must exist before it can be
  discharged, the owner is **whichever lands first** — it moves the work and the other consumes it,
  the mirror of the "lands second" case above.
- **Pointer row** — ships **no PR of this plan's sequence**: the work is scheduled from its own §8 slot,
  and that slot is the single home of all three fields. The row exists so the table is complete over the
  language surface, not so the work is owned here. ⚠ **This kind was left off the list while three rows
  already declared it** (`O`, `N`, and the `Function`/`eval` row — `grep -c 'is a pointer rather than a
  slice'` over the row lines), so their `Deps` and acceptance were read under the terminal defaults by
  inference; adding it is the same repair as the complement above, and for the same reason — an
  enumeration of kinds leaves the kind it omits governed by whatever the reader assumes. What a pointer
  row *may* carry is an **ordering** the reader of this table needs in order to place the slice sequence
  around it, and that edge's home is the slot's row: where the two disagree the slot is authoritative,
  and a pointer row states no acceptance condition of its own, since the PR that would satisfy one is
  the slot's.
- **Kind-undetermined row** — the criterion has been applied and **returns neither answer**: the cell's
  evidence establishes neither ≥3 intersecting invariant axes nor a subsystem with no canonical
  algorithm, so the row is neither an umbrella nor a terminal unit. ⚠ **This kind exists because the
  alternative was tried and is forbidden by this section's own rule.** The rows now carrying it were
  first repaired by leaving the declaring field asserting a kind and appending a note saying the ground
  for it was missing — prose that *contradicts* the structured cell, which is strictly worse than the
  restatement the single-home rule already forbids, and which leaves a scheduler simultaneously
  mandated to run a derivation and told the mandate is unfounded. So the undetermined state is written
  **in the declaring field**, where the kind's one home is. Such a row carries **the split** and
  nothing else: no ordering, no owner, no acceptance condition, and **no PR may be scheduled under it —
  its sub-slices included — until the measurement its cell names is made**. That measurement is the
  row's next event, and making it resolves the row into one of the four kinds above; it is not a
  standing state a row may retire in. ⚠ **A row in this state is invisible to the marker-based census
  above and is therefore counted by any program that partitions rows into "umbrella" and "everything
  else"** — including the acceptance-vocabulary approximation §8's falsifiability slot publishes, which
  reports these rows under its *terminal* population until they resolve. That is a true statement about
  an unresolved row, not a defect to silence by writing an acceptance condition onto a row whose kind
  is not yet known. ⚠ **A sub-slice of such a row keeps its own kind, its own acceptance condition and
  its `Deps` edges to its siblings, and that is not a contradiction.** The criterion is stated over the
  *work*, not over the section that records it — as this section's head says of §8's slots — so a
  sub-slice's kind is a fact about **that sub-slice's** work, which its parent's open question does not
  reach; and an undetermined row carries **the split**, so the sub-slices and the boundaries between
  them are exactly the part of it that *is* settled. What the parent's state forbids is **starting**
  them: the acceptance condition is read at a landing point that cannot arrive yet, so it stands as
  evidence rather than as an unreachable claim, and the sibling edges order work whose beginning is
  blocked rather than scheduling it. The one thing a sub-slice may **not** carry is an edge or an owner
  that reaches *outside* the split, since that would schedule against a row which forbids it. Stated
  here because it was read the other way once: four sub-slices were flagged as holding conditions their
  parents could not, and the flag was wrong — the rule had simply never been written down.

⚠ **A derivation's *input* is as much a claim as its output, and clause lists kept failing on it.**
Four were written across two revisions and all four omitted a clause a reader reaches from the entry
point in one step — the home object at **§15.4.5** from a class element, **§27.5.2** Promise Jobs from
CreateResolvingFunctions, **§22.2.3**/**§22.2.4** from the RegExp constructor surface,
**§20.4.2**/**§6.1.5.1** from `Symbol.dispose`. Written as a list the failure is one-directional and
silent: an output defined in an unlisted clause is minted by nobody while every child retires having
followed the stated walk. ⚠ **The answer is NOT a closure rule — that was tried and the machinery is
withdrawn; the procedure that replaces it is stated once, in the paragraph after this one, and this
paragraph does not restate it.** Three formulations were written in three review rounds — a clause list, a prose
"close transitively over what it reaches", and a command-shaped edge walk — and each failed for a
*different* reason: the list was incomplete, the prose closure was unbounded (`String.raw`'s seed
reaches `ToObject` / `Get` / `LengthOfArrayLike` in one step and from there the whole VM's conversion
and property semantics), and the command walk was not computable (`.claude/tools/webref body` reduces
`<a>` / `<emu-xref>` to plain text by design — `_webref/extractor.py` states it — and `webref aoid`
takes an AO name the caller must already know, so the frontier and the fixed point were being
maintained by hand while the text called them computed). Three different failure modes is the
signature of a wrong **goal**, not a wrong formulation: all three were attempts to have this umbrella
derive a *complete partition of the spec surface* at umbrella time.

⚠ **That is the job §5 already assigns to slice time, and this is its third disguise.** The demotion
above says in as many words that a finite sentence cannot bound the spec surface it is written over,
and that deriving a slice's obligations from the clauses governing its surface is what the slice's own
mandatory `/elidex-plan-review` owns, **against that slice's parent HEAD**. Obligation lists were the
first disguise, clause lists the second, closures the third. **So a derivation here names the seed —
the clause that defines the surface, and any clause the evidence has shown a reader cannot reach from
it — and stops.** What the slice owes beyond the seed is the slice's plan-review to derive, and the
umbrella's specification remains what §5 says it is: the split, the ordering, and the owner.

⚠ **And the seed is produced by a command, not from recall.** Demoting the derivation to *seed +
slice-time plan-review* puts the whole weight of the input on the seed, and the seed is exactly where
this has now failed five times, each time by omitting a governing clause a reader cannot reach from
the ones named: Promise Jobs **§27.5.2**, the RegExp constructor clauses, the disposal symbols,
module **§16.2.1**, dynamic import **§13.3.10**. Every one of those seeds was curated by inspection,
which is the class [§13](2026-07-vm-p4-umbrella-review-rounds.md)'s own rule already names — *enumerate by command, cache the count inline,
never by recall*, written there because every enumeration derived mechanically held up and every one
curated by inspection did not. So **a derivation's seed is produced by a command over the spec's own
clause index, and the row records the command and its output**. `.claude/tools/webref` is that
command: `webref heading ecma262 <clause>` enumerates a clause's subclauses, which is the step recall
skips. A seed that cannot name the command that produced it is a claim, not a derivation.

⚠ **And naming the command is not enough if the object it reads does not outlive this branch.** This PR is squash-merged, so none of
its commits enter `main`'s history; once the branch is deleted a fresh clone resolves none of them and every `git show <sha>:<path>`
here stops reproducing — the seed is a claim again, by the sentence above. ⚠ **The population is this document *and the three files
carved out of it*, and it read "this file" for several revisions** — which left the carved files governed by nothing while the rule
that reaches them sat here, the same "swept the population that motivated it" failure §5's naming rule records. Re-derived at this
revision, per file, with the commands below: this document cites **45** distinct 8-hex tokens, of which **44** resolve as commits and
**all 44** are ancestors of HEAD, i.e. branch-local; only **4** of those are reachable from `origin/main`, leaving **40** that die at
the squash. The 45th, `04734642`, is a *blob*, and a blob of this file dies with them.
⚠ *This read **43 / 42 / 38** until the revision that corrected the umbrella census, which cited two more of this branch's own
commits in the act of pinning the steps it describes — so the figure was invalidated by an edit whose entire subject was
invalidated figures. **And the repair then staled its own two neighbours**: the summary comparing the external anchors against
this document's total, and the sentence naming what the rule's second limb carries, both stood at 38 for a revision while the
figure they restate read 40 — three copies of one count, of which the commit moved one. That is the fourth time in this PR a
commit has staled a count while repairing another one, and it is the
argument for the second limb of the rule below rather than a note about it: the numbers survive because the measurement that
supports them is quoted inline, not because anyone remembered to re-run this one.*
[The 0a landing record](2026-07-vm-p4-slice-0a-landing-record.md) cites **23**, all commits, **1** on `origin/main` ⇒ **22** die —
and only **1** is an ancestor of this branch's HEAD, so the other 22 are already anchored to a ref this PR does not carry.
[The 1a/1b detail](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) cites **3** ⇒ **1** dies;
[the review-rounds record](2026-07-vm-p4-umbrella-review-rounds.md) cites **2** ⇒ **1** dies. **24 outside this document**, against
the 40 inside it. Enumerate with `grep -oE '\b[0-9a-f]{8}\b' <file>` de-duplicated through `sort -u`, then per token
`git cat-file -e "<sha>^{commit}"` and `git merge-base --is-ancestor <sha> origin/main`. So the rule, and it has two limbs:
**an evidence anchor is either reachable from `origin/main`, or the measurement it supports is quoted inline** so the figure survives
the object. The second limb is what carries the 40, and it is why they are *not* rewritten — a mechanical re-anchoring sweep costs
more than it buys while the branch still exists. What is not permitted is the third shape: a bare sha carrying a figure stated nowhere
but at that sha. **At merge time the lander re-runs the two commands above against the squash commit, over all four files, and converts every anchor
still in that shape**, which is the step that keeps this from rotting silently.

⚠ **And a prose rule is not the remedy, only its specification.** Three rounds running, the finding was
a hand-written claim contradicting a cell — a class that stops when a **checker reads the memo**, not
when the memo adds a fourth rule about itself. Carved as `#11-plan-memo-spec-field-single-home-check`
(§8), whose children's deliverable is a check over this document — every prose assertion of a dependency,
independence, ordering or ownership reconciled against the `Deps`/`Slot`/row cells — and whose natural
home is a program rather than another paragraph here. ⚠ *This read "the `claim-gate-plan-check` tooling already in flight", which §8's own cell for that slot has since recorded as false — the first enumerator landed in this PR as a separate program, and a re-derivation over every in-flight worktree returns **four** plan-memo checkers on three branch families, none on `main`. The correction was made in §8 and not here, which is the second-copy failure this very sentence is about.*

⚠ **But the override runs one way only, and a plan-review that lands on the other side of the line
must come back.** Evidence and specification are not sealed off from each other: a solution the
plan-review settles differently can *change one of the three*. If the plan-review of the child umbrella 1b mints for the argument-emit helper replaces the
`ArgsForm` mechanism, the dependency stated for the child of umbrella 4a that owns the tagged-call lowering is reshaped or gone; if a slice's plan-review finds
a seam its row denied, the split changes. Left one-way that produces two sources of truth for exactly
the data later scheduling and Slice D read — the state CLAUDE.md *One issue, one way* forbids, and
worse than the demotions it replaces, because those govern claims nobody reads as binding. **So: a
plan-review result that changes the split, the ordering, or an owner updates this table in the same
PR; everything else supersedes silently.** The test is not "how big was the change" but "does it land
on one of the three?" — the same complement, applied to the direction of travel.

⚠ **The rule is a complement, and stating it that way is the point.** It stood as four separate
demotions — the `Primary module(s)` column, the edge-completeness boundary, the acceptance conditions,
the terminality verdicts — each added in the round its own class started generating findings, and each
leaving every class it did not name authoritative by default. A row's **solution design** was the class
no statement covered: obligations named with their answers attached (a release point, a key carrier, a
crate to extend), written in specifying voice, measured against a baseline the slice will not start
from. It went on producing exactly the findings the other four had stopped producing, which is the
signature of a layer rather than a list needing one more entry. A list of demoted classes has to be
extended per class and cannot be checked; a complement cannot leave one out. **A row that reads as
though it settles a design question is therefore not a row to correct — it is a row whose obligation
is to be named and whose answer is to be deleted.**

⚠ **The `Primary module(s)` column is a non-authoritative HINT, not a touch set.** It was written
against one tree; the slices land months apart against moving code, so a column that reads as a
specification is wrong by construction — and it duplicates a decision each slice's own **mandatory
`/elidex-plan-review` already owns**: deriving the touch set against that slice's parent HEAD. Four
consecutive Codex rounds each checked one more column against current code and found it incomplete
(the 0b and P rows missing `stmt_loop.rs`; Slice 6 missing `stmt_loop.rs` *and* `vm/interpreter.rs`; Slice 8
missing the well-known-symbol table and string-method dispatch) — hopping corner to corner, which is
the signature of a whole layer generating findings rather than a list needing one more entry.
**So: derive the touch set at slice time; treat the column as a starting point.** The gaps already
found are kept below as *evidence about the code*, because they were expensive to find — not as a
claim that the columns are now complete.

⚠ **The evidence block is the single home for those gaps; the cells carry none of them.** The first
pass at this demotion moved Slice 8's gap into the evidence block and left the others written into
their cells, so the column and the evidence contradicted each other inside one document — a
strangler state, and exactly the decision-surface duplication the demotion was for. A gap detail
appears **once**, below.

**The load-bearing exception that survives as specification**, because it is an *ordering* fact rather
than a file list: the `Deps` column, **and nothing else** — ⚠ *"and the prerequisites called out per
row" stood here, and in the specification-vs-evidence definition, and both are withdrawn: the phrase
gave ordering a second home and let a prose-only prerequisite read as authoritative against an empty
cell, which is the state the single-home rule and `#11-plan-memo-spec-field-single-home-check` exist to
remove.*

**What puts an edge in the Deps column — stated once for the whole table, and nowhere else.** An
ordering edge exists **only where the later row names the artifact it consumes** from the earlier
one: an opcode, a data structure, a helper signature, a spec-algorithm output. A shared file, a
shared arm, a shared family and a shared spec area are **not** edges. Where two rows must each carry
the other's contract but neither's deliverable is an input to the other, that is **coordination** —
recorded in the rows, never in the Deps column. Two rows used to restate this locally, having each
been corrected into it a round at a time; both restatements are deleted in favour of this one.

**What that rule's completeness covers, and what it does not — the same demotion the `Primary
module(s)` column just took, applied to the column that survived it.** The rule above says when an
edge *exists*. It does not say the column holds every edge that exists, and successive rounds have
each answered one completeness question and reported the answer as the column's. There are four,
and a fifth that is not about existence at all:

- **(a) does each stated edge name an artifact?** Answerable by reading the cells. The column is
  maintained complete against this.
- **(b) is an edge missing that a row's own cited runtime algorithm implies?** Answerable by a
  derivation, named here so it is re-runnable rather than asserted: expand each row's cited Abstract
  Operations through the steps `webref body ecma262 <anchor-or-AO-name>` returns, and draw an edge
  wherever the expansion reaches an artifact another row delivers. The column is maintained complete
  against **that derivation**.
- **(c) obligations from static semantics, early errors and parse context.** Reachable by neither,
  and **not claimed here**. These are derived by each slice's own mandatory `/elidex-plan-review`
  against that slice's parent HEAD — where this document already puts every claim it cannot bound.
- **(d) edges whose artifact is not a spec construct at all.** Split out of (c), which bundled the
  two: (c)'s unreachability is *proved* below and this class's is not, and it is not a residue —
  **5** of the **15** slice→slice edges the column held at `b3eb07c1` are of this class, enumerated
  in the next paragraph. (a) validates only edges already stated and (b) is declared unable to reach
  a non-spec artifact, so this class has no derivation at all; the honest statement of its discovery
  method is the one that in fact found all five — **reading the rows**, round after round.

**The boundary is measured, not asserted.** On (b)'s reach, run against the table at `b3eb07c1`
— the revision before the edges below were added, so the figure does not move under this document's
own edit — the derivation reaches **10** of the **15** slice→slice edges the column then held. The 15
are read off the Deps column with a splitter that consumes the cells' escaped pipes:
`git show b3eb07c1:docs/plans/2026-07-vm-p4-es-language-completeness.md | sed -n '/^| # | Slice | Primary/,/^$/p' | grep '^| \*\*' | perl -pe 's/\\\|/\x01/g' | cut -d'|' -f7 | grep -vx ' — '`
returns the **14** non-`—` cells; the escape handling is load-bearing, since `awk -F'|' 'NF!=8'` over
the same input reports **8** of the 57 rows, which is exactly the set whose Deps field a naive split
truncates. The **5** the derivation cannot reach are the five whose artifact is not a spec construct:
`1b → 1a` and `4b → 1b` (a shared Rust helper signature — `lay_out_call_args`, and 1b's `ArgsForm`
input contract; ⚠ **`4b` is the id that edge carried at `b3eb07c1`**, and it stays written that way
because every figure in this paragraph is a reading *of that revision's table* — the row is `4a` from
this revision on, and a rename must not silently restate a past measurement),
`Pa → Pb` (a bytecode transport), (no E edge — row E's kind is undetermined, so it carries no ordering), and
`0cb → 0ca` (a sweep-ordering fact). No spec fetch can produce any of them. The two slot-valued Deps
entries (`Pa`'s layering gate, `D`'s module connects) are outside the 15 and outside (b) for the same
reason.

On (c)'s unreachability the proof is direct, and it is structural rather than incidental: an early
error is attached to a **production**, not to a step of any runtime AO, so no forward expansion
arrives at one. The 2a family's `arguments` prerequisite in the evidence block below is that class —
`webref body ecma262 sec-runtime-semantics-classfielddefinitionevaluation | grep -c 'ContainsArguments'`
returns **0** against the control `| grep -c 'MakeMethod'` → **1** on the same output, so the grep
discriminates and the runtime AO genuinely does not mention the rule governing its own production.

**A fifth question, and it is not about which edges exist.** (a)-(d) all ask that. None asks what an
edge was *also* carrying: an ordering edge can be the only site holding a §4 obligation for the later
row — a layering gate, a non-`engine` build requirement, a host seam — and the artifact rule does not
see those. §4's I-5 pins obligations to Slices **Pa** and **Pc** and to the children umbrellas **7b**, **10b** and **M** mint, and §5's rows are not
their home: Pa's is carried by a **Deps entry** — which is exactly the fragility — and the others
reach a row only when a deleted edge forces it (7b's onto umbrella 4a's derivation, and 10b's `Reflect` half onto umbrella 10a's derivation,
both at this revision). So adding or deleting an edge is not finished until §4's
obligations for that slice id have been re-read and any the edge alone was carrying re-homed.
Deleting `Pc → Pa` at `b3eb07c1` is the instance: right under the artifact rule, and also the only
thing holding Pc — three of whose four listed modules are under `vm/host/` — behind this document's
own unresolved `vm/host/` layering gate. `10b → 10a` did the same to I-5's `Reflect` half.

State the boundary rather than the claim, for the reason the `Primary module(s)` demotion states: a
completeness claim the authoring context cannot support is what produces one finding a round, each
round fixing the instance it found and restating the claim. This is **not** a narrowing of what the
column must hold — every edge (a) or (b) reaches is still owed here, and the rows below add the ones
this pass found.

**Evidence found by those rounds, kept because it was expensive** (each verified against
`658cc302`; re-verify at slice time rather than reading forward):

- **Slices 6a/6b** cannot work from `stmt.rs` alone. `compile_for_of` (`stmt_loop.rs:62`) takes no
  `is_await` and owns every `GetIterator` / `IteratorNext` / body / close path. Worse, `call_internal`
  (`interpreter.rs:629-664`) and `push_js_call_frame` (`:862-912`) both test `is_async` **before**
  `is_generator`, so an async-generator function — whose compiled flags are *both* true — takes the
  ordinary async path and returns a Promise before generator construction is considered. Adding an
  `ObjectKind` and generator natives changes neither entry point, so `ag().next` stays non-functional:
  6a needs a distinct async-generator construction path in **both** callers.
- **Slice 8b** cannot complete the `@@match` / `@@replace` surface §1.1 assigns it from
  `natives_regexp.rs` + `globals_primitives.rs`: `well_known.rs:1552-1585` has no `match` / `replace`
  symbol ids, and `natives_string.rs:468` / `:549` special-case `ObjectKind::RegExp` **directly**
  instead of looking up the argument's symbol method — so methods can be installed but never
  dispatched to, and user overrides never honoured.
- **Slice 0bc** cannot work from `stmt.rs` alone either. `compile_forin_left_binding` is at
  `stmt_loop.rs:308` at `658cc302` (`git grep -n 'fn compile_forin_left_binding' 658cc302`) and is
  where `[a,b]` for-of heads and `for (obj.p in …)` currently reach `emit_unsupported`; `stmt.rs`
  only delegates `ForIn`/`ForOf` there. A touch set naming only `stmt.rs` leaves 0bc's headline cases
  unimplemented.
- **Slice Pb** likewise: `compiler/stmt_loop.rs` did not exist when the columns were first written —
  0a created it by splitting `compiler/stmt.rs`, and it took the `for-of` catch handler's
  `Op::IteratorClose` emit with it ([§6.2a-2](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md)).
- **Slice 1a**: `ic_call` / `ic_call_method`'s only callers are in `vm/dispatch.rs`, and their line
  numbers have already moved once — `:719`/`:730` at `f7d9b5ce`, `:705`/`:716` at `658cc302`
  (`git grep -n 'ic_call\b\|ic_call_method' <rev> -- vm/dispatch.rs`). Re-derive by grep; do not read
  either pair forward. Without re-signaturing both, 1a does not compile (dec. 11).

**Acceptance conditions are demoted the same way, and the reason is the one stated just above.** Every
acceptance condition this document writes was derived against one tree at one time; the slices land
months apart against moving code, and a finite sentence cannot bound the spec surface it is written
over — so a condition that reads as *the specification of when a slice may retire* is wrong by
construction, and it duplicates a decision each slice's own **mandatory `/elidex-plan-review` already
owns**: deriving the slice's obligation from the spec clauses governing the surface it touches,
against that slice's own parent HEAD. The plan-review round that produced the evidence below hit one
row after another with a sibling requirement in the same spec surface that the row's stated condition
does not detect — hopping from row to row, which is the signature of a whole layer generating findings
rather than a list needing one more entry.
**So: derive the obligation at slice time; treat what is written here as a starting point.** The
requirements already found are kept below as *evidence about the code*, because they were expensive to
find — not as a claim that the conditions are now complete. The load-bearing exception is the one the
passage above already states once, and it holds here unchanged: the `Deps` column, **and nothing
else**, is an *ordering* fact and survives as specification — ⚠ *"and the prerequisites called out
per row" stood here as well, a third home for a phrase the specification-vs-evidence definition and
the `Primary module(s)` demotion both withdraw above, and it is withdrawn here for their reason: a
prerequisite named in prose is a **reason**, never an ordering, and prose that schedules contradicts
the cell that does.* ⚠ **This passage is an instance
of the specification-vs-evidence complement at the head of this section, not a rule of its own** — it
is kept for the reasoning it records, and the complement is what a reader applies to a class this
passage does not name.

**A terminality verdict is evidence under the same complement**, and this passage likewise records
reasoning rather than adding a rule. It too was reached against one tree at one time, by reading a row's own charter sentence
rather than the algorithms the surface needs — so a row that reads **terminal** is a starting point,
and the criterion at the head of this section is re-applied at slice time by the slice's own
mandatory plan-review, against its own parent HEAD. Slice 4 is the instance **twice over, in both
directions**: it was judged terminal when the criterion was applied to the whole table, and that
verdict was wrong — it is an umbrella below; and the three-way split that replaced it was then wrong
the other way, because the row holding the template object had no live producer without the row
holding the tagged call. **And the merge that answered that was wrong in turn** — it read "no
admissible seam" off the two candidates in view rather than deriving the seam set, so it bundled
construction, lowering, a cache key, realm lifetime and hand-written GC rooting into one row. It is
**4a + 4c** below, and the umbrella row carries the *derivation* — the criterion, the unit its
candidates are built from, and every candidate tested against it — rather than a further verdict.
⚠ **Four verdicts in four review rounds on this one split**, the last of them a derivation whose
candidate *unit* was still curated: it enumerated steps inside one AO. That is [§13](2026-07-vm-p4-umbrella-review-rounds.md)'s rule reaching one
level down — **a seam set is an enumeration, and so is the set of things eligible to be enumerated.**

⚠ **And the verdicts themselves were an enumeration nobody had run, which is why they kept failing one
row per round.** Two successive review rounds each returned exactly one row whose verdict does not
survive the criterion — `4a`, then `9c` — the signature §5 names throughout of a *layer* generating
findings rather than a list needing one more entry. The answer is the one this document applies to
every other enumeration it writes: **re-derive the verdict for every row in one pass, from each row's
own enumerated deliverables and cited clauses, rather than patching the row the last round found.**
That pass is what makes `4a`, `9c` and **`9a`** umbrellas at the revision it ran in, and it is **not** what makes `1a` and `7a` ones — both converted in later single-row rounds, so the one-pass answer measurably did not end the one-row-per-round series it was written against — 9a being the row no
reviewer had reached, and `Array.from` against the §23.1.3 members is `Object.fromEntries` against
`Object.hasOwn` with the names changed. A verdict remains **evidence**: each row's own mandatory
plan-review re-applies the criterion against its own parent HEAD, and this pass replaces neither that
nor the criterion.

**Evidence found by that round, kept because it was expensive** (each verified against `658cc302`;
`git diff --stat 658cc302 ddd2b931 -- crates/` is empty, so these are current — re-verify at slice
time rather than reading forward):

- **Slice 0bc** — a destructuring **assignment** must evaluate to its RHS, and the storage helper it
  will reuse is specified to consume it. `compile_destructure_pattern`
  (`compiler/stmt_destructure.rs:23`) documents "Assumes the value to destructure is on top of the
  stack. After compilation, the value is consumed (popped)." (`:20-21`), and every store path in
  `compile_pattern_store` (`:189`) emits `Op::Pop` (`:211`, `:215`, `:221`, `:227`). Correct for the
  declaration form; wrong for the expression form. ECMA-262 **§13.15.2** Runtime Semantics: Evaluation,
  production `AssignmentExpression : LeftHandSideExpression = AssignmentExpression`, reaches step 5
  "Perform ? DestructuringAssignmentEvaluation of assignmentPattern with argument rightValue"
  (**§13.15.5.2**, which itself returns *unused*) and then step 6 "Return rightValue"
  (`webref body ecma262 sec-assignment-operators-runtime-semantics-evaluation`). So the child of umbrella 0bc that owns the destructuring-assignment store can satisfy
  every named-storage and `IteratorClose` regression it carries while leaving nothing on the stack.
  Identity probe: `const rhs=[1]; let a; let out=([a]=rhs); out===rhs` — `a` is **declared**, because in this strict-only core an undeclared target makes the store a write to an undeclared global and `Op::SetGlobal` raises a ReferenceError (`vm/dispatch.rs:195-222` at `d49465d0`). Today's no-op emits no store and hides that, so the undeclared spelling goes red on *setup* the moment the child of umbrella 0bc that owns the destructuring-assignment store lands it, testing nothing about the completion value.
- **Slice L** — a label is recorded as iterative only when a loop keyword follows it *immediately*, so
  chained labels lose the outer one. `parser/stmt.rs:113-117` computes
  `is_iteration = matches!(self.at(), TokenKind::Keyword(Keyword::For | Keyword::While | Keyword::Do))`
  from the token after the colon and pushes it with the label; `parser/control_flow.rs:444-454` then
  raises "'continue' label '…' does not refer to an iteration statement" whenever
  `!is_break && !is_iteration`. So `outer: inner: while (cond) { continue outer; }` is rejected though
  it is valid. The SDO that decides a labelled `continue`'s target is ECMA-262 **§8.3.3**
  ContainsUndefinedContinueTarget: its `LabelledStatement : LabelIdentifier : LabelledItem` production
  is "Let newLabelSet be the list-concatenation of labelSet and « label »" and recurses with it, its
  `BreakableStatement : IterationStatement` production is "Let newIterationSet be the
  list-concatenation of iterationSet and labelSet", and its `ContinueStatement : continue
  LabelIdentifier ;` production returns false when iterationSet contains the label — so a chain
  accumulates and every label in it names the loop. It is invoked as an early error from **§16.1.1**
  Static Semantics: Early Errors ("It is a Syntax Error if ContainsUndefinedContinueTarget of
  StatementList with arguments « » and « » is true"). **§14.8.1** is the separate rule — "It is a
  Syntax Error if this ContinueStatement is not nested … within an IterationStatement" — and decides
  nesting, not label targets.
- **Slice 2aa** — a computed field name is evaluated **once, when the class is defined**, and the key is
  retained for later instances. The baseline emits nothing at all for a non-static field:
  `ClassMemberKind::Property` (`compiler/expr_class.rs:393`) is wholly inside `if *is_static`
  (`:402`), and `:428` reads "Non-static properties: skip (would need field initializer injection)" —
  so the computed key is skipped with the rest. ECMA-262 **§15.7.14** ClassDefinitionEvaluation step 26
  runs ClassElementEvaluation over every class element at definition time; **§15.7.10**
  ClassFieldDefinitionEvaluation step 1 is "Let name be ? Evaluation of ClassElementName" and step 4
  returns a ClassFieldDefinition Record carrying it as `[[Name]]`; §15.7.14 step 30 then sets
  `ctorFunc.[[Fields]]` to that list, so construction reuses the key rather than recomputing it. An
  implementation that injects initializers into the constructor passes the `x = 1` and receiver tests
  while evaluating the key per construction. Probe:
  `let n=0; class C { [++n]=1 } new C; new C; n===1`.
- **Evidence for Slice K** — the class-name binding is initialized **before** static elements run.
  ⚠ **This stood as a "shared prerequisite of Slices 2ac and 2b", stated in prose because it sat outside
  either charter, and ending by naming "whichever of 2ac/2b lands first" as its owner. §5 now carries it
  as a terminal prerequisite **row**, so the ordering is read from **2b**'s `Deps` cell and this
  bullet is evidence only.** ⚠ *This read "2ac's and 2b's", and 2ac's edge has since been removed:
  neither of 2ac's regressions reads the inner binding, so it consumed no artifact of K's, and what
  remains between them is coordination over the shared `ClassMemberKind::Property` arm.* What the prose form could not do is what a slot could not do either: moving
  the initialization is a concrete compiler artifact rather than an owner-free decision, and the two
  consumers carry no edge between them, so their mandatory plan-reviews may run concurrently and
  "whichever lands first" orders nobody to produce it — Slice **K**'s row states both reasons.
  `compile_class` (`compiler/expr_class.rs:99`) emits every member in its body loop, the
  `ClassMemberKind::StaticBlock` arm (`:448`) included, and only afterwards initializes the inner
  class-name binding (`:479-500`; `Op::InitLocal` at `:498`, `Op::SetLocal` at `:499`). ECMA-262
  **§15.7.14** orders it the other way: step 28.a is "Perform ! classEnv.InitializeBinding(classBinding,
  ctorFunc)", and step 32 is the loop over staticElements that performs `DefineField(ctorFunc,
  elementRecord)` for a static field (step 32.a.i) and "Call(elementRecord.[[BodyFunction]], ctorFunc)"
  for a static block (step 32.b.ii) — the sub-step numbers re-read from
  `webref body ecma262 sec-runtime-semantics-classdefinitionevaluation`, where step 28 is itself the
  guard "If classBinding is not undefined, then". So
  `class C { static self = C; static { if (C !== this) throw 0 } }` reads an uninitialized binding
  today — of which K's own acceptance takes the static-**field** half, the block half being 2b's for the
  scope reason both rows now state.
- **Shared prerequisite of Slices 2aa, 2ac and 2b, and of the children umbrella **2a**'s derivation mints** (an ordering fact, so it is stated once here rather than on
  a row — and a shared prerequisite sits outside either *charter* but is **not owner-free**, so this bullet ends by naming its owner — §5's single-home rule) — `arguments` is
  accepted inside a class field initializer, and the 2a family is what turns that from inert into
  observable behaviour. The `arguments` arm of `visit_expr` (`scope/visitor.rs:241-263` at
  `658cc302`) walks the scope stack outwards and raises "'arguments' is not allowed in class static
  blocks" only on reaching a `ScopeKind::StaticBlock` (`:246-252`); every other kind falls to
  `_ => {}` (`:260`) until the first non-arrow `ScopeKind::Function`, which it marks
  `uses_arguments = true` (`:256-258`). `visit_class_body` (`:542-564`) pushes that scope for a
  `ClassMemberKind::StaticBlock` (`:555`) and for nothing else — a `Property` / `PrivateField`
  initializer is visited with a bare `visit_expr(prog, state, *v)` (`:549-552`) — so
  `function f(){ class C { x = arguments } }` reaches the enclosing function's arm and is accepted.
  ECMA-262 **§15.7.1** Static Semantics: Early Errors makes it a Syntax Error at the field
  production: under `FieldDefinition : ClassElementName Initializer[opt]`, "It is a Syntax Error if
  Initializer is present and ContainsArguments of Initializer is true", where **§15.7.9**
  ContainsArguments returns true for an `IdentifierReference : Identifier` whose StringValue is
  "arguments" and otherwise recurses into child nodes (§-numbers from
  `webref aoid ecma262 ContainsArguments` and `webref heading ecma262 15.7.1`, step text from
  `webref body ecma262 sec-class-definitions-static-semantics-early-errors` /
  `sec-static-semantics-containsarguments`). ⚠ **It is inert for the *instance* form only, and the
  "inert today" reading that stood here was scoped to the wrong half.** The early error sits on
  `FieldDefinition : ClassElementName Initializer_opt`, and `ClassElement` derives that production
  **twice** — `FieldDefinition ;` *and* `static FieldDefinition ;` (`webref body ecma262
  sec-class-definitions`) — so it governs static fields identically. Nothing emits a non-static field
  initializer (`compiler/expr_class.rs:428`, "Non-static properties: skip (would need field
  initializer injection)"), but static ones **are** emitted (`:393`, inside `if *is_static` at
  `:402`), so `function f(){ class C { static x = arguments } }` reads the enclosing function's
  `arguments` **today**, not on the family's first landing. Same evidential standard as the rest of
  this bullet — derived from the grammar and the arms, not probe-measured. The rejection is therefore
  owed by umbrella **A**, whose charter is this clause's parser early errors and whose files are the two this bullet names (`parser/mod.rs`, `parser/function.rs`) — ⚠ *A read "the terminal prerequisite row" here until this revision, and an umbrella is neither terminal nor a prerequisite: it ships no PR and never lands*; this sentence gives the reason, placing the owner and the acceptance on the child A's derivation mints is a mandatory output of that derivation, and 2aa's `Deps` cell carries the one edge on that child — §5's single-home rule. ⚠ **"whichever of 2aa / 2ab / 2ac first reaches an initializer" stood here and ordered nobody**: all three `Deps` cells were silent on it, so the three plan-reviews could each design the same parser/scope change independently — the defect rows **K** and **A** were themselves minted to fix, restated in prose one round later. **Derived from the arms and the scope pass; not
  probe-measured.** ⚠ **`await` and `yield` reach the same three call sites — that half is
  probe-measured, and its spec basis is unresolved, so no citation is written for it.** The arms are
  the ones just named: all three field sites call `parse_optional_initializer`
  (`parser/mod.rs:324-331`), namely `parser/function.rs:416` (the `static`-named field), `:482` (the
  private field) and `:579` (the ordinary field), and that helper changes **no** parse context at
  all — against the static-block arm one branch over (`:432-447`), which saves `self.context` and
  clears `in_function`, `in_async`, `in_generator`, `in_loop` and `in_switch` before parsing its
  body. Probed through `elidex_js::parse_script` at `658cc302`
  (`git diff --stat 658cc302 HEAD -- crates/` is empty, so the readings hold at HEAD), five spellings
  are **accepted**:
  `async function f(){ class C { static x = await 1 } }`,
  `async function f(){ class C { x = await 1 } }`,
  `function* g(){ class C { static x = yield 1 } }`,
  `function* g(){ class C { x = yield 1 } }` and
  `async function f(){ class C { static #x = await 1 } }` — against three controls that are all
  **rejected**: `async function f(){ class C { static { await 1 } } }` (the arm that clears the
  flags), `function f(){ await 1 }` (no enclosing async), and `async function f(){ class C { x = ) } }`
  (syntax garbage, so the harness does detect errors at this position). ⚠ **The governing rule is
  *absent*, and the absence is measured rather than open — this document settles the spec half and
  records the divergence that leaves.** ⚠ **"The rule that governs this is not identified, and this
  document does not settle it" stood here**: an unidentified rule and a measured absence are different
  claims, and only the second can be falsified, so it is restated as the second now that the
  complement has been read. The obvious candidate is absent:
  the grammar propagates **both** parameters into the initializer —
  `FieldDefinition[Yield, Await] : ClassElementName[?Yield, ?Await] Initializer[+In, ?Yield, ?Await]opt`
  — and the extract is not lossy about suppression, since the sibling production in the same output
  renders `ClassStaticBlockStatementList : StatementList[~Yield, +Await, ~Return]opt`
  (`webref body ecma262 sec-class-definitions`); the only `await` early error in **§15.7.1** is the
  static-block one (`webref body ecma262 sec-class-definitions-static-semantics-early-errors | grep -c -i await`
  → **1**, the matching line being "It is a Syntax Error if ClassStaticBlockStatementList Contains
  await is true"); and **§15.7.10** ClassFieldDefinitionEvaluation step 2.5 builds the
  initializer with `OrdinaryFunctionCreate(%Function.prototype%, sourceText, formalParamList,
  Initializer, non-lexical-this, envRecord, privateEnv)` — an *ordinary* function, neither async nor
  generator. Meanwhile the behaviour other engines ship is the opposite of what those three readings
  would predict: in Node v24.16.0, `new Function(src)` throws a SyntaxError for each of the four
  **public** `await`/`yield` initializer spellings above (the private one was not put to it)
  **and accepts** the computed-name forms
  `async function f(){ class C { [await 1] = 2 } }` and `function* g(){ class C { [yield 1] = 2 } }`.
  So the divergence is measured on both sides now, because the *absence* is measured too.
  **The complement, over the whole single-page spec body `webref body ecma262` extracts its chapters
  from**: `Contains await` occurs **2** times — the `ClassStaticBlockStatementList` early error quoted
  just above, and `Let async be body Contains await` inside ParseModule, which is not an early error —
  and `Contains AwaitExpression` occurs **6** times, every one of them over a *parameter list*
  (`ArrowParameters`, `UniqueFormalParameters` ×2, `FormalParameters` ×2,
  `CoverCallExpressionAndAsyncArrowHead`), never over an `Initializer`; and exactly **3** early errors
  in the entire spec name `Initializer` at all — the two §15.7.1 rules quoted above and the
  `LexicalBinding` const-without-initializer rule. The grammar chain is unbroken to match:
  `ClassDeclaration` / `ClassExpression` → `ClassTail` → `ClassBody` → `ClassElementList` →
  `ClassElement` → `FieldDefinition` each propagate `[?Yield, ?Await]`, and the `FieldDefinition`
  production quoted above is the raw `emu-production` (`params="+In, ?Yield, ?Await"`), not a lossy
  rendering of a suppressed `[~Yield, ~Await]`. ⇒ **ECMA-262 as published accepts all five spellings,
  and the engines are the divergent side.**
  ⚠ **Which inverts the risk this bullet assumed.** It read the danger as a slice shipping "an inferred
  Node-compatible rejection" without a spec basis; measured, there is no spec basis *for rejecting*, so
  a slice that rejects diverges from the published grammar while a slice that keeps accepting matches
  the grammar and matches no other engine. Node v24.16.0 parses the initializer `[~Yield, ~Await]` and
  its own messages say so: `async function f(){ class C { x = await } }` is **accepted** (`await` taken
  as an `IdentifierReference`), `x = await 1` fails "Unexpected number", and both `yield` forms fail
  "Unexpected strict mode reserved word" because a class body is strict code — none of which a
  `[+Await]` / `[+Yield]` parse could produce. The one reading that cuts the other way is internal to
  the spec rather than an engine's: **§15.7.10** builds the initializer with `OrdinaryFunctionCreate`,
  an *ordinary* function, so a licensed AwaitExpression would have no runtime semantics to reach —
  evidence about where the gap is, and an input to the decision, not a rule.
  ⚠ **So this is a *policy* choice, and by this bullet's own test that is the kind needing a single
  owner.** The test stated here was that I-5's question needs one owner because it is a policy choice
  two reviewers could settle differently and legitimately, while this one is "a *spec* fact with one
  right answer, so the risk is duplicated derivation rather than divergence". That premise is now
  measured false: with no rule to derive, 2aa, 2ab and 2ac would not be duplicating a derivation, they
  would be choosing between spec-as-written and engine consensus, and they could choose differently.
  **The choice is therefore owed by umbrella A**, whose charter is this
  clause's parser early errors and whose files are the two this bullet names (`parser/mod.rs`,
  `parser/function.rs`) — the same routing, on the same three call sites, that this bullet already assigns
  the `arguments` half one paragraph above; this sentence gives the reason, A's derivation places the
  owner and the acceptance on the child it mints, and the edge onto that child belongs in each
  field-initializer slice's `Deps` cell — §5's
  single-home rule. ⚠ **That is 2aa and 2ac, not 2aa alone — and not the children umbrella 2ab's derivation mints, on A's own test**: of the five accepted spellings
  **three are static** (`static x = await 1`, `static x = yield 1`, `static #x = await 1`) against two
  instance forms, static initializers are the ones already emitted at the baseline, and **2ac** owns
  their lowering with **no dependency on 2aa/2ab** (§5's `Deps` column), so it can land and retire
  first while every static form still inherits the enclosing async/generator context. Membership
  follows from touching an initializer rather than from a list of slice ids kept in step — which is why the children umbrella 2ab's derivation mints are
  outside it: its `Primary module(s)` cell holds no `parser/` or `compiler/` file, so it reaches no initializer
  parse, the same test A's own cell applies when it gives the children umbrella 2ab's derivation mints no edge on (ii). Both consuming cells carry the edge on the child
  **A**'s derivation mints, at this revision — ⚠ *they read "the **A** edge" here while their own cells
  name the child, and naming A itself would name nobody (§5).* ⚠ **"placing the edge in each cell is a mandatory output of A's plan-review
  rather than an assertion here" stood here and was withdrawn on the ground that both endpoints are minted
  terminal rows — a ground that expired when A became an umbrella, so the withdrawal is itself withdrawn:
  placing the edge **is** a mandatory output of A's derivation, and what it places is the child, not A.**
  What the earlier deferral did wrong is unchanged and is why the cells are written now rather than later:
  it left 2ac reading `—` while this bullet claimed a single prerequisite owner.
  ⚠ **"Whichever of 2aa / 2ab / 2ac *lands* first with a field initializer on its path resolves it"
  stood here and is withdrawn — the second landing-order owner written into this bullet.** All three
  rows are edge-free **within the family** apart from `2ab → 2aa` (each carries extra-family edges — 2aa and 2ac both on the child **A**'s derivation mints), so their plan-reviews run *concurrently*; landing order is
  not a dependency and names nobody accountable before the fact, which is the defect rows **K** and
  **A** were minted to fix. The form before it read "the mandatory plan-review of whichever … first
  reaches", and ordered nothing either. ⚠ **And the exemplar the landing argument leaned on had itself
  been withdrawn**: `2ac`/`2b`'s class-name-ordering prerequisite was a compiler *artifact* rather than
  a spec fact, so nothing was serialised by landing, and it became the terminal prerequisite row **K**
  that **2b** now depends on (⚠ *"that both rows now depend on" stood here; 2ac's edge was removed once
  its regressions were measured not to read the inner binding*). Writing a replacement citation from
  recall is how the citation this bullet already corrects got in.
- **Shared prerequisite of Slices 2 and 3** (an ordering fact, so it is stated once here rather than on
  either row — and a shared prerequisite sits outside either *charter* but is **not owner-free**, so this bullet ends by naming its owner — §5's single-home rule) — **a class field
  initializer and a class static block each need a home object of their own, and neither producer has
  one.** Umbrella 3's children make `super.x` work for the producers that already reach lowering with class frame
  state; these two do not reach it at all, so umbrella 3's children can retire with both still broken and umbrella 2's children can connect
  both while `super` inside them stays unavailable. Two producers, two layers, both measured at
  `b5356098` (`git diff --stat 658cc302 b5356098 -- crates/` is empty, so both readings hold at
  `658cc302`): (i) a **field initializer** is parsed by `parse_optional_initializer`
  (`parser/mod.rs:324-331`), which changes no parse context at all, and all three field sites call it
  that way (`parser/function.rs:416` the `static`-named field, `:482` the private field, `:579` the
  ordinary field), so the initializer inherits the class body's ambient context — while the `super`
  gate at `parser/primary.rs:417-428` errors with "'super' keyword unexpected here" unless
  `in_method || in_constructor || in_static_block`, none of which a class body sets. So
  `class B extends A { x = super.x }` is rejected outright at parse time, and what decides the verdict
  is where the class is *written* rather than what a field initializer is: the same text inside a
  method inherits that method's `in_method` and passes the same gate. (ii) a **static block** parses
  with `in_static_block = true`
  (`parser/function.rs:432-447`) and so passes the gate, but the compiler's arm builds an ordinary
  child closure — `FunctionCompiler::new(fc.func_scope_idx, fc.current_scope_idx, true)`
  (`compiler/expr_class.rs:456-457`), whose third argument is `is_strict`
  (`compiler/function.rs:81`) — and `is_class_ctor` stays at its `false` default
  (`compiler/function.rs:95`; the only two writes are `compiler/expr_class.rs:82` and `:125`, both the
  constructor). A frame's `home_class` is `Some` only when `compiled.is_class_ctor`
  (`vm/interpreter.rs:802-806`), and `dispatch_super_inner` raises
  `VmError::syntax_error("'super' keyword unexpected here")` when it is `None`
  (`vm/dispatch_class.rs:115-119`), so `class B extends A { static { this.y = super.x } }` cannot
  resolve the superclass. ECMA-262 gives both producers the class as an explicit argument:
  **§15.7.10** ClassFieldDefinitionEvaluation takes `homeObj` and, for
  `FieldDefinition : ClassElementName Initializer[opt]`, creates the initializer function at step 2.5
  (`OrdinaryFunctionCreate(%Function.prototype%, sourceText, formalParamList, Initializer,`
  `non-lexical-this, envRecord, privateEnv)`) and performs "MakeMethod(initializer, homeObj)" at step
  2.6; **§15.7.11** ClassStaticBlockDefinitionEvaluation takes the same argument and does the same at
  its steps 5 and 6 for `bodyFunc`; **§10.2.7** MakeMethod ( func, homeObj ) is what sets
  `[[HomeObject]]` (§-numbers from `webref aoid ecma262 ClassFieldDefinitionEvaluation` /
  `ClassStaticBlockDefinitionEvaluation` / `MakeMethod`, steps from `webref body ecma262`
  `sec-runtime-semantics-classfielddefinitionevaluation` /
  `sec-runtime-semantics-classstaticblockdefinitionevaluation`). Regressions, one per producer and one
  per receiver: instance-field `class B extends A { x = super.x }`, static-field
  `class B extends A { static x = super.x }` and static-block
  `class B extends A { static { this.y = super.x } }` must each read `A`'s property. ⚠ **The observable stated above is reached by none of the three.** Measured at `658cc302`, `class A { static x = 5 } class B extends A { static { this.y = super.x } } B.y` raises `TypeError: Cannot convert undefined or null to object`, not `dispatch_super_inner`'s `syntax_error`, because no `super`-property read is emitted at all (`ExprKind::Super` → `Op::PushUndefined`, `compiler/expr.rs:200-207`); the same block with `this.y = 99` already answers `99`, so the class frame state this bullet names is not what the three are red on, and a repair confined to `home_class` leaves all three red. **Derived from the
  arms; not probe-measured.**
  ⚠ **Owner, per producer — this bullet named none, and with none all three regressions could stay
  broken with every row retired**: umbrella 3's children retire having fixed the producers that already reach lowering
  with class frame state, and the 2 family retires having connected fields and static blocks, while
  nothing applies `MakeMethod` to the *new* producer closures. Discharge needs both sides to exist, so
  the owner is **whichever lands second**, per producer, and the table already has a concrete slice id
  for each — deferring the field half to "the slice that creates the initializer closure" was a
  derivation standing in for ids that exist, which let both field rows retire owning nothing:
  **public instance field → whichever of 2aa and *the child of umbrella 3 that owns the `[[HomeObject]]` representation on the closure* lands second**, **static field → whichever of 2ac and
  *the child of umbrella 3 that owns the `[[HomeObject]]` representation on the closure* — covering *both* static arms, `Property` and `PrivateField`**, **static block → whichever of 2b and *the child of umbrella 3 that owns the `[[HomeObject]]` representation on the closure***, and — ⚠ **the arm the first mapping still missed** —
  **private instance field → whichever of *the child of umbrella 3 that owns the `[[HomeObject]]` representation on the closure* and *the child of umbrella 5 that mints the field record* lands second** (the same correction as the method/accessor clause below — only that half was retargeted when 5 became an umbrella), and — since `PrivateMethod` is now Slice 5's (that row) — **private methods and accessors, static and instance → whichever of *the child of umbrella 3 that owns the `[[HomeObject]]` representation on the closure* and *the child of umbrella 5 that mints the method/accessor closure* lands second** — 5's derivation assigns this handoff to that child, and the child carries the coordination with that child of umbrella 3, with `class B extends A { #m(){ return super.x } read(){ return this.#m() } }` as its regression. `ClassMemberKind::Method` (`:293`) and `ClassMemberKind::PrivateMethod` (`:347`) are separate arms, so that child can repair the public one and umbrella 5's own child replace the private one with neither carrying the home object. `ClassMemberKind::PrivateField`
  (`compiler/expr_class.rs:430`) is a **separate compiler arm** from `Property` (`:393`), and §2.2's
  `expr_class.rs:430-447` row gives its missing non-static branch to *the child of umbrella 5 that mints the field record*, so a mapping naming only
  2aa lets every listed owner retire with `class B extends A { #x = (seen = super.x, 1) }` still broken
  while 2aa's public regression passes. ECMA-262 **§15.7.10** puts them under one production —
  `FieldDefinition : ClassElementName Initializer[opt]`, where `ClassElementName` derives `PropertyName`
  **and** `PrivateIdentifier` — and its step 2.6 `MakeMethod(initializer, homeObj)` is unconditional
  (`webref body ecma262 sec-runtime-semantics-classfielddefinitionevaluation`). ⚠ **This is the third
  finding in this loop from that same two-arm split** (private static receiver, private static
  initializer execution, and now the private home object), so a producer enumeration that names a public
  arm without asking what its private twin does is not finished. Each owner integrates the home object
  *and* runs this bullet's regression for its own producer — `class B extends A { x = super.x }` for 2aa,
  a **side-effect** form `class B extends A { #x = (seen = super.x, 1) }` for *the child of umbrella 5 that mints the field record* (a private *read* is 5's
  own deliverable, so the regression must not depend on one),
  `class B extends A { static x = super.x }` **and its private twin
  `class B extends A { static #x = (seen = super.x, 1) }`** for 2ac — the receiver regression 2ac already owns
  tests `this`, not the `MakeMethod` path §15.7.10 step 2.6 requires unconditionally, so it does not stand in for this
  one — and `class B extends A { static { this.y = super.x } }` for 2b.
- **Shared prerequisite of Slices 2b and M** (an ordering fact, so it is stated once here rather than on
  either row — and a shared prerequisite sits outside either *charter* but is **not owner-free**, so this bullet ends by naming its owner — §5's single-home rule) — **`await` is
  accepted inside a class static block in module mode.** `parse_class_member`'s static-block arm
  (`parser/function.rs:432-447` at `b5356098`) saves the context and clears `in_function`, `in_async`,
  `in_generator`, `in_loop` and `in_switch` while setting `in_static_block = true` (`:434-439`); it does
  not touch `is_module`. `await_is_keyword` (`parser/mod.rs:61-63`) is
  `self.in_async || self.in_async_params || (self.is_module && !self.in_function)`, so clearing
  `in_function` inside a module *satisfies* it, and the `Await` arm (`parser/expr.rs:208-226`) errors
  only for `in_async_params` (`:213-218`) before allocating `ExprKind::Await` at `:222-225`. So the
  moment the child of umbrella M that enables `parse_module` gives it a production caller, `class C { static { await 0 } }` parses, and
  Slice 2b's generic static-block lowering compiles it into the synchronous child closure of
  `compiler/expr_class.rs:456-466` — the silent-wrong class this umbrella exists to remove, arriving
  through a parser gate rather than a compiler arm. ECMA-262 forbids it as an early error:
  **§15.7.1** Static Semantics: Early Errors, production
  `ClassStaticBlockBody : ClassStaticBlockStatementList` — "It is a Syntax Error if
  ClassStaticBlockStatementList Contains await is true" (§-number from
  `webref heading ecma262 15.7.1`, rule text from
  `webref body ecma262 sec-class-definitions-static-semantics-early-errors`). ⚠ **The rule is the
  `Contains` SDO applied to the terminal `await`, not an AO named `ContainsAwait`**:
  `webref aoid ecma262 ContainsAwait` returns "no AO with aoid='ContainsAwait' in ecma262", so do not
  cite one. The fix is the parser early error, and ⚠ **"whichever comes first of 2b and the terminal child of umbrella M" is withdrawn: it orders nobody, which is the defect row K was minted to fix, and it is the only scheduling instruction this artifact had — 2b's `Deps` names only K, and umbrella M has no child to name yet.** So the artifact takes a **row of its own — row `A` in §5, an umbrella from this revision**, on the same occasion the class-name-ordering artifact became row **K** once the same wording was measured to order nobody — ⚠ *this sentence read "a terminal prerequisite row of its own … it lands before either consumer, both carry a `Deps` edge to it", and an umbrella does neither*: A's derivation mints the child that lands before either consumer, both consumers carry the `Deps` edge on **that child**, and neither can retire while `class C { static { await 0 } }` is still accepted in module mode —
  before the child of umbrella M that enables `parse_module` lands and before 2b lowers a body that can contain the token. Regression in
  **module mode**: `class C { static { await 0 } }` parsed through `parse_module` is a SyntaxError. One
  spelling is enough because the arm's other clears close the other routes into
  `await_is_keyword` — it sets `in_async = false` (`:435`), so a static block nested in an `async`
  function does not reach the first disjunct, and `in_async_params` is not a class-body state; the
  module disjunct is the only one the arm leaves live. **Derived from the arms; not probe-measured.**
- **Umbrella 3's derivation** — an object-literal method gets `[[HomeObject]]` too, and the frame state this row's mechanisms
  threads is class-only. The parser already enables `super` there: `parse_method_function`
  (`parser/object.rs:334`) sets `this.context.in_method = true` (`:345`) under the comment "S4: methods
  have [[HomeObject]] — super.prop is valid" (`:344`), and an object-literal method is mapped
  `MethodKind::Method | MethodKind::Constructor => PropertyKind::Init` (`:60`). The compiler then
  installs it through the ordinary paths with no home-object state: `compile_object_expr`
  (`compiler/expr_object.rs:46`) compiles the `PropertyKind::Init` value with `compile_expr` and
  defines it with `Op::DefineProperty` / `Op::DefineComputedProperty`, and the accessor kinds go
  through `compile_accessor`. `git grep -n 'home_class' 658cc302 -- crates/script/elidex-js/src`
  reaches `compiler/expr_class.rs:60`, `compiler/function.rs:40`, `compiler/expr_member.rs:111`,
  `bytecode/` and `vm/` but no line in `compiler/expr_object.rs`, and the frame field is derived from
  `compiled.is_class_ctor` (`vm/interpreter.rs:802`). ECMA-262 routes both spellings to the same
  operation: **§13.2.5.6** PropertyDefinitionEvaluation, production
  `PropertyDefinition : MethodDefinition`, step 1 "Let result be ? MethodDefinitionEvaluation of
  MethodDefinition with arguments obj and true"; **§15.4.5** MethodDefinitionEvaluation step 1 "Let
  methodDef be ? DefineMethod of MethodDefinition with argument obj"; **§15.4.4** DefineMethod step 7
  "Perform MakeMethod(closure, obj)"; **§10.2.7** MakeMethod step 2 "Set func.[[HomeObject]] to
  homeObj" — and §15.4.5's `get` / `set` productions perform MakeMethod at their own step 7. The audit as that
  row stated it while terminal is over `home_class` and a `new B().m` regression, so every class probe
  can pass while `const o={m(){return super.x}}; Object.setPrototypeOf(o,{x:1}); o.m()` stays broken —
  the discriminating measurement umbrella 3's row now cites as mechanism (d), and placing this producer
  on a child is a mandatory output of that derivation.
- **Slice M** — the parser already accepts top-level `await`, and the VM asserts it cannot happen.
  `parse_module` (`lib.rs:94`) builds the parser with `ProgramKind::Module`, which sets `is_module`
  (`parser/mod.rs:104`); `await_is_keyword` on `ParseContext` (`parser/mod.rs:61-63`) is
  `self.in_async || self.in_async_params || (self.is_module && !self.in_function)`; and
  `parser/expr.rs:208-226` allocates `ExprKind::Await` under it, its comment reading "Await — B21: also
  valid at top level in modules (not inside sync functions)". On the VM side `call_internal`
  debug-asserts `matches!(kind, FrameKind::Function) || !(is_async || is_generator)`
  (`vm/interpreter.rs:575-579`) and the comment above it (`:566-574`) says top-level bodies are never
  async or generator, "top-level await deferred". M's row derives over the **grammar** — ECMA-262
  §16.2.2 Imports and §16.2.3 Exports — and that derivation cannot reach this: module *execution* is
  **§16.2.1** Module Semantics. **§16.2.1.7.3.2** ExecuteModule takes the synchronous branch at step 3
  only "If module.[[HasTLA]] is false" and otherwise asserts a PromiseCapability Record and performs
  `AsyncBlockStart` at step 4; **§16.2.1.6.1.3.1** InnerModuleEvaluation step 12 routes a module whose
  `[[HasTLA]]` is true to **§16.2.1.6.1.3.2** ExecuteAsyncModule (whose step 2 asserts `[[HasTLA]]` is
  true), against step 13's plain "Perform ? module.ExecuteModule()". So M's derivation must reach
  §16.2.1 and not only the §16.2.2/§16.2.3 productions — the members-vs-semantics distinction at the
  head of this section, in its module spelling.
- **Slice B** — three consecutive rounds each produced one more construct whose block scope is never
  entered, one per round, and that set is derivable in two commands. Run at `658cc302`:
  `git grep -n 'ScopeKind::Block' 658cc302 -- crates/script/elidex-js/src/scope` returns **7** push
  sites once `scope/tests.rs` is excluded (`scope/visitor.rs:72`, `:85`, `:114`, `:131`, `:148`,
  `:166`, `:533`), and `git grep -n 'find_child_block_scope' 658cc302 -- crates/script/elidex-js/src`
  returns **5** entry sites (`compiler/stmt.rs:136`, `:315`, `compiler/stmt_loop.rs:37`, `:73`,
  `:228`) besides the `fn` definition at `stmt.rs:639` and the `use` at `stmt_loop.rs:20`. Reading
  each push site's arm maps them: `:72` is `StmtKind::Block` → `stmt.rs:136`; `:85` is
  `StmtKind::For` → `stmt_loop.rs:228`; `:114` is the `ForIn`/`ForOf` arm → `stmt_loop.rs:37` and
  `:73`; `:148` is the `try` block → `stmt.rs:315`. Three push sites have no entry: `:131`
  (`StmtKind::Switch`), `:166` (the `Try` arm's finalizer) and `:533` (`visit_class`). The first two
  are §2.2 rows and are B's; the third is not a defect — the class scope holds only the class-name
  binding, which `compiler/expr_class.rs:483-489` reaches by an open-coded scan for
  `kind == ScopeKind::Block && span == class.span` without entering the scope — but it is a **second
  copy of the lookup the (a) child of umbrella **B** is about to change**, so that child collapses it rather than leaving a diverged one
  (CLAUDE.md *One issue, one way*). **Per-iteration loop environments are not in this set and B
  does not own them**: `:85` and `:114` *are* entered, and Slice E's defect is that one environment
  serves every iteration. Correcting a "found one per round" pattern by listing the three found so
  far would have carried that misattribution in.

| # | Slice | Primary module(s) | Slot | Tier | Deps |
|---|---|---|---|---|---|
| **0a — MERGED `658cc302`** | Compound **and logical** assignment to member targets — killed **3** panic classes (the plan had recorded 1; the other two were found while implementing and land together, same concept + same files). NB only `Dup`/`Swap` exist, so preserving `[obj key]` across the load needs a **new stack-shuffle opcode** ⇒ handler only (**`bytecode/disasm.rs` needs no arm** — it dispatches generically on `op.operand_size()`; this corrects a cost model that also mis-stated Slices 1b/6/D) | ⚠ **charter, not outcome — the landing was 36 files** (`git show --stat 658cc302`), incl. `compiler/stmt.rs` + new `stmt_loop.rs`, `vm/object_kind.rs`, `vm/interpreter.rs`, `vm/value.rs` and four `vm/host/` files; [§16](2026-07-vm-p4-slice-0a-landing-record.md) has the record and §8's cold gate reasons over this column, so read [§16](2026-07-vm-p4-slice-0a-landing-record.md) first. Charter was: `compiler/expr_assign.rs`, `bytecode/opcode.rs`, `vm/dispatch.rs`, `vm/tests/{mod,tests_member_compound_assign}.rs` | **to retire, not register** (§8) `#11-vm-computed-compound-assignment` | T0 | — |
| **0b** | **Assignment/update target completeness — UMBRELLA, not a terminal unit.** The charter as written spanned binding-pattern storage, `IteratorClose` conformance ([C39]→[C36]), optional-call receiver preservation, parser-level early errors and catch-parameter binding initialization — and the row itself recorded that `peel_paren` does **not** reach the optional-call case, so there was no shared lowering chokepoint holding them together. Split below along the mechanism boundaries the charter already named. ⚠ **"the sub-slices are ordered and dependent" stood here and is withdrawn — prose restating an *authoritative* field, gone stale against the cells.** The *reason* survives and is all that belongs here: the intra-family edges `0bb → 0ba` and `0bc → 0bb` were shared-arm arguments rather than consumed artifacts, and were deleted at PR-B. What the family's ordering now **is**, is read from the `Deps` cells | — | `#11-vm-assignment-target-completeness` (umbrella) | T1 | — |
| **0ba** | **Parser-level rejections.** One contract: an arm that accepts a token the grammar cannot derive in the context the arm was reached from must reject it there, at parse time. What each of 0ba's rows has in common is that shape — an **ungated arm**, not a shared spec clause — so the rows are §2.2's and the slice reads them there rather than restating a list here: prefix `ExprKind::Spread` (the `Ellipsis` arm at `parser/expr.rs:256-263` is ungated), `delete this.#x` (ECMA-262 **§13.5.1.1**, whose first bullet is a single Syntax Error over **five** derived forms — `PrimaryExpression : IdentifierReference`, `MemberExpression . PrivateIdentifier`, `CallExpression . PrivateIdentifier`, `OptionalChain : ?. PrivateIdentifier` and `OptionalChain . PrivateIdentifier` — gated on `IsStrict(the UnaryExpression) is true`, which core elidex-js always is, and whose second bullet reapplies the whole rule recursively through a parenthesized cover grammar (`webref body ecma262 sec-delete-operator-static-semantics-early-errors`). ⚠ **Naming the member form alone leaves an arm that rejects `delete this.#x` and accepts the other four reading conformant**, so the acceptance is one parse-time **SyntaxError** per form. ⚠ **The `PrimaryExpression : IdentifierReference` form is a green guard, not a red probe, and stating it in the voice the other four are written in would retire a fifth of this acceptance on work already in the tree** (§4 I-8): `delete x` is rejected at `658cc302` by `parser/expr.rs:164-182` — the arm reading "Cannot delete an unqualified identifier in strict mode" — with the landed regression `parser/expr_tests_validation.rs:48 delete_identifier_strict_error`. It is stated as a guard the remedy may not break. ⚠ **And that arm tests the *raw* AST** — `matches!(self.exprs.get(arg).kind, ExprKind::Identifier(_))`, against `ExprKind::Paren` at `ast.rs:299` — so `delete (x)`, which is §13.5.1.1's second bullet applied to the *identifier* form, is accepted today and is a **red probe no other row carries**: 0bb owns `delete (o.x)` and this row's own recursive witness below is `delete (this.#x)`. For the four private forms a body **inside a class that declares `#x`**: `class C { #x; m(){ delete this.#x } }`, `class C { #x; m(){ delete f().#x } }`, `class C { #x; m(){ delete this?.#x } }` and `class C { #x; m(){ delete this?.a.#x } }` — plus `class C { #x; m(){ delete (this.#x) } }` for the recursive bullet, the case a fix written on the unparenthesized shapes alone misses. ⚠ **Spelled with `#x` outside any declaring class, as all five stood, every private witness is satisfied by a blanket undeclared-private-name rejection** — **§15.7.7** `AllPrivateIdentifiersValid` (`webref aoid ecma262 AllPrivateIdentifiersValid`), which is separate work — so an implementation could omit §13.5.1.1's rule entirely for otherwise valid private references and still pass the lot; the declaring class is what makes each failure exercise the delete rule rather than the missing-name one), and `#x` in an object property key, whose `parse_property_key` arm reads the token without reading its caller. ⚠ **Two of the three rows carried no acceptance at all, and a slice that implements the delete rule alone answers every condition above while both other arms stay ungated.** **Red probes at `658cc302`, one per arm**: prefix spread — `var y = ...x;` is a SyntaxError, where `parser/expr.rs:256-263`'s `TokenKind::Ellipsis` arm accepts it unconditionally and builds `ExprKind::Spread`; object-literal private key — `({ #x: 1 })` is a SyntaxError, where `parser/object.rs:200-203` returns `PropertyKey::PrivateIdentifier(name)` without consulting the caller. ⚠ **That arm has *two* callers that must reject and one that must not, so a gate on the object-literal caller alone retires this row with the binding form still accepted** — the caller set is enumerated rather than read off the arm: `git grep -n 'parse_property_key' 658cc302 -- crates/script/elidex-js/src` returns **4** lines, the definition (`parser/object.rs:176`) and three callers, of which only `parser/function.rs:518` sits in a context that derives the token (**§15.7** `ClassElementName : PropertyName \| PrivateIdentifier`), while `parser/object.rs:51` (**§13.2.5** `PropertyName : LiteralPropertyName \| ComputedPropertyName`) and `parser/pattern.rs:123` (**§14.3.3** `BindingProperty : PropertyName : BindingElement`) reach it from productions that derive none (`webref body ecma262 sec-class-definitions` / `sec-object-initializer` / `sec-destructuring-binding-patterns`). **So a third red probe, on the caller this row's `Primary module(s)` cell already names**: binding-pattern private key — `const { #x: y } = o` is a SyntaxError, and `function f({ #x: y }) {}` likewise, the same production reached from a formal parameter. Both measured **accepted, with zero parse errors**, by executing `parse_script` over them at `658cc302`. **Declared positive control for this third arm, because the class-element caller must keep accepting the token**: `class C { #x = 1; }` still parses — measured green in the same run. **Declared positive controls bounding each**, because both arms are also reached from contexts where the token *is* derivable and an over-broad gate would reject conformant source: `f(...a)` and `[...a]` still parse, and `class C { #x; m(){ return this.#x } }` and `class C { #x; m(o){ return #x in o } }` still parse. **Why that third row lands here rather than minting a slice**: it is the same contract in the same layer, its fix is a condition on an existing arm, and the two rows it might otherwise attach to own something else — `#11-vm-property-key-lowering-unification`'s scope covers how a *valid* `PropertyKey` lowers, and umbrella **5**'s charter covers class-private *operations*, and every child its derivation mints can complete without touching a non-class spelling. Adding it therefore adds no axis that intersects the one this row already carries — the three arms are one parse-time contract, each fix a condition on its own arm, and §13.5.1.1, §13.2.5, §14.3.3 and §15.7 each define the production that governs it — so the criterion above reaches neither disjunct and leaves the slice terminal | `parser/expr.rs`, `parser/object.rs`, `parser/pattern.rs` | `#11-vm-assignment-target-completeness` (0ba) | T1 | — |
| **0bb** | **References through a wrapper** — the arms that test the **raw** AST and are defeated by an `ExprKind::Paren` (`ast.rs:299`) or by an optional chain, losing the operand's Reference: the parenthesized callee `(o.m)()` (`expr_member.rs:92`) and the parenthesized assign/update targets `(x)++` / `(x)+=1` / `(a[0])++` — the `peel_paren` chokepoint of §9 dec. 14 — plus `delete (o.x)` / `delete o?.x` and `typeof (missingName)` (`expr_ops.rs:114-163`), plus the flat optional member call `o.m?.()` (`expr_member.rs:180-187`), which `peel_paren` does **not** reach: `compile_optional_chain_expr`'s `prev_is_member` reads `chain[i-1]` and never consults the chain's base. One contract — an operand that evaluates to a Reference must arrive at the arm that consumes it — over two mechanisms ⚠ **plus one negative direction, absent here, which is the failure mode a `peel_paren` chokepoint *introduces* rather than fixes**: peeling is admissible only where the parenthesized operand's `AssignmentTargetType` is `simple`, so a chokepoint applied ahead of that test turns `({a}) = x` into a valid destructuring assignment. **§8.6.4** AssignmentTargetType returns the covered expression's own value through `PrimaryExpression : CoverParenthesizedExpressionAndArrowParameterList`, and `ObjectLiteral` and `ArrayLiteral` are in the production list returning **invalid**, so **§13.15.1**'s second bullet — the LHS being neither an ObjectLiteral nor an ArrayLiteral, being a *parenthesized* one — makes it a Syntax Error (`webref body ecma262 sec-static-semantics-assignmenttargettype` / `sec-assignment-operators-static-semantics-early-errors`). Required negative regressions: ⚠ **both are green at `658cc302` and are therefore stated as guards, not as work** — `parser/expr.rs:529-554` `is_valid_assign_target` sets `seen_paren` when it unwraps `ExprKind::Paren` and gates its `Object`/`Array` arm on `!seen_paren`, its own comment stating the rule, so both already produce "Invalid left-hand side in assignment". ⚠ **And the positive direction — the eight defect sites this row exists to repair — stated no condition at all**, which is why the escape clause "every positive regression this row lands passes with both broken" constrains nothing: it quantifies over regressions the row never wrote. **Red probes at `658cc302`, one per defeated shape**: `(o.m)()` binds `this` to `o`; `(x)++` and `(x)+=1` store back to `x`; `(a[0])++` stores back to `a[0]`; `delete (o.x)` deletes the property; `delete o?.x` deletes it when `o` is non-nullish and evaluates to `true` when nullish; `typeof (missingName)` is `"undefined"` rather than a ReferenceError; and `o.m?.()` binds `this` to `o`. `({a}) = x` and `([a]) = x` must each remain a **SyntaxError**, and every positive regression this row lands passes with both broken; [§6.4](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) edge 35 | `compiler/expr_member.rs`, `compiler/expr_ops.rs`, `compiler/expr_assign.rs`, `compiler/expr.rs` | `#11-vm-assignment-target-completeness` (0bb) | T1 | — |
| **0bc** | **Binding-pattern storage — UMBRELLA, not a terminal unit.** ⚠ **This row read as terminal, and the span its own charter sentence lists is what ends that.** §5's criterion asks whether a row bundles ≥3 intersecting invariant axes, and this one bundles five: **§8.6.2** `BindingPattern : ArrayBindingPattern` step 3's `IteratorClose` across every array-binding consumer (parameters, `var`/`let`/`const`, both loop heads); §8.6.2's `ObjectBindingPattern` step 1 with **§13.15.5.2** `RequireObjectCoercible` across the declaration *and* the assignment forms, which sit in two different in-tree arms with opposite polarities; **§13.15.5.5** `AssignmentRestElement` step 4's drain, a VM opcode repair in a different layer from either; the **GC operand-rooting** invariant this row's own `Deps` cell carried; and **§14.15.2** step 5's statement-head pattern binding for the catch parameter, which never reaches `compile_destructure_pattern`'s array arm at all. §5's own rule that a derivation's output is a grouping and not a slice applies literally — these share no lowering chokepoint. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. **The two edges this row's `Deps` cell carried — Pa and `#11-vm-operand-rooting-by-construction` — are withdrawn from that cell and re-pointed onto the specific children that consume them, by the same derivation.** The charter it routes: destructuring assignment (`[a,b]=…`, `({x}=…)`, for-of pattern heads), `for (obj.p in …)`, the dead `AssignTarget::Pattern` arm, and the destructured catch parameter `catch ({x})` / `catch ([a])` (§2.2's `stmt.rs:424-439` row; ECMA-262 §14.15.2 step 5). 0bc **gains** the catch parameter rather than finding it already in charter: it is the only slice lowering a `PatternKind` reached from a statement head (`compile_forin_left_binding`, evidence block above), matched only for `Identifier`. Carries the [C39]→[C36] `IteratorClose` conformance, which is why it waits on **Pa** — re-pointed at PR-B with the Pa/Pb inversion: what 0bc would otherwise inherit is the *inverted precedence contract*, and that contract is Pa's deliverable, not Pb's transport. Depending on Pa reaches Pb through Pa's own Deps. **Charter widened at PR-B: the `IteratorClose` obligation is over every array binding-pattern consumer, not over destructuring assignment alone.** As written it named [C39] DestructuringAssignmentEvaluation, which is the *assignment* form; the declaration form is governed by **§8.6.2** BindingInitialization step 3 and goes through the same `compile_destructure_pattern` arm, which pops an unexhausted iterator (§2.2's `stmt_destructure.rs:36-81` row) and is reached from parameter patterns, `var`/`let`/`const` declarations and both loop heads. So 0bc retiring on the assignment form alone would leave the shared arm wrong. Required regressions are **both directions of §8.6.2 step 3**, which applies `IteratorClose` to *the completion binding initialization returned*: a **normal early exit** — `const [x] = it` over an iterator whose `return()` records the call — and ⚠ **an abrupt one, which no regression here required**: a default initializer that throws (`const [x = (() => { throw new Error('init') })()] = it`) must still call `return()` on the still-open iterator, with the original error escaping. Pa's tests fix the helper's precedence *once called* and cannot prove the compiler emits the close on that edge, so an implementation that closes only on normal fallthrough passed everything this row asked for. Not only an abrupt-completion one, since [C36] is reached from step 3 on the normal path. **Deliverable on the rest form: repair and consume `Op::IteratorRest`** (`compiler/stmt_destructure.rs:70`, `vm/dispatch_iter.rs:295-332`), factoring out what it shares with `ArraySpread` rather than adding a second drain — with **no `iter_close` on the exhausted-rest path**, since **§13.15.5.5** `AssignmentRestElement` step 4 repeats only while `[[Done]] is false` and performs no close (§9 dec. 13, [§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md)). Second required regression: `[...r] = it` over an iterator that records both its `next()` calls and any `return()` — **asserting `r`'s complete ordered contents and the observed `next()`/done count, not only the absence of `return()`**. ⚠ **The no-`return()` half alone cannot fail**: an implementation that never advances `it` and stores an empty array satisfies it, while **§13.15.5.5** `AssignmentRestElement` step 4 requires repeated `IteratorStepValue` until `[[Done]]` is true — so the drain itself has to be asserted or this row retires with the rest path unimplemented and the regression green. ⚠ **And a third direction on the declaration form, which no regression here required, because §8.6.2 step 3's close is *guarded*.** The step is "If iteratorRecord.[[Done]] is false, return ? IteratorClose(iteratorRecord, result)" (`webref body ecma262 sec-runtime-semantics-bindinginitialization`, production `BindingPattern : ArrayBindingPattern`) — so a pattern that **exhausts** the iterator must close it not at all. Both required regressions above leave `[[Done]]` **false**: the normal early exit by construction, and the throwing default initializer because the throw precedes exhaustion. An implementation that closes unconditionally therefore passes both while `const [a, b, c] = it` over a one-value iterator calls `return()` where §8.6.2 calls nothing. Third required regression: that shape, over an iterator that records `return()`, asserting it was **not** called — the confinement half of the two above, and the one an over-eager fix to them produces. ⚠ **And the object form, which is in this row's charter (`({x}=…)`, and the object binding patterns reaching the same lowering) and carries no acceptance at all — every required regression above is an *array* pattern.** Its governing step is not §8.6.2 step 3 but the coercibility check that precedes any property read: **§8.6.2** `BindingPattern : ObjectBindingPattern` step 1 is `Perform ? RequireObjectCoercible(value)`, and on the assignment form **§13.15.5.2** DestructuringAssignmentEvaluation opens *each* of its four `ObjectAssignmentPattern` productions with the same step (`webref body ecma262 sec-runtime-semantics-bindinginitialization` / `sec-runtime-semantics-destructuringassignmentevaluation`). At `658cc302` the `PatternKind::Object` arm has no such check — it emits `Op::Dup` and a property read directly (`compiler/stmt_destructure.rs:83-110`) — so `const {} = null` completes normally where the spec throws. Fourth and fifth required regressions: `const {} = null` and `({} = undefined)` must each throw a **TypeError**, and the pattern must be the **empty** one — a non-empty pattern throws anyway on the first property read off `null`, so it cannot separate the missing check from an incidental failure. ⚠ **The declaration spelling is a red probe and the assignment spelling is not, and stating them as one pair let the assignment half retire itself.** At `658cc302` `const {} = null` completes normally (executed → no throw), the declaration form being implemented; `({} = undefined)` **already throws a TypeError**, from the *unimplemented-target* arm (`TypeError "assignment to this target is not yet supported"`, the same arm the evidence block above §5 measures for the array form). So an implementation of this row that lands the array form and leaves the object *assignment* form on that arm retires the fifth regression green without writing the `RequireObjectCoercible` check. The assignment half therefore carries a **paired positive that pins the provenance**: `({} = {})` must complete normally — which the unimplemented-target arm fails — and `({} = undefined)`'s TypeError is asserted only beside it | `compiler/expr_assign.rs`, `compiler/stmt.rs`, `compiler/stmt_loop.rs`, `compiler/stmt_destructure.rs`, `compiler/expr_function.rs` | `#11-vm-assignment-target-completeness` (0bc) | T1 | — |
| **P** | **`IteratorClose` precedence convention — UMBRELLA, not a terminal unit.** The charter carried the Rust `iter_close` completion/result semantics (core `vm/`), the compiler→bytecode completion transport (`compiler/` + `bytecode/`, which [§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) records as **design work, not a sweep**, governed by the still-open §9 dec. 2b), and the WebIDL-governed conversions plus the WHATWG HTML citation retag (`vm/host/`). [§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) already names both axes — transport first, then governing algorithm; this is that partition applied to the slice table. **Pd is minted from a measurement rather than from that partition**: the order in which a `for-of`'s close and an enclosing `finally` run is ECMA-262 §14.7.5.7's, not §7.4.11's, and it is a precedence question — when the close runs relative to the rest of completion processing — so it lands under this umbrella rather than beside it. ⚠ **ECMA-262 §7.4.15 AsyncIteratorClose is inside this charter and is un-minted, not out of scope**, per §5's row-kind rule that the children listed under an umbrella are its derivation's output so far and never its boundary. The precedence this umbrella is named for is stated *verbatim* in both AOs: **§7.4.11** IteratorClose and **§7.4.15** AsyncIteratorClose share steps 5-8 word for word — "If completion is a throw completion, return ? completion", "If innerResult is a throw completion, return ? innerResult", "If innerResult.[[Value]] is not an Object, throw a TypeError exception", "Return ? completion" (§-numbers and titles from `webref aoid ecma262 IteratorClose` / `AsyncIteratorClose`; steps from `webref body ecma262 sec-iteratorclose` / `sec-asynciteratorclose`). ⚠ **It is not Pa's, and the two are easy to conflate because the precedence is the same text**: Pa's charter is stated over §7.4.11 and its deliverables are that AO's Rust signature, its per-caller audit and its step 7, while §7.4.15 differs at step 4.d — `Set innerResult to Completion(Await(innerResult.[[Value]]))`, a suspension of the calling execution context — which is outside Pa's charter — stated over §7.4.11, its Rust signature, its per-caller audit and its step 7 — and carries a consumer edge Pa has none of, onto the child umbrella **9da**'s derivation mints for CreateResolvingFunctions, so it is a child this umbrella mints rather than one appended to Pa rather than something appended to it. Its consumers are the child of umbrella **9a** that carries `Array.fromAsync`, the children of umbrella **R** that carry the `await using` and `for await (using …)` forms, the children the slot umbrella `#11-vm-yield-delegation-lowering`'s derivation mints for the 7.4.15 close on the delegation path, and **Slice 6b** — each of those cells already reads §7.4.15 and none of them could name a producer. ⚠ **6b was absent from this list until this revision, and it is the consumer whose *own governing clause* reaches the AO** rather than one that reaches it through a member it implements: **§14.7.5.7** ForIn/OfBodyEvaluation branches on `iteratorKind` at both abrupt exits and the async branch is `AsyncIteratorClose`, not `IteratorClose` (that row's cell carries the steps and the commands). ⚠ **And the child that owns §7.4.15 is itself a consumer, of the child umbrella `9da`'s derivation mints for CreateResolvingFunctions** — an edge the AO's own steps produce and no reading of the four consumers above could: step 4.d is "If innerResult is a normal completion, set innerResult to Completion(Await(innerResult.[[Value]]))", **§27.10.5.3** `Await ( arg )` step 2 is "Let promise be ? PromiseResolve(%Promise%, arg)", **§27.5.4.7.1** PromiseResolve step 2 is "Let promiseCapability be ? NewPromiseCapability(ctor)" with step 3 "Perform ? Call(promiseCapability.[[Resolve]], undefined, « resolution »)", **§27.5.1.5** NewPromiseCapability step 6 constructs `ctor`, and for `%Promise%` that is **§27.5.3.1** `Promise ( executor )`, whose step 9 is "Let resolvingFuncs be CreateResolvingFunctions(promise)" — **§27.5.1.3**, which the child umbrella **9da**'s derivation mints for thenable assimilation delivers (§-numbers from `webref aoid ecma262 Await` / `PromiseResolve` / `NewPromiseCapability` / `CreateResolvingFunctions`; steps from `webref body ecma262 sec-asynciteratorclose` / `Await` / `sec-promise-resolve` / `NewPromiseCapability` / `sec-promise-executor`). ⚠ **Derive `Await`'s number rather than recalling it — it is §27.10.5.3, not §27.5.x.** So the async close awaits the `return()` result through the same resolving-functions closure 6b's own edge on the child umbrella **9da**'s derivation mints for CreateResolvingFunctions is owed for, and written against today's `settle_promise` — which adopts only `ObjectKind::Promise` (9da's row) — it binds a thenable `return()` result instead of adopting it. **Minting the child that owns §7.4.15, placing those consumers' edges on that child, and placing that child's own edge on the child umbrella **9da**'s derivation mints for CreateResolvingFunctions, are all mandatory outputs of this umbrella's derivation.** ⚠ **[§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) and its Slice-P prose stay the authoritative home for the site partition and the two commands that re-derive it** — the sub-slice rows name owners, not counts. **The kind is the first disjunct, and the axes are counted by independence rather than by unit or by layer — three of them, each red while the other two are green.** (i) **§7.4.11 IteratorClose's own completion/result semantics** — the completion-kind parameter on the Rust signature, the per-caller audit and step 7, which is Pa's charter — stays wrong when the close already runs at the right moment and the WebIDL-governed conversions are already gone. (ii) **The §14.7.5.7 precedence Pd is minted from** — the order a `for-of`'s close takes against an enclosing `finally` — is, as this cell states, §14.7.5.7's and not §7.4.11's, so a §7.4.11 correct in every step still sequences it wrongly. (iii) **The WebIDL-governed conversions Pc removes** are governed by a different specification altogether and are reached by neither of the other two, so they survive a close that is both correct and correctly sequenced. **Pb is not a fourth**: its own cell calls itself behaviour-preserving and Pa's prerequisite, so it cannot fail while Pa passes, and the count is taken over Pa, Pc and Pd. §7.4.15 AsyncIteratorClose is a further axis this derivation *mints* rather than counts — it departs from §7.4.11 at step 4.d and carries a consumer edge Pa has none of. The second disjunct is unavailable: this cell takes §7.4.11 IteratorClose and §7.4.15 AsyncIteratorClose from `webref aoid ecma262 IteratorClose` / `AsyncIteratorClose`, so the AO that governs the work is in the specification. | — | `#11-vm-iteratorclose-precedence-convention` (umbrella) | T1 | — |
| **Pa** | **ECMA-262 §7.4.11 `iter_close` semantics** — the completion-kind parameter on the Rust signature, the per-caller audit of the ECMA-262-governed callers ⚠ **which the six regressions below cannot observe, because every one of them is spelled over `for-of` with `break`** — a remedy that adds the parameter, implements steps 4.b/6/7 and the GetMethod partition, and then threads the kind only at the `for-of` sites answers all six while `vm/natives_array_hof.rs:485`, `vm/host/typed_array_static.rs:798` and `vm/ops.rs:60` keep hand-rolled precedence. **Seventh regression, a red probe at `658cc302` at a site this row's own module cell names** — ⚠ **restated, because the spelling that stood here was neither at that site nor red.** `{ next(){ … }, return(){ … } }` is an iterator *record*, not an iterable, so **§23.1.2.1** step 4's `GetMethod(items, %Symbol.iterator%)` sends it down the array-like path: measured, `Array.from` over it returns a length-0 array here and under Node alike, the mapper never runs and nothing is closed. Repaired to an iterable it is *green*: `Array.from(it, () => { throw e })` over `{ [Symbol.iterator](){ return { next(){ return {value:1, done:false} }, return(){ return 1 } } } }` surfaces `e` here and under Node alike, with `return()` measured as called once in both. The condition that is red at that site is the **step-5-before-step-6** precedence: over the same iterable with `return(){ throw r }`, `Array.from(it, () => { throw e })` must surface `e` (§7.4.11 step 5 returns the incoming throw completion before step 6 can return `innerResult`), and at `658cc302` it surfaces `r` instead, against the control just given, which passes that a `return()` yielding a non-Object produces when the inner completion is discarded, **and §7.4.11 step 7** ("If innerResult.[[Value]] is not an Object, throw a TypeError exception", `webref body ecma262 sec-iteratorclose`), which `iter_close` does not implement: at `658cc302`, `dispatch_iter.rs:362` ends the `.return()` call with `.map(\|_\| ())` and discards the result, so `{ return() { return 1 } }` is accepted. Deliverables: signature + result-object validation + **six** regressions — (1)-(6) below, case (6) carrying both its `null` and its `undefined` variant — because no one of the six directions can fail on the others. **(1) Primitive-return on an otherwise-normal close**: an iterator whose `return()` yields a non-Object must throw a TypeError. **(2) The same close on a *throw* completion must surface the original error, not the TypeError — over an iterator that *records* the `return()` call, asserting it happened** — **§7.4.11** step 5 returns the incoming throw completion *before* step 7 validates the result, so an implementation that adds the completion-kind parameter, ignores it and validates unconditionally passes (1) while inverting the precedence this row exists to fix. ⚠ **Asserting only which error escapes is not enough, and that is what this regression first said**: the `return()` call is **step 4.c**, *before* step 5, so an implementation that returns the incoming throw completion without ever calling `return()` also surfaces the original error and passes. The witness records the call — with `return()` throwing too, so the original error still winning is the observable rather than the absence of a second one. ⚠ **And a third case, because neither of the first two reaches §7.4.11 step 6**: a close on a **normal** completion whose `return()` *throws* must surface **that** error (step 6, `If innerResult is a throw completion, return ? innerResult`). Case (1) returns a primitive normally and case (2) starts from an incoming throw that step 5 hands back before step 6 is reached, so an implementation that swallows `return()` exceptions on normal completions passes both. ⚠ **(1) alone was the whole acceptance for several revisions**, and the ordering prose above named the axis without turning it into a test. ⚠ **And a fourth and a fifth, because all three enter §7.4.11 at step 4.c with a callable `return`, and neither of the other two exits from steps 3-4 is asserted.** Step 3 is `Let innerResult be Completion(GetMethod(iterator, "return"))`, and **§7.3.10** GetMethod is `GetV`, then "If func is either undefined or null, return undefined", then step 3 "If IsCallable(func) is false, throw a TypeError exception" (`webref body ecma262 sec-iteratorclose` / `sec-getmethod`). **(4) A present, non-callable `return`**: `{ next(){…}, return: 42 }` closed on a normal completion must surface *that* TypeError at step 6, where an implementation that simply skips a non-callable `return` closes silently and passes (1)-(3). **(5) No `return` at all** — the confinement half of (1), and the case an over-eager fix to (1) breaks: step 4.b is `If return is undefined, return ? completion`, **before** step 7, so an iterator without the method must produce **no** TypeError. Unconditional result validation makes every `const [x] = [1, 2]` throw, `%ArrayIteratorPrototype%` having no `return` method, and nothing in (1)-(3) sees it. ⚠ **And a sixth, because (4) and (5) partition on the *presence* of `return` while **§7.3.10** GetMethod partitions on its *value*, and the in-tree close partitions on presence.** GetMethod step 2 is "If func is either undefined or null, return undefined" and step 3 is the `IsCallable` TypeError (`webref body ecma262 sec-getmethod`), so a **present** `return` holding `null` — or `undefined` — must reach §7.4.11 step 4.b and exit cleanly exactly as (5) does, and only a present *other* non-callable reaches the TypeError, which is (4). At `658cc302` `iter_close` branches on `get_property` returning `Some` against `None` (`vm/dispatch_iter.rs:360-365`) and hands a `Some` holding `Null` or `Undefined` straight to `resolve_property` and then `call_value`, so `{ next(){…}, return: null }` and `{ next(){…}, return: undefined }` both throw where §7.4.11 requires no error — and both pass (4) and (5), whose witnesses are `return: 42` and an object with no `return` key at all. **(6) A present `return` of `null`, and the same of `undefined`**, each closed on a normal completion, must produce no error; this one is an in-tree defect the row repairs, not only an acceptance gap. ⚠ **All six are stated over "a close" while the cell named no close site — the parameter that decides whether any of them can fail.** At `658cc302` the two candidate sites disagree: `for-of` with a `break` **does** close (`let c = 0; const it = { [Symbol.iterator]() { return { next() { return { value: 1, done: false } }, return() { c++; return { done: true } } } } }; for (const v of it) { break } c` → `1`) while array destructuring does **not**, the same iterator under `const [x] = it` leaving `c` at `0` — which is Slice **0bc**'s own §8.6.2 step-3 finding, which no child its derivation mints has landed. **All six are therefore spelled over `for-of` + `break`.** Spelling them over a destructuring close makes (1), (4) and (6) vacuous — nothing is closed, so a primitive `return()`, a non-callable `return` and a `null` `return` all complete silently — and takes an edge on the child umbrella **0bc**'s derivation mints for the array binding-pattern close that this row's `Deps` cell does not carry and, under §5's edge rule, must not, that child consuming Pa rather than the reverse. **Polarity at that site**: (1) and (6) are **red probes**, measured — `{ next(){…}, return(){ return 1 } }` is accepted today, and both `return: null` and `return: undefined` throw where §7.4.11 step 4.b requires no error; (4) is a **guard**, `{ next(){…}, return: 42 }` closed by `for-of` + `break` already throwing `TypeError "not a function"`, so it fails only on the over-eager repair to (1) that skips a non-callable `return`; (5) is a **guard** for the same reason, an iterator with no `return` producing no error today; (2) and (3) are stated against the precedence arms this row rewires, and their polarity is re-derived at this row's own plan-review against its parent HEAD **Pa consumes Pb, not the reverse** (corrected at PR-B; the cells were pointed the other way): one of the ECMA-262-governed callers [§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) assigns to Pa is `dispatch_iter.rs:337`, which is the body of `op_iterator_close` (`:335`) — the `Op::IteratorClose` handler — and the opcode carries no operand, so there is no completion kind in scope for that call to pass. Pa ahead of Pb therefore hard-codes one kind at that handler and leaves the other wrong, or carries a second `iter_close` entry point until Pb replaces it: the strangler intermediate CLAUDE.md *One issue, one way* forbids. **Ordering inside §7.4.11**: step 7 sits after step 5 (a throw completion returns the *original* completion) and step 6 (an `innerResult` throw returns that), so validation must not fire on either path — read the numbered steps in the `body` output before wiring it | `vm/dispatch_iter.rs`, `vm/ops.rs`, `vm/natives_array_hof.rs`, `vm/host/typed_array_static.rs` | `#11-vm-iteratorclose-precedence-convention` (Pa) | T1 | **Pb**, **the child `#11-vm-typed-array-family-layering-and-gate`'s derivation mints for the family's location decision**, via that slot's derivation, which mints it (§8 — Pa edits `vm/host/typed_array_static.rs:798`, so the layering/gating decision must settle first) |
| **Pb** | **Compiler→bytecode completion transport** — `Op::IteratorClose` is operandless (`bytecode/opcode.rs:284` at `658cc302`, stack effect `[iterator -- ]`) and is emitted from a throw handler *and* the non-throw Return path, so the handler cannot tell them apart. Deliverable is the transport itself (an operand, or separate opcodes) plus the statement-lowering emit sites, with the throw and Return paths tested **separately** — a single fixed argument in the handler gets §7.4.11 step 5 right and steps 6-7 wrong, or the reverse. Governed by **§9 dec. 2b**, which is open, so Pb's plan-review settles it. **Pb is the prerequisite of **Pa** — ⚠ **not "the family's first PR"**, which restated an ordering the cells do not carry: Pc and Pd have empty `Deps` and sit outside the only `Pb → Pa` chain, so the phrase would have blocked two independent T1 slices on an artifact they never consume — and is behaviour-preserving**: it widens the transport and hands the handler a completion kind it did not have, and Pa is what then makes the two kinds behave differently — so Pb ships with the handler still doing what it does today on both routes, and its own tests assert that the throw and Return routes arrive distinguishable, not that they diverge yet. That is why it depends on nothing: [§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md)'s axis order (transport first, then governing algorithm) is a dependency, not just a reading order. ⚠ Excluded, with owners: the `expr_yield_star.rs` emits (delegation, not §7.4.11 — the child the slot umbrella `#11-vm-yield-delegation-lowering`'s derivation mints for the `yield*` delegation emits, [§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md)) and the inline re-implementation in `op_array_spread` (dec. 13a assigns its *removal* to the child of umbrella 1a that removes `op_array_spread`'s `return()`, via that umbrella's derivation, which mints it; that child's edge 21 verifies it). **Acceptance, with polarity (I-8)** — stated from Rust, which is what §5's prerequisite-row rule allows where the artifact has no JS-reachable holder yet, and this row's has none by construction, being behaviour-preserving. **(1) Red probe** at `658cc302`: compile one `for-of` whose body can both throw and `return`, and assert that the `Op::IteratorClose` reached from the catch handler and the one reached from the Return path carry **different** completion-kind encodings ⚠ **and "different" is a condition an *absent* implementation fails while a *wrong-but-present* one passes** — emitting the normal encoding at `compiler/stmt_loop.rs:147`, the throw handler, and the throw encoding at `compiler/stmt.rs:661`, the Return path, satisfies it with the two kinds swapped, which bakes in below Pa exactly the precedence inversion umbrella P is named for and is behaviour-preserving, so nothing else diverges either. **The condition is therefore stated over *which* encoding each site carries**: `stmt_loop.rs:147` carries **throw** and `stmt.rs:661` carries **normal**, each asserted on the emitted operand rather than on the pair being unequal. The opcode is operandless there, so the two sites encode identically and the assertion is red — red for the defect's own reason rather than for the deliverable's absence, which is what separates it from asserting that a name resolves. **(2) Green guard**: both routes still *behave* identically after this row lands, the existing `for-of` abrupt-completion regressions passing unchanged. Green at the pinned tree and stated so the remedy may not break it — a remedy that makes the two routes diverge has taken **Pa**'s work, which is what the transport-first axis order exists to prevent, and it is the prose assertion above turned into a condition rather than a second copy of it. **(3) Declared negative control**: the new encoding reaches no `expr_yield_star.rs` emit and no `op_array_spread` inline drain — this row's own excluded set, bounding a sweep that widened to every `IteratorClose` producer instead of the two §7.4.11 statement-lowering sites. | `compiler/stmt.rs`, `compiler/stmt_loop.rs`, `bytecode/opcode.rs`, `vm/dispatch_iter.rs` | `#11-vm-iteratorclose-precedence-convention` (Pb) | T1 | — |
| **Pc** | **WebIDL-governed conversions — the deliverable is *removal*, not adaptation.** The governing algorithm is WebIDL **§3.2.21.1** "Creating a sequence from an iterable", whose entire body is `GetIteratorFromMethod`, then a repeat of `IteratorStepValue` / return-on-done / "Initialize S\_i to the result of converting next to an IDL value of type T" — **no `IteratorClose` step anywhere** (`.claude/tools/webref body webidl create-sequence-from-iterable \| grep -ci iteratorclose` → **0**; positive control `body ecma262 sec-array.from` → **1**, so the grep discriminates; and `webref dfn webidl "IteratorClose"` answers *no dfn matching 'IteratorClose' in webidl*). **[§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md)'s "5 of the sites, across 4 files, are not governed by ECMA-262 at all" paragraph is the authoritative home of this partition and already says so**, so "what completion kind does a WebIDL caller pass" is a settled non-question and **Pc consumes nothing Pa produces**. **All five lose the call**, including `vm/webidl_sequence.rs:149`, where the element `validator` fails. ⚠ **A retain verdict stood on `:149` and is withdrawn here**, on the row's own measurement: its argument was that the validator failure *is* step 3.3's conversion to type T and so closes on a step that genuinely exists — which conflates *the step existing* with *the step mandating a close*. §3.2.21.1 has no `IteratorClose` step **anywhere**, on step 3.3 or on any other, so an abrupt IDL element conversion is simply propagated; keeping the call lets a user iterator's `return()` run side effects or replace the conversion error with its own. The five split instead on **residue after the deletion**, which is what the deferred row below owns. `:149` leaves none — deleting the call makes it §3.2.21.1-faithful outright. **Four leave one, because they also take an early exit the governing algorithm does not have**: `vm/webidl_sequence.rs:141`, an **engine cap** (`index >= cap`, `cap_exceeded_msg`) with no counterpart in §3.2.21.1; `vm/host/url_search_params.rs:318` and `vm/host/headers/parse_init.rs:208`, **arity checks the two consuming specs perform on the already-converted sequence** — URL **§6.2**'s `URLSearchParams` constructor step 1 is "If init is a sequence, then for each innerSequence of init: If innerSequence's size is not 2, then throw a TypeError" and Fetch **§5.1**'s *fill a Headers object* step 1.1 is "If header's size is not 2, then throw a TypeError", each over the converted value rather than mid-drain (`webref body url interface-urlsearchparams` / `webref body fetch concept-headers-fill`; §-numbers from `webref dfn url "URLSearchParams"` / `webref dfn fetch "fill"`); and `vm/host/structured_clone.rs:1064`, an **unsupported-transferable bail-out** ("Transferable objects are not yet supported"), an engine limitation and not a spec step. The residue those four leave — they still stop the drain early, so their `next()` call count differs from a conversion that drains to done and checks afterwards — is deferred with its own §8 row, `#11-vm-webidl-sequence-early-exit-drain`. **Also a named deliverable**: retag the drifted in-code `§2.9` tags on the structured-clone surface to WHATWG HTML §2.7.3 / §2.7.4 / §2.7.6 / §2.7.7 — ⚠ **§2.7.3 StructuredSerializeInternal belongs in the destination set and the first three wordings omitted it**, so a classification could sort a hit correctly as HTML and still assign it the wrong outer clause: the memory-map identity invariant is §2.7.3's, which this implementation already attributes correctly at `vm/host/structured_clone.rs:722-723` ("HTML §2.7.3 step 2: *If memory[value] exists, then return memory[value]*") while the drifted comments at baseline `:194-195` and `tests_typed_array_extras.rs:235` describe that same invariant under `§2.9`. **The classification is to the exact destination clause, not merely to HTML-versus-DOM** (§-number from `webref heading html 2.7.3`) — ⚠ **The population is the surface, not the one file — and it is established by an exhaustive classification, not by keyword greps.** At `155f9469` `git grep -c '§2\.9' -- crates/script/elidex-js/src` returns **126** occurrences, spanning two unrelated specs: DOM **§2.9** *Dispatching events*, which is correct as written, and the drifted WHATWG HTML tags this deliverable retags. ⚠⚠ **A keyword split stood here — 14 in `clone\|serializ\|transfer` context against 101 in `dispatch\|event` — and it was wrong in both directions and did not even sum**: re-measured over the matching lines the two return **11** and **65**, **0** match both and **50** match neither, so 11 + 65 + 0 + 50 = 126 while the published 14 + 101 = 115 ≠ 126 — an arithmetic check on the parts against the total would have caught it before it was written down. And the two keywords misclassify in both directions on inspection: `vm/tests/tests_event_source.rs:910` reads "WHATWG DOM §2.9 inner-invoke: the listener list is **cloned**" — a valid DOM citation the clone keyword captures — while `vm/tests/tests_typed_array_extras.rs:2`, `:235` and `:733` are drifted HTML tags that carry neither keyword. **So the deliverable is: classify all 126 once, by reading each hit's spec, and derive both sets from that classification** — the drifted set then reaches beyond `structured_clone.rs` into at least `vm/globals.rs:952`, `tests_structured_clone.rs:1` / `:158` and those three `tests_typed_array_extras.rs` sites, and the retagged count plus the untouched DOM count must equal the total ⚠ **which is an arithmetic identity any classification satisfies, and which reaches neither of this row's deliverables** — classifying every §2.9 hit as DOM and retagging none makes 0 + 126 = 126 hold while the drifted HTML tags survive, and deleting the `iter_close` at `vm/webidl_sequence.rs:149` alone satisfies every word about the removal half. **Red probes at `658cc302`, one per removal site**, since the row's deliverable *is* the removal: no `iter_close` call remains at `vm/webidl_sequence.rs:141` or `:149`, `vm/host/url_search_params.rs:318`, `vm/host/headers/parse_init.rs:208` or `vm/host/structured_clone.rs:1064`, each of which `git grep -n 'iter_close' 658cc302 -- <that file>` returns today. **And the identity gains the criterion it lacked**: a hit may be counted "untouched" only when its governing algorithm is WHATWG DOM, established per site from the surrounding call's own entry point, so a site whose governing algorithm is HTML or WebIDL is in the retagged count by construction and cannot be moved into the free variable the classification started from. Re-derived at Pc's own parent HEAD rather than read forward) — ⚠ **row F's own classification correction reaches this cell and had not been swept into it**: the file fuses StructuredSerialize (**§2.7.4**) with StructuredDeserialize (**§2.7.6**) and several of the 8 stale tags describe constructing the cloned *output* (`:194` the memo resolving to "the same cloned wrapper", `:573` and `:604` the fresh RegExp's `lastIndex`), so a §2.7.4/§2.7.7-only target assigns serialization numbers to deserialization behaviour — the very defect F records itself as having had (§-numbers from `webref heading html 2.7`) — `git show 658cc302:crates/script/elidex-js/src/vm/host/structured_clone.rs \| grep -c '§2\.9'` → **8**, and 0a touched none. **The touch-time split of `vm/host/structured_clone.rs` is a *dependency* of this row, not a verdict its plan-review may record either way** — ⚠ *the either/or stood in **1a**'s module cell, which is wrong twice: it ordered this row from another row's `Primary module(s)` column (§5's single-home rule puts ordering in the `Deps` cell of the row it orders), and CLAUDE.md's touch-time rule leaves the either/or available only while **no** real cohesion seam has been identified.* One is identified, by this row's own WebIDL/HTML partition and by the file's own section banners: the file is **1077** lines (`git show 658cc302:crates/script/elidex-js/src/vm/host/structured_clone.rs \| wc -l`, and `wc -l` on the worktree agrees), of which `:1`-`:914` are the HTML clone algorithm as this file implements it — the **fused §2.7.4 StructuredSerialize and §2.7.6 StructuredDeserialize** pass its entry point's docstring names at `:63-64`, not §2.7.4 alone — (`clone_value` `:75` through the `// Error helper` banner's `data_clone_error` `:909`) and `:916`-`:1077` are the `// Global registration` banner and the WebIDL binding layer — `register_structured_clone_global` `:922`, `native_structured_clone` `:944`, `validate_transfer` `:965` and `ensure_empty_transfer_list` `:998`, the last already consumed from outside the file (`git grep -n 'ensure_empty_transfer_list' 658cc302 -- crates/script/elidex-js/src` → its definition, one in-file caller, and `vm/host/pending_tasks.rs:673`, the `postMessage` path). So the split lands as a **standalone prereq PR** — single commit, not bundled here — leaving the clone half at **914** lines and the binding half at ~**162** | `vm/webidl_sequence.rs`, `vm/host/url_search_params.rs`, `vm/host/headers/parse_init.rs`, `vm/host/structured_clone.rs` | `#11-vm-iteratorclose-precedence-convention` (Pc) | T1 | **F** (the `vm/host/structured_clone.rs` touch-time split — a terminal prerequisite row of its own, minted because this cell named a standalone PR with no id, and so with no acceptance and no plan-review; F's row states why the split is mandatory rather than optional) |
| **Pd** | **Abrupt-exit sequencing at a `for-of` body — UMBRELLA, not a terminal unit.** ⚠ **This row was held terminal in two separate sentences of its own, and it is the terminality verdict rather than the grouping that fails.** Its three obligations are governed by **three** algorithms — **§14.7.5.7** ForIn/OfBodyEvaluation for the close-versus-enclosing-finalizer ordering, **§14.7.1.1** LoopContinues for the labelled-`continue` exit range, and **§14.15.3** *Runtime Semantics: Evaluation* for the finalizer region — and they ride **two** emit helpers, `emit_iter_close_range` and `emit_pending_finally_bodies`, over two different compiler stacks, `fc.loop_stack` and `fc.finally_stack`, whose targets are computed separately and one of which the child umbrella **L**'s derivation mints for mechanism (a), the loop-stack representation, is concurrently replacing (this row's own L paragraph below states the two-directional contract). The three intersecting invariant axes are: *which enclosing regions the close must run inside*, *which loop frames a labelled jump leaves and which it re-enters* — the polarity here being the **opposite** of `break`'s, per §14.7.1.1 — and *which finalizer regions the jump actually leaves*. They intersect at the same two arms and in the same emitted instruction sequence, which is precisely why a throwing finally body suppresses the close. That satisfies the first disjunct of the criterion above on its own, and §5's head already answers the counter-argument that they are one *question* asked of one arm family in one layer: a group can be well-formed as a unit and still hold mechanisms with **no shared lowering chokepoint**, which two helpers over two stacks is the definition of. ⇒ umbrella. It mints terminal children at its own start from the obligations enumerated below; **placing each obligation, each acceptance condition, and the coordination with the child umbrella **L**'s derivation mints for the retained exit-range depth on the specific child is a mandatory output of that derivation** (§5's row-kind rule), and this row carries none of the three. Everything below is evidence for it to route, not a charter. The close and an enclosing `finally` run in the wrong order, and because the two are emitted as one sequence a throwing finally body suppresses the close entirely (§2.2's `compiler/stmt.rs:203-205` row). Governed by **§14.7.5.7** ForIn/OfBodyEvaluation, whose steps 8.i.vi and 8.m.v perform the close inside the loop's own evaluation, so a completion reaches an enclosing `finally` already closed — while a `try`/`finally` nested inside the body runs first, as part of step 8.10's "Evaluation of stmt". Deliverable is nesting-aware sequencing at the arms that pre-emit an abrupt exit, with the two nestings asserted **separately** — an iterator whose `return()` records the call and a `finally` that records its own run, ordered one way for the outer nesting and the other for the inner, **each stated with its polarity (§4 I-8), because only the outer half is red**: at `658cc302` the inner nesting already logs `fin,closeI`, which is §14.7.5.7's own order (step 8.10 evaluates stmt, step 8.13.5 then closes), so it is a green guard, while the outer nesting logs `fin,close,afterloop,fin` against the owed `close,afterloop,fin` — plus a throwing finally, which is a **green guard, not a red probe** (§4 I-8) because the close already runs at `658cc302`: the loop body's implicit exception handler (`compiler/stmt_loop.rs:99-102` arms it, `:143-147` performs the close gated on `close_flag`) catches the finalizer's own throw, so `try { for (const x of it) { return } } finally { throw }` logs `finthrow,closeI,finthrow,caught` — the close is present. What is red is the **ordering and the duplicated finalizer**: §14.7.5.7 step 8.13.5 performs the close inside ForIn/OfBodyEvaluation, so the same program owes `closeI,finthrow,caught`. **The `Continue` arm is Pd's too, and does not make Pd an umbrella** (§2.2's `compiler/stmt.rs:262-282` row): it is one of the arms that pre-emit an abrupt exit, and the only one that calls no `emit_iter_close_range` at all, so an inner `for-of` left by `continue outer` is never closed. ⚠ **That sentence read "same governing AO, same arm family, same helper — and this adds neither", and two of the three are contradicted by the text immediately after it.** The governing AO here is **§14.7.1.1** LoopContinues, which the next sentence cites; the helper for the finalizer obligation is `emit_pending_finally_bodies`, which the next paragraph names beside `emit_iter_close_range`. What is genuinely shared is the *arm family* — and that the obligations land on the same arms is what the criterion measures, not what discharges it. What it does add is the arm's **exit range**, which is a design question of its own rather than a copy of the `Break` arm's: **§14.7.1.1** LoopContinues returns true for the loop the label names (step 4) and false for the loops inside it, so the targeted frame is re-entered and must not close while the frames strictly inside it must — where `break` leaves the targeted frame and closes from it. Regression: a labelled `continue` out of an inner `for-of`, over iterators whose `return()` records the call at both depths, asserting the inner closed and the targeted loop's own did not. ⚠ **That program does not terminate at `658cc302`, so it can be neither red nor green** — it reads a log it never reaches. Measured: `outer: for (const a of [1,2]) { for (const b of [1,2,3]) { continue outer } }` is killed at a 12 s cap here (Node answers promptly), while the labelled-`break` sibling completes identically here and under Node. The hang needs only two nested `for-of` loops and a labelled `continue`; it is not the custom iterator. So this row **understates its own defect**: the `Continue` arm (`compiler/stmt.rs:262-282`) emits the pending finally bodies and jumps with no `emit_iter_close_range` and no unwind of the inner loop's iterator/state, and the outer loop resumes on a corrupted stack. The regression is owed in a form that terminates — a bounded loop whose log is read after the hang is fixed — and the non-termination is the first thing the slice must clear. **The finalizer half of those same two arms is Pd's too** (§2.2's `compiler/stmt.rs:227` / `:263` row): both arms open with an unconditional `emit_pending_finally_bodies`, whose helper takes no target and walks the whole of `fc.finally_stack`, so a jump whose target is **inside** the enclosing `try` runs the finalizer at the jump and again when the try block completes — against ECMA-262 **§14.15.3** Runtime Semantics: Evaluation, whose `TryStatement : try Block Finally` production evaluates the Finalizer at step 2 only after step 1's evaluation of the Block completes (§-number from `webref heading ecma262 14.15.3`). That is this row's own question — *which enclosing regions does this abrupt exit actually leave* — asked of the other pre-emitted sequence, over the same arms and against the same target the close range already computes (`:231-236`), so it is inside the charter — but **§14.15.3 is a second governing algorithm and a second helper all the same**, which is what the withdrawn verdict above denied and what puts this obligation on a child of its own rather than beside the close-ordering one. Deliverable is target-region-aware finalizer emission at both arms, with a regression that **stays inside the `try`** — `try { while (true) { break } log.push('after') } finally { log.push('fin') }` logging `after` before a single `fin` — against a control whose target is outside the `try`, which must still run it. **Shares a contract with the child umbrella **L**'s derivation mints for the retained exit-range depth, in both directions, which is coordination rather than ordering**: the close range for a labelled `break` is `emit_iter_close_range(fc, target, fc.loop_stack.len())` (`:238`), whose lower bound is the label's `fc.loop_stack` index — the representation L replaces. ⚠ **and ⚠ neither party is a row that lands — both are umbrellas from this revision, so "whichever of the two lands second" named nobody on both sides.** The contract is between **the child umbrella L's derivation mints for mechanism (c), the retained exit-range depth** — ⚠ *and not the representation child: an earlier wording preassigned it to mechanism (a), while L's own cell classifies the representation as (a) and the retained depth as the independently failing (c), so the child that actually owns the contract would have received none of it* — and **the child umbrella Pd's derivation mints for the labelled-`continue` exit range**, and placing it on each is a mandatory output of the two derivations: the L child by retaining a label target's enclosing loop depth (L's row states it), the Pd child by deriving its sequencing against whatever representation exists at Pd's own parent HEAD rather than against the one written here. **Minted rather than folded into Pb**, which the three intersecting axes enumerated above already settle — an umbrella has no terminal row to be folded into: Pb is behaviour-preserving by charter and its governing algorithm is §7.4.11's completion transport, while this is §14.7.5.7's ordering and a deliberate behaviour change. **Not ordered after Pb, though the Deps cell said so until PR-B**: "Pb rewrites the same emit sites" is a shared-arm argument, and Pd's deliverable is §14.7.5.7 sequencing, which consumes nothing Pb's transport produces | `compiler/stmt.rs`, `compiler/stmt_loop.rs` | `#11-vm-iteratorclose-precedence-convention` (Pd umbrella) | T1 | — |
| **0c** | **I-1 discharge + the permanent conformance table — KIND UNDETERMINED: this row is neither an umbrella nor a terminal unit until the measurement stated below is made, and no PR may be scheduled under it — its sub-slices **0ca** and **0cb** included — until it is.** §2.5 classifies this work as edge-dense on a breadth product (substitution classes × compiler files × a deliberate user-visible behaviour change per dec. 5 — the program's widest blast radius; the figures are §2.5's), and a product of counts is not the independence §5's criterion asks for. The behaviour change and the permanent test artifact are separate deliverables with separate design questions. **The measurement that settles the kind, and until it is made the field above stands undetermined**: three probes at 0c's own parent HEAD, one per candidate axis — the residue of the I-1 substitution classes, dec. 5's user-visible behaviour change, and the §7.2 conformance table's row derivation — each stated red while the other two are green. A file count and a class count are breadth, and §2.5, which this row defers to, states its pair-intersection table for the 1a+1b axes A-F and names 0c only inside that product, so the site deferred to holds no independence measurement for this row either. For the second disjunct the measurement is whether the three-pass sweep and the conformance-table row derivation are governed by any canonical algorithm at all — a claim this cell makes in neither direction. If fewer than three axes come back red-while-green and the second disjunct stays unclaimed, the row is terminal and 0ca and 0cb are the one PR's two deliverables rather than its children. Neither disjunct is grounded here: the cell cites no clause at all, so the second is unclaimed rather than disproved — 0ca's sweep is §2.2's and 0cb's table is §7.2's, this document's own procedures rather than an AO absent from the specification. | — | (invariant, no slot) | — | — |
| **0ca** | **I-1 discharge** — re-run the §2.2 three-pass sweep against **0ca's own parent HEAD** (not `658cc302`: a sweep pinned to a fixed baseline reconstructs an inventory predating every child umbrella **0b**'s derivation mints and makes 0ca double-own cases 0bb and the children umbrella **0bc**'s derivation mints have already fixed), then convert the residue of all three substitution classes to a loud throw per dec. 5. ⚠ **That is a deliverable and this row stated no condition over it**, so "the residue" is whatever the implementer says it is — and running Pass 3 for arm *presence* rather than arm *content*, the failure this cell warns about, retires the row with every silent substitution hiding inside a present arm intact. **Acceptance, all red at 0ca's own parent HEAD**: the re-run sweep's output is recorded as a list of sites in the PR, and for **each** site the source spelling it refuses evaluates to a thrown `TypeError` carrying that site's message rather than to a value — asserted per site, not in aggregate, because an aggregate count is satisfied by converting one arm. **And the two reachable Layer-B arms are in scope by the same rule**: `vm/dispatch.rs:1010-1017` at `658cc302` pops and pushes `JsValue::Undefined` for `Op::GetPrivate` / `Op::PrivateIn`, which is class 1 in the VM rather than the compiler, so each must throw as well. **Declared positive control**: the sweep's own negative pass — a source spelling whose lowering is complete — still evaluates without throwing, bounding a remedy that makes an implemented arm loud. ⚠ **Narrowed by 0a's landing** — the 9 rows marked *0a ✅ loud* in §2.2 are discharged; `compiler/expr_assign.rs` has no residue at all. ⚠ **Pass 3 must be read for arm *content*, not only arm presence** — the obligation §2.2's scope statement puts on 0ca by name. The module list opposite is the pre-0a one and is **not** the charter | sweep-derived (§9 dec. 9) — pre-0a list, **stale**: `compiler/{expr,expr_object,expr_ops,expr_class,expr_assign,stmt}.rs` **+ `vm/dispatch.rs`** (the reachable Layer-B arms: `GetPrivate`/`PrivateIn`) | (invariant, no slot) | — | — |
| **0cb** | **The permanent ES-language conformance table** (§7.2) — `vm/tests/tests_es_language_surface.rs`, row-derivation per §9 dec. 9(b) **plus** the second source §7.2 adds (§1.1/§1.2 absences, one row per Slice 7-10 slot) **plus** the rows later slices contribute under §2.2's per-slice-sweep rule, and the crash-aware outcome type of §9 dec. 16. Lands after 0ca so its `KNOWN-DIVERGENCE` rows record post-discharge behaviour. **Acceptance, with polarity (I-8)** — **(1) Red probe** at this row's parent HEAD (0ca landed): the table records `f(a×256)` — §7.2's process abort, owned by the child umbrella **1b**'s derivation mints for the argument-emit helper ⚠ **(", which lands after this row" stood here and is deleted: §9 dec. 16 withdrew exactly this ordering-in-prose, and the crash-aware outcome is what makes the row writable before that child lands — umbrella 1b itself never lands, so nothing here needs it)** — and a construct 0ca converted to `Op::ThrowUnsupported`, under **different** outcome constructors. Red there for a reason stronger than the file's absence: the `(source, expected)` *value* shape §7.2 started from cannot express the first row at all, which is why §9 decision 16 minted a crash-aware outcome type, so a table that compiles at all against the old shape has already failed this. **(2) Declared negative control**: an outcome type that folds every non-spec result into a crash passes (1) and is excluded by the second row reading the *throw* constructor rather than the crash one. **(3) Green guard**: the rest of `vm/tests/` passes unchanged. ⚠ **And none of the three conditions reads the table's *content*, which is the whole deliverable** — a two-row file, one `Panics` row and one `Throws` row, satisfies the outcome-shape condition, the two-constructor condition and this guard, and ships as the "permanent conformance table" every later slice inherits. **Fourth condition, over the derivation rather than the harness**: the row set is the union of the two sources §9 dec. 9(b) and §7.2 name, each row traceable to the source that produced it, and the count is re-derived by re-running both enumerations at 0cb's own parent HEAD rather than transcribed — a table whose rows cannot be reproduced from the two enumerations has recorded the implementer's sample, not the surface — the table is additive, and a row that turns green only by relaxing an existing test has recorded a divergence the engine does not have. **Producer half (I-8)**: every row the second source contributes for a Slice 7-10 slot asserts an **absence**, so it reads no artifact a later row mints and waits on no producer; the `Deps` cell is empty while 0c's kind is undetermined; the edge is re-derived when 0c resolves. | `vm/tests/` | (invariant, no slot) | — | — |
| **1a** | **Call-spread VM infrastructure — UMBRELLA, not a terminal unit.** ⚠ **The 1a/1b boundary is admissible and stands; what fails §5's criterion is the *terminality verdict* on the 1a half.** §9 dec. 6 settled that boundary on a **breadth** axis — VM infrastructure against compiler + opcodes — and it is unchanged; the criterion applied to the 1a half runs on a **different axis**, over the mechanisms that half contains, and returns umbrella. **Three mechanisms, and each fails while the other two pass** — the criterion's measurement, not a count. **(a) The argument-layout and inline-cache contract**: `lay_out_call_args` as a stack-layout helper, the `Empty` normalisation (`.or_undefined()`, `vm/natives_function.rs:272-277`), converting `op_super_call_spread` to consume it, correcting that handler's falsified docstring (the `expr_class.rs:145-152` producer is **spec-required** and is NOT folded — [§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) / I-3 carve-out), and `ic_call`/`ic_call_method` → `call_ic_idx: Option<usize>` (dec. 11). **(b) An *observable* iterator-closing correction**: removing `op_array_spread`'s non-spec `return()` (dec. 13a) — ECMA-262 **§13.2.4.1** ArrayAccumulation's `SpreadElement` production drains through **§7.4.10** IteratorStepValue and performs no **§7.4.11** IteratorClose, so what changes is what a user iterator sees, not a layout. **(c) GC root lifetime across four call-entry windows** (dec. 10): `stack_slice` **and** `actual_args` at `vm/interpreter.rs:867`/`:893` and `call_internal`'s `:629-659`/`:664-700` — ⚠ *not* `gc_enabled` bracketing, overturned in round 8 because `:893` hands off to `make_async_coroutine_and_drive`, which drives the async body. **The measurement that separates them is already in this document, one per mechanism, and each is blind to the other two.** (a)'s assertion is edge **32**, which executes `new B(1, 2)` against an observable `%Array.prototype%[Symbol.iterator]` stand-in plus the hand-written positive control (**§15.7.14** step 14.a.iv.1 NOTE): it constructs a class and never drains an array-literal spread, so (b) is invisible to it, and never suspends a frame across a collection, so (c) is too. (b)'s assertion is edge **21**, and [§6.4](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) measures that **no in-tree test asserts `.return()` behaviour on array-literal spread** — `tests_generator.rs:714/736` assert the *opposite*, for `yield*`/`for-of` — so (b) is omissible with the entire existing suite and both other mechanisms green; that is exactly what made the bare *"the full existing suite passes unchanged"* acceptance vacuous. (c)'s assertions are edges **27/28**, and [§6.4](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md)'s own ⚠⚠ note measures that **neither can be written as a JS program at all**: `function* g(a){arguments}; g(...arrOf1000)` passed with **all four** rooting fixes omitted, because by resume time the frame sits inside an `ObjectKind::Generator` whose existing arm (`vm/gc/trace.rs:345-370`) marks both vectors — so (c) is invisible to every JS-level assertion (a) and (b) are written as, and is executed from Rust through the placement affordance `#11-vm-operand-rooting-by-construction` owns. Three intersecting invariant axes — an in-VM stack-and-IC calling contract, ECMA-262 iterator-protocol observability, and GC root lifetime — sitting in the dispatch layer, in `vm/dispatch_iter.rs`'s drain and in the collector's hand-written root set ⇒ **the criterion's first disjunct is met**. ⚠ **Its second disjunct is *not* claimed, and §5's head requires a row whose evidence turns on layers to say so in its own cell**: each of the three has a canonical algorithm — §13.2.4.1/§7.4.11 for (b), the in-tree `construct_synchronous` stack-layout form (`vm/dispatch_class.rs:303-306`) for (a), `mark_roots`'s existing per-field walk for (c) — so the layer count is the evidence and the absence of a canonical algorithm is not asserted. ⚠ **This is not the withdrawn "the criterion's fixed point is everything" reading** — that thesis was implemented in this program, falsified against CLAUDE.md clause (c)'s own parenthetical (*else infinite regress*) and reverted; the argument here runs over **this row's three mechanisms and their independent failure** and reaches no further, and dec. 6's closing sentence is amended rather than contradicted. It **mints terminal children at its own start**, from those three mechanisms and from the obligations written on this row elsewhere in the document; **placing each obligation, each acceptance condition and each `Deps` edge on the specific child is a mandatory output of that derivation** (§5's row-kind rule), and this row carries none of the three. ⚠ **This row mints none of them and invents no ids** — which children, and how the three mechanisms divide between them, is that derivation's output, and a mechanism absent from this cell is **un-minted, never out of scope**. **The obligations that derivation must place, each of which names this row today**: edge **32** and dec. 11's ratified acceptance item — a direct unit test on `ic_call(.., None)`, without which the `None` arm ships as exactly the dead I-4 third state — onto the (a) child; edge **21**, [§6.2a](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md)'s "`op_array_spread` is [C19]/[C22]-only afterwards" consequence and the [§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) transport table's one inline site onto the (b) child; edges **27/28** onto the (c) child; and the `lay_out_call_args` edge onto the (a) child — ⚠ *this read "which is what 1b's `Deps` cell now names"; 1b became an umbrella in the same sweep, its `Deps` cell is now **—**, and re-pointing that edge onto the specific children of both derivations is a mandatory output of each.* **Column changes, stated rather than left to inference.** `Deps` becomes **—**, which the rule *requires* rather than merely permits, an umbrella carrying no ordering — so the **`#11-vm-operand-rooting-by-construction`** edge this row held is **re-pointed onto the (c) child as a mandatory output of this derivation**, that slot owning the Rust placement affordance (`vm/tests/tests_member_compound_assign_gc.rs:38-41`) without which edges 27/28 cannot be executed at all. No other child consumes it: [§6.4](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) edge **16** is explicitly *a structural assertion, not a runtime GC edge* — no collection can be placed in the `lay_out_call_args` pop → re-push window however the helper is written — so what the (a) child owes there is a debug assertion, not that slot's affordance. The `Slot` stays `#11-vm-call-spread-arguments`, still shared with 1b — the `(1a umbrella)` qualifier is the kind marker rows **4a** and **7b** already carry on the identical adoption, not a second slot; the `Tier` stays **T1**, inherited by the children; and the `Primary module(s)` cell stays as **evidence** for the derivation to route — rows **3**, **5** and **Pd**, all umbrellas, keep theirs on the same reading — and is not a touch set, this row having none | **`vm/dispatch_helpers.rs`** (home of `lay_out_call_args` — the proven cohesion seam, §5 1000-line note), `vm/dispatch_class.rs`, `vm/dispatch_iter.rs`, `vm/dispatch_ic.rs`, `vm/dispatch.rs`, `vm/interpreter.rs` — **no `compiler/` file** (the fold is withdrawn; edge 32 is a test) | `#11-vm-call-spread-arguments` (shared with 1b; 1a umbrella) | T1 | — |
| **1b** | **Call-argument spread — UMBRELLA, not a terminal unit.** ⚠ **This row read as terminal, and the five roots its own deliverable list names are what end that.** They intersect at one argument-emit helper and share no chokepoint below it: spread aggregation into an args array (**§13.3.8.1** ArgumentListEvaluation over `SpreadElement`); callee-shape normalisation and receiver preservation across `Paren` and optional chains (**§13.3.6.2** EvaluateCall's `thisValueRef`), consumed from 0bb but selecting this row's emit form; `new` with spread against the bound-function prefix splice (`vm/ops.rs:695-703`); the **I-3 fixed-prefix / item-iterator input contract** that **§13.2.8.4** GetTemplateObject's site-object prefix needs, which is a *different* umbrella's consumer promoted to a deliverable here; and §9 dec. 12's stack-depth bound, which this row's own module cell records as having no implementation today. ⚠ **And its single labelled acceptance was a bare pointer to a section this same cell says does not carry the contract** — that pointer is evidence for the derivation to route, never a condition. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. **The edges the `Deps` cell carried are withdrawn from it and re-pointed onto the specific children that consume them, by the same derivation.** The deliverables it routes: `compile_call_arguments`/`ArgsForm` + `emit_call` aggregation (dec. 2) + `CallMethodSpread` (dec. 2b) + the 3 handlers + arity-based form selection **+ the I-3 tagged-template input contract, as a deliverable rather than a carry-over note**. §3's TemplateLiteral rows record that `ArgsForm` as specified cannot express GetTemplateObject's `« siteObj »` prefix followed by the substitutions, and [§11](2026-07-vm-p4-umbrella-review-rounds.md) assigned the question to 1b — but [§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) still specifies only `compile_call_arguments(…) -> ArgsForm`, so 1b could satisfy its charter and its tests without producing the path I-3 requires, leaving the child umbrella **4a**'s derivation mints for the tagged-call lowering to change the helper or add a second argument-emission mechanism (the strangler shape I-3 exists to prevent). 1b **defines and tests** the fixed-prefix / item-iterator input contract; its *shape* is 1b's own plan-review to settle. Acceptance condition the child of umbrella 4a that owns the tagged-call lowering consumes: [§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md). ⚠ **And this row consumes `0bb`, which no edge recorded although §9 dec. 14 had already assigned the artifact and [§6.4](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) had already allocated the tests.** That allocation gives **1b** edges 31, 33 and 34 — `(o.m)(...a)`, `o.m?.(...a)` and `o?.m?.(...a)` must each bind `this` to `o` — and gives 0bb only the no-spread spelling, edge 35. All three of 1b's turn on a callee-shape test **0bb** owns and this row does not: `compile_call_expr` reads `ExprKind::Member` off the **raw** callee so `ExprKind::Paren` defeats it — dec. 14's `peel_paren` chokepoint, assigned there in as many words, with `(o.m)()` in 0bb's own acceptance suite and edge 31 kept here as a regression guard — and `compile_optional_chain_expr`'s `prev_is_member` reads `chain[i-1]` and never consults the chain's base, which 0bb's cell names as its second mechanism. §9 dec. 2b then routes the optional member-Reference shape to the existing **`CallMethodSpread`** *because* its stack shape is `[receiver callee args_array]`, identical to `o.m(...a)`, and calls what remains an **emit-site selection** change — this row's. So the artifact consumed is the normalised callee and its preserved receiver: without it this row's form selection has no member callee to select the method form for, emits `Call` / `CallSpread`, and retires with three of its own edges red. ⚠ **This is an artifact edge and not the shared arm the rule excludes**: the `ArgsForm` / `compile_call_arguments` half is downstream of the shape decision and consumes nothing else from 0bb, which is why the edge is stated over the receiver contract rather than over `compiler/expr_member.rs` | `compiler/expr_member.rs`, `expr.rs`, `bytecode/opcode.rs`, `bytecode/disasm.rs`, `vm/dispatch.rs`, **`vm/ops.rs`** (`do_new` §3 rows 17/18, the bound-prefix splice `:696-700` for edge 26, and dec. 12's stack bound — which has no implementation today) | `#11-vm-call-spread-arguments` | T1 | — |
| **2** | **Class field initializers + static-block lowering — UMBRELLA, not a terminal unit.** Assigning generic static-block lowering here (§8) put three invariant axes in one row: field initialization turns on base/derived construction timing and custom-element receiver identity, while a static block is a separate lexical function-scope boundary with its own binding isolation and capture. Different governing algorithms — ECMA-262 §15.7.10 / §7.3.32 against §15.7.11 — so they are minted separately. **The kind is the first disjunct: three axes, each red while the other two are green.** (i) **Base/derived construction timing** — a derived class's field initializers run at the wrong point relative to `super()`, while the fields still land on the right object and a static block still keeps its own scope. (ii) **Custom-element receiver identity** — correctly timed initializers install the fields on the constructing object rather than on the upgrade receiver, which no ordering fix reaches. (iii) **The static block's own lexical function-scope boundary** — its binding isolation and capture are the enclosing scope's (Slice 2b's deliverable), which neither the timing fix nor the receiver fix touches. `Different governing algorithms … so they are minted separately` derives the *split*, which §5 calls specification, and is not what carries the kind. The second disjunct is unavailable: the cell names ECMA-262 §15.7.10 / §7.3.32 and §15.7.11, so the AOs that govern the work are in the specification. | — | **adopt** `#11-step9-class-extras` (umbrella) | T1 | — |
| **2a** | **Class field definition and initialization — KIND UNDETERMINED: neither an umbrella nor a terminal unit until the measurement stated below is made, and no PR may be scheduled under it — its sub-slices **2aa**, **2ab** and **2ac** included — until it is.** The charter as written spanned what a field record *is* and when its name is computed, *when* instance elements are initialized (base construction against derived `super()`), the receiver a static initializer runs against, and which receiver a custom-element upgrade substitutes — across `compiler/expr_class.rs`, core `vm/dispatch_class.rs` and `vm/host/custom_elements/`. ⚠ **This row read UMBRELLA on the criterion's second disjunct, grounded on "the last of those has no clause to be run against", and that is false.** WHATWG HTML **§3.2.3** *HTML element constructors* steps 12-15 (peek the construction stack, replace its top with the already-constructed marker) and **§4.13.5** *Upgrades* step 6 are exactly which receiver an upgrade substitutes — `webref heading html 3.2.3` / `4.13.5` — and `crates/dom/elidex-custom-elements/src/construction_stack.rs` already implements the engine-independent half, citing those very steps; `vm/host/custom_elements/html_element.rs:14-17` cites them too. So the second disjunct is **unavailable**, and the routing this row must state is the layering mandate's, not a licence: `elidex-custom-elements` owns the §4.13.5 step 6 / §3.2.3 steps 12-15 stack state, and `vm/host/custom_elements/` owns wrapper allocation and `ctx.vm.call` marshalling. **The measurement this row needs is the first disjunct's**: the four obligations above intersect at *which receiver holds which record at which time*, and nothing in this cell says any of them fails while the others pass — breadth across four clauses and three files is not an axis count. ⚠ *Its blast radius was measured before this was written, not after: three sites name umbrella 2a, so the cost of the honest answer was never the reason to avoid it.* its sub-slices are minted from the clause boundaries rather than one per spelling, per the grouping rule above. The clauses that draw those boundaries: **§15.7.14** ClassDefinitionEvaluation and **§15.7.10** ClassFieldDefinitionEvaluation decide the record and its definition-time name, **§10.2.2** `[[Construct]]` and **§7.3.33** InitializeInstanceElements decide when instance elements run, §15.7.14 step 32 with **§7.3.32** DefineField decides the static receiver, and the custom-element receiver is a host-layer identity question `construct_synchronous` decides (§-numbers from `webref aoid ecma262 ClassDefinitionEvaluation` / `ClassFieldDefinitionEvaluation` / `InitializeInstanceElements` / `DefineField` and `webref heading ecma262 10.2.2`) | — | `#11-step9-class-extras` (2a, umbrella) | T1 | — |
| **2aa** | **The field record.** A class field's name is computed **once, at class-definition time**, and retained for later instances; the baseline emits nothing at all for a non-static field — `ClassMemberKind::Property` (`compiler/expr_class.rs:393`) sits wholly inside `if *is_static` (`:402`) and `:428` reads "Non-static properties: skip (would need field initializer injection)". ECMA-262 **§15.7.14** step 26 runs ClassElementEvaluation over every class element at definition time; **§15.7.10** step 1 is "Let name be ? Evaluation of ClassElementName" and step 4 returns a ClassFieldDefinition Record carrying it as `[[Name]]`; §15.7.14 step 30 sets `ctorFunc.[[Fields]]` to that list. Deliverable is that record and its place on the constructor — an implementation that injects initializers into the constructor body passes a field-*value* test while recomputing the key per construction (evidence block above). ⚠ **"Regression: `let n=0; class C { [++n]=1 } new C; new C; n===1`" stood here and cannot fail on this row's own artifact.** `n===1` is satisfied by any implementation that evaluates the computed key once at class-definition time and then discards it — the key, the initializer, or the whole ClassFieldDefinition Record — so this prerequisite can retire while the artifact the child umbrella **2ab**'s derivation mints for 7.3.33 instance-element initialization consumes is absent, which is the single failure a prerequisite row's acceptance exists to prevent. **§5's prerequisite rule bounds the replacement**: acceptance is bounded to what this row's own artifact buys at the point it lands, and nothing in JavaScript reaches `ctorFunc.[[Fields]]` until the child umbrella **2ab**'s derivation mints for 7.3.33 initializes instance elements — demanding a JS path would pull 2ab's work in — so it is asserted **from Rust against the record itself**, the way the children umbrella **C**'s derivation mints state theirs, and **6c**'s does and as the GC tests read `vm.inner`: after evaluating `let n=0; class C { [++n]=1 }`, the constructor retains a field list of length **1** whose entry's `[[Name]]` is the property key `"1"` — the §7.1.21 result the computed key produced, retained rather than recomputed — and which carries the initializer for `1`; `n===1` is kept as the once-only half. The discriminating control is the implementation the withdrawn regression admitted: with the record dropped the field list is length **0** and the assertion fails while `n===1` still holds. ⚠ **And the same step 2 has two further definition-time outputs the replacement still cannot see, because "carries the initializer" is a presence test.** §15.7.10 step 2.5 creates the initializer, step 2.6 is `Perform MakeMethod(initializer, homeObj)` and step 2.7 is `Set initializer.[[ClassFieldInitializerName]] to name` (`webref body ecma262 sec-runtime-semantics-classfielddefinitionevaluation`). A field list whose entry holds *some* callable satisfies the assertion above while that callable has no home object — so `super.x` inside a field initializer resolves against nothing — and no `[[ClassFieldInitializerName]]`, which is what NamedEvaluation reads, so `class C { f = () => {} }` yields `""` where the spec yields `"f"`. Both are outputs of this row's own record construction and both are Rust-assertable at the record, on the same reading that puts `[[Name]]` there, so the acceptance asserts all three Which accessor the `vm/tests/` harness reads it through is **evidence**, not specification (§5's complement) — this row's own mandatory `/elidex-plan-review` settles it against its own parent HEAD. ⚠ **"First in the family" stood here and is withdrawn** — 2ac and 2b carry no edge on this row ⚠ **(this read "carry empty `Deps`" and was already false of 2b, whose cell reads `K` together with the child umbrella `A`'s derivation mints; it is now false of 2ac too, which carries the child umbrella `A` mints for the `FieldDefinition` grammar decision, A's routed item (iii))** and 2ac's row records that its already-live static-element path consumes nothing of this record, so the phrase serialised two independent slices behind an artifact they never use. The one edge inside the family runs onto this row from the children umbrella **2ab**'s derivation mints for 7.3.33 instance-element initialization, placed by that derivation, and its reason is the one this sentence was reaching for: instance initialization cannot run a field list that does not exist. ⚠ **Outside the family this row consumes a slot, and the reason the edge was missed is that the finding which raised it named the wrong helper — so it was probed rather than read.** The name in the record is a §7.1.21 result: §15.7.10 step 1 is "Let name be ? Evaluation of ClassElementName", and a computed `ClassElementName` is a `PropertyName : ComputedPropertyName` whose **§13.2.5.5** *Runtime Semantics: Evaluation* is "Let exprValue be ? Evaluation of AssignmentExpression" / "Let propertyName be ? GetValue(exprValue)" / "Return ? ToPropertyKey(propertyName)" (§-numbers and titles from `webref aoid ecma262 ToPropertyKey` and `webref heading ecma262 15.7.10 --exact` / `13.2.5.5 --exact`; steps from `webref body ecma262 ClassFieldDefinitionEvaluation` / `sec-object-initializer-runtime-semantics-evaluation`). The probe: `git grep -n 'to_property_key(' 658cc302 -- crates/script/elidex-js/src` returns four call sites and **every one is inside `vm/natives_object/`** (`descriptor.rs:389`, `iteration.rs:217`, `prototype.rs:146`, `prototype.rs:219`), so the class path does **not** reach `natives_object/mod.rs:28`. It reaches §7.1.21 through the *sibling* helper `VmInner::make_property_key` (`vm/ops.rs:422`), whose `vm/dispatch_objects.rs:48` / `:80` / `:232` callers are the handlers for the computed-property, computed-method and computed-accessor opcodes `compiler/expr_class.rs:415` and `:529-532` emit — and which matches `JsValue::Symbol` on the **argument** and sends everything else to `to_string`, the divergence the slot records. Both helpers are among the eight copies the slot names (`vm/tests/tests_member_compound_assign.rs:454-455`), which is why the edge is owed to the slot rather than to either of them: this row emits nothing for a non-static field today, so it builds the name-evaluation path fresh and would otherwise reuse a broken copy or add a ninth. ⚠ **This row answers §4 I-7's predicate, and it is an out-of-vocabulary positive that I-7's own seed cannot return.** A *computed* key is a runtime value, so the record this row retains cannot live in `compiled_functions`: it puts a property key and an initializer — both `JsValue` / `ObjectId`-valued in general — on the constructor object, and `vm/gc/trace.rs`'s `ObjectKind::Function(fo)` arm marks `fo.upvalue_ids` and `fo.captured_this` and nothing else (`:251-258`; the match is *exhaustive, no wildcard* by its own statement at `:244`), so a field list hung there is somewhere the collector does not walk — and an unmarked slot is returned to `free_objects`, i.e. a recycled `ObjectId`, not a leak. I-7's seed grep does not return this row: `git show c26d7148:<this file> \| grep -E '^[\|] \*\*2aa\*\*' \| grep -c -E -e GcRoots -e mark_roots -e 'gc/trace\.rs' -e 'gc/roots\.rs' -e 'gc/collect\.rs' -e 'tracing edge' -e 'trace arm' -e 'root set'` → **0**, against the same pipeline unrestricted on that blob → **11** and its negative control (`-e GcRootsXX -e mark_rootsXX` in place of the eight) → **0**, so the pipeline discriminates and this row sat outside its vocabulary — the same class as the **10b** and **6a** positives I-7 already names as failures of the seed. ⚠ **Read against `<this file>` the two return 1 and 12 from this note on, this row being its own hit**: writing the gap down is what moves the row inside the vocabulary, which restates the seed's failure mode one level up and is not a correction of the measurement — which is why the figures above are anchored on a blob and the command is re-runnable rather than as-of-writing. What this row owes under I-7 is the marking arm and a forced-collection regression with the record as the object's **sole** root, the collection asserted *placed* rather than requested; the shape is this row's own plan-review's to derive — ⚠ **and the *observable* is not, because the row's three record assertions are Rust-side reads that pass under a no-op trace arm.** **Red probe**: the field record is built inside an immediately invoked function so that the only surviving handle is the constructor the record hangs off, a collection is forced and asserted *placed* (`vm.inner.force_gc_before_next_alloc`, consumed — the `vm/tests/tests_member_compound_assign_gc.rs:56-62` pattern), and the computed key and the initializer are then read back through the record; without the arm the slots are returned to `free_objects` by `sweep_objects` and the read resolves a recycled `ObjectId`. ⚠ **Second gap, and it is unowned rather than deferred**: this row folds §15.7.10 step 2.6 `MakeMethod` and step 2.7 `[[ClassFieldInitializerName]]` into its acceptance for the *instance* path, while 2ac's own verified argument is that §15.7.14 step 30 sets `[[Fields]]` to instanceFields only — so neither row covers them on the static path. They are named here as **un-minted, never out of scope** | `compiler/expr_class.rs`, `bytecode/`, `vm/dispatch_class.rs` | `#11-step9-class-extras` (2aa) | T1 | the child umbrella **A**'s derivation mints for §15.7.1's `ContainsArguments` rejection — naming A itself would name nobody, so placement is a mandatory output of that derivation (§15.7.1's `ContainsArguments` rejection: this row is what emits a non-static field initializer at all, so its landing is what turns `arguments` inside one from inert into a scope question the initializer function it constructs must answer — and §15.7.9 forbids the answer from existing), **`#11-vm-topropertykey-symbol-from-toprimitive`** (§8 — the name this record retains is a §7.1.21 result, per the charter cell's citations and probe, and the slot owns collapsing the open-coded copies of that AO onto one implementation and then fixing it) |
| **2ab** | **When instance elements are initialized — UMBRELLA, not a terminal unit.** ⚠ **This row read as terminal, and an obligation it states it cannot discharge alone is what ends that**: it hands the private-element half to "whichever of this row and umbrella 5's child lands second", and a row that must wait on an **un-minted child of a different umbrella** to state a condition is a charter, not a slice. Five roots across four modules: **§10.2.2** `[[Construct]]`'s base-path initialization point together with the explicit-object-return substitution; **§13.3.7.1** step 12's derived-path initialization at `super()`, a distinct code path keyed on `thisER.[[FunctionObject]]`; **§7.3.33** InitializeInstanceElements' intra-initialization ordering, whose steps 1-2 precede steps 3-4; the **§7.3.28** / **§7.3.30** private-element machinery, which does not exist in tree and is co-owned; and the host-layer custom-element upgrade receiver. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. **The `Deps` edge on 2aa is withdrawn from that cell and re-pointed onto the specific children that consume the field record, by the same derivation.** The account it routes — base against derived. ECMA-262 **§10.2.2** `[[Construct]]` initializes a base class's instance elements **before** the constructor body and performs the explicit-Object-return substitution **afterwards**; a derived class initializes when `super()` establishes `this`, via **§7.3.33** InitializeInstanceElements. So `class A { x = 1; constructor() { return {} } }` must not add `x` to the returned object, a base constructor body must observe `this.x`, and a derived constructor that returns an object without calling `super()` must not acquire fields — three regressions, one per shape. ⚠ **None of the three is a derived class that *does* call `super()`, so the clause performing derived-class initialization is unasserted and the third passes vacuously.** The performer is **§13.3.7.1** `SuperCall : super Arguments` step 12, `Perform ? InitializeInstanceElements(result, funcObj)`, with step **10**'s `Let funcObj be thisER.[[FunctionObject]]` and step 11's assertion intervening, so step 9's `BindThisValue` is not immediately before it and step 10 is what supplies step 12's second argument (`webref body ecma262 sec-super-keyword-runtime-semantics-evaluation`), and at `658cc302` nothing performs it: `git grep -n -E -e 'InitializeInstanceElements' -e 'initialize_instance_elements' -e 'init_instance_elements' 658cc302 -- crates/script/elidex-js/src` returns **0** lines against the control `git grep -n 'DefineField' 658cc302 -- crates/script/elidex-js/src` → **3**. A derived instance therefore acquires no fields on *any* path, which is exactly what "must not acquire fields" asserts. Fourth regression, the positive direction the other three cannot reach: `class A { constructor(){} } class B extends A { y = 2; constructor(){ super(); this.z = this.y } }` must give `new B().y === 2` and `new B().z === 2` — the fields installed *by* `super()` and readable in the constructor body after it. ⚠ **All three are stated over *when* initialization runs relative to the constructor body, so none reaches the order §7.3.33 fixes *inside* one initialization.** Its steps 1-2 walk `ctor.[[PrivateMethods]]` and perform `PrivateMethodOrAccessorAdd` **before** steps 3-4 walk `ctor.[[Fields]]` and perform `DefineField` "For each element fieldRecord of fields" — i.e. in `[[Fields]]` order, which §15.7.14 step 26 appends to in source order (`webref body ecma262 sec-initializeinstanceelements` / `sec-runtime-semantics-classdefinitionevaluation`). Two more regressions, one per ordering: `class C { a = this.b; b = 1 }` must leave `a` as `undefined`, where any other field order answers `1` and passes all three above — ⚠ **and where an implementation that installs *no* field answers it too, which is the tree at `658cc302`** (`class C { x = 1 } (new C).x` → `undefined`), so alone it is a **guard**; it discriminates only beside the fourth regression above, which is red today, and it is stated as a pair with it rather than on its own; and `class C { #m(){} f = this.#m; g(){ return this.#m } }` must give `typeof new C().f === 'function'` together with `const c = new C(); c.f === c.g()` — the *retrieved method*, by callability and identity, never construction alone. ⚠ **"must construct" stood here and cannot fail on any implementation, correct or not**: `Op::GetPrivate`'s handler pops its operand and pushes `JsValue::Undefined` (`vm/dispatch.rs:1010-1015` at `658cc302`, under the banner `Private (Step 9 stubs)`), so `this.#m` answers `undefined` whatever §7.3.33 did and the witness constructs either way — while an implementation running steps 3-4 before steps 1-2 makes **§7.3.30** `PrivateGet` throw a TypeError once the read is real, which is the failure the assertion has to see. Identity holds because **§7.3.28** `PrivateMethodOrAccessorAdd` appends one PrivateElement whose `[[Value]]` every `PrivateGet` returns (`webref aoid ecma262 PrivateGet` / `PrivateMethodOrAccessorAdd`). ⚠ **And the strengthened witness is unrunnable until that read is real, so this row cannot own it alone**: §5's shared-prerequisite rule applies — both parties must exist before the obligation can be discharged, so the owner is **whichever of the child this row's derivation mints for the §7.3.33 ordering and the child umbrella 5's derivation mints for the `Op::GetPrivate` read lands second** — ⚠ *this named **this row** as the party, which the next sentence forbids for umbrella 5 and which this row's own marker forbids for it*, which integrates *and* tests it. Naming umbrella 5 itself would name nobody, so the party is that child, and placing this obligation on it is a mandatory output of umbrella 5's derivation — the same form row **A**'s cell uses for the child umbrella M mints for `parse_module`. Until then this row's §7.3.33 ordering deliverable stands on its Rust-side element order, not on a JS witness ⚠ The paragraph below this table headed *field-initialisation ordering* is this row's, and the reading it corrects — apply fields to `construct_synchronous`'s final, post-substitution value — is what this row must not ship. **The custom-element upgrade path is a no-regression test inside this row, not a slice of its own**: on that path the receiver and the pre-allocated instance are the same object (the paragraph below this table headed *custom-element upgrade receiver* is the measurement), so a host-layer slice would restate this row's receiver decision across a layer boundary with no artifact passing between the two. ⚠ **§10.2.2's this-binding *status* is not this row's.** All three regressions above pass while `this` stays readable before `super()`, because `do_new` preallocates the receiver for a derived constructor too — so retiring 2ab does not close it. the child the slot umbrella `#11-vm-derived-constructor-this-binding`'s derivation mints for 10.2.2's this-binding status owns that contract, and the slot and carries the carve reason | `vm/dispatch_class.rs`, `vm/interpreter.rs`, `vm/dispatch.rs`, `vm/host/custom_elements/` | `#11-step9-class-extras` (2ab) | T1 | — |
| **2ac** | **The static-element receiver.** The static initializer is compiled with the **enclosing** `FunctionCompiler` (`compiler/expr_class.rs:411` computed-key branch, `:418` otherwise, at `658cc302`), so `ExprKind::This` inside it reads the surrounding function's receiver and `class C { static x = this }` gives `C.x === C` → `false` (§2.2's `expr_class.rs:402-427` row). ECMA-262 **§15.7.14** step 32 performs DefineField on `ctorFunc` for each static field and **§7.3.32** DefineField calls the initializer with the field receiver. The mechanism already exists one arm over: the static **block** arm at `:448-471` builds a child compiler and invokes it with the ctor as `this` (`:454` `Op::Dup`, `:470` `Op::CallMethod`), and the static-field path does not use it. **The receiver contract covers the `PrivateField` static arm as well as the public `Property` one.** `:438` compiles a `static #x = …` initializer with the same enclosing `fc` (`git show 658cc302:crates/script/elidex-js/src/compiler/expr_class.rs \| sed -n '430,447p'`), and §7.3.32 DefineField reaches the initializer **before** it branches on the name — step 3.a is "Let initValue be ? Call(initializer, receiver)", the Private Name test is step 5 (`webref body ecma262 DefineField`) — so one receiver decision governs both arms. §2.2's `expr_class.rs:430-447` row covers that arm for a **different** defect (the missing non-static branch) and belongs to Slice 5, so without this sentence no row owns the private static receiver. ⚠ **"unobservable today only because the private read path is the stub Slice 5 implements" stood here and is FALSE — the defect is observable now, and the sentence would have let 2ac ship a regression that cannot see it.** The private static *initializer already executes*: `git show 658cc302:crates/script/elidex-js/src/compiler/expr_class.rs \| sed -n '435,438p'` is the `if *is_static` branch calling `compile_expr(fc, …)` with the **enclosing** `fc`, exactly as the public arm does. So a side effect inside the initializer reads the wrong receiver with no private *read* anywhere — `let seen; class C { static #x = (seen = this, 1) }` leaves `seen` as the surrounding function's `this` rather than `C`. **2ac therefore owes a side-effect-based private-static regression of its own**, because `ClassMemberKind::Property` (`:393`) and `ClassMemberKind::PrivateField` (`:430`) are **separate arms**: the row's public `class C { static x = this }` assertion passes while the private arm stays wrong. Same two-arm shape as the accessor-key and destructuring cases §2.2 records. ⚠ **And both regressions carry a single static element, so neither reaches the *order* the same step 32 fixes — which is the property most at risk from moving the initializer onto a child compiler.** §15.7.14 step 32 is "For each element elementRecord of staticElements" over the list step 26 appends to in source order, one interleaved pass rather than a pass per element kind (`webref body ecma262 sec-runtime-semantics-classdefinitionevaluation`). Third regression, confined to this row's own `ClassMemberKind::Property` arm so that it pulls in none of 2b's static-block work: `const log = []; class C { static a = log.push('a'); static b = log.push('b') } log.join('') === 'ab'` — ⚠ **green at `658cc302`** (executed → `"ab"`), so it is a **guard** on the remedy rather than a probe: the single interleaved pass is what the tree already does, and the regression exists so that moving the static-field initializer onto a child compiler may not re-order it. The fourth regression below is the red half of the pair (executed → `C.x === C` is `false`). ⚠ **And all three name a *literal* key, so none enters the computed-key branch at `:411` — the other of the two arms this row's first sentence names.** A repair applied at `:418` alone leaves `:411` compiling the initializer with the enclosing `FunctionCompiler` and every assertion above passes. Fourth regression, the same public-arm receiver contract driven through that branch — ⚠ **and a fifth and sixth, because the initializer function this remedy constructs owes the two outputs §15.7.10 step 2 states and this row names neither, while 2aa's `[[Fields]]` covers the instance path only**: `class A { static m(){ return 1 } } class B extends A { static x = super.m() }` must give `B.x === 1`, which requires the initializer to carry a home object, and `class C { static f = () => {} }` must give `C.f.name === "f"`, which requires `[[ClassFieldInitializerName]]`. ⚠ **The first of the two is unrunnable at `658cc302`** — `Op::GetSuperProp` pushes `JsValue::Undefined` at `vm/dispatch.rs:1027-1037` — so it takes the same "lands second" hand-off this row already uses for the ordering guard, stated rather than left implicit: `const k = 'x'; class C { static [k] = this }` must give `C.x === C`. **No dependency on 2aa**, though the Deps cell named it until PR-B: §15.7.14 step 30 sets `ctorFunc.[[Fields]]` to **instanceFields** only, static elements going to `staticElements` for step 32 (`webref body ecma262 ClassDefinitionEvaluation`), so 2aa's field record is not on this row's path. The class-name-ordering prerequisite the evidence block above states is **shared with Slice 2b and a member of neither charter** — that block says so in as many words — and it is owned by **Slice K**, a terminal prerequisite row of its own, rather than by this row or by a §8 slot — a slot ships no artifact and this row *consumes* one. ⚠ **"The `K` edge names an artifact" stood here with a `Deps` cell of `K`, and the artifact test fails against this row's own regressions.** §5's edge rule is artifact-consumption, and neither required regression reads the binding K moves: `class C { static x = this }` (asserting `C.x === C`) and `let seen; class C { static #x = (seen = this, 1) }` (asserting `seen === C`) have initializer bodies `this` and `(seen = this, 1)`, which contain no IdentifierReference to `C` at all. Nor does the assertion: `C.x === C` is read **after** class evaluation completes, off the *outer* binding, while what K reorders is the **inner** one — `compile_class` locates it as the `ScopeKind::Block` scope whose span is `class.span` (`compiler/expr_class.rs:485`) and emits `Op::InitLocal` / `Op::SetLocal` into that scope's local (`:498-499`), which nothing outside the class body reaches. The `class C { static self = C }` cited above is **K's own acceptance** — K's row asserts it there "with neither consumer landed" — a different initializer behaviour, and not a regression of this row's. The edge therefore ordered this row behind an artifact its deliverable does not consume, and it is removed. ⚠ **What is left is coordination, which §5 puts in the rows and never in the column**: K's acceptance runs through `ClassMemberKind::Property` (`:393`), the very arm this row repairs, so whichever of the two lands second carries the other's contract — this row by not regressing `C.self === C` when it moves the static-field initializer off the enclosing `FunctionCompiler`, K by deriving its emit position against whatever that arm looks like at K's own parent HEAD. Neither deliverable is an input to the other, which is exactly the case §5 names coordination. ⚠ **Deleting an edge is not finished until §4's obligations for this slice id have been re-read** (§5's fifth question): `awk '/^## §4\./,/^## §5\./' <this file> \| grep -c '2ac'` → **1**, re-derived at `29705dd6` (it was **0** through `3a34f4d5` and moved **0**→**1** at `f754c873`), and the slice-id-shaped control on the same window (`'\*\*10b\*\*'` / `'\*\*7b\*\*'` / `'\*\*6a\*\*'` → **4** / **2** / **2**, same command, same revision, moved **3**/**2**/**1** at `29705dd6`) shows the pipeline discriminates — so §4 *does* name this slice id: the single hit is **I-8**, which lists **2ac** among the rows stating an acceptance condition the tree already satisfies. That obligation is on this row's acceptance polarity and not on the deleted edge, which buys none of it | `compiler/expr_class.rs` | `#11-step9-class-extras` (2ac) | T1 | the child umbrella **A**'s derivation mints for §15.7.1's **(iii)** — naming A itself would name nobody, so placement is a mandatory output of that derivation (the `await`/`yield` `FieldDefinition` grammar policy: three of the five accepted spellings are static and this row lowers them, so it cannot choose spec-as-written or engine consensus independently) |
| **2b** | **Generic class static-block lowering** — `expr_class.rs:457` gives the block's child `FunctionCompiler` the *enclosing* `func_scope_idx`/`current_scope_idx` although `scope/visitor.rs:555` allocated the block its own function boundary, so block-local `let`/`var` resolve against the wrong scope (ECMA-262 **§15.7.11** step 5). Binding isolation and capture are the tests that retire the slot's static-block facet. ⚠ **The child compiler inherits *two* indices and this named one pair of test kinds over both, so a repair to either passes it — the acceptance is one regression per index.** `func_scope_idx` is the function boundary §15.7.11 step 5 creates (`OrdinaryFunctionCreate(%Function.prototype%, sourceText, formalParams, ClassStaticBlockBody, non-lexical-this, lexicalEnv, privateEnv)`, `webref body ecma262 sec-runtime-semantics-classstaticblockdefinitionevaluation`), so a `var` in the block must land inside it: `function f() { var v = 'outer'; class C { static { var v = 'inner' } } return v }` must give `'outer'`, which fails while the child carries the enclosing `func_scope_idx` and passes on a `current_scope_idx`-only repair. `current_scope_idx` is the block's own lexical scope, so a closure made in the block must resolve into it: `function g() { let x = 'outer'; let read; class C { static { let x = 'inner'; read = () => x } } return [x, read()] }` must give `['outer', 'inner']`, which a `func_scope_idx`-only repair leaves reading the enclosing `x`. ⚠ **Measured at `658cc302`, neither regression is the probe the two-index framing takes it for.** Regression 1 already answers `'outer'` — a **guard**, and what it guards is a repair that moves the block's `var` into the block instead of leaving it in the enclosing function. Regression 2 never reaches its assertion: `read = () => x` raises `ReferenceError "Cannot access variable before initialization"`, and the behaviour around it is a third mode this row's model does not name — a write from inside the block lands on an unrelated slot (`var out = ''; class C { static { out = 'ran' } } out` → `""`), the same write against a `let` raises that same `ReferenceError` at top level and inside a function (`let n = 0; class C { static { n = 1 } } n`; `function q(){ let n = 0; class C { static { n = 1 } } return n } q()`), and a closure made in the block reading a block-local `let` does not resolve at all (`function g(){ let read; class C { static { let x = 'inner'; read = () => x } } return typeof read } g()` → `ReferenceError "x is not defined"`). The deliverable is therefore not "the child carries one wrong index" but that together with the resolution failures those four probes record; regression 2 becomes a **red probe** only once it can reach its assertion, and this row's own plan-review re-derives the set against its parent HEAD. ⚠ **And after both declarations the row has no red condition left, so the pinned tree satisfies its acceptance in full**: regression 1 is a guard the tree already passes, regression 2 is declared unreachable, and the four probes record current behaviour without stating what each must answer afterwards — a PR that changes nothing retires the static-block facet. **The four probes are therefore stated with their post-fix values — the first three *red* at `658cc302`, the fourth a *green guard***: `var out = ''; class C { static { out = 'ran' } } out` is `"ran"`; a `let` declared in the block resolves inside it and not to an enclosing binding of the same name; a closure created inside the block still reads the block's own bindings after the block returns; and a `var` declared in the block does **not** escape to the enclosing function, which is the boundary §15.7.11 step 5's `OrdinaryFunctionCreate` establishes. ⚠ **That fourth condition is regression 1 under a second name**, and this cell measured it green two sentences above (`function f() { var v = 'outer'; class C { static { var v = 'inner' } } return v }` → `'outer'` at `658cc302`), so declaring it red contradicts the row's own measurement and §4 I-8: it is the **same guard**, kept because the repair that moves the block's `var` into the block is exactly what would break it, and this row's red set is therefore **three**, not four. **The `K` edge names an artifact**: a static block reads the inner class-name binding (`class C { static { if (C !== this) throw 0 } }`), and Slice **K** is what moves that binding's `Op::InitLocal` / `Op::SetLocal` initialization (`:479-500`) ahead of the member loop that emits this arm (`:99`, the `ClassMemberKind::StaticBlock` arm at `:448`) — K's own acceptance stops at the static-*field* form precisely because reading `C` from inside the block goes through the wrong-scope child compiler this row fixes, so the block form is **this** row's regression to assert once both have landed | `compiler/expr_class.rs` | `#11-step9-class-extras` (2b) | T1 | **K**, the child umbrella **A**'s derivation mints for it — naming A itself would name nobody (§15.7.1's `await` early error — without it this row's lowering ships a static block that accepts `await` once module mode is reachable) |
| **3** | **Super property references — UMBRELLA, not a terminal unit.** ⚠ **This row was terminal, and the frame-state axis its own module column already carries is what ended that.** §2.5 classified it as edge-dense in the same sentence that classified 0b/2/5/6, 0c and P, so what fails here is the terminality verdict, not the grouping. **Four mechanisms, and each fails while the other three pass** — the criterion's measurement, not a count. **(a) The emit shape**, [C24] **§13.3.7.3** MakeSuperPropertyReference and [C25] **§9.1.1.3.5** GetSuperBase: a super-specific `PushThis; GetSuperProp` / `GetSuperElem` lowering covering the named *and* the computed spellings, against the generic member path's `compile_expr(object); Dup; property-load` — the `super.m(...)` cross-slice paragraph below this table measures that following the generic contract leaves a stray operand or passes the super base as `this`, and today `compile_expr(ExprKind::Super)` emits `Op::PushUndefined` (`expr.rs:200-207`). **(b) The representation**, **§10.2.7** MakeMethod, whose step 2 sets `[[HomeObject]]` on the *function*: in-tree the home object is a **frame** field derived from `compiled.is_class_ctor` (`vm/interpreter.rs:802-806`) and `FunctionObject` carries no counterpart beside `captured_this` (`vm/value.rs:588-599`), so the frame-state paragraph's own conclusion — the home object belongs on the **closure** — is a closure-representation change in `vm/value.rs` and the interpreter, a different layer from (a), and by **I-7** a fourth layer besides: being a *frame* field is what roots the home object today (`vm/gc/roots.rs:297`, `vm/gc/trace.rs:357`), and a closure-carried one is reached by nothing, so the move retires a strong edge rather than adding an untraced one. **(c) Both call entry points**: `call_internal` independently hard-codes `home_class = None` (`vm/interpreter.rs:589`, whose own comment says so) and is the `NativeContext` path, so (a) and (b) can both land while `const m = new B().m; m.call(receiver)` still loses the home object. **(d) A second producer family, in a second compiler file, under a second clause chain**: an object-literal method is installed by `compile_object_expr` (`compiler/expr_object.rs:46`) and its accessors by `compile_accessor`, and the object-literal bullet above measures that `git grep -n 'home_class' 658cc302 -- crates/script/elidex-js/src` reaches `compiler/expr_class.rs`, `compiler/function.rs`, `compiler/expr_member.rs`, `bytecode/` and `vm/` and **no line** in `compiler/expr_object.rs` — while **§13.2.5.6** PropertyDefinitionEvaluation step 1 routes to **§15.4.5** MethodDefinitionEvaluation, whose step 1 is **§15.4.4** DefineMethod, whose step 7 performs §10.2.7 MakeMethod unconditionally, the `get` / `set` productions doing the same at their own step 7 (§-numbers from `webref aoid ecma262 MakeSuperPropertyReference` / `GetSuperBase` / `MakeMethod` / `DefineMethod` / `MethodDefinitionEvaluation` / `PropertyDefinitionEvaluation`). **The discriminating case is this document's own**: the audit as this row stated it is over `home_class` and a `new B().m` regression, so **every class probe can pass** while `const o={m(){return super.x}}; Object.setPrototypeOf(o,{x:1}); o.m()` stays broken — and (c)'s extracted-method case is that same shape one axis over. ≥3 intersecting invariant axes **and** more than one governing algorithm across three layers (compiler emit, bytecode/dispatch, closure representation) ⇒ **the criterion's first disjunct is met** — and the **second** disjunct is *not* claimed: §5's head states that "span more than one governing algorithm across layers" is CLAUDE.md's disjunct paraphrased, not CLAUDE.md's disjunct, so a row whose evidence is a layer count says so and stops there ⇒ umbrella. It **mints terminal children at its own start**, from those mechanisms and from the obligations written on this row elsewhere in the document; **placing each obligation, each acceptance condition and each `Deps` edge on the specific child is a mandatory output of that derivation** (§5's row-kind rule), and this row carries none of the three. ⚠ **This row mints none of them and invents no ids** — which children, and how the four mechanisms divide between them, is that derivation's output, and a mechanism absent from this cell is **un-minted, never out of scope**. **The obligations that derivation must place, each of which names this row today**: the per-producer home-object handoff of the *shared prerequisite* bullet above — public instance field, static field across **both** its `Property` and `PrivateField` arms, static block, private instance field, private methods and accessors — whose "whichever lands second" party must be named as a child, since naming an umbrella there names nobody; the object-literal producer of (d), which is also what fires `#11-vm-property-key-lowering-unification`'s site-stated trigger (§8); the `vm/value.rs:1049-1053` eval-contract retag [§10](2026-07-vm-p4-umbrella-review-rounds.md)'s carry-over table assigns here; the §2.2/§2.3 Layer-A/Layer-B decomposition's missing frame-state row; **I-7's tracing edge for the closure-carried home object and the retirement of the two frame-marking sites it replaces**, owed by *the child of umbrella 3 that owns the `[[HomeObject]]` representation on the closure* with a regression that survives a forced collection and whose home object has no other root — `let m; (function () { class A {} A.prototype.x = 42; class B extends A { m() { return super.x } } m = B.prototype.m })();` then the deterministic-collection idiom `vm/tests/` already uses, then `m.call({})` must be `42`, which fails on a missing arm and on a non-propagating one alike, and the object-literal spelling of (d) is that test one producer over; and the constraint that no child land `GetSuperProp` on the generic `CallMethodSpread` path. **The edges pointing *at* this row are the same rule read from the other end**: **S** carries a `Deps` on it and **N** reaches it through S — each is re-pointed at the child that owns the artifact it consumes, via this umbrella's derivation, which mints it, because an edge attached to an umbrella is stranded and "until 3 lands" names an event that does not occur. **No cell changes but the row-kind marker.** The Slot stays `#11-step9-class-extras` — the `(umbrella)` qualifier is the kind marker row **2** already carries on the identical adoption, not a second slot; the Tier stays **T2**, inherited by the children; and `Deps` stays **—**, which the rule *requires* rather than merely permits, an umbrella carrying no ordering. The `Primary module(s)` cell stays as **evidence** for the derivation to route — rows **5** and **Pd**, both umbrellas, keep theirs on the same reading — and is not a touch set, this row having none | `compiler/expr.rs`, `expr_member.rs`, `vm/dispatch.rs`, **+ the frame-state axis: `vm/interpreter.rs:795-806`, `vm/value.rs:1037-1046`, `bytecode/compiled.rs:74`** | **adopt** `#11-step9-class-extras` (umbrella) | T2 | — |
| **4** | **Tagged templates + `String.raw` — UMBRELLA, not a terminal unit.** The charter bundled three mechanisms that share no lowering chokepoint: the template object [C23] (a per-realm, per-call-site cached Array, frozen, carrying a frozen `raw`), the tagged-call lowering [C40] (which reaches [C20] EvaluateCall with the tag's **Reference**, so a property-reference tag keeps its receiver), and `String.raw` [C44] (an ordinary builtin, installed and coerced like any other). They sit in the compiler, in per-realm runtime object state, and in builtin installation respectively. So the charter can pass a basic tag-call test while the template object is freshly built on every evaluation, or mutable, or while ``obj.tag`x` `` calls the tag with the wrong receiver — three ways to be wrong that no one regression sees. The derivation below returns two independently live groups and `4a`, the larger of them, is itself an umbrella — a row holding an umbrella is not one PR ⇒ umbrella. ⚠ **The split here has been wrong twice and both times because it was a verdict rather than a derivation** — first "4 is terminal", then "4a = the object / 4b = the lowering", which put the tag path's **only** producer in the later row and left the first one dead (I-4). §5's own rule says a terminality verdict is evidence and the *criterion* is what this table specifies, so what belongs here is the derivation. **Criterion**: a seam is admissible when each side has a live producer or consumer inside its own PR, **or** when the earlier side is a *prerequisite row* under §5's row-kind rule — one whose artifact is asserted at its own landing point, from Rust when nothing in JavaScript reaches it yet. ⚠ **The criterion read "only when each side has a live producer or consumer", and that was too strong**: it is the terminal-row half of the rule stated as if it were the whole rule, and it is already falsified inside this table by **`6c`** (ships `%AsyncIteratorPrototype%` with no JS holder, asserted from Rust) and **`8d`** (ships symbol ids consumed only by other rows — the child umbrella 8 mints for RegExp creation, umbrella 8b's dispatching children, and umbrella 9b's). Under the corrected criterion the `{a}` / `{b}` seam — `GetTemplateObject` plus the per-realm template registry, its lifetime and its GC rooting, ahead of the tagged-call lowering that consumes them — **is admissible**, and the objection that the object row would be "dead on arrival" does not survive, since I-4 forbids a dead *opcode*, not an intrinsic awaiting its consumer. ⚠ **This row does not re-cut the split to say so.** Slice 4 is an umbrella, and its children are minted by its derivation at its own start (the row-kind rule); the split has already been re-cut four times by hand here, each time as a verdict, which is the failure this row's own next sentence records. What belongs here is the corrected criterion the derivation runs under. ⚠ **And the candidate set is built from *governing algorithms*, one clause each — never from the steps inside one.** A derivation stood here whose candidates were §13.2.8.4 *steps 4-15* and *steps 3/16* as if they were two mechanisms; they are one AO (`webref aoid ecma262 GetTemplateObject` → one entry, one anchor), and steps 4-15 run only on the cache miss step 3 takes. Cutting there produced a seam **inside** an algorithm, which is §5's head rule that a derivation names the spec clauses defining its surface's algorithms, applied to the wrong unit — the same curated-by-inspection failure [§13](2026-07-vm-p4-umbrella-review-rounds.md) records, one level down. **Candidate set, one governing algorithm each**: **(a)** [C23] **§13.2.8.4** GetTemplateObject, entire — the site- and realm-keyed object, its construction and its freezing; **(b)** [C40] **§13.3.11.1**, the tagged-call lowering; **(c)** [C44] **§22.1.2.4** `String.raw`. **Applying the criterion**: {a} alone has no producer — nothing invokes GetTemplateObject but a tag call ✗; {b} alone has no object to pass ✗; **{a,b}** is live in one PR, the tag call runs and returns the same frozen object for a repeated site ✓ — ⚠ **but only the first conjunct discriminates.** At `658cc302` tagged templates parse and do nothing: measured, `` var called=false; function t(){called=true} t`x` `` leaves `called` `false` and `` function t(){return 'TAGRAN'} t`x` `` evaluates to `undefined`, so the identity half is green here **and** under Node for the same repeated site, by `undefined === undefined` — it holds with the whole caching mechanism absent, and `Object.isFrozen` has no object to take. The identity assertion must be spelled across **different** sites, where `` function t(s){return s} function a(){return t`x`} function b(){return t`x`} a() === b() `` is `true` at `658cc302` against `false` under Node.; **{c}** is reachable alone ✓ (its row measures why it is off the tag path). ⇒ **two sub-slices: `4a` = {a,b}, `4c` = {c}.** ⚠ **The edge-density objection to `4a` was answered here by CLAUDE.md's own base case — *「承認済 umbrella 配下で plan-review を通った narrowly-scoped per-PR slice は terminal 単位 = 許容される単一 PR」* — and that answer is WITHDRAWN**, by §5's own reading of the same clause (c) at the head of this section. The base case still stops the infinite regress — it is what makes each of *4a's* children a permissible single PR — but it cannot be spent on a group that fails the criterion, and 4a's row measures that it does. `4a` is an umbrella from this revision. ⚠ **Ids `4b` and `4d` are both retired and neither is reused**: `4b` named the lowering through `86b47963`, `4d` named the registry through `6b1a0eb5`, and this row has now been split three ways in four review rounds — the ids are left burnt so no earlier reference silently re-points | — | `#11-vm-tagged-template-literals` (umbrella) | T1 | — |
| **4a** | **GetTemplateObject + the tagged-call lowering — UMBRELLA, not a terminal unit.** ⚠ **The seam is admissible and stands; what fails §5's criterion is the *terminality verdict* on the group that seam returned, and re-deriving every verdict in this table in one pass is what caught it.** §5's head already states that a derivation's output is a **grouping, not a slice** and that the criterion is applied to each group; applied to this group's own enumerated content it bundles the tagged-call **lowering** (**§13.3.11.1**, reaching [C20] **§13.3.6.2** for the receiver), the **construction and freezing** of a per-realm, per-call-site-keyed registry (**§13.2.8.4**), hand-written **GC root-set** integration ((i) below), **realm lifetime** and the release point it must be tied to ((iv)/(v)), an **identity-key carrier** that must not alias one site's frozen template onto another's ((vi)), and a **non-`engine` build** obligation ((iii)) — ≥3 intersecting invariant axes, sitting in the compiler, in per-realm VM runtime state and in the GC subsystem, so the criterion's **first** disjunct is met — and the **second** disjunct is *not* claimed: §5's head states that "span more than one governing algorithm across layers" is CLAUDE.md's disjunct paraphrased, not CLAUDE.md's disjunct, so a row whose evidence is a layer count says so and stops there. It therefore mints terminal children at its own start, from the obligations enumerated below; **placing each obligation, the I-3 edge onto the child umbrella **1b**'s derivation mints for the argument-emit helper and each acceptance condition on the specific child is a mandatory output of that derivation** (§5's row-kind rule), and this row carries none of the three. Seams **{a,b}** of the umbrella's corrected derivation (`{a,c}` stood here under the withdrawn step-level lettering, where `c` was the lowering; under the algorithm-level set `c` is `String.raw`, so the stale label handed 4c's mechanism to this row as well). ⚠ **This row has been re-cut four times — `4a`+`4b` (object / lowering), one merged row, `4a`+`4d` (construction / registry), and now the whole tag path — and the umbrella carries the derivation that replaces the verdicts.** **Construction** — [C23] **§13.2.8.4** GetTemplateObject steps 4-15: the cooked and raw elements are defined non-writable and non-configurable (steps 12.c/12.e), `rawObj` is frozen (step 13), `"raw"` is defined non-enumerable and non-writable (step 14), and `template` is frozen (step 15). **Lowering** — [C40] **§13.3.11.1**, whose `MemberExpression TemplateLiteral` and `CallExpression TemplateLiteral` productions both evaluate the tag to a **Reference** at step 1 and pass that reference to `EvaluateCall(tagFunc, tagRef, TemplateLiteral, tailCall)` at step 5, so [C20] **§13.3.6.2** step 1.a.i `GetThisValue` is what supplies the receiver: a property-reference tag keeps it, and a lowering that evaluates the tag to a *value* loses it silently. The baseline emits `Op::PushUndefined` for the whole node (`compiler/expr.rs:237-239` at `658cc302` — §2.2) and `Op::TaggedTemplate` has **zero** compiler emit sites (§2.3's derivation, re-run whole), so this row is what makes the mechanism live at all. The template object is the call's first argument, substitutions after it, and the argument list routes through the I-3 fixed-prefix / item-iterator input contract the child umbrella **1b**'s derivation mints for the argument-emit helper defines for `compile_call_arguments` / `ArgsForm`, which the child umbrella **1b**'s derivation mints for the argument-emit helper carries as a named deliverable — hence the edge on that child, which this umbrella's derivation places on the child that owns the lowering rather than this row carrying it in a `Deps` cell of its own. What those steps require of an observable result — the tag runs, receives a **frozen** array carrying a frozen `raw`, and `` obj.tag`x` `` sees `obj` as its receiver — is evidence for the derivation to place on a child as *its* acceptance condition, and is not an acceptance condition of this row (**examples, not a sufficient set** either way, §5's demotion). ⚠ **One trap measured and recorded because it is invisible to all three**: `TemplateElement::cooked` is `Option<Atom>` (`ast.rs:409` at `658cc302`) and the *ordinary* template lowering maps `None` to the empty string (`compiler/expr.rs:129-133`, `map_or_else(Vec::new, …)`), so reusing that representation on the tag path hands `` tag`\unicode` `` a `""` cooked entry where §13.2.8.2 *Static Semantics: TemplateStrings* requires **undefined** (TemplateStrings "returns a List of either Strings or undefined"; the undefined element is minted by §13.2.8.3 *Static Semantics: TemplateString ( templateToken, raw )*, whose Note reads "This operation returns undefined if raw is false and templateToken contains a NotEscapeSequence", via the §12.9.6.1 *Static Semantics: TV* it calls at step 2.a, which itself "returns a String or undefined"; §13.2.8.4 only consumes that List at step 6 and propagates the element at step 12.b, "Let cookedValue be cookedStrings[index]") — with the raw text preserved. **And the registry, which is the same algorithm and not a separable mechanism.** ⚠ **A `4d` row held §13.2.8.4 steps 3 and 16 separately, with 4a pinning the same-object guarantee as a declared divergence; both are withdrawn.** Step 3 returns the already-registered Array when `[[Site]]` is the same Parse Node and step 16 appends the Record to `realm.[[TemplateMap]]` — steps 4-15 run only on the miss, so the registry is the algorithm's spine rather than a layer over it. Two reasons the pin was wrong, and the second is the one that decides it: (1) the seam was cut **inside one algorithm** — `webref aoid ecma262 GetTemplateObject` returns one entry and one anchor for §13.2.8.4 — which the umbrella row's candidate-set rule, one clause each and never the steps inside one, does not admit — see the umbrella row's corrected candidate-unit rule; and (2) **the pinned intermediate would have been a new silent-wrong path, the exact defect class §2.1 says this program exists to remove.** Step 3's cache is what lets a tag function memoize on the template object — the `WeakMap`-keyed-on-the-strings-array idiom — so a fresh object per evaluation breaks memoizing tags with **no error at all**, silently, at the compiler emit layer. `KNOWN-DIVERGENCE` declares a divergence; it does not make a silent one loud, and I-1 asks for loud. Every obligation below is therefore this row's. ⚠ **The obligations the acceptance shape does not imply are **(i)-(vii) below, and the extent is the enumeration itself — no count is carried** (a "Six" stood here against seven items; [§13](2026-07-vm-p4-umbrella-review-rounds.md)'s rule is that a count is derived or absent, and a checklist's extent is one place a wrong count silently narrows what is binding). They are named, not solved**: this row states what the slice must answer and **4a's** own mandatory plan-review derives the answers against its own parent HEAD (§5's specification-vs-evidence rule), so the in-tree readings below are evidence about the code at `658cc302`. (i) **The registry must be added to the GC root set by hand, because root enumeration is hand-written throughout.** `GcRoots` (`vm/gc/roots.rs:21-262`) is a hand-written struct with one field per root set; its **only** construction site is a struct literal in `collect_garbage` (`vm/gc/collect.rs:52-1145`, 40 field lines) naming the `VmInner` fields one by one; and `mark_roots` (`vm/gc/roots.rs:266-619`) walks each field in its own lettered step. Nothing derives a trace from `VmInner`: `git grep -c 'trait Trace' -- crates/script/elidex-js/src` and `git grep -c 'derive(Trace' -- crates/script/elidex-js/src` each match nothing, against the positive control `git grep -c 'derive(' -- crates/script/elidex-js/src`, which matches in 65 files. So a per-realm template registry hung off `VmInner` is **invisible to the collector** until the slice adds the `GcRoots` field and the `mark_roots` arm for the map's cooked and raw arrays. (ii) **The identity assertion cannot be the root regression, and forcing a collection inside it does not make it one** — §4 I-7's second half. `a === b` has to *retain* the first template array in order to compare it, and that binding keeps the array marked by `mark_roots`'s walk whatever arm (i) adds, so the assertion passes under a no-op arm. **Two regressions, not one.** *Identity*: both evaluations inside one batch through one retained function (`parse_script` mints a fresh arena per parse, (vi), so a second batch is a different `[[Site]]` and owes a *different* object), asserting step 3's cache. *Root*: the registry as the array's **sole** root — a first batch that calls the function and keeps only a primitive derived from the result (`globalThis.n = f().length`), every object handle dropped, then `vm.inner.collect_garbage()` from Rust between batches (there is no JS-callable collector: `grep -rn 'collect_garbage' crates/script/elidex-js/src/` returns **143** lines at `29705dd6` — `vm/inner.rs:96`/`:104`, **111** under `vm/tests/`, **22** under `vm/gc/` (the definition at `gc/collect.rs:25`, **14** calls in `gc/tests.rs`, the rest doc comments) and **8** doc-comment mentions in `vm/mod.rs` / `vm/host/`; every call site is Rust, none is JS-callable, which is the property this parenthesis asserts), then a second batch calling the same function, whose read must not hit `get_object`'s `expect("object already freed")` (`vm/inner.rs:288-291`) — under a missing root field the entry's `ObjectId` was swept to `None` (`vm/gc/sweep.rs:22-31`). (iii) **I-5's non-`engine` consequence is not owed by the children umbrella 7b's derivation mints alone.** The field lands in that same hand-written root set, whose fields and `mark_roots` arms are individually `#[cfg(feature = "engine")]`-gated (and `vm/gc/collect.rs` carries a `#[cfg(not(feature = "engine"))]` counterpart for each), while a template object is pure ECMA-262 — so the field and its mark arm are owed identically to non-`engine` builds, which is what §4 I-5 requires of the children umbrella **7b**'s derivation mints, for the same directory. (iv) **Grain — the realm axis is part of the key, not an assumption.** "Keyed by call site *and* realm" plus a plain `VmInner` field are consistent only under one realm per VM, and `docs/plans/2026-06-agent-scoped-ecsdom-world.md` §5 **design req 7** schedules that away: *"The current `Vm` (singleton `global_object` + singleton prototype slots) must generalize to **N realms** (one per Window)."* The in-tree sibling annotates this at the residence rather than as a TODO — `HostData::ce_registry` (`vm/host_data/mod.rs:536-557`): *"the grain migration to a per-realm component rides agent-scoped EcsDom, docs/plans/2026-06-agent-scoped-ecsdom-world.md §5"*. (v) **Lifetime — realm lifetime, which is neither of the two release points already in the tree.** `unbind` (`vm/lifecycle.rs:15`) closes every script BATCH, so releasing there rebuilds the template on the next batch and breaks step 3's guarantee *across* batches. But `teardown_document` (`:536`) is **document** destruction, and a realm is not owed to its Document either: under the multi-realm design of (iv) a same-agent Window can retain a callable from a navigated-away iframe, and invoking it must still find that realm's `[[TemplateMap]]` entry. Both in-tree release points are **wrong in opposite directions**; what the slice owes is a release tied to realm destruction, and where that is expressible in its parent tree is its plan-review's to derive. The regression follows the same shape: single-batch is too weak, so it must **also cross an `unbind`**, and a retained-callable-across-teardown case is what distinguishes a correct release point from `teardown_document`. (vi) **Key — there is no Parse-Node identity in the VM, so `[[Site]]` needs a carrier.** `grep -rnw 'Arena' crates/script/elidex-js/src/vm/` → **0**, and the same for `NodeId` (control, identical shape: `ObjectId` → 2217); `NodeId<T>` is `{ index: u32, _marker }` whose `PartialEq`/`Hash` read `index` alone (`arena.rs:10-42`); and every `parse_script` / `parse_module` mints fresh arenas (`parser/mod.rs:109-111`), so index *n* from two separately parsed scripts compares equal. **Keying on a raw `NodeId` would alias one site's frozen template onto another's** — a worse violation than the unrooted one in (i), and that hazard is the obligation. The in-tree carriers that survive the aliasing test are a starting point and not the design: `FuncId` (`vm/value.rs:50-52`) indexes the VM-global `compiled_functions`, which is never truncated or recycled and rides every `CallFrame` beside `ip`, and the compiler already allocates a per-call-site call-IC slot index; either must still be crossed with (iv)'s realm axis. (vii) **Why this is a side store and not an ECS component**, since it is per-VM state holding `ObjectId`s: CLAUDE.md's exception (a) (per-VM identity handle) covers the values for the same reason `HostData::ce_constructors` claims it (`vm/host_data/mod.rs:576-582`) — and, as there, the key is not an `Entity` (`ce_constructors` is `HashMap<u64, ObjectId>`), so the per-entity precondition of the side-store→component rule is not met either. Two independent reasons; (iv)'s realm axis is what the eventual migration turns on | — | `#11-vm-tagged-template-literals` (4a umbrella) | T1 | — |
| **4c** | **`String.raw`** — [C44] **§22.1.2.4**, which is **not** on the tag path: it is an ordinary builtin whose steps 2-4 are `ToObject(template)`, `ToObject(Get(cooked, "raw"))` and `LengthOfArrayLike(literals)`, so it operates on any object carrying a `raw` property and needs its own installation and its own coercion behaviour, not the lowering the child of umbrella **4a** that owns the tagged-call lowering delivers. Absent at `658cc302`: `git grep -n '"raw"' 658cc302 -- crates/script/elidex-js/src/vm` reaches only an unrelated SubtleCrypto key-format arm (`vm/host/subtle_crypto/marshal/key.rs:89`), while the same grep shape for a sibling String static (`'"fromCharCode"'`) reaches `vm/globals_primitives.rs`. **Independent of 4a**, though the Deps cell named it until PR-B: the algorithm reaches its argument only through `ToObject(template)` (step 2), `ToObject(? Get(cooked, "raw"))` (step 3) and `LengthOfArrayLike(literals)` (step 4), i.e. ordinary property access on any object carrying `raw`, and the only mention of tagged templates in the clause is its closing **Note**, which is non-normative (`webref body ecma262 sec-string.raw`). ⚠ **This row states no acceptance at all, and §22.1.2.4 has three exits that one `` String.raw`a${1}b` `` call cannot separate.** Step 5 is `If literalCount ≤ 0, return the empty String`; step 8.4 is `If nextIndex + 1 = literalCount, return result` — the loop's only normal exit, so the last literal is appended and no substitution follows it; and step 8.5 is `If nextIndex < substitutionCount`, so a call with **fewer** substitutions than `literalCount - 1` appends nothing rather than `"undefined"` (same `body` output). One assertion per exit, each over an ordinary object rather than a template object, since steps 2-4 are `ToObject(template)` / `ToObject(? Get(cooked, "raw"))` / `LengthOfArrayLike(literals)` and this row's whole point is that they are: `String.raw({ raw: [] }) === ""`, `String.raw({ raw: ['a', 'b'] }, 1) === "a1b"`, and `String.raw({ raw: ['a', 'b', 'c'] }, 1) === "a1bc"`. ⚠ **All three pass an ordinary object carrying an array-valued `raw`, so none of them reaches steps 2-3 — the coercion this row's own opening sentence assigns to the slice.** Both are `?`-prefixed `ToObject` calls, so a nullish argument and a nullish `raw` are **abrupt**, not empty: two more, `String.raw(null)` throws a **TypeError** at step 2 — ⚠ **green at `658cc302` and therefore a guard rather than a probe**, because `String.raw` is absent (`git grep -n '"raw"' 658cc302 -- crates/script/elidex-js/src/vm` reaches only `vm/host/subtle_crypto/marshal/key.rs:89`) and the *call itself* already throws; both TypeError conditions read the same way. ⚠ **And nothing separates the steps 2-3 `ToObject` coercion this row's own opening sentence assigns to the slice**, so an implementation requiring both `template` and `Get(template, "raw")` to be objects passes every stated condition. **Two red probes at `658cc302`**: `String.raw({raw: "ab"}, 1)` is `"a1b"` — step 3's `ToObject` over a primitive — and `String.raw({raw: {length: 2, 0: "a", 1: "b"}}, 1)` is `"a1b"`, step 4's `LengthOfArrayLike` over an array-like rather than an Array — ⚠ **while `String.raw({raw: null})`'s TypeError at step 3 is not a third item in a list headed "two", but the second of the two guards this cell already declared** ("both TypeError conditions read the same way"): it is green at `658cc302` for the same reason `String.raw(null)` is, the call itself throwing while the static is absent, and it stays green once step 3's `? ToObject` lands, which is what makes it a guard (`webref body ecma262 sec-string.raw`), where an implementation treating either as an empty raw list answers `""` and passes all three above | `vm/globals_primitives.rs` | `#11-vm-tagged-template-literals` (4c) | T1 | — |
| **5** | **Private names — UMBRELLA, not a terminal unit; and "complete" is a membership claim, so the arms are named.** ⚠ **This row was terminal, and gaining `PrivateMethod` is what ended that.** It now spans three invariant axes that meet at the Private Name carrier §6.2.12 and §7.3.26 define and each fail while the other two pass, across compiler lowering, runtime representation and dispatch: Private Name **identity** with the `GetPrivate`/`SetPrivate`/`PrivateIn` dispatch (**§6.2.12**, **§7.3.26**), private **fields** — the field record and its instance initialization, which is why the `2ab` edge exists (**§7.3.32** / **§7.3.33**) — and private **methods and accessors** installed at class definition (**§7.3.28** PrivateMethodOrAccessorAdd). ⇒ umbrella under §5's criterion. Slice 4's partition was re-cut four times in four review rounds and each minting left dangling references behind it; the ids are derived by walking the `ClassMemberKind` variants the compiler matches on against the private-element algorithms that govern them, **and the spec side of that walk is a command rather than a `§7.3.x` wildcard**: `webref heading ecma262 7.3` returns the block **§7.3.26** PrivateElementFind, **§7.3.27** PrivateFieldAdd, **§7.3.28** PrivateMethodOrAccessorAdd, **§7.3.29** HostEnsureCanAddPrivateElement, **§7.3.30** PrivateGet, **§7.3.31** PrivateSet, **§7.3.32** DefineField and **§7.3.33** InitializeInstanceElements, and the walk is run against all eight together with **§6.2.12** *Private Names* and **§15.7.14** ClassDefinitionEvaluation, which no §7.3 clause reaches. ⚠ **A wildcard is not a seed, and the two clauses this row's own arms implement were the ones it left out**: the `GetPrivate` / `SetPrivate` stub the `Primary module(s)` cell measures is **§7.3.30** / **§7.3.31**, and neither appeared anywhere in this row while §7.3.26 PrivateElementFind — which those two *perform* — did, so the walk named the callee and not the caller — the same shape Slice 9's and Slice M's rows already use, and the reason is the same: a partition computed reactively, one finding at a time, is what produced the churn. ⚠ **The `ClassMemberKind::PrivateMethod` arm (`compiler/expr_class.rs:347` at `658cc302`) had no owner**: this row named the field branch and the `GetPrivate`/`SetPrivate`/`PrivateIn` stubs, §2.2's private-name row reaches `PrivateMethod` only as one of the two kinds `check_private_name_dup` inserts, and no other row mentions it — a **fifth** finding from the public/private two-arm split (`Property` `:393` vs `PrivateField` `:430`, `Method` `:293` vs `PrivateMethod` `:347`). It is this row's: private methods and accessors, static and instance, are private *names* by the same **§7.3.28** PrivateMethodOrAccessorAdd this row's [C] entry already cites. Its inventory is **derived at slice time from the `ClassMemberKind` variants the compiler matches on**, not from the list in this sentence — the list is what has just been shown to go stale. ⚠ **One representation fact recorded as evidence, because a field/get/set regression cannot see it**: `Op::GetPrivate` / `Op::PrivateIn` carry a constant **name index**, i.e. the interned spelling, so a string-keyed implementation passes those regressions while letting one class reach another class's same-spelled `#x`; ECMA-262 gives each *evaluation* of a class its own Private Names (**§15.7.14**, **§7.3.26**), so the carrier is a per-class-evaluation identity and the discriminating probe is a factory returning two classes where `A.read(new B())` must throw. **The `2ab` edge names an artifact** (it did not before; the row's only justification was §5 prose): a private *instance* field `#x = 1` is initialized by ECMA-262 **§7.3.33** InitializeInstanceElements, whose steps 3-4 walk `ctor.[[Fields]]` and perform `DefineField(obj, fieldRecord)`, and **§7.3.32** DefineField step 5.a is `PrivateFieldAdd(receiver, fieldName, initValue)` — so the invocation point the child umbrella **2ab**'s derivation mints for instance-element initialization decides is on this slice's path (`webref body ecma262 InitializeInstanceElements` / `DefineField`). ⚠ **The `2ac` edge is withdrawn, and by this document's own later correction.** It read: the wrong receiver is present on the `PrivateField` static arm too and *this* slice unmasks it, because the private read path is the stub below. 2ac's row now measures the opposite — the private static **initializer already executes**, so `let seen; class C { static #x = (seen = this, 1) }` observes the wrong receiver with no private read at all — and assigns both the fix and a side-effect regression to **2ac**. Slice 5 consumes no artifact of that correction, so the edge was shared-surface ordering of the kind the 0b family's deleted edges were, and it over-ordered the private-name work | `compiler/expr_class.rs`, `expr_member.rs`, `expr_ops.rs`, `expr_assign.rs:202-206`, **`vm/dispatch.rs:1011-1026`** (the `GetPrivate`/`SetPrivate`/`PrivateIn` stub — the start line was cited as `:1020`, which is a `self.pop()?;` inside the `SetPrivate` branch; re-derived with `git show 658cc302:crates/script/elidex-js/src/vm/dispatch.rs \| grep -n 'Op::GetPrivate'` → **1011**, same at HEAD. 0ca only makes the reachable arms loud; the implementation is this slice's, and `SetPrivate` is additionally an I-4 connect) | `#11-vm-class-private-fields` + `#11-step9-class-extras` | T1 ⚠ **The `2ab` edge is a *child's*, and the derivation places it.** Only the private-**field** child consumes the instance-initialization artifact the child umbrella **2ab**'s derivation mints for the invocation point (§7.3.33 → §7.3.32 step 5.a `PrivateFieldAdd`, this row's own citation); attaching it to the umbrella either strands it (the field child could retire before its invocation point exists) or over-orders the identity and method/accessor children behind work they do not consume | — |
| **6** | **Async generators + async iteration — UMBRELLA, not a terminal unit.** Three axes intersect here and the first disjunct is met on them: async-generator objects (ECMA-262 **§27.9** *AsyncGenerator Objects*), the sync→async iterator adapter (**§27.1.5.1** CreateAsyncFromSyncIterator) and the async form of ForIn/OfBodyEvaluation — ⚠ **both numbers were drifted and are re-derived here**: `webref heading ecma262 27.6` is *GeneratorFunction Objects* and `27.1.4` is *The %AsyncIteratorPrototype% Object*, while `webref heading ecma262 27.9` gives *AsyncGenerator Objects* and `webref aoid ecma262 CreateAsyncFromSyncIterator` gives §27.1.5.1 (consistent with 6a's own §27.9.x citations) — and the evidence block above adds a further axis inside the first, two call entry points rather than one. ⚠ **The seed gains a further clause, produced by the command rather than by recall: §27.7 *AsyncGeneratorFunction Objects*.** `webref heading ecma262 27` returns it between **§27.6** *GeneratorFunction Objects* and **§27.8** *Generator Objects* — it is **not** §27.4, which the same command gives as *AsyncDisposableStack Objects*, Slice R's clause — and `webref heading ecma262 27.7` returns **§27.7.1** *The AsyncGeneratorFunction Constructor* with §27.7.1.1 `AsyncGeneratorFunction ( ...paramArgs, bodyArg )`, **§27.7.2** *Properties of the AsyncGeneratorFunction Constructor* with §27.7.2.1 `AsyncGeneratorFunction.prototype`, **§27.7.3** *Properties of the AsyncGeneratorFunction Prototype Object* with §27.7.3.1 `constructor` / §27.7.3.2 `prototype` / §27.7.3.3 `[ %Symbol.toStringTag% ]`, and **§27.7.4** *AsyncGeneratorFunction Instances* with §27.7.4.1 `length` / §27.7.4.2 `name` / §27.7.4.3 `prototype`. ⚠ **§27.9 reaches those objects only as values, and a mention is not a seed** — the relationship Slice R's row records for the disposal symbols: `webref body ecma262 sec-asyncgenerator-objects \| grep -c AsyncGeneratorFunction` → **2**, both identity statements about `%AsyncGeneratorPrototype%` ("is %AsyncGeneratorFunction.prototype.prototype%" at §27.9.1, "The initial value of %AsyncGeneratorPrototype%.constructor is %AsyncGeneratorFunction.prototype%" at §27.9.1.1), against the control `\| grep -c AsyncGenerator` → **97** on the same output, so the grep discriminates. What *defines* the function object's own surface is §27.7.3, so under the seed as it stood a child could ship async-generator objects and their request protocol while `Object.getPrototypeOf(async function*(){})` carries neither `constructor` nor `%Symbol.toStringTag%` and `AsyncGeneratorFunction.prototype` is minted by **nobody**, every child having retired on the stated walk — the one-directional silent failure §5's head records. Which child allocates it is a mandatory output of this umbrella's derivation. ⚠ **And §27.7.1.1's *call* behaviour is not this umbrella's to build — routing that edge is a mandatory output of the same derivation.** `AsyncGeneratorFunction ( ...paramArgs, bodyArg )` is three steps ending in `Return ? CreateDynamicFunction(activeFunc, NewTarget, async-generator, paramArgs, bodyArg)` (`webref body ecma262 sec-asyncgeneratorfunction`), and **§20.2.1.1.1** CreateDynamicFunction takes `kind` as *normal, generator, async, or async-generator* — its fifth step opens "Assert: kind is async-generator" and takes `fallbackProto` to be `"%AsyncGeneratorFunction.prototype%"` (`webref aoid ecma262 CreateDynamicFunction`; steps from `webref body ecma262 sec-createdynamicfunction`). So the constructor is a `kind` argument to the dynamic-code algorithm the `Function`/`eval` row at the foot of this table owns, not a second mechanism, and the child that owns §27.7.1.1 carries an edge on **the child `#11-vm-function-constructor-global`'s derivation mints for CreateDynamicFunction's `kind` argument**, via that slot's derivation, which mints it — the slot that row points at. §27.7.2.1 `AsyncGeneratorFunction.prototype` and the §27.7.3 members are reachable with the constructor still uncallable, so the edge is that one child's rather than the whole of §27.7's. ⚠ **The traffic also runs the other way and the same derivation owes that half**: CreateDynamicFunction's async-generator arm reads `%AsyncGeneratorPrototype%` — `Let protoProto be OrdinaryObjectCreate(%AsyncGeneratorPrototype%)` in the clause's `Else if kind is async-generator` arm, same `body` output — which is the intrinsic 6a's object-and-construction prerequisite mints, so the dynamic-code side consumes *this* umbrella rather than the reverse and the two edges are not a cycle between the same pair of children. **The measurement, taken over those three: each is red while the other two are green.** (i) **§27.1.5.1 CreateAsyncFromSyncIterator** — `for await (x of [1,2])` cannot adapt a sync iterable though async-generator objects and the async body evaluation are both complete. (ii) **The async form of ForIn/OfBodyEvaluation** — the `for-of` lowering takes no `is_await` (6b's `is_await: _` discard, the evidence block above), so `for await` awaits nothing even with the adapter built and §27.9 complete. (iii) **§27.9 AsyncGenerator Objects** — the request queue and its settlement are unbuilt while `for await` over a sync iterable adapts and awaits correctly. `two call entry points rather than one` is breadth inside (iii) and is not counted as a fourth. **§27.7.3 is a further axis this cell already states red-while-green**: a child could ship async-generator objects and their request protocol while `Object.getPrototypeOf(async function*(){})` carries neither `constructor` nor `%Symbol.toStringTag%` — which is why allocating it is a mandatory output of this umbrella's derivation. The second disjunct is unavailable: the cell re-derives §27.9 with `webref heading ecma262 27.9` and §27.1.5.1 with `webref aoid ecma262 CreateAsyncFromSyncIterator`, so the AOs are in the specification. | — | **new** `#11-vm-async-generators` (umbrella) | T2 | — |
| **6c** | **`%AsyncIteratorPrototype%`** — the shared intrinsic, and nothing else. **It exists because the 6a and 6b work both inherits from it and neither side can be told to consume the other's copy**: the object-and-construction prerequisite that umbrella 6a's derivation must mint requires `%AsyncGeneratorPrototype%` to inherit it (that row), and ECMA-262 **§27.1.5.2** puts `%AsyncFromSyncIteratorPrototype%` on the same intrinsic, which is 6b's adapter. Measured at `658cc302`: `git grep -l -E -e 'AsyncIteratorPrototype' -e 'async_iterator_prototype' -- crates/script/elidex-js/src` → **0** files, against the control `git grep -c 'asyncIterator' -- vm/well_known.rs` → **1** (the symbol id exists, the prototype does not) — so whichever consumer landed first would have created a private copy the other was under no obligation to reuse. The intrinsic exists; `webref heading ecma262 27.1.4` returns **two** members on it — **§27.1.4.2** `%Symbol.asyncIterator%` returning `this`, and **§27.1.4.1** `%Symbol.asyncDispose%`, whose id is owned by the child of umbrella **R** that produces it. ⚠ **The owner is settled here rather than deferred, because deferring it settles nothing — the defect row K was minted to fix** ("whichever runs first" orders nobody), and the previous wording left this row's prose promising a `Deps` edge while its `Deps` cell stayed `—`, which is prose contradicting the structured field it points at. **§27.1.4.1 is not this row's**: it is owned by the child of umbrella **R** that produces the `%Symbol.asyncDispose%` id, and placing `Deps: 6c` on that child is a mandatory output of umbrella **R**'s derivation, which R's own row now records — ⚠ **"that child carries `Deps: 6c`" stood here in the present tense about a child no derivation has minted, so the edge lived only in this prose** — it installs onto the intrinsic this row delivers. The direction is forced rather than chosen. This row is a **prerequisite** that 6b and whichever of umbrella 6a's children owns the object and its prototype chain wait on, while **R** is promoted outside this plan's sequence, so an edge the other way would park a prerequisite of the 6 family behind out-of-sequence work; and the member cannot be written before the id exists, whereas the id can be written before the member. So this row takes no edge **on R** — its `Deps` names the allocator prerequisite alone — and it retires carrying §27.1.4.2 alone — **which is all it carries**, the second member being named here so that it is owned rather than silently dropped — ⚠ **with the observation path named, because §5 requires an *observable* result and nothing reaches this intrinsic from JavaScript before a consumer lands**: it is asserted from Rust against **what the algorithm returns**, not against the slot being populated: the `vm/tests/` harness reads the intrinsic directly (as the GC tests read `vm.inner`), gets its `%Symbol.asyncIterator%` property, calls it with an arbitrary receiver and asserts that receiver comes back — the whole of ECMA-262 **§27.1.4.2**, whose step 1 is "Return the this value" (`webref body ecma262 'sec-%asynciteratorprototype%-%symbol.asynciterator%'`). Asserting only that the slot is non-empty passes on a bare `Object.create(null)` and is the name-resolves shape §5's head forbids. ⚠ **And the receiver assertion above and the `name` read below *also* pass on a bare `Object.create(null)` carrying that one member, because neither reads the intrinsic's own prototype.** §27.1.4's bullet list is two items — "has a [[Prototype]] internal slot whose value is %Object.prototype%" and "is an ordinary object" (`webref body ecma262 sec-asynciteratorprototype`) — and the first is a contract of *this* row rather than of the consumers that inherit from it: every object inheriting %AsyncIteratorPrototype% reaches `Object.prototype` through it, so a null-prototype intrinsic silently strips `toString` and `hasOwnProperty` from every async iterator while the receiver assertion stays green. Required alongside it, read from Rust: the intrinsic's `[[Prototype]]` is the realm's `%Object.prototype%`. ⚠ **LOW: the clause states one thing beside step 1 and the acceptance stops at step 1.** §27.1.4.2's closing sentence is "The value of the \"name\" property of this function is \"[Symbol.asyncIterator]\"" (`webref body ecma262 'sec-%asynciteratorprototype%-%symbol.asynciterator%'`), which an anonymous closure installed under the symbol key satisfies the receiver assertion without. Row **8d** already reads its five ids' `name` and descriptor through `Object.getOwnPropertyDescriptor` for the same reason; the same read applies here, from Rust. ⚠ **And `name` is one half of what this function's creation path defines — `length` is the other, and the allocator this row installs through defines neither it nor anything else beyond `name`.** §27.1.4.2's heading is `%AsyncIteratorPrototype% [ %Symbol.asyncIterator% ] ( )` — an empty parameter list, so its length is **0**; §10.3.4 CreateBuiltinFunction closes with "Each built-in function defined in this specification is created by calling the CreateBuiltinFunction abstract operation" and its **step 12** performs `SetFunctionLength(func, length)`; and §10.2.10 SetFunctionLength **step 2** defines an **own** `"length"` data property `{[[Writable]]: false, [[Enumerable]]: false, [[Configurable]]: true}`. ⚠ **Both §-numbers are the `aoid` output and not the ones this round's report carried**, which named §10.3.3 for CreateBuiltinFunction: `webref aoid ecma262 CreateBuiltinFunction` gives **§10.3.4** and `webref aoid ecma262 SetFunctionLength` **§10.2.10** (steps from `webref body ecma262 sec-createbuiltinfunction` / `sec-setfunctionlength`, heading from `sec-%asynciteratorprototype%-%symbol.asynciterator%`). In-tree the allocator defines the `name` key and returns — `create_native_function_keyed` (`vm/shape_ops.rs:511-526` at `658cc302`) — and the absence is measured rather than read off that one window: `git grep -c 'well_known.length' 658cc302 -- crates/script/elidex-js/src/vm/shape_ops.rs` returns **no line** against the control `'well_known.name'` → **1** on the same file and rev, so the grep discriminates. So a **fourth** Rust-side read, the same `Object.getOwnPropertyDescriptor` shape as the `name` one: `length` is an **own** property of value `0` with `{writable: false, enumerable: false, configurable: true}`. ⚠ **The descriptor is defined by the allocator, not at this row's own installation site — the local install is withdrawn.** `CallShape` is `Ordinary` / `CallableOnly` / `ConstructorOnly` / `IllegalConstructor` (`vm/value.rs:728-733`) and carries no arity, so supplying one is a signature change on `create_native_function_keyed` and on the entry points that reach it; that change is **`#11-vm-native-function-length-on-allocator`**'s deliverable (§8, ordered ahead of this row and named in this row's `Deps`), not a reason to route one built-in around the allocator. ⚠ **The withdrawn alternative was a mechanical constraint driving a design outcome**: defining the pair beside the member is what `Function.prototype.bind`'s result does (`vm/natives_function.rs:124-138`) and it would have been reachable without adding a §8 row, but taking it would leave §10.3.4's `length` on a special path for this one built-in while the allocator kept omitting it for every other — the parallel-mechanism state CLAUDE.md's *One issue, one way* forbids, and a pipe-parity constraint on this document is not an argument about the engine. ⚠ **So a green 6c *is* a repaired allocator, and this row asserts only its own half of it**: the remaining sweeps are still neither §10.2.10's ordinary-function one nor an intrinsic-property one — `#11-vm-ordinary-function-name-length`'s cell scopes itself to the closure objects **§10.2.3** OrdinaryFunctionCreate step 22 creates in `vm/ops.rs`, and `#11-vm-builtin-prototype-static-sweep`'s to properties defined *on an intrinsic object*, so §10.3.4's path falls between them and is the prerequisite's, registered at landing with the §8 rows. The inheritance edge and its regression belong to each consumer — `%AsyncGeneratorPrototype%` to whichever of umbrella 6a's children owns the object and its prototype chain, `%AsyncFromSyncIteratorPrototype%` to **6b** | `vm/globals_async.rs` | `#11-vm-async-generators` (6c) | T2 | **`#11-vm-native-function-length-on-allocator`** (§8 — the fourth Rust-side read above is the `length` descriptor §10.3.4 defines through §10.2.10, and that descriptor is the prerequisite's deliverable on the very allocator this row installs through) |
| **6a** | **Async-generator objects — UMBRELLA, not a terminal unit.** ⚠ **This row was terminal, and its own acceptance sentence — "Promise-returning `next` / `return` / `throw`, a request queue they enqueue on, and settlement driven from yield / await / return" — enumerates mechanisms that fail independently.** It spans **five** intersecting axes: the distinct construction path in **both** call entry points (evidence block above); a new object and prototype representation (the `ObjectKind`, and `%AsyncGeneratorPrototype%` — **§27.9.1** — inheriting `%AsyncIteratorPrototype%` — **§27.1.4**); suspension state carried across the interpreter and the bytecode; the Promise-returning members **§27.9.1.2** `.next`, **§27.9.1.3** `.return` and **§27.9.1.4** `.throw`; and the request queue with its yield/await/return settlement — **§27.9.3.1** AsyncGeneratorRequest Records, **§27.9.3.4** AsyncGeneratorEnqueue, **§27.9.3.5** AsyncGeneratorCompleteStep, **§27.9.3.6** AsyncGeneratorResume, **§27.9.3.8** AsyncGeneratorYield and **§27.9.3.10** AsyncGeneratorDrainQueue (inventory by command, not recall: `webref heading ecma262 27.9` returns §27.9.1.1-§27.9.1.5 under *The %AsyncGeneratorPrototype% Object* and §27.9.3.1-§27.9.3.10 under *AsyncGenerator Abstract Operations*, and `webref heading ecma262 27.1` gives §27.1.4 *The %AsyncIteratorPrototype% Object*). ⚠ **They fail independently, and the discriminating case sits inside this row's own acceptance**: the prototype and construction regressions — `g[Symbol.asyncIterator]() === g`, `ag().next() instanceof Promise` — can every one pass while concurrent `.next()` calls still resume immediately and settle out of order, because what forbids that is **§27.9.1.2** step 9's guard (resume *only* when the state is suspended-start or suspended-yield, step 10 asserting the alternative is executing or draining-queue) acting on the step-8 `AsyncGeneratorEnqueue` — and nothing in the object's shape or its prototype chain observes it. ⇒ umbrella. **Three things are mandatory outputs of this umbrella's derivation.** **(1) A Rust-testable object-and-construction prerequisite**: the `ObjectKind` and the prototype chain have no JS-reachable holder until a member exists, so under §5's **prerequisite** row-kind its acceptance is bounded to what its own artifact buys at the point it lands and is asserted from Rust against the representation — exactly as **6c**'s intrinsic is and as the child umbrella **C**'s derivation mints for the binding-cell representation is — rather than through a JS expression, which would pull a consumer's exposure work in. **(2)** a request-queue child and **(3)** an execution-and-settlement child, kept apart because the queue is a structure the members enqueue on while settlement is driven from the suspension points, and the §27.9.1.2 guard above is the thing that a child owning only one of the two cannot assert. ⚠ **This row mints none of them and invents no ids**: which children, and how the five axes divide between them, is that derivation's output, and an axis absent from any list in this cell is **un-minted, not out of scope**. **The `6c` and `9da` edges are the derivation's to place, on the children that consume them rather than on this umbrella**: 6c's `%AsyncIteratorPrototype%` is consumed by the object-and-construction prerequisite — the inheritance edge 6c's own row assigns to "each consumer" — while the resolving-functions closure the child umbrella **9da**'s derivation mints for **§27.5.1.3** CreateResolvingFunctions is consumed by whichever child owns `.return`, which the prerequisite never reaches. Everything below is evidence for the derivation to route, not a charter. ⚠ **Recorded because the request protocol alone does not reach it**: the object must also sit on the async-iterator prototype chain — `%AsyncGeneratorPrototype%` inheriting `%AsyncIteratorPrototype%` (ECMA-262 §27.9.1 / §27.1.4), with `g[Symbol.asyncIterator]() === g` — or `for await` cannot consume an async generator through the protocol at all while every request-protocol assertion passes. **What was this row's acceptance condition — the request protocol, not a callable member — is now evidence the derivation attaches to the child that owns it** (rewritten at PR-B; "so `ag().next` becomes callable" is a member test, and routing async generators through the existing synchronous generator path satisfies it). ECMA-262 **§27.9.1.2** `%AsyncGeneratorPrototype%.next` creates a promise capability at step 2, performs **§27.9.3.4** `AsyncGeneratorEnqueue` at step 8, resumes at step 9 *only* when the state is suspended-start or suspended-yield — step 10 asserting the alternative is executing or draining-queue — and returns `promiseCapability.[[Promise]]` at step 11; so a call arriving while an earlier one is in flight is queued rather than run, and `.return` (**§27.9.1.3**) and `.throw` (**§27.9.1.4**) enqueue against that same queue. In-tree at `658cc302` there is no queue to enqueue against and no promise to return: `grep -rl 'AsyncGeneratorEnqueue' crates/script/elidex-js/src` → **0** files, and `native_generator_next` (`vm/natives_generator.rs:356`) resumes at `:374` and returns `create_iter_result` directly at `:375-376` — a synchronous resume handing back an iterator-result object, which is exactly the shape a member test accepts. The obligations, for the derivation to distribute rather than for this row to assert: Promise-returning `next` / `return` / `throw`, a request queue they enqueue on, and settlement driven from yield / await / return — with the regressions attaching the same way, `ag().next() instanceof Promise` to the child owning the members and *two `.next()` calls issued with no `await` between them settling in call order* to the request-queue and settlement children, which is precisely the pair the object-and-construction prerequisite cannot assert. **The artifact is the resolving-functions closure the child umbrella `9da`'s derivation mints, rather than that umbrella's member list**: **§27.9.1.3** `%AsyncGeneratorPrototype%.return` step 8.b performs **§27.9.3.9** AsyncGeneratorAwaitReturn, whose step 7 is `Let promiseCompletion be Completion(PromiseResolve(%Promise%, completion.[[Value]]))` — the same **§27.5.4.7.1** → **§27.5.1.3** chain 6b's cell traces, so `ag().return(thenable)` must adopt the thenable before the queue's completion step runs (§-numbers from `webref aoid ecma262 AsyncGeneratorAwaitReturn` / `PromiseResolve`; steps from `webref body ecma262 sec-asyncgenerator-prototype-return` / `sec-asyncgeneratorawaitreturn`). **Not intra-family**: the request queue and the promise its members return are this umbrella's own, while step 7 consumes thenable assimilation inside `settle_promise`, which 9da's row measures as absent — so a child landing `.return` ahead of the child umbrella **9da**'s derivation mints for the resolving-functions closure writes it against that gap | — | `#11-vm-async-generators` (6a umbrella) | T2 | — |
| **6b** | **`for await (x of …)` lowering** — the `is_await: _` discard and the `for-of` lowering that takes no `is_await` (evidence block above). **Does not depend on 6a**, though the Deps cell named it until PR-B, and it consumes nothing 6a produces: ECMA-262 **§7.4.4** `GetIterator(obj, async)` step 1.b routes an object with no `%Symbol.asyncIterator%` through `GetIteratorFromMethod(obj, syncMethod)` (step 1.b.iii) and `CreateAsyncFromSyncIterator(syncIteratorRecord)` (step 1.b.iv) — `webref body ecma262 GetIterator` — so `for await` over a **sync** iterable is **§27.1.5.1**'s adapter and constructs no async-generator object at all. **Coordination with the child of umbrella 6a that owns the `is_await` lowering, not ordering**: whichever lands second must not re-derive the other's `is_await` lowering. **Its Deps edges each name the artifact they consume — the count and the list live in the cell, not here — and the adapter-clause ones are on the clause the paragraph above already reaches.** **The child umbrella 9da's derivation mints for the resolving-functions closure** — **§27.1.5.4** AsyncFromSyncIteratorContinuation step 6 is `Let valueWrapper be Completion(PromiseResolve(%Promise%, value))`, and **§27.5.4.7.1** PromiseResolve step 2 is `Let promiseCapability be ? NewPromiseCapability(ctor)` with step 3 `Perform ? Call(promiseCapability.[[Resolve]], undefined, « resolution »)` — so every value a `for await` over a **sync** iterable yields passes through the resolving-functions closure **§27.5.1.3** CreateResolvingFunctions delivers, which is that child's named deliverable (§-numbers from `webref aoid ecma262 AsyncFromSyncIteratorContinuation` / `PromiseResolve` / `CreateResolvingFunctions` — ⚠ re-derive rather than recall, the number carried into this round for the first of them was wrong; steps from `webref body ecma262 sec-asyncfromsynciteratorcontinuation` / `sec-promise-resolve`). Measured consequence of landing on today's `settle_promise`, which adopts only `ObjectKind::Promise` (9da's row): with the read taken in a **second** `eval` on the same `Vm`, after the end-of-eval microtask checkpoint, `globalThis.out = undefined; Promise.resolve({ then(r) { r(7) } }).then(v => { globalThis.out = v });` then `typeof (globalThis.out && globalThis.out.then)` gives **`"function"`** — the thenable object itself is bound, `String(globalThis.out)` being `"[object Object]"` — against the control `Promise.resolve(Promise.resolve(7))`, which gives **`"7"`**. **Pa** — the same clause closes the sync iterator on an abrupt completion at two points, step 7.1 `Set valueWrapper to Completion(IteratorClose(syncIteratorRecord, valueWrapper))` when the wrapper is abrupt and step 13.1.1 `Return ? IteratorClose(syncIteratorRecord, ThrowCompletion(error))` inside the `onRejected` closure, so this drain consumes Pa's completion-kind parameter exactly as the drains owned by the children of umbrella 0bc and umbrella 9c that consume it do. ⚠ **And Pa is not the close this row's own governing clause reaches on the async arm, which no edge recorded — the two are easy to conflate because both are named "close" and both are abrupt-exit paths of the same AO.** **§14.7.5.7** ForIn/OfBodyEvaluation branches on `iteratorKind` at each of its two abrupt exits, and the async branch is a *different* AO: step 8.9.5 is "If iteratorKind is async, return ? AsyncIteratorClose(iteratorRecord, status)" on the abrupt-`status` exit and step 8.13.4 is the same call on the `LoopContinues` exit, each sitting immediately above the "Return ? IteratorClose(iteratorRecord, status)" the sync lowering takes at 8.9.6 and 8.13.5 — the two steps Slice Pd's cell cites (§-number and title from `webref aoid ecma262 ForIn/OfBodyEvaluation`; steps from `webref body ecma262 sec-runtime-semantics-forin-div-ofbodyevaluation-lhs-stmt-iterator-lhskind-labelset`; the decimal step ids are the extractor's rendering of the spec's 8.i.v / 8.m.iv). So `for await`'s abrupt exits consume **§7.4.15** AsyncIteratorClose, which umbrella `P`'s row already records as inside its charter and un-minted rather than out of scope — and lowering them onto `iter_close` instead open-codes a second close path that §7.4.15 step 4.d's `Await` makes semantically different, the strangler intermediate CLAUDE.md *One issue, one way* forbids. The producer is therefore that umbrella's derivation together with the child it mints, which is what the Deps cell records. ⚠ **And this row states no acceptance at all — everything above is dependency derivation, and the exits of the row's own lowering go unasserted.** §7.4.4's two branches and §14.7.5.7's async abrupt exit give one assertion each, none of which the others can fail on: **(1) the adapter path** — `for await` over a plain sync iterable of non-promise values yields those values in order, step 1.b routing it through **§27.1.5.1** `CreateAsyncFromSyncIterator`; **(2) resolution on that path** — the same loop over a sync iterable *of promises* binds the **resolved** values, not the promise objects, which is **§27.1.5.4** step 6's `PromiseResolve(%Promise%, value)` and is what a lowering that reuses the sync `for-of` arm unchanged fails while passing (1); **(3) the async-first branch** — ⚠ **which separates *which branch is taken* and not what that branch does with the value, so nothing here witnesses the async arm's value semantics**: routing every yielded value through `PromiseResolve`/await, the cheapest way to satisfy the adapter path, passes every stated condition. **Fourth red probe at `658cc302`**: §14.7.5.7 step 8.b awaits `nextResult` and step 8.f is `IteratorValue(nextResult)` with **no second await**, so `for await (const v of it)` over an async iterator whose result is `{ value: Promise.resolve(7), done: false }` must bind the **promise object**, not `7`. ⚠ **And condition (3)'s witness is spelled as an object "carrying" `%Symbol.asyncIterator%`, one case short of §7.4.4 step 1.b**, which fires when `GetMethod` returns **undefined** — so a present-but-`null` method must take the sync-adapter fallback too — an object carrying `%Symbol.asyncIterator%` is driven through that method and **not** the adapter, §7.4.4 step 1.a taking precedence over 1.b, witnessed by an object whose `%Symbol.asyncIterator%` and `%Symbol.iterator%` yield *different* values so that taking the wrong branch is observable; and **(4) the abrupt exit** — `break` out of a `for await` over a sync iterable whose `return()` records the call must call it, which is the **§7.4.15** AsyncIteratorClose edge the `Deps` cell routes to umbrella `P`'s child, so this assertion lands with that child rather than ahead of it. ⚠ **That observes only that `return()` was called, which a lowering that calls it and continues synchronously passes while omitting what §7.4.15 is *for*.** Its step 4.c is `Call(return, iterator)` and step **4.d** is "If innerResult is a normal completion, set innerResult to Completion(**Await**(innerResult.[[Value]]))"; step 5 returns early only when `completion` is a **throw** completion and a `break` is not one, so step **6** — "If innerResult is a throw completion, return ? innerResult" — is reached and an awaited **rejection** propagates out of the loop (`webref aoid ecma262 AsyncIteratorClose` → **§7.4.15**; steps from `webref body ecma262 sec-asynciteratorclose`). Two more, both over an object carrying `%Symbol.asyncIterator%` rather than the sync iterable above, so that the `Await` under test is §7.4.15's own: over a **sync** iterable the pending value would have to be threaded through **§27.1.5.2.2** `%AsyncFromSyncIteratorPrototype%.return` step 13 into AsyncFromSyncIteratorContinuation's `PromiseResolve(%Promise%, value)`, and the witness would then measure the adapter that cases (1) and (2) already own (`webref body ecma262 'sec-%asyncfromsynciteratorprototype%.return'` — **§27.1.5.2.2**, not the §27.1.5.3.2 the sibling *Instances* heading invites). **(4b) ordering** — the iterator's `return()` answers `Promise.resolve().then(() => (log.push('await'), { done: true }))`, and `(async () => { for await (const v of it) break; log.push('after') })()` must leave `log` as `return,await,after`, where a lowering that closes synchronously leaves `return,after,await`. **(4c) rejection** — `return()` answers `Promise.reject(new Error('x'))` and the async function's promise must **reject** with that same error, where the synchronous close fulfils it; this is the half (4b) cannot see, since an implementation may await and still discard the rejection at step 6. ⚠ **(4b)'s resolved value is an object on purpose and not decoration**: §7.4.15 **step 7** is "If innerResult.[[Value]] is not an Object, throw a TypeError exception", read *after* the Await, so a `return()` resolving to a primitive throws a TypeError and the ordering witness would go red on the wrong axis. ⚠ **And both carry the observation point 9da's row states, for the reason it states**: `log` and the settlement are unread at the end of synchronous script evaluation on a conformant engine as much as on this one, so the regression evaluates the script, drains the job queue to quiescence from the Rust harness, and reads only after that drain — never through a trailing `.then`, which is the checkpoint under test. ⚠ **One obligation is added here out of Slice 6c's cell, which was its only home**: ECMA-262 **§27.1.5.2** puts `%AsyncFromSyncIteratorPrototype%` — this row's adapter — on `%AsyncIteratorPrototype%`, the intrinsic 6c delivers and this row's `Deps` already names, so the inheritance is **this row's** to install and to assert, not 6c's to describe: a regression that the adapter's `[[Prototype]]` is that intrinsic, ⚠ **read through it rather than as an identity on the slot** — the adapter's `%Symbol.asyncIterator%` resolves and returns its receiver on a private copy exactly as on the shared intrinsic, so the behavioural read separates nothing: measured, `const proto = { [Symbol.asyncIterator](){ return this } }; const o = Object.create(proto); o[Symbol.asyncIterator]() === o` is `true` here and under Node alike while `Object.getPrototypeOf(o)` is not the intrinsic. **§27.1.5.2** further says the adapter "is never directly accessible to ECMAScript code" (`webref body ecma262 sec-%asyncfromsynciteratorprototype%-object`), so the assertion has no JS spelling at all and is owed as a Rust-side identity read of the adapter's `[[Prototype]]` against the realm's `%AsyncIteratorPrototype%` | `compiler/stmt.rs`, `compiler/stmt_loop.rs`, `vm/dispatch_iter.rs` | `#11-vm-async-generators` (6b) | T2 | **the child umbrella 9da's derivation mints for the resolving-functions closure**, via that derivation, which mints it — naming 9da itself would name nobody, **Pa**, **6c**, **the child of umbrella `P` that owns ECMA-262 §7.4.15 AsyncIteratorClose**, via that umbrella's derivation, which mints it |
| **O** | **Object-spread source coercion — SCHEDULED FROM ITS OWN SLOT, outside this plan's slice sequence** (§1.0). `op_spread_object` copies only when source **and** destination are both `JsValue::Object` (`vm/dispatch_objects.rs:138` at `658cc302`; `:137` is the `let obj_val = self.peek()?` above it), so `{...'ab'}` and `const {...r} = 'ab'` produce empty objects, against ECMA-262 **§7.3.25** CopyDataProperties. A live defect in a **connected** handler: §1.0's derivation did not produce it, §2.2 records the measurement and the §8 row carries the triple, so this row is a pointer rather than a slice | — | `#11-vm-object-spread-source-coercion` | T1 | — |
| **C** | **The runtime declarative environment, and the representation closure capture reads it through — UMBRELLA, not a terminal unit.** ⚠ **This row read as terminal while declining, in as many words, to say what its own representation *is* — a charter plus a derivation is what an umbrella carries, not what a slice does.** Four intersecting axes: environment-record creation and initialization across three distinct AOs (**§14.2.2** step 2, **§14.7.4.4** step 1.d, **§14.15.2** steps 2 and 3.a); the compiler resolution and capture representation, which replaces the compile-time `(scope_idx, name)` allocation and the `upvalue_map` dedup; **GC liveness** — a new marking site, and by this cell's own text possibly a new `ObjectKind` variant, which §4 I-9 makes a per-variant question; and the binding **state machine** including TDZ (**§9.1.1.1.6**), which this cell proves `UpvalueState::{Open, Closed}` cannot express. ⚠ **And its two stated conditions are Rust-side reads of the representation alone, so neither separates a representation-only landing from one that performs the closure-capture integration this row's own module cell claims** — `compiler/resolve.rs` can go untouched and both still pass. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. **Row E's kind is undetermined, so it and its sub-slices carry no `Deps` edge; re-pointing is an output of umbrella C's derivation only once E resolves.** The charter it routes. ⚠ **Carved at PR-B as a §8 *slot*, and that was wrong on §5's own reading of what a slot can own.** `#11-vm-inbound-host-call-membership-test` and the typed-array layering gate are slots because what each owns is a **decision** and it produces no artifact; this owns the runtime binding-cell representation *and its closure-capture integration*, which is **code**. A slot ships no PR, so with nothing producing the representation **the child umbrella B's derivation mints for the per-evaluation emit** and **Ea** can each still build one — the two-mechanism outcome the carve existed to prevent and the one CLAUDE.md *One issue, one way* forbids. **What both consumers need is the same runtime object.** B owes a per-entry environment for a block's lexical bindings: ECMA-262 **§14.2.2** step 2 is "Let blockEnv be NewDeclarativeEnvironment(oldEnv)" and it is part of the Block's *evaluation*, so a block evaluated twice has two (`webref body ecma262 sec-block-runtime-semantics-evaluation`). Ea owes one of the same kind: **§14.7.4.4** CreatePerIterationEnvironment step 1.d is "Let thisIterationEnv be NewDeclarativeEnvironment(outer)" and steps 1.e.ii-1.e.iii read the previous iteration's value and initialize this iteration's binding with it (`webref aoid ecma262 CreatePerIterationEnvironment`; steps from `webref body ecma262 sec-createperiterationenvironment`). In-tree there is no runtime environment at all and the allocation is compile-time throughout: `FunctionScope::locals` is a `HashMap<(usize, Atom), LocalInfo>` with one slot per `(scope_idx, name)`, and `add_upvalue` deduplicates captures by that same key through `upvalue_map` (`compiler/resolve.rs:31` and `:148-157`; B's row measures both at `b5356098`). Each consumer's own regression is blind to the other's — the one placed on B's per-evaluation-emit child constructs no loop head and Ea's constructs no body block — and the case that discriminates them is `for (let i=0;i<2;i++) { let x=i; fs.push(() => [i,x]) }`, where `i` is the head's per-iteration binding and `x` the body block's per-entry one and one closure must observe both. ⚠ **A third reader, found by walking the AO rather than the two consumer rows.** ECMA-262 **§14.15.2** *Runtime Semantics: CatchClauseEvaluation* creates one of the same kind: step 2 is "Let catchEnv be NewDeclarativeEnvironment(oldEnv)", step 3.a performs "catchEnv.CreateMutableBinding(argName, false)" for each BoundName of the CatchParameter, and step 5 is "Let status be Completion(BindingInitialization of CatchParameter with arguments thrownValue and catchEnv)" — the very step Slice **0bc**'s cell cites for the destructured catch parameter, so what 0bc's lowering initializes into is the record this row represents (§-number and title from `webref aoid ecma262 CatchClauseEvaluation`; steps from `webref body ecma262 sec-runtime-semantics-catchclauseevaluation`). **It is not Slice B's**: B's charter is §14.2.3 BlockDeclarationInstantiation, which §14.2.2 and §14.12.4 invoke and §14.15.2 does not — the catch clause creates its record and binds into it directly, and only the catch *body* reaches B, through step 7's "Evaluation of Block". So the representation this row owns has a third site, named here so that nothing builds a second one for it. ⚠ **And the readers were being found one at a time — a §5 row here, an AO walk there, a §8 slot in the round after — which is the signature this document names throughout, so §8's slots were *swept* rather than read.** The sweep is by concept rather than by one string, per the semantic-sibling rule: `grep -nE '^[\|] .#11-' <this file>` piped through `grep -ci -E -e NewDeclarativeEnvironment -e NewFunctionEnvironment -e 'Environment Record' -e environment -e upvalue -e closure -e capture` returns **15** of the **51** slot rows the first command emits, against the negative control `grep -icE 'Ephemeron'` on the same input → **0**. ⚠ **Both figures are re-derived in every edit that adds a slot row or a matching term, and both had gone stale before this one**: the match count moved 13 → 14 at `b9d014e9` and 14 → 15 at `99553562` without either commit re-running the sweep, and the row count moved 47 → 48 when the acceptance-falsifiability slot landed, 48 → 49 when `#11-vm-native-function-length-on-allocator` was minted at one revision and 49 → 50 when `#11-vm-structured-clone-layering-seam` was minted at the next — ⚠ *"at the same one" stood here and is false; the two mints are two revisions apart*, and 50 → 51 when `#11-vm-float16array-global` was minted at this one — which moved the denominator and **not** the numerator, the new row using none of the seven filter terms. Re-run is `grep -cE '^[\|] .#11-' <this file>` for the denominator and that command piped through the `grep -ciE` above for the numerator. ⚠ **That makes it a seed, not the inventory** (§4 I-7 states the general form): it selects slots that already use environment vocabulary, so a slot whose *deliverable* is a declarative environment record while its cell never names one is invisible to it, which is the class this sweep exists to catch. The predicate is what binds — *is this slot's deliverable a declarative environment record, or a capture through one?* — applied to all **51** at this row's own plan-review, with the 15 as its starting point; and any later re-run of the filter carries an out-of-vocabulary positive control before its count is quoted. So the filter discriminates rather than matching every row (re-run rather than read the figures forward). Its output is then classified against this row's charter, and the classification is a **complement, not a list of consumers**: a slot consumes this row when its own *deliverable* is a declarative environment record or a capture through one — `#11-vm-upvalue-tdz-check` and `#11-vm-named-function-expression-binding`, each of which now says so in its own cell, together with the child `#11-vm-parameter-environment-instantiation`'s derivation mints for the parameter environment record — and otherwise it does not, for the reason already stated for 0bc above: the cited AO mentions a record while the deliverable is elsewhere. Every other row the filter returns is of that second kind. `#11-vm-block-declaration-instantiation` and `#11-vm-per-iteration-loop-environment` are pointers to Slices **B** and **Ea**; **Ea** carries no edge — row E's kind is undetermined, and **B** is an umbrella from this revision on, so its `Deps` cell reads `—` and placing this edge on the child its derivation mints for the per-evaluation emit is a mandatory output of that derivation; the child of `#11-vm-derived-constructor-this-binding` that owns the `[[ThisBindingStatus]]` representation delivers it on the **call frame**, not a binding cell; `#11-vm-const-assignment-runtime-throw` delivers the substitution of a runtime throw for a `CompileError` at one compiler arm; `#11-vm-private-name-early-errors` is over the **Private** Environment, a different record kind (**§9.2**); `#11-vm-arrow-lexical-new-target` and `#11-vm-arrow-lexical-super` put a field on `FunctionObject` beside the `captured_this` that already exists; `#11-vm-ordinary-function-name-length` matched on the name of `create_closure` and nothing else; and Slice **K** reorders an existing `Op::InitLocal` / `Op::SetLocal` emit against the same compile-time allocation measured above. **Recording the sweep and the complement here, rather than the consumers, is what stops the next environment-shaped slot from being written outside it** — a list of consumers has to be extended per slot and leaves every class it does not name outside this row by default. ⚠ **This places no `Deps` edge on any child umbrella 0bc's derivation mints, and the reason is §5's own bound on a prerequisite row's reach**: the deliverable of the child umbrella 0bc's derivation mints for the catch-head pattern is the `PatternKind` lowering the catch head is missing, and its acceptance is that a destructured catch parameter binds its names, which turns true against the compile-time `(scope_idx, name)` allocation measured above — the same allocation `catch (x)` already uses — so an edge would order that child behind an artifact its deliverable does not consume. **An umbrella row under §5's row-kind rule, so it states no acceptance and never lands; the bound below is what its derivation places on the child it mints for the representation, whose acceptance is bounded to what that artifact buys at the point *it* lands** — ⚠ *this cell declared itself a **prerequisite** row while its own `Slice` cell already carried the umbrella marker*: nothing in JavaScript reaches the representation until a consumer instantiates it, so it is asserted from Rust against the representation itself — the `vm/tests/` harness reading it directly, as **6c**'s intrinsic is asserted and as the GC tests read `vm.inner` — that two entries of one `(scope_idx, name)` are distinct bindings and that a capture records which entry it captured. Demanding a JS path here would pull a consumer's lowering work in. ⚠ **And that assertion passes under a no-op trace arm, so I-7 binds this row too**: the record is heap-reachable the moment a closure references it, and whichever shape the plan-review settles on — an `ObjectKind` variant, or a field beside the `captured_this` already on `FunctionObject` — forces a marking site. The liveness of this row's own artifact is this row's obligation and not its consumers', so the Rust assertion is stated **across a forced collection, with the capture as the record's sole root** (I-7's second half): two entries of one `(scope_idx, name)`, a capture recorded against one of them, every other handle to the record dropped, collected, then read back — a harness that keeps its own handle passes under a no-op arm and measures nothing. ⚠ **And distinctness-plus-capture is satisfied by a **two**-state representation, while all three cited AOs create a binding in a state neither of those two can express.** §14.2.3 step 3.a binds every declaration (`CreateImmutableBinding` / `CreateMutableBinding`) while step 3.b.iv initializes only the function-shaped ones; §14.15.2 step 3.a is `catchEnv.CreateMutableBinding(argName, false)` with initialization deferred to step 5's BindingInitialization; §14.7.4.4 step 1.e.i creates before step 1.e.iii initializes; and **§9.1.1.1.6** GetBindingValue step 2 throws a ReferenceError for an uninitialized binding "regardless of the value of strict" (`webref body ecma262 sec-declarative-environment-records-getbindingvalue-n-s`). Two entries of one `(scope_idx, name)` can be distinct, and a capture can record which one it took, in exactly the `{ Open, Closed }` pair `UpvalueState` already is — which is why `#11-vm-upvalue-tdz-check`, a consumer this row's own complement classifies, then has no third state to add. So the Rust assertion covers it: an entry read *before* initialization raises, and the same entry read after it does not ⚠ **This is the one obligation the complement sweep above could not return** — it swept §8's slot rows for consumers of the representation, and a collector arm is owed by the producer. **What the representation *is*** — cell, environment record, slot generation — **is named as this row's obligation and deliberately not answered here** (§5's specification-vs-evidence rule): this row's own mandatory `/elidex-plan-review` derives it against its own parent HEAD | `compiler/resolve.rs` | **new** `#11-vm-declarative-environment-and-capture-representation` | T1 | — |
| **F** | **The `vm/host/structured_clone.rs` split** — a terminal **prerequisite** row, minted because the dependency Pc carries named a standalone PR with no id. ⚠ **§5 defines the split as the set of rows, so a prerequisite described in prose has no acceptance condition, no `Deps` cell and no mandatory `/elidex-plan-review` of its own** — the same defect row **A** was minted for one round earlier, and the same one row **K** was minted for before that. Deliverable is the seam the file's own section banners already mark: `:1`-`:914` is the WHATWG HTML clone algorithm as this file implements it — a **fused** **§2.7.4** StructuredSerialize **and §2.7.6** StructuredDeserialize pass, which the entry point's own docstring states at `:63-64` ("the fused WHATWG HTML StructuredSerialize (§2.7.4) + StructuredDeserialize (§2.7.6) algorithm") — running `clone_value` `:75` through `data_clone_error` `:909`. ⚠ **This row first classified the half as §2.7.4 alone**, which would let Pc's citation retag assign serialization numbers to deserialization behaviour: several of the stale comments describe constructing the cloned *output*, which is §2.7.6's half of the fusion, so the classification F hands Pc names both clauses (§-numbers from `webref heading html 2.7`), `:916`-`:1077` is `// Global registration` plus the WebIDL binding layer (`register_structured_clone_global` `:922`, `native_structured_clone` `:944`, `validate_transfer` `:965`, `ensure_empty_transfer_list` `:998`) — leaving **914** and ~**162** lines. `wc -l` → **1077** at `658cc302` and unchanged today. **Behaviour-preserving, and the seam is not a line-count judgement**: `ensure_empty_transfer_list` already has a cross-module consumer at `vm/host/pending_tasks.rs:673`, the `postMessage` path, so the binding layer is reached from outside this file today. **Acceptance is bounded to what the move buys at its own landing** (§5's prerequisite rule): the existing suite passes unchanged and the two halves compile as separate modules — ⚠ **which fails only on a move that changes behaviour, and not on a move drawn at a different seam or one that edits while moving**: carving at `:237` instead of `:916` leaves ~840/~237 and satisfies both clauses. **The separating condition for a pure move is the one this program already uses** (PR #508's prereq split): the diff is hunk- and line-conserving — every moved line appears byte-identical on both sides, the moved-out and moved-in line counts are equal, and the hunk count is recorded in the PR — so a reflow, a comment rewrite or a seam drawn elsewhere fails it while a behaviour-preserving move at the stated seam passes. **Declared green guard**: `vm/host/structured_clone.rs`'s own §2.7.4-vs-§2.7.6 classification is untouched by this row, which is Pc's, so the harness must show zero content delta on the classification comments — no behaviour is asserted, because none changes. ⚠ **The layering question this row's size invites is not F's to decide, and is *open*.** That the split leaves a ~914-line WHATWG algorithm under `vm/host/` is a question `#11-vm-webidl-sequence-early-exit-drain` states and measures in its structured-clone half and the children `#11-vm-structured-clone-layering-seam`'s derivation mints now own: both homes proposed there are closed — the mandate's routing rule keys on an engine-independent home and `elidex_plugin::JsValue`'s opaque `ObjectRef(u64)` cannot carry this algorithm's operands — but closing both is not a finding that `vm/host/` is the right one, so a seam has to be derived. ⚠ **"and is already settled … the serialization half stays" stood here.** F is behaviour-preserving and may not re-decide it; §1.0's rule sends anything measured outside this umbrella's derived class to a §8 slot with an owner, never into a §5 row's acceptance. ⚠ **Not a plan-review option.** CLAUDE.md's touch-time rule makes the split mandatory once a real cohesion seam is found, and this row records that it was; the either/or that stood in Slice **1a**'s module cell is withdrawn with it | `vm/host/structured_clone.rs` | **new** `#11-vm-structured-clone-file-split` | T1 | — |
| **K** | **The inner class-name binding, and when it is initialized.** ⚠ **Carved at PR-B as a §8 *slot* whose stated owner was "whichever of 2ac/2b starts first", and that is wrong twice over on §5's own reading.** A slot settles a **decision** and ships no artifact — which is why `#11-vm-inbound-host-call-membership-test` and the typed-array layering gate are slots, and it is the same test that turned the declarative-environment carve into Slice **C** one row above; *moving* the class-name binding's initialization ahead of `compile_class`'s member loop is a concrete compiler artifact, i.e. **code**, so no slot can own it. And "whichever starts first" coordinates nothing: 2ac and 2b carry no edge between them (§5's `Deps` column), so their mandatory plan-reviews may run **concurrently** — either could move the initialization, neither could, or both could, and the slot that was supposed to prevent the duplicate mechanism ships nothing that would. **Measured**: `compile_class` (`compiler/expr_class.rs:99`) emits every member in its body loop, the `ClassMemberKind::StaticBlock` arm (`:448`) included, and only afterwards initializes the inner class-name binding (`:479-500`; `Op::InitLocal` at `:498`, `Op::SetLocal` at `:499`). ECMA-262 **§15.7.14** ClassDefinitionEvaluation orders it the other way: step 28.a is "Perform ! classEnv.InitializeBinding(classBinding, ctorFunc)", and step 32 is the loop over `staticElements` performing "Let result be Completion(DefineField(ctorFunc, elementRecord))" for a static field (step 32.a.i) and "Let result be Completion(Call(elementRecord.[[BodyFunction]], ctorFunc))" for a static block (step 32.b.ii) — §-number from `webref aoid ecma262 ClassDefinitionEvaluation`, steps from `webref body ecma262 sec-runtime-semantics-classdefinitionevaluation`. So `class C { static self = C; static { if (C !== this) throw 0 } }` reads an uninitialized binding today. **A prerequisite row under §5's row-kind rule, so its acceptance is bounded to what its own artifact buys at the point it lands** — and the static-**field** half of that probe is already reachable, because the `ClassMemberKind::Property` arm (`:393`) is wholly inside `if *is_static` (`:402`) and so emits at the baseline: acceptance is `class C { static self = C }` with `C.self === C`, asserted through JS with neither consumer landed. ⚠ **That assertion cannot see the half of §15.7.14's order this deliverable most easily breaks, because the binding is *required to still be uninitialized* for part of the same member loop.** Step 3.a is `Perform ! classEnv.CreateImmutableBinding(classBinding, true)` — created, **not** initialized; step 12 makes `classEnv` the running LexicalEnvironment; step 26 then runs ClassElementEvaluation over every element, which is where a **computed** `ClassElementName` is evaluated (§15.7.10 step 1); and only step 28.a initializes it, with step 32 running the static elements afterwards (`webref body ecma262 sec-runtime-semantics-classdefinitionevaluation`). So the spec places the initialization *between* two halves of what `compile_class` emits as one sequence, and "ahead of the member loop" moves it ahead of **both** — while `C.self === C` turns true either way. Required negative regression, ⚠ **red at the baseline, not green — the polarity stated here was inverted, and it is this row's only guard against the wrong fix.** Read-path TDZ is not implemented at `658cc302`: `git grep -n 'CheckTdz' 658cc302 -- crates/script/elidex-js/src/compiler` returns exactly two lines, `expr_assign.rs:42` and `:91`, both on the write path, while `Op::GetLocal` (`vm/dispatch.rs:128-133`) reads `self.stack[base + slot]` with no TDZ consultation. ⚠ **Both halves of that reading are falsified by measurement.** `expr_assign.rs:42` is not on the write path: it sits inside `compile_identifier_load`, doc-commented "Compile an identifier load (read)", emitting `Op::CheckTdz` under `if info.needs_tdz` before the `Op::GetLocal` at `:45`; only the `VarLocation::Upvalue` arm at `:47` is unchecked, and `:91` is the store-path sibling. So read-path TDZ *is* implemented for locals, and `class C { static [C] = 1 }` raises `ReferenceError: Cannot access variable before initialization` at `658cc302`, matching Node — it is a **green guard**, not a red probe, and it is assertable through JS after all, against the controls `class C { static [1] = 1 }` and `const D = 'd'; class C { static [D] = 1 }`, which both define their key here and under Node and so pin the throw to the class-name binding rather than to computed keys generally. What the row still owes is the *negative* the guard cannot see: that the binding is still uninitialized at the point the computed key is evaluated rather than merely at some point, which is asserted at the compiler; and the naive move it exists to catch (hoisting the `Op::Dup` / `Op::InitLocal` / `Op::SetLocal` block at `compiler/expr_class.rs:496-500` ahead of the member loop) is otherwise inert either way, so without the compiler-level form the row retires on the wrong fix: `class C { static [C] = 1 }` must throw a **ReferenceError**. The deliverable is therefore a split of the loop rather than a move of the initialization, and this row's acceptance is the pair The static-**block** half of the probe is deliberately **not** this row's acceptance: reading `C` from inside the block runs through the child compiler `:457` builds against the *enclosing* `func_scope_idx`/`current_scope_idx`, which is precisely the defect Slice **2b** owns, so asserting it here would pull 2b's work in — it is recorded as evidence and belongs to 2b's regression. This row likewise asserts **nothing** about 2ac's static-element receiver: that regression is about which `this` the initializer sees and does not turn true from a reordering. ⚠ **"Both consumers depend on this row rather than on each other — a `static x = C` initializer and a `static { … C … }` block each *read* the binding this row moves" stood here and over-counted the consumers by one.** The `static { … C … }` half is real and is **2b**'s: its regression reads the inner binding and its `Deps` cell carries the edge. The `static x = C` half is **this row's own acceptance** — `class C { static self = C }` with `C.self === C`, asserted here with neither consumer landed — and not 2ac's: neither of 2ac's regressions contains an IdentifierReference to `C`, so its `Deps` edge onto this row is removed and 2ac's cell carries the measurement. What remains with 2ac is **coordination** rather than ordering, over the shared `ClassMemberKind::Property` arm (`:393`) this row's acceptance runs through. So one consumer, not two — and the reason this row exists is unchanged: 2b consumes an artifact **no slot ships**, and "whichever starts first" orders nobody to produce it, while this row's own acceptance covers a behaviour neither consumer asserts. **Id**: a free top-level letter by the table's convention (`C`, `B`, `N` are taken; `git show af4c2792:docs/plans/2026-07-vm-p4-es-language-completeness.md \| grep -c -E '^[\|] \*\*K\*\* [\|]'` → **0** and the same blob under `grep -cE 'Slice \`?K\`?\b'` → **0** — `af4c2792` being the parent of `cd3fb9c3`, the commit that mints this row, so the reading is re-runnable rather than as-of-writing; positive control on the same blob, `grep -c -E '^[\|] \*\*C\*\* [\|]'` → **1**. ⚠ Run against `<this file>` the two return **1** and **3** from `cd3fb9c3` on, this row being its own first hit) — the row is not a child of umbrella 2 or 2a and mints no child id | `compiler/expr_class.rs` | **new** `#11-vm-class-name-binding-initialization-order` | T1 | — |
| **A** | **The §15.7.1 class-body parser early errors — UMBRELLA, not a terminal unit.** ⚠ **This row read as a terminal prerequisite, and a deliverable it describes in its own words as "a **policy** choice between spec-as-written and engine consensus rather than a spec fact" is what ends that** — a surface on which the governing document does not settle the answer is CLAUDE.md's *second* disjunct, and it fires without any axis count. The count fires too, at four: **§8.5.1** `Contains` with §15.7.1's `ClassStaticBlockStatementList Contains await` early error; **§15.7.9** `ContainsArguments` with §15.7.1's `FieldDefinition` early error — a second SDO over a second production; the grammar-parameter threading of `[+Await]` into `ClassStaticBlockStatementList`, which is what makes the `await` half separable at all; and the `FieldDefinition[?Yield, ?Await]` initializer policy. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. The material it routes — formerly stated as a terminal **prerequisite** row, minted here because the wording it replaces ordered nobody, and carrying **both** of that clause's rules this document measures as unenforced: `await` in a `ClassStaticBlock`, and `arguments` in a `FieldDefinition` initializer. ⚠ **The second is folded in here rather than given a row of its own, and the reason is one artifact rather than one topic** (CLAUDE.md *One issue, one way*): **§15.7.9** `ContainsArguments`'s default definition is the same recursive descent over child nodes as **§8.5.1** `Contains`'s, and shares the same eight function productions that `Return false`, reached from the same two files — so a second row builds that walk a second time (`webref body ecma262 sec-static-semantics-containsarguments` / `sec-static-semantics-contains`). ⚠ **The two listed sets are not the *same* set, and the difference is where §15.7.9's own controls live**: §15.7.9 additionally lists `IdentifierReference : Identifier` — its only `return true` production — and `MethodDefinition` / `GeneratorMethod` / `AsyncGeneratorMethod` / `AsyncMethod`, whose rule is `Return ContainsArguments of ClassElementName`, so a method's *body* is never descended into. That is a positive control the eight-production reading cannot reach: `function f(){ class C { x = { m(){ return arguments } } } }` must **parse**, where a walk modelled on §8.5.1's group alone descends into `m`'s body and rejects it. ⚠ **It read "whichever comes first of 2b and the terminal child of umbrella M that enables `parse_module`", and that is the defect row K was minted to fix**: 2b's `Deps` names only K, umbrella M has no child to name yet, and an umbrella named as a party names nobody — so the only scheduling instruction the artifact had lived in prose, and either consumer could land and retire with `class C { static { await 0 } }` still accepted in module mode. Deliverable is the parser early errors and, third, the grammar parameters the `FieldDefinition` production carries — nothing beyond the parse. The two early errors are both under **§15.7.1** *Static Semantics: Early Errors* (`webref heading ecma262 15.7.1 --exact`): **(i)** the `ClassStaticBlockBody : ClassStaticBlockStatementList` rule — §15.7.1 hangs these bullets under that production, not under `ClassStaticBlock` — whose SDO is `Contains` applied to the terminal `await` — ⚠ **not an AO named `ContainsAwait`**, which `webref aoid ecma262 ContainsAwait` reports does not exist, so do not cite one; and **(ii)** under `FieldDefinition : ClassElementName Initializer_opt`, "It is a Syntax Error if Initializer is present and ContainsArguments of Initializer is true", whose SDO is **§15.7.9** `ContainsArguments` (`webref aoid ecma262 ContainsArguments`; rule text from `webref body ecma262 sec-class-definitions-static-semantics-early-errors`, over which the field rule and the `ClassStaticBlockStatementList` rule are two of that clause's bullets). ⚠ **And (iii) — the `await` / `yield` grammar of the initializer itself — added this round out of §5's field-initializer bullet, which owed the choice to this row and then left it in prose alone.** That bullet measures ECMA-262 as published accepting all five spellings — `FieldDefinition` propagating `[?Yield, ?Await]` unbroken from `ClassDeclaration` / `ClassExpression`, on the raw `emu-production` (`params="+In, ?Yield, ?Await"`) rather than a lossy rendering of a suppressed `[~Yield, ~Await]` — against Node v24.16.0, which rejects each public initializer spelling and **accepts** the computed-name forms. That is a **policy** choice between spec-as-written and engine consensus rather than a spec fact, and it is this row's because the parameters are set exactly where this row's two early errors are read. Acceptance is the parse in whichever direction the choice goes, ⚠ **asserted as a pair, because no single spelling can separate the two readings**: `async function f(){ class C { x = await 1 } }` and `async function f(){ class C { [await 1] = 2 } }` must take **opposite** verdicts under engine consensus and the **same** verdict under spec-as-written, so an assertion on either alone is passed by both. **The consumers that lower an initializer carry the edge — 2aa and 2ac, whose `Deps` cells name the child this row's derivation mints, never this row.** **2ab does not**: its `Primary module(s)` cell is `vm/dispatch_class.rs`, `vm/interpreter.rs`, `vm/dispatch.rs` and `vm/host/custom_elements/`, no `parser/` or `compiler/` file, so it reaches no initializer parse — the same test this cell applies to (ii) at its foot. ⚠ **Which mechanism discharges (ii) is evidence, not specification** (§5's complement): the clause's *other* `ContainsArguments` rule — §15.7.1 carries exactly **two**, as this cell states two sentences above — the one over `ClassStaticBlockStatementList`, is already enforced in tree by a hand-rolled scope-stack walk (`scope/visitor.rs:246-252` at `658cc302`), so whether this row collapses that onto one SDO or extends it is this row's own mandatory `/elidex-plan-review`'s to settle against its own parent HEAD. In tree the arm clears `in_async` (`parser/function.rs:435`, inside `parse_class_member` at `:403` — ⚠ **there is no `parser/class.rs`**, at `658cc302` or today, and this row cited one for a revision), so a static block nested in an `async` function does not reach the first disjunct and `in_async_params` is not a class-body state; the **module** disjunct of `await_is_keyword` (`parser/mod.rs:61-63`) is the only route by which a static block reaches an `Await` *expression*, which is why this row must precede that child rather than accompany it. ⚠ **It is not why the error is "unobservable until `parse_module` is reachable" — that claim stood here and is withdrawn, and it contradicts this row's own script-mode acceptance below**: §15.7.1's rule is `ClassStaticBlockStatementList Contains await` at the *token* level, and script parsing accepts `await;` as an identifier today (`parser/expr_tests_special.rs:59-63`), so the script spelling is reachable now and the module spelling is the second of two routes. **Acceptance is bounded to what this artifact buys at the point it lands** (§5's prerequisite rule): the **script-mode** spelling `class C { static { await 0 } }` is a SyntaxError, and the same source is one under `parse_module` the moment that path exists — both asserted against the parser, not through a module load this row does not enable. ⚠ **`await;` cannot be the script-mode case, and stating it made that half vacuous — and the reason is this row's *own* `[+Await]` deliverable rather than the goal symbol.** `ClassStaticBlockStatementList : StatementList[~Yield, +Await, ~Return]opt` carries `[+Await]` unconditionally, in **either** goal symbol (`webref body ecma262 sec-class-definitions`), so the moment this row threads that parameter `await` is a keyword inside a static block in **script** mode too and `await;` is rejected by the **grammar** — `AwaitExpression[Yield] : await UnaryExpression[?Yield, +Await]`, whose Note 2 reads "Unlike YieldExpression, it is a Syntax Error to omit the operand of an AwaitExpression" (`webref body ecma262 sec-async-function-definitions`) — for a reason that is not §15.7.1. An implementation that threads `[+Await]` and writes no `Contains await` check retires that witness **green**, which is the vacuity this row exists to remove, and it is vacuous against this row's *own* artifact rather than against a consumer's. `await 0` is what `[+Await]` **admits**, so §15.7.1's `ClassStaticBlockStatementList Contains await` bullet is the only rule that can reject it — the discriminating spelling in either goal symbol, which is why the module case is now the same source rather than a second one. ⚠ **Superseded reading**: that script mode makes `await` an identifier (`parser/expr_tests_special.rs:59-63`, `top_level_await_script_is_identifier`) — true at `658cc302`, and false inside a static block from the moment this row's deliverable lands, which is the tree this acceptance is read at (§4 I-8: polarity is a property of the tree), so the earlier pair had the two spellings exactly the wrong way round. ⚠ **But "at the token level" is not a token *scan*, and both required cases are rejections, so nothing here fails on the arm that rejects every `await` token inside the block.** `Contains` is **§8.5.1**, whose default definition descends into child nodes and whose **`Return false` group** — `FunctionDeclaration`, `FunctionExpression`, `GeneratorDeclaration`/`GeneratorExpression`, `AsyncGeneratorDeclaration`/`AsyncGeneratorExpression`, `AsyncFunctionDeclaration`/`AsyncFunctionExpression` — is a *group* of eight and **not** §8.5.1's listed set, which also carries `ClassTail`, `ClassStaticBlock`, `ArrowFunction`, `ArrowParameters`, `AsyncArrowFunction` (both alternatives), `PropertyDefinition : MethodDefinition`, `LiteralPropertyName : IdentifierName` and `MemberExpression : MemberExpression . IdentifierName` (§-number from `webref aoid ecma262 Contains`; listed productions and steps from `webref body ecma262 sec-static-semantics-contains`). ⚠ **The arrow productions `Return false` for this symbol too, and their control is unreachable from the eight**: `ArrowFunction` and `AsyncArrowFunction` step 1 is `If symbol is not one of NewTarget, SuperProperty, SuperCall, super, or this, return false`, which for the terminal `await` returns false without descending, so `class C { static { const f = async () => { await 0 } } }` must **parse** — a positive control an enumeration that stops at the eight cannot produce. A **positive** regression is therefore required, and it is what makes the SDO the deliverable rather than a scan: `class C { static { async function g(){ await 0 } } }` must **parse**. And on the rejecting side the same default descent reaches a spelling no `Await`-*expression* check sees, because the token is a child of the loop production itself: `class C { static { for await (const x of []) {} } }` is a Syntax Error, which neither `await;` nor `await 0` exercises. ⚠ **That spelling is already a SyntaxError at `658cc302`, from the opposite arm, so as written it separates nothing** — and the arm it comes from is a *grammar* failure this row's own prose rules out as the rejecting rule. `ClassStaticBlockStatementList : StatementList[~Yield, +Await, ~Return]opt` carries **[+Await]** (`webref body ecma262 sec-class-definitions`), so `for await` **is** grammatical inside a static block and §15.7.1's `Contains await` is what must reject it; in tree the class-member arm clears `in_async` (`parser/function.rs:433-435`), so the source fails as a parse cascade opening `"Expected LParen, got Identifier"` — identically to the same source inside an `async function`, while `(async function(){ for await (const x of [1]) {} })` and `class C { static { for (const x of []) {} } }` both parse. **Threading `[+Await]` into `ClassStaticBlockStatementList` is therefore a named deliverable of this row rather than a producer it waits on**: without it §15.7.1's rule has no production to apply to, and every implementation that leaves the parameter un-threaded retires this witness green. With it landed the witness is a **red probe** against this row's own artifact — the production parses, and `Contains` is what rejects it **Both consumers of (i) carry the edge**: 2b's `Deps` and the `Deps` of the child umbrella M mints for `parse_module`, the latter placed by that umbrella's derivation. **Acceptance for (ii) is bounded the same way** and is asserted against the parser with no consumer landed, §15.7.9 being purely syntactic: `function f(){ class C { x = arguments } }`, `function f(){ class C { static x = arguments } }` (the half the evidence block above §5 measures live today) and `function f(){ class C { #x = arguments } }` are each a SyntaxError — one per field call site, `parser/function.rs:579` / `:416` / `:482` through `parse_optional_initializer` (`parser/mod.rs:324-331`), the three arms that block names and the three a fix at one site leaves — as is `function f(){ class C { x = (() => arguments)() } }`, `ArrowFunction` being **absent** from §15.7.9's listed productions so the default descent reaches it, which a test on the initializer's immediate expression misses. ⚠ **All four are rejections, so the hole this row's own `await` half already names applies here**: the required **positive** control is `function f(){ class C { x = function(){ return arguments } } }`, which must **parse**, `FunctionExpression` being one of the eight productions that `Return false`. **(ii) has exactly one consumer, and it is 2aa**, whose `Deps` cell carries the edge: nothing emits a non-static field initializer at the baseline, so that is the row whose landing turns the instance spelling from inert into live and which must know whether `arguments` is a binding of the initializer function it constructs (§15.7.10 steps 2.5-2.7). **2ac carries none *of (ii)*** — it carries **(iii)**, for the reason (iii) gives — and the test for (ii) is the one 2ac's own cell applied when it withdrew its `K` edge: the static spelling is live *today*, so it is this row's own acceptance above rather than an artifact 2ac buys, and none of 2ac's four required regressions contains an `arguments` reference — what remains with 2ac is coordination over the shared `ClassMemberKind::Property` arm, which §5 puts in the rows and never in the column. **No child umbrella 2ab's derivation mints carries either**, its module cell holding no `parser/` or `compiler/` file and its deliverable reaching no initializer parse — one test disposing of (ii) and (iii) together | `parser/function.rs`, `parser/mod.rs` | **new** `#11-vm-class-body-parser-early-errors` | T2 | — |
| **B** | **Block-entry lexical instantiation — UMBRELLA, not a terminal unit.** ⚠ **A prior pass in this loop *rejected* this promotion and kept the row terminal; that verdict is withdrawn, and what falsifies it is two §2.2 rows that were already written when it was made.** The reject's measurement, quoted from the cell it stood in: the two obligations "are a sequence and step 3 of it — §14.2.2 steps 2 and 4 create and install the record, §14.2.3 step 3 instantiates into it — discharged at **one** site set, the block-entry arms, so a site missing from that set fails both at once", with `switch` (`scope/visitor.rs:131`) as the witness, where the case-body `let` resolves outward *and* the case-body function declaration reaches the no-op arm at `compiler/stmt.rs:31-39`. ⚠ **`switch` failing both is co-occurrence at a site that has no entry arm at all, not a joining of the two mechanisms, and §2.2 already measures the site that separates them.** §2.2's `switch` row records that `StmtKind::Block` (`compiler/stmt.rs:134-144`) and the `try` block (`:313-317`) **both** save `fc.current_scope_idx`, call `find_child_block_scope` and restore (that row's own probe, `grep -c -E -e current_scope_idx -e find_child_block_scope` → **4** over the Block arm against **0** over the Switch arm) — an ordinary block *does* enter its child scope — while §2.2's block-function row records that in that same ordinary block the declaration is **never instantiated**: `{ f(); function f() {} }` inside a function reaches a TypeError at the call (that row's probe is quoted exactly under (b) below), because both hoist loops (`compiler/mod.rs:82`, `compiler/expr_function.rs:167`) iterate an immediate-child list and `StmtKind::Block` only recurses. So at the ordinary-block site **entry passes while instantiation fails**, which is exactly the question the criterion asks — does each fail *while the others pass* — answered at the ordinary block rather than at `switch`. In tree the two are discharged at **different** site sets: entry at the block-entry arms and the scope lookup they key into, instantiation in the hoist loops, which are not block-entry arms at all. **Three mechanisms, and each fails while the other two pass** — the criterion's measurement, not a count. **(a) Scope entry, and the lookup key that makes an entry site addressable.** Witness `let x=1; switch(0){case 0: let x=2;} x` → **`2`** where ECMA-262 gives `1` (probe-measured at `658cc302`, §2.2's `switch` row); the finalizer spelling `let x=1; try {} finally { let x=2 } x` → **`2`** (probe-measured at `0c10870b`, §2.2's finalizer row, whose `git diff --stat 658cc302 0c10870b -- crates/` is empty) is the same mechanism and additionally needs the *key*, since `scope/visitor.rs:148` and `:166` push with the one `Try` span and `find_child_block_scope` (`compiler/stmt.rs:639-652`) matches on kind+span, so it returns the try block for both (§2.2's finalizer row measures this and states that no entry site can be added before the key distinguishes same-span siblings). Neither program constructs a closure or contains a function declaration, so (b) and (c) can both be right. **(b) Declaration instantiation into the entered record — §14.2.3 step 3.** Witness `(function(){ { try { return 'called:'+f() } catch(e) { return 'ERR:'+e } function f(){ return 7 } } })()` → **`"ERR:TypeError: not a function"`** (probe-measured at `658cc302`, §2.2's block-function row). The block is *ordinary*, so (a) passes on it — measured above — and the block is evaluated once and captures nothing, so (c) is not exercised; the layer is the hoist loops and the no-op arm, none of which is a block-entry arm. Step 3.a **binds** every BoundName of every LexicallyScopedDeclaration (`CreateImmutableBinding` for a constant declaration, `CreateMutableBinding` otherwise) while step 3.b **initializes only the function-shaped ones** at step 3.b.iv's `Perform ! envRecord.InitializeBinding(funcName, funcObj)`, so an acceptance reading "entry writes every declaration" deletes the block's TDZ. ⚠ Step 3.a.ii.1 and step 3.b.iii are *Normative Optional* — Block-Level Function Declarations Web Legacy Compatibility Semantics — which CLAUDE.md's core-vs-compat split puts in the LegacySemantics plugin and not in elidex-js core, so the arms this mechanism implements are the plain 3.a.ii.2 and 3.b.iv. **(c) The per-evaluation emit at the entry arm** — creating one record per evaluation of the Block production, binding the block's declarations into *that* record rather than into the function-wide `(scope_idx, name)` slot, and restoring at exit (§14.2.2 steps 2, 4 and 7; §14.12.4 steps 4, 6 and 9). Witness `const fs=[]; let n=0; while (n<2) { let x=n; fs.push(() => x); n++ } [fs[0](), fs[1]()]` must be `[0,1]` and is `[1,1]`. (a) passes on it — the `while` body is an ordinary block and is entered — and it contains no function declaration, so (b) is not exercised. ⚠ **The `while` spelling is chosen over the `for (let i=0;i<2;i++) { let x=i; … }` one this row carried, because that one contains a loop-head lexical binding and this one contains none**: `n` is declared outside the loop, so Slices **Ea**/**Eb** are not involved and cannot be credited with the fix — the discrimination this row already claims in prose, now carried by the witness itself. ⚠ **Provenance: this failure is *derived* from the allocation measured at `b5356098`, not probed** — `FunctionScope::locals` is a `HashMap<(usize, Atom), LocalInfo>` (`compiler/resolve.rs:31`), `add_local` (`:95-110`) assigns one slot per `(scope_idx, name)` and `add_upvalue` (`:148-157`) deduplicates captures by that same key, and the keying is by scope index rather than by loop form, so both entries of the `while` body share the one slot and the one upvalue. Probing it at the child's own parent HEAD, and carrying the result as that child's acceptance, is a mandatory output of the derivation. ⚠ **Whether (c) is a mechanism this row implements or merely C's artifact arriving as an edge — the question a prior pass answered "consumption", and the answer is drawn where the two cells already draw it.** The child umbrella **C**'s derivation mints for the runtime binding-cell representation owns that representation *and its closure-capture integration* in `compiler/resolve.rs`; §5's row-kind rule bounds that child's acceptance to what its own artifact buys at the point it lands, and C's cell states that bound in as many words — the assertion is made **from Rust** against the representation because "nothing in JavaScript reaches the representation until a consumer instantiates it". So that child landing whole leaves the `while` program above still reading `[1,1]`: what remains is the **emit**, and it is not in C's charter. If this row held only a `Deps` edge, that emit would be owned by nobody — the un-owned-obligation state §5 forbids. The **representation** stays that child's and reaches this umbrella's (c) child as an edge; the count is three because the emit is a third mechanism, not because the edge was counted. **⇒ three intersecting invariant axes — scope analysis and its lookup key, declaration instantiation and its TDZ split, and the per-evaluation environment emit that closure capture observes ⇒ the criterion's first disjunct is met.** ⚠ **Its second disjunct is *not* claimed, and §5's head requires a row whose evidence turns on layers to say so in its own cell**: each of the three has a canonical algorithm — §14.2.2 steps 2/4/7 with §14.12.4 steps 4/6/9 for (a) and (c), §14.2.3 step 3 for (b) — so the **axis count is the evidence** and the absence of a canonical algorithm is not asserted. ⇒ umbrella. It **mints terminal children at its own start**; **placing each obligation, each acceptance condition and each `Deps` edge on the specific child that consumes it is a mandatory output of that derivation** (§5's row-kind rule), and this row carries neither the ordering nor the owner nor an acceptance condition. ⚠ **This row mints none of them and invents no ids** — a mechanism absent from this cell is **un-minted, never out of scope**. ⚠ **Column change — the `Deps` cell becomes `—` and the edge onto C is re-pointed rather than dropped.** Naming this row in a `Deps` cell now names nobody, and leaving C in this cell would additionally over-order the (a) and (b) children, neither of which consumes a runtime record. **Placing that edge onto the child this derivation mints for (c) is a mandatory output of that derivation**, and that child's cell names the child umbrella **C**'s derivation mints for the binding-cell representation, via that derivation, which mints it — ⚠ *this read "names **C** directly, C being a terminal prerequisite row that lands", and C became an umbrella in the same sweep that converted this row.* **The seed is produced by a command, per §5, and the command returned clauses this cell had not named**: `webref heading ecma262 14.2` returns §14.2 *Block* and its three subclauses — **§14.2.1** Static Semantics: Early Errors, **§14.2.2** Runtime Semantics: Evaluation, **§14.2.3** BlockDeclarationInstantiation ( code, envRecord ) — and `webref heading ecma262 14.12` returns **§14.12.1** Static Semantics: Early Errors, **§14.12.2** Runtime Semantics: CaseBlockEvaluation, **§14.12.3** CaseClauseIsSelected ( caseClauseNode, input ), **§14.12.4** Runtime Semantics: Evaluation. **All four of §14.2.1, §14.12.1, §14.12.2 and §14.12.3 were absent from this whole document, not merely from this row** — ⚠ **measured on the blob *before* this note existed, because writing them down is what ends their absence and an as-of-writing figure would be false the moment it landed**: `git show 39ac7710^:docs/plans/2026-07-vm-p4-es-language-completeness.md \| grep -Fo -- '<clause>' \| wc -l` → **0** for each of the four (and → **0** for `CaseBlockEvaluation` and for `CaseClauseIsSelected` by name), against **9** for `'§14.2.3'`, **13** for `'§14.2.2'` and **5** for `'§14.12.4'` on the same pipeline, so the shape finds the clauses the document does cite; negative control `'§99.9.9'` → **0**. The two early-error clauses govern duplicate lexical declarations in a Block and in a CaseBlock, and §14.12.2 governs the order the CaseBlock's two clause lists are evaluated in inside the one environment §14.12.4 step 4 creates. ⚠ The seed also reaches **§14.2.2 step 6**, `DisposeResources(blockEnv.[[DisposableResourceStack]], blockValue)`, and **§14.12.4 step 8**'s assertion that a CaseBlock's stack is empty — the inbound edge umbrella **R** places on its own declaration-forms child, re-pointed at this umbrella's (c) child below. What each child owes past the seed is that child's own mandatory `/elidex-plan-review`, derived against its own parent HEAD (AO number from `webref aoid ecma262 BlockDeclarationInstantiation`; steps from `webref body ecma262 sec-block-runtime-semantics-evaluation` / `sec-switch-statement-runtime-semantics-evaluation` / `BlockDeclarationInstantiation`). **Split, against the rows that could have absorbed it** — what an umbrella does carry: no other row's charter is lexical-environment entry (0bc is binding-pattern *storage*, 2b is the static-block *function* boundary) and appending the three intersecting axes derived above to either makes it non-terminal. ⚠ **Not Slice E, and this is a boundary and not an ordering**: E owns a loop *head*'s bindings under §14.7.4.4 and §14.7.5.7 step 8.h.iii, while (c) is a **body-local** binding in a block whose environment is §14.2.2's — Ea and Eb can both retire while the `while` program above still reads `[1,1]`. ⚠ **This conversion strands no §4 obligation** (§5's fifth question, owed on a conversion as much as on an edge): `awk '/^## §4\./,/^## §5\./' <this file> \| sed 's/PR-B//g' \| grep -Fo -- '**B**' \| wc -l` → **0**, against the same pipeline over `'**10b**'` / `'**7b**'` / `'**6a**'` → **4** / **2** / **2** (re-derived at `29705dd6`, the revision that moved 10b **3**→**4** and 6a **1**→**2**), so the shape discriminates rather than matching nothing. ⚠ **The `B` tokens in §2.5's coupled-invariant matrix are a different namespace and are excluded from every enumeration this conversion runs**: `- **B · Call shape**` and the pairs `A×B` / `B×C` / `B×D` / `B×E` name the invariant *axes* of the 1a/1b call-spread pair, not slice ids. **Everything else in this cell is evidence for that derivation to route, not a charter** — including the `Primary module(s)` column, which (c) already shows is missing `compiler/resolve.rs` | `compiler/stmt.rs`, `compiler/mod.rs`, `compiler/expr_function.rs`, `compiler/expr_class.rs`, `scope/visitor.rs` | **new** `#11-vm-block-declaration-instantiation` | T1 | — |
| **L** | **Labelled-statement break targets — UMBRELLA, not a terminal unit.** ⚠ **The grouping is admissible and stands; what fails §5's criterion is the *terminality verdict*, and what defeats it is this row's own citations and this document's own measurements.** `outer: { break outer; }` is rejected at compile time, and where the labelled block contains a loop the same `break` patches the loop instead (§2.2's `compiler/stmt.rs:284-291` row). The representation is the defect: a label is stored as an index into `fc.loop_stack`, which has no entry for a statement that is neither a loop nor a switch, so the `Labeled` arm has nothing to patch and the `Break` arm has nothing valid to find. Deliverable is a label→patch-list representation that does not presuppose a loop, plus regressions for the bare labelled block, the labelled block wrapping a loop (the `break` must leave the block, not the loop), and `continue` on a non-loop label, which ECMA-262 **§8.3.3** ContainsUndefinedContinueTarget makes an early SyntaxError through **§16.1.1** Static Semantics: Early Errors (⚠ **this cell read `§14.8.1`, the citation the evidence block above already corrects: §14.8.1 decides *nesting* — "It is a Syntax Error if this ContinueStatement is not nested, directly or indirectly (but not crossing function or static initialization block boundaries), within an IterationStatement", `webref body ecma262 sec-continue-statement-static-semantics-early-errors` — not label targets**). ⚠ **A label target must also retain its enclosing loop depth, and none of those three regressions detects the loss.** Today the depth *is* the representation: the `Labeled` arm records `fc.loop_stack.len()` (`compiler/stmt.rs:287-288` at `b5356098`), and the `Break` arm reads it back at `:231-236` and uses it as the lower bound of `emit_iter_close_range(fc, target, fc.loop_stack.len())` at `:238` — before `fc.emit_jump(Op::Jump)` at `:240`, so the close range is computed from the label, not from the patch. A label→patch-list representation that carries only patch points therefore leaves `outer: { for (const x of it) { break outer } }` with no way to say how many iterators the `break` exits, and all three regressions above pass without it: none constructs an iterator. So the deliverable is the patch list **plus** the depth the exit range is derived from, with a fourth regression — a `break` to a **non-loop** outer label out of a `for-of`, over an iterator whose `return()` records the call, asserting the call happened. ECMA-262 **§14.7.5.7** ForIn/OfBodyEvaluation step 8.m.v is what requires it (`Return ? IteratorClose(iteratorRecord, status)` when `LoopContinues` is false), and **§14.13.4** LabelledEvaluation is what makes the labelled block the completion's target. **This contract runs in both directions with umbrella Pd, which is coordination rather than ordering — ⚠ and neither party is a row that lands**: Pd is an umbrella and this row is one from this revision on, so "whichever of the two lands second" named nobody on *both* sides. The contract is between **the child this row's derivation mints for the label representation** and **the child Pd's derivation mints for the labelled-`continue` exit range** (Pd's row already assigns the L coordination to a child of its own), and placing it on each is a mandatory output of the two derivations: the L child by writing the depth retention into the new representation, the Pd child by re-deriving its range against its own parent HEAD rather than against the representation written here. ⚠ **"Terminal under the criterion above: one contract …, one governing SDO (§14.13.4 LabelledEvaluation), one layer, and the state it changes (`label_map` / `loop_stack`, `compiler/function.rs`) is read from `compiler/stmt.rs` only — no second invariant axis is bundled" stood here and is withdrawn; three of its four clauses are contradicted inside this document.** The verdict rested on the depth being *a field of the same label representation this row delivers*, and §5's head already answers that: **7a**'s cell makes the identical concession about its adder — that a shared chokepoint "does not join their *failures*" — and §5's head states the case in general, that a group well-formed as a unit can still hold mechanisms with no shared lowering chokepoint. **Three mechanisms, and each fails while the other two pass** — the criterion's measurement, not a count. **(a) The label→patch-list representation**: `outer: { break outer; }` and the labelled block wrapping a loop, governed by **§14.13.4** LabelledEvaluation step 4, wholly inside the `Labeled` and `Break` arms of `compiler/stmt.rs` (`:287-288`, `:231-236`, `:240`). Constructs no iterator and reaches no parser arm, so (b) and (c) can both be right. **(b) The `continue`-label early error, which is not in that layer**: `parser/stmt.rs:113-117` computes `is_iteration` from the token *immediately* after the colon and `parser/control_flow.rs:444-454` raises on `!is_break && !is_iteration`, so `outer: inner: while (cond) { continue outer }` is rejected though valid — the evidence block above measures it, and note it is the **opposite direction** from the regression this cell lists (a valid label wrongly rejected, not an invalid one wrongly accepted), so the listed regression does not detect it. ⚠ **That valid nested-label spelling is therefore a required regression of the parser child, placed there by this derivation** — without it the child retires with the live defect intact, since no acceptance in this cell asks the valid form to parse and the invalid-form regression passes on the code as it stands; §8.3.3's rule is that the label set of the enclosing iteration statement contains the target, which `outer: inner: while (cond) { … }` satisfies through the nesting the in-tree check cannot see. Governed by **§8.3.3** ContainsUndefinedContinueTarget through **§16.1.1**; nothing in `compiler/` runs, so (a) and (c) can both be right. **(c) The exit-range depth a label target retains**, and the close it decides: `emit_iter_close_range(fc, target, fc.loop_stack.len())` (`:238`) takes the label's `fc.loop_stack` index as its lower bound, and **§14.7.5.7** ForIn/OfBodyEvaluation step 8.m.v is what requires a `break` out of a `for-of` to a non-loop outer label to close the iterator. None of (a)'s or (b)'s programs constructs an iterator, so both pass while this is lost — this cell says so in as many words. Three intersecting invariant axes — the compiler's labelled-jump lowering, the parser's static semantics for label targets, and the iterator protocol's close range — ⇒ **the criterion's first disjunct is met**. ⚠ **Its second disjunct is *not* claimed, and §5's head requires a row whose evidence turns on layers to say so in its own cell**: each of the three has a canonical algorithm — §14.13.4 step 4 for (a), §8.3.3 with §16.1.1 for (b), §14.7.5.7 step 8.m.v with §7.4.11 for (c) — so the **layer count is the evidence** and the absence of a canonical algorithm is not asserted. ⇒ umbrella. It **mints terminal children at its own start**; **placing each obligation, each acceptance condition, each `Deps` edge and the coordination with the child Pd's derivation mints for the labelled-`continue` exit range on the specific child is a mandatory output of that derivation** (§5's row-kind rule), and this row carries none of the three. ⚠ **This row mints none of them and invents no ids** — a mechanism absent from this cell is **un-minted, never out of scope**. **The seed is produced by a command, per §5, and the command found what this cell had not**: `webref heading ecma262 8.3` returns §8.3 *Labels* and its three static-semantics operations — **§8.3.1** ContainsDuplicateLabels, **§8.3.2** ContainsUndefinedBreakTarget, **§8.3.3** ContainsUndefinedContinueTarget — **none of which this row named before it was run**; `webref heading ecma262 14.13` returns §14.13.1 Static Semantics: Early Errors / §14.13.2 IsLabelledFunction / §14.13.3 Runtime Semantics: Evaluation / §14.13.4 LabelledEvaluation; and `webref heading ecma262 14.8` / `14.9` return each statement's early-error and evaluation clauses. §8.3.1 and §8.3.2 are satisfied in tree today (`parser/stmt.rs:106-111` rejects a duplicate label; `parser/control_flow.rs:458-463` rejects an undefined one), which makes them a **no-regression obligation of whichever child replaces the representation** rather than a fourth mechanism — that reading, and everything the seed reaches past it, is each child's own mandatory `/elidex-plan-review` to derive against its own parent HEAD. **Everything else in this cell is evidence for that derivation to route, not a charter** — including the `Primary module(s)` column, which (b) already shows is missing `parser/stmt.rs` and `parser/control_flow.rs`. ⚠ **This conversion strands no §4 obligation** (§5's fifth question, which is owed on a conversion as much as on an edge): `awk '/^## §4\./,/^## §5\./' <this file> \| grep -c -- '\*\*L\*\*'` → **0**, against the same pipeline over `'\*\*10b\*\*'` / `'\*\*7b\*\*'` / `'\*\*6a\*\*'` → **4** / **2** / **2** (re-derived at `29705dd6`, the revision that moved 10b **3**→**4** and 6a **1**→**2**), so the shape discriminates rather than matching nothing, and §4 names no obligation on this id | `compiler/stmt.rs`, `compiler/function.rs` | **new** `#11-vm-labelled-statement-break-target` | T2 | — |
| **E** | **Per-iteration loop environments — KIND UNDETERMINED: neither an umbrella nor a terminal unit until the measurement stated below is made, and no PR may be scheduled under it — its sub-slices **Ea** and **Eb** included — until it is.** A `let`/`const` loop head gets one binding for the whole loop (§2.2's `scope/visitor.rs:79-124` row), so every closure made in the body observes the last value. **Two governing algorithms decide it and they differ in substance**: **§14.7.4.4** CreatePerIterationEnvironment, reached from **§14.7.4.2** ForLoopEvaluation, makes a fresh environment per iteration and **copies the previous iteration's value forward** so the update expression can act on it; **§14.7.5.7** ForIn/OfBodyEvaluation step 8.h.iii makes a fresh environment per iteration and **instantiates the head from that iteration's value**, copying nothing. Both intersect scope analysis (one scope per loop today), the compiler's local allocation, and closure capture. The split is by **head form**, which is exactly where the two algorithms part, rather than by loop keyword. **The measurement that settles the kind**: whether scope analysis (one scope per loop today), the compiler's local allocation and closure capture — the three this cell says both algorithms `intersect` — are three axes or the three stages one per-iteration binding passes through, stated as three probes, each red while the other two are green. If fewer than three come back, this row carries two axes — §14.7.4.4's copy-forward and §14.7.5.7 step 8.h.iii's no-copy instantiation — against a threshold of ≥3, and it is a terminal row whose `Ea`/`Eb` boundary is a within-PR one rather than a mint. The second disjunct is unavailable: the cell names §14.7.4.4 CreatePerIterationEnvironment, §14.7.4.2 ForLoopEvaluation and §14.7.5.7 step 8.h.iii, so the AOs that govern the work are in the specification. | — | **new** `#11-vm-per-iteration-loop-environment` (kind undetermined) | T1 | — |
| **Ea** | **The `for` head** — §14.7.4.2 / §14.7.4.4: a fresh environment per iteration for the head's lexical bindings, with the previous iteration's value copied in before the update runs. Carries the representation the family needs — a per-iteration binding that closure capture observes — which is why **Eb** depends on it rather than building a second one (CLAUDE.md *One issue, one way*). Regression: a closure captured in iteration *n* still reads iteration *n*'s value after the loop ends — ⚠ **stated without its expected value, and asserting nothing about the *representation* Eb and the child umbrella **B**'s derivation mints for the per-evaluation emit consume**, so a compiler-local trick that re-emits the closure's upvalue as a per-iteration copy at the capture site passes it while the artifact Eb "depends on rather than building a second one" is never built. **The condition carries its value and its producer**: `const fs = []; for (let i = 0; i < 2; i++) fs.push(() => i); [fs[0](), fs[1]()]` is `[0, 1]` — red at `658cc302`, measured `[2, 2]` — **and** the per-iteration record is the one umbrella C's derivation mints, asserted from Rust as two distinct entries of one `(scope_idx, name)` across the loop, so a per-capture copy that produces the same JS answer without the record fails. ⚠ **That is one assertion over a `let` head, and §14.7.4.2 makes the *absence* of the environment a requirement of the sibling productions — so nothing here fails on an implementation that per-iterations every `for` head.** `ForStatement : for ( var VariableDeclarationList ; Expressionopt ; Expressionopt ) Statement` and `ForStatement : for ( Expressionopt ; Expressionopt ; Expressionopt ) Statement` each end in `Return ? ForBodyEvaluation(test, increment, Statement, « », labelSet)` — an **empty** perIterationBindings list — while only the `LexicalDeclaration` production computes one, and there step 9 is `If isConst is false, let perIterationLets be boundNames; else let perIterationLets be a new empty List` (`webref body ecma262 sec-runtime-semantics-forloopevaluation`). Second required regression, the negative control: `const fs = []; for (var i = 0; i < 2; i++) fs.push(() => i); [fs[0](), fs[1]()]` must be `[2, 2]` ⚠ **The per-iteration environment and the cells a closure captures are the same representation the child umbrella **B**'s derivation mints for the per-evaluation emit needs** — §14.7.4.4's `NewDeclarativeEnvironment(outer)` and §14.2.2 step 2's are the same runtime object, and both land in `compiler/resolve.rs`, whose `(scope_idx, name)` keying gives one slot and one upvalue per name today. Building it twice is what CLAUDE.md *One issue, one way* forbids, and neither row's plan-review can see the other's, so it is built once by the row this row's `Deps` cell carries | `scope/visitor.rs`, `compiler/stmt_loop.rs`, `compiler/resolve.rs` | `#11-vm-per-iteration-loop-environment` (Ea) | T1 | — |
| **Eb** | **The `for-in` / `for-of` heads** — §14.7.5.7 step 8.h.iii: a fresh environment per iteration into which the head is instantiated from that iteration's value, with no copy-forward. Consumes Ea's representation. Regression: the same closure-capture assertion over `for (let k in o)` and `for (let x of it)`, so both loop forms are covered by the family. ⚠ **Two head *kinds* are covered by neither spelling, and each is a branch of the step this row cites.** §14.7.5.7 creates the record on the lexical-binding arm only — that arm opens `Assert: lhsKind is lexical-binding` before `Let iterationEnv be NewDeclarativeEnvironment(oldEnv)`, while the `assignment` / `var-binding` arm sets `iterationEnv` to **undefined** — so `const fs = []; for (var k in {a: 1, b: 2}) fs.push(() => k); fs[0]()` must be `"b"`, the negative control an implementation that per-iterations every head fails. And step 8.h.**iv**'s `ForDeclarationBindingInstantiation` (**§14.7.5.4**) — a *different* step from the 8.h.iii `NewDeclarativeEnvironment(oldEnv)` cited above in this same cell, which had given one id to both — branches on `IsConstantDeclaration`: `CreateImmutableBinding(name, true)` for `const` against `CreateMutableBinding(name, false)` for `let`, so `for (const x of [1]) { x = 2 }` must throw a **TypeError** where the `let` spelling is silent — ⚠ **and this condition is retired by this row's own dependency with zero per-iteration work from this row**: `#11-vm-const-assignment-runtime-throw`'s deliverable is a const-ness test relocated at the *resolution*, so it throws for this source whether or not any per-iteration immutable binding was ever created. The row states that exact objection against the `CompileError` form and does not carry it to the TypeError form it depends on. **The condition that the dependency cannot supply**, red at `658cc302`: §14.7.5.4's `IsConstantDeclaration` branch performs `CreateImmutableBinding(name, true)` **per iteration**, so the *binding* is immutable in each fresh record — asserted from Rust against the record umbrella C's derivation mints, that a `for (const …)` head's per-iteration entry is created immutable, which a remedy creating always-mutable per-iteration bindings and leaning on the dependency's compile-time test fails — a fresh binding per iteration that is merely *fresh* satisfies the assertion above and not this one (steps from `webref body ecma262 sec-runtime-semantics-forin-div-ofbodyevaluation-lhs-stmt-iterator-lhskind-labelset` / `sec-runtime-semantics-fordeclarationbindinginstantiation`). ⚠ **At `658cc302` that `const` witness answers `CompileError "Assignment to constant variable 'x'"`, and converting it into a catchable runtime TypeError is not this row's deliverable.** `#11-vm-const-assignment-runtime-throw` (§8) owns that conversion and states the same construct as its own regression (iii), so the edge is recorded in this row's `Deps` cell rather than left implicit: it is a **red probe** against that slot's artifact together with this row's. Accepting the `CompileError` as satisfying "must throw" instead would stop discriminating `const` from `let` at all, since `compile_identifier_store` emits it whether or not a per-iteration immutable binding was ever created. The `var`-head control beside it is a **guard**, green today (`const fs = []; for (var k in {a: 1, b: 2}) fs.push(() => k); fs[0]()` → `"b"`) | `scope/visitor.rs`, `compiler/stmt_loop.rs` | `#11-vm-per-iteration-loop-environment` (Eb) | T1 | — ⚠ (edges withheld while E's kind is undetermined; re-derived when it resolves) (§8 — this row's `for (const x of [1]) { x = 2 }` witness requires a catchable runtime **TypeError**, and at `658cc302` the const-write rejection is a `CompileError` from `compile_identifier_store`; that slot owns the conversion and carries the same construct as its own regression (iii)) |
| **R** | **Explicit Resource Management — PROMOTED TO ITS OWN UMBRELLA, not a terminal unit, outside this plan's slice sequence** (the Slice M treatment). `using` / `await using` have **no AST production at all**, so the syntax is a parse error and the object surface is absent: measured at `658cc302`, `grep -rl <name> crates/script/elidex-js/src` returns **0** files for each of `UsingDeclaration`, `await using`, `DisposableStack`, `AsyncDisposableStack`, `SuppressedError`, and `grep -ni -E -e using -e dispose <ast.rs>` returns nothing (control, the same form with `-e Spread` in place of `-e using` → **9** lines at `658cc302`, so the shape is not vacuous). Probe-measured: `using x = {…};` → `CompileError` "Expected ';'", `async function a(){ await using x = {}; }` likewise, and `typeof DisposableStack` / `AsyncDisposableStack` / `SuppressedError` / `Symbol.dispose` → `"undefined"`. Spec surface: **§14.3.1** *Let, Const, Using, and Await Using Declarations* (the declaration forms), **§27.2** *Resource Management* (the Disposable / AsyncDisposable interfaces, §27.2.1.1 / §27.2.1.2), **§27.3** *DisposableStack Objects*, **§27.4** *AsyncDisposableStack Objects*, **§20.5.8** *SuppressedError Objects*, **§14.7.5** *The for-in, for-of, and for-await-of Statements* (the loop-head forms), **§20.4.2** *Properties of the Symbol Constructor* and **§6.1.5.1** *Well-Known Symbols* — every number from `webref heading ecma262`. ⚠ **The two disposal symbols are seeded on their own, because the rest of this list reaches them only as a *mention*, and a mention is not a seed.** §27.2.1.1 / §27.2.1.2 state the interfaces in terms of the *values* `%Symbol.dispose%` / `%Symbol.asyncDispose%`, and **§7.5.4** GetDisposeMethod reads them the same way; what *defines* them as observable properties is a different clause, and the command finds it: `webref heading ecma262 20.4.2` lists **§20.4.2.3** `Symbol.dispose` and **§20.4.2.1** `Symbol.asyncDispose` among the Symbol constructor's own properties, and `webref heading ecma262 6.1.5` gives **§6.1.5.1** *Well-Known Symbols* as the inventory they are drawn from. This row's own probe measures `typeof Symbol.dispose` → `"undefined"`, so under the seed as it stood the ids were minted by **nobody** while every child retired having followed the stated walk — the one-directional silent failure §5's head records. Only those two entries are this row's; the remainder of §20.4.2 sits inside Slice 9's rule, which already subtracts *any surface another slot owns* — the same relationship 8d and the child umbrella 8 mints for RegExp creation carry for `%Symbol.match%` — and which child allocates them is a mandatory output of this umbrella's derivation. ⚠ **§14.7.5 is seeded on its own, because nothing else in that list reaches it.** §14.3.1 gives the *declaration* forms; the loop-head forms are a separate production, and §14.7.5's `ForDeclaration[Yield, Await, Using]` carries the alternatives `[+Using] using ForBinding` and `[+Using, +Await] await using ForBinding`, admitted by the `of` forms of both `for` and `for await` — so `for (using x of it)` and its `for await` counterpart (grammar from `webref body ecma262 sec-for-in-and-for-of-statements`; §-number from `webref heading ecma262 14.7.5`). Their runtime obligations are **§14.7.5.7** ForIn/OfBodyEvaluation's, not the declaration clause's: steps 4.b / 4.c read **§8.2.5** IsAwaitUsingDeclaration / **§8.2.4** IsUsingDeclaration into a `declKind`, step 8.h.vii.4.d passes the iterated value to **§7.5.2** AddDisposableResource on the *iteration* environment's `[[DisposableResourceStack]]`, and **§7.5.5** DisposeResources runs once per iteration on both exits — step 8.i.i.1 on an abrupt binding completion, ahead of that arm's `IteratorClose` at step 8.i.vi, and step 8.k.i on the body's result, ahead of the `LoopContinues` test at step 8.m (AO numbers from `webref aoid ecma262 <AO name>`; steps from `webref body ecma262 sec-runtime-semantics-forin-div-ofbodyevaluation-lhs-stmt-iterator-lhskind-labelset`). None of it is reachable from §14.3.1 or §§27.2-27.4 in any number of steps, so **the derivation routes these forms' parsing, their per-iteration disposal and their abrupt-completion ordering onto a terminal child** — otherwise every child minted from the other seeds retires with `for (using x of it)` still a parse error. ⚠ **The edges enumerated in this paragraph belong to that child, and placing them is a mandatory output of this umbrella's derivation — an umbrella carries no `Deps` cell, so none of them is written on this row.** **Pa**, for the reason 7a's and 9c's cells already carry: the resource-loop forms are the *same* §14.7.5.7 arms this row already reads, and those arms close the iterator — step 8.i.vi is `Return ? IteratorClose(iteratorRecord, status)` on the abrupt binding completion and step 8.m.v is the same once `LoopContinues(result, labelSet)` is false — so a `using` loop head drains through **§7.4.11** exactly as 0bc's surface does, and a close written against today's `iter_close` bakes the inverted precedence into this surface too. The `await using` / `for await` spellings take the sibling steps 8.i.v and 8.m.iv, which are **§7.4.15** AsyncIteratorClose instead (`webref aoid ecma262 AsyncIteratorClose`) — **a different AO from the §7.4.11 the Pa obligation is stated over, so Pa does not deliver it**: the producer is the child of umbrella `P` that owns §7.4.15, which P's row records as inside that umbrella's charter and un-minted, and placing that edge on whichever child of this umbrella carries the `await using` and `for await (using …)` spellings is a mandatory output of this derivation. **6b**: `for await (using x of it)` is admitted by the grammar quoted above, and **§14.7.5.6** ForIn/OfHeadEvaluation step 8 sets `iteratorKind` to async with step 10 `Return ? GetIterator(exprValue, iteratorKind)` (`webref aoid ecma262 ForIn/OfHeadEvaluation`; steps from `webref body ecma262 sec-runtime-semantics-forinofheadevaluation`) — the same **§7.4.4** entry point 6b's row reads, whose step 1.b routes an object with no `%Symbol.asyncIterator%` through **§27.1.5.1** CreateAsyncFromSyncIterator. So the async resource-loop form **consumes 6b's adapter artifact** rather than building a second one, which is the `is_await` re-derivation 6b's own row warns about. **Eb**: the `[[DisposableResourceStack]]` a `using` loop head pushes onto is a slot **on the per-iteration environment**, not on the loop's. Step 8.h.iii is "Let iterationEnv be NewDeclarativeEnvironment(oldEnv)"; step 8.h.v makes it the running LexicalEnvironment, so the `ResolveBinding` at step 8.h.vii.3 resolves into it and steps 8.h.vii.4.b-c take `base` to be that record before step 8.h.vii.4.d performs AddDisposableResource on `base.[[DisposableResourceStack]]`; and the two disposal points name the record outright — step 8.i.i.1 and step 8.k.i are both `DisposeResources(iterationEnv.[[DisposableResourceStack]], …)` (same `body` output as this row's other step reads). row **E**'s sub-slice `Eb` would own that creation, but E's kind is undetermined and neither it nor Eb owns anything yet — its own charter cites §14.7.5.7 step 8.h.iii — so without the edge the resource-loop child either builds a second per-iteration environment and capture mechanism alongside Eb's, or hangs the stack on a lifetime that outlives the iteration, in which case the per-iteration disposal its acceptance asserts cannot be distinguished from a once-per-loop one. **The child of umbrella `Pd` that owns close-versus-enclosing-finalizer sequencing**: on these same abrupt-exit arms a resource loop must order three things — disposal, the iterator close, and an enclosing `finally` — and §14.7.5.7 fixes the first two against each other (step 8.i.i.1 disposes ahead of step 8.i.vi's `IteratorClose`; step 8.k.i disposes ahead of the step 8.m `LoopContinues` test and step 8.m.v's close), leaving the close's position *relative to the enclosing finalizer regions* as the open one — which is the obligation Pd's row measures as currently wrong, with a throwing finally body suppressing the close entirely. ⚠ **Pa does not substitute for it, and the two are easy to conflate because both are "the close":** Pa fixes `IteratorClose`'s **result precedence** inside **§7.4.11** — which completion survives when the close itself throws — while what this child consumes from the child of umbrella `Pd` named above is the compiler's **placement** of that close relative to enclosing finalizers, a different artifact under a different governing algorithm. Both edges are therefore named, and both are Pd's-child and Pa's respectively rather than one standing in for the other. ⚠ **And an edge onto umbrella `9da`'s derivation and the child it mints for the resolving-functions closure is owed by every child that ships an async-disposal path — the resource-loop child of the paragraph above included — so placing it on each of them is a mandatory output of this derivation too.** Async disposal *settles a promise* rather than merely calling a method: **§7.5.5** DisposeResources performs `Set result to Completion(Await(result.[[Value]]))` at step 4.e.ii.1 when the resource's kind is async-dispose, and `Perform ! Await(undefined)` at steps 4.d.i and 5.a for the sync-dispose-after-async and the null/undefined `await using` cases; **§27.10.5.3** Await step 2 is `Let promise be ? PromiseResolve(%Promise%, arg)`; and **§27.5.4.7.1** PromiseResolve step 2 is `Let promiseCapability be ? NewPromiseCapability(ctor)` with step 3 `Perform ? Call(promiseCapability.[[Resolve]], undefined, « resolution »)` — the resolving-functions closure **§27.5.1.3** CreateResolvingFunctions delivers, which is the named deliverable of the child umbrella **9da**'s derivation mints for the resolving-functions closure, and the same chain 6a's and 6b's rows already trace onto that row (§-numbers from `webref aoid ecma262 DisposeResources` / `Await` / `PromiseResolve` / `NewPromiseCapability` / `CreateResolvingFunctions`; steps from `webref body ecma262 sec-disposeresources` / `Await` / `sec-promise-resolve`). The object surface reaches the same AO rather than a second mechanism: **§27.4.3.4** `AsyncDisposableStack.prototype.disposeAsync` step 5 is `Return ? DisposeResources(asyncDisposableStack.[[DisposableResourceStack]], NormalCompletion(undefined))` (`webref body ecma262 sec-asyncdisposablestack.prototype.disposeAsync`), so the child owning that surface owes the edge on the same reading as the `await using` and `for await (using …)` children. A child landing ahead of the child umbrella **9da**'s derivation mints for the resolving-functions closure writes its disposal against a `settle_promise` that adopts only `ObjectKind::Promise` — 9da's row measures that gap, and a dispose method returning a **plain thenable** — `await using x = { [Symbol.asyncDispose]() { return { then(r) { r() } } } }`, which step 4.e.ii.1 hands straight to Await — is the shape that shows it. **§27.2 is Resource Management, not Promise Objects (§27.5)**; Slice **9da's §5 row** records that correction and this row is its first consumer — §8 carries only the family's umbrella slot `#11-vm-builtin-prototype-static-sweep`, no per-sub-slice row, so a `§8 row` pointer for 9da resolves to nothing, so do not "fix" §27.2 back to §27.5 here. ⚠ **And an edge onto umbrella `B`'s derivation and the child it mints for the per-evaluation emit, on the same reading as the `Eb` one above and owed for the same reason — a resource stack is a slot *on an environment record*, and the record a block-scoped declaration pushes onto is not this umbrella's to create.** **§14.3.1.3** BindingEvaluation takes `kind` as *normal, sync-dispose, or async-dispose*, and for `LexicalBinding : BindingIdentifier Initializer` its non-normal branch takes `base` to be `lhs.[[Base]]` — asserted to be a Declarative Environment Record — before performing `AddDisposableResource(base.[[DisposableResourceStack]], value, kind)`, `lhs` being the `ResolveBinding` of the declared name two steps earlier. What creates that record for a `using` declared in a block is **§14.2.2** `Block : { StatementList }`: its second step is `Let blockEnv be NewDeclarativeEnvironment(oldEnv)`, its third instantiates the block's declarations into it through **§14.2.3** BlockDeclarationInstantiation, and its sixth is `Set blockValue to Completion(DisposeResources(blockEnv.[[DisposableResourceStack]], blockValue))` — so creation, instantiation and disposal are all the Block production's, per *evaluation* (AO number from `webref aoid ecma262 BlockDeclarationInstantiation`; steps from `webref body ecma262 sec-bindingevaluation` / `sec-block-runtime-semantics-evaluation`). That per-entry environment is precisely the mechanism **(c)** of umbrella **B**, the one B's own row records as invisible to both of its static derivations — so the far end of this edge is **the child B's derivation mints for the per-evaluation emit**, not **B**, which is an umbrella from this revision on and ships no PR to depend on. Without the edge the declaration child either hangs the stack on the one compile-time scope in tree — in which case a block entered twice shares one stack and the per-entry disposal its acceptance asserts cannot be told apart from a once-per-function one — or builds a second environment-and-capture mechanism beside B's, which is what Slice **C** exists to prevent. **Placing it on the child that owns the declaration forms is a mandatory output of this umbrella's derivation.** Axes: lexer/parser productions, scope analysis (a new binding kind with a disposal list), and abrupt-completion disposal semantics that intersect `try`/`finally`, `return` and `IteratorClose` ordering ⇒ ≥3 intersecting invariant axes, so the CLAUDE.md edge-dense rule forbids one PR. The umbrella derives its sub-slices from those sections. ⚠ **One obligation is added here out of Slice 6c's cell, which had recorded it in prose alone**: ECMA-262 **§27.1.4.1** `%AsyncIteratorPrototype%[%Symbol.asyncDispose%]` is inside this charter — 6c delivers the intrinsic and retires carrying **§27.1.4.2** alone — so placing that member on the child this derivation mints for the `%Symbol.asyncDispose%` id, **and placing `Deps: 6c` on that child**, are both mandatory outputs of this derivation; the direction is forced, the member being unwritable before the intrinsic exists while the id is not | — | **new** `#11-vm-explicit-resource-management` | T2 | — |
| **N** | **Arrow-function lexical `new.target` — SCHEDULED FROM ITS OWN SLOT, outside this plan's slice sequence** (§1.0). `op_new_target` (`vm/dispatch_class.rs:29-37`) reads only the current frame's `CallMode` and `FunctionObject` has no lexical `new.target` beside `captured_this` (`vm/value.rs:598`), against ECMA-262 **§9.4.5** GetNewTarget. Same routing as **O**: a connected handler, measured in §2.2, triple in §8, so this row is a pointer rather than a slice. It shares the closure-capture seam (`FunctionObject`, `vm/value.rs`) and both call entry points with **the child of umbrella 3 that owns the super-property lowering**, which threads `[[HomeObject]]` through the same two — different AO, same seam, so the two coordinate rather than depend. ⚠ **Against Slice S that same test returns the other answer, so this row depends on S.** **§9.4.5** GetNewTarget step 1 is `Let envRecord be GetThisEnvironment()` (`webref body ecma262 sec-getnewtarget`), and **§9.4.3** GetThisEnvironment is the *one* governing AO S's row states — so it is the **same** AO, not a shared seam, and what this row consumes is S's deliverable: the environment link carried on the closure rather than read off the current frame, threaded through both call entry points. Reading `new.target` lexically through an arrow is that walk applied to a different field of the record S makes reachable, which is why the fix is not a second outward walk beside it | — | `#11-vm-arrow-lexical-new-target` | T1 | **S** |
| **S** | **Lexical `super` through an arrow** — `(() => super())()` in a derived constructor and `(() => super.x)()` in a method (§2.2's `parser/arrow.rs:104-119` row). One contract, and the criterion above reaches neither disjunct — one axis, and its algorithm is canonical: **§9.4.3** GetThisEnvironment, the outward walk that skips a record with no `this` binding, which **§13.3.7.2** GetSuperConstructor step 1 and **§13.3.7.3** MakeSuperPropertyReference step 1 both start from — so an arrow's `super` resolves against the enclosing method or constructor. Deliverable is that link carried on the closure rather than read off the current frame, threaded through both call entry points, with a regression for each spelling — ⚠ **which is a *count*, not a content spec, and cannot tell the deliverable this row names ("carried on the closure") from the mechanism it names as wrong ("read off the current frame")**: resolving `super` by walking the runtime call stack to the nearest non-arrow frame passes both spellings whenever the arrow is invoked immediately inside the enclosing method. **Third regression, the one that separates them, red at `658cc302`**: `class B { m(){ return 1 } } class D extends B { m(){ return () => super.m() } } const g = new D().m(); g()` is `1` — the enclosing frame is gone by the time `g` runs, so only a link carried on the closure answers it. **Declared polarity**: all three are red probes at `658cc302`, where `parser/arrow.rs:104-119` lets the arrow inherit nothing. **Minted rather than folded into umbrella 3**, per the terminality criterion above: that umbrella's charter is super-*property* references — the emit shape, the `[[HomeObject]]` representation and both call entry points ([C13], [C24], [C25]) — while S's deliverable is the environment link **§9.4.3** GetThisEnvironment supplies, which the super-*call* spelling and N's `new.target` read through the same walk. ⚠ **The carve reason as written no longer discriminates**: naming a second algorithm reaches neither disjunct of the criterion above — **§9.4.3** is canonical, so the second disjunct is unavailable, and one further axis is not three — and it is the two measurements below that keep the work out of umbrella 3, so S sits beside umbrella 3 on its own registered slot rather than inside it — ⚠ **and it holds — decided here rather than delegated, because the split is one of §5's three specification fields and its single home is the set of rows.** An earlier wording left "whether that holds" to umbrella 3's derivation; that is the one thing a child derivation may not settle, and it was self-referential besides, since S's own `Deps` names a child of umbrella 3 and 3's derivation would have been choosing whether to absorb the row that depends on it. Two measurements settle it. **S has its own governing AO**: **§9.4.3** GetThisEnvironment, which N's row independently identifies as *the one* governing AO S states, against umbrella 3's **§13.3.7.3** MakeSuperPropertyReference and **§9.1.1.3.5** GetSuperBase. **And S has a consumer outside 3's charter**: Slice **N** carries `Deps: S`, so folding S into a child of 3 would leave N's edge pointing at an artifact 3's derivation produces incidentally, for a row it has no charter over — the stranded-edge state §5's row-kind rule closes. So S stays a separate terminal slice, and umbrella 3's derivation inherits that boundary rather than re-opening it. Shares the closure-capture seam (`FunctionObject`, `vm/value.rs`) and both call entry points with **the child of umbrella 3 that owns the super-property lowering** and Slice **N**; that is coordination, not ordering. Depends on **the child of umbrella 3 that owns the super-property lowering** because the `super.x` spelling has no implementation to make lexical until that child lands — the umbrella itself never lands. ⚠ **And on the child of `#11-vm-derived-constructor-this-binding` that owns the `[[ThisBindingStatus]]` representation on the call frame (§8), because the super-*call* spelling does not end at the walk this row delivers.** ECMA-262 **§13.3.7.1** *Runtime Semantics: Evaluation* for `SuperCall : super Arguments` reaches that walk at step 7 — "Let thisER be GetThisEnvironment()" — and step 9 is "Perform ? BindThisValue(thisER, result)", while **§9.1.1.3.1** BindThisValue step 2 is "If envRecord.[[ThisBindingStatus]] is initialized, throw a ReferenceError exception" and step 4 sets it (§-numbers from `webref aoid ecma262 BindThisValue` / `GetThisEnvironment`; steps from `webref body ecma262 sec-super-keyword-runtime-semantics-evaluation` / `sec-bindthisvalue` — an AO over the record, `BindThisValue(thisER, result)`, not a record method, which is where a recalled signature drifts). In-tree there is no status to test: `dispatch_super_inner` reads the frame's `this_value` as the receiver (`vm/dispatch_class.rs:136`) and then *assigns* `this_value` and `new_instance` from what `construct_synchronous` returned (`:152-153`) — a swap, not the establishing of a binding, which is that slot's own measurement. So forwarding the §9.4.3 link makes `(() => super())()` return the preallocated receiver and look right while **`const f = () => super(); f(); f()` misses the ReferenceError** — and this row's stated acceptance is blind to it, *a regression for each spelling* asking one call per spelling. **The repeated-call regression is this row's**, and the edge is artifact-consumption rather than the shared-seam relation this row has with **3** and **N**: without the status the forwarding is written as that swap and rewritten when the slot lands. ⚠ **This row also *fires* that slot's registered trigger** — "with umbrella **2ab**'s derivation, which that occasion fires, or the next `vm/interpreter.rs` call-frame PR" — since threading the link through both call entry points is a call-frame change and `vm/interpreter.rs` is in this row's module cell. ⚠ **And by I-7, if that link is object-valued it owes the `ObjectKind::Function` tracing edge**, which this row's plan-review settles together with the representation | `vm/value.rs`, `vm/interpreter.rs`, `vm/dispatch_class.rs`, `compiler/expr_class.rs` | **new** `#11-vm-arrow-lexical-super` | T2 | **the child of umbrella 3 that owns the super-property lowering**, via that umbrella's derivation, which mints it, and **the child of `#11-vm-derived-constructor-this-binding` that owns the `[[ThisBindingStatus]]` representation on the call frame**, via that slot's derivation, which mints it (§8 — the super-call spelling's **§13.3.7.1** step 9 `BindThisValue(thisER, result)` needs the `[[ThisBindingStatus]]` that slot delivers, or a repeated `super()` through an arrow misses its **§9.1.1.3.1** step 2 ReferenceError) |
| **7** | **`Map`/`Set`/`WeakMap`/`WeakSet` — UMBRELLA, not a terminal unit.** The second disjunct is met: neither an umbrella nor a terminal unit until the measurement stated below is made, and no PR may be scheduled under it — its sub-slices **7a** and **7b** included — until it is.** The weak half intersects a different subsystem: entry liveness is a GC-tracing invariant, and §5's 1000-line note puts `vm/gc/collect.rs` (2074) and `vm/gc/trace.rs` (1255) in this row's reach. Keyed-collection semantics and GC liveness are two invariant axes, and §5's threshold is ≥3. **It had no derivation at all until PR-B** — it named two sub-slices and let their charter sentences stand as the scope, which is the member-shaped failure §5's head now rules out. **Its scope is a derivation rule, and running that derivation is its first step**: *the property surface and the abstract operations ECMA-262 **§24** *Keyed Collections* defines — the constructor clauses and the operations they invoke as much as the prototype clauses* (`webref heading ecma262 24`), run against the slice's own parent HEAD at the slice's start. **The measurement that settles the kind**: a third axis, stated red while keyed-collection semantics and GC liveness are both green — the two file sizes this cell cites being breadth and not an axis. For the second disjunct the measurement is whether ECMA-262 defines the weak half's entry-liveness algorithm at all or leaves it to the implementation, read from the §24 WeakMap / WeakSet clause bodies through `webref body` off `webref heading ecma262 24`. If neither comes back, the row carries two axes and is terminal, with `7a` and `7b` its deliverables rather than its children. The second disjunct is not reached either: this row's scope rule is a walk of ECMA-262 §24 *Keyed Collections* obtained with `webref heading ecma262 24`, so those AOs are in the specification, and whether the GC-tracing subsystem this cell also names has a canonical algorithm is a claim the cell does not make. | — | `#11-vm-map-set-collections` (umbrella) | T3 | — |
| **7a** | **`Map` / `Set` — UMBRELLA, not a terminal unit.** ⚠ **The 7a/7b seam is admissible and stands; what fails §5's criterion is the *terminality verdict* on the 7a half** — the same analysis that made 7b one, run over this half's own mechanisms, and the third axis is one this row itself supplied: it was judged terminal while its cell read "No GC-tracing change", and the withdrawal below establishes that sentence was false. Umbrella **7**'s own row already states that *keyed-collection semantics and GC liveness are separate invariant axes*; this half holds both, and a third besides. **Three mechanisms, and each fails while the other two pass** — the criterion's measurement, not a count. **(a) Keyed-collection identity, insertion order and the iterator surface**: `[[MapData]]` is an ordered List and the key test is **§24.5.1** CanonicalizeKeyedCollectionKey followed by SameValue, not **§7.2.10** SameValueZero applied directly — **§24.1.3.11** `Map.prototype.set` step 3 is `Set key to CanonicalizeKeyedCollectionKey(key)` and step 4.a tests `SameValue(entry.[[Key]], key)`, **§24.1.3.6** `Map.prototype.get` does the same at its steps 3 and 4.a, and §24.5.1 is the two-step `If key is -0𝔽, return +0𝔽` (§-numbers from `webref aoid ecma262 CanonicalizeKeyedCollectionKey` / `SameValueZero` and `webref heading ecma262 24.1.3`; steps from `webref body ecma262 sec-map.prototype.set` / `sec-map.prototype.get` / `sec-canonicalizekeyedcollectionkey`) — ⚠ **the "SameValueZero keying" this cell carried names an AO the algorithm no longer performs, and the difference is observable**: canonicalization normalises the *stored* key, so a row written from the withdrawn wording can key `-0` and `+0` together and still iterate `-0` back out. **(b) A new `ObjectKind` variant and the strong tracing arm it forces** — the withdrawal below, and §4 **I-7**'s obligation for this surface. **(c) The constructor drains' §7.4.11 completion precedence**, which is the `Pa` obligation: **§24.1.1.1** `Map ( [ iterable ] )` step 5 is `Let adder be ? Get(map, "set")` and step 7 `Return ? AddEntriesFromIterable(map, iterable, adder)`, whose **§24.1.1.2** body closes the iterator on abrupt completions throughout — step 2.c.ii is `Return ? IteratorClose(iteratorRecord, error)` with `error` a ThrowCompletion, and steps 2.e / 2.g / 2.i are `IfAbruptCloseIterator` — while **§24.2.2.1** `Set ( [ iterable ] )` performs **no** AddEntriesFromIterable at all and inlines its own drain: step 5 `Let adder be ? Get(set, "add")`, step 7 `Let iteratorRecord be ? GetIterator(iterable, sync)`, step 8.d `IfAbruptCloseIterator(status, iteratorRecord)` (§-numbers from `webref aoid ecma262 AddEntriesFromIterable` and `webref heading ecma262 24.1.1` / `24.2.2`; steps from `webref body ecma262 sec-map-iterable` / `sec-add-entries-from-iterable` / `sec-set-iterable`). **One measurement per mechanism, each blind to the other two — and ⚠ each in *both* spellings, because every witness this cell first carried exercised `Map` alone while the row's own (c) establishes that `Set` has a separately implemented path.** **§24.2.4.1** `Set.prototype.add` applies CanonicalizeKeyedCollectionKey on its own (`webref heading ecma262 24.2.4`), and §24.2.2.1 inlines its own drain, so a `Map`-only witness set passes while `Set` still iterates `-0`, loses sole-rooted values to a collection, or mishandles its distinct drain. The derivation routes the `Set` case beside the `Map` one on each of (a), (b) and (c). (a): `const m = new Map(); m.set(NaN, 1); m.get(NaN) === 1`, `const n = new Map(); n.set(-0, 1); Object.is([...n.keys()][0], 0)`, and the same pair over `Set.prototype.add` / `[...s]` — no iterable argument, so no drain runs, and no collection is placed. (b): §4 **I-7** states in as many words that a reader running at call time is *invisible to the functional probes*, so the witness is the forced-collection regression below, which neither (a)'s nor (c)'s program can express — under an arm written as a no-op both of them still pass. (c): an iterable whose first value is not an Object and whose `return()` throws must make `new Map(it)` throw the **§24.1.1.2** step 2.c.i TypeError, not the `return()` error, because **§7.4.11** step 5 (`If completion is a throw completion, return ? completion`) precedes step 6 (`webref body ecma262 sec-iteratorclose`); that path never calls `adder`, so (a) can be perfect while this is wrong, and it places no collection, so (b) can be. ⚠ **That witness has no `Set` counterpart in this spelling, and this row's own (c) is why** — **§24.2.2.1** accepts arbitrary element values, so there is no not-an-Object step to reach, and it inlines its own drain: step 8.c `Let status be Completion(Call(adder, set, « next »))` and step 8.d `IfAbruptCloseIterator(status, iteratorRecord)`, which **§7.4.13** expands to `Return ? IteratorClose(iteratorRecord, value)` on an abrupt `value` (`webref body ecma262 sec-set-iterable` / `sec-ifabruptcloseiterator`, §-number from `webref aoid ecma262 IfAbruptCloseIterator`). So the `Set` witness for the same precedence puts the throw in the **adder**, and it is runnable: `let closed = false; const it = { [Symbol.iterator]: () => ({ next: () => ({ value: 1, done: false }), return() { closed = true; throw new Error('close') } }) }; const saved = Set.prototype.add; Set.prototype.add = function () { throw new Error('adder') }; let err; try { new Set(it) } catch (e) { err = e } finally { Set.prototype.add = saved }` — asserting **both** halves, `closed === true` so the close was actually *placed* and the test is not passing vacuously through a drain that never closed, and `err.message === 'adder'`, the adder's error winning over the `return()` error by §7.4.11 step 5 preceding step 6. ⚠ **Like the `Map` witness it installs its own adder, so it does not exercise (a)'s `Set.prototype.add`** and does not discharge the (c)→(a) edge this derivation places, for exactly the reason stated there. Three intersecting invariant axes — keyed-collection identity and order in the natives layer, strong reachability in the collector's mark phase, and ECMA-262 iterator-protocol completion precedence in the drain — ⇒ **the criterion's first disjunct is met**. ⚠ **Its second disjunct is *not* claimed, and §5's head requires a row whose evidence turns on layers to say so in its own cell**: each of the three has a canonical algorithm — §24.1.3.11 / §24.1.3.6 with §24.5.1 for (a), the in-tree `ObjectKind::Array { elements }` arm at `vm/gc/trace.rs:246` for (b), §24.1.1.2 and §24.2.2.1 with §7.4.11 for (c) — so the layer count is the evidence and the absence of a canonical algorithm is not asserted. ⚠ **This is not the withdrawn "the criterion's fixed point is everything" reading** — that thesis was implemented in this program, falsified against CLAUDE.md clause (c)'s own parenthetical (*else infinite regress*) and reverted; the argument here runs over **this row's three mechanisms and their independent failure** and reaches no further. ⚠ **The counter-argument is that (c)'s `adder` *is* (a)'s `Map.prototype.set` — §24.1.1.1 step 5 and §24.2.2.1 step 5 both `Get` it off the instance — so the two share a chokepoint; that is true, and it does not decide this.** The chokepoint joins (c)'s call site to (a)'s method and joins neither of them to the mark phase, and it does not join their *failures*: the (c) witness above reaches step 2.c.ii without ever calling `adder`. §5's head states precisely that case — a group well-formed as a spec unit can still hold mechanisms with no shared lowering chokepoint. ⇒ umbrella. It **mints terminal children at its own start**; **placing each obligation, each acceptance condition and each `Deps` edge on the specific child is a mandatory output of that derivation** (§5's row-kind rule), and this row carries none of the three. ⚠ **This row mints none of them and invents no ids** — a mechanism absent from this cell is **un-minted, never out of scope**: `Map.groupBy` (**§24.1.2.1**) and the `Map` / `Set` iterator objects are inside the charter and un-minted here on exactly that reading. **The obligations that derivation must place**: the **Pa** edge and both constructor drains onto the **(c)** child, together with **an edge from (c) to (a)** — ⚠ **absent from the first wording, and (c) cannot be scheduled first without it**: §24.1.1.1 step 5 and §24.2.2.1 step 5 `Get` `map.set` / `set.add` off the instance, and this row assigns those methods to (a), so a (c) child landing first has no adder to call and only the pre-adder error path — the witness above — works at all, which is precisely why that witness did not expose the dependency; the unconditional marking arm and its forced-collection regression — §4 **I-7**'s obligation for this surface — onto the **(b)** child; **7b**'s drain child's edge and **9c**'s `Object.fromEntries` child's edge, both of which consume this family's **§24.1.1.2** implementation, onto the **(c)** child, re-pointing each once minted; and 9c's `GroupBy` "whichever lands first" party, which is the child carrying **§24.1.2.1** `Map.groupBy` and never this row. **Column changes, stated rather than left to inference.** `Deps` becomes **—**, which the rule *requires* rather than merely permits — so the **Pa** edge this row held is re-pointed onto the (c) child as a mandatory output of this derivation. The `Slot` stays `#11-vm-map-set-collections`, the `(7a umbrella)` qualifier being the kind marker rows **1a**, **4a** and **7b** already carry on the identical adoption; the `Tier` stays **T3**, inherited by the children; and the `Primary module(s)` cell stays as **evidence** for the derivation to route. **Everything below is evidence for that derivation to route, not a charter.** The surface clause — insertion-order iteration and the entries/keys/values iterator surface — is the **seed**, not the charter, which comes from umbrella 7's §24 derivation. ⚠ **"No GC-tracing change" stood here and is withdrawn as FALSE — the entries this row introduces must be held *strongly* by the collector, and that sentence instructed the opposite.** A `Map` instance has a `[[MapData]]` internal slot (**§24.1.4** *Properties of Map Instances*) and a `Set` a `[[SetData]]` one (**§24.2.5** *Properties of Set Instances*), and nothing permits their contents to be reclaimed while the collection is reachable: **§9.9.3** *Execution* enumerates exactly what an implementation may empty when a set of values is not live, and the four kinds it names under its step 1 are a WeakRef's `[[WeakRefTarget]]`, a FinalizationRegistry cell's `[[WeakRefTarget]]`, a `[[WeakMapData]]` entry's `[[Key]]` and `[[Value]]`, and a `[[WeakSetData]]` element — `[[MapData]]` and `[[SetData]]` appear in **none** of them, while **§9.9.2** *Liveness* makes a Map's contents live with the Map, since `Map.prototype.get` / `forEach` / `entries` observe their identity (§-numbers and titles from `webref heading ecma262 24.1.4 --exact` / `24.2.5 --exact` / `9.9.2 --exact` / `9.9.3 --exact`; steps from `webref body ecma262 sec-weakref-execution`, slot names from `sec-properties-of-map-instances` / `sec-properties-of-set-instances`). ⚠ **In-tree the sentence was not merely inaccurate, it was actionable in the wrong direction.** There is no `ObjectKind::Map` or `Set` at `658cc302` (`git grep -c 'ObjectKind::Map' 658cc302 -- crates/script/elidex-js/src` returns nothing, against the control `'ObjectKind::Array'` on the same input, which returns a per-file count for `vm/coerce.rs`, `vm/dispatch.rs`, `vm/dispatch_class.rs`, `vm/dispatch_iter.rs`, `vm/dispatch_objects.rs` and more), so this row mints the variant that carries `[[MapData]]` / `[[SetData]]`; `vm/gc/trace.rs:244` states its own match is "ObjectKind — exhaustive, no wildcard", so a new variant **forces** an arm, and "no GC-tracing change" is an instruction to write that arm as a no-op. The consequence is not a leak: unmarked slots go back to `free_objects` for reuse (`vm/gc/collect.rs:1422`), so a key or value reachable only through a Map would be reclaimed and its `ObjectId` handed to an unrelated object. **A tracing arm that marks every key and value unconditionally is therefore the deliverable this derivation places on the (b) child** — the shape `ObjectKind::Array { elements }` already has at `vm/gc/trace.rs:246`, `mark_value` over each element — with a regression that survives a forced collection: a Map whose only reference to an object key and an object value is the Map itself, collected, then read back through `forEach` (both key and value sole-rooted) and through `get` with the key held (value sole-rooted) — the `get` spelling needs a key handle, so it cannot be the sole-root witness for the key. ⚠ **This moves nothing about umbrella `7`'s seam, and the distinction is the one that split is made on**: an unconditional mark is ordinary strong tracing, not a liveness invariant; the *ephemeron fixpoint* — a value reachable only if its key is — stays wholly in the weak half. **The `Pa` obligation belongs to the (c) child and is not written in this row's `Deps`, an umbrella's cell naming nobody — for the reason 9c's cell already carries**: the constructors are in charter because the umbrella's derivation covers *the constructor clauses and the operations they invoke as much as the prototype clauses*, and **§24.1.1.1** `Map ( [ iterable ] )` step 7 is `Return ? AddEntriesFromIterable(map, iterable, adder)`, whose **§24.1.1.2** body closes the iterator on abrupt completions throughout — step 2.c.ii is `Return ? IteratorClose(iteratorRecord, error)` with `error` a ThrowCompletion, and steps 2.e / 2.g / 2.i are `IfAbruptCloseIterator` (§-numbers from `webref aoid ecma262 AddEntriesFromIterable` and `webref heading ecma262 24.1.1`; steps from `webref body ecma262 sec-map-iterable` / `sec-add-entries-from-iterable`). Same §7.4.11 inverted-precedence contract Pa's completion-kind parameter delivers, so writing either drain against today's `iter_close` bakes the inversion into another surface | `vm/natives_*`, `vm/object_kind.rs`, `vm/gc/trace.rs` | `#11-vm-map-set-collections` (7a umbrella) | T3 | — |
| **7b** | **`WeakMap` / `WeakSet` — UMBRELLA, not a terminal unit.** ⚠ **The seam against 7a is admissible and stands; what fails §5's criterion is the terminality verdict on the group that seam returned.** Umbrella **7**'s own row already states that *keyed-collection semantics and GC liveness are separate invariant axes*, and this is the row where the two meet — together with a third that the family's other child does not carry. **(1)** The weak **API surface**: the §24.3 / §24.4 property surface and the constructor drain, whose abrupt paths are **§7.4.11** closes (the `Pa` obligation below). **(2)** **Ephemeron tracing**: a weak entry's value is reachable only if its key is, which is a fixpoint over the collector's mark phase rather than a "do not trace this edge" rule, and it lands in `vm/gc/collect.rs` (2074) and `vm/gc/trace.rs` (1255) — the two files §5's own 1000-line note puts in this row's reach. **(3)** **I-5's `engine`-gate parity**: §4's I-5 pins an obligation to this row by name, and §8's `#11-vm-typed-array-family-layering-and-gate` records that this row's tracing work sits beside engine-gated wrapper-store rooting *with identical semantics required for non-`engine` builds*. Three intersecting invariant axes, sitting in the natives layer, in the GC subsystem and at the feature-gate boundary, so the criterion's **first** disjunct is met — and the **second** disjunct is *not* claimed: §5's head states that "span more than one governing algorithm across layers" is CLAUDE.md's disjunct paraphrased, not CLAUDE.md's disjunct, so a row whose evidence is a layer count says so and stops there. ⚠ **The counter-argument was that the constructor drain's `adder` *is* the prototype member the row implements, so the two share a chokepoint — that is true, and it does not decide this.** `webref body ecma262 sec-weakmap-iterable` step 5 is `Let adder be ? Get(map, "set")` and `sec-weakset-iterable` step 5 is `Let adder be ? Get(set, "add")`: the chokepoint joins the two *API* obligations to each other and joins neither of them to the mark phase. §5's head states precisely that case — a group well-formed as a spec unit can still hold mechanisms with no shared lowering chokepoint. ⇒ umbrella. It mints terminal children at its own start; **placing the `Pa` obligation, the I-5 obligation and each acceptance condition on the specific child is a mandatory output of that derivation** (§5's row-kind rule), and this row carries none of the three. Everything below is evidence for it to route, not a charter. Weakly-held keys, so one deliverable is the GC-tracing side: entries must not keep their keys alive, and collection must remove them. **No child minted here consumes an artifact of any child of umbrella 7a for the tracing deliverable**, which is the one the withdrawn cell was argued over, though the Deps cell named 7a until PR-B — the same shape as the already-withdrawn `10b → 7b`. ⚠ **The reason this rested on is withdrawn with the sentence it quoted — 7a's row said "No GC-tracing change" and that was false, so a conclusion drawn from it has to be re-derived rather than carried.** It survives on the re-derivation, and the re-derivation is sharper than the quotation was: umbrella 7a's derivation now places a tracing arm on the child that mints the `ObjectKind` variant carrying `[[MapData]]` / `[[SetData]]`, but an **unconditional** one, the shape `ObjectKind::Array { elements }` already has (7a's cell). **(2)**'s deliverable is a **conditional** rule over the *weak* variants — a value marked only if its key is, resolved to a fixpoint across the mark phase, which **§9.9.3** *Execution* permits for `[[WeakMapData]]` and `[[WeakSetData]]` and for nothing else. A second arm over different variants under a different rule consumes no artifact of the first, so the absence of the edge is a fact about the mark phase rather than about how much GC work the child of umbrella 7a that owns that tracing arm happens to do. ⚠ **That sentence stood unqualified, and read that way it is FALSE, because this row has three deliverables and the API-surface one does consume a child of umbrella 7a.** `webref heading ecma262 24.1.1` puts **§24.1.1.2** AddEntriesFromIterable under **§24.1.1** *The Map Constructor* — inside the §24 surface umbrella **7**'s rule assigns to 7a, whose cell already reads that same body for the `IteratorClose` obligations its derivation places on the child that owns the `Map` constructor drain — while the weak constructors *perform* it, §24.3.1.1's step below being that call verbatim. So the child carrying **(1)**'s drain consumes the AO from the child of umbrella 7a that owns the `Map` constructor drain — which that umbrella's derivation mints, and re-pointing this edge at it once minted is a mandatory output of that derivation — rather than open-coding a second copy, which is what CLAUDE.md *One issue, one way* forbids and what the row-level withdrawal, scoped to tracing, could not see. **Placing that edge on the drain child is a mandatory output of this derivation**, beside the `Pa` obligation it already owes. **A `Pa` edge is owed for 7a's reason rather than 7a's surface, and it belongs to the child that carries the drain — an umbrella's `Deps` cell names nobody, so it is not written here**: the weak *constructors* are in this row's charter because the umbrella's derivation covers *the constructor clauses and the operations they invoke as much as the prototype clauses*, and both of them close the iterator on an abrupt path — **§24.3.1.1** `WeakMap ( [ iterable ] )` step 7 is `Return ? AddEntriesFromIterable(map, iterable, adder)`, whose **§24.1.1.2** body is the one umbrella 7a's cell already reads for the `IteratorClose` obligations its derivation places on the child that owns the `Map` constructor drain, and **§24.4.1.1** `WeakSet ( [ iterable ] )` step 8.d is `IfAbruptCloseIterator(status, iteratorRecord)` (clause numbers from `webref heading ecma262 24.3.1` / `24.4.1` and `webref aoid ecma262 AddEntriesFromIterable`; steps from `webref body ecma262 sec-weakmap-iterable` / `sec-weakset-iterable`). Same §7.4.11 inverted-precedence contract Pa's completion-kind parameter delivers, so a constructor drain written against today's `iter_close` bakes the inversion into this surface too and is rewritten when Pa lands | `vm/natives_*`, `vm/object_kind.rs`, `vm/gc/` | `#11-vm-map-set-collections` (7b umbrella) | T3 | — |
| **8** | **RegExp completion — UMBRELLA, not a terminal unit.** The charter carries the RegExp instance/constructor surface and the well-known-symbol **dispatch** path inside the String methods. The second is not a RegExp deliverable at all — it is how `String.prototype.match`/`replace` find a method — so one PR would either skip it or rewrite string dispatch under a RegExp charter. **Its scope is a derivation rule, and running that derivation is its first step** — not the sub-slice rows below, which are what the §1.1 probe seeded. The rule, given Slice 9 explicitly excludes surfaces another slot owns (so 8a/8b can retire while a protocol stays missing and no row notices): *ECMA-262 **§22.2.6** Properties of the RegExp Prototype Object — accessors, methods and the `%Symbol.*%` protocol entries alike — plus, for each `%Symbol.*%` entry there, the **§22.1.3** String-prototype method that dispatches to it, **plus the ECMA-262 §22.2.7 *Abstract Operations for RegExp Matching* those members delegate to**, each read for what it computes and returns*, **plus the RegExp *creation and construction* clauses `webref heading ecma262 22.2` lists and none of the three above reaches** — **§22.2.3** *Abstract Operations for RegExp Creation* (§22.2.3.1 RegExpCreate / §22.2.3.2 RegExpAlloc / §22.2.3.3 RegExpInitialize / §22.2.3.4 ParsePattern), **§22.2.4** *The RegExp Constructor* (§22.2.4.1 `RegExp ( patternOrRegexp, flags )`), **§22.2.5** *Properties of the RegExp Constructor* (§22.2.5.1 `RegExp.escape` / §22.2.5.2 `RegExp.prototype` / §22.2.5.3 `get RegExp [ %Symbol.species% ]`) and **§22.2.8** *Properties of RegExp Instances* (§22.2.8.1 `lastIndex`) — run against the slice's own parent HEAD at the slice's start. ⚠ **The seed was §22.2.6 / §22.1.3 / §22.2.7 and it was curated, not commanded, which is why the constructor half was outside it for three revisions while 8a's row said "plus the constructor surface" in prose that no walk reads.** ⚠ **That prose is withdrawn at this revision and 8a is accessors-only, so the creation and construction half is this derivation's alone to mint** — and **placing 8d's `%Symbol.match%` edge on the child it mints for RegExp creation, together with the `Construct(speciesCtor, …)` edges the `%Symbol.matchAll%` and `%Symbol.split%` children of umbrella 8b carry onto that same creation child, is a mandatory output of this derivation** (§5's row-kind rule): named on this row they name nobody, and 8d's row states why the family is cyclic without them. The command is `webref heading ecma262 22.2` and the rule is now what it returns, less the two clauses the additions make reachable: §22.2.1 *Patterns* and §22.2.2 *Pattern Semantics* are both reached from **§22.2.3.3** RegExpInitialize — step 14 `Let parseResult be ParsePattern(patternText, u, v)`, step 16 asserting it is a Pattern Parse Node, and step 22 `Set obj.[[RegExpMatcher]] to CompilePattern of parseResult with argument regexpRecord` (`webref body ecma262 sec-regexpinitialize`) — and §22.2.9 *RegExp String Iterator Objects* from §22.2.6.9 `RegExp.prototype [ %Symbol.matchAll% ]` step 13, `Return CreateRegExpStringIterator(matcher, string, global, fullUnicode)` (`webref body ecma262 sec-regexp-prototype-%symbol.matchall%`). ⚠ **§22.2.3/§22.2.4 are *not* what a well-known-sounding citation would call them** — `webref heading ecma262 22.2.3 --exact` returns *Abstract Operations for RegExp Creation* and `22.2.4` *The RegExp Constructor*, with *Properties of the RegExp Constructor* one clause further at **§22.2.5**; a review round that named the first two as "RegExp Constructor / Properties of the RegExp Constructor" had the numbers right and the titles off by one, so the pairing is looked up here rather than recalled. **The §22.2.7 clause is what makes the rule bite and it was absent until PR-B** — §5's head now states the general form. Read over §22.2.6's members alone, the rule is discharged by `RegExp.prototype.exec` being present; but **§22.2.6.2** `exec` is four steps ending in `Return ? RegExpBuiltinExec(regexp, string)` (`webref body ecma262 sec-regexp.prototype.exec`), so what the member *does* is **§22.2.7.2**, and that is where the named-capture deliverable **8c** now owns was hiding. ⚠ **The seed is not the boundary**: the probe found `.global`/`.ignoreCase`/`.multiline`/`.sticky` and `@@match`/`@@replace`, and re-running the greps at `658cc302` finds more of the same class — `grep -rl <name> crates/script/elidex-js/src` returns **0** files for each of `dotAll`, `hasIndices`, `Symbol.search`, `Symbol.split` and `Symbol.match`/`Symbol.replace`/`Symbol.matchAll`, and probe-measured `typeof /a/s.dotAll`, `typeof /a/d.hasIndices`, `typeof /a/v.unicodeSets`, `typeof /a/g.global` and `typeof Symbol.search` / `Symbol.split` / `Symbol.matchAll` are all `"undefined"`. *(`unicodeSets` returns **4** files, but reading them shows all four are v-flag **engine** internals — `regexp/class_set.rs:1`, `regexp/unicode_property.rs:134`, `regexp/tests.rs:768`, `parser/expr_tests_special.rs:210` — not the §22.2.6.19 prototype accessor, which is absent like the rest. A file count is not an accessor.)* **The kind is the first disjunct: three axes, each stated red while the other two are green.** (i) **The §22.2.3-§22.2.5 creation and construction clauses** — this cell's own reason for widening the rule, `8a/8b can retire while a protocol stays missing and no row notices`, so the creation half stays red with the accessors and the String-side dispatch both green. (ii) **The §22.1.3 String-side dispatch** — a complete `RegExp.prototype [ %Symbol.match% ]` is unreachable while `String.prototype.match` never looks the method up, and this cell measures it: `grep -rl Symbol.match` returns **0** files and `typeof Symbol.search` is `undefined`, with the RegExp side green. (iii) **The §22.2.7 matching AOs** — read over §22.2.6's members alone the rule is discharged by `RegExp.prototype.exec` being present, while what the member *does* is §22.2.7.2, so the named-capture deliverable **8c** is red under both other axes green. `one PR would either skip it or rewrite string dispatch under a RegExp charter` is a charter-mismatch argument and the file count is breadth; neither is what carries the kind. The second disjunct is unavailable: this row's own rule is what `webref heading ecma262 22.2` returns, and it quotes §22.2.6.2 and §22.2.7.2 through `webref body`, so the AOs are in the specification. | — | `#11-vm-regexp-constructor-and-flags` (umbrella) | T3 | — |
| **8a** | **RegExp flag accessors.** ⚠ **"plus the constructor surface" is withdrawn — the creation half is umbrella 8's derivation's to mint, and this row claiming it as well was double ownership.** Umbrella 8's rule is `webref heading ecma262 22.2` and it enumerates **§22.2.3** *Abstract Operations for RegExp Creation*, **§22.2.4** *The RegExp Constructor*, **§22.2.5** *Properties of the RegExp Constructor* and **§22.2.8** *Properties of RegExp Instances* by name, so creation and construction are already inside that derivation's output; a charter sentence here naming them too is the duplicated decision surface CLAUDE.md *One issue, one way* forbids. 8c's cell had already read this row as accessors-only ("8a's accessor charter") while this row's prose said otherwise — the table contradicting itself — and this resolves it in 8c's favour. Charter is **§22.2.6**'s accessor half and nothing else: `§22.2.6.3` `dotAll` … `§22.2.6.19` `unicodeSets`, the inventory read off `webref heading ecma262 22.2.6` — which interleaves the accessors with the `%Symbol.*%` entries rather than listing them contiguously, so it is read off that command and not off the endpoints — rather than off the probe list `.global` / `.ignoreCase` / `.multiline` / `.sticky` that seeded the row. What the accessors read is state the tree already stores: `git grep -n 'kind: ObjectKind::RegExp {' -- crates/script/elidex-js/src` gives `vm/dispatch_helpers.rs:176`, the `Constant::RegExp` arm of `load_constant`, which builds the object carrying `pattern`, `flags` and the compiled matcher. ⚠ **The `8d` edge goes with the creation half, not with this row; what replaces it in the `Deps` cell is that creation child, not a blank.** Every acceptance condition below is a **red probe** that reads the `RegExp` global or `%RegExp.prototype%`, and neither exists at `658cc302` — `typeof RegExp` is `"undefined"`, while regex *literals* compile and match, so the missing artifact is the intrinsic binding rather than the engine. Under §5's consumer rule the row records that derivation instead of an empty cell: the child umbrella 8's derivation mints for RegExp creation is what this row's entire acceptance set waits on, and placing that edge is a mandatory output of the derivation, an umbrella named in a `Deps` cell naming nobody. What consumes `%Symbol.match%` is **§22.2.4.1** `RegExp ( patternOrRegexp, flags )` step 1 `Let patternIsRegExp be ? IsRegExp(patternOrRegexp)` → **§7.2.6** IsRegExp step 2 `Let matcher be ? Get(arg, %Symbol.match%)` (`webref body ecma262 sec-regexp-pattern-flags` / `sec-isregexp`); the accessor half consumes none of it, and the **complement** is what is measured rather than the claim — `webref body ecma262 <anchor> \| grep -c -E -e 'IsRegExp' -e '%Symbol\.match%' -e 'RegExpCreate' -e 'RegExpAlloc' -e 'RegExpInitialize' -e 'Construct\('` returns **0** for **all twelve** §22.2.6 accessor and accessor-helper clauses (`sec-get-regexp.prototype.dotAll`, `.flags`, `sec-regexphasflag`, `.global`, `.hasIndices`, `.ignorecase`, `.multiline`, `.source`, `sec-escaperegexppattern`, `.sticky`, `.unicode`, `.unicodesets`) against **5** for `sec-regexp-pattern-flags` under the same pattern, so it discriminates. The edge therefore moves onto the child umbrella 8 mints for RegExp creation, per §5's row-kind rule, and every site that stated the 8a↔8d relationship is re-pointed with it. ⚠ **`const r = /x/; r[Symbol.match] = false; RegExp(r) === r` → false is likewise not this row's regression** — it turns false only when that creation child implements §22.2.4.1 step 1, and 8d's cell names that child. What `exec` *returns* is not this row's either — that is §22.2.7.2's algorithm and it is **8c**, so 8a retiring says nothing about it. ⚠ **Acceptance is stated over the two branches of `RegExpHasFlag` step 2, not over reading a flag off a regex** — `/g/.global === true` passes on the brand-check-then-read shape the accessors invite, so it separates nothing. ⚠ **And nothing that replaces it pins *which* flag each accessor reads**, so an implementation with two accessors' code units transposed — `global` reading `"y"` and `sticky` reading `"g"` — satisfies every stated condition, including the `flags` witness, because §22.2.6.4 `get flags` sets both source flags and appends `g` at step 7 and `y` at step 19 regardless. **The condition is therefore stated per accessor against a regexp carrying exactly one flag**, red at `658cc302`: for each of the eight, a regexp constructed with that flag alone answers `true` on its own accessor and `false` on the other seven — one assertion per accessor, so a transposition fails at both ends of the swap. **§22.2.6.4.1** `RegExpHasFlag` (`webref body ecma262 sec-regexphasflag`) step 2 splits the no-`[[OriginalFlags]]` case by **identity, not by prototype**: 2.a `SameValue(regexp, %RegExp.prototype%)` → return **undefined**, 2.b → **throw a TypeError**. The core pair is therefore `RegExp.prototype.global === undefined` (not `false`, not a throw) **together with** `Object.create(RegExp.prototype).global` throwing a TypeError — one prototype chain, opposite results, which is what kills an `instanceof`/prototype-walk accessor in **both** directions — asserted for all **eight** accessors whose body *is* `RegExpHasFlag`, each measured to contain it rather than assumed (`dotAll` §22.2.6.3, `global` §22.2.6.5, `hasIndices` §22.2.6.6, `ignoreCase`, `multiline`, `sticky`, `unicode`, `unicodeSets` — one `RegExpHasFlag` hit each under `webref body ecma262 sec-get-regexp.prototype.<name>`, the anchor spelling being mixed-case for `dotAll`/`hasIndices` and lower-case for the rest, so a run that reports 0 is an anchor error and not a measurement). **§22.2.6.4** `get flags` is a second separator precisely because it is **not** `RegExpHasFlag`: its steps 4–19 are eight `Get(regexp, …)` reads in the fixed order d g i m s u v y, so `new RegExp('x', 'yg').flags === 'gy'` (an implementation returning `[[OriginalFlags]]` verbatim yields `'yg'`) and — spelled over an *ordinary* receiver, not over `Object.create(RegExp.prototype)` and not over `Object.create`'s second argument — `Object.getOwnPropertyDescriptor(RegExp.prototype, 'flags').get.call({ global: true, dotAll: true }) === 'gs'` (a slot read yields `''` or throws). ⚠ **The `Object.create(RegExp.prototype)` receiver that stood here fails a *correct* implementation too — the second time in this cell that the receiver, not the assertion, is what broke.** §22.2.6.4 **step 4** reads `hasIndices` *first*, an `Object.create(RegExp.prototype)` receiver inherits that accessor, and **§22.2.6.4.1** RegExpHasFlag step 2.b then throws: measured, Node answers `TypeError: RegExp.prototype.hasIndices getter called on non-RegExp object`, while an implementation carrying this row's own named defect (2.b answering `undefined` instead of throwing) returns exactly `'gs'` — satisfied by the defect and failed by the remedy. A plain `{}` receiver inherits no accessor, so each of the eight `Get`s reads a data property or `undefined` and Node answers `'gs'` (`webref body ecma262 sec-get-regexp.prototype.flags`), §22.2.6.4 `get flags` reading each of its eight through `Get` and applying no brand check. ⚠ **The `Object.create(RegExp.prototype, { global: …, dotAll: … })` spelling that stood here fails a *correct* implementation of this row.** `native_object_create` (`crates/script/elidex-js/src/vm/natives_object/prototype.rs:12-34` at `658cc302`) reads `args.first()` at `:17` and never touches `args[1]` — `Object.getOwnPropertyNames(Object.create(null, { a: { value: 1 } })).length` is `0` — and there is no fallback: `git grep -c '"defineProperties"' 658cc302 -- crates/script/elidex-js/src` matches nothing, rc=1, against the control `git grep -c '"defineProperty"' 658cc302 -- crates/script/elidex-js/src` → `globals.rs:1`. The produced object therefore carries no own `global`/`dotAll`, its inherited accessors take §22.2.6.4.1 step 2.b and **throw**, and the witness cannot yield `'gs'` however correctly the accessors are written. `Object.defineProperty` alone is live at `vm/globals.rs:1386-1387`; **§20.1.2.2** `Object.create` step 3's `ObjectDefineProperties` (**§20.1.2.3.1**) is unimplemented and owned by no row in §5 or §8, so this row spells no witness on it. **§22.2.6.13** `get source` carries its own pair, and ⚠ **the pair as it stood is passed by an accessor that returns the constant `'(?:)'`**: that value satisfies step 3.a's `RegExp.prototype.source === '(?:)'` and is `!== '/'`, so both checks hold on an implementation that loses every nonempty pattern. The **preservation** case is what separates them, and it is asserted behaviourally rather than by string equality, so that no `eval` is needed to run it: `const q = new RegExp(new RegExp('a+').source)` must give `q.test('aaa') === true` **and** `q.test('b') === false`, where the constant accessor yields `new RegExp('(?:)')` and answers `true` to both; and the same round trip over `new RegExp('/')` must match `'/'` and **not** `'a'`, which the constant accessor again fails on the second. ⚠ **An *exact* escaped result cannot be required for the slash case, and requiring one would assert something §22.2.6.13.1 does not say**: `EscapeRegExpPattern` step 5 gives `"\/"` and `"\u002F"` as what escapedPattern *could* be, "among other possibilities, but not `"/"`", and step 4 requires only that the result be a `Pattern` whose Abstract Closure behaves identically to the constructed object's `[[RegExpMatcher]]` (`webref body ecma262 sec-escaperegexppattern`). So `new RegExp('/').source !== '/'` is kept as the one exact consequence step 5 does fix — `'/' + source + '/'` must re-parse as a *RegularExpressionLiteral* rather than lex as a comment — and the behavioural round trip carries what an exact string cannot. Every TypeError above is a rejection, so the `undefined` / `'(?:)'` / `'gs'` results are the positive control an over-throwing accessor cannot pass | `vm/natives_regexp.rs`, `vm/globals_primitives.rs` | `#11-vm-regexp-constructor-and-flags` (8a) | T3 | **the child umbrella 8's derivation mints for RegExp creation** (§22.2.3-§22.2.5 — every accessor witness this row states reads the `RegExp` global or `%RegExp.prototype%`, and `typeof RegExp` is `"undefined"` at `658cc302`; naming umbrella 8 itself would name nobody, so placing this edge is a mandatory output of its derivation) |
| **8d** | **The `@@match` / `@@replace` / `@@search` / `@@split` / `@@matchAll` symbol ids** — allocation in `WellKnownSymbols` **and their installation as `Symbol` properties** — no dispatch and no constructor work. ⚠ **The two halves cannot be separated without making this row's own acceptance unsatisfiable**: `register_symbol_global`'s `well_known_props` is a hard-coded 7-entry array (`vm/globals_primitives.rs:259-269` at `658cc302`, holding `iterator` / `asyncIterator` / `hasInstance` / `toPrimitive` / `toStringTag` / `species` / `isConcatSpreadable`), so allocating an id leaves `Symbol.match` `undefined` and the expression below unwritable. **This row exists because the id is a *shared prerequisite of more than one family*, and leaving it inside 8b made the family cyclic.** ⚠ **The cycle ran through 8a's constructor and no longer does — 8a is accessors-only from this revision — so that argument is withdrawn and re-derived through the creation half, which is where §22.2.4.1 actually lives.** The child umbrella 8 mints for RegExp creation cannot be written without the `%Symbol.match%` id (ECMA-262 **§22.2.4.1** step 1 `IsRegExp(patternOrRegexp)` → **§7.2.6** step 2 `Get(arg, %Symbol.match%)`, `webref body ecma262 sec-regexp-pattern-flags` / `sec-isregexp`), while two of umbrella 8b's children construct *through* that creation half — **§22.2.6.9** `%Symbol.matchAll%` step 4 `Let speciesCtor be ? SpeciesConstructor(regexp, %RegExp%)` and step 6 `Let matcher be ? Construct(speciesCtor, « regexp, flags »)`, and **§22.2.6.14** `%Symbol.split%` steps 4 and 10 in the same shape (**§7.3.22** from `webref aoid ecma262 SpeciesConstructor`; steps from `webref body ecma262 sec-regexp-prototype-%symbol.matchall%` / `sec-regexp.prototype-%symbol.split%`) — so ids left inside 8b would give `8b`-child → creation child → `8b`-child. The complement is measured: of the five `%Symbol.*%` methods, `grep -c 'Construct('` over the same command returns **1** for those two and **0** for `sec-regexp.prototype-%symbol.match%` / `-%symbol.replace%` / `-%symbol.search%`, so exactly two carry the edge. Umbrella **9b**'s children are a third consumer, on their own walk. Measured at `658cc302`: `WellKnownSymbols` (`vm/well_known.rs:1552-1585`) allocates **7** symbols and none of these five, so no consumer can be written first. ⚠ **Acceptance is what the observable *installation* buys, and stops there — stated over the result, never over five names resolving** (§5's head). `typeof Symbol.match === 'symbol'` ×5 passes on an implementation that allocates **one** id and installs it five times, which is exactly the failure the allocation half can have. So: the five are **pairwise distinct** (all ten `Symbol.match !== Symbol.replace`-shaped pairs — `Set` is inside umbrella 7a's charter, minted onto none of its children yet and so not available here); each carries its Table 1 description (`Symbol.match.toString() === 'Symbol(Symbol.match)'`, `Symbol.prototype.toString` at `vm/globals_primitives.rs:209`); and each sits on `Symbol` with the attributes ECMA-262 **§20.4.2.9** states — `{ [[Writable]]: false, [[Enumerable]]: false, [[Configurable]]: false }` (`webref body ecma262 sec-symbol.match`) — read through `Object.getOwnPropertyDescriptor(Symbol, 'match')` (`vm/globals.rs:1392`). Using one as a key on a user object is the reachability witness, not the acceptance — ⚠ **and with it ruled out, every stated condition is satisfied by five symbols minted anywhere**, so allocating them at `Symbol`-global install time in `vm/globals_primitives.rs` without touching `WellKnownSymbols::alloc_all` (`vm/well_known.rs:1552-1585`, this row's only module entry) passes the distinctness checks, the descriptions and the descriptor triple while the three consumer families still read a `well_known` table that has none of the five. **§5's prerequisite rule already licenses the fix** — where the artifact has no JS-reachable holder yet, the observable-result rule is satisfied against the slot from Rust — so the condition is that each of the five ids is readable from `WellKnownSymbols` by the same accessor the seven existing ones use, asserted from `vm/tests/`, and that the `Symbol` own property for each resolves to *that* id rather than to an equal-described symbol. `const r = /x/; r[Symbol.match] = false; RegExp(r) === r` → **false** is the regression of **the child umbrella 8 mints for RegExp creation** — not this row's, and not 8a's — since it turns false only once that child implements §22.2.4.1 step 1, and that child depends on this row | `vm/well_known.rs` | `#11-vm-regexp-constructor-and-flags` (8d) | T3 | — |
| **8b** | **Well-known-symbol dispatch for the String↔RegExp protocols — UMBRELLA, not a terminal unit.** ⚠ **This row was terminal, and its own account of its dependencies is what ends that.** The charter bundles the String-side `GetMethod` entry with **all five** §22.2.6 `%Symbol.*%` algorithms, and by this row's own measurements their inputs differ: **only** `%Symbol.replace%` consumes 8c's named-capture result (**§22.2.6.11** step 15.j), **four of the five** consume 8a's `flags` accessor and `%Symbol.search%` consumes neither, and the five are five invariant axes that meet at the one String-side `GetMethod` entry this charter also bundles and each fail while the others pass — `%Symbol.matchAll%` additionally constructing an iterator, `%Symbol.split%` carrying its own limit and sticky-flag handling. ⚠ **"Three of those RegExp-side methods read the `flags` accessor" was an undercount, and the omitted one is the one that would have been minted with no edge**: `webref body ecma262 sec-regexp-prototype-%symbol.matchall%` gives **§22.2.6.9** step 5 as `Let flags be ? ToString(? Get(regexp, "flags"))`, alongside **§22.2.6.8** step 4, **§22.2.6.11** step 7 and **§22.2.6.14** step 5 (same command over `sec-regexp.prototype-%symbol.match%` / `-%symbol.replace%` / `-%symbol.split%`); the one method with no `flags` read is **§22.2.6.12** `%Symbol.search%` (same command, zero matches for `flags`). The inventory is a command rather than a recollection: `webref heading ecma262 22.2.6` returns §22.2.6.8 `%Symbol.match%`, §22.2.6.9 `%Symbol.matchAll%`, §22.2.6.11 `%Symbol.replace%`, §22.2.6.12 `%Symbol.search%` and §22.2.6.14 `%Symbol.split%` as the `%Symbol.*%` entries, interleaved with the accessors 8a takes. ⚠ **The counter-argument is that all five are reached the same way — `GetMethod` on a well-known symbol — and §5's head already answers it**: that is a shared dispatch **idiom**, not a shared lowering **chokepoint**, and a group can be well-formed as a spec unit while holding mechanisms with no chokepoint between them (the `Object` demonstration, stated once for the whole table and not restated here). A `%Symbol.replace%` regression says nothing about whether `split`'s limit handling or `matchAll`'s iterator is built, so the five fail independently. ⇒ umbrella. **Attaching the 8a and 8c edges, and each acceptance condition, to only the children that consume them is a mandatory output of this umbrella's derivation** — on the umbrella they are either stranded, because nothing transfers an umbrella's `Deps` to a generated child, or they over-order the child that reads no `flags` and the four that read no `groups`. **8d is the contrast that fixes the shape**: every child dispatching on a `%Symbol.*%` id consumes 8d's allocation, so that edge goes on all of them — placed by the derivation, not by this row. ⚠ **And a fourth edge, onto a producer outside this family**: **§22.2.6.9** `%Symbol.matchAll%` and **§22.2.6.14** `%Symbol.split%` each take `Let speciesCtor be ? SpeciesConstructor(regexp, %RegExp%)` at step 4 and then `Construct(speciesCtor, …)` at step 6 and step 10 respectively (**§7.3.22** from `webref aoid ecma262 SpeciesConstructor`; steps from `webref body ecma262 sec-regexp-prototype-%symbol.matchall%` / `sec-regexp.prototype-%symbol.split%`), while the other three take no `Construct` at all — `grep -c 'Construct('` over the same command returns **0** for `sec-regexp.prototype-%symbol.match%` / `-%symbol.replace%` / `-%symbol.search%` against **1** for each of the two, so it discriminates. Those two children therefore consume the child umbrella **8** mints for RegExp creation; the other three do not, and the derivation places it on the two. **And the children umbrella 9b mints for `matchAll` and `replaceAll` consume the String-side `GetMethod` child of this umbrella — child to child, since naming either umbrella names nobody**: placing that edge is a mandatory output of both derivations, since those two members reach the protocol through the String entry rather than through any one RegExp-side algorithm (9b's row states the reason). Everything below is evidence for the derivation to route, not a charter. Measured at `658cc302`: `WellKnownSymbols` (`vm/well_known.rs:1552-1585`) allocates **7** symbols — `iterator`, `asyncIterator`, `hasInstance`, `toPrimitive`, `toStringTag`, `species`, `isConcatSpreadable` — so `@@match`/`@@replace`/`@@search`/`@@split`/`@@matchAll` cannot even be *installed*, let alone dispatched; and the consumers bypass the protocol structurally: `native_string_search` (`natives_string.rs:633`) tests `ObjectKind::RegExp` directly at `:665` and otherwise `ToString`-coerces, while `native_string_split` (`:350`) `ToString`-coerces the separator at `:356` before any lookup. ECMA-262 **§22.1.3.21** `String.prototype.search` step 3.a is `GetMethod(regexpOrPattern, %Symbol.search%)` with the `ToString` only at step 4, and step 6 is `Invoke(regexp, %Symbol.search%, « string »)`; **§22.1.3.23** `split` is the same shape (`GetMethod(separator, %Symbol.split%)` at step 3.a, `ToString(separator)` at step 6); **§22.1.3.14** `matchAll` at step 3.c. The RegExp-side implementations are §22.2.6.8 / .9 / .11 / .12 / .14. The `GetMethod`-based dispatch, 8d's ids consumed rather than allocated, and the RegExp-side methods each entry then reaches are what the children divide between them. **The `8a` artifact** is the `flags` accessor **§22.2.6.4** `get RegExp.prototype.flags`, in 8a's accessor half (the accessor's number from `webref heading ecma262 22.2.6`). **The `8c` artifact**, whose edge was previously undrawn because it crosses a family line, is `groups` — 8c's sole deliverable — read at §22.2.6.11 **step 15.j** `Let namedCaptures be ? Get(result, "groups")`, the locator read out of `webref body ecma262 sec-regexp.prototype-%symbol.replace%` counting nesting depth, where the *top-level* step 10 is `Let results be a new empty List` and step 15 is `For each element result of results, do`. Both stood in this row's `Deps` cell while it was terminal; on an umbrella they name nobody, so each is now an edge for the derivation to place on the children measured above as consuming it — and the `8c` one is why a `%Symbol.replace%` child that retires without it ships named-capture substitution silently dead | — | `#11-vm-regexp-constructor-and-flags` (8b umbrella) | T3 | — |
| **8c** | **Named-capture result construction** — the first output of the §22.2.7 clause the umbrella's rule gained at PR-B. ECMA-262 **§22.2.7.2** RegExpBuiltinExec step 30 creates the `groups` object when the pattern contains a `GroupName` (step 31 leaves it `undefined` otherwise), step 32 defines it on the result array, and step 34.5 defines each matched `GroupName` on it. At `658cc302` the result array stops before that: `native_regexp_exec` (`vm/natives_regexp.rs:93`) defines `index` at `:128` and `input` at `:136` through `ctx.vm.well_known.*` and returns at `:144`, while `git show 658cc302:crates/script/elidex-js/src/vm/natives_regexp.rs \| grep -c '"groups"'` → **0** and `grep -c 'groups' crates/script/elidex-js/src/vm/well_known.rs` → **0** — neither of the two spellings the file uses for the other properties defines this one, so a named-capture read off the result resolves to nothing. **Minted rather than folded into 8a, per §5's terminality criterion**: the name→index pairing step 34.5 consumes does not exist below the JS surface either — `grep -rn 'GroupKind::Named' crates/script/elidex-js/src` returns **2** lines, `regexp/parser.rs:450` producing the variant and `regexp/tests.rs:110` asserting it, and `NamedBackreference` reaches only its declaration (`regexp/mod.rs:46`) and its producer (`regexp/escape.rs:80`), so the name does not leave the parser. That puts it outside 8a's accessor charter and on a producer of its own — the name→index pairing does not leave `regexp/` at all — so umbrella **8**'s derivation mints it rather than 8a absorbing it. Deliverables: the name→capture-index pairing carried through to exec, the `groups` object per those steps, and regressions over it. ⚠ **"a named capture is readable" cannot fail on the pairing, which is this row's own deliverable**: step 34.5.3.3's `CreateDataPropertyOrThrow(groups, groupName, capturedValue)` sits inside step 34's per-*i* loop, so `capturedValue` is the *i*th capture — while an implementation that builds `groups` by mapping every `GroupName` onto capture 1 satisfies a one-group assertion exactly, with no pairing at all. So the regression takes **≥2 named groups with distinct captures, each asserted separately — ⚠ **and both names in that pattern are also its only capturing groups, so the name-to-capture-*index* mapping §22.2.7.2 step 34.5 requires is still untested**: building `groups` by mapping the k-th declared `GroupName` onto capture k passes it, since y and m happen to be captures 1 and 2. **Third condition, red under the ordinal map**: `/(\d)(?<m>\d)/.exec('12').groups.m` is `'2'` — step 34 iterates `1 ≤ i ≤ capturingGroupsCount` and 34.5 fires when the *i*th capture was defined with a `GroupName`, so an unnamed group ahead of a named one shifts the index and the ordinal map answers `'1'`. **Polarity**: all three are red probes at `658cc302`, where `Object.getOwnPropertyNames(/(\d)/.exec('1'))` is `0`, `index` and `input` with no `groups`**: `/(?<y>\d{4})-(?<m>\d{2})/.exec('2026-08')` must give `groups.y === '2026'` **and** `groups.m === '08'`, where the name→first-capture map answers `'2026'` to both — plus step 31's complement. ⚠ **`/(\d)/.exec('1').groups === undefined` stood as that complement and is satisfied by the baseline, where the property is absent entirely.** Step 32 is `Perform ! CreateDataPropertyOrThrow(array, "groups", groups)` and sits **outside** the step 30 / step 31 branch, so the own property is created on *every* ordinary match and merely holds `undefined` in the step 31 case (`webref body ecma262 sec-regexpbuiltinexec`). The complement is therefore own-property **presence** carrying the `CreateDataProperty` attributes: for `const r = /(\d)/.exec('1')`, `Object.getOwnPropertyNames(r).includes('groups')` is `true`, `r.groups === undefined`, and `Object.getOwnPropertyDescriptor(r, 'groups')` is `{ value: undefined, writable: true, enumerable: true, configurable: true }` — both accessors registered at `658cc302` (`vm/globals.rs:1392` and `:1395`). The baseline fails presence outright and an "only when the pattern has named groups" implementation fails it on exactly this pattern, while the value-equality form passes on both. ⚠ **And two properties of the object step 30 *creates*, which every assertion above passes while getting wrong.** Step 30.1 is `Let groups be OrdinaryObjectCreate(null)`, so the object has a **null** prototype: `Object.getPrototypeOf(/(?<a>x)/.exec('x').groups) === null`, where a plain `{}` answers every named read identically and leaks `%Object.prototype%` into a namespace whose keys are user-chosen. And step 34.5.3.3's `CreateDataPropertyOrThrow(groups, groupName, capturedValue)` runs for every declared `GroupName`, `capturedValue` being `undefined` when step 34.2 finds the capture undefined — so a group that did **not** participate is still an own property: `'a' in /(?<a>x)\|(?<b>y)/.exec('y').groups` must be `true`, where an implementation defining only matched groups answers `false` and still satisfies the two-group pairing above (`webref body ecma262 sec-regexpbuiltinexec`) [§10](2026-07-vm-p4-umbrella-review-rounds.md)'s disposition table routes *RegExp named-groups* to "Slice 8", but a round record cannot own work, because nothing re-reads it at re-eval time ([§10](2026-07-vm-p4-umbrella-review-rounds.md)'s own preamble) — this row is the owner | `vm/natives_regexp.rs`, `regexp/` | `#11-vm-regexp-constructor-and-flags` (8c) | T3 | — |
| **9** | **Builtin prototype/static surface — UMBRELLA, not a terminal unit.** The charter as written spanned the Array / String / Object prototype-and-static surface, plus iterator-protocol semantics (`Object.fromEntries`), plus RegExp-argument dispatch inside string methods (`matchAll` / `replaceAll`) — intersecting invariant axes that the CLAUDE.md *Edge-dense work* rule forbids in one PR. **Its scope is a derivation rule, and running that derivation is its first step** — not the sub-slice rows below. The rule, **widened again here because two successive wordings were each under-scoped**, and it is now a **user-reachable** rule rather than a global-rooted one: *for every intrinsic object a program can obtain — by reading a global property, by evaluating syntax that produces it, or by reading a property of a value it already holds — the whole property surface ECMA-262's main body defines on it* — **minus Annex B (I-6)**, **minus any surface another slot already owns**. What puts a surface inside the rule is the spec clause that defines properties on such an object, so the rule reads the clause and never the object's shape, nor the route by which a program reached it. Each wording failed on the axis it did not name: "constructor / prototype surface" could not reach an object that is neither, which is how `Math` sat unowned through ten rounds (measurement **(4)** in the §8 row), and "reachable from the global object" could not reach an intrinsic that is never a global property, which is measurement **(5)**. ⚠ **And the global object is a seed in its own right, because the rule quantifies over intrinsics and the global object is not one.** A Realm Record records `[[Intrinsics]]` and `[[GlobalObject]]` as **separate** fields (**§9.3** *Realms*, Table 20 — `webref body ecma262 sec-code-realms`), and `globalThis` is absent from the well-known-intrinsic table (`webref body ecma262 sec-well-known-intrinsic-objects`, over which `grep -ci globalThis` → **0** against `%eval%`/`%parseInt%` → **2**), so obtaining the global object by reading `globalThis` does not put *its own* property surface inside the rule. What defines that surface is **§19** *The Global Object* — **§19.1** *Value Properties*, **§19.2** *Function Properties*, **§19.3** *Constructor Properties*, **§19.4** *Other Properties* (`webref heading ecma262 19`) — which **§9.3.3** SetDefaultGlobalBindings step 2 installs as a clause reference ("For each property of the Global Object specified in clause 19"), its step 2.b drawing the values of §19.2 / §19.3 / §19.4 from the realm's intrinsics and leaving §19.1 with no intrinsic at all (`webref body ecma262 sec-setdefaultglobalbindings`). So the rule as written enumerates `Math` and `JSON` (**§19.4.3** / **§19.4.2**) as intrinsics while never auditing the value, function and constructor properties clause 19 defines on the global object itself, and the separately carved `Function`/`eval` row at the foot of this table covers **two** of them (**§19.3.18** / **§19.2.1**), not the clause. **§19 is therefore a seed of this rule**, under the same two subtractions the rule already carries — minus Annex B per I-6, minus any surface another slot owns (`Atomics` **§19.4.1** to the child umbrella `#11-vm-atomics-global`'s derivation mints for it, `Reflect` **§19.4.4** to the child umbrella 10a's derivation mints for it). The umbrella runs it; what the rule returns is a set of **groupings**, and terminal sub-slices are minted from them by applying the terminality criterion at the head of this section — not one per family, for the reason and with the `Object` demonstration stated there. Each minted sub-slice is its own PR under its own mandatory `/elidex-plan-review`; this umbrella is the approved parent that makes each of them a base case. ⚠ **Edition-agnostic charter.** The unit is the *property surface ECMA-262's main body defines on a builtin object*, not an ECMAScript edition: the ES2021-2024 probe list is the **seed that found the class, not the boundary**. ⚠ **Minus Annex B (I-6)**, program-wide across every sub-slice: "derive a complete builtin inventory from the spec" executed literally pulls in §B.2.1 Additional Properties of the Global Object and §B.2.2 Additional Properties of the String.prototype Object (`webref heading ecma262 B.2.1` / `B.2.2`), plus `Date.prototype.getYear`/`setYear` and `RegExp.prototype.compile` — LegacySemantics compat territory, which §4 I-6 forbids program-wide. The inventory is the **main-body** surface. ⚠ **The `{1n:…}` key fix is NOT here.** It is not builtin work: the arm sits inside `expr_object.rs`'s `PropertyKind::Init` key match, so it belongs to `#11-vm-property-key-lowering-unification` (§8), whose kind is UNDETERMINED and which mints no child until its measurement is made (§2.2, §8). ⚠ **An ordinary function's own `name` and `length` are NOT here either.** The rule above reads the clauses that define properties *on an intrinsic object*; **§10.2.9** SetFunctionName and **§10.2.10** SetFunctionLength define these on every function syntax creates, reached from **§10.2.3** OrdinaryFunctionCreate step 22 and **§15.2.4** / **§15.2.5** — outside the rule's reach by construction, so this umbrella can retire with its inventory complete and `f.name` still absent. the children `#11-vm-ordinary-function-name-length`'s derivation mints own them (§8) | — | **new** `#11-vm-builtin-prototype-static-sweep` (umbrella) | T3 | — |
| **9a** | **`Array` prototype/static surface — UMBRELLA, not a terminal unit.** ⚠ **Found by re-deriving the verdict for every row in this table in one pass, rather than one row per review round — it is 9c's failure with `Array.from` in `Object.fromEntries`'s place.** The row's own content is two clauses, **§23.1.2** (the statics) and **§23.1.3** (the prototype): **§23.1.2.1** `Array.from` consumes the iterator protocol and closes it on the error path (the measurement below, and the reason for the `Pa` obligation), while the §23.1.3 members that seeded the row — `at` / `findLast` / `findLastIndex` / `toSorted` / `toReversed` / `toSpliced` / `with` — reach no iterator at all and share no lowering chokepoint with it. That is exactly the bundling §5's head rules out when it says one PR for the `Object` family "bundles an iterator-protocol contract with a member that has none", and the §23.1.2/§23.1.3 walk therefore returns a **grouping** from which terminal children are minted at this row's own start; **placing the `Pa` obligation and each acceptance condition on the child that carries the drain is a mandatory output of that derivation** (§5's row-kind rule), and this row carries none of the three. Seeded by the §1.2 absence probe (`at` / `findLast` / `findLastIndex` / `toSorted` / `toReversed` / `toSpliced` / `with`); derives its own inventory at its start from ECMA-262 **§23.1.2** (Properties of the Array Constructor = the statics) and **§23.1.3** (Properties of the Array Prototype Object), rather than from that probe; both numbers and titles from `webref heading ecma262 23.1`, which also lists §23.1.1 *The Array Constructor*, §23.1.4 *Properties of Array Instances* and §23.1.5 *Array Iterator Objects* — the call/construct behaviour, the exotic `length` and the iterator object, none of them the *prototype/static property surface* this row is cut on, and each therefore an output for umbrella 9's rule to route rather than a silent omission from this seed. **The `Pa` obligation, for the reason 9c's cell carries**: **§23.1.2.1** `Array.from` closes the iterator on the error path — step 5.e.i.2 is `Return ? IteratorClose(iteratorRecord, error)` with `error` a ThrowCompletion — and its steps 5.e.v.2 and 5.e.viii are `IfAbruptCloseIterator` (`webref body ecma262 sec-array.from`, over which `grep -c IteratorClose` → **1** and `grep -c IfAbruptCloseIterator` → **2**). Same §7.4.11 abrupt-completion contract, so a drain written against today's `iter_close` inherits the inverted precedence here too. ⚠ **And the §23.1.2 walk reaches a *second* row's artifact, which no edge recorded.** `webref heading ecma262 23.1.2` lists **§23.1.2.2** `Array.fromAsync ( items [ , mapper [ , thisArg ] ] )` beside `Array.from`, and its sync-iterable branch is `Set iteratorRecord to CreateAsyncFromSyncIterator(? GetIteratorFromMethod(items, usingSyncIterator))` (`webref body ecma262 sec-array.fromasync`) — **§27.1.5.1** (`webref aoid ecma262 CreateAsyncFromSyncIterator`), which is **6b**'s adapter and its sole runtime deliverable. So the child that carries `Array.fromAsync` consumes 6b rather than building a second sync→async adapter, and **placing that edge on it is a mandatory output of this row's derivation**, beside the `Pa` one. Two further outputs from the same clause: its abrupt paths are `AsyncIteratorClose` / `IfAbruptCloseAsyncIterator`, i.e. **§7.4.15** rather than the §7.4.11 the `Pa` obligation above is stated over, so that obligation does not cover this member — what does is the child of umbrella `P` that owns **§7.4.15**, which P's row now records as inside its charter and un-minted, so an edge onto that child is an output of this row's derivation on the same reading as the `Pa` one; and its per-element `Set nextResult to ? Await(nextResult)` runs the **§27.5.4.7.1** PromiseResolve → **§27.5.1.3** CreateResolvingFunctions chain 6b's and R's cells already trace onto **9da**, so an edge onto the child umbrella **9da**'s derivation mints for thenable assimilation is an output of this derivation on the same reading | — | `#11-vm-builtin-prototype-static-sweep` (9a umbrella) | T3 | — |
| **9b** | **`String` prototype surface — UMBRELLA, not a terminal unit.** ⚠ **The row read terminal off a one-line charter, and it is the terminality verdict rather than the grouping that fails — it is `Object.fromEntries` against `Object.hasOwn` with the names changed, the demonstration §5's head states this whole table by.** Its §22.1.3 walk returns **36** members — `.claude/tools/webref heading ecma262 22.1.3 \| grep -cE '^\s+§22\.1\.3\.[0-9]+\s'` → **36**, against **6** for the nested-AO control `'^\s+§22\.1\.3\.[0-9]+\.[0-9]+\s'` over the same output, so the pattern separates a member from an AO defined beneath one — and they share no lowering chokepoint. Three of them are the demonstration: **§22.1.3.15** `String.prototype.normalize` is eight steps whose step 7 is "Let normal be the String value that is the result of normalizing string into the normalization form named by form as specified in the latest Unicode Standard, Normalization Forms" — a Unicode-data algorithm reaching no iterator, no `%Symbol.*%` and no RegExp; **§22.1.3.14** `String.prototype.matchAll` reaches `Let isRegexp be ? IsRegExp(regexpOrPattern)` (step 3.a), `Let matcher be ? GetMethod(regexpOrPattern, %Symbol.matchAll%)` (step 3.c) and `Let regexp be ? RegExpCreate(regexpOrPattern, "g")` (step 5); and **§22.1.3.36** `String.prototype [ %Symbol.iterator% ]` builds an iterator object, its step 5 being `Return CreateIteratorFromClosure(closure, "%StringIteratorPrototype%", %StringIteratorPrototype%)` (steps from `webref body ecma262 sec-string.prototype.normalize` / `sec-string.prototype.matchall` / `sec-string.prototype-%symbol.iterator%`; §-numbers and titles from `webref heading ecma262 22.1.3`). A `normalize` regression says nothing about whether the iterator object exists or whether a user `@@matchAll` is honoured, and none of the three fails when another does ⇒ umbrella. The §22.1.3 walk therefore returns a **grouping** from which terminal children are minted at this row's own start, and **placing every obligation, every edge and every acceptance condition below on the specific child that consumes it is a mandatory output of that derivation** (§5's row-kind rule); this row carries none of the three. ⚠ **The seed is §22.1.3 *plus* §22.1.5 *String Iterator Objects*, and the second is a clause no walk over §22.1.3's own members returns.** §22.1.3.36 hands `%StringIteratorPrototype%` to **§27.8.3.8** CreateIteratorFromClosure as an *argument* (`webref aoid ecma262 CreateIteratorFromClosure`), so the walk leaves §22.1 and never arrives at the clause that defines that object's own properties — **§22.1.5.1** *The %StringIteratorPrototype% Object*, **§22.1.5.1.1** `%StringIteratorPrototype%.next ( )` and **§22.1.5.1.2** `%StringIteratorPrototype% [ %Symbol.toStringTag% ]` (`webref heading ecma262 22.1.5`). The **complement** is what is measured, not the claim: over all **36** member anchors `webref body ecma262 <anchor> \| grep -c 'StringIteratorPrototype'` is nonzero for exactly **1** — the `%Symbol.iterator%` member itself, and there as an argument — against **27** of the same 36 for the control `grep -c 'ToString'`, so the scan is live and the clause is reached by nobody. The contrast that shows this is a String-side gap rather than a general one is the RegExp side, where umbrella 8's rule *does* reach its iterator clause through a named AO: `webref aoid ecma262 CreateRegExpStringIterator` returns **§22.2.9.1**, while `webref aoid ecma262 CreateStringIterator` returns "no AO with aoid='CreateStringIterator' in ecma262" — there is no String-side counterpart to walk to. Left out of the seed, a child ships `[%Symbol.iterator%]` with the prototype it names minted by nobody, which is the one-directional silent failure §5's head describes. Everything below is evidence for the derivation to route, not a charter. Named deliverables: `String.prototype.matchAll` (absent) and `String.prototype.replaceAll`'s RegExp behaviour — measurements in the §8 slot row, which is their permanent home. **The `natives_string.rs` §21.1.3 → §22.1.3 citation retag** (§21.1.3 is *Properties of the Number Prototype Object*; the String prototype is §22.1.3 — both verified with `webref heading ecma262`) sits under this row because this file is outside umbrella P's charter; it is file-wide rather than member-shaped, so it decomposes along no grouping this walk returns, and the derivation places it on **whichever child it mints lands first with `vm/natives_string.rs` in its touch set** — landing is what serialises, the §5 ordering the 2a family's field-initializer prerequisite already uses for the same reason. ⚠ **And that device is the one row K was minted to replace, so this umbrella's derivation rules on it rather than inheriting it**: "whichever lands first" ordered nobody there and the fix was a prerequisite row of its own, so the derivation either mints one for the file-wide retag or records why landing order suffices here. ⚠ **The `Deps` cell read `8b` while that row was terminal, and an umbrella named there names nobody**: `matchAll` is ECMA-262 §22.1.3.14, whose step 3.c is `GetMethod(regexpOrPattern, %Symbol.matchAll%)`, and `replaceAll`'s RegExp behaviour is §22.1.3.20 — both consume the well-known-symbol dispatch the child of umbrella 8b that owns the String-side `GetMethod` entry delivers (the *dispatch*; the ids are 8d's), so implementing them first would either re-open-code the `ObjectKind::RegExp` special-casing umbrella 8b's children exist to remove or ship a `matchAll` that ignores a user `@@matchAll`. Both ends of that edge are now un-minted, so what gets placed is a child-to-child edge — the child of umbrella 8b that owns the String-side `GetMethod` entry onto the child of this umbrella that carries the member — and placing it, then re-pointing it once both are minted, is a mandatory output of both derivations, which 8b's row records by name. ⚠ **And an `8d` edge, found by walking this row's own seed rather than its dependants: both named deliverables read a `%Symbol.*%` id *before* any dispatch happens.** **§22.1.3.14** `String.prototype.matchAll` reaches `Let isRegexp be ? IsRegExp(regexpOrPattern)` inside its `If regexpOrPattern is an Object` branch, and **§22.1.3.20** `String.prototype.replaceAll` reaches the same AO on its own `searchValue`; **§7.2.6** IsRegExp then performs `Let matcher be ? Get(arg, %Symbol.match%)` before it ever looks for a `[[RegExpMatcher]]` slot, and `replaceAll` goes on to `Let replacer be ? GetMethod(searchValue, %Symbol.replace%)` — two of 8d's five ids, neither of them 8b's dispatch (§-numbers from `webref heading ecma262 22.1.3.14 --exact` / `22.1.3.20 --exact` and `webref aoid ecma262 IsRegExp`; steps from `webref body ecma262 sec-string.prototype.matchall` / `sec-string.prototype.replaceall` / `sec-isregexp`). With the ids unallocated `IsRegExp` cannot be written at all, so a child landing first re-opens the `ObjectKind::RegExp` branding umbrella 8b's children exist to remove, under two more member names. 8d is **terminal**, so the derivation places that edge directly on the two children that carry those members rather than routing it through 8b. ⚠ **And a RegExp-*creation* edge from the same walk, which was left unplaced a round earlier on the doubt that RegExp creation already exists in tree — it does not, in the form this step needs.** `matchAll`'s fallback is not the `%Symbol.matchAll%` dispatch above but a RegExp *created from a runtime value*: **§22.1.3.14** step 5 is "Let regexp be ? RegExpCreate(regexpOrPattern, "g")", and **§22.2.3.1** RegExpCreate is two steps — "Let obj be ! RegExpAlloc(%RegExp%)" then "Return ? RegExpInitialize(obj, pattern, flags)" — which are step 7 and step 8 of **§22.2.4.1** `RegExp ( patternOrRegexp, flags )` verbatim, entered with `%RegExp%` in place of NewTarget and past that clause's steps 1-6. The two AOs it is composed of live in **§22.2.3** *Abstract Operations for RegExp Creation*, listed there as §22.2.3.2 RegExpAlloc and §22.2.3.3 RegExpInitialize (§-number and title from `webref aoid ecma262 RegExpCreate` and `webref heading ecma262 22.2.3` / `22.2.4`; steps from `webref body ecma262 sec-string.prototype.matchall` / `sec-regexpcreate` / `sec-regexp-pattern-flags`). ⚠ **That edge was written onto `8a`, and it is re-pointed at this revision**: 8a is accessors-only from here on, and §22.2.3/§22.2.4 are clauses umbrella **8**'s own rule enumerates by name, so what step 5 consumes is the child umbrella 8 mints for RegExp creation — placing the edge on that child is an output of umbrella 8's derivation and of this one alike, and neither umbrella may be named in the other's place. ⚠ **The in-tree literal path is a different artifact and cannot stand in for it.** RegExp objects are constructed at exactly two sites — `git grep -n 'kind: ObjectKind::RegExp {' -- crates/script/elidex-js/src` gives `vm/dispatch_helpers.rs:176`, the `Constant::RegExp` arm of `load_constant`, whose pattern and flags are **compile-time constants** and so cannot take step 5's runtime `regexpOrPattern`, and `vm/host/structured_clone.rs:590`, a deserialization path — while `git grep -nic regexpcreate` / `regexpalloc` / `regexpinitialize` restricted to `-- crates/script/elidex-js/src` each return **0** files (the pathspec is load-bearing and was missing: run unrestricted at `29705dd6` the three return **1** file each — this document, which names the three AOs in this very sentence, `regexpcreate` with **3** hits), so §22.2.3 has no implementation to reuse and a child landing ahead of that creation child open-codes a third construction site beside the one it must ship | — | `#11-vm-builtin-prototype-static-sweep` (9b umbrella) | T3 | — |
| **9c** | **`Object` statics — UMBRELLA, not a terminal unit.** ⚠ **This row contradicted the demonstration §5's own head states the table by, and it is the terminality verdict rather than the grouping that fails.** That head says a derivation's output is a **grouping, not a slice**, and it uses **these exact members** to show why: **§20.1.2.7** `Object.fromEntries` step 6 performs **§24.1.1.2** AddEntriesFromIterable, whose step 1 is `GetIterator(iterable, sync)`, and **§20.1.2.13** `Object.groupBy` step 1 performs **§7.3.35** GroupBy, whose step 4 is the same `GetIterator`, while **§20.1.2.14** `Object.hasOwn` is `ToObject` → `ToPropertyKey` → `HasOwnProperty` and consumes no iterator — so "one PR for the Object family bundles an iterator-protocol contract with a member that has none". Read literally against the criterion this row bundles the iterator protocol and its **§7.4.11** close precedence (the `Pa` obligation below), a **§7.1.21** ToPropertyKey coercion whose canonical artifact is a slot, and the property-definition side of the two drains — with no shared lowering chokepoint between them. It therefore mints terminal children at its own start, from the grouping the umbrella's rule returns over **§20.1.2** *Properties of the Object Constructor* (`webref heading ecma262 20.1`); **placing the `Pa` obligation, the `#11-vm-topropertykey-symbol-from-toprimitive` obligation and each acceptance condition on the specific child is a mandatory output of that derivation** (§5's row-kind rule), and this row carries none of the three. Everything below is evidence for it to route, not a charter: named deliverables: `Object.fromEntries`'s iterator protocol (ECMA-262 §20.1.2.7 step 6 → `AddEntriesFromIterable` §24.1.1.2), `Object.hasOwn`, `Object.groupBy` — measurements in the §8 slot row. **The `Pa` obligation, for the reason 0bc's cell already carries**: §24.1.1.2 step 2.c.ii is `Return ? IteratorClose(iteratorRecord, error)` with `error` a ThrowCompletion, and steps 2.e / 2.g / 2.i are `IfAbruptCloseIterator` (`webref body ecma262 AddEntriesFromIterable`) — every one of them a §7.4.11 close on an **abrupt** completion, which is exactly the inverted-precedence contract Pa's completion-kind parameter delivers. Writing the drain against today's `iter_close` bakes the inversion into a second surface. ⚠ **And the drain is not this row's to *write*, which no edge recorded: §24.1.1.2 sits inside another row's clause.** `webref heading ecma262 24.1.1` puts **§24.1.1.2** AddEntriesFromIterable under **§24.1.1** *The Map Constructor*, i.e. inside the §24 surface umbrella **7**'s rule assigns to **7a** — *the constructor clauses and the operations they invoke as much as the prototype clauses* — and 7a's cell already reads that same body for the `IteratorClose` obligations its derivation places on the child that owns the `Map` constructor drain. `Object.fromEntries` performs the AO verbatim, so the child that carries it consumes the implementation of the child of umbrella 7a that owns the `Map` constructor drain, which that umbrella's derivation mints, rather than open-coding a second copy, and **placing that edge on it is a mandatory output of this row's derivation**, beside the `Pa` and slot ones. ⚠ **The `Object.groupBy` half does not get the same treatment, and the difference is where the AO lives**: **§7.3.35** GroupBy is in §7.3 *Operations on Objects*, inside no row's clause, and **§24.1.2.1** `Map.groupBy` performs the identical `Let groups be ? GroupBy(items, callback, collection)` (`webref body ecma262 sec-map.groupby`; AO number from `webref aoid ecma262 GroupBy`) — so umbrella 7a's children and this row reach it symmetrically and neither delivers it to the other, and it was recorded here as a shared obligation with **no owner**. ⚠ **That state is the one §5's own rule forbids, and the rule assigns the owner rather than leaving the artifact ownerless**: a "shared prerequisite" is outside either charter, which is why it is stated in prose, but the owner is one of the three specification fields, so a prerequisite with no owner is a specification hole. GroupBy is a prerequisite **of** both consumers rather than something both must exist before it can be discharged — neither `Map.groupBy` nor `Object.groupBy` can be written until the AO exists — so the rule's second case applies and the owner is **whichever lands first**, moving the work while the other consumes it. The two parties are the child of umbrella **7a** that carries `Map.groupBy` — naming an umbrella as a "lands first" party names nobody, so the party is that child and placing it is a mandatory output of 7a's derivation — whose charter the umbrella 7 rule already extends to *the constructor clauses and the operations they invoke* and so to **§24.1.2** *Properties of the Map Constructor* (`webref heading ecma262 24.1.2`, which lists §24.1.2.1 `Map.groupBy`), and the child of umbrella **9c** that carries `Object.groupBy` (**§20.1.2.13**, `webref heading ecma262 20.1.2.13 --exact`); naming that child, and re-pointing the obligation at it once minted, is a mandatory output of the derivation above, beside the `Pa` and slot ones. **And an obligation onto a slot** — slot-valued Deps entries are in convention on the child that carries it: **§20.1.2.14** `Object.hasOwn` step 2 is `Let propertyKey be ? ToPropertyKey(key)` (`webref body ecma262 sec-object.hasown`), and **§7.1.21** ToPropertyKey step 1 is `Let key be ? ToPrimitive(arg, string)` with step 2 returning the **result** when *that* is a Symbol (`webref body ecma262 sec-topropertykey`). The helper this file would reuse tests the **argument** instead: `to_property_key` (`vm/natives_object/mod.rs:28-34` at `658cc302`) matches `JsValue::Symbol` on `val` and sends everything else through `to_string_val`. Probed through the sibling that *is* implemented, `Object.prototype.hasOwnProperty`: `const s=Symbol('k'); const o={}; o[s]=1; const k={[Symbol.toPrimitive](){return s}}; o.hasOwnProperty(k)` throws **TypeError "Cannot convert a Symbol value to a string"**, against two controls that both give `true` — the raw Symbol key `o.hasOwnProperty(s)`, and a `@@toPrimitive` returning the string `'a'` on `{a:1}`. `#11-vm-topropertykey-symbol-from-toprimitive` owns collapsing the eight open-coded copies of §7.1.21 onto one implementation and then fixing it, `natives_object/mod.rs:28` named among them, so `Object.hasOwn` written on today's helper ships the divergence under a new member name. ⚠ **And `Object.hasOwn` is not the only member of this row that reaches that AO — the obligation was placed on one child when it belongs to two.** **§20.1.2.7** `Object.fromEntries` step 4 builds the adder as "a new Abstract Closure with parameters (key, value) that captures obj" whose step 4.a is "Let propertyKey be ? ToPropertyKey(key)", and step 5 hands that closure to the `AddEntriesFromIterable` of step 6 (`webref body ecma262 sec-object.fromentries`; §-number and title from `webref heading ecma262 20.1.2.7 --exact`) — so the coercion is inside the adder this row implements, not inside the AO it consumes from the child of umbrella 7a that owns the `Map` constructor drain. In-tree the adder already calls the very copy the slot names: `natives_object/iteration.rs:217` at `658cc302` is `let pk = to_property_key(ctx, key_val)?`, and `to_property_key` is `natives_object/mod.rs:28` (`git grep -n 'to_property_key(' 658cc302 -- crates/script/elidex-js/src`, which returns that site among four, all in `vm/natives_object/`). So the slot obligation is owed by **both** the child carrying `Object.hasOwn` and the child carrying `Object.fromEntries`, and placing it on each of them — not on whichever one a review reached first — is a mandatory output of this row's derivation. Open in the live ledger at the time of writing: `grep -c -- '#11-vm-topropertykey-symbol-from-toprimitive' <ledger>` → **2**, against the nonexistent-slug control `grep -c -- '#11-vm-topropertykey-symbol-from-toprimitive-NOPE' <ledger>` → **0** (`stat -f %Sm <ledger>` = 2026-08-08 11:51; the ledger is the memory-dir `project_open-defer-slots.md`, outside this repo, so re-run rather than read this forward) | — | `#11-vm-builtin-prototype-static-sweep` (9c umbrella) | T3 | — |
| **9d** | **`Promise` constructor statics — UMBRELLA, not a terminal unit.** ⚠ **This row was terminal, and the combinator obligation added at `c8476ce7` is what made it stop being so** — not an extra acceptance test on the resolution work but a change to iterator consumption and subscription ordering in another module. Three axes intersect here, each red while the other two are green — **resolution** — ECMA-262 **§27.5.1** Promise Abstract Operations and **§27.5.2** Promise Jobs, in `vm/natives_promise.rs` — and the **combinator algorithms** of §27.5.4.1-.5, which consume an iterator and are therefore also governed by **§7.4.11**, in `vm/natives_promise_combinator.rs`. §5's seam criterion admits the split both ways: the resolution half is live because `Promise.resolve` already assimilates (wrongly) today, and the combinator half is live because `Promise.all` and its siblings are already registered and connected. **The derivation is the umbrella's**: walk **§27.5.1** *Promise Abstract Operations*, **§27.5.2** *Promise Jobs*, **§27.5.4** *Properties of the Promise Constructor* and **§27.5.5** *Properties of the Promise Prototype Object*, and route each output to the sub-slice whose family governs it. **The seed is not curated — it is what `webref heading ecma262 27.5` returns**, which is those four together with **§27.5.3** *The Promise Constructor* (whose §27.5.3.1 step 9 is `Let resolvingFuncs be CreateResolvingFunctions(promise)`, `webref body ecma262 sec-promise-executor`) and **§27.5.6** *Properties of Promise Instances*; all six are walked. ⚠ **§27.5.2 was named in this row's first sentence and absent from the walk, and that is not a fix** — a clause a derivation *mentions* is still minted by nobody, because what a child follows is the list the walk enumerates: with §27.5.4/§27.5.5/§27.5.1 alone, **§27.5.2.2** NewPromiseResolveThenableJob — the job 9da's own evidence shows `settle_promise` does not build — is an output of no listed clause, and a child could retire with every §27.5.1 AO written and thenable assimilation still enqueuing nothing. **The measurement, taken over the three**: (i) **resolution** (§27.5.1 / §27.5.2) is red while the combinators are green — `settle_promise` binds a thenable instead of adopting it and §27.5.2.2 NewPromiseResolveThenableJob is enqueued by nobody, though `Promise.all` and its siblings are already registered and connected; (ii) the **combinators'** own iterator consumption and subscription ordering (§27.5.4.1-.5) are red while resolution is green — the obligation added at `c8476ce7` changed exactly those and no resolution fix reaches them; (iii) **§7.4.11 IteratorClose on the combinator path** is red while both are green — a combinator that subscribes in the right order over a correct resolution still fails to close its iterator on an abrupt exit — the axis **9db** carries. `no shared lowering point` is the head's *a derivation's output is a grouping, not a slice* test and decides the split, not the kind. The second disjunct is unavailable: the seed is what `webref heading ecma262 27.5` returns and §27.5.3.1 step 9 is quoted from `webref body ecma262 sec-promise-executor`, so the AOs are in the specification. | — | `#11-vm-builtin-prototype-static-sweep` (9d umbrella) | T3 | — |
| **9da** | **Resolution and the constructor surface — UMBRELLA, not a terminal unit.** ⚠ **This row is one *group* of umbrella 9d's derivation read as one slice, which is precisely what §5's head forbids**: "a derivation's output is a grouping, not a slice", and a group "is well-formed as a spec unit and can still hold mechanisms with no shared lowering chokepoint". Five here, and `Promise.prototype.finally`'s ThenFinally/CatchFinally closures share none with `settle_promise`: thenable assimilation (**§27.5.1.3** CreateResolvingFunctions with **§27.5.2.2** NewPromiseResolveThenableJob); job scheduling and microtask-checkpoint ordering (**§27.5.1.8** TriggerPromiseReactions, **§27.5.2.1** NewPromiseReactionJob), whose ordering witness this cell says needs a *different observation mechanism* — a Rust-side drain — which is the tell; capability and species plumbing (**§27.5.1.5** NewPromiseCapability, with `then` and `finally` each performing `SpeciesConstructor(promise, %Promise%)`); the **§27.5.1.9** HostPromiseRejectionTracker embedder boundary, a layering axis; and the constructor statics **§27.5.4.8** `Promise.try` / **§27.5.4.9** `Promise.withResolvers`, which this cell newly derives and for which it states no condition at all. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. The material it routes — minted by the derivation above rather than by a probe list — an output the rows 9a-9c did not carry. Measured: `crates/script/elidex-js/src/vm/globals_async.rs:58-66` registers exactly `resolve` / `reject` / `all` / `allSettled` / `race` / `any` (identical at `658cc302` and HEAD `d49465d0` — `git diff 658cc302 HEAD -- crates/script/elidex-js/src/vm/globals_async.rs` is empty), while ECMA-262 **§27.5.4** Properties of the Promise Constructor also carries **§27.5.4.8** `Promise.try` and **§27.5.4.9** `Promise.withResolvers` — main body, not Annex B (`webref heading ecma262 27.5.4`). Both absent in-tree: `grep -rl 'withResolvers' crates/script/elidex-js/src` → **0** files; `grep -c '"try"' crates/script/elidex-js/src/vm/globals_async.rs` → **0**. Promise Objects is **§27.5**, not §27.2 — `webref heading ecma262 27.2` returns *Resource Management*, so re-derive the number rather than recalling it. ⚠ **A complete inventory is not a correct `Promise`, and this row is where §5's members-vs-semantics rule at the head of that section bites**: the rule says a derivation is stated over the algorithms the spec defines for a surface, not over the members it has, and resolution is the algorithm here. Measured at `658cc302`: `settle_promise` (`vm/natives_promise.rs:123`) adopts a resolution only when it is already an `ObjectKind::Promise` (`:155`), and the function's own doc comment states the gap — "Arbitrary thenables are not yet assimilated" (`:121`). ECMA-262 **§27.5.1.3** CreateResolvingFunctions is the governing algorithm: its resolve closure fulfils directly when "resolution is not an Object" (step 2.5), retrieves `then` as "Let then be Completion(Get(resolution, \"then\"))" (step 2.6) and **rejects with the getter's own throw value** when that completion is abrupt (step 2.7), fulfils with the resolution itself when `IsCallable(thenAction)` is false (step 2.9), and otherwise builds and enqueues **§27.5.2.2** NewPromiseResolveThenableJob (steps 2.10-2.12) — whose job calls `then` with a fresh resolve/reject pair from a second CreateResolvingFunctions (step 1.1-1.2) and routes an abrupt call result to that reject (step 1.3.1). That chain is not separable from this row's own new members: **§27.5.4.8** `Promise.try` step 6.1 and **§27.5.4.9** `Promise.withResolvers` step 5 both hand out the `[[Resolve]]` of **§27.5.1.5** NewPromiseCapability, and for `%Promise%` that function is the one **§27.5.3.1** `Promise ( executor )` step 9 obtains from CreateResolvingFunctions — so registering either member on today's `settle_promise` re-ships the same defect under a new name, which is why the extension belongs to this row rather than to a slot beside it. §-numbers from `webref aoid ecma262 CreateResolvingFunctions` / `NewPromiseResolveThenableJob` / `NewPromiseCapability`; step text from `webref body ecma262 sec-createresolvingfunctions` / `sec-newpromiseresolvethenablejob` / `sec-promise.try` / `sec-promise.withResolvers` / `sec-promise-executor`. Regressions — **examples of what step 2 requires, not a partition of its branches** (the label "one per branch" stood here and was false: §27.5.1.3 also carries the self-resolution rejection and the `[[AlreadyResolved]]` record the resolve and reject closures share, so `resolve(p)` with `p` the promise itself, and `resolve(pendingThenable); reject(x)`, both go undetected by the three below; recorded as evidence, and the slice derives its own set): `Promise.resolve({then(r){r(7)}}).then(v => globalThis.x = v)` must set `x` to `7`; a `then` **getter** that throws must reject with the thrown value; a non-callable `then` must fulfil with the object itself; and — the branch the first of those three cannot separate — assimilation costs a **job**. §27.5.1.3 step **2.11** builds `NewPromiseResolveThenableJob(promise, resolution, thenJobCallback)` and step **2.12** is `Perform HostEnqueuePromiseJob(job.[[Job]], job.[[Realm]])` — substeps of the `resolveSteps` closure, the AO carrying six top-level steps, which is the depth this same cell already uses for 2.5 / 2.6 / 2.7 (`webref body ecma262 sec-createresolvingfunctions`), so an implementation that calls `then` synchronously inside `settle_promise` sets `x` to `7` exactly as required while the callback runs one checkpoint early. The witness is an ordering one: `const log = []; Promise.resolve({ then(r) { r(1) } }).then(() => log.push('thenable')); Promise.resolve().then(() => log.push('plain')).then(() => log.push('plain2'));` must give `plain,thenable,plain2`, where a synchronous assimilation gives `thenable,plain,plain2`. ⚠ **The witness states no observation point, and without one it measures nothing on any engine.** Executed at `658cc302` with `log.join()` appended as the final expression → `""`: at the end of synchronous script evaluation no job has run, so the array is empty on a *conformant* engine exactly as on this one and the ordering the assertion is about is unobservable. **The reading point is part of the regression**: it evaluates the script, drains the job queue to quiescence from the Rust harness, and *then* evaluates `log.join()`, which must be `"plain,thenable,plain2"`. A trailing `.then` is not admissible in its place — which checkpoint that continuation lands on is the quantity under test, so reading through it begs the question. The retag comes with it — `git show 658cc302:crates/script/elidex-js/src/vm/natives_promise.rs \| grep -c '§27\.2'` gives **16** lines still citing the withdrawn `§27.2` numbering. Its charter is the **resolution** family — what 9d's walk routes here, recorded as an owned surface rather than re-derived: **§27.5.1** *Promise Abstract Operations* and **§27.5.2** *Promise Jobs*, the constructor body **§27.5.3.1**, the prototype object **§27.5.5**, the instances **§27.5.6**, and — inside **§27.5.4** — only the statics that hand out a `[[Resolve]]` rather than consume an iterator: **§27.5.4.6** `Promise.reject`, **§27.5.4.7** `Promise.resolve`, **§27.5.4.8** `Promise.try`, **§27.5.4.9** `Promise.withResolvers`. ⚠ **Not §27.5.4 whole, and that is why the clause cannot stand as this row's seed**: the same clause carries **§27.5.4.1** `Promise.all`, **§27.5.4.2** `Promise.allSettled`, **§27.5.4.3** `Promise.any` and **§27.5.4.5** `Promise.race` (`webref heading ecma262 27.5.4`) — **9db's entire charter** — so a derivation stated over §27.5.4 returns another row's work as this row's output and double-owns it. ⚠ **And the walk has one home, 9d's row**, which states it and records §27.5.2's absence from it; this row runs no second walk and restates neither, per §5's single-home rule — what allocates a clause to a sub-slice is that umbrella's derivation, and this cell records only what this row owes. Its Deps cell is `—` and stays so: the resolution machinery closes no iterator, so **Pa is 9db's edge, not this row's** — the `c8476ce7` revision put it on the whole of 9d, which over-ordered this half | `vm/globals_async.rs`, `vm/natives_promise.rs` | `#11-vm-builtin-prototype-static-sweep` (9da) | T3 | — |
| **9db** | **Combinator iterator preservation.** **Depends on Pa, for the reason 9c's cell already carries.** **§27.5.4.1** `Promise.all` step 8.a is "If iteratorRecord.[[Done]] is false, set result to Completion(IteratorClose(iteratorRecord, result))" and the same step stands in `allSettled` / `any` / `race` (`webref body ecma262 sec-promise.all` / `sec-promise.allsettled`). ⚠ **And it is not only an ordering edge — the shape in tree makes that step unreachable**, so it is a named deliverable: `run_combinator` (`vm/natives_promise_combinator.rs:308`) calls `collect_iterator` (`:330`) and only *then* subscribes per item, so the iterator is already exhausted before any resolution can go abrupt and `return()` cannot be called at all; the combinator must keep the iterator open across per-item resolution. `collect_iterator` (`vm/ops.rs:44-64` at `658cc302`) also takes today's inverted precedence on its cap path — its own comment says "if that call itself throws, its error takes precedence over the range-error", which is exactly the §7.4.11 step 5 inversion Pa corrects ⚠ **And an edge onto the child umbrella `9da`'s derivation mints for thenable assimilation, measured**: `subscribe` (`vm/natives_promise_combinator.rs:423` at `658cc302`) normalises every non-`ObjectKind::Promise` input with `create_promise` + **`settle_promise`** — the very function the child umbrella **9da**'s derivation mints for thenable assimilation rewrites — so a 9db that lands first fixes iterator preservation while `Promise.all([{ then(r){ r(7) } }])` still yields the thenable object instead of `7`. Same consumer relationship the child of umbrella **6a** that owns `.return` and 6b's `for await` drain already carry onto the child umbrella **9da**'s derivation mints for thenable assimilation. ⚠ **Acceptance is stated over *which abrupt site* closes the iterator, not over the iterator staying open** — a lazy rewrite that steps, subscribes, steps again satisfies the deliverable sentence above and can still never call `return()` at all (`git grep -c -e iter_close -e '"return"' 658cc302 -- crates/script/elidex-js/src/vm/natives_promise_combinator.rs` matches **nothing**, rc=1), so *keeping it open* is not by itself observable. §27.5.4.1 step 8.a fires **only while** `iteratorRecord.[[Done]]` is false, and **§7.4.6** `IteratorNext` steps 3.a/5.a plus **§7.4.9** `IteratorStep` steps 3.a/5.a set `[[Done]]` to **true** before their throw (`webref body ecma262 sec-iteratornext` / `sec-iteratorstep`), so the assertion's sign flips between two adjacent failures of the same iterable. Three cases over one hand-written iterator that counts its own `return()` invocations: **(i)** `next()` throws at item 2 → `return()` called **zero** times; **(ii)** `next()` succeeds and **§27.5.4.1.2** `PerformPromiseAll` step 5.k `Perform ? Invoke(nextPromise, "then", …)` throws — reachable **without subclassing**, because **§27.5.4.7.1** `PromiseResolve` step 1.b returns its input unchanged when that input is a promise whose `constructor` is `ctor`, so yielding `const p = Promise.resolve(1); Object.defineProperty(p, 'then', { get() { throw new Error('r') } })` makes `nextPromise` *be* `p` — → `return()` called **exactly once**, `next()` never asked for item 3, and the result promise rejects with that same `Error('r')` (§7.4.11 step 5 discards the `return()` outcome when the outer completion is a throw) — ⚠ **the `return()` counts are readable synchronously and this rejection is not, so it carries the observation point 9da states**: the regression drains the job queue to quiescence from the Rust harness and reads the rejection reason after that drain, never at the end of script evaluation. ⚠ **And the `PromiseResolve` identity this case turns on is an unasserted read**: §27.5.4.7.1 step 1.b returns its input unchanged only when `Get(p, "constructor")` is `%Promise%`, so a failure of that lookup on an object whose own `then` has been redefined is indistinguishable from a failure of the `return()` accounting (ii) exists to measure. It is asserted separately rather than assumed — `const p = Promise.resolve(1); p.constructor === Promise` must hold before (ii) is read — with its polarity re-derived at this row's own plan-review against its parent HEAD; **(iii)** normal exhaustion → `return()` called **zero** times. (i) and (iii) are the positive controls — ⚠ **and all three cases pin the `[[Done]]` discipline at exactly *one* abrupt site, while §27.5.4.1 step 8.a is stated over *any* abrupt result of step 7**: a rewrite that special-cases the `then`-invoke site closes there and nowhere else and passes all three verbatim. **Fourth condition, at the other abrupt site inside PerformPromiseAll, red at `658cc302`**: step 5.d is `Let nextPromise be ? Call(promiseResolve, ctor, « next »)`, reachable without subclassing because §27.5.4.7.1 PromiseResolve step 1.a is `Get(resolution, "constructor")` — so yielding `const p = Promise.resolve(1); Object.defineProperty(p, 'constructor', { get(){ throw new Error('c') } })` throws at 5.d with `[[Done]]` false and the iterator must be closed. ⚠ **And all four were spelled over `Promise.all`, so `allSettled` / `any` / `race`, named in this same cell, were unasserted**: without them, an implementation of `Promise.all` alone answers every condition above while the other three keep the eager-collection path, and one that closes unconditionally once collection ends — today's eager shape with an `iter_close` bolted on after `collect_iterator` — passes (ii) on its own. **Fifth condition: (ii) and the fourth condition restated once per remaining combinator** — ⚠ **and this row stays *terminal* rather than becoming an umbrella that mints owners, which is the choice §5's criterion makes here rather than a preference.** That criterion asks whether the row bundles ≥3 intersecting invariant axes **or** touches a subsystem with no canonical algorithm, and this row does neither: the governing sentence is one sentence, repeated verbatim as step **8.a** of **§27.5.4.1** `Promise.all`, **§27.5.4.2** `Promise.allSettled`, **§27.5.4.3** `Promise.any` and **§27.5.4.5** `Promise.race` (`webref body ecma262 sec-promise.allsettled` / `sec-promise.any` / `sec-promise.race`), and all four members reach it through **one** in-tree chokepoint — `run_combinator` (`vm/natives_promise_combinator.rs:308` at `658cc302`), tail-called by `native_promise_all` `:492`, `native_promise_all_settled` `:501`, `native_promise_race` `:510` (passing `None`) and `native_promise_any` `:519`. All four members discharging that one repeated sentence at that one chokepoint is one axis, so the members are carried by acceptance and not by minted children. The restatements run against **each combinator's own** abrupt sites, because the step numbering is not shared: **§27.5.4.2.1** PerformPromiseAllSettled steps **5.d** and **5.p**, **§27.5.4.3.1** PerformPromiseAny steps **5.d** and **5.k**, and **§27.5.4.5.1** PerformPromiseRace steps **1.c** and **1.d** — that AO's `Repeat` is step **1**, not step 5, so a repair written to `PerformPromiseAll`'s indices addresses neither site in `race` (AO names and numbers from `webref aoid ecma262 PerformPromiseAllSettled` / `PerformPromiseAny` / `PerformPromiseRace`; steps from `webref body ecma262 sec-performpromiseallsettled` / `sec-performpromiseany` / `sec-performpromiserace`). **Declared separating power**: an implementation that closes only on `Promise.all`'s path passes all four conditions above and fails every one of these, and one that closes once collection ends passes the abrupt-site case on every combinator and still fails the (i) and (iii) controls on each. | **`vm/natives_promise_combinator.rs`** | `#11-vm-builtin-prototype-static-sweep` (9db) | T3| **Pa**, the child umbrella **9da**'s derivation mints for the thenable-job tick — naming 9da itself would name nobody |
| **10** | **`Proxy`/`Reflect` — KIND UNDETERMINED: neither an umbrella nor a terminal unit until the measurement stated below is made, and no PR may be scheduled under it — its sub-slices **10a** and **10b** included — until it is.** `Reflect` is a static-method surface (ECMA-262 §28.1) that ships and tests on its own; `Proxy` is an exotic-object algorithm (§10.5) whose traps intersect every essential internal method plus their invariant checks. That is two axes; `reaches every property operation` is reach and not a third. **The measurement that settles the kind**: whether §10.5's target-consistency validation is a third axis — a probe stated over those validation steps that is red on a `Proxy` whose trap dispatch is otherwise correct — while the `Reflect` statics and the trap dispatch are both green. `ships and tests on its own` is the separability of one half from the other, which is what gives the two. If no third comes back, the row carries two axes against a threshold of ≥3 and is terminal, with `10a` and `10b` its deliverables rather than its children. The second disjunct is unavailable: the cell names ECMA-262 §28.1 and §10.5, so the algorithms that govern the work are in the specification. | — | `#11-vm-proxy-reflect` (kind undetermined) | T3 | — |
| **10a** | **`Reflect` statics — UMBRELLA, not a terminal unit.** (ECMA-262 **§28.1** *The Reflect Object*.) ⚠ **This row was terminal, and the acceptance condition `d544f634` added to it is what ends that** — its three separators are stated as *independent* failures over *different* essential internal methods (receiver threading through §28.1.5 / §28.1.12, the Boolean return through §28.1.3 / §28.1.12, `[[OwnPropertyKeys]]` ordering through §28.1.10), which is the same measurement that promoted 9a / 9b / 9c and 10b. Each static mirrors an essential internal method and needs no exotic object — and that is exactly why they share no lowering chokepoint, registration on one object not being one — so it **mints terminal children at its own start**, from the grouping the walk over §28.1 returns (`webref heading ecma262 28.1`, which lists §28.1.1 `Reflect.apply` through §28.1.13 `Reflect.setPrototypeOf` plus §28.1.14 `Reflect [ %Symbol.toStringTag% ]`, the data property and not a function — the **thirteen**), partitioned by the internal method each mirrors and the parameter each threads. **This row mints none of them and invents no ids**: which groups, and how the thirteen divide between them, is that derivation's output, and a static absent from any list in this cell is **un-minted, never out of scope**. **Placing each obligation, each edge and each acceptance condition on the specific child is a mandatory output of that derivation** (§5's row-kind rule), and this row carries none of the three. Everything below is evidence for the derivation to route, not a charter. **And an edge to a slot, for the reason 9c's cell already carries**: `Reflect.defineProperty` (**§28.1.3**), `Reflect.deleteProperty` (**§28.1.4**), `Reflect.get` (**§28.1.5**), `Reflect.getOwnPropertyDescriptor` (**§28.1.6**), `Reflect.has` (**§28.1.8**) and `Reflect.set` (**§28.1.12**) each have `Let propertyKey be ? ToPropertyKey(key)` as **step 2** (§-numbers from `webref heading ecma262 28.1`; steps from `webref body ecma262 sec-reflect.defineproperty` and its five siblings), and **§7.1.21** ToPropertyKey step 1 is `Let key be ? ToPrimitive(arg, string)` with step 2 returning the **result** when *that* is a Symbol (`webref body ecma262 sec-topropertykey`) — the canonical artifact `#11-vm-topropertykey-symbol-from-toprimitive` owns, and which 9c's cell measures diverging from on the sibling `Object.hasOwn`. **And I-5's second consequence has a `Reflect` half that the deleted `10b → 10a` edge used to carry across**: §4's consequence #2 charges **whichever child this derivation mints for that routing** with resolving `Reflect` × `ObjectKind::HostObject` (`vm/object_kind.rs:272`) inside core dispatch — `Reflect` is this row's surface, but the row is an umbrella and §4 states the obligation on the child rather than on the row — and leaves the `Proxy` half to umbrella 10b's children — the split §4 states directly, rather than one this cell derives back from a charge on 10b. **The inbound-rule membership test itself is not this row's**: it is settled once by `#11-vm-inbound-host-call-membership-test` (§8). A test owned only by a child of umbrella 10b was not available to the children this derivation mints at the time they do the work — one would otherwise land having added `Reflect` × `ObjectKind::HostObject` special cases to core dispatch under no test at all — and with no edge between 10a's children and 10b's a per-surface settlement cannot be ordered either, so the decision sits on a slot the children of both umbrellas consume rather than on any one child's plan-review (§4 I-5). ⚠ **What a terminal reading of this row stated as its acceptance is retained below as the derivation's input and is *not* an acceptance condition of this row** — an umbrella states none, and each separator belongs on the child that carries the statics it discriminates. It is bounded by the parameters an essential-internal-method mirror silently drops, never by the thirteen names existing — `typeof Reflect.get === 'function'` and `Reflect.get({ x: 1 }, 'x') === 1` both pass on a `Reflect` that forwards to the VM's existing property helpers, which is exactly the implementation this row will reach for: `Reflect` is **absent** from the VM at `658cc302` (`git grep -n Reflect -- crates/script/elidex-js/src` returns only comments plus `vm/tests/tests_dom_exception.rs:240`'s "`Reflect` isn't yet exposed in the VM"), so the row starts from `vm/ops_property.rs`, and whether those helpers thread a receiver is what a name-existence acceptance cannot see. The count **thirteen** is `webref heading ecma262 28.1` minus §28.1.14, which is the `%Symbol.toStringTag%` data property and not a function. One separator per dropped parameter: **(a) receiver** — §28.1.5 step 4 is `? target.[[Get]](propertyKey, receiver)` and **§10.1.8.1** `OrdinaryGet` step 7 is `? Call(getter, receiver)`, so `Reflect.get({ get x() { return this.v } }, 'x', { v: 7 }) === 7`, and via §28.1.12 step 4 into **§10.1.9.2** `OrdinarySetWithOwnDescriptor` step 2.d.ii, `const t = {}, r = {}; Reflect.set(t, 'x', 1, r)` leaves `'x' in t` **false** with `r.x === 1`; **(b) the Boolean return** — §28.1.3 step 4 and §28.1.12 step 4 *return* the internal method's Boolean instead of throwing on false, so `Reflect.defineProperty(Object.freeze({}), 'x', { value: 1 }) === false` and `Reflect.set(Object.freeze({}), 'x', 1) === false` must both be `false` **without throwing**, which a delegation to `Object.defineProperty` (`vm/globals.rs:1387`) cannot do; **(c) key order and key kind** — **§28.1.10** step 2 is `? target.[[OwnPropertyKeys]]()`, i.e. **§10.1.11.1** `OrdinaryOwnPropertyKeys`'s array-index-ascending, then string-creation-order, then symbol-creation-order partition, so `Reflect.ownKeys({ b: 1, 2: 1, 1: 1, [Symbol.iterator]: 1 })` is exactly `['1', '2', 'b', Symbol.iterator]`, which a forward to `Object.getOwnPropertyNames` (`vm/globals.rs:1395`) fails on the symbol alone. Step 1 of **eleven** of the thirteen is the same `target is not an Object` TypeError, while **§28.1.1** `Reflect.apply` step 1 is `If IsCallable(target) is false, throw a TypeError exception` and **§28.1.2** `Reflect.construct` step 1 is `If IsConstructor(target) is false, throw a TypeError exception` (`webref body ecma262 sec-reflect.apply` / `sec-reflect.construct`), so the obligation set this derivation routes carries **three** guard predicates and not one — `Reflect.apply({}, null, [])` and `Reflect.construct(() => {}, [])` must each throw a **TypeError** on a `target` that *is* an Object, which a shared `is not an Object` reading drops. Every rejection still points one way and **(b) is their positive control**: an over-throwing `Reflect` satisfies all of them and fails `=== false`. **Column changes, stated rather than left to inference.** `Deps` becomes **—**, which the rule requires rather than merely permits, an umbrella carrying no ordering — so **both slot edges are re-pointed as mandatory outputs of this derivation**, onto the children that consume them rather than onto all of them: `#11-vm-topropertykey-symbol-from-toprimitive` onto the children carrying the six statics whose step 2 is `ToPropertyKey` (§28.1.3, §28.1.4, §28.1.5, §28.1.6, §28.1.8 and §28.1.12, this cell's own reading), and `#11-vm-inbound-host-call-membership-test` onto whichever children resolve `ObjectKind::HostObject` inside core dispatch (§4 I-5 consequence #2) — the same disposition 10b's cell already records for that slot, and the reason §4's charge on "this row" is a charge on that derivation's output. The `Slot` keeps `#11-vm-proxy-reflect` with the `(10a umbrella)` kind marker rows **4a** and **7b** already carry on the identical adoption, the `Tier` stays **T3** and is inherited by the children, and the `Primary module(s)` cell stays as **evidence** for the derivation to route — rows **1a**, **3**, **5** and **Pd**, all umbrellas, keep theirs on the same reading — and is not a touch set, this row having none | `vm/ops_property.rs` | `#11-vm-proxy-reflect` (10a umbrella) | T3 | — |
| **10b** | **`Proxy` exotic object — UMBRELLA, not a terminal unit.** (ECMA-262 **§10.5**.) ⚠ **This row was terminal, and its own charter sentence — "the trap table and the invariant checks *each* internal method must enforce" — is what ends that.** `webref heading ecma262 10.5` enumerates **13** essential internal methods: §10.5.1 `[[GetPrototypeOf]]` through §10.5.11 `[[OwnPropertyKeys]]` for the property family, §10.5.12 `[[Call]]` and §10.5.13 `[[Construct]]` — three families, thirteen trap dispatches, and a *different* invariant set per method. They fail independently, and the discriminating measurement is that no two share an invariant: §10.5.1 step 12 is "If SameValue(handlerProto, targetProto) is false, throw a TypeError exception", §10.5.11 step 22 is "If uncheckedResultKeys is not empty, throw a TypeError exception", and §10.5.13 step 10 is "If newObj is not an Object, throw a TypeError exception" (`webref body ecma262` over `sec-proxy-object-internal-methods-and-internal-slots-getprototypeof` / `-ownpropertykeys` / `-construct-argumentslist-newtarget`). So a `[[Get]]`-forwarding regression says nothing about whether `[[OwnPropertyKeys]]`'s key invariants or `[[Construct]]`'s result validation exist at all — while this row's own §4 I-5 consequence has the work reaching **every** property operation in core dispatch. ⇒ umbrella. **A shared Proxy representation-and-revocation prerequisite is a mandatory output of this umbrella's derivation**, and the same command locates it: all thirteen open on the same steps — step 1 "Perform ? ValidateNonRevokedProxy(obj)" (**§10.5.14**), then the `obj.[[ProxyTarget]]` and `obj.[[ProxyHandler]]` reads, verified on §10.5.1, §10.5.11 and §10.5.13 above — over the slots **§10.5.15** ProxyCreate establishes, with the revoked state produced by **§28.2.2.1** `Proxy.revocable` (`webref heading ecma262 28.2`). Without it every internal-method child re-derives the exotic-object shape and its own revocation check, the N-mechanism outcome CLAUDE.md *One issue, one way* forbids; with it, what remains are narrowly scoped internal-method-family children. ⚠ **This row mints none of them**: which families, and how the thirteen divide between them, is that derivation's output, and a method absent from any list in this cell is **un-minted, not out of scope**. **The `#11-vm-inbound-host-call-membership-test` edge is the derivation's to place as well**: it stood in this row's `Deps` cell while the row was terminal, and it is consumed by whichever children resolve `ObjectKind::HostObject` inside core dispatch rather than by all of them (§4 I-5 consequence #2; that slot's own row states the ordering it needs). Everything below is evidence for the derivation to route, not a charter. **No child this derivation mints consumes an artifact of any child of umbrella 7b**, though the Deps cell named it until PR-B: the two rows share `vm/object_kind.rs`. the deliverable of the child umbrella 7b's derivation mints for ephemeron tracing is weak-key GC tracing; 10b holds a Proxy's target and handler strongly and consumes nothing any child of umbrella 7b produces. **The `10a` edge went the same way at PR-B and for the same reason**, having survived while the row withdrew its neighbour at length: `Reflect`'s statics each mirror an essential internal method and are not what a trap's default behaviour calls, so nothing here consumes what any child of umbrella 10a produces either | — | `#11-vm-proxy-reflect` (10b umbrella) | T3 | — |
| **D** | **Dead-opcode sweep** — mechanically re-derive the §2.3 set and delete what no slice connected. ⚠ **Slice M is outside this sequence** (promoted to its own umbrella), and §2.3 assigns `Op::ImportMeta` / `Op::DynamicImport` to the connect work `#11-vm-module-opcode-connects` carries — so at the documented "after 1b-5" point D would have to either delete two opcodes `#11-vm-module-opcode-connects` owns or leave them dead and fail its own connect-or-delete criterion (Codex R1). **The connects `#11-vm-module-opcode-connects` carries are a prerequisite for D.** That prerequisite now has a **§8 row with a slot id** (`#11-vm-module-opcode-connects`) instead of a by-name prose carve-out: an unschedulable dependency whose escape hatch is a permanent third state is precisely what I-4 forbids, and a carve-out stated only in prose is unretirable (§8 Forward rule). ⚠ **`Op::Wide` is in this slice's scope, not exempt** (§2.3, §4 I-4): it has zero emit sites like the rest — `git grep -n 'Op::Wide' -- crates/` returns exactly `bytecode/opcode.rs:562` and `vm/dispatch.rs:1074`, neither of them a `compiler/` emit, against the negative control `git grep -l 'emit(Op::Pop)' 658cc302 -- crates/script/elidex-js/src/compiler`, which returns emits in **13** files, against the negative control `git grep -l 'emit(Op::Wide)' 658cc302 -- crates/script/elidex-js/src/compiler` → **0** on the same pathspec, so the emit-shaped pattern discriminates — and nothing pins its discriminant. Mechanical consequences to carry, each from `git grep -n 'Wide' -- crates/script/elidex-js/src` at HEAD: `Wide` is the **last** enum discriminant (`bytecode/opcode.rs:388`, the variant immediately before the closing brace), so `from_byte`'s bound `if byte <= Self::Wide as u8` (`:396`) and the `roundtrip` test's range `for byte in 0..=Op::Wide as u8` (`:562`) both retarget to the new last variant; the `operand_size` arm `\| Self::Wide => 0` (`:494`) goes; the dispatch arm at `vm/dispatch.rs:1074-1075` goes; and the comments at `opcode.rs:13`, `:387` and `:397` go with it, while `:411`'s "excluding Wide prefix" is reworded. ⚠ **Deps re-stated as a criterion rather than a row-order range**: `after 1b-5` named no artifact and "1b-5" is a position in this table, not a set of slice ids. ⚠ **Acceptance is stated over the two things a mechanical sweep gets wrong, and the first is this cell's own discriminator** — `git grep -l 'emit(Op::Wide)' …` → **0** against `emit(Op::Pop)` → 13 files separates *nothing*, because the identical shape returns 0 for **live** opcodes: at `658cc302`, `git grep -cE "emit[a-z0-9_]*\(Op::DefineGetter" -- crates/script/elidex-js/src/compiler` matches **nothing** (rc=1) while `git grep -c 'Op::DefineGetter' -- crates/script/elidex-js/src/compiler` sums to **7** across `expr_class.rs` (6) and `expr_object.rs` (1), because that opcode reaches the buffer only through a **bound name** — `let define_op = match kind { MethodKind::Get => Op::DefineGetter, … }` then `fc.emit_u16(define_op, idx)` (`compiler/expr_class.rs:362,367`) and the `op: Op` parameter of `emit_class_member_name_op` (`:583`, emitting at `:600`, `:605`, `:619`) and of `compile_accessor` (`compiler/expr_object.rs:134`). `Op::DefineSetter` measures the same 0-against-7. So the acceptance runs the sweep and asserts that **`Op::DefineGetter` and `Op::DefineSetter` both survive it**: they are the positive control the emit-spelling filter fails, and a deletion set computed from that spelling silently removes class getters and object-literal getters. (`Op::DefineMethod` and `Op::DefineProperty` are **not** controls — both do have emit-literal sites, 1 and 9; an earlier count saying otherwise had used `\b`, unsupported in `git grep -E`, and matched nothing for every opcode.) The enumerator the surviving set is compared against must accordingly be one that binding an `Op` to a name cannot evade, not a filter on the `emit(Op::…)` text. **Second, the decode bound**: `from_byte`'s `if byte <= Self::Wide as u8` (`bytecode/opcode.rs:396`) is checked only by `roundtrip`, whose range is `for byte in 0..=Op::Wide as u8` (`:562`) — the range is computed from the **same symbol the bound uses**, so no retarget of that pair can ever fail it — while `invalid_byte` probes the single byte 255 (`:570`), leaving every byte between the new bound and 255 unmeasured; retargeting both to a variant that is *not* last therefore passes the whole suite with the genuinely-last opcode decoding to `None`. So the post-sweep assertion runs over `0..=u8::MAX` against an oracle built from an **exhaustive `match` over `Op`** (completeness compiler-checked) rather than from the bound: `from_byte(b)` is `Some(op)` with `op.to_byte() == b` for exactly the bytes that list yields, and `None` for every other `b` — ⚠ **and both stated conditions are green at `658cc302`, so this acceptance is satisfied by the *absence* of the deliverable**, which is I-8's named inversion: `Wide` is already the last discriminant (`bytecode/opcode.rs:388`), `from_byte`'s bound is already `if byte <= Self::Wide as u8` (`:396`) over a gapless sequential enum, and an oracle built from an exhaustive `match` over today's `Op` already agrees with `from_byte` on all of `0..=u8::MAX` — a PR that adds the two assertions and deletes nothing retires the row with the §2.3 set intact. **The condition is therefore stated over the *difference*, red at D's own parent HEAD**: the re-derived §2.3 set is recorded in the PR, `Op` contains no member of it afterwards, `Op::Wide` among them, and the decode bound plus its round-trip range move with the deletion — so the round-trip assertion becomes non-vacuous only because the enum shrank | `bytecode/opcode.rs`, `vm/dispatch.rs` | **adopt** `#11-dead-opcode-removal` | — | every slice that connects a §2.3 opcode **and** `#11-vm-module-opcode-connects` |
| **M** | **ES modules — PROMOTED TO ITS OWN UMBRELLA, not a terminal unit, outside this plan's slice sequence** (R2 round 5). It carries the `stmt.rs:31-39` precondition, 3 §2.2 rows (⚠ two of them — the module-binding update and for-in head — are **0a ✅ loud**, so M inherits their *implementation*, not their conversion; I-2's `expr_assign.rs:102` `unreachable!` is likewise already converted), I-5's host-fetch-seam boundary, and the `ImportMeta`/`DynamicImport` connects = ≥3 intersecting invariant axes, so the CLAUDE.md edge-dense rule forbids one PR. Only the **`#11-vm-dynamic-import`** T1 carve stays homed here (**0ca** makes `import()` loud; the Promise-returning impl belongs to the module umbrella). ⚠ **And the §16.2 walk cannot derive it, so the seed gains ECMA-262 §13.3.10 *Import Calls*** — `import()` is an **expression**, valid in an ordinary script (this document measures that at §2.2's `import('x')` row), so nothing in the Modules grammar states its runtime contract: a child could connect `Op::DynamicImport` and return a Promise while a throwing specifier still threw **synchronously** instead of rejecting. Its outputs are assigned **independently of the `parse_module` path**, since the two are reachable separately. **Its preconditions are a derivation, and the list just given is that derivation's seed, not its output** (PR-B). **The rule**: at the module umbrella's own start, walk the ES-modules grammar of ECMA-262 **§16.2** *Modules* — the productions of **§16.2.2** *Imports* and **§16.2.3** *Exports*, **and §16.2.1 *Module Semantics* with them** — the three being what `webref heading ecma262 16.2` returns under §16.2, so the seed is the command's output rather than a recalled list, with §13.3.10 added below for the reason stated there ⚠ **(the seed was Imports/Exports alone, and this row's own evidence below shows why that is not reachable-from: import/export productions do not derive `[[HasTLA]]`, ExecuteModule or AsyncBlockStart, so a child could enable `parse_module` while top-level `await` still reaches the VM's "top-level bodies are never async" assertion)** — against `parser/module.rs` and `ast.rs` **and the compiler and VM execution path** at that umbrella's parent HEAD, and table every production the parser does not accept, the AST does not represent, **or the execution path cannot run**. ⚠ **The code side of the walk was the parser and AST alone, and that could not reach the defect this row's own evidence records**: the parser already represents top-level `await` while `vm/interpreter.rs` rejects an async top-level body (evidence above), so every grammar/AST output could be routed and a child could enable `parse_module` with `[[HasTLA]]`, ExecuteModule and AsyncBlockStart still unbuilt — the assertion fires at run time, not at parse time. **Adding §16.2.1 to the seed does not fix that on its own**; the walk has to reach the layer where the work lives, and the resulting top-level-`await` work is a mandatory assignment to a terminal child. ⚠ **And that child consumes the child umbrella **9da**'s derivation mints for thenable assimilation. This was raised once as "may be shared surface rather than a consumed artifact" and left unplaced on that doubt; the doubt is resolved here, against the steps, in favour of a consumed artifact.** Both halves of top-level `await` reach the same closure. The module half: **§16.2.1.6.1.3.2** ExecuteAsyncModule step 3 is "Let promiseCapability be ! NewPromiseCapability(%Promise%)", step 8 "Perform PerformPromiseThen(promiseCapability.[[Promise]], onFulfilled, onRejected)" and step 9 "Perform ! module.ExecuteModule(promiseCapability)" — so a module with `[[HasTLA]]` **is** driven by a promise capability. The `await` half: **§27.10.5.3** `Await ( arg )` step 2 is "Let promise be ? PromiseResolve(%Promise%, arg)", and **§27.5.4.7.1** PromiseResolve step 2 is "Let promiseCapability be ? NewPromiseCapability(ctor)" with step 3 "Perform ? Call(promiseCapability.[[Resolve]], undefined, « resolution »)". Both end in the same place: **§27.5.1.5** NewPromiseCapability step 6 is "Let promise be ? Construct(ctor, « executor »)", and for `%Promise%` that is **§27.5.3.1** `Promise ( executor )`, whose step 9 is "Let resolvingFuncs be CreateResolvingFunctions(promise)" — **§27.5.1.3**, the named deliverable of the child umbrella 9da's derivation mints for thenable assimilation (§-numbers from `webref aoid ecma262 ExecuteAsyncModule` / `Await` / `PromiseResolve` / `NewPromiseCapability` / `CreateResolvingFunctions`; steps from `webref body ecma262 ExecuteAsyncModule` / `Await` / `sec-promise-resolve` / `NewPromiseCapability` / `sec-promise-executor`). **What decides it is that the `[[Resolve]]` handed to `ExecuteModule` is not a surface the two rows happen to share but the specific function that AO produces**: written against today's `settle_promise`, which adopts a resolution only when it is already an `ObjectKind::Promise` (9da's row), `await thenable` at module top level binds the thenable object instead of adopting it, and the module's own settlement inherits that. ⚠ **And the edge is owed by the `import()` child on the same reading, which an earlier wording missed by scoping it to top-level `await` alone.** The `#11-vm-dynamic-import` carve homed above reaches the identical closure by the identical route: **§13.3.10.2** EvaluateImportCall step 7 is `Let promiseCapability be ! NewPromiseCapability(%Promise%)` and **§13.3.10.3** ContinueDynamicImport resolves it with the namespace — its fulfilled path is `Let namespace be GetModuleNamespace(module)` then `Perform ! Call(promiseCapability.[[Resolve]], undefined, « namespace »)` (§-numbers from `webref aoid ecma262 EvaluateImportCall` / `ContinueDynamicImport`; steps from `webref body ecma262 sec-evaluate-import-call` / `sec-ContinueDynamicImport`) — so the `[[Resolve]]` it calls is the same CreateResolvingFunctions product, and against today's non-assimilating `settle_promise` a namespace whose module exports a callable `then` fulfils with the namespace instead of assimilating it. That the two children reach the child umbrella **9da**'s derivation mints for thenable assimilation by different AOs is why the edge has to be stated per child rather than once for the row. **Placing this edge on the child that owns top-level `await` and on the child that owns `import()` is a mandatory output of this umbrella's derivation**, alongside the assignment above — an umbrella's `Deps` cell names nobody, so it is not written in this row's. ⚠ **And so is the edge on the child `A`'s derivation mints, which nothing in this row named while row `A` names this one — naming `A` itself would name nobody (§5).** Slice **A** is the umbrella whose derivation mints the child carrying ECMA-262 **§15.7.1**'s `ClassStaticBlock` early error — "It is a Syntax Error if ClassStaticBlockStatementList Contains await is true", the only `await` rule in that clause (`webref heading ecma262 15.7.1 --exact` → *Static Semantics: Early Errors*; rule text from `webref body ecma262 sec-class-definitions-static-semantics-early-errors`, `\| grep -c -i await` → **1**) — ⚠ **and it is *not* module-only, which this clause asserted for one revision by carrying the reasoning row A had already withdrawn**: script parsing accepts `await;` as an identifier (`parser/expr_tests_special.rs:59-63`), while §15.7.1's rule is over the token, so `class C { static { await; } }` is a required *script-mode* regression on the child umbrella **A**'s derivation mints for §15.7.1's `Contains await` rule — ⚠ **not as spelled, because the static block contributes nothing to its outcome.** Measured at `658cc302`, that source and a bare `await;` with no class at all fail *identically* with `ReferenceError: await is not defined`, and `class C { static { await 0 } }` and a bare `await 0` fail identically as parse errors, so neither spelling can fail on the block's `[+Await]` parameter. The spelling that can is one whose *enclosing* context already admits `await`: `async function f(){ class C { static { await; } } }` must be a SyntaxError and is **accepted** at `658cc302` (Node: `SyntaxError`), against the controls `async function f(){ class C { static { 0 } } }` and `async function f(){ await 0 }`, both of which parse here and under Node. Under `ClassStaticBlockStatementList : StatementList[~Yield, +Await, ~Return]opt` the `[~Await] await` alternative of `IdentifierReference` is unavailable, so that regression is carried by the grammar parameter and not by §15.7.1's `Contains await` rule; the rule's own witness is `await 0` in the same position, which `658cc302` already rejects and which is therefore a green guard and the module disjunct of `await_is_keyword` (`parser/mod.rs:61-63`) is only the second of the two routes. A's row records both consumers; **2b** carries its half in its own `Deps` cell, and the other consumer is the child this umbrella mints for the `parse_module` path, which does not exist yet — so nothing places the edge except this derivation, and without it that child can land and retire with `class C { static { await 0 } }` accepted in module mode. **Placing the child umbrella **A**'s derivation mints for §15.7.1's `Contains await` rule in that child's `Deps` is therefore a mandatory output of this derivation too**, on the same reading as the edges onto the child umbrella **9da**'s derivation mints above and by the same rule that keeps it out of this row's own cell. **Measured instance the derivation must produce**, at `658cc302`: `WithClause` is in both productions — `ImportDeclaration : import ImportClause FromClause WithClause[opt] ;` and `ExportDeclaration : export ExportFromClause FromClause WithClause[opt] ;` (`webref body ecma262 sec-imports` / `sec-exports`) — and is absent in both layers. `grep -rl 'ImportAttributes' crates/` and `grep -rl 'WithClause' crates/` each return **0** files; `ast.rs:695-699` `ImportDecl` carries only `specifiers` / `source` / `span`; `parser/module.rs:22-24` and `:86-96` call `expect_semicolon()` immediately after the module specifier. So `import data from './data.json' with { type: 'json' }` is a parse error, and the re-export form with it — a defect no fixed precondition list held, because it is a grammar production rather than a compiler arm | — | `#11-vm-dynamic-import` | T1 | — |
| **—** | `Function`/`eval` — ⚠ **had no slice, no deps and no owner (Codex R5)**; §9 dec. 7 recorded the policy decision as unowned, so I-6's core strict-mode `eval` surface could stay unimplemented forever inside a *completeness* program. ⚠ **Two different kinds stood in this row as one, and "it is a policy call, not a language slice" is withdrawn — true of one half, false of the other.** **(1) The language half is code — and it is *divided*, ⚠ which this row asserted as one undivided "language half" against §4 I-6's own correction**: ECMA-262 **§19.2.1** `eval ( source )` with its **§19.2.1.1** PerformEval, and **§20.2.1.1** `Function ( ...paramArgs, bodyArg )` with its **§20.2.1.1.1** CreateDynamicFunction (§-numbers and titles from `webref heading ecma262 19.2` / `20.2.1`, AO numbers from `webref aoid ecma262 PerformEval` / `CreateDynamicFunction`) — of which **core owes the strict *direct* `eval` path through PerformEval, the strict-source *indirect* `eval` path through the same AO, and strict-source `Function` through CreateDynamicFunction — and no sloppy half of any of them, sloppy *evaluation* on the direct path and sloppy *source* on the other two**. ⚠ **The reading that stood here is superseded by §4 I-6, which draws the line on the strictness of the *evaluation* rather than on the spelling — and on the direct path that is not the strictness of the parsed source: PerformEval *step 12* makes a strict caller's `eval` strict whatever the source's own directive says.** Superseded text: the other two forms were called sloppy *by construction*, not by an omitted directive — PerformEval **step 1** `Assert: If direct is false, then strictCaller is also false` with **steps 12-13** setting `strictEval` from `strictCaller` or from the script's own directive, and CreateDynamicFunction **step 28** reaching `OrdinaryFunctionCreate` over the **step 26** `envRecord` = `currentRealm.[[GlobalEnv]]` with no step forcing strictness — so that indirect `eval` and the global `Function` constructor were LegacySemantics compat whole. Neither step closes the form: **§19.2.1.1** PerformEval **step 13** `Else, let strictEval be ScriptIsStrict of script` (**§16.1.2**) is reached exactly when `strictCaller` is false, i.e. on the indirect path, so `(0, eval)('"use strict"; …')` runs **strict**; and **§10.2.3** OrdinaryFunctionCreate **step 7** `Let strict be IsStrict(body)` (**§11.2.2.1**) with **step 10** makes `Function('"use strict"; return this')()` strict as well (§-numbers from `webref aoid ecma262 PerformEval` / `CreateDynamicFunction` / `ScriptIsStrict` / `IsStrict` / `OrdinaryFunctionCreate`; steps from `webref body ecma262 sec-performeval` / `sec-ordinaryfunctioncreate`). **The sloppy half of each of the three forms is LegacySemantics compat, not core — sloppy *evaluation* on the direct path, where §19.2.1.1 *step 12*'s `strictCaller` disjunct makes a strict caller's directive-less `eval` a strict eval and so core's, and sloppy *source* on the other two, where step 1's assert leaves the handed source deciding** (§4 I-6; CLAUDE.md *Core vs compat split*), and what the umbrella **`#11-vm-function-constructor-global`** (§8 — this row's own Slot cell) charters is the strict children plus the hand-off of the three sloppy halves — never all of PerformEval and CreateDynamicFunction as one core surface opens when its registered trigger fires; §8's row says in as many words that the slot *opens its own umbrella when triggered*, and §5's head reads such a slot as an umbrella row from the moment it says so, so the producer carries an id a `Deps` cell can name today rather than one invented when the trigger fires. **(2) Whether `eval` is permitted ships nothing**: it is a policy decision, and not an ECMAScript question at all. **The spec joins the two at exactly one host hook.** **§19.2.1.2** HostEnsureCanCompileStrings is *host-defined* — it exists, in its own words, to let host environments block the functions that interpret and evaluate strings as code — and its **default implementation returns NormalCompletion(unused)**; PerformEval reaches it at step 5 and CreateDynamicFunction at step 11 (`webref body ecma262 sec-hostensurecancompilestrings` / `sec-performeval` / `sec-createdynamicfunction`). ⚠ **That the hook's *default* is permissive is what makes it a gate, not what dissolves one, and "lands against the default" was this row's reason for carrying no `Deps` — withdrawn.** The default is stated as prose in §19.2.1.2 ("The default implementation of HostEnsureCanCompileStrings is to return NormalCompletion(unused)"), not as a numbered step, and it is the *host's* to replace; step 5 and step 11 above are inside the two algorithms this program ships, and both run the hook before either parses. So a renderer that exposes these globals against the unreplaced default executes strict `eval` and `Function` under a CSP that forbids string compilation — precisely what `docs/design/ja/10-network-architecture.md:316` assigns the Renderer to enforce. Nothing supplies that gate today: `git grep -nI HostEnsureCanCompileStrings -- crates` returns **0** hits and so does the same search for `EnsureCSPDoesNotBlockStringCompilation`, against a positive control `git grep -nI HostPromiseRejectionTracker -- crates` that returns one (`vm/natives_promise.rs:288`), so the zero is an absence and not a search that reaches nothing. §9 dec. 7 therefore records the question **and** gates the algorithms: the dependency is carried in this row's `Deps` cell and in §8's `Trigger`, not in prose. ⚠ **And the policy half does not belong in this document**, which is an ES-language completeness plan: CSP already has a home in the security layer — `docs/design/ja/08-security-model.md` §8.2 *Webセキュリティポリシー* places CSP in the `elidex-security` crate, parsed in the Network Process and enforced in the Renderer — and that layer, not this plan, is where "is `eval` permitted, and under which policy" is settled and plugged into the hook above. ⚠ **The Why/Trigger/Re-eval triple now lives in §8's table**, which is this document's registry — a full deferral triple carried in a slice-table row is invisible to §8's derive recipe (Forward rule). This row is a pointer rather than a slice | — | `#11-vm-function-constructor-global` (§8) | policy | **prereq (blocking)**: a deny-capable §19.2.1.2 `HostEnsureCanCompileStrings` — wired to the Renderer's CSP, or defaulting to deny — lands before either global is exposed |

**Ordering rationale.** 0a first on severity (process abort). *(Rounds 2-9 put a `vm/dispatch.rs`
prereq split ahead of it; removed — see the 1000-line check below.)* The 0b family next — at the baseline a silent no-op (0a has since made it a scoped throw; the *implementation* is still the family's) on the
ubiquitous swap/destructure idiom, and it shares `expr_assign.rs` with 0a. Inside the family the only
thing stated here is why an edge is or is not owed: `0bb → 0ba` and `0bc → 0bb` were shared-arm
arguments rather than consumed artifacts and were deleted at PR-B, while the **Pa** edge the child of umbrella 0bc that owns its [C39]→[C36] conformance claim carries is owed
because its [C39]→[C36] conformance claim inherits the inverted contract otherwise. What the cells
then say is the cells' to say. 0ca discharges I-1
program-wide and 0cb lands the conformance table (§7.2) so every later slice inherits a baseline.
Inside the P family the only chain is **Pb → Pa**, and it was inverted here until PR-B: **Pb** first
because it is behaviour-preserving and because Pa's own handler caller has no completion kind to pass
until Pb's transport exists (Pa's row), then **Pa**. **Pc** and **Pd** sit outside that chain, per
their rows: Pc's deliverable is the *removal* of the `iter_close` calls WebIDL §3.2.21.1 never asks
for, and Pd's charter is §14.7.5.7 sequencing — neither consumes what Pa or Pb produces, and whatever edges Pd's children do owe are placed by that umbrella's own derivation, not by this paragraph. ⚠ **Pa's dependents
are read off the Deps column, not listed here** — the naming of 0bc alone once read as that list, and
the row that closes an iterator on abrupt *per-item* resolution (**9db**, the combinator half of what
was then a terminal 9d) sat outside it with an empty Deps cell while 7a / 9a / 9c carried the edge (all three are umbrellas now and carry `—`; the sentence records the state at the revision the undercount was measured in).
`sed -n '/^| # | Slice | Primary/,/^$/p' <this file> | grep '^| \*\*' | perl -pe 's/\\\|/\x01/g' | awk -F'|' '$7 ~ /\*\*Pa\*\*/'` over §5 is the derivation — the `Deps` **cell**, through the same escaped-pipe splitter §4 uses, never a prose spelling — → **2** rows (`6b`, `9db`) at this revision — ⚠ *it read **3** (`0bc`, `6b`, `9db`) and was staled by this document's own conversions: 7a's cell became an umbrella's and, in the same sweep, so did 0bc's, so both `Deps` cells read `—` and the derivation returns neither*; negative control, the same command with `$7 ~ /\*\*Pz\*\*/` → **0**, so the field test discriminates. ⚠ `grep -c 'Depends on Pa'` is **not** the derivation: it returns **2** — one prose spelling (9db's cell) plus its own line, re-derived at `29705dd6`; it was **3** until `fb43da49` retired 7a's spelling — and omits `6b`, the exact undercount this paragraph exists to prevent;
the child of umbrella 0bc that owns its [C39]→[C36] conformance claim is the instance whose reason is stated (its [C39]→[C36] conformance claim inherits the inverted
contract otherwise), not the extent. And
**the child `#11-vm-typed-array-family-layering-and-gate`'s derivation mints for the family's location decision** (§8) must land before **Pa** — the edge is read from Pa's `Deps` cell, never from this paragraph, and naming the slot itself would name nobody — before Pa rather
than before the family, since Pb's touch set reaches no `vm/host/` file at all. **Umbrella 1a runs its derivation at its own start, and the child umbrella 1b mints for the argument-emit helper follows the child 1a's derivation mints for `lay_out_call_args`** — both endpoints are children, neither is an umbrella — the edge is that child's and is placed by both derivations, never from this paragraph; "1a then 1b" named an umbrella as a schedulable position, and under §5's row-kind rule no event advances an order past one. The registered slot ordering and standing directive are the *reason* the pair sits here. The **2a family**, **2b**
and **umbrella 3's children** follow
(new machinery), Slices 2aa/2ac/2b ordered by their Deps column. The edges onto them belong to
**umbrella 5's children** and its derivation places them (its row says so): they are owed
for the artifacts that row names — the field record and the instance-initialization timing a private
field is defined through — and which child carries which is read there, not restated here. **S** follows **the child of umbrella 3 that owns the super-property lowering**
for the reason its row states. Inside Slice 4 the only thing this paragraph states is the *reason* an
edge exists at all: I-3 requires the child of umbrella 4a that owns the tagged-call lowering to reach its tagged-template argument list through the child umbrella 1b's derivation mints for the argument-emit helper
rather than emitting a second path, which is what that artifact buys. Which rows carry the edge is read
from the `Deps` cells — the 0b row two paragraphs down withdraws exactly this kind of restatement, and
writing the verdict here again rebuilds the copy it removed. D runs after the connecting slices so it deletes only what
remains. **B**, **L** and the **E** family sit outside that chain — no row's deliverable is an input
to any of them (⚠ for **L** and **B** that is now a statement about the children each derivation mints,
both being umbrellas carrying no ordering of their own; whether any of them takes an inbound edge is an
output of that derivation — and for **B** at least one does, the edge onto the child umbrella **C**'s derivation mints for the binding-cell representation, which its own row re-points), and inside E nothing is ordered — row E's kind is undetermined, so it carries no ordering. **O** and **N** are
outside the sequence entirely: each is scheduled from its own owner per §1.0 and its row here is a
**pointer row** in §5's sense. **R** and **M** are outside it as well, but they are **umbrellas
promoted out of this plan** — their rows carry the marker — so each still runs its own derivation and
mints terminal children at its own start, and every field of theirs is read under the umbrella entry
rather than the pointer one; treating them as slot-owned pointers would skip that mandatory
umbrella-start work. N's row records the seam it shares with Slice 3, which is a coordination note, not an
ordering — but its **S** edge *is* ordering (same AO, not a shared seam; N's cell states it), so N is
scheduled from its own slot no earlier than S.

**The 6 family's edges point T2 rows at a T3 one, and this paragraph said nothing about the 6 family
at all until PR-B.** §2.4's taxonomy orders by severity and these edges run the other way: the **6a**
and **6b** work is T2 while **9da** is T3. They hold anyway, because a Deps edge records that the earlier row's
deliverable is an *input*, not that its defect is worse: the child of umbrella 6a that owns `.return`
and 6b's `for await` drain
both reach §27.5.4.7.1 PromiseResolve and therefore the resolving-functions closure the child umbrella 9da's derivation mints for thenable assimilation delivers (each
cell traces its own chain), so landing either first writes the async settlement path against a
`settle_promise` that adopts only `ObjectKind::Promise` and rewrites it when that child lands. Severity says
which defect is worse; the Deps column says which artifact has to exist first — and on the 6a side,
that row now being an umbrella with no `Deps` cell, placing the edge on the child that consumes it is
a mandatory output of its derivation rather than a cell anyone can read today. ⚠ **A sentence naming
one further thing 6b waits on stood here and is deleted rather than extended.** That cell has since
gained two more entries, and a prose list maintained beside a growing `Deps` cell is the device the ⚠
paragraph at the head of this section withdraws — the restatement is always the copy that rots, and
this one had already stopped being complete. What 6b waits on is read from its cell.

**`import()` note**: tiered **T1** (silent `undefined` in ordinary script context), so it is *not*
deferred with the rest of ES modules as R1 implied. Interim: 0ca makes it throw loudly; the real
implementation lands with the child umbrella **M** mints for `import()`.

**Slice M preconditions — the rule is in M's row above; this paragraph records the one instance the
§2.2 pass-3 structural sweep produced.** `StmtKind::ImportDeclaration` and
`StmtKind::ExportDeclaration` are already grouped into `compile_stmt`'s **no-op arm**
(`stmt.rs:31-39`), while the parser and scope analysis otherwise support modules. Enabling
`parse_module` therefore makes `export const x = 1` compile to nothing **silently**. The child umbrella **M**'s derivation mints for the `parse_module` path must
fix that arm **before** wiring module parsing, and its plan-memo must state so — otherwise the
module program's first milestone ships the exact silent-wrong class this umbrella exists to remove.
This instance came from a sweep over `compiler/` arms, which is why it says nothing about the module
**grammar**; the grammar walk M's row mandates is what produces the rest, and the import-attributes
measurement there is the first thing it returns.

**Slices 7-10 coupled-invariant duty**: each child those umbrellas' derivations mint needs its own §2.5-style enumeration (see I-5 for the
Slice 7 / Slice 10 boundary obligations).

**Umbrella 3's frame-state axis** (R2 round 2): `super.x` in an *ordinary method* needs frame state that
does not exist. `vm/interpreter.rs:795-806` sets `home_class` **only on class-ctor frames**, and
`vm/value.rs:1043-1045` documents this as "fail-closed-by-construction: a non-ctor method frame has
`home_class = None`, so any future super-property reader trips a SyntaxError fallback".
⚠ **and the frame-state audit named only one of the two call entry points (Codex R5).**
`call_internal` **independently hardcodes** `home_class = None` (`vm/interpreter.rs:589`, whose own
comment says "always `None` on the `call_internal` entry") and is the `NativeContext` path — so
`Function.prototype.call` / `.apply` lose `[[HomeObject]]` even after direct method dispatch is
fixed, and a method containing `super` breaks when invoked as an extracted function. The home object
belongs on the **closure**, threaded through *both* entries (§§13.3.7.3, 9.1.1.3.5), with a
regression like `const m = new B().m; m.call(receiver)` — ⚠ **which as spelled carries no `super` and so cannot fail on this defect**: measured at
`658cc302`, `class A {} class B extends A { m(){ return 'x' } } const m = new B().m; m.call({})` answers
`'x'` here and under Node alike. The regression is
`class A { g(){ return 1 } } class B extends A { m(){ return super.g() } } const m = new B().m; m.call({})`
→ `1`, red at `658cc302` (`TypeError: Cannot convert undefined or null to object`) — as is its
direct-dispatch control `(new B()).m()`, for the missing-emit reason the `parser/arrow.rs:104-119` row
records, so this regression isolates the `call_internal` entry only once that emit lands. Original finding: this row is
**not** emit+dispatch only — a child must add `[[HomeObject]]`-equivalent state on the closure
([C24]/[C25] both key off it), which is axis (b) of the umbrella row's derivation. The §2.2/§2.3 Layer-A/Layer-B decomposition has no row for this third (frame-state)
dimension; adding it is a mandatory output of umbrella 3's derivation, which places it on the child that
owns the frame-state axis, and that child's memo carries it.

⚠ **Slice 2ab field-initialisation ordering — the contract below is wrong as stated (Codex R2, P1).**
Applying fields to `construct_synchronous`'s **final, post-substitution** value is not what the spec
does. ECMA-262 **§10.2.2** `[[Construct]]` initialises a *base* class's instance elements **before**
the constructor body and performs the explicit-Object-return substitution **afterwards**; a *derived*
class initialises when `super()` establishes `this`, via **§7.3.33** `InitializeInstanceElements`.
Under the post-substitution reading, `class A { x = 1; constructor() { return {}; } }` wrongly adds
`x` to the returned object, a base constructor body cannot observe `this.x`, and a derived
constructor that returns an object without calling `super()` acquires fields it should not have.
**Initialise at base frame entry, or immediately after a successful `super()`, using the receiver
established there** — not the value `construct_synchronous` finally returns. The custom-element
concern below is real and unchanged; it constrains *which* receiver, not *when*.

**Slice 2ab custom-element upgrade receiver** — the measurement that folded the separate host-layer
row back into 2ab (this paragraph carried that row's contract until PR-B). The upgrade caller passes
one object in **both** receiver positions: `invoke_upgrade` calls
`ctx.vm.construct_synchronous(constructor, JsValue::Object(wrapper_id), &[], CallMode::Construct { new_target: constructor }, Some(wrapper_id))`
(`vm/host/custom_elements/upgrade.rs:315-323` at `658cc302`), so the receiver argument and the
pre-allocated instance are the same `wrapper_id`. The `HTMLElement` constructor's upgrade branch
hands back that same object rather than allocating one: it reads the construction-stack entry's
entity, looks the wrapper up with `ctx.host().get_cached_wrapper(entity)` (`html_element.rs:199`) and
returns `Ok(JsValue::Object(wrapper_id))` (`:219`). And `dispatch_super_inner` writes the constructed
object back onto the derived frame before anything the constructor body does can observe it —
`self.frames[frame_idx].this_value = result` and `.new_instance = Some(id)`
(`vm/dispatch_class.rs:150-154`). So the receiver the child of umbrella 2ab that owns instance-field initialisation initialises against, whether at base frame entry
or immediately after a successful `super()`, is already the wrapper the upgrade published; the only
way to reach a different object is an explicit `return otherObj`, which `upgrade.rs:350-358` already
rejects with a `NotSupportedError` (`matches!(value, JsValue::Object(id) if id != wrapper_id)`)
rather than initialising anything.
A host-layer slice would therefore decide the same receiver question the child of umbrella 2ab that owns instance-field initialisation decides, one layer down,
with nothing passing between them; the deliverable that remains is the no-regression test owed by the child of umbrella 2ab that owns instance-field initialisation on the
upgrade path.

**Slot triggers this program fires**: `#11-compiler-class-emit-readability` and
`#11-reflect-apply-ce-test`. Both now have **§8 rows** — a trigger stated only here fires into a
paragraph nothing re-reads at re-eval time (Forward rule), and both slugs return **0** against the
live ledger, so neither is registered anywhere else either.

**1000-line touch-time check — the standalone prereq split is withdrawn (2026-07-27), on a narrower
ground than the first statement of this section claimed.** Rounds 2-9 all carried a mandated
"standalone prereq split branch" for `vm/dispatch.rs`.

⚠ **The 2026-07-27 reversal shipped after the [§15](2026-07-vm-p4-umbrella-review-rounds.md) convergence call, ungated, and its measurements did
not survive re-derivation** — caught by this document's own plan-review at PR-B (`#506`). Its
originals were 1036/1113 (93%), 68 arms ≤8, 13 arms >20, a `run()`-residue enumeration that omitted
two top-level items, and — the figure the argument turned on — "29 arms using inline loop control
flow (16 `continue`, 13 `return`)", which summed two occurrence counts as though the sets were
disjoint.

**Every arm-classification figure has been removed rather than corrected.** Two independent passes
agreed exactly on every number taken straight off `wc -l` or `grep -c`, and disagreed on every number
requiring an arm-boundary convention (arm span, size buckets, control-flow arms) — the convention is
a choice, so those figures were argument dressed as measurement. What remains is re-derivable in one
command each:

| Measure | `f7d9b5ce` (rounds 2-9 basis) | `658cc302` (0a landed) |
|---|---|---|
| `wc -l vm/dispatch.rs` | 1112 | **1103** |
| `wc -l vm/dispatch_{class,helpers,ic,iter,objects}.rs` | 1725 across 5 files | **1888** |

*(An arm **count** is deliberately absent. `grep -c '^ *Op::.*=>'` returns 113 / 117 — it counts
pattern lines, and multi-pattern arms like `Op::IncElem | Op::DecElem =>` are one arm. Any figure
that reconciles the two encodes an arm-boundary convention, which is the class of number this
section got wrong. The dispatch match is order-100 arms; nothing below needs it sharper.)*

Two conclusions, and they are **not** the same conclusion:

1. **The match is not split.** CLAUDE.md's discipline is cohesion judgment, not line-count mechanics,
   and names the exemption: *「一枚岩の cohesive unit・巨大 generated table・**flat な case table** は
   対象外」*. A ~100-arm opcode dispatch table is that case, and that clause is the whole of the
   argument. *(The reversal also argued the match "cannot be split without a behavioural rewrite"
   because N arms `continue`/`return` the loop and read loop-local state. Removed: Slice 0a itself
   relocated `Op::IncElem | Op::DecElem` — an arm carrying two inline `continue`s — into
   `dispatch_helpers.rs::op_inc_dec_elem`, by returning `Result` and calling `throw_error` at the call
   site. No control-flow enum, no semantic rewrite. The premise was false.)*

2. **The file is NOT exempt from reduction, and the earlier text claimed it was.** "The match cannot
   be split without a behavioural rewrite" was read as covering the file; it does not follow, and per
   conclusion 1 it was not even true of the match. The `dispatch_*.rs` family is the in-tree seam that
   absorbs arm bodies, and Slice 0a moved the numbers in the right direction while implementing an
   unrelated feature: `dispatch_helpers.rs` 237→391, `dispatch_objects.rs` 429→438, `dispatch.rs`
   1112→1103.

**Decision: no standalone prereq-split PR — because the debt is discharged continuously, not because
the file cannot be reduced.** The forward rule for every slice that touches this file: extract the
*arm body* into the existing `dispatch_*.rs` family, never grow an arm in place — **choosing the
destination by I-5's predicate (the `engine` feature gate + host-binding dependency), not by the
file-family name.** Measured at `658cc302`: `dispatch_{class,helpers,ic}.rs` have zero
`feature = "engine"` occurrences; `dispatch_iter.rs` has 3 (`:114`, `:127`, `:153`) and
`dispatch_objects.rs` 2. ⚠ **The predicate is the *region*, not the file** — those three gates are
narrow bands **inside a 370-line file**, and everything outside them, including `iter_close` at
`:354` (which [§6.2a-2](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) calls the canonical §7.4.11 implementation), is core-only. *(370 is the
**whole** `dispatch_iter.rs`, not a remainder: `git show <rev>:…/vm/dispatch_iter.rs | wc -l` gives
370 at `f7d9b5ce`, `658cc302` and HEAD alike. An earlier reading of this sentence as "the rest of
the file, 370 lines" double-counted.)* A file-level reading would forbid
the children umbrella **1a**'s derivation mints for the call-spread VM infrastructure, and Slice **Pa**, from the file both their module columns name, and Pa's whole deliverable is
re-signaturing that function. Read the predicate in **both directions**: do not place a core-only
body *inside* an engine-gated region, **and** do not extract an engine-bound arm body into a
currently core-only file by wrapping it in a fresh `#[cfg(feature = "engine")]`. That rule is the
discharge mechanism, so a slice that adds an arm and leaves the file larger has not complied.
(The withdrawn text also argued the reversal "takes a PR off the critical path and unblocks the T0 fix
immediately" — schedule is judgment-supporting information, not a design constraint (CLAUDE.md
*Ideal over pragmatic*), so it is removed rather than restated.)

**Files the watch list still owns** (`#11-d17b-dispatch-expr-file-growth`): `vm/interpreter.rs` 1366
(**umbrellas 1a and 3**, through the children each mints for the work), `vm/value.rs` 1187 (**umbrella 3**, through the children it mints for the work). **Not on any watch list and >1000**:
`vm/object_kind.rs` 1700 (**umbrellas 6, 7 and 10**, through the children each mints for the work — and see the cross-lane note in §8),
`vm/gc/collect.rs` 2074 (**umbrella 7b**, through the children it mints for the work), `vm/gc/trace.rs` 1255 (**umbrellas 7a and 7b**, through the children each mints for the work — 7a's unconditional
marking arm for the `[[MapData]]` / `[[SetData]]` variant as well as 7b's ephemeron fixpoint; ⚠ the
attribution to 7b alone was read off 7a's "No GC-tracing change", which that row now withdraws as
false — and **every instance §4 I-7 enumerates is a further reader of that same file**, since what I-7
obliges is an arm in it. The instances are **not listed here**: this note carried a copy for one
revision and the copy was wrong within that revision — it named Slice N after the obligation had moved
to N's slot, and it omitted Slice C after C's row gained the obligation. A second copy of an
enumeration drifts from the first by construction, so the list has one home and this is a pointer to
it). ⚠ **Two more, and the reason they were missing is that this list was curated rather than derived.**
The population is *every file a §5 `Primary module(s)` cell names*, and it is one command — over this
document's blob `2718d8d4`,
`git show 2718d8d4:<this file> | sed -n '/^| # | Slice | Primary/,/^$/p' | grep '^| \*\*' | perl -pe 's/\\\|/\x01/g' | cut -d'|' -f4`,
then `git show 658cc302:crates/script/elidex-js/src/<f> | wc -l` for each `.rs` token it emits: **63**
rows and **7** files over 1000. *(The **47** distinct files that stood here is withdrawn rather than recounted: the cells emit **50** distinct `.rs` tokens of which **44** resolve under `crates/script/elidex-js/src` at `658cc302`, and no tokenisation of the command's output returns 47 — a convention-dependent figure, the class this section removes.)* Five are already named above; the two that were not are
**`vm/well_known.rs` 1588** (Slice **8d**) and **`vm/host/structured_clone.rs` 1077** (Slice **Pc**).
Pc's own cell now discharges the second as a mandatory prereq split; 8d's plan-review owes the first a
verdict. *(Anchored to the blob because the input is this document: the same revision removes
`structured_clone.rs` from **1a**'s module cell and leaves it in **Pc**'s, so the set of 7 is unchanged
by it — re-run the command rather than reading the figure forward.)* Each needs a cohesion verdict in its own
slice's memo — and this reversal is the precedent: **measure the file's shape before assuming a
split**.

**And the verdict this check has never stated about the document it is written in — stated, and
discharged in the commit carrying this paragraph.** This memo was **3714** lines at `f754c873` and
**3857** at `3526b722`, the split's parent — `git show f754c873:<this file> \| wc -l` and
`git show 3526b722:<this file> \| wc -l`, anchored to blobs because the split moves the answer — and
CLAUDE.md's *1000-line debt = touch-time split* discipline is a cohesion
judgment, not a `crates/**` scope: the sibling citation-hygiene lane recorded exactly this verdict
when its own umbrella crossed **1007** lines ([[project_citation-hygiene-program]]). ⚠ **From the
split on, `<this file>` in every command in this document means the umbrella alone**: the three
carve-outs below are separate files, and no command written here reaches into them. This document
reaches for the rule **ten** times (`grep -c '1000-line' <this file>` → **10**, re-derived at the
split, which moved it **12**→**10** by carrying two mentions into the round-disposition carve-out;
⚠ the same grep over the umbrella **and** its three carve-outs returns **15**, not 12 — the two that
moved plus **three** new ones, one per carve-out head naming the discipline that produced it, which is
the figure this very sentence would have carried stale had it been reasoned instead of run), and eight of the ten
name a `.rs` path or a per-file scope; the two that do not are the sentence above and this one, i.e. this passage, which is the first place in the document that does name this file. That is the omission shape this
section has now named twice — a rule swept over the population that motivated it rather than over
the population it reaches.

**Verdict: the seams are real, and the split landed here — a standalone, content-preserving prereq
commit, not a deferral.** The disposition this passage carried until then owed the carve to "the next
PR making a substantive edit to this document"; that was **wrong**, and its own evidence says so.
**This PR is that touch**: it *creates* the file — `git cat-file -e origin/main:<this file>` fails and
`2b4789eb` is the adding commit — so introducing a 3857-line memo *is* the substantive touch the rule
fires on, and there is no later PR for the obligation to be owed to. The cost the deferral argument
named is paid rather than avoided: every finding, round number and line anchor in the carved sections
is stated *about the pre-split document*, so each carve-out's head records that and **not one anchor
was re-derived by the move** — the carve relocates lines and rewrites pointers, and changes nothing
else. The three seams, boundaries by `awk '/^## §/{print NR}'` and sizes by `sed -n` plus `wc -l`,
first taken over the blob `git show f754c873:<this file>` and **re-measured over `3526b722`** before
carving:

- **§§16-18 — Slice 0a's landing record and its two review gates, 388 lines** (3327-3714 at
  `f754c873`; **3470-3857** at `3526b722`) → `docs/plans/2026-07-vm-p4-slice-0a-landing-record.md`. A record of
  a *landed* PR is a different document kind from a forward plan, and nothing in it is scheduled: §5's
  three specification fields are read from none of it.
- **§§10-15 — six round dispositions and the convergence call, 271 lines** (3056-3326 at `f754c873`;
  **3199-3469** at `3526b722`) → `docs/plans/2026-07-vm-p4-umbrella-review-rounds.md`. An append-only
  review archive, and its own headings run out of order ([§13](2026-07-vm-p4-umbrella-review-rounds.md) precedes [§12](2026-07-vm-p4-umbrella-review-rounds.md)), which is what append-only
  looks like rather than what a designed section looks like.
- **[§6](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) — Slice 1a + 1b detail, 492 lines** (1945-2436 at `f754c873`; **2030-2521** at `3526b722`) →
  `docs/plans/2026-07-vm-p4-slice-1a-1b-call-spread-detail.md`. The *asymmetry* is the seam, not the size: §5
  states that deriving a slice's design belongs to that slice's own mandatory plan-review, and [§6](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) is
  one slice pair's design memo living inside the umbrella. No other slice has one here.

The plan does cite the record — [§13](2026-07-vm-p4-umbrella-review-rounds.md)'s enumerate-by-command rule, [§16](2026-07-vm-p4-slice-0a-landing-record.md)'s Slice 0a record, [§17](2026-07-vm-p4-slice-0a-landing-record.md)/[§18](2026-07-vm-p4-slice-0a-landing-record.md)'s
ratification of decision 5 — so the carve is a **pointer rewrite**, not a clean cut, and that is
part of what it cost. **The rewrite is done, and its extent is now measurable without the convention
this passage refused to trust.** A bare `§NN` count still cannot separate this document's own section
numbers from ECMA-262 clause numbers of the same spelling — so the population was taken
**structurally** and narrowed by a **derived** section set plus a **named** exclusion list, never by a
vocabulary of section names. Structurally: every `§<n>` token in the pre-split file
(`grep -oE '§[0-9][0-9]*(\.[0-9]+)*[a-zA-Z]*(-[0-9]+)?' <this file> \| wc -l` → **2392** at
`3526b722`), narrowed to the tokens this document uses as **its own** headings
(`grep -oE '^#{2,3} §[0-9][0-9.a-zA-Z-]*' <this file>`, whose carved subset is `§6`, `§6.1`, `§6.2`,
`§6.2a`, `§6.2a-2`, `§6.2a-3`, `§6.3`, `§6.4`, `§6.5`, `§10`-`§18`, `§18.1` and `§18.2`), then minus
the bodies being carved out, leaves **87** occurrences. Named exclusion list: **seven** of the 87 are
spec clauses of colliding spelling, named rather than pattern-matched away — WHATWG URL `§6.2` (four
occurrences), W3C IndexedDB `§6.1`-`§6.6` (one), WHATWG URL `§6` (one) and ECMA-262 `§14` (one). Line
anchors for them are deliberately **not** carried, because this very commit moves line numbers; the
residual command below is what locates them. The other **80** are references to a carved section, and
each is now a markdown link naming its carve-out, so the extent is re-derived **from the artifact
rather than from the convention**:
`grep -oE '\]\(2026-07-vm-p4-(slice-1a-1b-call-spread-detail|umbrella-review-rounds|slice-0a-landing-record)\.md\)' <this file> \| wc -l`
→ **84** in the umbrella as it now stands — **80** until the umbrella-naming sweep of this revision,
which added four: three in the `edge NN` namespace declaration at §6's carve-out stub and one in the
evidence-anchor rule, each a link that did not exist before because the sentence carrying it did not.
⚠ *A figure written "as it now stands" is invalidated by the next edit to the file it counts, and it
was — by an edit in this same PR, four commits after the sentence was written. The re-run is the
claim; the number beside it is only the last time anyone ran it.* *(The rewrite of this passage was
separately net zero, dropping eight such references and reintroducing eight — measured, not
assumed.)* The **residual** — a
carved heading token in running prose that is neither a link nor a backticked spelling under
discussion — is the check that does not depend on that arithmetic, and it returns the seven named
collisions and nothing else:
`python3 -c "import re,sys;B=chr(96);L=open(sys.argv[1]).read().split(chr(10));t=re.compile('§[0-9][0-9]*(\.[0-9]+)*[a-zA-Z]*(-[0-9]+)?');H={'§6','§6.1','§6.2','§6.2a','§6.2a-2','§6.2a-3','§6.3','§6.4','§6.5','§10','§11','§12','§13','§14','§15','§16','§17','§18','§18.1','§18.2'};print(sum(1 for l in L if not l.startswith(chr(35)) for m in t.finditer(l) if m.group(0) in H and l[:m.start()].count(B)%2==0 and l[m.start()-1:m.start()]!='['))" <this file>` → **7**.

**What is *not* a seam: §5 itself, 1020 lines and over the threshold alone.** Its conventions
passage and its table are one unit — the conventions define what each cell of the table means and
the table is the only reader of them — so CLAUDE.md's *「一枚岩の cohesive unit」* exemption is why it
stays whole. That is a reason about §5's shape and **not** a reason the file stays whole, which is
the same two-conclusions-are-not-one-conclusion the `vm/dispatch.rs` reversal above turns on.
*(**1020** is the `f754c873` figure; at `3526b722` §5 spans **954-2029** = **1076** lines. The
exemption is a judgment about shape, which neither figure moves.)*

**Owner and discharge, and why this mints no slot.** The obligation is CLAUDE.md's touch-time one —
*「触った時点で bounded 化する」* — which is discharged by the PR that touches the file, not scheduled
from a ledger, so it is not a deferral and §8's Forward rule (which is stated over *surfaces this
program does not implement*) does not reach it. **It is discharged here**: the commit carrying this
paragraph carves the three seams and does nothing else, ahead of the rest of this PR's edits, exactly
as §5 requires of a `crates/**` file. The earlier reading — that the converge rounds "only append to
§§10-18" and so are not that PR — mistook the *appends* for the touch; the touch is the introduction
of the file, which is this PR.

Globals this program does not implement are registered in §8's table, like every other deferral.
`Intl`, `Atomics` and `WeakRef`/`FinalizationRegistry` moved there; this paragraph used to carry
their dispositions in prose, inside a section about `vm/dispatch.rs` line counts, which is not a
place a deferral can be retired from.

---

## §6. Slice 1a + 1b detail — call-argument spread → **carved out**

Moved **verbatim** to `docs/plans/2026-07-vm-p4-slice-1a-1b-call-spread-detail.md` by this document's
touch-time prereq split (§5's *"And the verdict this check has never stated…"* passage carries the
seam derivation, the measured ranges and the discharge). `§6.1`, `§6.2`, `§6.2a`, `§6.2a-2`,
`§6.2a-3`, `§6.3`, `§6.4` and `§6.5` keep their numbers there, and every `§6…` reference in this
document links to that file — so scheduling from §5, §8 and §9 reads exactly as it did before the
carve.

⚠ **The `edge NN` namespace went with §6.4, and this sentence is its single home.** Every bare
`edge NN` / `edges NN/NN` in this document, in
[the review-rounds record](2026-07-vm-p4-umbrella-review-rounds.md) and in the detail file itself
names an entry of **§6.4's numbered edge matrix** in that file, and nothing else in this program
numbers edges. Most such references carry no pointer to §6.4, and before this sentence they resolved
against a section number this document no longer contains. The repair is one declaration and not one
annotation per site: a namespace is one of the things §5 says has exactly one structured home, and a
pointer on every reference would be a second copy of it at every reference.

Two commands, and they are the claim rather than an illustration of it. References, per file, over
the four files of this program in the order they are listed above — **29 / 21 / 2 / 0**:

    grep -o -E '\b[Ee]dges? \*{0,2}[0-9]' <file> | wc -l

⚠ *Not* `grep -c`, which counts matching **lines**: §5's rows are one line each and carry several
references apiece, so the line count reads 15 / 17 / 2 / 0 and disagrees with the reference count on
two of the four files. Definitions, from §6.4 — **1-35, contiguous**:

    grep -o -E '(^|· )\*{0,2}[0-9]{1,2}\. ' \
        docs/plans/2026-07-vm-p4-slice-1a-1b-call-spread-detail.md |
      grep -o '[0-9]*' | sort -n | uniq | tr '\n' ' '

A reference outside that range is dangling, and the second command is what says so rather than a
reader's memory of how far the matrix runs.

---


## §7. Verification strategy

**§7.1 Per-slice unit tests** in `crates/script/elidex-js/src/vm/tests/`, one module per slice,
covering that slice's full edge matrix.

**§7.2 Permanent ES-language conformance table** — land the probe harness as
`vm/tests/tests_es_language_surface.rs`: a declarative `(source, expected)` table asserting current
truth across the language/builtin surface. This is the artifact that would have caught every gap in
§1.1 **and** this plan's own §1.2 over-claim. Rows for not-yet-implemented slices assert their
**known divergent** value with a `KNOWN-DIVERGENCE (#11-slug)` marker, flipping to the spec value in
the fixing slice — the `vm/tests/tests_dataset.rs:283` pattern. **Deliverable of Slice 0cb**, so
every later slice inherits the baseline.

⚠ **The row shape needs a crash-aware outcome (Codex R2) — ratified as §9 decision 16.**
"One row per §2.2 row" includes
`f(a×256)`, whose defect is a **process abort** — `assert!(arguments.len() <= 255, …)` in
`compiler/expr_member.rs` — and is owned by the child umbrella **1b**'s derivation mints for the argument-emit helper — ⚠ **", which lands after 0cb" is deleted here for the reason §9 dec. 16 gives in full: ordering has one structured home and `0cb` occurs in no cell of it.** No `(source,
expected)` *value* comparison can be written for an input that panics the compiler, so 0cb's
permanent suite cannot pass as specified. **Resolution: the outcome type expresses
`Panics`/`Throws` alongside a value** (§9 dec. 16, which is also the single home for why sequencing
the arity fix ahead of the table is excluded).

⚠ **The row-derivation rule needs a second source** (round 8). §9 dec. 9(b) sets "one row per §2.2
defect row", but §2.2 is **Layer-A compiler-emit only** — so the rule structurally produces *no* row
for the T3 absence surface (`Map`/`Set`/`WeakMap`/`WeakSet`, `Proxy`/`Reflect`, the `RegExp` ctor,
`Array.prototype.at`/`findLast`/`Object.hasOwn`/`String.matchAll`), which is roughly half of what
§1.1/§1.2 found. As written, 0cb ships the program's safety net with a hole exactly where the probe
found most gaps. Second source: one row per **§1.1/§1.2 absence** finding + per Slice 7-10 slot. Note the I-1
discharge reduces the number of divergence rows up front by converting silent stubs to loud throws.

⚠ **A third source, for the same reason on a different axis**: §2.2's derivation is scoped to what
its three passes read (`compiler/` arms, their marker comments, and productions that have an AST
variant — see that section's scope statement), so "one row per §2.2 defect row" inherits that scope.
Rows for what lies outside it arrive the way §2.2's own non-`compiler/` rows did — from the slice
that owns the code, derived against its own parent HEAD, at the point that slice lands. 0cb
therefore ships the table it can derive, and later slices add rows; the table is not asserted
complete at 0cb.

**§7.3 Standard gate** per slice: `cargo fmt --all` → `mise run ci` → `/pre-push` (6-stage) → push →
`/external-converge` (edge-dense ⇒ converge, per
[[feedback_gate-miss-on-edge-dense-escalate-to-converge]]).

**§7.4 No WPT dependency** — engine-independent language semantics covered by unit tests.

---

## §8. Slot ledger changes at landing

⚠ **This section was written before Slice 0a landed and is a *plan* for registration, not a record of
it. Re-derive both lists against the live ledger before acting** — at #489's landing 7 of the 11
below were already registered, so following the section as originally written would have minted
duplicates (`grep -c '#11-<slot>' <ledger>`; the ledger is the memory-dir
`project_open-defer-slots.md`, outside this repo, so **landing this document registers nothing**).

**Adopt (do not duplicate) 2 pre-existing slots** — this umbrella homes them:

- `#11-step9-class-extras` — scope "static members / private fields / getters & setters in class
  bodies / computed-name methods / static blocks / `Op::GetSuperProp` + `Op::SetSuperProp`".
  **Already in the SoT ledger** (adopted at #489's landing; the earlier claim
  that it lived only in `m4-12-pr-d17b-html-element-constructor-base-vm-landing.md` is superseded).
  Partially discharged already (computed method keys verified working, §1.2). ⚠ **Static
  fields are NOT among the discharged** — narrowed at PR-B to their *definition*; the initializer's
  receiver is wrong (§2.2's `expr_class.rs:402-427` row), owned by Slice **2ac**, and the probe that
  established the discharge (`static x = 1`) could not see a receiver. **The claim that this scope is "exactly Slices 2/3/5" is deleted, not amended.** Those three
  rows own class instance fields, super property references and private names; the scope string also
  names **class static blocks**, which none of them covers, and reading the scope as exhausted by the
  slice list is what hid the following. Measured at `658cc302` and re-derived at HEAD `d49465d0`
  (`git grep -n 'FunctionCompiler::new' <rev> -- crates/script/elidex-js/src/compiler/`; both revs
  give the same line numbers): scope analysis allocates a static block its **own function boundary** —
  `scope/visitor.rs:555` pushes `ScopeKind::StaticBlock`, and `compiler/resolve.rs:433` is
  `matches!(kind, ScopeKind::Function | ScopeKind::StaticBlock)` — while `expr_class.rs:457` builds
  the block's child compiler as `FunctionCompiler::new(fc.func_scope_idx, fc.current_scope_idx, true)`
  and `:464` finishes it against `func_scopes[fc.func_scope_idx]`: the **enclosing** function scope
  both times. Contrast `expr_function.rs:111` / `:243`, which pass the child's own `child_func_idx`.
  So a static block's block-local `let`/`var` bindings compile against the wrong scope. ECMA-262
  **§15.7.11** ClassStaticBlockDefinitionEvaluation step 5 creates the body with
  `OrdinaryFunctionCreate(%Function.prototype%, sourceText, formalParams, ClassStaticBlockBody,`
  `non-lexical-this, lexicalEnv, privateEnv)` — its own function environment (§-number from
  `webref aoid ecma262 ClassStaticBlockDefinitionEvaluation`, step text from
  `webref body ecma262 ClassStaticBlockDefinitionEvaluation`). **Generic static-block lowering is
  Slice 2b's**: the Slice 2 family already owns class-body emit in `compiler/expr_class.rs` and the construction
  path in `vm/dispatch_class.rs`, and no other slice touches the static-block arm. This slot cannot
  retire until Slice 2b tests **block-local binding isolation** (a `let` in a static block does not
  resolve to an enclosing binding of the same name) **and capture** (a closure created inside the
  block still reads the block's own bindings after it returns). **The 2a family, 2b, and the children umbrellas **3** and **5** mint for super-property lowering and private names, retag every in-code citation `grep -rn 'step9-class-extras' crates/` reports
  at retag time** — 6 at `658cc302` (`compiler/expr.rs`, `compiler/expr_assign.rs`,
  `compiler/stmt_loop.rs`, `vm/interpreter.rs`, `vm/value.rs`, `bytecode/compiled.rs`), where this
  section originally froze a 4-site list measured 2026-07-26; 0a added two. Retagging a frozen list
  is what leaves the dangle the sentence exists to prevent.
- `#11-dead-opcode-removal` — "`Op::CreateClass` verifiably dead; bundle with D-26 Op-enum
  re-baseline"; trigger **already fired** at #458. Becomes Slice D, broadened to the §2.3 set.
  **Still absent from the SoT ledger** (0 hits, present only in the `m4-12-pr-d17b-*` landing memos),
  as is `#11-d17b-dispatch-expr-file-growth` below — so both adoptions are genuinely outstanding,
  unlike `#11-step9-class-extras`.

**Also adopt** `#11-d17b-dispatch-expr-file-growth` (uncounted watch slot, D-17b r1/r2 landings;
homes the `vm/dispatch.rs` + `compiler/expr_class.rs` + `vm/interpreter.rs` + `vm/value.rs`
1000-line debt). ⚠ **The `dispatch.rs` facet is NOT discharged, and it is not a
no-action entry either** — §5's 1000-line check exempts the *match* from being split but explicitly
does **not** exempt the file from reduction; its discharge mechanism is the arm-body extraction rule,
so the slot's `dispatch.rs` facet is measured against `wc -l` at each slice that touches it (1103 at
`658cc302`, down from 1112). Its `interpreter.rs` / `value.rs` / `expr_class.rs` facets remain live. **Cold-gate recorded 2026-07-26** (⚠ *narrowed round 9 — the round-5 wording over-claimed; and the open-PR set below is that date's, since superseded — re-run `gh pr list` before relying on it*): **`vm/dispatch.rs` was clear** — verified no branch/worktree/PR touched it (PR #488 layout/ecs,
PR #487 shell, PR #486 dependabot manifest-only, `vm-input-value-as-date` plan-doc only, and
`domform-submittable-category` = 0 hits). But the wider claim "nothing touches
`elidex-js/{compiler,bytecode,vm}`" is **false**: the active L3 lane
(`elidex-wt-submittable`, `domform-submittable-category`) has committed changes to
`vm/object_kind.rs`, `vm/globals.rs`, `vm/mod.rs` and 6 `vm/host/` files. **`vm/object_kind.rs` is in
the module column of Slices 6/7/10** ⇒ cross-lane coordination required there, though not for 0a,
the 0b family, 0c and its sub-slices, or any child umbrella **1a** or **1b** mints.

**Forward rule.** Every surface this program identifies and does not implement gets a **row in the
table below with a slot id** — a deferral stated only in prose elsewhere in this document is
structurally unretirable, because (as this section already says) landing this document registers
nothing and the ledger is the SoT, so nothing outside this table is ever re-read at re-eval time.

**Slots this umbrella owns** — the table below, each with the required triple.

⚠ **The frozen registration partition that stood here ("12 rows = 8 already in the ledger + 3 still to
register + 1 to retire", measured at `658cc302`) is withdrawn.** It was already wrong before this
revision added rows: `#11-vm-yield-delegation-lowering` **is** in the ledger and the list of eight
omitted it. A hand count of this table goes stale every time the table grows — the same failure this
section's opening ⚠ warns about. **Derive it instead**, one command per row against the live ledger
(the memory-dir `project_open-defer-slots.md`, outside this repo):
`grep -c -- '#11-<slug>' <ledger>`, run over **every `#11-*` slug in the table — not only the
`#11-vm-*` ones**. ⚠ **The recipe used to sweep `#11-vm-*` alone**, which made every adopted
non-`vm` slot (`#11-step9-class-extras`, `#11-dead-opcode-removal`,
`#11-d17b-dispatch-expr-file-growth`, `#11-compiler-class-emit-readability`,
`#11-reflect-apply-ce-test`, `#11-webidl-sequence-dense-array-fast-path`) invisible to it **in
principle**, however many rows the table gained. Widened here.

⚠ **The derivation is anchored to the ledger, not to a repo commit** — the ledger lives outside this
repo, so a repo SHA cannot date its contents. Run against `project_open-defer-slots.md` as of
**2026-08-08** (the run's own date, recorded here because it is **not recoverable later**: the anchor is an mtime and the ledger has been edited since — `stat -f %Sm <ledger>` under this repo's macOS toolchain, `stat -c %y <ledger>` under GNU coreutils, already answers **2026-08-16** — so a re-run dates the *current* ledger and never this one, and re-establishing the figures below means re-running the `grep -c` sweep and reporting the new date beside it), it returns **nonzero** for
`#11-vm-iteratorclose-precedence-convention`,
`#11-vm-assignment-target-completeness`, `#11-vm-topropertykey-symbol-from-toprimitive`,
`#11-vm-operand-rooting-by-construction`, `#11-vm-internal-error-hard-exit`,
`#11-vm-delete-elem-raw-key-array-fast-path`, `#11-vm-statement-completion-updateempty`,
`#11-vm-typed-array-family-layering-and-gate`, `#11-vm-yield-delegation-lowering`,
`#11-vm-function-constructor-global`, `#11-step9-class-extras` and
`#11-webidl-sequence-dense-array-fast-path`; and **zero** for
`#11-vm-async-generators`, `#11-vm-builtin-prototype-static-sweep`, `#11-vm-dynamic-import`,
`#11-vm-property-key-lowering-unification`, `#11-vm-atomics-global`,
`#11-vm-weakref-finalization-registry`, `#11-vm-module-opcode-connects`,
`#11-vm-webidl-section-3-10-retag`, `#11-dead-opcode-removal`,
`#11-d17b-dispatch-expr-file-growth`, `#11-compiler-class-emit-readability` and
`#11-reflect-apply-ce-test` — those are what is still to register. The slots later revisions add also
return **zero** against the same ledger (`stat -f %Sm` = 2026-08-08 11:51) and join that set:
`#11-vm-block-declaration-instantiation` and `#11-vm-explicit-resource-management`, plus
`#11-vm-labelled-statement-break-target`, `#11-vm-per-iteration-loop-environment`,
`#11-vm-for-in-enumeration-semantics`, `#11-vm-arrow-lexical-super` and
**`#11-plan-memo-spec-field-single-home-check`** (same command against the same ledger, dated
2026-08-08, → **0**; controls run in the same invocation,
`#11-vm-iteratorclose-precedence-convention` → **2** and a nonexistent slug → **0**)
(`grep -c -- '<slug>' <ledger>` → **0** for each of the last
four, run 2026-08-09), and `#11-vm-parameter-environment-instantiation` and
`#11-vm-instanceof-callable-check` (same command, same ledger mtime, → **0** for both; control, run
in the same invocation, `#11-step9-class-extras` → **1** and
`#11-vm-typed-array-family-layering-and-gate` → **1**, so the pattern discriminates rather than
returning zero for everything), and `#11-vm-const-assignment-runtime-throw`,
`#11-vm-upvalue-tdz-check` and `#11-vm-named-function-expression-binding` (same command, same ledger
mtime, → **0** for each; the same two controls, run in the same invocation, → **1** each), and
`#11-vm-derived-constructor-this-binding`, `#11-vm-ordinary-function-name-length` and
`#11-vm-object-literal-proto-setter` (same command, same ledger mtime, → **0** for each; the same two
controls, run in the same invocation, → **1** each), and `#11-vm-private-name-early-errors` (same
command, same ledger mtime `stat -f %Sm` = 2026-08-08 11:51, → **0**; the same two controls, run in
the same invocation, → **1** each), and `#11-vm-webidl-sequence-early-exit-drain` and
`#11-vm-class-name-binding-initialization-order` (same command, same ledger mtime, → **0** for both;
the same two controls, run in the same invocation, → **1** each), and
`#11-vm-inbound-host-call-membership-test` (same command, same ledger — `stat -f %Sm` = 2026-08-08
11:51 — → **0**; the same two controls, run in the same invocation, → **1** each), and `#11-plan-memo-acceptance-falsifiability-check` (same command against the live ledger, `stat -f %Sm` = 2026-08-16 13:10, → **0**; controls in the same invocation, `#11-step9-class-extras` → **1** and `#11-vm-iteratorclose-precedence-convention` → **2**, so the pattern discriminates), and `#11-vm-native-function-length-on-allocator`, `#11-vm-structured-clone-layering-seam` and `#11-vm-float16array-global`, all three minted in this PR (same command against the live ledger, `stat -f %Sm` = 2026-08-19 23:38, → **0** for each; the same two controls, run in the same invocation, → **1** and **2**, so the pattern still discriminates) — ⚠ *the last two were outside this derivation's output until this revision, the very class the ⚠ above records, and they are found by the set-difference this paragraph prescribes rather than by re-reading it.*
`#11-vm-computed-compound-assignment`
also returns zero, but it is the
to-retire row below, not a registration gap; and the `Intl` row mints no slot at all, since it points
at an externally owned standing decision. ⚠ **Three slugs that *are* in the table were outside the derivation's output for four revisions** — `#11-vm-arrow-lexical-new-target`, `#11-vm-object-spread-source-coercion` and `#11-vm-declarative-environment-and-capture-representation` — which is the stated population ("every `#11-*` slug in the table") not being met rather than a class the recipe cannot reach; the omission is found by set-differencing the table's slugs against this paragraph's, not by re-reading it. Measured against the same ledger (`stat -f %Sm` = 2026-08-16 13:10): **0** for each of the three, so all three join the still-to-register set — and §1.0's "already have such a slot" is true of §8's table and **false of the ledger**, which the Forward rule makes the SoT. Controls in the same invocation: `#11-vm-iteratorclose-precedence-convention` → **2**, a nonexistent slug → **0**. Re-run rather than read forward.

⚠ **Slice slots with no row in the table below are stated as the *set* a command returns, never as a
count**, which is the remedy this section just adopted for its umbrella census and is adopted here
for the same reason: **the count was wrong, and the delta that renamed a slug enlarged the
population it was wrong about**. "Three" was true when three were known; a count cannot say which
member it is missing, and nothing re-derives it when a row is added. The derivation above cannot see
any of them, because it runs over the slugs in *that* table. The set-difference of §5's slugs
against this table's first cells — both halves falling out of one parse on field count, §5 rows
having 8 fields and §8 slot rows 6, exactly as §5's umbrella census parses them, and §5's deliberate
nonexistent-slug control `#11-vm-topropertykey-symbol-from-toprimitive-NOPE` excluded by name in the
same run — is
`python3 -c "import re,sys;C=chr(124);B=chr(92);L=open(sys.argv[1]).read().split(chr(10));n=lambda l:len(l.replace(B+C,chr(1)).split(C));cell=lambda l,i:l.replace(B+C,chr(1)).split(C)[i];S5={s for l in L if l.startswith(C+chr(32)) and n(l)==8 for s in re.findall('#11-[A-Za-z0-9-]+',l)};S8={s for l in L if l.startswith(C+chr(32)) and n(l)==6 for s in re.findall('#11-[A-Za-z0-9-]+',cell(l,1))};print(sorted(S5-S8-{'#11-vm-topropertykey-symbol-from-toprimitive-NOPE'}))" <this file>`
and it returns **8** at this revision: `#11-vm-call-spread-arguments`,
`#11-vm-class-body-parser-early-errors`, `#11-vm-class-private-fields`,
`#11-vm-map-set-collections`, `#11-vm-proxy-reflect`, `#11-vm-regexp-constructor-and-flags`,
`#11-vm-structured-clone-file-split`, `#11-vm-tagged-template-literals`. Re-run rather than reading
the membership forward.

⚠ **One of the eight is a registration gap the slug rename carried forward rather than closed.**
`#11-vm-class-static-block-await-early-error` was renamed to
`#11-vm-class-body-parser-early-errors`, and the new slug reaches the live ledger no better than the
old one did: `grep -c -- '#11-vm-class-body-parser-early-errors' <ledger>` → **0**, and `grep -c --
'#11-vm-class-static-block-await-early-error' <ledger>` → **0** on the same run, so the rename moved
a name and not a registration state (same ledger, `stat -f %Sm` = 2026-08-16 13:10; controls in the
same invocation, `#11-step9-class-extras` → **1** and `#11-vm-iteratorclose-precedence-convention` →
**2**, so the pattern discriminates rather than returning zero for everything). §5 row **A** is an
*umbrella* row whose derivation mints the producer **2aa**'s and **2b**'s `Deps` cells consume, so a
registration driven by this section would omit an artifact two scheduled rows wait on — the identical
failure row **F** produced. ⚠ *This read "a *prerequisite* row … registered at landing", and an umbrella
never lands.* **The child that derivation mints is what registers at its own landing, with the rows below**, and the bold **new** its §5
Slot cell already carries is warranted by this sentence, per the complement rule below, rather than
by hand. The four unnamed remainder are **already registered** — `#11-vm-call-spread-arguments`,
`#11-vm-class-private-fields`, `#11-vm-regexp-constructor-and-flags` and
`#11-vm-tagged-template-literals`, `grep -c` → **1** each against the same two controls in the same
invocation — so no row is owed for any of them.

⚠ **F is why this passage was first rewritten rather than recounted**, and the two it was written
beside are `#11-vm-map-set-collections` (Slice 7) and `#11-vm-proxy-reflect` (Slice 10) — while
**F's slug is new and
unregistered** (`grep -c -- '#11-vm-structured-clone-file-split' <ledger>` → **0**, against
`#11-vm-iteratorclose-precedence-convention` → **2** in the same invocation), and Slice **Pc** now
carries a `Deps` edge on it — so a landing driven by this section would omit the one prerequisite a
scheduled row depends on. **F is registered at landing with the rows below**, and the population this
derivation runs over is widened to *§5's slugs as well as this table's*, the failure being the
population rather than the recipe. The first two are **already registered** — same ledger, same mtime,
`grep -c -- '<slug>' <ledger>` → **1** and **2**, against a nonexistent-slug control in the same
invocation → **0**. **No row is owed**: this section registers ledger *changes*, and they need none.
What *was* owed is the marker: §5's rows carried a hand-written bold **new** on both, and acting on
it at landing mints duplicates. ⚠ **The marker's admissible set is a complement, not a list**: bold
**new** may stand in §5 only on a slot this derivation reports as a registration gap, and is removed
everywhere else — otherwise each new way of *not* being a gap has to be found separately, and the
first pass removed only the class it happened to be looking at. Removed on **five** slots in **two**
classes: four **already registered** — those two plus `#11-vm-assignment-target-completeness` and
`#11-vm-iteratorclose-precedence-convention`, which the derivation above reports nonzero — and one
**zero-return but to-retire**, `#11-vm-computed-compound-assignment` on the merged Slice 0a row,
whose disposition is the "To retire, not register" paragraph below and not a registration at all.
Re-derive the complement rather than reading this count forward. **This derivation, not a marker in
§5, is what a lander reads for registration state.**

**Ledger reconciliation owed at landing, not by this PR** (the ledger is outside this repo):
`project_open-defer-slots.md:217` lists `Intl` / `Atomics` / `WeakRef`+`FinalizationRegistry` /
`String.matchAll` under "Deliberately NOT slotted", and its registered trigger for
`#11-vm-function-constructor-global` ("a dynamic-`Function` need, OR the LegacySemantics/core-strict
compat work") is not the one this document states — so `#11-vm-function-constructor-global`,
`#11-vm-atomics-global`, `#11-vm-weakref-finalization-registry` and the `Intl` row are reconciled on
the ledger side at landing, and the divergence must not be closed by quietly overwriting the
registered text.

**Design-doc reconciliation — done by this PR, and not deferred to landing.** ⚠ **This paragraph read
"owed at landing, not by this PR", by analogy with the ledger paragraph above, and the analogy does not
hold: the ledger is outside this repository and `docs/design/ja/14-script-engines-webapi.md` is inside
it.** The memo applies the opposite rule to itself one table over: the child umbrella `#11-vm-typed-array-family-layering-and-gate`'s derivation mints for the typed-array layering decision
discharges a mandate only at its **SSoT**, and of the restatements it says in as many words that
"amending only those two leaves the root restriction on `vm/host/` standing and discharges nothing". A
PR that states a wider core/compat split in §4 I-6 and leaves the SSoT stating a narrower one is that
shape on the other boundary — two definitions of core/compat with no scheduled owner for the
difference. That file is the SSoT under CLAUDE.md § *Whole-engine core/compat/deprecated
consistency*, so **§4 I-6's split of dynamic code by the strictness of the evaluation is a design-doc
change, not a reading of one** — and this PR makes it. Before this PR §14.1.1 carried exactly one
dynamic-code row, feature cell 「eval()（直接）」, core 「制限付き」, compat 「✓」, whose criterion was
**caller-scope injection** (「sloppy eval（ローカルスコープ注入）は互換」) and not source strictness, with
indirect `eval` and the `Function` constructor appearing nowhere in the file:
`git grep -c -e 'eval' 01f3bdd7 -- docs/design/ja/14-script-engines-webapi.md` printed
`01f3bdd7:docs/design/ja/14-script-engines-webapi.md:2` — that row and the `ScriptEngine::eval`
signature — while the same command with `-e 'Function('` printed **no line** and exited 1, the `eval`
run being its own positive control. ⚠ **Both commands are pinned to `01f3bdd7` precisely because this
PR moves both figures**; an un-pinned `grep -c '<that file>'` was the form this paragraph carried, and
the amendment this PR makes to that file staled both of its figures. What the doc owed is what it now carries: a row for indirect `eval`,
a row for `Function`, the direct-`eval` row's 根拠 cell restated over **effective evaluation strictness**
— `strictCaller` or the source's own directive, per **§19.2.1.1** steps 12-13, source strictness alone
being right for the other two forms only — and a note under the table stating the one criterion that
governs all three forms and how it is derived on each — so core's dynamic-code surface is a
conformant subset of the standard one rather than a half named core. ⚠ **The same reconciliation is
owed on `CLAUDE.md` and is made here rather than deferred**: its *Core vs compat split* clause held a
flat prohibition on `FrameKind`/opcode/dispatch extensions that distinguish direct from indirect
`eval`, which the split above requires, so the clause is narrowed to the motivation test its own
preceding sentence states — legacy-motivated extensions stay out of core, spec-derivation-motivated
ones do not — with §4 I-6 recording the amendment and its warrant. What is still owed at landing is
the **ledger** side above; nothing is owed on either of these two.

**To retire, not register**:
`computed-compound-assignment` — its stated purpose was "a ledger home until it lands", and it landed
as `658cc302` while never reaching the ledger, so the row below is the only place it has ever existed.

| Slot | Why deferred | Trigger | Re-eval |
|---|---|---|---|
| `#11-vm-iteratorclose-precedence-convention` | **(carved R2 round 4)** the §7.4.11 error-precedence inversion spans `compiler/`, core `vm/` and `vm/host/`, and the correct behaviour is completion-kind-dependent ⇒ `iter_close`'s signature must change. A cross-cutting convention sweep, not a slice deliverable. ⚠ **The site count and its ECMA-262/WebIDL partition are NOT restated here** — [§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) is their single home and carries the two commands that re-derive them; five successive figures in this row's lineage (14 lines → 5 → ~15 → 14 → 15) were each withdrawn, and the last of them survived in this row after [§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) had been corrected | **now** — Pb gates the child umbrella **0bc**'s derivation mints for the [C39]→[C36] conformance claim, which inherits the inverted contract. **Pa blocked by the child `#11-vm-typed-array-family-layering-and-gate`'s derivation mints for the family's location decision** (§5 Deps) | 2026-09-30 |
| `#11-vm-computed-compound-assignment` | Slice 0a work item, not a defer. ⚠ **To retire, not registered** — this row read "registered so the T0 crash has a ledger home until it lands" while the reconciliation above measures its ledger count at **0** and the paragraph below states it never reached the ledger. Its stated purpose is spent either way: 0a landed at `658cc302`. A lander following the old wording would retire an entry that does not exist | now (Slice 0a) — remove from this table's to-register set; nothing to retire in the ledger | 2026-09-30 |
| `#11-vm-assignment-target-completeness` | The work item of Slices **0ba** and **0bb** and of the children umbrella **0bc**'s derivation mints — ⚠ *it read "the 0b family's work item", and a family has no id, so an obligation written on one is owned by nobody, which is the third state §5's **I-4** forbids*; distinct SDO ([C39]) + owns the `IteratorClose` obligation umbrella **1b**'s charter does not cover, and the Paren-normalisation fix shares the same catch-all arms | now (Slices 0ba and 0bb, and the children umbrella **0bc**'s derivation mints for binding-pattern storage) | 2026-09-30 |
| `#11-vm-object-spread-source-coercion` | **(carved at PR-B)** Slice **O**. `op_spread_object` copies only between two `JsValue::Object`s, so `{...'ab'}` and `const {...r} = 'ab'` give empty objects where ECMA-262 §7.3.25 applies `ToObject` to every non-nullish source. **Deliverable is §7.3.25's whole per-key loop, and the `ToObject` at step 2 is one of its independent subtractions** — an acceptance written for the coercion alone is passed by an implementation that gets the others wrong, so **the extent is the lettered enumeration below and no count is carried** ([§13](2026-07-vm-p4-umbrella-review-rounds.md)'s rule: a count is derived or absent). ⚠ **"three regressions, not one" stood here against five lettered dimensions carrying more witnesses than letters** — the same failure §5's row **4a** records for its own withdrawn "Six", and it is one-directional: a checklist derived from the stale figure stops before the later nullish and symbol-order guards and retires this slot with those branches untested. The letters are **derived, not restated** — `grep '^\| .#11-vm-object-spread-source-coercion.' <this file> \| grep -o '\*\*(.)' \| tr -d '*()' \| paste -sd' ' -` prints `a b c d e` at this revision, and re-running it in the same edit is what re-derives the extent when a letter is added. **(a) Coercion**: `Object.keys({...'ab'})` is `["0","1"]`, and `const {...r} = 'ab'; r[1]` is `"b"`. **(b) Per-key descriptor recheck**: step 3 snapshots the key list **once** via `[[OwnPropertyKeys]]`, but step 4.c.i re-reads each key with `[[GetOwnProperty]]` **inside** the loop and step 4.c.ii copies only when that descriptor "is not undefined and propertyDesc.[[Enumerable]] is true" — so a key an earlier getter **deleted**, or made **non-enumerable**, is skipped. Two regressions in one shape: `const s = {get a(){ delete s.b; return 1 }, b: 2}; 'b' in {...s}` is `false`, and the variant whose getter runs `Object.defineProperty(s, 'b', {enumerable: false})` gives `Object.getOwnPropertyNames({...s}).length === 1`. Today the handler filters on `attrs.enumerable` while *snapshotting* (`vm/dispatch_objects.rs:158-160` at `658cc302`) and then calls `get_property_value` unconditionally once per snapshotted key (`:169`), so both keys survive, carrying the value `undefined`. **(c) Key order**: step 3's `[[OwnPropertyKeys]]` is **§10.1.11.1** OrdinaryOwnPropertyKeys — array indices in ascending numeric order, then the remaining Strings in creation order, then Symbols — and the observable is **the order the source's getters run**, not the target's key order, because the target is itself ordinary and re-sorts, hiding a wrong copy order behind a right-looking `Object.keys`: ⚠ **The witness may not be spelled with numeric accessor keys, and this slot's §5 row has an empty `Deps` cell.** `({ get 1(){ return 7 } })` defines nothing today — §1.2 and §2.2's `compiler/expr_object.rs:26-39` row measure `let o={ get 1() { return 7 } }; o[1]` → `undefined` — and `#11-vm-property-key-lowering-unification` is not a dependency of this slot, so a literal `get 2()` / `get 1()` source is red for an unrelated owned gap whatever the copy order is. Built instead through the mechanism (b) already uses: `const log = []; const s = {}; Object.defineProperty(s, 'b', {get: function(){log.push('b')}, enumerable: true}); Object.defineProperty(s, '2', {get: function(){log.push('2')}, enumerable: true}); Object.defineProperty(s, 'a', {get: function(){log.push('a')}, enumerable: true}); Object.defineProperty(s, '1', {get: function(){log.push('1')}, enumerable: true}); ({...s}); log.join()` must be `"1,2,b,a"` — creation order still b, 2, a, 1, so the today-answer below is unchanged. **No loop head in the builder**: a `for (const k of …)` spelling would capture one shared binding (§2.2's `scope/visitor.rs:79-124` row), a second unrelated gap. Today the ascending-index tier is harvested only from `ObjectKind::Array { ref elements }` (`:143-151`), so a plain object's integer-like keys stay in `storage` insertion order and that probe answers `"b,2,a,1"`. **(d) The nullish short-circuit, which *precedes* the coercion (a) asserts.** Step 1 is `If source is either undefined or null, return unused` — ahead of step 2's `! ToObject(source)` — so `{...null}` and `{...undefined}` are `{}` with no throw, the same step-6.a-against-step-6.b pair `#11-vm-for-in-enumeration-semantics` states for §14.7.5.6 and for the same reason. (a) supplies a *coercible* primitive and so cannot distinguish the two steps, and the natural fix for it — dropping the `JsValue::Object` guard and coercing — turns `{...null}` into a TypeError where the spec returns an empty object; the handler is accidentally right there today and the regression is what keeps it so: `Object.keys({...null}).length === 0` and the same for `undefined`. **(e) Symbol keys.** Step 3's `[[OwnPropertyKeys]]` is the same **§10.1.11.1** (c) already reads, whose step 4 appends "each own property key propertyKey of obj such that propertyKey is a Symbol, in ascending chronological order of property creation" after the two String tiers, and step 4.c.ii copies it like any other enumerable key. Every witness above is String-keyed, so a copy that harvests only string keys passes (a)-(d) — ⚠ **but the *presence* spelling of (e) is green at `658cc302`**: `const k = Symbol('k'); const s = { [k]: 1 }; ({...s})[k]` is executed as `1` today, the harvest already reaching the symbol tier, so that witness is a **guard** — it exists so that the fix to (b)'s descriptor recheck may not drop the tier. **The red half of (e) is the tier *order*, and it is spelled as a witness of its own rather than left as prose**, kept off (c)'s object so that (c)'s stated today-answer is unchanged: `const log = []; const s = {}; const k = Symbol('k'); Object.defineProperty(s, k, {get: function(){log.push('k')}, enumerable: true}); Object.defineProperty(s, 'b', {get: function(){log.push('b')}, enumerable: true}); Object.defineProperty(s, '1', {get: function(){log.push('1')}, enumerable: true}); ({...s}); log.join()` must be `"1,b,k"` — the symbol **last** however early it was created. ⚠ **And every one of the five letters is witnessed through an object-literal spread or a rest pattern with an *empty* exclusion set, so §7.3.25 steps 4.a/4.b — the `excludedItems` test that sits inside the very loop this row calls its deliverable — are unasserted.** The step order is what makes it observable: 4.a/4.b skip an excluded key **before** 4.c.i `[[GetOwnProperty]]` and 4.c.ii.1 `Get`, so an excluded accessor's getter must not run for the copy, while `compiler/stmt_destructure.rs:126-160` lowers rest exclusion as `SpreadObject` (copy everything) followed by `DeleteProp`/`DeleteElem` per excluded key and runs it an extra time. **Sixth red probe at `658cc302`**: `const log=[]; const o={}; Object.defineProperty(o,'a',{get(){log.push('a');return 1},enumerable:true}); Object.defineProperty(o,'b',{get(){log.push('b');return 2},enumerable:true}); const {a, ...r} = o; log.join()` is `"a,b"`, not `"a,a,b"`, §10.1.11.1 step 4 appending that tier after both String tiers, which a harvest walking `storage` in insertion order gets wrong. §-numbers from `webref aoid ecma262 CopyDataProperties` / `OrdinaryOwnPropertyKeys`; steps from `webref body ecma262 sec-copydataproperties` / `sec-ordinaryownpropertykeys`. Carved rather than folded into an existing slice because §2.2 derives from `compiler/` and §2.3's membership test is zero emit sites, so a live defect in a **connected** dispatch handler had no owner at all | now (Slice O) | 2026-09-30 |
| `#11-vm-arrow-lexical-new-target` | **(carved at PR-B)** Slice **N**. ⚠ **Ordered after Slice S**, and stated here because §5's *Pointer row* rule makes this slot the home of N's three fields: **§9.4.5** GetNewTarget step 1 is `Let envRecord be GetThisEnvironment()` and **§9.4.3** GetThisEnvironment is the one governing AO S delivers, so reading `new.target` lexically through an arrow consumes S's environment link rather than sharing a seam with it — N's `Deps` cell says the same, and a re-evaluation driven from this slot would otherwise start the work first and build a second link. ⚠ **A `new.target` is an object, so by §4 I-7 this slot owes the `ObjectKind::Function` tracing edge for it and a forced-collection regression**: the frame spelling is marked (`vm/gc/roots.rs:294-295`, `vm/gc/trace.rs:353-355`) and the closure spelling this work adds is reached by nothing. ⚠ **And the regression has to scope the constructor out, or it passes under a no-op arm** — a `new.target` *is* the constructor function, so leaving that binding live keeps the object marked through it and the test measures nothing. The shape: `let f; (function () { function C() { f = () => new.target } C.tag = 42; new C() })();` then a forced collection, then `f().tag === 42` — after the IIFE the only reference to `C` is the arrow's captured `new.target`, which is the edge this work adds. ⚠ **That witness constructs with `new C()`, where NewTarget and the callee are the same object, so it cannot fail on an implementation that captures the *callee*.** **§9.4.5** GetNewTarget is `Let envRec be GetThisEnvironment()` then `Return envRec.[[NewTarget]]`, and **§9.1.2.4** NewFunctionEnvironment **step 5** sets that field from the `newTarget` argument — which `[[Call]]` supplies as `undefined` and `Reflect.construct` supplies as its third argument (`webref body ecma262 sec-getnewtarget` / `sec-newfunctionenvironment`). Two more regressions, and the first is the confinement half this row states nowhere: `let f; function C(){ f = () => new.target } C(); f() === undefined` — ⚠ **green at `658cc302`** (executed → `true`), a **guard** and not a probe, since `Op::NewTarget` already reads `undefined` out of a `[[Call]]` frame and what this work adds is the lexical capture that must not turn it into the callee. "Reachable today" was the load-bearing phrase and it is the wrong one — green today is what decides how the pair reads. The second separates NewTarget from the callee outright and waits on **umbrella 10a's derivation together with the child that derivation mints for `Reflect.construct`** (**§28.1.2**) — §5's rule for a consumer whose producer is not yet minted, an umbrella never landing, and re-pointing this edge at that child once minted is a mandatory output of the same derivation: `function D(){}; Reflect.construct(C, [], D); f() === D`. ⚠ **The observable is a property established inside the IIFE, deliberately, and not `f().name`**: this document records that an ordinary function receives no own `name` today and that fixing it is `#11-vm-ordinary-function-name-length`, an unowned slot with no edge to here — so a `name`-based assertion would be red for a reason that is not this work's, and the row could not satisfy its own acceptance in isolation. `Op::NewTarget` reads the current frame's `CallMode` and `FunctionObject` has no lexical `new.target` beside `captured_this`, so `new.target` is `undefined` inside an arrow where ECMA-262 §9.4.5 resolves the surrounding function environment. Separate from Slice 3 because the AO differs, though both add lexical state to the same closure/frame seam. ⚠ **All three witnesses place the observable inside an *arrow*, so none asserts that an ordinary nested function is unaffected**: populating the new field at *every* closure creation instead of gating on the lexical this-mode passes all three, adds the tracing edge, and is wrong. **Fourth condition, a declared negative control, green at `658cc302` and required to stay green**: `let g; function C(){ g = function(){ return new.target } } new C(); g()` is `undefined` — an ordinary function gets its own this-environment, so the §9.4.3 walk stops there and never reaches the constructor frame | now (Slice N, after Slice S — the ordering this row's *Why* cell derives, since §5's *Pointer row* rule makes this slot N's home) — ⚠ **and additionally on umbrella 10a's derivation having run and produced the child it mints for `Reflect.construct` (§28.1.2)**, the wait the *Why* cell above states in prose: §5's *Pointer row* rule makes this cell the single home of N's ordering, so a producer named only beside it is one a lander reading the ordering field never sees, and N would start with `function D(){}; Reflect.construct(C, [], D); f() === D` unrunnable — its one regression that separates NewTarget from the callee outright. Re-pointing the edge at that child once minted is a mandatory output of the same derivation | 2026-09-30 |
| `#11-vm-arrow-lexical-super` | **(carved at PR-B)** Slice **S**. The parser deliberately lets an arrow inherit `super` context (`parser/arrow.rs:104-119` at `658cc302`), but `FunctionObject` carries no home-object field beside `captured_this` (`vm/value.rs:588-599`) and `dispatch_super_inner` (`vm/dispatch_class.rs:115-119`) resolves against the **current** frame's `home_class`, which is `Some` only on a class-ctor frame (`vm/interpreter.rs:802`) and hard-coded `None` on the `call_internal` entry (`:589`) — so `super` reached through an arrow raises "'super' keyword unexpected here". ECMA-262 **§13.3.7.2** GetSuperConstructor step 1 and **§13.3.7.3** MakeSuperPropertyReference step 1 both begin at **§9.4.3** GetThisEnvironment, whose outward walk skips an arrow's environment. ⚠ **The S-versus-3 split is §5's, and this row states none of it.** §5's row for **S** is the single home of that boundary — the split is one of §5's three specification fields and its home is the set of rows — and it records the decision and both measurements (S's own governing AO, and N's `Deps: S` putting a consumer outside umbrella 3's charter). This row previously carried its own carve rationale, which gave the boundary two homes; it is a **pointer** now, as the `L` ledger row is. ⚠ **It also said S and N use a "different AO", and that is false**: **§9.4.5** GetNewTarget step 1 is `Let envRecord be GetThisEnvironment()`, so N reaches **§9.4.3** — S's AO — which is exactly why N carries `Deps: S` rather than a coordination note. Same closure/frame seam as **N** and **3**, and the **same** AO as N | now (Slice S, after the child of umbrella 3 that owns the super-property lowering and after the child umbrella `#11-vm-derived-constructor-this-binding`'s derivation mints for the `[[ThisBindingStatus]]` §13.3.7.1 step 9 needs — and which Slice S's own `vm/interpreter.rs` touch fires by that slot's stated trigger) | 2026-09-30 |
| `#11-vm-block-declaration-instantiation` | **(carved at PR-B)** Slice **B**. elidex has no ECMA-262 **§14.2.3** BlockDeclarationInstantiation. Probe-measured consequences already in hand: `let x=1; switch(0){case 0: let x=2;} x` → `2` (spec `1`), `let x=1; try {} finally { let x=2 } x` → `2` (spec `1`), and `{ f(); function f(){} }` inside a function → `TypeError: not a function`. ⚠ **"Carved as its own slice … the deliverable is one block-entry instantiation, not a patch per construct" is withdrawn**: §5's **B** row is an **umbrella** — three mechanisms, each failing while the other two pass (block-scope entry with the lookup key an entry site is addressed through, §14.2.3 declaration instantiation, and the per-evaluation environment emit closure capture observes) — and *one* block-entry instantiation is exactly the terminality claim §2.2's own rows falsify, since at an ordinary block entry passes while instantiation fails. §5's **B** row is the single home of the charter, the derivation, the split and every measurement this row carried; this row is a pointer | **now** — the derivation Slice **B** runs at its own start, which mints the terminal children that carry the work; never a single PR, since an umbrella ships none | 2026-09-30 |
| `#11-vm-labelled-statement-break-target` | **(carved at PR-B)** Slice **L**. A label is recorded as an index into `fc.loop_stack` (`compiler/stmt.rs:287-288` at `658cc302`), which has no entry for a statement that is neither a loop nor a switch, so `outer: { break outer; }` is rejected at compile time by the `Break` arm's guard at `:244-249` and a labelled block wrapping a loop patches the loop instead (`:251`). ECMA-262 **§14.13.4** LabelledEvaluation step 4 of `LabelledStatement : LabelIdentifier : LabelledItem` catches a break completion whose `[[Target]]` is the label at the labelled statement itself, so a labelled statement is a break target regardless of whether it is a `BreakableStatement`. Deliverable is the representation — a label→patch-list that does not presuppose a loop — plus the `continue`-on-a-non-loop-label early error, which is **§8.3.3** ContainsUndefinedContinueTarget through **§16.1.1** and ⚠ **not §14.8.1**, the nesting rule this row cited until now. ⚠ **"Terminal under §5's criterion: one contract, one SDO, one layer" is withdrawn**: §5's **L** row is an **umbrella** — three mechanisms, each failing while the other two pass (the compiler's label→patch-list lowering, the parser's §8.3.3 label-target early error, and the §14.7.5.7 exit-range depth) — and it is the single home of the charter, the derivation and every measurement this row carried; this row is a pointer | **now** — the derivation Slice **L** runs at its own start, which mints the terminal children that carry the work; never a single PR, since an umbrella ships none | 2026-09-30 |
| `#11-vm-per-iteration-loop-environment` | **(carved at PR-B)** **Pointer into §5's row E** (sub-slices **Ea** — `for` head — and **Eb** — `for-in`/`for-of` heads); E's row is the single home of its kind, and its kind is UNDETERMINED — it holds no ordering and no owner, and this cell restates nothing. ⚠ *It used to: it opened by declaring E's kind in the marker's own words, which §5 forbids a pointer slot outright — and the copy then went stale the moment E's kind changed, so this cell asserted a kind E no longer had. A pointer that restates what it points at is the second copy the single-home rule exists to prevent, and it rotted in exactly the way that rule predicts.* `scope/visitor.rs` pushes one `ScopeKind::Block` for the whole loop (`:85` for `for`, `:114` for `for-in`/`for-of` at `658cc302`) and the compiler stores the head binding into that one scope's local, so every closure created in the body shares one binding; `git grep -c CreatePerIterationEnvironment 658cc302 -- crates/script/elidex-js/src` exits **1** (no match). ECMA-262 **§14.7.4.4** CreatePerIterationEnvironment (from **§14.7.4.2** ForLoopEvaluation) copies the previous iteration's value into a fresh environment each iteration; **§14.7.5.7** ForIn/OfBodyEvaluation step 8.h.iii instantiates a fresh environment from the iteration value with no copy-forward. **plan-review MANDATORY** per the CLAUDE.md edge-dense rule. Carved rather than folded into **B**, whose contract is block *entry*, or into 0bc, whose contract is binding-pattern storage | blocked on row E's measurement (§5) | 2026-09-30 |
| `#11-vm-for-in-enumeration-semantics` | **(carved at PR-B)** `op_for_in_iterator` (`vm/dispatch_iter.rs:141` at `658cc302`) snapshots the key list once into `ForInState { keys, index: 0 }` (`:231-236`) and records a name in `seen` only inside `if attrs.enumerable && seen.insert(sid)` (`:188`, `:220`), and `op_for_in_next` (`:243`) replays that snapshot with no re-check. ECMA-262 **§14.7.5.9** EnumerateObjectProperties requires the opposite on both counts — "The values of [[Enumerable]] attributes are not considered when determining if a property of a prototype object has already been processed", so a non-enumerable own key must shadow an inherited enumerable one of the same name; and "A property that is deleted before it is processed by the iterator's next method is ignored", so existence must be re-checked as iteration advances. Deliverable is the enumeration state machine (**§14.7.5.10.1** CreateForInIterator and **§14.7.5.10.2.1** `%ForInIteratorPrototype%.next`, whose incremental shape is what the spec's carve-out is written against), with regressions for shadowing and for mid-iteration deletion — ⚠ **both stated in prose only, and neither is among the four witnesses this cell actually spells**, so keeping the snapshot shape and repairing only the three spelled things retires the row with the enumeration state machine never built. **Two red probes at `658cc302`, one per prose obligation**: shadowing — ⚠ **the witness stated here could not exhibit the defect it names, and is replaced.** It read `const p = {a:1}; const o = Object.create(p); o.a = 2; let n = 0; for (const k in o) n++; n` is `1`: `o.a = 2` creates an **enumerable** own property, so `attrs.enumerable && seen.insert(sid)` (`:188`) does insert it, the prototype copy is already suppressed, and the tree already answers `1` — a green condition in red-probe voice, and an *enumerable* own key is precisely the case the named defect cannot reach. §14.7.5.9's informative generator adds a key to `visited` on **existence** and yields it only `if (desc.enumerable)` (`webref body ecma262 sec-enumerate-object-properties`), so the own property has to be **non-enumerable** — and a bare count of the shadowed name would still assert only that an inherited key does *not* appear, which the ⚠ sentence below measures as vacuous, so the replacement carries a **second, unshadowed** inherited key and is the positive prototype-chain assertion that sentence asks for: `const p = {a:1, b:2}; const o = Object.create(p); Object.defineProperty(o, 'a', {value: 9, enumerable: false}); let out = ''; for (const k in o) out += k; out` must be **`"b"`** — red at `658cc302`, where Rust's `&&` short-circuits past `seen.insert` for a non-enumerable own key so `a` never enters `seen`, the prototype tier (`:220`) yields both names and the tree answers `"ab"`, while an implementation that never walks the chain answers `""`; and mid-iteration deletion — `const o = {a:1, b:2}; let out = ''; for (const k in o) { out += k; delete o.b } out` is `"a"`, since §14.7.5.9 says a property deleted before it is processed is ignored, which a key list collected once at `op_for_in_iterator` (`vm/dispatch_iter.rs:141`) structurally cannot answer. ⚠ **And the existing at-most-once guard deletes a key *after* it is processed**, so it does not reach that rule and is a guard rather than a probe. ⚠ **Both of those are stated over an *object* source and over keys that already appear, so neither can fail on the two subtractions that come first.** **(a) The source is never coerced.** `op_for_in_iterator` binds its key list inside `if let JsValue::Object(obj_id) = obj` and gives a primitive the `else { Vec::new() }` arm (`:145`, `:228-229` at `658cc302`), which is right for `undefined` and `null` — **§14.7.5.6** ForIn/OfHeadEvaluation step 6.a returns a `break` completion for exactly those two — and wrong for every other primitive, because step 6.b is `Let obj be ! ToObject(exprValue)`. So `for (const k in 'ab')` must yield `"0"` then `"1"` **while the nullish pair still yields nothing**: the pair is the regression, since an implementation returning the empty list for both cannot distinguish step 6.a from step 6.b. **(b) The prototype tier is neither ordered nor element-aware.** The own tier harvests `ObjectKind::Array { ref elements }` and sorts its index keys (`:173-183`, `:198`); the prototype loop does **neither**, reading only `obj_ref.storage.iter_keys` (`:218-226`). §14.7.5.9 requires the same treatment at every tier — "The enumerable property names of prototype objects must be obtained by invoking EnumerateObjectProperties passing the prototype object as the argument" — which carries **§10.1.11.1** OrdinaryOwnPropertyKeys down the chain with it. Two regressions: `const p = ['x','y']; const o = Object.create(p);` must enumerate `"0"` then `"1"` where today it enumerates nothing, and a prototype given `b`, `2`, `1` in that creation order must enumerate `"1","2","b"` where today it gives `"b","2","1"`. ⚠ **This is why the shadowing regression above carries a second, unshadowed inherited key**: spelled as a bare count of the shadowed name it would assert only that an inherited key does **not** appear, which an implementation that never walks the chain passes *vacuously* — the two here discriminate only with a positive inherited-key assertion standing beside them, and the corrected shadowing witness is now one of those, answering `"b"` where a chain-blind implementation answers `""`. ⚠ **LOW: a further rule in the same §14.7.5.9 paragraph the two regressions above quote — and a *guard*, not a subtraction** — "Returned property keys do not include keys that are Symbols", while step 3's `[[OwnPropertyKeys]]` (§10.1.11.1 step 4) returns them in a tier of their own. Every witness above is String-keyed, so: `const k = Symbol('k'); const o = {[k]: 1, a: 2}; const seen = []; for (const p in o) seen.push(p); seen.join()` must be `"a"` — which it already is at `658cc302` (executed → `"a"`), today's harvest never reaching the Symbol tier. It is owed anyway, and as a guard rather than a probe: the enumeration state machine this row delivers takes its keys from `[[OwnPropertyKeys]]`, which *does* return that tier, so the exclusion has to be written rather than inherited. ⚠ **Both stated regressions are *negative* — a key must not appear — so neither can fail on a machine that returns a key **twice**, which is the polarity the deliverable's own incremental shape introduces.** **§14.7.5.10.2.1** step 5.b.iii.2.a appends the key to `iterator.[[VisitedKeys]]` on *existence* — one step ahead of the step-5.b.iii.2.b `[[Enumerable]]` test — and that list survives step 5.c's `[[GetPrototypeOf]]` hop, so a state machine that instead re-tests own-ness on the *live* object at each tier (the shape the deletion rule invites) re-reports an inherited key that an already-yielded own key had shadowed. **The rule binds even after the delete**: `delete o.a` is itself one of the four occurrences §14.7.5.9 lists as releasing the CreateForInIterator equivalence, but "A property name will be returned by the iterator's next method at most once in any enumeration" is one of the *rules* its step 1 requires conformance to unconditionally, not part of that equivalence. One more regression, of a polarity none of the others has: `const p = {a: 1}; const o = Object.create(p); o.a = 2; let out = ''; for (const k in o) { out += k; delete o.a } out` must be `"a"`, where that machine answers `"aa"` — ⚠ **green at `658cc302`** (executed → `"a"`), today's snapshot shape being unable to report a key twice. It is a **guard** on the remedy: it fails only on the incremental machine this deliverable introduces, which is what "a polarity none of the others has" meant and what the sentence did not say. Steps from `webref body ecma262 sec-runtime-semantics-forinofheadevaluation` / `sec-enumerate-object-properties` / `sec-ordinaryownpropertykeys`. **Owned by a slot rather than a §5 slice** because it is a live defect in a connected dispatch handler, which §1.0's derivation does not produce; the child umbrella **0bc**'s derivation mints for binding-pattern storage owns only the store into the left-hand side | the next VM dispatch-loop PR, or with `#11-vm-operand-rooting-by-construction` | 2026-09-30 |
| `#11-vm-parameter-environment-instantiation` | **UMBRELLA, not a terminal unit.** ⚠ **This slot read as terminal on the argument that "all three are steps of one AO and one emit sequence", and that is the reasoning §5's head rejects**: being a well-formed spec unit is what a *group* is, and a group is an **intermediate** result to which the terminality criterion is then applied, never a slice by itself. Four axes, and they do not share a chokepoint: **§10.2.11** conformance itself; the **scope-analysis binding model**, where one `ScopeKind::Function` must become a parameter record plus a conditional body record; the **runtime declarative-environment representation and the capture through it**, which this cell states it *consumes from the child umbrella C's derivation mints for it* and warns would otherwise produce "the two-mechanism outcome CLAUDE.md *One issue, one way* forbids"; and **compiler emit ordering**. The measurement that settles it: the emit-ordering obligation is a two-line reorder landable today with no `C` edge at all, while the representation obligation cannot start until `C`'s own derivation has minted its producer — bundling them parks a shippable fix behind an unbuilt runtime mechanism. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. **(carved at PR-B)** `compile_param_prologue` (`compiler/expr_function.rs:31-66` at `658cc302`) lowers a parameter default as read-the-slot / compare-to-`undefined` / conditionally store over positional slots the caller has already filled, and the arm's own comment records the consequence — "param bindings carry no TDZ" (`:52`); `visit_function` (`scope/visitor.rs:442-486`) pushes a single `ScopeKind::Function` (`:456`) and visits both the parameters (`:479`) and the body statements (`:481-483`) into it. ECMA-262 **§10.2.11** FunctionDeclarationInstantiation requires the opposite on both counts: step 21.3.a creates each parameter binding with `CreateMutableBinding(paramName, false)` and step 28's IteratorBindingInitialization is what initializes them, so a default initializer that reads a later parameter must raise rather than read a filled slot; and step 30.2 gives the body its own `NewDeclarativeEnvironment(envRecord)` whenever step 8's `hasParamExprs` (ContainsExpression of formals) holds, so a body `var` must not be visible to a default initializer. ⚠ **That names one direction of step 30, and the other is what a body environment built to satisfy it gets wrong.** Inside the same step, **30.5.a.iii** takes `initialValue` to be `undefined` only "If paramBindings does not contain name or funcNames contains name", and **30.5.a.iv** otherwise takes it to be `! envRecord.GetBindingValue(name, false)` — the *parameter's* value — step 30.5 having a single substep `a`, which puts these at the full depth the other steps in this cell already use, with step **30.5.a.vi**'s own NOTE saying so: "A var with the same name as a formal parameter initially has the same value as the corresponding initialized parameter" (`webref body ecma262 sec-functiondeclarationinstantiation`). An implementation that creates the separate body record and initializes every `var` to `undefined` satisfies the isolation regression while `function f(a = 1) { var a; return a } f(5)` answers `undefined` where the spec answers `5`. That shape is a fourth regression, and it is the one the isolation direction structurally cannot see. ⚠ **It is a *guard*, not a probe: `function f(a = 1) { var a; return a } f(5)` already answers `5` at `658cc302`** (executed), because there is no separate body record to initialize wrongly. The regression exists so that building one — this slot's own deliverable — may not turn that `5` into `undefined`; it measures a direction a repair introduces, not a defect a repair removes. **A third obligation sits in the same AO and the same emit order**: step 22.5 "Perform ! envRecord.InitializeBinding(\"arguments\", argumentsObj)" precedes the step-28 IteratorBindingInitialization already cited, so a default initializer may read the arguments object (`webref body ecma262 sec-functiondeclarationinstantiation`). elidex emits the two in the opposite order — `compile_nested_function` calls `compile_param_prologue` at `compiler/expr_function.rs:136` and reaches the `CreateArguments` / `SetLocal` / `InitLocal` sequence only afterwards (`:156-158`, under the `uses_arguments` guard at `:139`). The guard is not what suppresses it: `visit_params` visits a parameter default through `visit_expr` (`scope/visitor.rs:513-514`, reached from `:479`), so `uses_arguments` is set and the sequence *is* emitted — after the prologue that reads its slot. Deliverable is that environment shape — uninitialized parameter bindings initialized left to right, the arguments binding established before any of them, and a body environment distinct from the parameter one under the step-8 condition — with a regression for each; for the third, `function f(a = arguments.length, b) { return a } f(undefined, 2)` → `2`. Stated here rather than as a second slot because all three are steps of one AO and one emit sequence. **Owned by a slot rather than a §5 slice** because it is a live defect in fully emitted code, which §1.0's derivation does not produce; 0bc's parameter coverage is binding-pattern storage and iterator closing, a different mechanism. ⚠ **It consumes the child umbrella `C`'s derivation mints for C's runtime declarative-environment and closure-capture representation** — the edge was missing while this slot's two triggers between them supplied it only by accident. Step 30.2's `NewDeclarativeEnvironment(envRecord)` is a second environment record of exactly the kind C delivers, and the closures step 30.1's note exists to protect ("closures created by expressions in the formal parameter list do not have visibility of declarations in the function body") are captures *through* it — while in-tree there is no runtime environment at all and the allocation is compile-time throughout, which is the gap C's row measures at `compiler/resolve.rs:31` and `:148-157`. So settling this before C exists builds a second parameter/body environment and a second capture mechanism that C then has to replace: the two-mechanism outcome CLAUDE.md *One issue, one way* forbids, and the one C was carved out of a slot to prevent. **Neither existing trigger supplies it**: "the next `compiler/expr_function.rs` + `scope/` PR" supplies nothing at all, and the "with Slice **B**" route is transitive rather than stated — the child umbrella **B**'s derivation mints for the per-evaluation emit *consumes* C rather than producing it, and B itself carries no such edge: its `Deps` cell reads `—`, so routing through B is an ordering that happens to hold, not an edge, and it evaporates the moment the other trigger fires first | **after the child umbrella `C`'s derivation mints for the runtime declarative-environment and closure-capture representation**, then the next `compiler/expr_function.rs` + `scope/` PR, or with the child umbrella **B**'s derivation mints for the per-evaluation emit — ⚠ **and this row is an umbrella, so the occasion fires its *derivation*, never a PR that would carry the work**: that derivation mints the terminal children, each its own PR under its own mandatory `/elidex-plan-review`, and re-points every ordering stated above onto the specific child that consumes it | 2026-09-30 |
| `#11-vm-derived-constructor-this-binding` | **(carved this round)** a derived constructor's `this` is never uninitialized, so a pre-`super()` read succeeds where it must throw. `do_new` (`vm/ops.rs:681` at `658cc302`) allocates the instance unconditionally (`:766-771`) and passes it as the new frame's `this` (`:784-793`, `push_js_call_frame(callee, JsValue::Object(instance), …, Some(instance), CallMode::Construct { … })`); `Op::PushThis` (`vm/dispatch.rs:738-741`) pushes `frames[frame_idx].this_value` with no status check; and `dispatch_super_inner` (`vm/dispatch_class.rs:115`) reads that same `this_value` as the receiver it forwards (`:136`) and then *substitutes* it with what `construct_synchronous` returned (`:152`) — a swap, not the establishing of a binding. ECMA-262 **§10.2.2** `[[Construct]]` creates `thisArg` only "If kind is base" (step 3) and binds it only under the matching step 6, so a derived constructor's `this` is unbound until `super()`; **§9.1.2.4** NewFunctionEnvironment step 4 is "Else, set envRecord.[[ThisBindingStatus]] to uninitialized", **§9.1.1.3.1** BindThisValue step 2 throws a ReferenceError when the status is already initialized and step 4 sets it, and **§9.1.1.3.3** GetThisBinding step 2 throws when it is uninitialized — which §10.2.2 step 15 reaches for a derived constructor that completes without one. §-numbers from `webref heading ecma262 10.2.2` / `9.1.1.3.3` and `webref aoid ecma262 NewFunctionEnvironment` / `BindThisValue`; **§9.1.1.3.3 is GetThisBinding and §9.1.1.3.4 is HasSuperBinding**, so derive rather than recall. Everything from here is evidence for that derivation to route, not a charter — placing each obligation and each acceptance condition on the specific child is a mandatory output of it (§5's row-kind rule), and this row carries none. The status on the call frame is the artifact, and the throw sites it must make observable are: `class B extends A { constructor() { this.x = 1; super() } }`, `class B extends A { constructor() { super(); super() } }`, and `class B extends A { constructor() {} }` must each throw. **The return-substitution branch of the same clause is this slot's too — a fourth throw site, not a second mechanism.** `complete_inline_frame` (`vm/dispatch.rs:1086` at `b5356098`; `git diff --stat 658cc302 b5356098 -- crates/` is empty, so the reading holds at `658cc302`) substitutes for **every** non-Object return whenever the field is present — `let final_val = if let Some(instance_id) = frame.new_instance { if matches!(return_value, JsValue::Object(_)) { return_value } else { JsValue::Object(instance_id) } }` (`:1089-1094`) — with no test of base against derived and none of `undefined` against another primitive, while `do_new` sets `new_instance` for a derived constructor as well (this row's own measurement above). So `class B extends A { constructor() { super(); return 1 } }` succeeds and evaluates to the instance. ECMA-262 **§10.2.2** separates the three cases in consecutive steps: step 12 "If result.[[Value]] is an Object, return result.[[Value]]"; step 13 "If kind is base, return thisArg" — the substitution the handler performs unconditionally, and the only case in which it is correct; step 14 "If result.[[Value]] is not undefined, throw a TypeError exception" — and `undefined` is permitted by falling past it to step 15's "Let thisBinding be ? ctorEnv.GetThisBinding()" and step 17's return of that binding (steps from `webref body ecma262 sec-ecmascript-function-objects-construct-argumentslist-newtarget`). **Extended rather than carved**: step 15 is the same GetThisBinding the third regression above already reaches, and steps 13/14 turn on the same base-against-derived `kind` this deliverable puts on the frame, so the handler needs no state this slot is not already adding — a separate slot would restate one clause's branch table across two ids. Fourth regression: `class B extends A { constructor() { super(); return 1 } }` must throw a TypeError, against `return undefined`, which must still yield the instance. ⚠ **All four witnesses are derived classes, and §10.2.2 puts step 13 *between* the two steps they straddle — so none of them fails on a fix that applies the derived rule to every constructor.** The clause reads "12. If result.[[Value]] is an Object, return result.[[Value]]. 13. If kind is base, return thisArg. 14. If result.[[Value]] is not undefined, throw a TypeError exception", and step 6 binds a base constructor's `this` before the body runs (`webref body ecma262 sec-ecmascript-function-objects-construct-argumentslist-newtarget`). The unconditional substitution `complete_inline_frame` performs is step 13, correct there and only there — so a `kind`-blind throw is as wrong as a `kind`-blind substitution, and each half needs a base-class positive control: `class A { constructor(){ return 1 } } new A() instanceof A` against the fourth regression, and `class A { constructor(){ this.x = 1 } } new A().x === 1` against the first three. **Carved rather than folded into Slice 2ab**, whose charter is *when* instance elements are initialized under §10.2.2 and §7.3.33: this carries three intersecting invariant axes that meet at the call frame's this-binding status and that this cell's own derivation sentence below states **fail independently** — the `[[ThisBindingStatus]]` **representation** on the frame, the **§9.1.1.3.1** BindThisValue / **§9.1.1.3.3** GetThisBinding operations that set and read it across the constructor and `super()` entry paths, and the derived-constructor **return substitution** of §10.2.2 steps 12-14 — reaching `vm/ops.rs`, `vm/interpreter.rs`, `vm/dispatch.rs` and `vm/dispatch_class.rs`, so the criterion's **first** disjunct is met; its **second** is unavailable, §10.2.2 and the §9.1.1.3 AOs being canonical and cited above through `webref` — **UMBRELLA, not a terminal unit.** **So this slot is one, and its trigger runs that derivation rather than shipping the work** (§5's head reads a slot declaring edge-density as an umbrella row from the moment it says so). The derivation's own seam is visible in the sentence above: the call-frame `[[ThisBindingStatus]]` **representation**, the **binding operations** that read and set it across the constructor and `super()` entry paths, and the derived-constructor **return substitution** fail independently, and which children the three yield is that derivation's output rather than this cell's to mint. ⚠ **The trigger therefore cannot be "with Slice 2ab" read as folding the unit into 2ab** — that is the bundling the criterion forbids; 2ab is an *occasion* that fires the derivation. ⚠ **That is the reason for a *split*, not for a *carve*, and the two are distinct** — the S/umbrella-3 note above corrects the same conflation. What justifies the carve here is that this deliverable is an artifact with its own consumers rather than a sub-part of 2ab's charter: the children umbrella **2ab**'s derivation mints satisfy all three of its field-placement regressions without it, which is why the split has to be stated rather than left to be noticed at 2ab's retirement. **Owned by a slot rather than a §5 slice** because it is a live defect in fully emitted code, which §1.0's derivation does not produce | with umbrella **2ab**'s derivation, which that occasion fires, or the next `vm/interpreter.rs` call-frame PR | 2026-09-30 |
| `#11-vm-ordinary-function-name-length` | **(carved this round)** **UMBRELLA, not a terminal unit.** ⚠ **This slot read as terminal, and the "changes the deliverable's shape" paragraph below is what ends that.** §5's head reads a slot whose *Why* cell declares ≥3 intersecting invariant axes as an umbrella row from the moment it says so, and this cell declares four across three layers: the closure object's own-property creation under **§10.2.9** SetFunctionName / **§10.2.10** SetFunctionLength's descriptor contract (`vm/ops.rs`); **§15.1.5** ExpectedArgumentCount's two independent subtractions over the compiler's `param_count` (`compiler/expr_function.rs`); **§8.4.5** NamedEvaluation at four *different* productions of the compiler's binding and assignment paths (**§14.3.1.3**, **§13.2.5.6**, **§14.3.3.3**, **§13.15.2**); and §10.2.9's own Symbol / Private Name / accessor-prefix transforms reached from **§15.4.5** MethodDefinitionEvaluation, whose private-method spelling additionally consumes a child of umbrella **5**. Each fails while the others pass — the criterion's measurement, not a count — and this cell's own conclusion below, that the binding name is compile-time information `create_closure` never receives, is the statement that no one chokepoint discharges the slot: left terminal it admits either a single edge-dense PR or retirement after the closure half alone. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each regression below, each acceptance condition and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. **The measurement it routes**: an ordinary function gets no own `name` and no own `length`. `create_closure` (`vm/ops.rs:820` at `658cc302`) allocates the function object root-shaped (`:882-891`) and defines exactly one own property on it — `.prototype`, non-arrow only (`:918`). Negative control for the absence: the same `define_shaped_property(…, well_known.name, …)` shape *is* present elsewhere — `vm/shape_ops.rs:513` for built-in functions and `vm/ops.rs:211` for Error instances — so the pattern discriminates; and `git grep -n 'well_known.length' 658cc302 -- crates/script/elidex-js/src/vm/ops.rs` returns nothing while the identical shape for `well_known.prototype` over that same file returns `:755` and `:918`. The only compensation is bind-local: `target_function_length_name` (`vm/natives_function.rs:151`, called once, from `:100`) falls back to `internal_function_length` (`:205`) and `internal_function_name_u16` (`:212`) when the property is absent, and that length source is `CompiledFunction.param_count`, assigned `func.params.len()` (`compiler/expr_function.rs:220`; arrows `:287`). ECMA-262 **§10.2.3** OrdinaryFunctionCreate step 21 is "Let length be the ExpectedArgumentCount of paramList" and step 22 performs **§10.2.10** SetFunctionLength, whose step 2 defines `length` with `[[Writable]]: false, [[Enumerable]]: false, [[Configurable]]: true`; **§15.1.5** ExpectedArgumentCount returns 0 for a `FormalParameterList : FormalParameter` whose HasInitializer is true and returns the count to the left of the rest parameter, so `params.len()` is the wrong number as well as the wrong place. **§15.2.4** InstantiateOrdinaryFunctionObject step 4 and **§15.2.5** InstantiateOrdinaryFunctionExpression perform **§10.2.9** SetFunctionName, whose step 6 defines `name` with those same attributes. §-numbers from `webref aoid ecma262 OrdinaryFunctionCreate` / `SetFunctionLength` / `SetFunctionName` / `ExpectedArgumentCount` / `InstantiateOrdinaryFunctionObject` / `InstantiateOrdinaryFunctionExpression`. Deliverable is both own properties at closure creation. ⚠ **The single `function f(a, b = 0, ...c) {}` case is one regression, not two, and it cannot fail on the rest-parameter axis** — **§15.1.5**'s `FormalParameterList : FormalParameterList , FormalParameter` step 2 returns `count` as soon as *either* side HasInitializer, so `b = 0` truncates at index 1 **before** the rest parameter is reached, and an implementation that only stops at the first initializer already answers **1**. The rest exclusion is a second, independent subtraction — `FormalParameters : FormalParameterList , FunctionRestParameter` returns the ExpectedArgumentCount of the list alone — and today's length source counts the rest element in: `compiled.param_count = func.params.len()` (`compiler/expr_function.rs:220` at `658cc302`), over the same `params` vector whose last element the next line reads as the rest (`compiled.has_rest_param = func.params.last().is_some_and(\|p\| p.rest)`). **So three regressions, one per subtraction plus the name**: `function f(a, b = 0, ...c) {}` → `f.length === 1` (initializer truncation), **`function g(a, ...r) {}` → `g.length === 1`** where `params.len()` is **2** (rest exclusion, invisible to the first case), and `f.name === "f"` — ⚠ **each asserted on its *descriptor*, not only its value**: **§10.2.10** SetFunctionLength step 2 and **§10.2.9** SetFunctionName step 6 both `DefinePropertyOrThrow` an **own** property with `{ [[Writable]]: false, [[Enumerable]]: false, [[Configurable]]: true }`, so an implementation supplying the right values through ordinary writable/enumerable own properties, or off the prototype, passes a value-only check while shipping the wrong function-object contract. `Object.getOwnPropertyDescriptor(f, 'length')` and `…(f, 'name')` are asserted against those three attributes. ⚠ **All three name their function *syntactically*, so none of them can fail on the name-**inference** sites** — an implementation that defines `name` in `create_closure` from `CompiledFunction.name` answers `"f"` for a declaration and the empty String for every anonymous definition, and passes this row as written. Inference is a different clause and a different layer: **§8.4.5** NamedEvaluation supplies the name, and its callers are the **binding** productions, not the function ones — **§14.3.1.3** BindingEvaluation step 3 for `LexicalBinding : BindingIdentifier Initializer` ("If IsAnonymousFunctionDefinition(Initializer) is true … NamedEvaluation of Initializer with argument bindingId"), **§13.2.5.6** PropertyDefinitionEvaluation step 5 (the same test with `propertyKey`, and suppressed while `isProtoSetter` is true), and **§14.3.3.3** KeyedBindingInitialization step 4.a.i for a destructuring default. So a fourth, fifth and sixth regression, one per site — `const f = () => {}; f.name === "f"`, `({m: function(){}}).m.name === "m"`, and `const {q = () => {}} = {}; q.name === "q"` — each again asserted on the descriptor, not the value. ⚠ **And that caller list is drawn as "the *binding* productions", which is one production short — NamedEvaluation is invoked from an ordinary *assignment* too, and an assignment binds nothing.** **§13.15.2** Runtime Semantics: Evaluation, production `AssignmentExpression : LeftHandSideExpression = AssignmentExpression`, **step 1.3**: "If IsAnonymousFunctionDefinition(AssignmentExpression) is true and IsIdentifierRef of LeftHandSideExpression is true … Let rightValue be ? NamedEvaluation of AssignmentExpression with argument lhs" (steps from `webref body ecma262 sec-assignment-operators-runtime-semantics-evaluation`), so an implementation wired to the three binding sites alone passes all six above while `let s; s = function () {}` still carries the empty String. **Seventh regression, on the descriptor like the rest**: `let s; s = function () {}; s.name === "s"`. ⚠ **And all seven name their function with a String, while **§10.2.9** *transforms* the argument before it defines anything — three branches none of them enters.** Step 2 rewrites a **Symbol** name to `"["`, its `[[Description]]`, `"]"`, or to the empty String when that description is undefined; step 3 rewrites a **Private Name** to its `[[Description]]`; and step 5 prefixes `prefix` and U+0020 when a caller supplies one, which **§15.4.5** MethodDefinitionEvaluation does at step 8 for `get` and step 7 for `set` (`webref body ecma262 sec-setfunctionname` / `sec-runtime-semantics-methoddefinitionevaluation`). A closure that defines `name` from `CompiledFunction.name` answers the empty String for all three and passes this row as written, which is the same failure mode the inference paragraph above records one clause over. Three more, on the descriptor like the rest: `({ [Symbol.iterator](){} })[Symbol.iterator].name === "[Symbol.iterator]"`, `Object.getOwnPropertyDescriptor({ get x(){} }, "x").get.name === "get x"`, and — once umbrella **5**'s children make a private method readable — `class C { #m(){} n(){ return this.#m.name } } (new C).n() === "#m"` — read through an **instance**: `#m` is an *instance* private method, so inside a `static n()` the receiver is `C`, which carries no such Private Name, and **§7.3.30** PrivateGet **step 2** ("If entry is empty, throw a TypeError exception", `webref body ecma262 sec-privateget`) throws before `.name` is ever observed — a spelling a correct SetFunctionName cannot pass. ⚠ **And every case above names the function, while §15.2.5 supplies a name where nothing does.** The anonymous production `FunctionExpression : function ( FormalParameters ) { FunctionBody }` opens with **step 1** "If name is not present, set name to the empty String" and reaches the same step-6 `SetFunctionName` (`webref body ecma262 sec-runtime-semantics-instantiateordinaryfunctionexpression`), so an implementation that defines `name` only where a name exists — syntactic or inferred — passes every case above while `(function(){})` carries no own `name` at all. **And the value form cannot see it either**: **§20.2.3** gives %Function.prototype% itself a `"name"` property "whose value is the empty String" (`webref body ecma262 sec-properties-of-the-function-prototype-object`), so `(function(){}).name === ""` is satisfied by inheritance from an implementation that defines nothing. One more, on the descriptor for exactly that reason: `Object.getOwnPropertyDescriptor(function(){}, "name")` is `{value: "", writable: false, enumerable: false, configurable: true}`. **This changes the deliverable's shape rather than adding assertions to it**: the binding name is compile-time information `create_closure` does not receive, so the slot cannot be discharged wholly at closure creation. `git grep -rn -e NamedEvaluation -e SetFunctionName -e OrdinaryFunctionCreate -- crates/script/elidex-js/src` → **0** at `c6b5bc31`, against `-e CopyDataProperties` → **2** and `-e OrdinaryHasInstance` → **1** by the same command on the same tree, so the probe discriminates rather than returning zero for every AO name. §-numbers from `webref aoid ecma262 NamedEvaluation` / `BindingEvaluation` / `PropertyDefinitionEvaluation` / `KeyedBindingInitialization`. **Not Slice 9's**: §5's Slice 9 row scopes its derivation to the property surface ECMA-262's main body defines *on an intrinsic object*, and these are the own properties syntax gives every ordinary function, so 9 can retire with its inventory complete and these still absent. **Owned by a slot rather than a §5 slice** because `create_closure` emits and connects correctly — a live defect in fully emitted code, which §1.0's derivation does not produce | this slot's own **derivation**, at the next `vm/ops.rs` closure-creation or `Function.prototype` PR — that occasion runs the derivation and mints the terminal children, never a PR that would carry the slot, an umbrella shipping none | 2026-09-30 |
| `#11-vm-object-literal-proto-setter` | **(carved this round)** `{__proto__: p}` defines an own property instead of setting the prototype. The parser already isolates the production and then discards the distinction: `parse_object_literal` tracks `has_proto` (`parser/object.rs:21`) for a non-computed `__proto__` identifier or string key (`:86-102` at `658cc302`) solely to raise the duplicate-field SyntaxError. `compile_object_expr`'s `PropertyKind::Init` arm (`compiler/expr_object.rs:46`) has no `__proto__` branch — an `Identifier` key (`:84`) or a `Literal::String` key (`:89`) lowers to `Op::DefineProperty` like any other, so `const p={x:1}; const o={__proto__:p}` leaves `o`'s prototype unchanged and gives it an own `__proto__` data property. ECMA-262 **§13.2.5.6** PropertyDefinitionEvaluation, production `PropertyDefinition : PropertyName : AssignmentExpression`, step 3 sets `isProtoSetter` when "propertyKey is \"__proto__\" and IsComputedPropertyKey of PropertyName is false"; step 7.1 then performs `obj.[[SetPrototypeOf]](propertyValue)` when "propertyValue is an Object or propertyValue is null" and step 7.2 returns — so for any other value the production defines nothing at all — and step 5 suppresses NamedEvaluation while `isProtoSetter` is true (`webref aoid ecma262 PropertyDefinitionEvaluation`, `webref body ecma262 sec-runtime-semantics-propertydefinitionevaluation`). Deliverable is that branch, with one regression per value class in step 7 — ⚠ **and the §13.2.5.6 step 5 clause this cell cites, which suppresses NamedEvaluation while `isProtoSetter` is true, carries no condition of its own**, so a branch that performs `[[SetPrototypeOf]]` for the object and null cases while still running NamedEvaluation on the value passes every stated condition. **Seventh red probe at `658cc302`**: `Object.getPrototypeOf({__proto__: function(){}}).name` — read once §8 slot `#11-vm-ordinary-function-name-length`'s children make `name` an own property — must be the empty String and not `"__proto__"`, and until then the same condition is asserted at the compiler, that the `__proto__` arm emits no name-inference for its value: an object, `null`, and a primitive that must leave the literal with neither the property nor a changed prototype. ⚠ **All three are the *setter* spelling, so none can fail on an implementation that routes every `__proto__` key to `[[SetPrototypeOf]]`** — step 3's test is a conjunction, `propertyKey is "__proto__"` **and** `IsComputedPropertyKey of PropertyName is false`, and the three exercise only the first conjunct. Four more, one per production the same clause sends elsewhere: `{["__proto__"]: p}` is a **computed** PropertyName, so `isProtoSetter` is false and it must leave an own data property with the prototype untouched — `Object.getPrototypeOf({["__proto__"]: p}) === Object.prototype` **and** `Object.getOwnPropertyDescriptor({["__proto__"]: p}, "__proto__") !== undefined` — ⚠ **the pair, on a warrant that measurement falsifies.** "Either alone is passed by an implementation that does nothing at all" is backwards: the tree at `658cc302` **is** that implementation — nothing sets a prototype in any spelling (`const p = {x: 1}; Object.getPrototypeOf({__proto__: p}) === p` → executed `false`) — and it answers **both** halves green. What the pair discriminates is the opposite error, a repair routing *computed*, shorthand or method `__proto__` into `[[SetPrototypeOf]]`, and against that error either half alone would do; both are kept because they fail on different halves of that repair, and both are **guards** on the remedy rather than probes of a live defect; `{__proto__}` is the `PropertyDefinition : IdentifierReference` production, which has no proto step; `{__proto__(){}}` is the `PropertyDefinition : MethodDefinition` production, whose entire body is `MethodDefinitionEvaluation of MethodDefinition with arguments obj and true` and which has no proto step either (`webref body ecma262 sec-runtime-semantics-propertydefinitionevaluation`), so `const o = {__proto__(){}}; Object.getPrototypeOf(o) === Object.prototype` **and** `Object.getOwnPropertyDescriptor(o, "__proto__") !== undefined` — the same *guard* pair, on the corrected warrant above, and green at `658cc302` for the same reason; and a destructuring **pattern** is a different algorithm — **§14.3.3.3** KeyedBindingInitialization is a plain `GetV(value, propertyName)` with no `__proto__` branch, so `const {__proto__: q} = o` must bind `q` to the *read* of that key and change no prototype. The parser already computes exactly the discriminant these need (`has_proto` is set only for a non-computed identifier or string key, `parser/object.rs:86-102`), which is why an implementation can discard it a second time and still pass the three value-class regressions. ⚠ **And all six are written with the *identifier* spelling, while the two spellings are separate compiler arms this row's own measurement names.** Step 3's first conjunct tests the **evaluated** `propertyKey`, not the PropertyName's syntactic form — "propertyKey is \"__proto__\" and IsComputedPropertyKey of PropertyName is false" — so a `Literal::String` key is a proto setter exactly as an `Identifier` key is (`webref body ecma262 sec-runtime-semantics-propertydefinitionevaluation`), and this cell measures those as `compiler/expr_object.rs:84` and `:89`, two arms. A branch added to the identifier arm alone passes every one of the six: a seventh regression is the pair `const p = {x: 1}; const o = {"__proto__": p}; Object.getPrototypeOf(o) === p` **and** `Object.getOwnPropertyDescriptor(o, "__proto__") === undefined`, ⚠ **the one genuinely red pair in this cell**: at `658cc302` `{"__proto__": p}` sets nothing and leaves an own property, so **both** halves fail today. Its warrant is not the (corrected) one above — half one alone is passed by a repair that sets the prototype and leaves the own property standing, half two alone by one that drops the property without setting anything. ⚠ **The same non-computed test is also a parse-time rule the change must not break**: **§13.2.5.1** Static Semantics: Early Errors makes a duplicate `"__proto__"` a SyntaxError only when at least two entries came from `PropertyDefinition : PropertyName : AssignmentExpression`, and its Note 1 excludes computed names from PropertyNameList — so `({__proto__: 1, "__proto__": 2})` must throw while `({__proto__: 1, ["__proto__"]: 2})` must not. Steps from `webref body ecma262 sec-object-initializer-static-semantics-early-errors` / `sec-runtime-semantics-keyedbindinginitialization`. **Core, not Annex B**: `webref body ecma262 sec-runtime-semantics-propertydefinitionevaluation` resolves to `multipage/ecmascript-language-expressions.html`, so the initializer form is main-body normative; the separate `__proto__` **accessor** is §20.1.3.8 (`webref heading ecma262 20.1.3`). `crates/script/elidex-js/src/lib.rs:18` lists "`__proto__` semantics" inside its "No Annex B" policy paragraph; that sentence reaches the accessor and not §13.2.5.6, and the slice that lands this narrows it. **Not `#11-vm-property-key-lowering-unification`**: that slot's deliverable is collapsing the open-coded `PropertyKey` → static-name lowerings into one, and every one of them would still emit a define for this key; the mechanism here replaces the define with `[[SetPrototypeOf]]` on a production-level test, a different governing algorithm. Both land in `compile_object_expr`'s `Init` arm, so whichever goes second inherits the other's shape — an ordering fact, not a shared charter. **Owned by a slot rather than a §5 slice** because none of §2.2's three keys is on this lowering: it is a marker-free `Op::DefineProperty` emit inside an arm that exists, so a marker sweep, a `PushUndefined` census and a variant-presence walk each pass over it — the §2.2 property that section states as a property of its commands | the next `compiler/expr_object.rs` PR, or with `#11-vm-property-key-lowering-unification` once its kind is resolved (it is UNDETERMINED and mints no child today) | 2026-09-30 |
| `#11-vm-instanceof-callable-check` | **(carved at PR-B)** `op_instanceof` (`vm/dispatch_objects.rs:352-384` at `658cc302`) rejects a non-Object right operand (`:353-357`), then either calls whatever the `@@hasInstance` lookup returned (`:361-364`) or walks the left operand's prototype chain against `rhs.prototype` (`:367-383`) — with no callability test between the two, so `({}) instanceof {prototype:{}}` answers `false`. ECMA-262 **§13.10.2** InstanceofOperator puts one there: step 2's `GetMethod` treats a `null` or `undefined` `@@hasInstance` as absent and step 3 therefore routes it to the default path, step 4 is "If IsCallable(target) is false, throw a TypeError exception", and only step 5 reaches **§7.3.21** OrdinaryHasInstance — whose step 5 additionally throws when the `"prototype"` read is not an Object, where the handler answers `false`. Deliverable is that ordering, with regressions for a non-callable right operand, a `null`-valued `@@hasInstance`, and a callable whose `prototype` is a primitive. ⚠ **The `null`-valued case exercises the *nullish* arm of step 2's `GetMethod` and never its throwing one.** **§7.3.10** GetMethod is `GetV`, then step 2 "If func is either undefined or null, return undefined", then step 3 "If IsCallable(func) is false, throw a TypeError exception" (`webref body ecma262 sec-getmethod`) — so a **present but non-callable** `@@hasInstance` must abort §13.10.2 at step 2 rather than reach step 3's default route. A seventh regression: `({}) instanceof { [Symbol.hasInstance]: 42 }` must throw a TypeError, where an implementation that tests callability of the looked-up value and falls through on failure answers `false` and passes all six above — ⚠ **green at `658cc302`** (executed → `TypeError "not a function"`), so it is a **guard** on the remedy and not a probe of the tree: it fails only on the fall-through repair that sentence names. ⚠ **All three land in §13.10.2's step 4 or in §7.3.21's steps 4-5, so none of them reaches §7.3.21 step 2 — the bound-function forward.** OrdinaryHasInstance orders it early on purpose: step 1 returns `false` for a non-callable `ctor`, and step 2 is "If ctor has a [[BoundTargetFunction]] internal slot … Return ? InstanceofOperator(instance, boundCtor)" — **before** step 4's `Get(ctor, "prototype")`. The ordering is load-bearing, not cosmetic: **§20.2.3.2** `Function.prototype.bind` Note 1 says bound functions "do not have a `prototype` property", so an implementation that reaches step 4 with a bound `ctor` reads `undefined` and step 5 **throws a TypeError** exactly where the answer is `true`. In-tree the slot exists and the unwrap exists elsewhere — `ObjectKind::BoundFunction { target, .. }` (`vm/natives_function.rs:113` at `658cc302`, whose bound object gets `prototype: func_proto` and only `length` and `name` defined, `:122-138`) and `vm/dispatch_class.rs:187-199` already walks the chain for `[[Construct]]` — while `op_instanceof` has none, so it falls out of its `if let` and answers `false`. Fourth and fifth regressions, as a pair: `class C {}; const B = C.bind(null); new C() instanceof B` is `true`, **and** `(function(){}) instanceof B` is `false` — ⚠ **the warrant stated here was false and is corrected**: **§7.3.21** step 2.2 is verbatim `Return ? InstanceofOperator(instance, boundCtor)` (`webref body ecma262 sec-ordinaryhasinstance`), so an unwrap that forwards the target *without* re-entering §13.10.2 answers `true` to the first half and `false` to the second — it **passes** the stated pair rather than failing it, and the pair therefore separates nothing. **The condition that does**, red at `658cc302`: re-entering §13.10.2 is what re-consults `%Symbol.hasInstance%` on the *bound* function, so `class C { static [Symbol.hasInstance](x){ return true } }; ({}) instanceof C.bind(null)` must be `true`, where an unwrap calling the ordinary chain walk on the target answers `false`. ⚠ **Installing the inherited method does not route around this**: **§20.2.3.6** `Function.prototype [ %Symbol.hasInstance% ]` step 2 is itself `OrdinaryHasInstance(thisValue, value)`, so the §13.10.2 step-3 route and the step-5 route reach the same step 2. ⚠ **And that method is absent today, which is a second defect this row is the only home for**: `git grep -n has_instance 658cc302 -- crates/script/elidex-js/src` returns **10** lines and every one is accounted for — four are `op_instanceof`'s own lookup (`vm/dispatch_objects.rs:360-363`), one is the `Symbol.hasInstance` property **on the `Symbol` constructor** (`vm/globals_primitives.rs:264`), two are the `SymbolId` field and its `alloc` (`vm/well_known.rs:1555`, `:1580`) and three are tests — **none installs a method on `%Function.prototype%`** (controls by the same command on the same rev: `to_primitive` → **51**, `well_known_symbols.iterator` → **18**, so the sweep is not returning near-zero for everything). §20.2.3.6's own attribute sentence makes that a sixth regression: `Object.getOwnPropertyDescriptor(Function.prototype, Symbol.hasInstance)` is `{writable: false, enumerable: false, configurable: false}` — ⚠ **a red probe whose producer is not this slot's, and the cell named neither the producer nor its owner.** `typeof Function` is `"undefined"` at `658cc302`, so the witness dies on `Function is not defined` before it reads any attribute; the producer is the child umbrella `#11-vm-function-constructor-global`'s derivation mints for the `Function` global, and §5's `Function`/`eval` pointer row states which forms of that surface are core. This slot waits on that umbrella's derivation for the sixth regression **alone** — the other six read `op_instanceof` and values reachable without the `Function` global, and stand at this slot's own parent HEAD. §-numbers from `webref aoid ecma262 InstanceofOperator` / `OrdinaryHasInstance`; steps from `webref body ecma262 sec-ordinaryhasinstance` / `sec-function.prototype.bind` / `sec-function.prototype-%symbol.hasinstance%`. **Owned by a slot rather than a §5 slice** because it is a live defect in a connected dispatch handler, which §1.0's derivation does not produce — the same routing as `#11-vm-for-in-enumeration-semantics` and `#11-vm-object-spread-source-coercion` | the next VM dispatch-loop PR, or with `#11-vm-proxy-reflect` — ⚠ **and, for the sixth regression alone, on `#11-vm-function-constructor-global`'s derivation having produced the child that supplies the `Function` global**, the wait the *Why* cell above states in prose. §5's single-home rule makes this cell that wait's only readable home, so a producer named only beside it is one a lander reading the ordering field never sees, and the slot would start with `Object.getOwnPropertyDescriptor(Function.prototype, Symbol.hasInstance)` dying on `Function is not defined`. **The §20.2.3.6 install is this slot's deliverable either way** — only its witness waits — so the slot may not retire with that method absent; the other six regressions stand at this slot's own parent HEAD. Re-pointing the edge at that child once minted is a mandatory output of the same derivation | 2026-09-30 |
| `#11-vm-const-assignment-runtime-throw` | **(carved at PR-B)** `compile_identifier_store` returns `CompileError { message: "Assignment to constant variable '…'" }` for a write to a `BindingKind::Const` local (`compiler/expr_assign.rs:81-88` at `2039766a`), so a syntactically valid assignment yields no bytecode for the whole script and `try { const x = 1; x = 2 } catch (e) { 42 }` never reaches its `catch`. ECMA-262 puts the failure at run time: **§13.15.2** Runtime Semantics: Evaluation reaches **§6.2.5.6** PutValue, whose Declarative Environment Record path is **§9.1.1.1.5** SetMutableBinding step 5.b, "If strict is true, throw a TypeError exception". **The remedy is not re-derived here**: [§17](2026-07-vm-p4-slice-0a-landing-record.md)'s private-store CRIT had this exact shape — loud but not scoped — and §9 dec. 5 resolved it against measured evidence to a runtime throw carried by `Op::ThrowUnsupported` (an engine-constructed error raised through `throw_error`, catchable and local), with `CompileError` reserved for what the spec rejects before execution. Deliverable is that conversion at this arm, with a regression that the `catch` runs and the statements after it still execute — the observable [§18](2026-07-vm-p4-slice-0a-landing-record.md) measured as the cost of the `CompileError` form. ⚠ **That assertion is over control flow and pins no error, while the AO it cites names one — and the mechanism this row selects is the one that produces a different one.** **§9.1.1.1.5** SetMutableBinding step 5.b is "If strict is true, throw a **TypeError** exception", whereas `Op::ThrowUnsupported` is what `#11-vm-internal-error-hard-exit` describes as reporting "an unimplemented construct" — catchable and local, as required, and not a TypeError. So the regression asserts `e.constructor === TypeError`, not merely that `catch` ran. And a second, because step 5 is reached only after the value exists: **§13.15.2**'s `AssignmentExpression : LeftHandSideExpression = AssignmentExpression` evaluates the right-hand side at step 1.4 and performs `PutValue` at step 1.5, so `let n = 0; try { const x = 1; x = (n++, 2) } catch (e) {} n === 1` — a throw emitted at the store site *ahead of* the operand's bytecode answers `0` and passes the control-flow assertion (steps from `webref body ecma262 sec-declarative-environment-records-setmutablebinding-n-v-s` / `sec-assignment-operators-runtime-semantics-evaluation`). ⚠ **And both of those are the *simple-assignment* spelling — the one arm this row already measures — while §9.1.1.1.5 step 5.b is reached from **every** write to the binding, and three sibling write paths never enter `compile_identifier_store` at all.** **(i) `UpdateExpression`**: **§13.4.2.1** and **§13.4.4.1** each perform `PutValue` at **step 6**, and the update compiler resolves the identifier itself and takes an `Op::IncLocal` / `Op::DecLocal` fast path with no const test (`compiler/expr_ops.rs:189-196` at `658cc302`; its upvalue arm `:213-225` likewise). **(ii) a *captured* const**: the `Const` rejection sits inside `compile_identifier_store`'s `VarLocation::Local` arm (`compiler/expr_assign.rs:81-88`), while the sibling arm immediately below it is a bare `VarLocation::Upvalue(idx) => fc.emit_u16(Op::SetUpvalue, idx)` (`:96`) with no test of any kind. **(iii) the `for (x of …)` / `for (x in …)` assignment-target head**: **§14.7.5.7** ForIn/OfBodyEvaluation **step 8.g.ii.4** is `PutValue(lhsRef.[[Value]], nextValue)`, and `compile_forin_left_binding`'s `ForInOfLeft::Pattern` arm emits `Op::SetLocal` / `Op::SetUpvalue` straight off `resolve_identifier` (`compiler/stmt_loop.rs:324-343`, its own comment "Assignment target (e.g. `for (x in obj)`)"). Three more regressions, one per path, each asserting the same `TypeError`: `function h(){ const x = 1; x++ } h()`, `function h(){ const x = 1; const w = () => { x = 2 }; w() } h()`, and `function h(){ const x = 1; for (x of [7]) {} } h()`. ⚠ **This enumeration has now been found short twice — it began as the simple assignment alone, grew to four, and `const x = 1; [x] = [2]` is a fifth — so what follows replaces the list as the statement of scope, and the list above is only that derivation's output at this rev.** The population is **every `resolve_identifier` call in `compiler/` whose result is consumed by a binding-store emit**, and both halves of it are enumerated by command rather than recalled. **Opcodes, from the definition**: `git show 658cc302:crates/script/elidex-js/src/bytecode/opcode.rs \| grep -cE '^    [A-Z][A-Za-z0-9]*,$'` → **128** variants, of which exactly **six** write a *binding* — `SetLocal`, `InitLocal`, `SetUpvalue`, `SetGlobal`, `IncLocal`, `DecLocal`. The complement is measured and not assumed: the rest of the `Set` / `Inc` / `Dec` family (`SetProp`, `SetElem`, `SetPrivate`, `SetSuperProp`, `IncProp`, `DecProp`, `IncElem`, `DecElem`, `SetPrototype`, `DefineSetter`, `DefineComputedSetter`) writes **properties**; there is **no** `IncUpvalue` / `DecUpvalue` / `IncGlobal` / `DecGlobal`, those two arms lowering to a `Get`/`Set` sequence (`compiler/expr_ops.rs:197-225`), which is why (i) above is a `Local`-arm fact and not an update-expression one; and `GetModuleVar` has no write counterpart at all — every `VarLocation::Module` arm reaches `emit_unsupported`, which is the child of umbrella **M** that implements module bindings — the owner §2.2's `expr_ops.rs:226` and `stmt.rs:877-878` rows already carry for module-binding stores — and not this slot's ⚠ **(this read `#11-vm-assignment-target-completeness`, an attribution neither that slot's own cell nor any 0b-family row carries)**. **Sites**: `git grep -n 'resolve_identifier(' 658cc302 -- crates/script/elidex-js/src/compiler` returns **12** lines, two of them `resolve.rs:266` (the `fn` itself) and `resolve.rs:507` (a unit test), leaving **10** in compiled code. Two are **loads** and store nothing — `expr_assign.rs:26` (identifier read) and `expr_ops.rs:118` (`typeof`). Four store into **the binding that site's own declaration introduces**, so their target can never be a pre-existing `const` and they are out of scope by algorithm rather than by convenience — **§9.1.1.1.4** InitializeBinding is a different abstract method from the **§9.1.1.1.5** SetMutableBinding this slot is about, and a `const` declarator's own store is the one write that must *not* throw: `stmt.rs:46` (class declaration), `stmt.rs:427` (catch parameter, whose binding **§14.15.2** CatchClauseEvaluation step 3.a creates with `CreateMutableBinding(argName, false)`), and `mod.rs:94` / `expr_function.rs:181` (hoisted function declarations, both emitting `Op::Closure` then the store). The remaining **four** are assignment stores and are this slot's scope: `expr_assign.rs:65` `compile_identifier_store`, `expr_ops.rs:181` the update expression, `stmt_loop.rs:329` the for-in/of assignment-target head, and `stmt_destructure.rs:197` `compile_pattern_store`. Each resolves into three storing arms (`Local` / `Upvalue` / `Global`; `Module` emits unsupported), so the scope is **twelve arms**, exactly **one** of which tests `BindingKind::Const` — `expr_assign.rs:81-88`, the `Local` arm of the first site. ⚠ **"twelve" over-counts, and the corrected denominator still exceeds the five regressions listed above.** `resolve_identifier` returns `VarLocation::Global` only on the not-found path (`compiler/resolve.rs:266-317`, whose step-4 comment reads "Not found — it's a global"), so a `const` binding can never resolve to a Global arm: the const-reachable scope is at most **eight** arms, 4 assignment sites × Local/Upvalue. Three of those eight are uncovered by any stated condition, and the row left them so. **Two red probes at `658cc302`, one per uncovered storing arm**: `function h(){ const x = 1; const w = () => { x++ }; w() }` must throw a **TypeError** where `expr_ops.rs:213-225` reaches a bare `Op::SetUpvalue` with no `BindingKind` test, and `function h(){ const x = 1; const w = () => { [x] = [2] }; w() }` must throw one where `compile_pattern_store` does the same — silent today in the first and already a `TypeError` in the second — ⚠ **so the second cannot fail on the arm it names**: measured at `658cc302`, `const x = 1; const w = () => { x++ }; w()` completes and leaves `x` at `2`, while the pattern spelling raises `TypeError: assignment to this target is not yet supported` from the unconnected `AssignTarget` arm §1.2 records, satisfying "must throw a **TypeError**" with no `BindingKind` consulted anywhere. The pattern half is red only as a pair with its `let` twin, `function h(){ let x = 1; const w = () => { [x] = [2] }; w(); return x } h() === 2`, which raises that same `TypeError` today where Node answers `2`, against the plain-store control `function h(){ let x = 1; const w = () => { x++ }; w(); return x } h() === 2`, which already passes. Both are invisible to the five conditions above, which is what admits the five-copy transcription this cell rejects in prose, and the reason the list kept growing is that it enumerated *arms* and *sites* interchangeably: "simple" and (ii) "captured" are two arms of one site, not two paths. ⚠ **`compile_pattern_store` is the site that is *both* kinds of store, which is why it cannot take an unconditional copy of that check.** It takes a `kind: VarKind`, `VarKind` is `Var` / `Let` / `Const` with no assignment variant (`ast.rs:139-143`), and all **four** of its callers pass a declarator's kind (`stmt.rs:118`, `stmt_destructure.rs:34`, `stmt_loop.rs:248`, `stmt_loop.rs:319`) — so today it is reached only from declarations, and its `Op::InitLocal` at `:208` is gated on the `Let` and `Const` kinds accordingly. The child umbrella **0bc**'s derivation mints for destructuring-assignment connection is what makes it an assignment store as well, at which point one function serves both algorithms. The deliverable is therefore that the const test sits **at the resolution, keyed on which of the two algorithms the store is performing**, rather than as a fourth transcription of the `expr_assign.rs:81-88` branch — CLAUDE.md *One issue, one way*, and the reason a fifth bullet would not have been the fix. Its regression is `function h(){ const x = 1; [x] = [2] } h()` throwing that same `TypeError` — ⚠ **which it already does at `658cc302`, and for the wrong reason**: measured, it raises `TypeError: assignment to this target is not yet supported` from the arm §1.2 records as unconnected, so the condition is satisfied with no const test on any path. It is red only as a pair with `function h(){ let x = 1; [x] = [2]; return x } h() === 2`, which raises that same `TypeError` today where Node answers `2`, with the **positive control** `function h(){ const [x] = [1]; return x } h() === 1` that the declarator's own store through the very same function still succeeds — the control without which "key the check on the algorithm" degrades to "reject every pattern store". §-numbers by AO name: `webref aoid ecma262 CatchClauseEvaluation`, and `webref heading ecma262 9.1.1.1` for the **§9.1.1.1.4** / **§9.1.1.1.5** pair. Steps from `webref body ecma262 sec-postfix-increment-operator-runtime-semantics-evaluation` / `sec-prefix-increment-operator-runtime-semantics-evaluation` / `sec-runtime-semantics-forin-div-ofbodyevaluation-lhs-stmt-iterator-lhskind-labelset`. **Owned by a slot rather than a §5 slice** because §2.2's pass 3 flags an arm that neither emits an op nor returns `CompileError`, and this arm returns one, so §1.0's derivation does not produce it | the next PR that edits any of the **four** assignment-store sites this row's derivation returns — `compiler/expr_assign.rs:65`, `compiler/expr_ops.rs:181`, `compiler/stmt_loop.rs:329`, `compiler/stmt_destructure.rs:197` — re-derived from the predicate at that PR's parent HEAD rather than read off the frozen line numbers. ⚠ **"the next `compiler/expr_assign.rs` PR — Slices 0bb and 0bc both name that file" stood here**, which is the pre-widening single file the *Why* cell above already replaced as the statement of scope: a PR editing only `expr_ops.rs`, `stmt_loop.rs` or `stmt_destructure.rs` would not have fired it | 2026-09-30 |
| `#11-vm-upvalue-tdz-check` | **(carved at PR-B)** `compile_identifier_load` emits `Op::CheckTdz` before `Op::GetLocal` for a `VarLocation::Local` (`compiler/expr_assign.rs:34-42` at `2039766a`) and a bare `Op::GetUpvalue` for a `VarLocation::Upvalue` (`:45`), and the runtime half cannot recover the check: the `Op::GetUpvalue` arm pushes `read_upvalue` (`vm/dispatch.rs:155-160`), which returns the stack slot or the closed value (`vm/ops.rs:597-602`); `UpvalueState` carries exactly `Open { frame_base, slot }` and `Closed(JsValue)` (`vm/value.rs:952-957`), with no uninitialized state; and the TDZ bits sit on `CallFrame` (`is_in_tdz`, `vm/value.rs:1106`), which an upvalue read never consults. So `let f = () => x; f(); let x = 1` reads `undefined` where **§9.1.1.1.6** GetBindingValue step 2 throws a ReferenceError for an uninitialized binding, "regardless of the value of strict". Deliverable spans the three layers together — ⚠ **and this slot is ordered after the child umbrella `C`'s derivation mints for the uninitialized state and the read-before-initialization raise, which C's own cell already claims and the read-before-initialization raise, so two of the three conditions are plausibly green before this slot starts and none of them declares a reading.** **Polarity is therefore re-derived at this slot's own plan-review against its parent HEAD**, per §4 I-8, and the row states which of its conditions it expects `C`'s child to have turned green. **The condition `C`'s child cannot turn green**, red at `658cc302` and red after `C`'s child as well: a binding captured and then *escaped* while still uninitialized — `let g; try { const f = () => y; g = f; throw 0; let y = 1 } catch (e) {} g()` — must raise a ReferenceError per **§9.1.1.1.6** step 2, where a remedy that consults the owner frame's TDZ bits has no frame left to consult and answers `undefined` — ⚠ **which the spelling above does not produce, because its `try` sits at script top level and that frame is still executing when `g()` runs.** Measured at `658cc302` over the same shape with a second escaped closure that *writes* the binding, the write lands and the escaped reader sees it (`a=undefined`, then `b=9`), so the owner frame is live and answerable; wrapping the whole shape in a function gives `b=undefined` and is the spelling that has no frame left. The condition is therefore `function make(){ let g; try { const f = () => y; g = f; throw 0; let y = 1 } catch (e) {} return g } make()()`, which answers `undefined` at `658cc302` where Node raises the ReferenceError, against the positive control this row already states, `let g = () => x; let x = 1; g() === 1`, measured green here and under Node. ⚠ **And the first limb of this deliverable is stated by Slice `C` as well, which is one artifact with two owners** — the resolution is `C`'s derivation's, and this slot's scope narrows to whatever that derivation does not place — an uninitialized state the upvalue representation can carry, the emit that checks it, and the dispatch arm that raises — with a regression reading a captured binding before its declaration. ⚠ **That is a single rejection, so it cannot fail on the arm that raises for *every* upvalue read — the failure `#11-vm-private-name-early-errors` states in as many words for its own rejection set — and it pins neither the error nor the write path.** **§9.1.1.1.6** GetBindingValue step 2 throws a **ReferenceError** "regardless of the value of strict", and **§9.1.1.1.5** SetMutableBinding **step 3** throws the same on a *write* to an uninitialized binding, one step ahead of the mutable-against-immutable split (`webref body ecma262 sec-declarative-environment-records-getbindingvalue-n-s` / `sec-declarative-environment-records-setmutablebinding-n-v-s`). Three regressions, not one: the read raises a `ReferenceError` specifically; the **positive control** `let g = () => x; let x = 1; g() === 1` still reads through the same upvalue once initialized, which is what makes the uninitialized *state* the deliverable rather than a blanket raise; and `let f = () => { x = 1 }; f(); let x` raises — the trailing `let x` is **part of the source, not a gloss on it**: with no declaration `x` resolves to `VarLocation::Global` and today's `Op::SetGlobal` already raises a ReferenceError for an undeclared name (`vm/dispatch.rs:195-222` at `d49465d0`), so the case would go green without reaching `Op::SetUpvalue` at all, which an emit placed only ahead of `Op::GetUpvalue` never reaches. **Owned by a slot rather than a §5 slice** because §2.2's sweep is over `compiler/` arms and this arm emits an op, so §1.0's derivation does not produce it; the defect is only visible across the compiler/runtime seam. ⚠ **It consumes the child umbrella `C`'s derivation mints for C's runtime declarative-environment and closure-capture representation** — the same edge the parameter-environment slot above was found to be missing, and missing here for the same reason. This slot's own deliverable names it: "an uninitialized state the **upvalue representation** can carry", and the upvalue representation *is* what C owns — C's row measures the gap as `add_upvalue` deduplicating captures by `(scope_idx, name)` through `upvalue_map` (`compiler/resolve.rs:31`, `:148-157`), which is the same compile-time allocation the `UpvalueState { Open, Closed }` pair above records with no third state. So settling this before C exists adds an uninitialized state to a capture mechanism C then replaces: the two-mechanism outcome CLAUDE.md *One issue, one way* forbids, and the one C was carved out of a slot to prevent. **Neither existing trigger supplies it**: "the next `vm/value.rs` + `compiler/` PR" supplies nothing at all, and the "with Slice **B**" route is transitive rather than stated — the child umbrella **B**'s derivation mints for §14.2.3 declaration instantiation *consumes* C rather than producing it, and B itself carries no such edge: its `Deps` cell reads `—` | **after the child umbrella `C`'s derivation mints for the runtime declarative-environment and closure-capture representation**, then the next `vm/value.rs` + `compiler/` PR, or with the child umbrella **B**'s derivation mints for §14.2.3 declaration instantiation, which owns when a block's bindings become initialized | 2026-09-30 |
| `#11-vm-named-function-expression-binding` | **(carved at PR-B)** ⚠ **Deliverable includes the immutability regression, carried here and not only in §2.2's evidence row**: recursion through the self-name and identity with the bound value **both pass on a mutable binding in the function's own scope**, while **§15.2.5** step 5 is `CreateImmutableBinding(name, false)` — so strict-mode assignment to the self-name inside the body must throw a TypeError (**§9.1.1.1.5** SetMutableBinding step 5.b). A lander schedules from this row, so an acceptance living only in the evidence row is one this row can retire without. `visit_function` adds a function **expression**'s own name as a `BindingKind::Function` binding inside the function's scope (`scope/visitor.rs:458-463` at `2039766a`), `compile_nested_function` initializes every such local above the parameter count to `undefined` (`compiler/expr_function.rs:120`, `:130-132`), and the `ExprKind::Function` arm emits only `Op::Closure` (`compiler/expr.rs:210-214`) — the two *declaration* paths are the ones that follow `Op::Closure` with a `resolve_identifier` and a store (`compiler/mod.rs:93-96`, `compiler/expr_function.rs:180-183`), which is the probe that separates them rather than counting the opcode. So `let f = function g(){ return g }; f() === f` is false and recursion through `g` fails. ECMA-262 **§15.2.5** InstantiateOrdinaryFunctionExpression's `FunctionExpression : function BindingIdentifier ( FormalParameters ) { FunctionBody }` production requires a different shape: step 4 `NewDeclarativeEnvironment(outerEnv)`, step 5 `CreateImmutableBinding(name, false)`, step 8 creating the closure against that environment, step 11 `InitializeBinding(name, closure)`. Deliverable is that environment and its initialization — immutable, and reachable only from inside the body — with regressions for recursion through the self-name and for its identity with the bound value. ⚠ **Both of those are read from *inside* the body, so neither can fail on a remedy that puts the binding in the *enclosing* scope** — which is the likelier wrong shape, because the enclosing scope is the one the compiler already has open when it reaches `ExprKind::Function`. Step 4's environment is `NewDeclarativeEnvironment(outerEnv)` and §15.2.5's own Note says the BindingIdentifier "cannot be referenced from and does not affect the scope enclosing the FunctionExpression", so a third regression states the negative half from outside: `(function f(){}); typeof f === "undefined"` — `typeof` rather than a bare reference because **§13.5.3.1** step 2.a returns `"undefined"` for an unresolvable reference instead of throwing (`webref body ecma262 sec-typeof-operator`), and the compiler already emits for that case (`compiler/expr_ops.rs:115` "typeof on unresolved global should not throw", `Op::TypeOfGlobal` at `:128`). ⚠ **It is green today, and is a guard on the remedy rather than a red probe**: `visit_function` pushes `ScopeKind::Function` *before* adding the self-name (`scope/visitor.rs:456` then `:458-462` at `658cc302`), so nothing leaks now — what this asserts is that the funcEnv this row builds does not *start* leaking, which is the one axis the other two regressions structurally cannot see. ⚠ **And all three read one closure, while §15.2.5's environment is built per *evaluation* of the FunctionExpression.** Step 4's `NewDeclarativeEnvironment(outerEnv)` and step 11's `InitializeBinding(name, closure)` are steps of a syntax-directed operation the production runs on each evaluation, so two evaluations of one syntactic site owe two records bound to two closures (`webref body ecma262 InstantiateOrdinaryFunctionExpression`). A funcEnv allocated once per site — the shape a compile-time `(scope_idx, name)` allocation invites, and precisely the gap C's row measures — satisfies recursion, identity and the `typeof` guard alike while aliasing one instance's self-name onto another's. Fourth regression: `function make(){ return function g(){ return g } } const a = make(), b = make(); a() === a && b() === b && a() !== b()`. ⚠ **And all four are satisfied by the shape already in tree — the self-name as a binding in the function's *own* scope — merely made immutable in place, which is precisely the remedy that breaks two things that are correct today.** Step 4's `NewDeclarativeEnvironment(outerEnv)` and step 8's `OrdinaryFunctionCreate(…, funcEnv, privateEnv)` put funcEnv *outside* the call environment — **§9.1.2.4** NewFunctionEnvironment **step 6** sets the call record's `[[OuterEnv]]` to `func.[[Environment]]`, which is funcEnv — so **§10.2.11** FunctionDeclarationInstantiation **step 21.3.a** `CreateMutableBinding(paramName, false)` and its **steps 29.3.a.ii / 30.5.a.ii** `CreateMutableBinding(name, false)` — 29.3.a.ii as written, while the §10.2.11 sibling is **30.5.a.ii** and there is no 30.5.b, step 30.5 carrying a single substep `a` — install a **mutable shadowing** binding of the same name one record *inside* the immutable one, and a write resolves to that inner binding and never reaches the immutable one step 5 created (`webref body ecma262 sec-newfunctionenvironment` / `sec-functiondeclarationinstantiation`). Collapsing the two records makes both writes throw. Two **positive controls**, both green today and both structurally invisible to the four regressions above — ⚠ **and both write the self-name before reading it, so neither can observe the failure mode they exist to bound: a self-name store that clobbers the shadowing binding's *initial* value.** **Two conditions that do — ⚠ green guards at `658cc302`, not the red probes this cell called them**, which is §4 I-8's named inversion and contradicts this cell's own next sentence, that a bad remedy *fails* both. Measured at `658cc302`: `compiler/resolve.rs` allocates every `Param` binding first (`:207-226`) and then every non-param one (`:232-252`), each pass skipping a name already keyed `(scope_idx, name)` (`:222`, `:238`), and `scope/visitor.rs:458-463` puts the self-name in the *same* scope the params enter — so in the first the param takes slot 0 and the self-name is skipped, and in the second the self-name takes slot 0 and `var g` is skipped while `compiler/expr_function.rs:116-133` zeroes that slot. Both from §10.2.11 FunctionDeclarationInstantiation: `let f = function g(g){ return g }; f(7)` is `7` — step 21.3.a creates the parameter binding in the function environment, which shadows the self-name — and `let f = function g(){ var g; return g }; f()` is `undefined`, step 30.5.a.v initializing the `var` binding to `undefined` over the same name. A remedy that keeps the self-name where `scope/visitor.rs:458-463` already puts it, marks it immutable and stores the executing closure into that slot at frame entry passes both existing controls and fails both of these. ⚠ **And this cell argues only "slot rather than §5 slice", never the terminality criterion itself**, so it states it here: the four invariants it names are all properties of the one record umbrella `C`'s derivation mints, giving one mechanism and one PR, which is why it stays terminal rather than converting as `#11-vm-ordinary-function-name-length` did on a superficially identical four-across-three-layers count: `let f = function g(g){ g = 1; return g }; f(0) === 1` and `let f = function g(){ var g; g = 1; return g }; f() === 1`. **Owned by a slot rather than a §5 slice** because the arm emits an op, so §1.0's derivation does not produce it, and no §5 row owns function-expression naming (`git show 2039766a:docs/plans/2026-07-vm-p4-es-language-completeness.md \| grep -ci 'function expression'` → **0**, against **8** for `'static block'` and **38** for `'arrow'` by the same command on the same blob, so the probe discriminates). ⚠ **It consumes the child umbrella `C`'s derivation mints for the same artifact** — one of the slots the sweep recorded in Slice C's row returns, rather than a slot found on its own after the two before it. The four steps this row already cites *are* C's artifact: step 4's `NewDeclarativeEnvironment(outerEnv)` is a declarative environment record of exactly the kind C delivers, step 5's `CreateImmutableBinding(name, false)` an entry in it, step 8's `OrdinaryFunctionCreate(…, funcEnv, privateEnv)` a closure created **against** that record — a capture through it — and step 11's `InitializeBinding(name, closure)` its initialization (verified `webref aoid ecma262 InstantiateOrdinaryFunctionExpression` / `webref body ecma262 InstantiateOrdinaryFunctionExpression`). In-tree there is no runtime environment at all and the allocation is compile-time throughout, which is the gap C's row measures, so building "an environment reachable only from inside the body" before C exists is a second environment representation and a second capture mechanism C then has to replace. **Neither existing trigger supplies it**, for the reason the parameter-environment row states: the file-shaped trigger supplies nothing, and "with Slice **B**" is transitive — the child umbrella **B**'s derivation mints for the per-evaluation emit consumes C rather than producing it, and ⚠ **B itself does not: B is an umbrella from this revision and its `Deps` cell reads `—`**, so the C edge exists only once that child is minted, which is a mandatory output of B's derivation and not an authoritative dependency anyone can schedule from today | **after the child umbrella `C`'s derivation mints for the runtime declarative-environment and closure-capture representation**, then the next `compiler/expr_function.rs` + `scope/visitor.rs` PR, or with the child umbrella **B**'s derivation mints for the per-evaluation emit | 2026-09-30 |
| `#11-vm-private-name-early-errors` | **(carved this round)** a private reference is never validated against a declared private name, so `({}).#x` and `class C { m() { return this.#missing } }` reach lowering where ECMA-262 requires an early SyntaxError — including where the reference sits in code that never runs, which is the case no runtime diagnostic can reach. Three layers, each measured at `b5356098` (§2.2's `parser/primary.rs:200-203` row): `parse_member_prop`'s `PrivateIdentifier` arm returns `MemberProp::PrivateIdentifier` without consulting which private names are in scope; the parser's only private-name set is a `parse_class_body` local (`function.rs:370-371`) that `check_private_name_dup` (`:281-292`) fills from **declarations** for duplicate detection and that dies with the loop; and scope analysis carries no declared-private-name environment at all — `git grep -in 'private' b5356098 -- crates/script/elidex-js/src/scope/` returns three lines, all child traversals (`visitor.rs:278`, `:546`, `:550`), against **48** files for the same case-insensitive pattern over `crates/script/elidex-js/src/`. Governing: **§15.7.7** Static Semantics: AllPrivateIdentifiersValid, whose reference productions return false when `names` does not contain the StringValue and whose `ClassBody : ClassElementList` production supplies those names, checked as an early error by **§16.1.1** (`ScriptBody : StatementList`) and **§16.2.1.1** (`ModuleBody : ModuleItemList`) — **not** by §15.7.1, which does not mention the SDO. Deliverable is that environment — the private names each enclosing class body declares, carried to every reference site — plus the two early errors written over it, with regressions for a reference with no enclosing class, a reference to an undeclared name inside a class, and a reference in **unreached** code, the last being what separates an early error from a runtime throw. ⚠ **All three are rejections, so the set cannot fail on the implementation that rejects every `PrivateIdentifier`** — which passes all three while making `class C { #x; m() { return this.#x } }` a SyntaxError too, i.e. while the deliverable (the *environment*) is entirely absent. **A positive regression is therefore required, and it is what makes the environment the deliverable rather than a blanket reject**: a reference to a **declared** private name must stay accepted — `class C { #x; m() { return this.#x } }` parses, and so does a reference from a **nested** class body ⚠ **and only the two *outward* directions are stated — sibling and after-class — while §15.7.7's `ClassBody : ClassElementList` step 2 passes `newNames` to that ClassBody's subtree alone, so the inward direction is unasserted**: threading the enclosing names *plus* every class body nested inside the innermost enclosing one passes all the stated negatives. **Third red probe at `658cc302`**: `class C { m(){ return this.#y } n(){ return class D { #y; } } }` is a SyntaxError, `#y` being declared only by the nested body. ⚠ **And `parse_member_prop`, named here and at §2.2, does not exist** — `git grep -n 'fn parse_member_prop' 658cc302 -- crates/script/elidex-js/src` returns nothing; the arm at `parser/primary.rs:200-203` belongs to `try_parse_dot_property` (`parser/primary.rs:188`), through which all four member-family productions funnel, so they are one arm rather than four. ⚠ **Every case above is a `MemberExpression . PrivateIdentifier`, and §15.7.7 defines **five** reference productions, one of which is not a member expression at all.** Beside `MemberExpression . PrivateIdentifier`, `CallExpression . PrivateIdentifier`, `OptionalChain : ?. PrivateIdentifier` and `OptionalChain . PrivateIdentifier`, the SDO carries `RelationalExpression : PrivateIdentifier in ShiftExpression` (`webref body ecma262 sec-static-semantics-allprivateidentifiersvalid`) — the brand-check spelling `Op::PrivateIn` already exists for, reached from a parser path that is not the `parse_member_prop` arm this row measures. So threading the environment to that arm alone satisfies all of the cases above, positive and negative. Two more, one per polarity: `#x in ({})` at top level is a SyntaxError, and `class C { #x; static has(o) { return #x in o } }` parses. ⚠ **And every negative above names a private name declared *nowhere*, so none can fail on a name set that is never popped when a `ClassBody` closes** — a single accumulator filled from every declaration in the script rejects `({}).#x` and `#missing` exactly as a scoped environment does, and accepts both positive cases, while the deliverable (the *environment*) is only half present. §15.7.7's `ClassBody : ClassElementList` production makes the enlargement local: **step 2** is `Return AllPrivateIdentifiersValid of ClassElementList with argument newNames`, so the concatenated set is the argument to *that* ClassBody's subtree alone and reaches neither a sibling class nor the code after it (`webref body ecma262 sec-static-semantics-allprivateidentifiersvalid`). Two more negatives: `class C { #x; } class D { m(){ return this.#x } }` and `class C { #x; } ({}).#x` are both early SyntaxErrors. ⚠ **And a third acceptance route this row does not own: direct `eval`.** This document's own §16.1.1 reading records that direct eval is **exempt** from the empty-private-name ScriptBody check precisely because it inherits the caller's private environment, so `class C { #x; m() { return eval('this.#x') } }` must stay valid once core strict direct eval lands — and an implementation that rejects every private reference parsed outside a class body satisfies everything above while breaking it. **The private environment is an input the strict-direct-eval child consumes**, so placing that positive case on that child, and the environment handoff with it, is a mandatory output of the derivation the umbrella `#11-vm-function-constructor-global` opens; this row delivers the environment and does not own the eval route, since §15.7.7's `ClassBody : ClassElementList` production is `Let newNames be the list-concatenation of names and the PrivateBoundIdentifiers of ClassBody` (**§15.7.8**, `webref aoid ecma262 PrivateBoundIdentifiers`), so the valid set grows inward and a reject-everything arm is precisely what that concatenation forbids. **Carved rather than routed to Slice 0ba**: 0ba's contract is an arm that accepts a token the grammar cannot derive *in the context the arm was reached from*, and each of its fixes is a condition on an existing arm over state the arm already has — the `#x`-as-property-key row is answered by which of `parse_property_key`'s three callers is asking. A private *reference* is grammatical wherever it is written; what decides it is a set of names accumulated by enclosing class bodies, so the fix is a new carrier threaded through the parser or the scope pass, which is a representation change rather than a condition. **Nor to Slice 5**, whose charter is the compiler emit and VM dispatch of the private *operations* (`compiler/expr_class.rs`, `expr_member.rs`, `expr_ops.rs`, `expr_assign.rs:202-206`, `vm/dispatch.rs:1020-1026`): all of those can land while `({}).#x` still compiles, because nothing on that path asks whether the name was declared. **Owned by a slot rather than a §5 slice** because the defect is in `parser/` and `scope/`, which §1.0's derivation does not read | the next `parser/` + `scope/` PR, or with any child of umbrella **5** | 2026-09-30 |
| `#11-vm-explicit-resource-management` | **(carved at PR-B)** **Pointer into §5's row R**, which declares its own kind and is promoted out of this plan's sequence like Slice M. ⚠ *This cell restated that kind in the marker's own words, which put a §5 row's declaration inside a §8 cell and made the census count one umbrella twice; §5 says a pointer slot carries no marker of its own, and this is the second such slot — the other pointed at row E.* `using` / `await using` have no AST production, so they are parse errors, and the whole object surface is absent: `grep -rl <name> crates/script/elidex-js/src` → **0** files for `DisposableStack`, `AsyncDisposableStack` and `SuppressedError` (⚠ the two *syntax* needles that stood here, `UsingDeclaration` and `await using`, are withdrawn as non-discriminating: the negative control `grep -rl ConstDeclaration crates/script/elidex-js/src` is also **0** although `const` is fully implemented — the in-tree spelling is `VarKind::{Var, Let, Const}` at `ast.rs:139-143`, so a `using` implementation would read `VarKind::Using` and a JS two-word spelling never occurs as a Rust identifier at all; the executed parse-error probe below is what carries the absence claim); `typeof` on each of the last three, and on `Symbol.dispose`, → `"undefined"`. Spec surface, all numbers from `webref heading ecma262`: **§14.3.1** *Let, Const, Using, and Await Using Declarations* (declaration forms — there is **no** `#sec-using-declarations` anchor; `webref` rejects it), **§27.2** *Resource Management* (§27.2.1.1 Disposable / §27.2.1.2 AsyncDisposable interfaces), **§27.3** *DisposableStack Objects*, **§27.4** *AsyncDisposableStack Objects*, **§20.5.8** *SuppressedError Objects*. ⚠ §27.2 is Resource Management; **Promise Objects is §27.5** (the correction the `#11-vm-builtin-prototype-static-sweep` row records) — and `DisposableStack` / `AsyncDisposableStack` are §27.3 / §27.4, *not* under §27.2, so do not cite §27.2 for them. Edge-dense — parser productions × a scope-analysis binding kind carrying a disposal list × abrupt-completion disposal ordering against `try`/`finally`, `return` and `IteratorClose` ⇒ **plan-review MANDATORY** (CLAUDE.md rule, not judgment). Invisible to §2.2 by that section's stated scope: pass 3 walks `ExprKind`/`StmtKind` variants, and this syntax has none | a WPT/site depending on it, or the next `parser/` + `scope/` language-feature program | 2026-12-31 |
| `#11-vm-async-generators` | ⚠ **"= own slice" is stale, and this row is a **pointer**.** §5 already carries **6a** as an umbrella row rather than a terminal unit ("spans **five** intersecting axes"), with **6b** and **6c** as its terminal siblings, so the charter, the derivation, the split and every acceptance condition live there and this row schedules nothing of its own. ⚠ **And the only evidence this cell carries is 6b’s, not the async-generator object’s** — the `is_await: _` discard is §5 row **6b**’s measurement. What remains here: needs a new async-iterator opcode + `compiler/stmt.rs` await-flag plumbing (the `is_await: _` discard — `:83` at `658cc302` and at HEAD, `git grep -n 'is_await: _' 658cc302 -- crates/script/elidex-js/src` → **1** line; the `:103` this row used to carry predates 0a's `stmt.rs` split and was never re-derived) = own slice | now (Slices 6b/6c, and umbrella 6a's children as that derivation mints them) | 2026-10-31 |
| `#11-vm-builtin-prototype-static-sweep` | **UMBRELLA, not a terminal unit.** **Umbrella slot whose scope is a derivation rule, not a list of sub-slices** (§5), and the rule is **user-reachable** rather than global-rooted: for every intrinsic object a program can obtain — by reading a global property, by evaluating syntax that produces it, or by reading a property of a value it already holds — the whole property surface ECMA-262's main body defines on it, minus Annex B (I-6), minus any surface another slot already owns. The rule reads the spec clause, not the object's shape and not the route — see §5 for why the "constructor / prototype surface" and "reachable from the global object" wordings were each replaced. *(The earlier rationale "batched to avoid N micro-PRs" is withdrawn: batching those families into one PR is what the CLAUDE.md* Edge-dense work *rule forbids, and that rule says a plan-review does not substitute for the split.)* Pure builtin surface, no language-layer dependency. Charter is **edition-agnostic** — the unit is the *property surface ECMA-262's main body defines on a builtin object*, which is why the id's "prototype-static" wording is a name and not the rule — **minus Annex B** (§B.2.1 / §B.2.2, `Date.prototype.getYear`/`setYear`, `RegExp.prototype.compile`), which §4 I-6 forbids program-wide. *(Renamed from `#11-vm-es2021-2024-prototype-sweep`. The old id occurred **only in this document** — repo-wide `grep -rl 'es2021-2024'` returns this file alone, live ledger returns 0 — so the rename cost nothing, and it removes two ⚠ annotations that existed solely to tell readers the name lied about its own scope.)* **This row is the permanent home for the measurements below** ([§10](2026-07-vm-p4-umbrella-review-rounds.md)'s round record found the first three, and a round record owns nothing), each tagged with the sub-slice that discharges it: **(1) → the child of umbrella 9b that its §22.1.3 derivation mints for `replaceAll`**: `String.prototype.replaceAll` exists — `vm/natives_string_ext.rs:205` doc comment "ES2021 String.prototype.replaceAll(searchValue, replaceValue)", registered at `vm/globals_primitives.rs:62` `("replaceAll", native_string_replace_all)` — so this is a behavioural fix, not an absence: `'aabb'.replaceAll(/b/,'x')` → `'aabb'` where ECMA-262 **§22.1.3.20** step 3.b.iii throws a **TypeError**, and `'aabb'.replaceAll(/b/g,'x')` → `'aabb'` too, so the `g` path is silently wrong as well. **(2) → the child of umbrella 9c that its §20.1.2 derivation mints for the iterator drain** ([§10](2026-07-vm-p4-umbrella-review-rounds.md)'s own table already names it that way): `Object.fromEntries` — `native_object_from_entries` at `vm/natives_object/iteration.rs:157` requires `ObjectKind::Array` and otherwise raises `"Object.fromEntries requires an iterable"` (`:165` / `:181`), then index-walks the dense array; **§20.1.2.7** step 6 instead runs `AddEntriesFromIterable` (**§24.1.1.2**), the iterator protocol. Measured: `function* g(){ yield ['a',1] }; Object.fromEntries(g())` → `TypeError`. **(3) → the child of umbrella 9b that its §22.1.3 derivation mints for `matchAll`**: `String.prototype.matchAll` genuinely absent — `typeof 'ab'.matchAll` → `undefined`, and `grep -rn 'matchAll' crates/script/elidex-js/src` → **23** hits, every one ServiceWorker `clients.matchAll` / Cache `matchAll`. A parallel ES2019/ES2020 slot is **not** to be minted — that is the duplicated mechanism CLAUDE.md *One issue, one way* forbids. **(4) → an un-minted sub-slice, and the reason §5's rule was re-derived at PR-B**: `Math` is neither a constructor nor a prototype, so the previous wording could not produce it and it had no owner. Measured at `658cc302` (`crates/` is byte-identical at `b478b2b6` — `git diff --stat 658cc302 b478b2b6 -- crates/` is empty): `register_math_global` (`vm/globals.rs:1466`) installs **27** methods (`awk 'NR>=1468 && NR<=1494' crates/script/elidex-js/src/vm/globals.rs \| grep -c '^\s*("'`) while ECMA-262 **§21.3.2** *Function Properties of the Math Object* carries **37** (`.claude/tools/webref heading ecma262 21.3.2 \| grep -c 'Math\.'`); every installed name is a §21.3.2 entry, and `comm -23` over the two sorted name lists leaves **10** absent — `acosh` `asinh` `atanh` `cosh` `expm1` `f16round` `log1p` `sinh` `sumPrecise` `tanh`, each returning **0** files from `grep -rl '"<name>"' crates/script/elidex-js/src`. ⚠ **Both sides of that `comm` are §21.3.2, so the figure is a statement about `Math`'s *function* properties and not about `Math`.** `webref heading ecma262 21.3` returns **§21.3.1** *Value Properties of the Math Object* beside it — §21.3.1.1 `Math.E` through §21.3.1.8 `Math.SQRT2` **plus §21.3.1.9** `Math [ %Symbol.toStringTag% ]`, which is a member of the clause and not a value constant, so "`Math.E` through `Math.SQRT2`" understates the very clause this cell tells the child to re-run over — a clause no walk over §21.3.2's members reaches and one the installed-name list was never compared against, so this measurement cannot say whether `Math.PI` exists. Re-run over §21.3 whole before a child is minted from it; taken as the seed it is, a child ships the 10 functions with the value properties minted by nobody, which is the one-directional silent failure §5's head records. `JSON` (`register_json_global`, `vm/globals.rs:1520`) is the same shape and is likewise produced by the re-derived rule. The sub-slice is minted by running the derivation, not by this row. **(5) → an un-minted sub-slice, and the reason §5's rule was widened a second time**: the shared generator prototype is never a global property, so a rule rooted at the global object could not produce it either. Measured at `658cc302` (`git diff --stat 658cc302 909a77cb -- crates/` is empty, so the reading holds at this document's HEAD as well): `register_generator_prototype` (`vm/globals_async.rs:84-105`) builds the object with `create_object_with_methods(&[("next", …), ("return", …), ("throw", …)])` (`:90-94`) and one `[Symbol.iterator]` (`:96-104`), and defines nothing further — `create_object_with_methods` (`vm/globals.rs:1025-1037`) allocates an `Ordinary` object and calls `install_methods` with exactly the pairs it is given, so no `constructor` and no `%Symbol.toStringTag%` reach it. The sibling `Promise` registration in the same file **does** define `constructor` (`globals_async.rs:50-56`), so the absence is a property of this function and not of the file. ECMA-262 **§27.8.1** *The %GeneratorPrototype% Object* defines both beside the three methods — **§27.8.1.1** `%GeneratorPrototype%.constructor` and **§27.8.1.5** `%GeneratorPrototype% [ %Symbol.toStringTag% ]` (`webref body ecma262 sec-properties-of-generator-prototype`, `webref heading ecma262 27.8`). A program obtains the object only through syntax — `Object.getPrototypeOf((function*(){})())` — since there is no `GeneratorFunction` surface to read it off: `grep -rn 'GeneratorFunction' crates/script/elidex-js/src` returns exactly one line, a comment at `vm/tests/tests_typed_array_static.rs:344`, so the pattern does match in-tree text and still reaches no installation site. The function's own citations drifted with it: `:81` reads "Generator.prototype (§25.4.1)" and `:95` "spec §25.4.1.5", while `webref heading ecma262 25.4.1` is *Waiter Record*; the retag belongs to whichever sub-slice the derivation mints for this object. **Also owned here, under umbrella 9b**, whose derivation places it on whichever child it mints lands first with the file in its touch set: the `vm/natives_string.rs` §21.1.3 → §22.1.3 citation retag, moved off `#11-vm-webidl-section-3-10-retag` because that slot's WebIDL class and this ECMA one fire independently and one retirable id cannot record half a discharge. `git show 658cc302:crates/script/elidex-js/src/vm/natives_string.rs \| grep -c '§21\.1\.3'` → **8**, and **8** at HEAD `6edda6f2`; `webref heading ecma262 21.1.3` is "Properties of the Number Prototype Object" and `22.1.3` is "Properties of the String Prototype Object". Re-derive by grep at slice time rather than reading the figure forward | any sub-slice the derivation mints (§5 carries them), or a WPT/site needing `at`/`findLast`/`hasOwn`/`fromEntries` on a non-Array iterable/`matchAll`/`withResolvers` | 2026-10-31 |
| `#11-vm-dynamic-import` | the T1 carve out of the ES-modules program: 0ca makes `import()` loud immediately, the Promise-returning implementation needs the module loader | the child of umbrella M that owns `import()`'s Promise-returning implementation | 2026-10-31 |
| `#11-vm-topropertykey-symbol-from-toprimitive` | **(carved by #489's converge)** ⚠ **The direction of this sentence was inverted, and is corrected here**: **§7.1.21** ToPropertyKey step 1 is `Let key be ? ToPrimitive(arg, string)` and step 2 is "If **key** is a Symbol, then Return key" — the *spec* tests the **result** (§-number from `webref aoid ecma262 ToPropertyKey`, steps from `webref body ecma262 sec-topropertykey`); it is **elidex** that tests the *argument* for Symbol-ness and sends everything else to `to_string`, so an `@@toPrimitive`-returns-Symbol key throws. As written the row asserted the defect was ECMA-262's. Open-coded **8 times** (2 named helpers + 6 inline) and `get_element`/`set_element` are **not** `make_property_key` callers ⇒ the unit is "collapse the 8, then fix", not a helper patch. ⚠ **And that last clause is a scope claim, not an acceptance — a patch to the two named helpers satisfies every word of it**, which is why the conditions below are stated **per consumer class** rather than over the collapse. The witness is one object `k` whose `@@toPrimitive` returns a Symbol `s`, and each condition asserts that the entry point keys on `s`. **Red probes at `658cc302`, one per class**, the classes read off the call graph rather than off a member list (`git grep -n 'make_property_key' 658cc302 -- crates/script/elidex-js/src` → one definition at `vm/ops.rs:422` and **6** call sites): the plain read and the plain write, which are *not* among those six (`o[s]=1; o[k] === 1` and `o[k]=2; o[s] === 2`); the compound-assign element path (`vm/ops_element.rs:183`); `delete o[k]` (`vm/dispatch.rs:651`); `k in o` (`vm/dispatch_objects.rs:419`); the computed object-literal key and the computed store (`:48` / `:80`); the computed accessor, `({ get [k](){ return 1 } })[s] === 1` (`:232`); `Object.defineProperty(o, k, {value: 3})` then `o[s] === 3` (`vm/natives_object/mod.rs:28`); and a primitive receiver, `"str"[k]`. **That set is what separates the collapse from the helper patch**: patching `make_property_key` and `natives_object::to_property_key` alone turns the `delete` / `in` / computed-literal / computed-accessor / compound-assign / `defineProperty` probes green while the plain read, the plain write and the primitive receiver stay red. **Declared green guard on the remedy**: a key object whose `@@toPrimitive` returns the String `"x"` still keys on `"x"` and not on the object — green today, and what bounds a fix that stops performing step 1 at all. ⚠ **The eight sites are the seed and not the inventory** — the population is re-derived from the call graph at this slot's own plan-review, and a coercion path absent from the eight is un-minted rather than out of scope | the next property-key coercion PR, or with `#11-vm-proxy-reflect` | 2026-09-30 |
| `#11-vm-operand-rooting-by-construction` | **(carved by #489's converge; SUPERSEDES `#11-vm-element-access-base-rooting`, which must not be recorded as closed)** ~20 dispatch arms pop an operand into a Rust local and then run user JS before reading or storing through it; `gc/roots.rs` walks the VM stack but not Rust locals. Beyond the 5 element opcodes: `GetProp`/`SetProp`, `IncProp`/`DecProp`, `In`, `Add`, `Instanceof`, `TemplateConcat`, `ops.rs`'s three operator helpers, the three computed-key definition bodies, `SpreadObject`, `ArraySpread`, `IteratorRest`, the unary arms and `op_get_iterator` — two of them *panic*, two *store* the collected id. **Deliverable is an invariant making an unrooted hold unrepresentable, NOT a sixth sweep**: five successive audits each drew the boundary differently and each was falsified by the next round. `Op::GetElemRef`, the one arm #489 introduces, is rooted by construction and pinned. Implementation + a 14-arm test module preserved on branch — ⚠ **an artifact, not a condition anyone can read at the pinned tree, and nothing stated distinguishes the deliverable from converting the arms by hand with no type-level enforcement, which is the sixth sweep this cell rejects in prose.** **The condition is stated over representability**, red at `658cc302`: an arm that holds an operand across a call reaching user code does not compile unless the hold is expressed through the guard — asserted by a compile-fail test on a deliberately unsafe arm, paired with a **declared positive control** that a conforming arm still compiles, so a remedy that only converts the twenty known sites fails the first. ⚠ **And this cell answers only the first disjunct of §5's criterion while its own evidence is a verbatim instance of the second**: "five successive audits each drew the boundary differently" is a subsystem with no canonical algorithm. It stays terminal not because that disjunct fails but because the cell's own argument is that *any* partition leaves the invariant unenforced — a row that cannot be split without defeating its deliverable is terminal by necessity, and that is the sentence the criterion was owed `vm-p4-rooting-carved` | **now** — the next VM dispatch-loop PR, or any of Slices Pa/Pb/Pc, or any child of umbrella **Pd** (which ships no PR of its own, so it cannot be the trigger). **Terminal, and the *edge-dense* verdict this cell carried is withdrawn as mis-applied**: an invariant is established only in the commit after which an unrooted hold no longer compiles, so every partition of this row lands children that leave it unenforced — the sixth sweep the *Why* cell rejects, drawn over the same arm boundary five audits already drew five ways. It is a **prerequisite** row in §5's sense, and its `/elidex-plan-review` is the one §5's opening line puts on every slice. ⚠ **Reversed only by a finding that the hold can be made unrepresentable per holder** — under that finding the row is an umbrella and both consequences return | 2026-09-30 |
| `#11-vm-internal-error-hard-exit` | **(carved by #489's converge)** ⚠ **"every extracted `op_*` helper's dispatch arm" is a universal this row never measured, and the complement refutes it.** Re-derived at `658cc302` over the **32** `self.op_` call sites in `vm/dispatch.rs` (`git show 658cc302:crates/script/elidex-js/src/vm/dispatch.rs \| grep -c 'self\.op_'`): **12** hand a returned `Err` to `throw_error` (`:228`, `:331`, `:339`, `:465`, `:581`, `:683`, `:688`, `:696`, `:895`, `:900`, `:905`, `:910`) and `Op::ThrowUnsupported` at `:772` routes a constructed `VmError` the same way — **13**; **15** propagate with `?`; and `op_create_object` (`:667`), `op_create_array` (`:679`) and `op_new_target` (`:1061`) are infallible while `op_end_finally` (`:802`) returns an action enum rather than a `Result`. ⚠ **The contrast drawn against "inline arms" is wrong on its own example**: `Op::PopUnder` is **extracted** — `:124` is `self.op_pop_under()?;`, body in `dispatch_helpers.rs` — so it sits on the `?` side *while being extracted*, which is exactly why the disposition may not key on extraction. So the defect is that **13 of 32** arms turn a broken VM invariant into a catchable JS `Error` (`throw_error` hands it to `handle_exception` first, `vm/ops.rs:155-166`) and user `try`/`catch` can swallow it. The disposition must key on the error's *kind*, not on whether the body was extracted. `Op::ThrowUnsupported` must stay catchable — it reports an unimplemented construct, not a broken invariant. ⚠ **Both of those sentences are normative and neither is falsifiable as written**: adding the kind test at each of the 13 call sites individually satisfies them while leaving the next extracted helper to re-open the decision. **Red probe, by fault injection, at `658cc302`**: a Rust-side harness that forces `VmError::internal` out of one of the 13 extracted helpers and evaluates `var caught = false; try { <the construct that arm lowers> } catch (e) { caught = true }` must leave `caught` **false** and must return `Err` from `Vm::run` carrying `VmErrorKind::InternalError` — red today, for the `handle_exception` reason above. **Declared positive control, green today and green after**: `Op::ThrowUnsupported` stays catchable — `try { <an unlowered construct> } catch (e) { e instanceof TypeError }` is `true`, since `op_throw_unsupported` (`vm/dispatch_helpers.rs:381-390`) builds a `TypeError` and `:772` routes it through the same `throw_error`. That control is what bounds the over-broad fix keying on "the arm returned `Err`". **Declared green guard**: the **15** arms already propagating with `?` — `Op::Swap`'s inline `return Err(VmError::internal("stack underflow on Swap"))` at `:118` among them — keep the same uncaught result, so the remedy may not route them *back* through `throw_error` to make the kind test uniform. **And the structural condition that makes a per-arm sweep unnecessary**: the kind test occurs at **exactly one** site, `throw_error` itself, so an arm added later cannot re-open it — the One-issue-one-way form of "not on whether the body was extracted" | with `#11-vm-operand-rooting-by-construction`, or the next dispatch-loop PR | 2026-09-30 |
| `#11-vm-delete-elem-raw-key-array-fast-path` | **(carved by #489's converge)** `Op::DeleteElem` derives its array-index fast path from the **raw** operand, so an object key that stringifies to an index skips it while the generic `try_delete_property` path never consults dense array storage: `var a=[1,2,3]; delete a[{toString(){return '0'}}]` reports `true` and leaves `a[0]` as `1`. Same root as `#11-vm-topropertykey-symbol-from-toprimitive` — a fast path keyed on the raw value rather than the `ToPropertyKey` result. ⚠ **Deriving `arr_idx` from the `ToPropertyKey` result and keeping the dense short-circuit passes the probe above and is still not the acceptance**, because the fast path also skips the configurability check: §13.5.1.2 step 4.e is `Let deleteStatus be ? baseObj.[[Delete]](ref.[[ReferencedName]])` → **§10.1.10.1** OrdinaryDelete step 4 "Return false" for a non-configurable property, and step 4.f then throws a **TypeError**, always reached since elidex core is strict-only. The fast path (`vm/dispatch.rs:636-648`) gates on `idx < elements.len() && !elements[idx].is_empty()`, writes `JsValue::Empty` and pushes `true` unconditionally; freeze cannot rescue it, since `lock_and_collect_keys` (`vm/natives_object/integrity.rs:8-17`) walks `obj.storage.iter_keys(…)` while dense elements live in `ObjectKind::Array { elements }`, so `Object.freeze` never marks them non-configurable. **Separating observable**: `const a = Object.freeze([1,2,3]); delete a[0];` ⚠ **which separates the configurability limb alone, while this row then demotes its raw-key probe out of acceptance — so the headline defect has no condition at all.** **The primary condition, red at `658cc302`**: `var a=[1,2,3]; delete a[{toString(){ return '0' }}]` deletes element 0, so `0 in a` is `false` afterwards — a fast path deriving `arr_idx` from the raw operand (`vm/dispatch.rs:630-648`) reports `true` and leaves `a[0]` at `1`, and it passes the configurability condition untouched. Both are red probes; the freeze case is additionally the **declared control** bounding a fix that derives the key correctly and then ignores `[[Configurable]]` must throw `TypeError` and leave `a[0] === 1` — ⚠ **and that witness reads a producer this slot does not own, which is §4 I-8's second half.** This same cell measures that `lock_and_collect_keys` walks `obj.storage.iter_keys(…)` while dense elements live in `ObjectKind::Array { elements }`, so **§20.1.2.6** `Object.freeze` → **§7.3.15** SetIntegrityLevel step 3's `[[OwnPropertyKeys]]` walk never marks index `0` non-configurable in tree: **no change to `Op::DeleteElem` can turn this condition green**, and the trigger beside this cell named only the property-key slot, so the integrity gap had no owner and this slot could retire on a probe that cannot go green from its own deliverable. The producer is the child umbrella `#11-vm-builtin-prototype-static-sweep` mints for §20.1.2's integrity statics — its rule reaches the whole property surface ECMA-262's main body defines on the `Object` constructor, and that row's measurement **(1)** already records a behavioural fix to an *existing* method as inside it — so this slot records that derivation and that child rather than a blank, and the condition above is a **red probe** only once both have landed | with that slot, **and** umbrella `#11-vm-builtin-prototype-static-sweep`'s derivation having run and produced the child that owns §20.1.2.6 / §7.3.15 over dense array elements | 2026-09-30 |
| `#11-vm-yield-delegation-lowering` | **(carved at PR-B, Codex R3/R4)** `expr_yield_star.rs:146`/`:157` emit a plain `Op::IteratorClose` for outer `.throw(e)` / `.return(v)` injected during `yield*` delegation, and the compiler's own docstring admits it reduces both protocols to that. **ECMA-262 §15.5.5 steps 8.b/8.c** instead invoke the *inner* iterator's corresponding method **with the injected value**, validate the result, and **continue delegating when `done` is false** — which no `IteratorClose` operand or split opcode can express. Reclassified out of Slice P ([§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md)), so without this row the two known-broken generator paths would be permanently unowned. Deliverable is a yield-delegation lowering, not a precedence sweep — a **new lowering** for §15.5.5's injected-value resume protocol, reaching the compiler emit, the opcode/transport, the generator-resume path and both injection protocols — which are the stages one lowering passes through and not the axis count. **The kind is the first disjunct — **UMBRELLA, not a terminal unit.**** Its derivation's seed is §15.5.5's own `genKind` branch, which is where the artifacts this row consumes stop being shared: the sync spelling needs the completion-kind parameter and nothing else; the async spelling additionally resumes through CreateAsyncFromSyncIterator and settles through CreateResolvingFunctions. ⚠ **And the async spelling of that production delegates *through* the sync→async adapter rather than around it, which is why the ordering opposite gains 6b.** §15.5.5 writes one `YieldExpression : yield * AssignmentExpression` production for both generator kinds: step 1 is `Let genKind be GetGeneratorKind()`, step 2 asserts it is sync or async, and step 5 is `Let iteratorRecord be ? GetIterator(value, genKind)`. **§7.4.4** GetIterator's async branch then routes an object with no `%Symbol.asyncIterator%` through `GetIteratorFromMethod(obj, syncMethod)` (step 1.b.iii) and `CreateAsyncFromSyncIterator(syncIteratorRecord)` (step 1.b.iv) — **§27.1.5.1**'s adapter, which Slice **6b**'s row owns and which nothing on the `yield*` path constructs a second time (§-numbers from `webref aoid ecma262 GetGeneratorKind` → §27.8.3.5 and `GetIterator` → §7.4.4; steps from `webref body ecma262 sec-generator-function-definitions-runtime-semantics-evaluation` / `sec-getiterator`). So `async function* g(){ yield* [1,2] }` resumes its delegate through that adapter, and a lowering written ahead of 6b either handles an already-async delegate only or re-derives the adapter beside it. **The measurement: three axes, each red while the other two are green.** (i) **§15.5.5 steps 8.b/8.c's injected-value resume protocol** — `expr_yield_star.rs:146`/`:157` emit a plain `Op::IteratorClose` instead of invoking the inner iterator's method with the injected value, validating the result and continuing to delegate when `done` is false. (ii) **The completion kind the close carries** — step 8.b.iii.2 makes `closeCompletion` a *normal* completion and step 8.b.iii.4 closes on it, so a lowering that gets (i) exactly right and re-emits an operandless close still cannot say which completion it carries; that is the parameter Pa puts on `iter_close`, red while (i) and (iii) are green. (iii) **The async spelling's route through §27.1.5.1** — §7.4.4's async branch sends a delegate with no `%Symbol.asyncIterator%` through CreateAsyncFromSyncIterator, so `async function* g(){ yield* [1,2] }` is red on a lowering complete in both sync respects. A fourth this derivation mints rather than counts: settlement through §27.5.1.3 CreateResolvingFunctions at the `? Await(` sites measured in the *Trigger* cell. The split stays §15.5.5's `genKind` branch, which is where the artifacts stop being shared. The second disjunct is unavailable: §15.5.5's steps are taken from `webref body ecma262 sec-generator-function-definitions-runtime-semantics-evaluation` and §7.4.4 from `webref aoid ecma262 GetIterator`, so the AOs are in the specification. | **the derivation that mints this row's children**, fired by the first of Slices Pa / 6b and of the children umbrella **9da**'s derivation mints to land, or by the next generator PR that reaches `expr_yield_star`. Each edge below lands on the child that consumes the artifact, never on this row, which ships no PR: **Pa** on the child lowering the sync `throw` / `return` injection, which consumes the completion-kind parameter Pa puts on `iter_close`; **6b** and the child umbrella **9da**'s derivation mints for CreateResolvingFunctions on the child lowering the async spelling, which resumes through CreateAsyncFromSyncIterator and settles through CreateResolvingFunctions; the child of umbrella **6a** that owns generator execution and settlement on whichever child ships the resume transport, the two sharing the `expr_yield_star` / generator-resume surface. Ordering the whole row behind all three, as this cell did, over-orders the sync child on artifacts it never reads. **Pa first because §15.5.5's `throw` branch closes on a *normal* completion**: when the delegate has no `throw` method, step 8.b.iii.2 is `Let closeCompletion be NormalCompletion(empty)`, step 8.b.iii.4 is `Else, perform ? IteratorClose(iteratorRecord, closeCompletion)` (step 8.b.iii.3 takes `AsyncIteratorClose` when genKind is async) and only step 8.b.iii.6 is `Throw a TypeError exception` (`webref body ecma262 sec-generator-function-definitions-runtime-semantics-evaluation`). The lowering that replaces the two plain closes therefore consumes the completion-kind parameter Pa puts on `iter_close` — without it the replacement re-emits a close that cannot say which completion it carries, which is the contract Pa exists to fix. **6b because the async spelling of the same production delegates through §27.1.5.1's sync→async adapter, which is that row's deliverable — the derivation is in this row's *Why* cell**. ⚠ **9da was measured while landing the 6b edge and is added with it**, because the same async spelling settles through the resolving functions: the one `yield *` production performs `If genKind is async, set innerResult to ? Await(innerResult)` at **4 sites** (`webref body ecma262 sec-generator-function-definitions-runtime-semantics-evaluation \| grep -c '? Await('` → **4**; negative control `\| grep -c 'Await(innerNext)'` on the same output → **0**. ⚠ `\| grep -c 'genKind is async'` is **not** the enumerator — it returns **8**, adding three `AsyncGeneratorYield` lines and one `AsyncIteratorClose` line, and only **2** of the four carry the spelling quoted here; the other two read `Set receivedValue to ? Await(receivedValue)` and `set innerReturnResult to ? Await(innerReturnResult)`), and **§27.10.5.3** `Await` step 2 is `PromiseResolve(%Promise%, arg)` → **§27.5.4.7.1** steps 2/3 `NewPromiseCapability` + `Call(promiseCapability.[[Resolve]])` → **§27.5.1.3** CreateResolvingFunctions, which is the deliverable of the child umbrella **9da** mints for CreateResolvingFunctions — the same route by which the async-disposal children reach it | 2026-10-31 |
| `#11-vm-typed-array-family-layering-and-gate` | **UMBRELLA, not a terminal unit.** ⚠ **This slot read as terminal, and its own concession that "Neither discharge, taken alone, closes the gate half this row also measured" is what ends that**: it carries two deliverables whose remedies do not overlap, plus a third stated over variant existence. §5's head reads a slot declaring ≥3 intersecting invariant axes as an umbrella row from the moment it says so, and this cell declares three: the **layering-mandate amendment at its SSoT** together with the two restatements that must move with it; **build-configuration completeness** of §19.3's twelve typed-array constructors under a non-`engine` build; and the **`ObjectKind` cfg asymmetry** — `TypedArray`/`DataView` un-gated against a gated `ArrayBuffer` — which §4 I-9 makes a per-variant question rather than a mechanical one. A relocation discharges the first and leaves the second and third untouched; a uniform gating discharges the third and leaves the ES-completeness hole this cell inventories. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. **(carved at PR-B by this document's own plan-review)** two normative sources disagree about the same files. `vm/host/mod.rs:11-13` states that everything under `vm/host/` is restricted to **engine-bound** responsibilities (prototype install / brand check / marshalling); §4 I-5's Converse declares `vm/host/typed_array_*.rs` — ECMA-262 §23.2, a pure language built-in — to be a **language-semantics site the core convention owns**. Both are normative and they cannot both hold, and there is no relocation slot. The measurable consequence the umbrella never inventoried: the whole family is `#[cfg(feature = "engine")]` (`vm/host/mod.rs:339-352` for the seven `typed_array*` modules, plus `array_buffer` `:75` and `data_view` `:102`), so a **non-`engine` core build has no `ArrayBuffer` / `TypedArray` / `DataView` constructors** — and the gating is not uniform: `ObjectKind::ArrayBuffer` is gated while `ObjectKind::TypedArray` and `ObjectKind::DataView` are **not**, so such a build carries a `TypedArray { buffer_id, … }` variant pointing at one that does not exist in that configuration. An ES-completeness hole absent from §2.2, §2.3, §3 and the §5 slice table, in an umbrella named for ES language completeness. Deliverable is a decision, not a doc edit — and **the admissible set is the one this row states below, not three alternatives**: ungating and a `vm/host/mod.rs`-local exemption are listed nowhere as discharges, because neither moves the responsibility | ⚠ **prerequisite of Slice Pa (Codex R4; narrowed from "Slice P" at PR-B)** — Pa must edit `vm/host/typed_array_static.rs:798`, an `iter_close` caller, so under the documented ship order Pa would have to either change pure ECMA-262 behaviour inside a directory whose mandate forbids it, or omit a required site and ship an incomplete convention sweep that the children umbrella **0bc** mints then depend on. **Pa, and the scope is stated from touch sets, not from a chain** — the `Pc → Pa` edge that used to put Pc after this gate was deleted at `b3eb07c1` under §5's edge rule, so no ordering does. **Pa** edits `vm/host/typed_array_static.rs`, a file of the very family this row is about ⇒ gated. **Pb** reaches no `vm/host/` file ⇒ not gated. **Pc** reaches three (`url_search_params.rs`, `headers/parse_init.rs`, `structured_clone.rs`), but those implement WebIDL §3.2.21.1 and WHATWG HTML §2.7.4/§2.7.6/§2.7.7 — engine-**bound** responsibilities, which is what `vm/host/mod.rs:11-13` mandates that directory hold — so they are not the pure-ECMA-262-inside-`vm/host/` conflict this row exists to settle ⇒ not gated. ⚠ **"engine-bound" is this exclusion's conclusion, not its warrant, and stating only the conclusion is what let the question be re-opened at row **F** on a new argument — that a ~900-line *algorithm* is not marshalling however engine-bound its subject matter is.** `vm/host/mod.rs:11-13` admits three named categories and a fused clone algorithm is literally none of the three, so a reader taking those three words as an exhaustive admission list reaches the opposite verdict from this cell. The warrant is stated **once**, in `#11-vm-webidl-sequence-early-exit-drain`'s structured-clone half below — this document's single home for these files' layering disposition — and this cell asserts only that its exclusion follows from it, and that exclusion no longer rests on Pc being downstream of Pa. My earlier trigger ("Slice 7, or the next `vm/host/` layering pass") put the decision *after* the slice that needs it. ⚠ **Two of those three are not discharges, and listing them as alternatives let the slot retire without resolving anything.** *Ungating* changes which builds compile the family; it leaves a responsibility this row itself classifies as **pure ECMAScript language semantics** sitting inside a directory `vm/host/mod.rs:11-13` restricts to engine-bound work — the conflict is untouched. A *file-local* exemption is the same move written down: a local override of a root-level mandate, which is what CLAUDE.md's layering rule exists to forbid. The admissible discharges are exactly **(a) relocation of the family to the core `vm/` layer**, or **(b) an explicit amendment of the mandate at its **SSoT**, which is the repository's root `CLAUDE.md` § *Layering mandate* — ⚠ **not** `vm/host/mod.rs:11-13` or §4 I-5's Converse, which merely restate it; amending only those two leaves the root restriction on `vm/host/` standing and discharges nothing — with the module comment and §4 brought into line afterwards so that one statement stands. ⚠ **Neither discharge, taken alone, closes the gate half this row also measured, so the gate observable is required of whichever is chosen**: a `git mv` of the seven `typed_array*` modules plus `array_buffer` / `data_view` into `vm/` that carries every `#[cfg(feature = "engine")]` across verbatim satisfies (a) exactly as written and moves the responsibility, while a non-`engine` build still exposes **none** of ECMA-262 **§19.3** *Constructor Properties of the Global Object* (`webref heading ecma262 19.3`: §19.3.3 `ArrayBuffer`, §19.3.9 `DataView`, §19.3.19–§19.3.21 `Int8Array` / `Int16Array` / `Int32Array`, §19.3.38–§19.3.41 `Uint8Array` / `Uint8ClampedArray` / `Uint16Array` / `Uint32Array`, §19.3.15–§19.3.17 `Float16Array` / `Float32Array` / `Float64Array` — `Float16Array` carved out of this enumeration to `#11-vm-float16array-global` below, so this cell no longer inventories it — §19.3.6 / §19.3.7 `BigInt64Array` / `BigUint64Array`) — the ES-completeness hole this cell inventories — and the `ObjectKind` asymmetry is untouched by either. **Separating observables**: in a non-`engine` build `typeof ArrayBuffer`, `typeof DataView` and `typeof Int32Array` are each `"function"`; and no configuration carries a `TypedArray { buffer_id, … }` or `DataView { … }` variant (`vm/object_kind.rs:677` / `:694`, uncfg'd) without the `ArrayBuffer` variant they point at (`:420-421`, cfg'd). ⚠ **The first of those is unrunnable, because the non-`engine` configuration does not compile — and repairing it is not this row's, nor a deliverable of either discharge: it already has an owner.** `crates/script/elidex-js/Cargo.toml:14` is `default = []`, and at `907f3135` `cargo check -p elidex-js` on default features fails with **16** errors across **4** files which partition into three groups, **none** of them a typed-array module: **3** unresolved crates (`elidex_plugin` `vm/init.rs:11`, `elidex_api_observers` `vm/lifecycle.rs:45`, `elidex_ecs` `vm/gc/collect.rs:47`); **12** `HostData`-shape errors, all in `vm/lifecycle.rs` — 2 × `no method named dom` and 10 × `no field …` over **9** distinct names (`live_range_registry`, `node_iterator_states_shared` ×2, `tree_walker_states`, `range_instances`, `tree_walker_instances`, `node_iterator_instances`, `selection_state`, `selection_instance`, `selectionchange_pending`); and **1** intra-crate `cannot find function is_constructor in module super::object_kind` (`vm/dispatch.rs:998`). `--no-default-features` fails identically, which is why every JS probe this document records was built with `--features engine`. ⚠ **The five error kinds listed here before omitted `elidex_api_observers`, so the enumeration summed to 15 against a stated 16** — the partition above is re-derived from the compiler's output rather than read forward. So this cell's layering half is reasoning about a configuration that does not build, and its gate half's first observable is unrunnable there. **Neither is repaired by anything this row can choose**: the partition above touches no `typed_array*` / `array_buffer` / `data_view` module, so relocating the family (discharge (a)) or amending the mandate at its SSoT (discharge (b)) leaves all **16** exactly where they are. ⚠ **"Restoring it is a deliverable of whichever discharge is chosen, not a precondition someone else supplies" stood here, and it is wrong on both halves**: it is a precondition, and someone else already supplies it. `#11-elidex-js-no-default-features-support` is an open ledger slot carved at #376 (A1, Codex R11 finding-2) over these same errors — it names `object_kind::is_constructor` and `HostData::{dom, live_range_registry, selection_*, …}` and, in its own DX note, the unresolved `elidex_plugin` / `elidex_ecs` / `elidex_api_observers` crates — states the fork it must settle (make `engine` default, versus fully cfg-gate the engine code so parser-only is genuinely supported), and triggers on the S5 boa→VM cutover or the first parser-only consumer. §1.0's disposition for a defect that already has such a slot is to **point at it**, so this correction mints no row and leaves this document's slot-bearing count untouched. ⚠ **Observable (i) is therefore restated as conditional on that slot rather than as a deliverable of this one**: *once the non-`engine` configuration builds*, `typeof ArrayBuffer`, `typeof DataView` and `typeof Int32Array` are each `"function"` in it. Until then observable (ii) is this row's only runnable one, and it is runnable today because it is read from `vm/object_kind.rs` rather than from a build. Settle **before Pa**, since Pa modifies `vm/host/typed_array_static.rs` and would otherwise edit the file while the question of whether it belongs there is open; the child of umbrella **7b** that carries the `vm/gc/` tracing work remains the fallback owner if the P family is resequenced — of umbrella 7's children that is the one I-5's named consequence puts on the `engine` gate (`vm/gc/trace.rs` beside engine-gated wrapper-store rooting, with identical semantics required for non-`engine` builds), which is the facet this row's gating half turns on — ⚠ **and this row is an umbrella, so the occasion fires its *derivation*, never a PR that would carry the work**: that derivation mints the terminal children, each its own PR under its own mandatory `/elidex-plan-review`, and re-points every ordering stated above onto the specific child that consumes it | 2026-10-31 |
| `#11-vm-inbound-host-call-membership-test` | **(carved at PR-B)** §4 I-5's **inbound** rule — no slice may pull engine-bound responsibility into core — has **no membership test**, while core dispatch already carries host knowledge. Measured at `658cc302` (the measurement §4 I-5 states; not re-measured here): `vm/dispatch_objects.rs:404`/`:415` (the `in` operator special-casing `DOMStringMap` / `Storage`) and `vm/dispatch_iter.rs:121`/`:132` (for-in named-property exotic keys) call `host::dataset::*` / `host::storage::*` from core. I-5's consequence #2 then directs the children of umbrella **10a** (its `Reflect` half) and the children of umbrella **10b** (its `Proxy` half) to resolve `Proxy`/`Reflect` × `ObjectKind::HostObject` *inside* core dispatch, i.e. to add more — which is why the test cannot sit on either consumer. This slot owns **(a)** the membership test ⚠ **and stated no condition over either deliverable, so a PR adding one permissive sentence to the layering SSoT and classifying all four measured sites as conforming discharges it while admitting every future call by construction — which is the outcome this cell exists to prevent.** **The condition is stated over the test's *discrimination*, red at `658cc302`**: the adopted membership test, applied to the four sites `vm/dispatch_objects.rs:404`/`:415` and `vm/dispatch_iter.rs:121`/`:132`, returns a verdict for each **and** rejects at least one constructed call that a literal reading of I-5's inbound rule would admit — a test that admits every one of the four and every probe put to it has recorded a permission, not a boundary. **Declared positive control**: a call from core into `vm/host/` that both umbrella 10a's and umbrella 10b's children will need — resolving `ObjectKind::HostObject` inside core dispatch — is classified by the same test rather than exempted, which is the whole reason both inherit it. ⚠ **And its terminality**: this row stays terminal because its deliverable is a single boundary rule shipped as one documentation PR at CLAUDE.md's SSoT, not because it is small — §5's three kinds are read off what a row *ships*, and a decision that ships one PR is a terminal row and not an umbrella for what core `vm/` may call in `vm/host/` — the inbound half of the direction whose **outbound** half already has a Converse, which exists precisely because a literal reading stops at the wrong place — and **(b)** the audit and classification of those four sites under whatever test it adopts. **Slot rather than a slice row, and the precedent is `#11-vm-typed-array-family-layering-and-gate` above**: the deliverable is a decision, not a PR, and each consumer reaches it through the edge its umbrella's derivation places on the children that resolve `ObjectKind::HostObject`, no umbrella being nameable in a `Deps` cell (§5). **Not settleable per surface**: umbrella 10a's children and umbrella 10b's children carry no edge between them, so their mandatory plan-reviews may run concurrently — "whichever runs first" orders nothing and both could classify the same four host calls differently — and a core/engine boundary is project-wide rather than a per-surface choice | **now** — settled **before any child of umbrella 10a starts, and before any child of umbrella 10b starts**, since neither row can carry it in a `Deps` cell (§5) and placing it on the children that resolve `ObjectKind::HostObject` inside core dispatch is a mandatory output of each umbrella's derivation, and no edge orders them; or the next core `vm/dispatch*` PR that adds a `host::` call | 2026-10-31 |
| `#11-vm-declarative-environment-and-capture-representation` | **The work item of the child umbrella C mints for the runtime declarative-environment and binding-cell representation, not a defer** — kept here so the representation has a ledger home until it lands. ⚠ **This row owned the representation as a *slot* until this revision, and no longer does.** A slot settles a **decision** and ships no PR — which is why `#11-vm-inbound-host-call-membership-test` and the typed-array layering gate are slots — while the runtime declarative-environment / binding-cell representation and its closure-capture integration in `compiler/resolve.rs` are **code**: with no row producing them, the children umbrella **B** mints and Slice Ea can each still build one, which is the two-mechanism outcome the carve existed to prevent. §5's **Slice C** row is the single home for the charter, for the derivation that places each acceptance condition on the child it mints, and for every measurement this row carried (**§14.2.2** step 2's `NewDeclarativeEnvironment(oldEnv)`, **§14.7.4.4** CreatePerIterationEnvironment step 1.d and steps 1.e.ii-1.e.iii, `compiler/resolve.rs:31` and `:148-157` at `b5356098`, and the discriminating case `for (let i=0;i<2;i++) { let x=i; fs.push(() => [i,x]) }`); this row is a pointer | now — umbrella **C**'s derivation, which mints the child that owns the representation | 2026-10-31 |
| `#11-vm-statement-completion-updateempty` | **(carved by #489's converge)** the half of the completion-ownership bug 0a did not fix: no `UpdateEmpty` (AO §6.2.4.4) equivalent, so `42; if (false) {}` yields `42` where §14.6.2 says `undefined` — for *that* example at **step 3** (`IfStatement : if ( Expression ) Statement`, which returns before ever reaching step 5's `UpdateEmpty`); step 5 is the right cite for a truthy test or the else-bearing production. Preserving the already-correct forms while adding the resets is ≥3 axes ⇒ **edge-dense, so this row is an umbrella — **UMBRELLA, not a terminal unit.****; its derivation's seed is UpdateEmpty (§6.2.4.4) together with the §14 statement productions whose evaluation invokes it, enumerated by command at the derivation per §5's seed rule rather than listed here, and it mints one child per production family whose reset discipline differs | the next VM statement-lowering PR, which fires that derivation rather than carrying the work | 2026-09-30 |
| `#11-vm-property-key-lowering-unification` | **(carved at PR-B)** `PropertyKey` → static-name lowering is open-coded at **6** match sites in `crates/script/elidex-js/src/compiler/` (`grep -rn -e 'match &prop.key' -e 'match &property.key' -e 'match key {' -e 'let name_idx = match key' compiler/` at `39bbdb1b`: `expr_object.rs:26`, `expr_object.rs:83`, `stmt_destructure.rs:92`, `stmt_destructure.rs:135`, `expr_class.rs:550`, `expr_class.rs:596`). Five of them answer the same question and give **4 different** answers to a key kind they do not list: `expr_object.rs:26` `compile_accessor` emits `Op::Pop` (silent), `expr_object.rs:83` `PropertyKind::Init` is exhaustive, `stmt_destructure.rs:92` emits `Op::PushUndefined` (silent), and both `expr_class.rs` sites return `CompileError`. The sixth (`stmt_destructure.rs:135`, rest-key exclusion) answers a *different* question — which already-destructured keys to delete — and its `_ => {}` is paired with the computed-key temp-local loop below it; measured, it still drops non-computed non-Identifier/String keys: `const {1: a, ...r} = {1:'x', z:'y'}` leaves `r` as `{"1":"x","z":"y"}`. **Deliverable is one canonical `PropertyKey` lowering plus its consumers** (CLAUDE.md *One issue, one way*), not four catch-all patches — the point is that a key kind cannot be silently absent from one site. The five sites answering the same question **collapse** into it; the sixth (`stmt_destructure.rs:135`) answers a different question and is instead **converted to consume** the canonical lowering, since "collapse the partial copies" does not literally apply to it. **Ownership, scoped to match §2.2's own Slice column**: this slot owns the `expr_object.rs:26-39`, `expr_object.rs:117-121` (the `{1n:'x'}` empty-string key — **routed here off Slice 9**, because the arm is inside the `:83` `PropertyKind::Init` key match itself, not builtin surface), `stmt_destructure.rs:117-119` and `stmt_destructure.rs:149-150` rows outright, and of the `expr_class.rs` row **only the dead `if computed` guard** (an I-4 connect-or-delete) — the two *reachable* `_ =>` arms belong to **the child of umbrella 2 that owns class-member key lowering**, which is what that row's Slice column already says. An earlier wording claimed all four rows plus the guard and over-read one of them. **KIND UNDETERMINED — and no PR may be scheduled under this slot until the measurement below is made.** The breadth this cell can state is 6 sites × 3 files × key kind × syntactic position (object literal / class body / binding pattern / rest exclusion) × failure-mode discipline (four different current answers across I-1's substitution classes), and a product of counts is not an axis count. Its split is latent in this cell rather than in the site enumeration — two children if the measurement below returns an umbrella, the one PR's two deliverables if it does not: the canonical lowering **and** the five collapses are one child, since landing the canonical form while a copy survives is the coexistence *One issue, one way* forbids; `stmt_destructure.rs:135`'s conversion is a second child, consuming that artifact and carrying the `const {1: a, ...r}` defect that is its own question; the class-member arms are outside both, on the child of umbrella **2** this cell already names. **Why the sweeps missed it**: §2.2's passes are file- and marker-oriented (`grep` per file, comment keywords, `Op::PushUndefined` occurrences), while this concept spans **3** files (`grep -rln 'PropertyKey::' crates/script/elidex-js/src/compiler/`) with catch-alls of four different shapes, one of them a `CompileError` return that no substitution-class grep sees. ⚠ **Row-versus-slot, ruled here rather than left for the next round to re-open: this stays a slot.** The test Slices **C** and **K** applied when they left §8 is **not** "the deliverable is code" — §8 owns `#11-vm-topropertykey-symbol-from-toprimitive` (collapse the open-coded §7.1.21 copies onto one implementation, then fix it), `#11-vm-object-literal-proto-setter` (the `__proto__` branch in `compile_object_expr`) and `#11-vm-yield-delegation-lowering` (a new lowering for §15.5.5's resume protocol), every one of them a concrete artifact, and reading the test that way would make the ledger illegal wholesale. What C and K actually turn on is stated in both cells: the slot stood in for **an artifact two named §5 slices must each consume before either can be written**, with ownership expressed as coordination between slices that carry no `Deps` edge — "whichever starts first coordinates nothing", because a slot ships no PR that would settle which of them moves it, so the two-mechanism outcome the carve existed to prevent survives the carve. This row fails that test on both halves. Its deliverable **collapses copies that already exist** rather than producing an artifact a consumer waits on: every consumer can land leaving the six sites as they are, which is the status quo and not a second mechanism, and none of them is blocked meanwhile. And its ownership is not coordination but the **site-stated trigger** in the next cell, whose own ⚠ note records it as the deliberate replacement for a "whichever of the 2 family's rows … first touches …" wording — i.e. for precisely the shape K rules out — and which fires on any child of umbrella 3 that edits one of them, a consumer no `Deps` edge in §5 reaches. So the edges onto it stay slot-valued `Deps` entries in the convention 9c's cell states, and "materialize it ahead of its consumers" is what the trigger's leading **now** already says. **The measurement that settles the kind**: whether key kind, syntactic position and failure-mode discipline are three axes or one missing lowering seen from three sides. This cell's own account is that five of the six sites answer the *same* question and give `4 different` answers to it, which makes the position and the four answers manifestations of the single absent canonical lowering rather than invariants that fail independently. So the probes are: one red on the rest-key exclusion semantics (`const {1: a, ...r} = {1:'x', z:'y'}`) with the canonical lowering already in place, and a third red while both of those are green — the site count and the file count being breadth and not factors. If only two come back, the row is terminal and the two children named above are the one PR's two deliverables. The second disjunct is unavailable without a citation this cell does not carry, and the cell makes no absence claim either: its own account is that the six sites give `4 different` answers to one question, which asserts that a canonical answer exists rather than that it is absent from the specification. | **blocked** — the row's next event is the measurement the *Why* cell names; no PR and no derivation may be scheduled until it resolves the kind; failing that, **the next PR that edits one of the `PropertyKey` → static-name lowering sites this row enumerates, or the function holding one of them**, the set re-derived against that PR's parent HEAD rather than read off the frozen line numbers — **from the predicate (*a match over a `PropertyKey` that lowers it to a static name*), with this row's four-alternative `grep` as the seed for it**: a seventh site spelled outside those four alternatives is exactly what the pattern cannot return, and this row already records the sweeps missing one once. ⚠ **Narrowed from "with Slice 0ca's sweep, or whichever of the 2 family's rows (2aa/2ab/2ac/2b) and umbrella 5's children first touches `expr_class.rs` or `expr_object.rs` (Slice 9 is no longer a trigger: `compiler/expr_object.rs` left its module column with the `{1n:…}` row)"**, which enumerated by the `Primary module(s)` column §5 demotes as non-authoritative — and which under-fired for exactly the reason a module column cannot see: **Umbrella 3**'s object-literal `[[HomeObject]]` obligation — mechanism (d) of its derivation — edits `compile_object_expr`'s `PropertyKind::Init` arm (`compiler/expr_object.rs:46`) and `compile_accessor` (§5's evidence block), i.e. the two functions holding the `expr_object.rs:26` and `expr_object.rs:83` sites, while nothing in the sweep-and-class-family enumeration it replaced reaches umbrella 3's children at all. Stating the trigger over the **sites** removes the dependence on the column entirely, and it is what makes them fire it; Slice 0ca's sweep still fires it, by the same test | 2026-09-30 |
| `#11-vm-float16array-global` | **(carved this round, out of the row above's own §19.3 enumeration)** `Float16Array` is absent while the other eleven TypedArray subclasses are present, and the absence is **structural rather than a missing registration**: `ElementKind` (`vm/value.rs:455-466` at `1e956809`) declares **11** variants with `COUNT = 11` (`:475`), `SUBCLASS_TABLE` is `[SubclassEntry; ElementKind::COUNT]` (`vm/host/typed_array_install.rs:431`) and a `const _: () =` block asserts each entry's position against `ElementKind::index()` (`:506-516`) — so the table is exhaustive **over the enum** and cannot see a subclass the enum never names. That is §5's *exhaustive is not the same as correct* in its literal form, and it is why the row above cannot catch this: its separating observables name `ArrayBuffer`, `DataView` and `Int32Array`, all three of which the eleven-variant enum already supplies, so **both** of that row's admissible discharges pass with this constructor still absent. Measured at `1e956809`: `git grep -c 'Float16' 1e956809 -- crates/script/elidex-js/src` returns one file, `vm/host/data_view.rs` → **2**, and both lines are the tracking comment on `DataView.prototype.getFloat16` / `setFloat16` (`:27-28`) — no implementation anywhere — against the control `git grep -c 'Float32' 1e956809 -- crates/script/elidex-js/src` → **13** files. Spec surface read from the clauses rather than from a member list: ECMA-262 **§19.3.15** `Float16Array` (`webref heading ecma262 19.3`, which lists **12** typed-array constructors against the enum's 11); §23.2's *TypedArray constructors* table gives element type **float16**, element size **2**, "16-bit IEEE floating point"; and the governing conversion is **§25.1.3.17** NumericToRawBytes step 1 — "converting value to IEEE 754-2019 binary16 format using roundTiesToEven mode" — with **§25.1.3.14** RawBytesToNumeric its inverse (`webref body ecma262 sec-numerictorawbytes` / `sec-typedarray-objects`). **Acceptance is stated over that conversion and not over the name resolving** (§5's head): a value exactly representable in binary16 survives a store-then-load unchanged while one that is not becomes its roundTiesToEven neighbour — `new Float16Array([1.0009765625])[0] === 1.0009765625` (1 + 2⁻¹⁰, the step at 1) **and** `new Float16Array([1.00048828125])[0] === 1` (1 + 2⁻¹¹, exactly half that step, so it ties to the even significand) — ⚠ **the warrant stated for that pair was wrong, and is corrected here**: `1.00048828125` = 1 + 2⁻¹¹ is *exact* in binary32 and binary64, so an implementation that stores f32 or f64 and never rounds returns `1.00048828125` and **fails the second case on its own**. The second case is the red probe; the first is a **declared positive control** bounding an over-eager rounder — the I-8 reading the pair never declared. ⚠ **And both cases sit inside one binade of 1, which is where a converter is least likely to be wrong.** **§25.1.3.17** step 1 names the *format*, not a range, so the conditions are stated over that format's boundary classes, none of which a rounding path near 1 reaches. **Signed zero**: `Object.is(new Float16Array([-0])[0], -0)`, false for a converter that rebuilds the value from magnitude alone. **Subnormals**: `new Float16Array([5.960464477539063e-8])[0] === 5.960464477539063e-8` — 2⁻²⁴, the least positive binary16 subnormal — zero for a converter that flushes below the least *normal* 2⁻¹⁴, paired with `Object.is(new Float16Array([2.9802322387695312e-8])[0], 0)` — 2⁻²⁵, exactly half of it, which roundTiesToEven sends to the even significand, i.e. to +0 rather than up. **Overflow**: `new Float16Array([65520])[0] === Infinity`, against the **declared positive control** `new Float16Array([65519])[0] === 65504` — the greatest finite binary16 is (2−2⁻¹⁰)×2¹⁵ = 65504, 65520 is its midpoint with the unrepresentable 2¹⁶, and roundTiesToEven takes the even significand — so a converter that *clamps* to 65504 passes the control and fails the probe. **NaN**: `Number.isNaN(new Float16Array([NaN])[0])` **and nothing stronger**, because step 1 says the bytes "may be set to any implementation chosen IEEE 754-2019 binary16 format NaN encoding", so a condition over a bit pattern would be unfalsifiable in the other direction. **And the storage width, which no value condition reaches**: `Float16Array.BYTES_PER_ELEMENT === 2`, `new Float16Array(4).byteLength === 8`, and `new Float16Array(new ArrayBuffer(8), 2).length === 3` — a twelfth `SUBCLASS_TABLE` entry copied from `Float32` carries element size **4**, satisfies every value condition above once its conversion is right, and addresses the buffer in the wrong stride. **Polarity**: every condition above is a **red probe** at `1e956809`, where the constructor does not exist at all, and each is stated so that it stays red under a *partial* conversion rather than only under the missing global; the two named controls are the exceptions. ⚠ **And this row orphans a surface its own producer file assigns to it** — `vm/host/data_view.rs:27-28` at `1e956809` records `DataView.prototype.getFloat16` / `setFloat16` as "tracked alongside Float16Array", and both consume the same §25.1.3.17 float16 arm; they are named here as **un-minted rather than out of scope** — ⚠ **and "this row's plan-review either takes them or hands them to a named owner" was withdrawn while this row read terminal** — a terminal row has no later derivation step, so that was a hand-off to nobody — **and the reclassification below restores one: routing them is a mandatory output of this row's derivation.** There is no other owner to hand them to either: `git grep -nI -i -c float16 1e956809 -- .` returns exactly two files — `crates/script/elidex-js/src/vm/host/data_view.rs` → **2**, which is that comment, and this memo → **1** — so no implementation and no other row. **Both methods are therefore inside this row's charter**, and they land on the child this row's derivation mints for them: **§25.3.4.7** `DataView.prototype.getFloat16` and **§25.3.4.18** `setFloat16` are thin wrappers that call **§25.3.1.5** GetViewValue / **§25.3.1.6** SetViewValue with `type` = float16, which reach **§25.1.3.16** GetValueFromBuffer / **§25.1.3.18** SetValueInBuffer and so the very §25.1.3.14 / §25.1.3.17 arm named above — reached through §25.3.1.5 / §25.3.1.6 rather than around it (§-numbers from `webref aoid ecma262 GetViewValue` / `SetViewValue` / `GetValueFromBuffer` / `SetValueInBuffer`). **Two more red probes at `1e956809`**, where `DataView.prototype.getFloat16` is `undefined` and both die before reading anything: the rounding condition restated through the view — `const d = new DataView(new ArrayBuffer(2)); d.setFloat16(0, 1.00048828125); d.getFloat16(0)` must be `1` — and the byte-level condition **no `Float16Array` witness above can reach**, pinning the 2-byte width and the default endianness together: `const d = new DataView(new ArrayBuffer(2)); d.setFloat16(0, 1); [d.getUint8(0), d.getUint8(1)]` must be `[60, 0]`, binary16 `1` being `0x3C00` and the omitted `littleEndian` argument reaching `ToBoolean(undefined)` = **false** at GetViewValue **step 4** / SetViewValue **step 6** (`webref body ecma262 sec-getviewvalue` / `sec-setviewvalue`) — so a little-endian-by-default wrapper answers `[0, 60]`, and one that copies `Float32`'s 4-byte element size throws a `RangeError` at SetViewValue **step 13** on this 2-byte view rather than answering at all. **Not the row above's**: that row inventories a **cfg** hole — the family missing from non-`engine` builds — and its deliverable is a layering decision whose admissible discharges it closes at (a) relocation and (b) amendment at the SSoT; neither produces a twelfth subclass, so folding this in would be a third "discharge" of exactly the kind that row withdrew. **Not a Slice-9 sub-slice**: `#11-vm-builtin-prototype-static-sweep`'s rule is the property surface of every intrinsic **a program can obtain**, and this one cannot be obtained at all, which is the same routing that makes `#11-vm-atomics-global` and `#11-vm-weakref-finalization-registry` their own slots rather than children of that umbrella. **UMBRELLA, not a terminal unit** — reclassified from terminal at this revision. **The first disjunct is met three times over, on this cell's own probes, each stated red while the other two are green**: (i) **the binary16 conversion** (§25.1.3.17 NumericToRawBytes / §25.1.3.14 RawBytesToNumeric) — `a converter that *clamps* to 65504 passes the control and fails the probe`; (ii) **the element width and stride** — a twelfth `SUBCLASS_TABLE` entry copied from `Float32` `satisfies every value condition above once its conversion is right, and addresses the buffer in the wrong stride`; (iii) **the `DataView` byte order** — `a little-endian-by-default wrapper answers [0, 60]`, a byte-level condition no `Float16Array` witness above can reach. `one governing algorithm … in one layer` is the retracted paraphrase's negation, and §5's head records that one AO in one layer still satisfies CLAUDE.md's disjunct, so it never carried a terminal verdict. **This derivation therefore mints terminal children and places each condition above on the child that owns it** — the binary16 conversion arm, the twelfth `ElementKind` variant with its `SUBCLASS_TABLE` entry and element size **2**, and the `DataView.prototype.getFloat16` / `setFloat16` pair — and, per §5's row-kind rule, this row carries no acceptance condition of its own: every probe stated above is placed on one of those children, and the row retires once all of them have landed. The second disjunct is unavailable: §25.1.3.17 NumericToRawBytes is quoted from `webref body ecma262 sec-numerictorawbytes`, so the AO is in the specification. | **after the child `#11-vm-typed-array-family-layering-and-gate`'s derivation mints for the family's location decision** — this adds a twelfth member to the family whose location that row settles, so landing first would write a new subclass into a directory whose mandate is under decision — then the next `vm/value.rs` `ElementKind` + `vm/host/typed_array*` + `vm/host/data_view.rs` PR — the third file entering the trigger with the two `DataView` float16 methods the *Why* cell now puts in the deliverable — or with the children `#11-vm-atomics-global`'s derivation mints, which wait on the same gate | 2026-10-31 |
| `#11-vm-atomics-global` | **UMBRELLA, not a terminal unit.** ⚠ **This slot read as terminal, and its own declared split is what ends that** — the split it states is non-`wait` against `wait`/`waitAsync`, and the cell then measures that **§25.4.12** `Atomics.pause` "touches no buffer at all, so neither half of the split reaches it": a declared split that does not cover the declared surface. §5's head reads a slot whose *Why* cell declares ≥3 intersecting invariant axes as an umbrella row from the moment it says so, and this cell declares five with no shared lowering chokepoint: **§25.4.3.1** ValidateIntegerTypedArray's typed-array / no-tear validation; the read-modify-write, `load` and `store` memory algorithm reached through **§25.4.3.3** ValidateAtomicAccessOnIntegerTypedArray (`waitable` false) and **§25.4.3.17** AtomicReadModifyWrite; `Atomics.notify` (**§25.4.10**), which calls §25.4.3.1 with `waitable` **true** and then runs the §25.4.3.5–§25.4.3.12 WaiterList machinery; `Atomics.pause` (**§25.4.12**), a host/CPU spin-wait signal touching neither a buffer nor an Agent Record; and the **§25.4.18** `%Symbol.toStringTag%` descriptor shape together with installation of the object itself (§-numbers re-verified with `webref heading ecma262 25.4` and `webref aoid ecma262 ValidateIntegerTypedArray` / `ValidateAtomicAccessOnIntegerTypedArray` / `AtomicReadModifyWrite` / `DoWait`). Each fails while the others pass — the criterion's measurement, not a count. **This is §5's own `Object` demonstration in its literal form**: §25.4 is a well-formed *clause* unit bundling an atomic-memory contract with a member that touches no memory, exactly as `Object.fromEntries`/`groupBy` share `GetIterator` while `Object.hasOwn` has none — and §5's head already rules that a derivation's output is a grouping and not a slice. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. `typeof Atomics` is `undefined` (probe at `39bbdb1b`) and `grep -rn 'Atomics' crates/script/elidex-js/src` → 0. ⚠ **The earlier rationale — "meaningless without SharedArrayBuffer, which is itself COEP/COOP-gated, so the feature has no reachable surface today" — is spec-wrong for almost the whole global** and is withdrawn. Verified by `webref body ecma262`: **§25.4.8** `Atomics.isLockFree ( size )` takes only a byte size (`ToIntegerOrInfinity(size)`, then the surrounding agent's Agent Record) and touches no buffer at all; **§25.4.3.1** `ValidateIntegerTypedArray ( ta, waitable )` has **no SharedArrayBuffer requirement**, only `IsNoTearConfiguration` and, at **step 5**, "If waitable is true and elementType is neither int32 nor bigint64, throw a TypeError exception". ⚠ **Two routes into it, and an earlier version of this row collapsed them into one**: the **RMW / `load` / `store`** family reaches it via **§25.4.3.3** `ValidateAtomicAccessOnIntegerTypedArray`, whose step 1 passes `waitable` **false**; but **`notify` calls §25.4.3.1 directly with `waitable` true, and `wait` / `waitAsync` reach it one step in through `DoWait`** — `sec-atomics.notify` step 1 is `Let taRecord be ? ValidateIntegerTypedArray(ta, true)`, and `sec-dowait` step 1 is identical. Routing `notify` through the waitable-false gate would let `Atomics.notify(new Uint8Array(8), 0)` succeed where the spec throws. All four anchors re-verified with `.claude/tools/webref body ecma262 <anchor>`. **§25.4.10** `Atomics.notify` step 7 says "If IsSharedArrayBuffer(buffer) is false, return +0𝔽" — defined behaviour, not a throw. The **only** SAB gate is **§25.4.3.14** `DoWait` step 3 ("If IsSharedArrayBuffer(buffer) is false, throw a TypeError exception"), whose only callers are **§25.4.15** `Atomics.wait` and **§25.4.16** `Atomics.waitAsync` (each one step: `Return ? DoWait(…)`). So the read-modify-write / `load` / `store` / `isLockFree` / `notify` surface of §25.4 is reachable on ordinary `ArrayBuffer`-backed integer TypedArrays, which already exist in-tree (`grep -rn 'Int32Array' crates/script/elidex-js/src` → **17**, `typeof Int32Array` → `function`). ⚠ **"Implementable today" holds for `engine` builds only** — the whole backing surface is `#[cfg(feature = "engine")]`: `vm/host/mod.rs` gates `array_buffer` at `:75`, `data_view` at `:102` and the seven `typed_array*` modules at `:340`-`:352`; `Int32Array` is registered at `vm/host/typed_array_install.rs:463`; and `ObjectKind::ArrayBuffer` is gated at `vm/object_kind.rs:420-421` while `TypedArray`/`DataView` are not. That backing surface is what this slot **depends on the child `#11-vm-typed-array-family-layering-and-gate`'s derivation mints for the family's location decision** for — the same decision Slice **Pa** reaches from the other side. ⚠ **The earlier routing of this work into Slice 9's widened charter is withdrawn**: §25.4 operates on TypedArrays under `vm/host/`, behind that gate, while the Slice-9 umbrella's units are the `Array` / `String` / `Object` prototype-and-static surface in core `vm/natives_*`. This slot gets its own slice. **Split**: the non-`wait` surface is an ordinary unimplemented builtin; only `wait`/`waitAsync` are SAB-gated. ⚠ **The non-`wait` half must be enumerated from the clause index, not by inspection — two §25.4 members sit outside the read-modify-write / `load` / `store` / `isLockFree` / `notify` list derived above**, and were un-minted rather than out of scope until `webref heading ecma262 25.4` was run over the clause index (§5's own seed rule). **§25.4.12** `Atomics.pause ( )` — "1. If the execution environment of the ECMAScript implementation supports signaling to the operating system or CPU that the current executing code is in a spin-wait loop, send that signal. 2. Return undefined." — touches no buffer at all, so neither half of the split reaches it; and **§25.4.18** `Atomics [ %Symbol.toStringTag% ]`, initial value the String `"Atomics"` with `{ [[Writable]]: false, [[Enumerable]]: false, [[Configurable]]: true }`, is a property-descriptor shape nothing on this row asserts. **Evidence for the derivation to route rather than this row’s acceptance** (an umbrella states none): `typeof Atomics.pause === "function"` with `Atomics.pause() === undefined`, and `Object.prototype.toString.call(Atomics)` returning `"[object Atomics]"` **through a descriptor whose `[[Writable]]` and `[[Enumerable]]` are both false and whose `[[Configurable]]` is true** (§25.4.18) — a build shipping exactly the RMW / `load` / `store` / `isLockFree` / `notify` list satisfies every observable stated above and fails these, and a build defining the tag as a plain writable-and-enumerable data property satisfies the `toString` read while failing the descriptor. ⚠ **And the `waitable` distinction this cell derives is written here only as prose about a *wrong routing*, never as a condition**, which is the third thing the derivation must place: `Atomics.notify(new Uint8Array(8), 0)` must throw a **TypeError** — §25.4.3.1 step 5, reached with `waitable` true — where a build routing `notify` through the waitable-false gate returns `+0𝔽` | non-`wait` surface: **after the child `#11-vm-typed-array-family-layering-and-gate`'s derivation mints for the family's location decision** — its own slice, not a Slice-9 sub-slice. `wait`/`waitAsync`: SharedArrayBuffer + COEP/COOP — ⚠ **and this row is an umbrella, so the occasion fires its *derivation*, never a PR that would carry the work**: that derivation mints the terminal children, each its own PR under its own mandatory `/elidex-plan-review`, and re-points every ordering stated above onto the specific child that consumes it | non-`wait`: **2026-10-31** (matching the other builtin slots — the `2027-01-31` this row carried belonged to the withdrawn "no reachable surface today" rationale and did not follow it out); `wait`/`waitAsync`: 2027-01-31 |
| `#11-vm-weakref-finalization-registry` | **UMBRELLA, not a terminal unit.** ⚠ **This slot read as terminal, and its own sentence below — that the work "[n]eeds a GC-observability design of its own" — is what ends that**: that is CLAUDE.md's *second* disjunct in the row's own words, a subsystem with no canonical algorithm in tree, and §5's head reads a slot that says so as an umbrella row from the moment it does. Four axes, none sharing a chokepoint: **§26.1**'s `[[WeakRefTarget]]` together with the agent-level KeepDuringJob / ClearKeptObjects liveness rule, which is a property of the *job boundary* rather than of either constructor; **§26.2**'s `[[Cells]]`, unregister tokens and the CanBeHeldWeakly test; the collector change itself in `vm/gc/` — weak marking plus a sweep-time hook, which §4 I-7 makes an invariant question rather than a mechanical one; and the enqueue of the cleanup callback onto the job queue. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. ECMA-262 **§26.1 WeakRef Objects** / **§26.2 FinalizationRegistry Objects** (verified `webref heading ecma262 26.1` / `26.2`; both under §26 Managing Memory). Needs a GC-observability design of its own (exposing collection timing to script). Absent in-tree: `grep -rn 'WeakRef' crates/script/elidex-js/src` → 0, `FinalizationRegistry` → 0, and `typeof` on each is `undefined` (probe at `39bbdb1b`) | a WPT/site depending on them, or the GC-observability pass — ⚠ **and this row is an umbrella, so the occasion fires its *derivation*, never a PR that would carry the work**: that derivation mints the terminal children, each its own PR under its own mandatory `/elidex-plan-review`, and re-points every ordering stated above onto the specific child that consumes it | 2027-01-31 |
| `#11-vm-function-constructor-global` | **(pre-existing, registered 2026-07-18 at #474's landing; ledger count 3)** global `Function` is absent and §9 dec. 7 recorded the policy decision as unowned, so I-6's core strict-mode `eval` surface could stay unimplemented forever inside a *completeness* program. `Function`'s constructor is §20.2.1 = dynamic creation via §20.2.1.1.1 CreateDynamicFunction, entangled with the core-strict-only stance. It opens its own umbrella when triggered — **UMBRELLA, not a terminal unit.** §5's head reads such a slot as an umbrella row from the moment it says so — and what that umbrella carries is **code, divided at the core/compat line rather than chartered whole** — ⚠ *the undivided wording stood here and contradicted §4 I-6, which had already been corrected*. **Core**: the strict **direct** `eval` path through §19.2.1 `eval ( source )` / §19.2.1.1 PerformEval, **plus the strict-source indirect `eval` path through the same AO and strict-source §20.2.1.1 `Function ( ...paramArgs, bodyArg )` through §20.2.1.1.1 CreateDynamicFunction**. **LegacySemantics compat**: the **sloppy half of each of those three forms**, and only that — sloppy *evaluation* for direct `eval`, since **§19.2.1.1** step 12 makes `eval("var x = 1")` from a strict caller a strict eval and therefore core's, and sloppy *source* for the other two, where step 1's assert leaves the handed source's own directive deciding. ⚠ **The reading that stood here is superseded by §4 I-6.** Superseded text: indirect `eval` and the `Function` constructor were chartered to compat whole, "both sloppy *by construction*" — PerformEval step 1 asserting `strictCaller` is false whenever `direct` is false, and CreateDynamicFunction step 28 reaching `OrdinaryFunctionCreate` with no step forcing strictness. Neither makes a *form* sloppy: PerformEval **step 13** `Else, let strictEval be ScriptIsStrict of script` is reached exactly on the indirect path, and OrdinaryFunctionCreate (**§10.2.3**) **step 7** `Let strict be IsStrict(body)` reads the handed body's own directive, so a strict-source indirect `eval` and a strict-source `Function` both run strict (§4 I-6's measurement). So this umbrella's derivation mints the strict children and **hands the three sloppy halves to the LegacySemantics plugin**; chartering those here is what §4 I-6 forbids program-wide. ⚠ **The policy question — whether `eval` is permitted — is a separate kind and the security layer owns it, but "separate owner" is not "no dependency", and this cell read as the latter.** It joins these algorithms at §19.2.1.2 HostEnsureCanCompileStrings (host-defined, default `NormalCompletion(unused)`), which PerformEval reaches at **step 5** and CreateDynamicFunction at **step 11** — i.e. inside both AOs this umbrella ships, ahead of parsing. A deny-capable hook is therefore a **prerequisite** of this umbrella, carried in the `Trigger` cell and in §5's pointer-row `Deps`; §5's pointer row states the separation and names the security layer that owns CSP. **§5's slice table carries a pointer row only**: a full triple living in a slice-table cell is invisible to this section's derive recipe | **the ledger's registered trigger** (SoT): "a dynamic-`Function` need, OR the LegacySemantics/core-strict compat work". ⚠ **Additionally**, per this plan: the first WPT/site needing strict-mode `eval`. ⚠ **The CSP / sandbox-policy decision was struck from this cell as "no longer a trigger"; that is withdrawn, and it returns as a *prerequisite*, which binds harder than a trigger.** "Gates none of the algorithms this slot's umbrella ships" is false against the text: PerformEval reaches §19.2.1.2's hook at **step 5** and CreateDynamicFunction at **step 11**, so it gates *both*, before either parses; and its default returns `NormalCompletion(unused)`, so shipping against the unreplaced default ships an ungated `eval` and `Function` under a CSP that forbids string compilation. **Prerequisite (blocking): no child of this umbrella exposes global `eval` or `Function` until a deny-capable `HostEnsureCanCompileStrings` exists** — wired to the Renderer's CSP (`docs/design/ja/10-network-architecture.md:316`), or, if that integration is not yet available, defaulting to **deny** rather than to the spec default. Owning the *policy* stays with the security layer; owning the *gate's existence* is this umbrella's, because it is the thing that exposes the globals. Neither trigger subsumes the other; **reconciling the two is owed on the ledger side at landing** and is not done by this PR | 2026-10-31 |
| `#11-vm-module-opcode-connects` | **(carved here)** Slice D's connect-or-delete criterion cannot be satisfied while `Op::ImportMeta` / `Op::DynamicImport` are assigned to the ES-modules umbrella's connect work (§2.3, §5 Slice D): D would have to delete two opcodes M owns, or leave them dead and fail its own criterion. Carving the two connects by name rather than by prose gives D's Deps something **retirable** — an escape hatch that is a permanent third state is exactly what I-4 forbids, and a carve-out recorded only in prose is unretirable (Forward rule). Distinct from `#11-vm-dynamic-import`, which owns `import()`'s Promise-returning *implementation*; this owns the two opcode connects D is blocked on. ⚠ **"Connected" cannot be the acceptance for the `ImportMeta` half, because nothing else owns its semantics**: `grep -o '#11-vm-[a-z0-9-]*import[a-z0-9-]*' docs/plans/2026-07-vm-p4-es-language-completeness.md \| sort -u` returns **only** `#11-vm-dynamic-import`, so a row that retires on "the compiler emits it and dispatch pushes an object" leaves ECMA-262 **§13.3.12.1** *Runtime Semantics: Evaluation*, production `ImportMeta : import . meta` (`webref body ecma262 sec-meta-properties-runtime-semantics-evaluation`), owned by nobody. Its steps 3–6 are exactly what a bare connect misses: step 3 reads `module.[[ImportMeta]]`, step 4 runs **only** `If importMeta is empty`, step 4.a is `OrdinaryObjectCreate(null)` — a **null** prototype, not `%Object.prototype%` — step 4.e is `Set module.[[ImportMeta]] to importMeta`, and steps 5–6 return the **cached** object. **Separating observables**: `import.meta === import.meta` must be `true` and `Object.getPrototypeOf(import.meta)` must be `null`; an arm that mints a fresh ordinary object per evaluation connects the opcode and fails both. Measured at HEAD: `vm/dispatch.rs:1063` is `Op::ImportMeta => { self.stack.push(JsValue::Undefined); }` and `compiler/expr.rs:200` folds every `MetaProperty(_)` other than `new.target` into `Op::PushUndefined`, so neither half exists yet. ⚠ **And the first of the two observables above is *green* at this row's own declared producer point, so it is a guard and not a probe**: with `Op::ImportMeta` pushing `JsValue::Undefined`, `import.meta === import.meta` evaluates `undefined === undefined` at the PR that first enables `parse_module`. **The condition it cannot supply**, red under a module-agnostic singleton as well as under the stub: **§13.3.12.1** step 3 reads `module.[[ImportMeta]]` and step 4.e sets it *per Module Record*, so two distinct modules in one graph must receive two distinct objects. ⚠ **And the `Op::DynamicImport` half of this row's own title carries no condition at all** — its semantics belong to `#11-vm-dynamic-import`, so the condition here is the connect itself: the opcode is reached and produces a value distinguishable from `JsValue::Undefined`, stated separately so that the two halves cannot retire on one | whichever PR first enables `parse_module` | 2026-12-31 |
| `#11-vm-webidl-section-3-10-retag` | **(carved at PR-B by this document's own plan-review)** [§16](2026-07-vm-p4-slice-0a-landing-record.md) asserts that "§3.10.16 and §3.10.21 do not exist in Web IDL at all" and 0a retagged **one file** (`vm/host/events_modern/touch.rs`). Verified with `webref heading webidl 3.10`: §3.10 *does* exist — "Observable array exotic objects" — but stops at **§3.10.9**, so every `§3.10.N` with `N > 9` is a dangling citation. Measured at HEAD: `grep -rn -E -e '3\.10\.1[0-9]' -e '3\.10\.[2-9][0-9]' crates/` → **58** sites over **10** distinct nonexistent numbers (`3.10.10` ×9, `.13` ×2, `.14` ×6, `.16` ×11, `.18` ×9, `.20` ×3, `.21` ×10, `.23` ×4, `.25` ×2, `.34` ×2) — and `touch.rs:253`, the very file 0a retagged, still carries §3.10.18. **This is an identified-and-unfixed class, so the Forward rule requires a row**; deliverable is the retag of that class, **the grep supplying neither the population nor the destination**. ⚠ **That pattern encodes the *symptom* and not the property, so the 58 is a seed and not the inventory**: the property is "citation written against the retired §3.10 numbering", and current §3.10 is *Observable array exotic objects*, so the `§3.10.1`–`§3.10.9` sites are drifted on exactly the same ground while `-e '3\.10\.1[0-9]' -e '3\.10\.[2-9][0-9]'` excludes them **by construction** — a pattern built from `N > 9` cannot return the class it is meant to enumerate, and the negative it does return proves only that the seed is non-empty. The inventory is derived at slice time over every `§3.10.` site — `grep -rn -E '3\.10\.[0-9]' crates/`, classified per site — and the 58 is what found the class, not its boundary. ⚠ **A digit-preserving `3.10.N → 3.2.N` substitution is not derivable from the grep, and it is provably wrong for one of the ten numbers**: `webref heading webidl 3.2` returns *JavaScript type mapping* with **28** subclauses (`… \| grep -cE '^\s+§3\.2\.[0-9]+\s'` → **28**, §3.2.1 *any* through §3.2.28 *Observable arrays*), so the **2** `§3.10.34` sites have no counterpart at all and the substitution mints a fresh dangling `§3.2.34`. The remaining nine land inside the range, but **eight of them land on the wrong clause**, so the substitution is wrong for **9 of the 10** numbers and right for exactly one — `.25` (union resolution, `intersection_observer.rs:385`) → §3.2.25. Re-derived here from `webref heading webidl 3.2`, and the reason is structural: §3.2 is **not** §3.10 shifted by a constant, because the eight integer types are **grouped** under **§3.2.4** *Integer types* as §3.2.4.1–§3.2.4.**8** — §3.2.4.9 is *Abstract operations*, not a ninth type — displacing every later subclause. So the nine `.10` sites (`coerce.rs:316` `unsigned long long`, `file_list.rs:194` `unsigned long` ToUint32) belong at §3.2.4.8 / §3.2.4.6 while the swap lands them on §3.2.10 *DOMString*; the two `.13` indexed-getter sites belong at §3.2.4.6 while the swap lands them on §3.2.13 *object*; and the six `.14` sites (`dom_bridge.rs:296` — Symbol → TypeError raised **inside a string conversion**, i.e. §3.2.10 / §3.2.11 / §3.2.12 step 2's `ToString`) would get §3.2.14 *symbol*, the IDL `symbol` type, which is not what any of them converts to. ⚠ **The destination is not even a function of the source number**: the ten `.21` sites split between enum coercion (→ §3.2.18 *Enumeration types*, `websocket.rs:678`) and interface-type coercion (→ §3.2.15 *Interface types*, `drag.rs:49`), so no per-number table can be authoritative and the destination set is **derived per site or not at all**. **So the acceptance is not the 58-count alone** — a tree that swapped all 58 digits satisfies "retagged plus deliberately-left-alone equals 58" while carrying ~56 wrong citations, and "read rather than computed" is a process claim the artifact does not carry — but additionally that each destination **exists** (§3.2 stops at §3.2.28) **and** that its heading names the construct that site converts to, checked per site — §3.2.16 is *Callback interface types*, §3.2.18 *Enumeration types*, §3.2.25 *Union types* — which is exactly the correction Slice **Pc**'s `§2.9` half records as having needed three passes before it stopped assigning a plausible number to the wrong clause. So the deliverable is the same exhaustive per-site classification Pc's cell specifies for its own class, and the acceptance is that the retagged count plus the count deliberately left alone equals the count the **derived** inventory returns — ⚠ **and "deliberately left alone" carried no criterion, which leaves the identity a free variable: retag the 58 and declare the remaining 40 left alone, and 58 + 40 = 98 holds to the digit while forty citations into the retired §3.10 numbering survive** (measured at `658cc302`: `grep -rnE '3\.10\.[0-9]' crates/` → **98**, of which `grep -rnE '3\.10\.(1[0-9]\|[2-9][0-9])' crates/` → **58**, the seed's own subset). **The criterion**: a site may be counted left alone only where the §3.10.N it cites still resolves to a heading in the *current* WebIDL index naming the construct the surrounding code implements — established per site by a `webref heading webidl` lookup recorded with it — so a site whose destination no longer exists, or names a different construct, is in the retagged count by construction. **And the per-site half gains a witness**: each retag's destination is recorded together with the `webref` invocation that produced it, so a retag to a heading that merely sounds right — every string coercion sent to §3.2.10 *DOMString* where the IDL type is `ByteString` or `USVString` — fails on the recorded lookup rather than on a reader's judgement — not the **58** the seed grep started from, which excludes `§3.10.1`–`§3.10.9` by construction — every destination read rather than computed. **No `.rs` file was edited by this PR**. ⚠ **This slot owns the WebIDL class only.** The `vm/natives_string.rs` §21.1.3 → §22.1.3 ECMA-262 drift was folded in here and is **moved out to umbrella 9b** (`#11-vm-builtin-prototype-static-sweep`), whose derivation places it on a child: Slice **Pc** reaches the WebIDL callers but not `natives_string.rs`, so a single id could be neither retired after it nor marked half-discharged | with Slice Pc (the P-family row that already re-attributes WebIDL sections in three of these files), or the next `vm/host/` citation sweep | 2026-10-31 |
| `#11-step9-class-extras` | **ADOPTED, not minted** — pre-existing and **already in the live ledger**. Scope "static members / private fields / getters & setters in class bodies / computed-name methods / static blocks / `Op::GetSuperProp` + `Op::SetSuperProp`". Slices 2/3/5 do **not** exhaust it — class static blocks are in the scope string and outside all three, and generic static-block lowering is assigned to Slice 2b above, with the measured scope-index defect and the retirement conditions. The adoption terms and the in-code retag obligation are stated above this table | Slices 2aa/2ac/2b, the children umbrella **2ab** mints, umbrella 3's children and umbrella 5's children, plus **2b**'s static-block lowering | per the ledger entry |
| `#11-dead-opcode-removal` | **ADOPTED, not minted** — terms stated above this table; **absent from the live ledger** (0 hits), so the adoption is genuinely outstanding. Becomes Slice D, broadened to the §2.3 set — which now includes `Op::Wide`, whose prose-only "reserved" exemption was withdrawn at PR-B | Slice D (after every slice that connects a §2.3 opcode **and** `#11-vm-module-opcode-connects`) | 2026-10-31 |
| `#11-d17b-dispatch-expr-file-growth` | **ADOPTED, not minted** — terms stated above this table; **absent from the live ledger** (0 hits). Homes the `vm/dispatch.rs` + `compiler/expr_class.rs` + `vm/interpreter.rs` + `vm/value.rs` 1000-line debt | each slice that touches one of the four files | ⚠ **the carried re-eval `2026-08-08` has expired.** Re-set to **2026-09-30**, aligning it with the first children umbrellas P / 0b mint that touch these four files |
| `#11-compiler-class-emit-readability` | **(trigger this program fires)** Slices 2 and 5 both touch `compiler/expr_class.rs`. ⚠ **This row stated no acceptance condition, and its trigger equates an enumeration to a predicate.** The trigger names "whichever lands first of **2aa / 2ac / 2b** and the children of umbrella **5**" and then glosses that as "the rows whose module column names `compiler/expr_class.rs`" — running that predicate over §5 at this revision returns **seven** rows, **2aa**, **2ac**, **2b**, **5**, **K**, **B** and **S**, so **K**, **B** and **S** are outside the enumeration and **K** is a slot row in this same table. The enumeration is withdrawn — ⚠ **and so is the predicate that stood here to replace it, which this row selected without reading its own sibling.** `#11-vm-property-key-lowering-unification` (§8) narrowed away from a trigger "which enumerated by the `Primary module(s)` column §5 demotes as non-authoritative" after measuring it *under-fire*: umbrella **3**'s children edit `compiler/expr_object.rs` while no module column names it for them, so a column-keyed trigger cannot see them. §5 states the same demotion directly — the column is "a non-authoritative HINT, not a touch set". The predicate is therefore right about **K**, **B** and **S** and wrong about its key, and this row adopts the one form that sibling settled on: **the trigger is stated over the site**, the next PR that edits `compiler/expr_class.rs` whichever row it belongs to, which fires on the edit itself rather than on a column that may not name it. **And the acceptance**, red at whichever parent HEAD that is: `compiler/expr_class.rs` is smaller than at the landing's parent HEAD by a real seam — the class-emit path reached through one named entry point rather than through the arms this cell lists — with the existing suite unchanged, so a rename-and-comment pass does not answer it. Registered here because a trigger stated only in §5 prose fires into a paragraph nothing re-reads at re-eval time; ledger count **0** | the next PR that edits `compiler/expr_class.rs`, whichever row it belongs to — stated over the **site**, per `#11-vm-property-key-lowering-unification`'s narrowing above. ⚠ **Two forms are withdrawn here, not one.** The explicit enumeration this cell carried — "whichever lands first of **2aa / 2ac / 2b** and the children of umbrella **5**" — omits **K**, **B** and **S**, which the *Why* cell's module-column predicate does return; but that predicate is keyed on `Primary module(s)`, which §5 demotes as a **non-authoritative hint**, and which that sibling slot measured *under-firing* on this exact shape | ⚠ **the carried re-eval `2026-08-08` has expired.** Re-set to **2026-09-30** |
| `#11-reflect-apply-ce-test` | **(trigger this program fires)** paired with the child umbrella 10a mints for `Reflect.apply` (**§28.1.1**), whose deliverable §9 dec. 17 now scopes — the `Trigger` beside this cell reads as that umbrella's derivation having run and produced that child, never the umbrella landing, which does not occur (§5). Registered here for the same reason as the row above; ledger count **0**. ⚠ **And this row cites §9 decision 17 as what *scopes* the deliverable while stating no condition, so a test asserting `Reflect.apply(f, null, [1, 2]) === 3` retires it and the dense fast path dec. 17 forbids passes.** **The condition is dec. 17's own separating witness, stated here rather than pointed at**: **§7.3.19** CreateListFromArrayLike performs a `Get` per index, so a getter installed on `Array.prototype[0]` must run for `Reflect.apply(f, null, new Array(1))` — a dense `collect_array_like` fast path reading the backing store never calls it. Red at `658cc302`, where no `Reflect` global exists at all | umbrella 10a's derivation having run and produced the `Reflect.apply` child | 2026-10-31 |
| `#11-webidl-sequence-dense-array-fast-path` | **ADOPTED, not minted** — registered by Slice 0a while it was in `vm/host/`, cited at three in-code sites and **already in the live ledger**. An Array fast path that skips WebIDL §3.2.21 step 2's `GetMethod`, so an overridden `@@iterator` is ignored. ⚠ **Honouring `@@iterator` is not honouring the iterator, so restoring `GetMethod` — or the usual intrinsic guard — is not the acceptance**: step 4 hands off to **§3.2.21.1**, whose step 1 `GetIteratorFromMethod` (ECMA-262 **§7.4.3**) calls **§7.4.2** GetIteratorDirect step 1 `Let nextMethod be ? Get(obj, "next")`, and whose step 3.1 `IteratorStepValue` (**§7.4.10**) then *calls* that method once per element. All three sites read `ObjectKind::Array { elements }` straight off the heap, so they perform neither the `Get` nor any `Call`, and a guard of the form `Get(V, %Symbol.iterator%) === %Array.prototype.values%` repairs only the first. **Separating observable** — ⚠ **and the *outer* level may not be an Array, or the witness measures nothing.** `Object.getPrototypeOf([][Symbol.iterator]())` is `%ArrayIteratorPrototype%`, shared by **every** array iterator, so patching its `next` against an `[['a','b']]` input stops the **outer** drain at zero elements — that level already honours the protocol (`vm/webidl_sequence.rs`, as this cell states one sentence on) — the inner conversion never runs, and a correct implementation and the dense fast path alike build an empty `Headers` and **both succeed**. Written instead with an outer iterable that does not inherit that `next`: after `Object.getPrototypeOf([][Symbol.iterator]()).next = () => ({ done: true })`, then `const pair = ['a','b']; const outer = {}; outer[Symbol.iterator] = function () { let n = 0; return { next: function () { n = n + 1; return n === 1 ? { done: false, value: pair } : { done: true } } } };`, `new Headers(outer)` must throw — the outer iterator's `next` is an **own** property untouched by the patch and still yields the pair, whose own `@@iterator` is the intrinsic (so an intrinsic guard passes it) while the array iterator that method returns now yields **0** items, not 2, and Fetch §5.1's *fill* step 1.1 ("If header's size is not 2, then throw a TypeError", `webref body fetch concept-headers-fill`) rejects it — whereas the intrinsic-guarded fast path still reads `elements[0]` / `elements[1]` and succeeds. Sites at HEAD: `vm/host/url_search_params.rs:287`, `vm/host/headers/parse_init.rs:171`, `vm/host/structured_clone.rs:1009`; the outer level is already strict (`vm/webidl_sequence.rs`). Belongs to Slice **Pc**, umbrella P's WebIDL half ([§16](2026-07-vm-p4-slice-0a-landing-record.md), §4 I-5); it was adopted in prose above this table before PR-B, which made it invisible to the derive recipe. ⚠ **The separating observable above declares no reading, though the tree settles it: it is a **red probe** at `658cc302`**, where all three conversion sites read `ObjectKind::Array { elements }` off the heap (`vm/host/url_search_params.rs:287`, `vm/host/headers/parse_init.rs:171`, `vm/host/structured_clone.rs:1009`), so the user-visible `next` is never called and the observable is absent rather than wrong. Stating it is what stops a reader re-deriving the polarity from the tree, per §4 I-8 | Slice Pc | per the ledger entry |
| `#11-plan-memo-spec-field-single-home-check` | **UMBRELLA, not a terminal unit.** ⚠ **This slot read as terminal, and the mixture of its own four assertions is what ends that.** Two are mechanical programs over this document's table parse — population derivation from the declaration field (the pipeline §5's head already publishes) and the umbrella-carries-no-acceptance/`Deps` cell scan. Two are not: reconciling an ordering stated in **prose** against a named `Deps` cell, and detecting two sentences of one row assigning one obligation to two owners, are natural-language claim extraction, for which **no canonical algorithm exists** — CLAUDE.md's second disjunct, which §5's head applies to a slot from the moment its *Why* cell says so. A mechanical checker discharges the first pair and is structurally blind to the second, so left terminal the slot admits a PR that ships the parse and declares the rest done. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. **(carved at PR-B, by the R27 root-check)** §5 specifies three fields — split, ordering, owner — and each has one structured home (the set of rows, the `Deps` column, the slice id an obligation is written on). Prose that *restates* one makes a second copy nothing checks, and three consecutive review rounds each surfaced one that had rotted: a `0b` umbrella sentence read "ordered and dependent" against three Deps cells saying otherwise; an ordering paragraph listed Pa's dependents while a fourth row sat outside the list; "shared prerequisite" bullets recorded obligations belonging to no slice id. A fourth prose rule cannot fix a class whose failure mode is *an unexecuted claim* — the remedy is a **checker that reads this memo**, reconciling every prose assertion of dependency / independence / ordering / ownership against the cells. Carved rather than written here because its home is a program, not this document. ⚠ **This cell read "its home is the `claim-gate-plan-check` tooling already in flight", and that is now false in the direction that matters**: the first enumerator landed in this PR as `.claude/tools/plan-memo-umbrella-check.py`, a separate program, and its own docstring argues the split — `claim-gate-plan-check.py` is bound to the claim-gate memos' schema and executes git and preflight against pasted answers, while this one reads one document's table parse and executes nothing. Neither is on `main` (`git ls-tree origin/main -- .claude/tools/` lists neither), so joining them today would couple this PR's landing to that branch's unconverged review; the trigger to collapse the two is both being on `main`. **What landed is the mechanical pair only** — the marker population read from the declaring field, and the `Deps` half of the umbrella-carries-no-cell scan. Assertions (c) and (d), and the acceptance half of (b), are printed as declared seeds, which is this row's own verdict restated by a program instead of by a sentence. ⚠ **The trigger below has fired, and the rounds that fired it widen the scope.** PR-B's converge reported a prose/cell contradiction in four separate rounds — this row's prose promising a `Deps` edge while the cell read `—` (twice, on 6c and on Slice 1a's hook prerequisite), a row assigning one spec member to two owners in consecutive sentences, and an umbrella carrying the acceptance conditions §5 says only a terminal child may hold. So the checker's assertions are not only prose-versus-cell: **(a)** every row that declares itself an umbrella carries the marker, with the population read from the *declaration* and never from a grep over the marker's own vocabulary — the control this document first wrote searched the same uppercase string as the marker and so could not see a lowercase declaration; **(b)** no umbrella row carries an acceptance condition or a `Deps` edge; **(c)** no prose asserts an ordering the `Deps` cell it names does not carry; **(d)** no two sentences of one row assign one obligation to two owners. Each is a violation this converge found by hand, one per round, which is the cost of not having the checker | with that tooling's next slice, or the first review round that again reports a prose/cell contradiction — ⚠ **and this row is an umbrella, so the occasion fires its *derivation*, never a PR that would carry the work**: that derivation mints the terminal children, each its own PR under its own mandatory `/elidex-plan-review`, and re-points every ordering stated above onto the specific child that consumes it | 2026-12-31 |
| `#11-plan-memo-acceptance-falsifiability-check` | **UMBRELLA, not a terminal unit.** ⚠ **This slot read as terminal, and its own reason for existing is what ends that** — it states that "the deliverable is a predicate over content rather than this grep", i.e. that deciding whether a stated observable is *separating* is exactly what no mechanical rule does, which is CLAUDE.md's second disjunct in the cell's own words. Three axes, and they do not share a chokepoint: the mechanical vocabulary-and-structure pre-filter, whose measured miss class this cell already publishes; **attributing each cited §-number to the `webref` command that produced it**, a provenance assertion over the memo's text; and **deriving I-8 polarity**, which §4 I-8 states is "a property of the tree, not of the sentence" and therefore cannot be answered by any checker that reads only this document. Left terminal the slot admits precisely the two-token approximation this cell prints and rejects. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. **(carved at PR-B, beside `#11-plan-memo-spec-field-single-home-check`)** that slot's four assertions are **structural**: an umbrella declares its kind in the declaring field; an umbrella carries no acceptance condition and no `Deps` edge; no prose asserts an ordering the cell it names does not carry; no two sentences of one row assign one obligation to two owners. Every one reconciles prose against a cell, and **none reads an acceptance condition's content** — so a row can satisfy all four and still state an acceptance no implementation could fail, which is the class **I-8** names and which that slot's deliverable structurally cannot reach. This is a measured boundary, not a preference for two checkers: the sibling's assertions are prose↔cell reconciliation by construction. ⚠ **The population this predicate is defined over has been measured, and the result is evidence for the derivation above to route rather than a figure to read forward**: applied to all **75** terminal rows §5 and §8 carried at `d8e799aa`, the revision the sweep ran against, it returns **58** assessable rows, **2** passes and **56** failures, each failure carrying a named wrong-but-passing implementation — so a checker whose recall is anywhere near the two-token approximation this cell prints would report a handful against a true population of 56, and the miss class is the whole deliverable rather than a tail. Re-derived by re-running the sweep, never transcribed. Deliverable is a second predicate over §5's rows — a **terminal** row states ≥1 acceptance condition; each condition names the `webref` command that produced the spec claim it asserts and ≥1 **separating** observable, one a plausible *wrong* implementation fails rather than one only an *absent* implementation fails; and each declares its polarity per I-8. **Partial mechanizability is measured, not assumed**: the two-token approximation *(a §5 row ∧ not an umbrella ∧ not a pointer ∧ containing neither `must` nor `acceptance`)* returns **9** rows at `023512e8^`, **6** at `b9d014e9`, **3** at `f754c873` and **1** at this revision, tracking four batches of repairs without being told about them. ⚠ **Those four figures were transcribed until this revision and are now re-derivable**: `.claude/tools/plan-memo-umbrella-check.py <memo>` prints the approximation as `[ACCEPT-VOCAB]`, and running it over `git show <rev>:<memo>` at each of the three cited revisions returns 9 / 6 / 3 — an independent implementation reproducing them, which is what makes them measurements rather than a chain of recollections. ⚠ *And that program first implemented the predicate as four tokens, adding `witness`, `regression` and `assert`; it agreed with nothing, because a figure reproduced by a wider predicate than the figure's own is not reproduced.* ⚠ **Its miss class is measured too, and that is why the deliverable is a predicate over content rather than this grep**: of the four rows `d544f634` repaired by hand, **three** — `8a`, `10a`, `D` — were inside that 9, and the fourth, `9db`, never was. Its cell carried `must` exactly once, in ordinary design prose (*the combinator must keep the iterator open across per-item resolution*), while stating no acceptance condition at all: a predicate keyed on the *vocabulary of the symptom* leaves the row that spells it otherwise authoritative. Its home is a program over this memo. ⚠ **This cell read "the `claim-gate-plan-check` tooling this document's sibling slot already points at — one checker over this memo, not a second one", and the sibling's landing falsified both halves**: the program that landed is `.claude/tools/plan-memo-umbrella-check.py`, which is a second checker, and the sibling slot no longer points where this sentence says it does. The count is not the invariant — *one home per rule* is, and each checker is the single home of its own memo's schema. What that program reaches of this row is the two-token approximation printed above and nothing more: it re-derives the figure rather than transcribing it, and prints beside it that the figure bounds nothing | with that tooling's next slice, or the first review round that again reports an acceptance condition no implementation could fail — ⚠ **and this row is an umbrella, so the occasion fires its *derivation*, never a PR that would carry the work**: that derivation mints the terminal children, each its own PR under its own mandatory `/elidex-plan-review`, and re-points every ordering stated above onto the specific child that consumes it | 2026-12-31 |
| `#11-vm-webidl-sequence-early-exit-drain` | **UMBRELLA, not a terminal unit.** ⚠ **This slot read as terminal and had already begun behaving otherwise — it minted `#11-vm-structured-clone-layering-seam`, which is what a derivation does and what a terminal row does not.** §5's head reads a slot declaring ≥3 intersecting invariant axes as an umbrella row from the moment it says so, and this cell declares three: WebIDL **§3.2.21.1**'s drain composed with per-element user-code conversion ordering; the **layering routing, which resolves differently per half** — the Fetch half extends an existing crate and takes a new dependency, the URL half has no home at all, and the structured-clone half is explicitly staying — so one relocation rule cannot cover the three; and the **engine cap's memory contract** together with the transferable bail-out's cost, which this cell itself calls a design question rather than a deletion. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. **(carved at PR-B, by Slice Pc's re-derivation)** Pc deletes the `iter_close` call at four sites whose triggering early exit WebIDL **§3.2.21.1** does not have (Pc's §5 row enumerates them: an engine cap, two arity checks, an unsupported-transferable bail-out). Deleting the call does not make those four spec-faithful — they still **stop the drain early**, whereas §3.2.21.1 repeats `IteratorStepValue` until done and the consuming specs then check the *converted* sequence (URL §6.2 step 1, Fetch §5.1 step 1.1), so the observable `next()` call count still differs on a `sequence<sequence<…>>` longer than the check allows. ⚠ **Matching the `next()` count is not the acceptance either**: §3.2.21.1 step 3 repeats **both** 3.1 `IteratorStepValue` **and 3.3** "Initialize S_i to the result of converting next to an IDL value of type T", and for `sequence<sequence<USVString>>` / `sequence<sequence<ByteString>>` that conversion runs **user code** (`ToString` → `toString` / `valueOf`) on every element the drain reaches, which a `next()`-count-only fix does not restore. **Separating observable** — ⚠ **whose inner pairs must be generic iterables, not Array literals, or it never reaches the site this row names.** `validate_pair_entry` short-circuits on `elements.len() != 2` at `vm/host/url_search_params.rs:288`, *before* the generic drain whose `iter_close` early exit at `:318` is one of the four named sites, so with Array-literal pairs no `next()` is called on an inner sequence at all: measured, the Array-literal spelling leaves `log` equal to `['a','b']` at `658cc302` **and under Node alike**, separating nothing. Spelled over generic iterables — `const log = [], mk = s => ({ toString(){ log.push(s); return s } }), gen = a => ({ [Symbol.iterator](){ let i = 0; return { next(){ return i < a.length ? {value: a[i++], done: false} : {value: undefined, done: true} } } } }); try { new URLSearchParams([gen([mk('a'), mk('b')]), gen([mk('c'), mk('d'), mk('e')]), gen([mk('f'), mk('g')])]) } catch {}` — the drain is on the path, `log` is `['a','b']` at `658cc302` against `['a','b','c','d','e']` under Node, and the well-formed control `[gen([mk('a'), mk('b')]), gen([mk('c'), mk('d')])]` leaves `['a','b','c','d']` in both. ⚠ **The expected value is the strict IDL reading and no shipping engine matches it**, so it is asserted from §3.2.21.1 and URL §6.2 rather than from an oracle: `log` must equal `['a','b','c','d','e','f','g']` — every pair is converted first, and only then does URL §6.2's initialize algorithm step 1.1 ("If innerSequence's size is not 2, then throw a TypeError") reject the 3-element pair — whereas pumping `iter_next` to `None` and then throwing matches the `next()` count while `log` stops at `'b'`. Draining to done changes the cap's memory contract and the transfer-list bail-out's cost, which is a design question rather than a deletion, so it is not Pc's. ⚠ **Layering disposition (CLAUDE.md *Layering mandate*), stated on the row because the host sites are the ones the mandate reaches — and there are three of them, not two.** The four sites are `vm/webidl_sequence.rs:141` (core `vm/`, outside the mandate's paths) plus `vm/host/url_search_params.rs:318`, `vm/host/headers/parse_init.rs:208` **and `vm/host/structured_clone.rs:1064`; the disposition is owed per host site, so enumerating the halves by *consuming spec* left the third with none** — the same under-enumeration the §8 marker rule above answers with a complement. **Fetch half**: an engine-independent home exists — `crates/api/elidex-api-fetch/src/headers.rs` already owns Fetch §2.2/§5.1 header rules — but it does not hold *fill a Headers object*, and `elidex-js` does not depend on that crate (`grep -rn 'elidex_api_fetch' crates/script/elidex-js/src/` → **0**), so that branch is **extend `elidex-api-fetch` and take the dep**. **URL half**: no home exists — `grep -rl 'URLSearchParams' crates/ \| grep -v crates/script/elidex-js/` → **0** files, against the control `grep -rl 'Headers' crates/ \| grep -v crates/script/elidex-js/` → 8 files in **4** crates (`elidex-net` ×5, `elidex-api-fetch`, `elidex-cache-api`, `elidex-layout-table`), the crate count enumerated rather than eyeballed off the file list — `grep -rl 'Headers' crates/ \| grep -v crates/script/elidex-js/ \| cut -d/ -f1-3 \| sort -u \| wc -l` → **4**, against the negative control with `URLSearchParams` in place of `Headers` → **0**, so the shape discriminates — and the mandate's no-home branch is *extend the engine-independent crate*, never write the check into `vm/host/`. ⚠ **What moves in both halves is the consuming spec's check over the already-converted sequence, never the conversion itself** — URL §6.2 step 1 and Fetch §5.1 step 1.1 both run on the converted value, which is why they are engine-independent at all. **Structured-clone half — and the mandate does NOT reach the drain here.** ⚠ **A disposition that moved this drain out of `vm/host/` stood here and is withdrawn; it was the third appearance of a mis-attribution §4 records as already corrected twice** (Codex R1 then R3, §4's `structured_clone.rs` clause): the `iter_close` at `vm/host/structured_clone.rs:1064` sits inside `ensure_empty_transfer_list`, which is the **WebIDL §3.2.21.1 `sequence<object>` conversion** and runs *while consuming the iterable* — before HTML StructuredSerializeWithTransfer ever receives the list. Converting a JS iterable into an IDL sequence is binding/marshalling by definition, and it is what `vm/host/mod.rs:11-13` places in that directory rather than what it excludes; requiring it to leave would drag `JsValue` and the VM iterator machinery into an engine-independent crate, which is the mandate read backwards. **So the drain stays, and what the mandate reaches is the outer native** — HTML **§2.7.4** StructuredSerialize **fused with §2.7.6 StructuredDeserialize** / **§2.7.7** StructuredSerializeWithTransfer over the *already-converted* list. That half has no home: `grep -rli -E -e structuredserialize -e structured_clone -e 'structured clone' crates/ \| grep -v crates/script/elidex-js/` → **4** files (`elidex-shell` ×2, `elidex-navigation`, `elidex-script-session`), and reading each shows they carry `StructuredSerializeForStorage` *bytes* across the history/navigation boundary (`elidex-navigation/src/navigation.rs:31` is the field's docstring, `:36` already carrying `#11-history-state-structured-serialize-fidelity`) — transport types for a serialization the engine performs, **not an in-tree StructuredSerialize outside `elidex-js`**. ⚠ **"No home" was left as an observation, and an observation is not a disposition — so the question re-opened at row F in a form the three prior appearances did not take: that a ~900-line *algorithm* is not marshalling however engine-bound its subject matter is. It is **not** answered by the two measurements below, and this row no longer claims it is.** They close both *proposed* homes; neither shows `vm/host/` to be a legitimate one, and `vm/host/mod.rs:11-18` admits three things — prototype install, brand check, `JsValue` ↔ `Entity` marshalling — of which a fused ~914-line WHATWG algorithm is none. So they are recorded as **constraints on a derivation that is now owed**, not as a warrant for staying, and that derivation is owned by `#11-vm-structured-clone-layering-seam` below. ⚠ **"It is answered here, and the answer is that the serialization half stays" stood here**: "both proposed homes are closed" and "the current one is legitimate" are different claims, and only the first was measured. Two measurements, neither of them a line count. **(i) The mandate's operative sentence is a routing rule keyed on an engine-independent home, not the three words at `vm/host/mod.rs:11-13`** — read as an exhaustive admission list those three condemn the majority of the directory: at `155f9469`, `git grep -L 'Entity' 155f9469 -- 'crates/script/elidex-js/src/vm/host/*.rs' 'crates/script/elidex-js/src/vm/host/**/*.rs' \| wc -l` returns **128** of **232** files, **50,047** of **95,928** lines (**52%**), that never mention `Entity` and so are not `JsValue` ↔ `Entity` marshalling under any reading, **35** of them over 500 lines — WHATWG Fetch CORS **965**, W3C IndexedDB §4.5/§6.1-§6.6 **904**, WHATWG Encoding §8 **853**, WHATWG URL §6 **792**, WebCrypto **781**, WHATWG Streams §4 **780** — and `structured_clone.rs` **1077** is the *largest member of that class*, not an outlier from it (negative control: the complementary `git grep -l 'Entity'` over the same rev and paths returns **104**, so the pattern discriminates rather than matching everything). **(ii) The mandate's no-home branch is "extend the engine-independent crate", and there is nothing here to extend.** The in-tree engine-agnostic JS value type is `elidex_plugin::JsValue` (`crates/core/elidex-plugin/src/js_value.rs`, **83** lines) whose object case is an opaque `ObjectRef(u64)` handle: it can express no prototype, no own-property table, no `ObjectKind`, no `[[ArrayBufferData]]` and no reference cycle — which is the entirety of what this algorithm walks (`classify(kind: &ObjectKind)` `:237`, the `shape`/`PropertyStorage` walks, the `HashMap<ObjectId, ObjectId>` cycle memo, `alloc_wrapper` `:704`). Every function of the half takes `&mut VmInner` and returns `JsValue`/`ObjectId`, so its domain **and** its codomain are the VM heap and hoisting it would export `VmInner` — the same "mandate read backwards" refused for the drain one sentence above, and refused harder, since the clone half *allocates*. **The spec's own seam is the serialization *record*, and the engine-independent part of it is already outside**: `elidex-navigation`'s `classic_history_api_state: Option<Vec<u8>>` (`src/navigation.rs:39`, docstring `:31-38`) is that record, with `#11-history-state-structured-serialize-fidelity` already owning its upgrade; what remains on either side is `JsValue` → bytes and bytes → `JsValue`. ⚠ **"binding work by construction" stood here as the conclusion, and it is the proposition a derivation has to establish rather than its premise** — asserted of the residue, it is the same move (i) was measured against. **So a relocation slot *is* minted — `#11-vm-structured-clone-layering-seam` below — and what it owns is the derivation these two measurements bound, not a home this row picks**: `#11-vm-typed-array-family-layering-and-gate`'s decision shape fits a conflict whose relocation target exists, and this one's does not exist *yet*, which makes its deliverable deriving a seam rather than choosing between two — and that row's gating half correspondingly rests on this paragraph and on the new slot, not on the bare phrase "engine-bound" its own cell states. The residue this row owns is likewise split by that line: draining to done is the *binding* layer's to change, while the unsupported-transferable bail-out it guards ("Transferable objects are not yet supported") is an engine limitation in the serialization half | with Slice Pc's own plan-review, or the first WPT/site that observes the `next()` count on an over-long sequence init — ⚠ **and this row is an umbrella, so the occasion fires its *derivation*, never a PR that would carry the work**: that derivation mints the terminal children, each its own PR under its own mandatory `/elidex-plan-review`, and re-points every ordering stated above onto the specific child that consumes it | 2026-12-31 |
| `#11-vm-class-name-binding-initialization-order` | **Slice K's work item, not a defer** — kept here so the reordering has a ledger home until it lands. ⚠ **This row owned the initialization order as a *slot*, with "whichever of 2ac/2b starts first" as its owner, and no longer does.** A slot settles a **decision** and ships no artifact — which is why `#11-vm-inbound-host-call-membership-test` and the typed-array layering gate are slots — while *moving* the class-name binding's initialization ahead of `compile_class`'s member loop is **code**; and 2ac and 2b carry no edge between them, so their mandatory plan-reviews may run concurrently and "whichever starts first" ordered nobody to produce it. The same test that turned the declarative-environment carve into Slice **C** applies unchanged. §5's **Slice K** row is the single home for the charter, the acceptance and every measurement this row carried (`compiler/expr_class.rs:99`, the `ClassMemberKind::StaticBlock` arm at `:448`, the `:479-500` initialization, ECMA-262 **§15.7.14** step 28.a and step 32, and the probe `class C { static self = C; static { if (C !== this) throw 0 } }`); this row is a pointer | now (Slice K) | 2026-10-31 |
| `#11-vm-native-function-length-on-allocator` | **(minted this round, by Slice 6c's re-derivation — a work item ordered ahead of 6c, not a defer)** **Arity belongs on the native-function creation path.** ECMA-262 **§10.3.4** CreateBuiltinFunction takes `length` as a parameter and its **step 12** performs `SetFunctionLength(func, length)`, whose **§10.2.10** **step 2** defines an **own** `"length"` data property `{[[Writable]]: false, [[Enumerable]]: false, [[Configurable]]: true}`; §10.3.4 closes with "Each built-in function defined in this specification is created by calling the CreateBuiltinFunction abstract operation", so the property is universal over built-ins rather than per-member (§-numbers from `webref aoid ecma262 CreateBuiltinFunction` / `SetFunctionLength`, steps from `webref body ecma262 sec-createbuiltinfunction` / `sec-setfunctionlength`). In-tree the allocator defines `name` and returns — `create_native_function_keyed` (`vm/shape_ops.rs:493-526` at `658cc302`) — measured rather than read off that window: `git grep -c 'well_known.length' 658cc302 -- crates/script/elidex-js/src/vm/shape_ops.rs` returns **no line** against the control `'well_known.name'` → **1** on the same file and rev, so the grep discriminates. **Deliverable**: an arity parameter on `create_native_function_keyed`, the §10.2.10 descriptor defined there, and every reaching call site passing its own arity. ⚠ **The reaching set is an *inventory*, not a seed, because the two hops are private and both live in one file** — `git grep -n -E 'create_native_function_keyed\(\|create_native_function_impl\(' 658cc302 -- crates/script/elidex-js/src` returns 9 lines, all `vm/shape_ops.rs`, and they close the graph: `create_native_function_keyed` is reached only from `create_native_function_impl` (`:487`) and from `create_native_function_bound` (`:399`), and `create_native_function_impl` only from the five `create_*` helpers (`:271`, `:284`, `:302`, `:325`, `:340`), so the entry points are exactly `create_native_function`, `create_constructable_function`, `create_constructor_only_function`, `create_illegal_constructor_function`, `create_native_function_with_sid` and `create_native_function_bound`, plus the three installers `install_native_method`, `install_bound_accessor_pair` and `install_accessor_pair` that wrap them — ⚠ **`install_accessor_pair` (`vm/shape_ops.rs:354-381` at `658cc302`) sat outside this list while its own two `create_native_function` calls (`:370-371`) were being subtracted as `vm/shape_ops.rs`'s own wiring, so a reaching set this cell calls *exact* did not see its **253** external call sites** (`git grep -h -o -E '\.install_accessor_pair\(' 658cc302 -- crates/script/elidex-js/src \| wc -l` → **253** over **91** files; negative control `'\.install_accessor_pair_NOPE\('` → **0**). It allocates a getter and an optional setter through `create_native_function` and passes each the same display name, so an arity threaded through the other eight leaves every built-in accessor with the allocator's default. Their call sites, counted rather than hand-listed — `git grep -h -o -E -e '\.create_native_function\(' -e '\.create_native_function_with_sid\(' -e '\.create_native_function_bound\(' -e '\.create_constructable_function\(' -e '\.create_constructor_only_function\(' -e '\.create_illegal_constructor_function\(' -e '\.install_native_method\(' -e '\.install_bound_accessor_pair\(' -e '\.install_accessor_pair\(' 658cc302 -- crates/script/elidex-js/src \| wc -l` → **504** over **123** files (`git grep -l`, same patterns), of which **5** are `vm/shape_ops.rs`'s own wiring between those nine — `:370`, `:371`, `:432`, `:434`, `:471`, which is the whole of that file's contribution, so it drops out entirely — leaving **499** in **122** files. Negative control, same pipeline with `.create_native_function_NOPE\(` → **0**. ⚠ *The eight-entry-point reading of this pipeline returned **251** over **87**, leaving **246** in **86**, and it was the omission of `install_accessor_pair` that made the two `:370`/`:371` subtractions look like internal wiring rather than the ninth wrapper's body.* ⚠ **A signature change on 499 sites is what makes this a slice rather than a note**, and it is why 6c cannot carry it. **Acceptance, with polarity (I-8)** — **(1) Red probe** at this row's parent HEAD, from Rust: for a built-in installed through each of the nine entry points, `Object.getOwnPropertyDescriptor(f, "length")` is an own descriptor of the spec arity with `{writable: false, enumerable: false, configurable: true}`. Red at `658cc302` on the objects the allocator returns, which carry `name` and nothing else — the measurement above. ⚠ **Whether some install site defines one afterwards is deliberately not asserted here**: the probe runs over all nine entry points rather than one so that any such site surfaces as a green answer this row must then route through the allocator, instead of being assumed away by a universal this row has not measured. **(1a) Accessor case, red at `658cc302` and separating on its own**: for an ES built-in accessor installed through `install_accessor_pair` — `%TypedArray%.prototype.buffer` is one the cell's own docstring names — the getter's `length` is `0` and the setter's is `1`, per **§18** *ECMAScript Standard Built-in Objects* ("this value is the number of required parameters shown in the subclause heading for the function description", `webref body ecma262 sec-ecmascript-standard-built-in-objects`), a getter's heading showing none and a setter's one. Both functions come out of the same `create_native_function` call pair with the same arguments, so a repair that threads one arity per installer call — rather than per allocated function — yields `length === 0` on both and fails the setter half while passing every other case here. **(2) Declared negative control**: an arity threaded only through `create_native_function` passes any single-entry-point probe and is excluded by (1) reading all nine. **Positive control from outside the allocator's own vocabulary**: `Function.prototype.bind`'s result already carries the §10.2.10 descriptor (`vm/natives_function.rs:124-138`) and is allocated straight through `ctx.alloc_object` rather than through any of the eight — so what is missing is the descriptor's *placement*, not the mechanism, and a repair that only moves bind's copy has fixed nothing. **(3) Green guard**: the existing `name` descriptor is unchanged on the same objects — §20.2.4.2 `name` and §20.2.4.1 `length` are separate clauses of Function Instances (`webref heading ecma262 20.2.4`), so a repair that defines `length` while relaxing `name` has moved a property rather than added one. Not `#11-vm-ordinary-function-name-length` (§10.2.3 step 22, the `vm/ops.rs` script-defined functions) and not `#11-vm-builtin-prototype-static-sweep` (properties defined *on an intrinsic object*): §10.3.4's own path falls between the two, which is the gap 6c measured | **now — ahead of Slice 6c**, whose `Deps` names it | 2026-09-30 |
| `#11-vm-structured-clone-layering-seam` | **UMBRELLA, not a terminal unit.** ⚠ **This slot read as terminal while stating that its deliverable is "a derivation, not a doc edit" — and a derivation that mints children is §5's definition of an umbrella rather than of a slice.** It declares three intersecting axes and the second disjunct besides: the layering mandate's admission list measured against a ~914-line WHATWG algorithm; a representation carrying prototype, own-property table, `ObjectKind`, `[[ArrayBufferData]]` and reference cycles across the seam **without exporting `VmInner`**, for which this cell measures both proposed homes shut and so names no canonical algorithm; whether the warrant generalises over the 128-file class it measures, which is CLAUDE.md *One issue, one way*; and HTML **§2.7.4** / **§2.7.6** / **§2.7.7** fidelity across whatever seam is drawn. It mints terminal children at its own start, each its own PR under its own mandatory `/elidex-plan-review`; **placing each acceptance condition, each obligation and each edge on the specific child is a mandatory output of that derivation**, and this row mints none of them and invents no ids — everything below is evidence for it to route, and a mechanism absent from this cell is un-minted, never out of scope. **(carved at PR-B, against this document's own "settled" disposition)** `vm/host/structured_clone.rs:1`-`:914` is the fused WHATWG HTML **§2.7.4** StructuredSerialize / **§2.7.6** StructuredDeserialize algorithm — `wc -l` → **1077**, `clone_value` `:75` through `data_clone_error` `:909`-`:914`, the fusion stated in the entry point's own docstring at `:63-64` — and `vm/host/mod.rs:11-18` admits three things: prototype install, brand check, and `JsValue` ↔ `Entity` marshalling. A ~914-line algorithm is none of the three, so the local contract does not admit it and the question of where it belongs is open. **Deliverable is a derivation, not a doc edit and not a move to a home that already exists**: `#11-vm-webidl-sequence-early-exit-drain`'s structured-clone half measured both *proposed* homes shut — the core `vm/` layer by §4 I-5's inbound rule, and an engine-independent crate because `elidex_plugin::JsValue` is **83** lines over an opaque `ObjectRef(u64)` while every function of the half takes `&mut VmInner` and returns `JsValue`/`ObjectId` — and those two measurements **bound** this derivation rather than discharging it, which is the correction that minted this row. What it must answer, stated so the row is falsifiable and deliberately not answered here: **(1)** what representation carries a prototype, an own-property table, an `ObjectKind`, `[[ArrayBufferData]]` and a reference cycle without exporting `VmInner`; **(2)** if none can, on what warrant a ~914-line algorithm is admissible under `vm/host/mod.rs:11-18` — written at the mandate's **SSoT**, the repository's root `CLAUDE.md` § *Layering mandate*, and not at `vm/host/mod.rs:11-18` or §4 I-5 which merely restate it, the same SSoT rule `#11-vm-typed-array-family-layering-and-gate` states for its own discharge (b); and **(3)** whether that warrant generalises, since (i) measured `structured_clone.rs` to be the **largest member** of a **128**-file class rather than an outlier from it, so a warrant reaching only this file leaves the class re-deciding it per PR — CLAUDE.md *One issue, one way*. ⚠ **Distinct from `#11-vm-structured-clone-file-split`** (Slice **F**), the behaviour-preserving `:1`-`:914` / `:916`-`:1077` split, which may not re-decide this; F is what puts the two halves in front of a reviewer separately, which is why the trigger below is F's plan-review rather than F's landing. ⚠ **Not in the ledger**: `grep -c -- '#11-vm-structured-clone-layering-seam' <ledger>` → **0**, so it joins the still-to-register set §8's registration paragraph states in general form above | with Slice **F**'s mandatory `/elidex-plan-review` — the first forum with both halves in front of it — or the next `vm/host/` layering pass — ⚠ **and this row is an umbrella, so the occasion fires its *derivation*, never a PR that would carry the work**: that derivation mints the terminal children, each its own PR under its own mandatory `/elidex-plan-review`, and re-points every ordering stated above onto the specific child that consumes it | 2026-12-31 |
| `Intl` → owned externally by [[intl-icu-deferral]] (**no slot minted here**) | governed by **ECMA-402**, not ECMA-262 — which is why it is out of this umbrella's spec scope as well as its implementation scope. Standing project decision: no ICU dependency. `typeof Intl` → `undefined` (probe at `39bbdb1b`). The row exists so the deferral is *visible in the registry* rather than only in prose | per that standing decision | per that standing decision |

**Cap accounting — by classification, not by a total.** The ratified policy counts a PR's *own*
deferrals (≤3) and separates two other categories out of that count. **The classification is what the cap reads,
and it is read off the rows by marker rather than asserted here** — no row-total is stated, because a
hand total of a growing table is the figure this section already withdrew once. ⚠ **The claim that
every row carries *exactly one* of the classifications below is withdrawn, not amended**: it was
false in both directions — four rows carried a carve spelling neither marker grep matched, and
`#11-vm-iteratorclose-precedence-convention` carries a carve marker *and* is the P family's own
slot:

- **Relocation of a disposition that already existed elsewhere in this document** — `Atomics`,
  `WeakRef`/`FinalizationRegistry` and `Intl`. Derivable: `git show 2a51106c` deletes the "**Not
  slotted**, each with its disposition: `Intl` → … `Atomics` → … `WeakRef`/`FinalizationRegistry` →
  …" paragraph from §5 and adds these three rows. They are not new deferrals; they moved out of a
  paragraph about `vm/dispatch.rs` line counts, which is not a place a deferral can be retired from.
- **Adoption of a pre-existing slot** — `#11-step9-class-extras`, `#11-dead-opcode-removal`,
  `#11-d17b-dispatch-expr-file-growth`, `#11-webidl-sequence-dense-array-fast-path`. Minted by
  earlier work, homed here. Not deferrals of any PR in this program.
- **Gate-found pre-existing defect** — everything carved by a review gate against code this program
  did not write, identified **by marker, not by a written list or count**. ⚠ **The marker has four
  spellings and the two greps that stood here matched two of them**: `carved by #489` (**5** rows)
  and `carved at PR-B` (**20**) missed `carved this round` (**4**) and `carved here` (**1**). One
  command over all four —
  `grep -cE '^\| .*(carved by #489|carved at PR-B|carved this round|carved here)' <this file>`
  → **30** at `6cf0ec1e` and **33** at this revision, the three rows between them being what the command classifies rather than anything a reader has to be told; negative control, the same command with `carved at PR-C` → **0**, so it discriminates
  rather than matching every row. The rows it returns *are* the classification. ⚠ **Every figure above is
  re-run, never carried, and the reason is measured**: the previous wording asserted
  `carved at PR-B` → **4** and named four rows, while the
  same command returned **6** at `42c80199` — `#11-vm-object-spread-source-coercion` and
  `#11-vm-arrow-lexical-new-target` were added under the marker without the list being re-run. A
  figure that must be hand-maintained alongside a growing table is the class of number this section
  already withdrew once. Plus `#11-vm-function-constructor-global`, registered 2026-07-18 at
  #474's landing, which the grep cannot reach because it carries no carve marker. *(The
  `^| ` anchor is load-bearing: an unanchored `grep` matches the mentions in this very bullet — see
  [[feedback_document-landing-invalidates-its-own-measurements]].)*
- **Own deferral** — none. The rows the marker grep above does **not** return, less the relocations
  and adoptions named above, are **slice work items**, not deferrals at all. The hand-written list
  that stood here is deleted in favour of that complement: a list maintained beside a growing table
  is the same device this section already withdrew for counts, and it was already one row short of
  the rows it was meant to classify.

**PR-B's own count is therefore 0**, by that rule: PR-B is a plan document; it writes no
implementation surface, so it can defer none. **#489's own count is 2** — the operand-rooting and
internal-error slots are its own *carves*, since it deliberately reverted work it had already
implemented ([§18.1](2026-07-vm-p4-slice-0a-landing-record.md)); ≤3 holds. Whatever the row total, the rows register as **discovery carves of
this umbrella** (the treatment the 2026-07-18 P4 registration used,
`project_open-defer-slots.md` §"VM P4 ES-language + builtin gaps",
"**Origin**: not a carve — a **discovery**"), not slice-introduced defers. Net ledger delta is
smaller than the table looks: the adoptions mint nothing, and
`#11-vm-class-instance-field-init` / `#11-vm-super-property-reference` from R1 are **withdrawn** in
favour of `#11-step9-class-extras`.

**Memo corrections at landing** — `project_vm-p4-es-language-gaps.md`:
- §2 table: add super-property total loss, public instance fields → `undefined`, `obj[k] += v`
  panic, destructuring assignment no-op, `import()` → `undefined`.
- §2 scope note: retract the `new X(...args)` "works" claim.
- §4: async generators resolved **broken**; `new.target` resolved **working in the
  direct-constructor spelling only** — the arrow case is Slice N (§1.1, §2.2).
- §line 113 + MEMORY.md: the 2026-07-18 probe is attributed to `f7d9b5ce`, whose commit date is
  **2026-07-26** — the baseline attribution is wrong and should name the then-current tip.
- SoT ledger `#11-vm-call-spread-arguments` reads "**First slice**", and MEMORY.md reads
  "slice 1 = call-spread". Slice 0a and the children umbrellas 0b / P mint all precede it (the standalone `dispatch.rs` split that
  an earlier round sequenced here was **withdrawn** — see §5). Both texts need "first *feature*
  slice" or an explicit reorder.
- Cite the discovery-carve precedent by **section heading** (§"VM P4 ES-language + builtin gaps"),
  not line number — the R1/R2 citation `project_open-defer-slots.md:139` is already off by 3
  (actual :142) because ledger line numbers drift with every registration.

---

## §9. Open decisions for plan-review

1. **Slice 0a/0b/0c admission.** All three surfaced during this re-probe / round-1 review and are
   not in the registered slot set. Recommendation: admit — 0a is a process abort, 0b a silent no-op at the baseline
   on ubiquitous syntax, 0c the I-1 discharge that makes the rest of the program honest.
2. **Emit-site aggregation** (round 2 split this from decision 2b — they are orthogonal, and R2
   round 1 conflated them). I-3 says "no per-call-site spread branch", but `ArgsForm` unifies only
   the *decision*: the emit sites that [§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) tables still each hand-write
   `match form { Flat => emit(op, argc, ic), Array => emit(spread_op) }` — the very per-site emit
   branching whose divergence caused this bug. Options: (a) one `emit_call(fc, shape, args_form, ic)`
   owning op selection, so no site names an opcode; (b) keep the branches that [§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) tables. **Recommendation: (a)**
   — it is fully compatible with keeping `CallMethodSpread`, and it is what actually discharges I-3.
2b. **Opcode family shape.** Given (a), does the spread family stay four variants
   (`CallSpread`/`CallMethodSpread`/`NewSpread`/`SuperCallSpread`) or collapse to a sentinel `argc`
   on the existing opcodes? Round-2 review notes the variants' only difference is how many slots
   below the args array they read (0/1/2) — data the emit site knows statically — which is the
   lesson-#276 variant-vs-data shape, and that the sentinel is structurally safe (both IC accesses
   are bounds-checked, so an out-of-range sentinel reliably misses). Recommendation: **keep the four
   variants** (dispatch handlers genuinely differ in stack shape; a sentinel overloads a
   width-constrained operand with a mode bit) — but the burden of proof is now on the family, so
   ratify explicitly. ⚠ **The enumeration is of *opcodes*, not of *call shapes*.** §3's
   ChainEvaluation step-3 row adds a fifth shape — the optional call whose callee is a member
   Reference (`o.m?.(…)`, `o?.m?.(…)`) — which this decision's own criterion assigns to the existing
   **`CallMethodSpread`** rather than to a fifth variant: its stack shape is
   `[receiver callee args_array]`, identical to `o.m(...a)`, and "dispatch handlers genuinely differ
   in stack shape" is the whole justification for having variants at all. What the shape needs is an
   **emit-site selection** change ([§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md)'s table), not an opcode. Edges 33/34 guard it.
3. **Array-literal path sharing.** `expr.rs:105-119` emits the same sequence but handles elisions
   ([C22] ≠ [C19]). Recommendation: keep separate.
4. ~~Call-IC on spread calls~~ — **resolved** in [§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) (monotonic counter; skip allocation).
5. **0ca: throw vs `CompileError` — RESOLVED (runtime throw), ratified by evidence in Slice 0a.**
   A `CompileError` fails the whole script; a thrown `TypeError` scopes the failure to the
   expression, matching how the feature would fail if half-implemented. Slice 0a shipped the
   `CompileError` form first and the post-push gate showed the cost concretely: one
   `this.#x = 1` anywhere took **every unrelated statement in the file** down with it, making it
   *worse* than the silent-no-op it replaced for `=` and `+=`. So: **runtime throw** for
   unimplemented *expressions*, `CompileError` reserved for what the compiler already rejects
   and for parse-time rejections (dec. 15). ⚠ **The illustration this decision used to carry —
   "e.g. computed accessor keys" — was falsified by PR-B and is replaced.** Computed class accessor
   keys *work* (`let k='p'; class C { get [k]() { return 7 } }; (new C).p` → `7`); the cited guard is
   unreachable dead code (§2.2). The surviving measured reject is a **BigInt literal key**:
   `class C { get 1n() { return 7 } }` → `CompileError "unsupported class member key type"`, while
   `class C { get 1() {…} }` succeeds. Note that this reject is **itself a divergence** — ECMA-262
   **§13.2.5.5** evaluates `LiteralPropertyName : NumericLiteral` as `ToString(NumericValue)`,
   yielding the String `"1"` — so it illustrates the
   *mechanism* (`CompileError` is for what the compiler already rejects), not an endorsement of the
   behaviour; the child of umbrella 2 that owns class-member key lowering owns making it correct — the owner both structured homes name for that row's two *reachable* arms (§2.2's `compiler/expr_class.rs` row's Slice column, and `#11-vm-property-key-lowering-unification`'s own ownership clause, which takes from that row only the dead `if computed` guard) ⚠ **this read `#11-vm-property-key-lowering-unification`, a surviving copy of the over-read that slot's cell already records withdrawing**.
   **Mechanism**: `Op::ThrowUnsupported` (u16 constant index → `TypeError` with that message),
   following `Op::CheckTdz`'s precedent of an engine-constructed error raised through
   `throw_error`, so the failure is catchable and local. Note this is a deliberate, user-visible
   behaviour change: code that today silently gets `undefined` will start throwing.
6. **Slice 1 breadth — RESOLVED (split adopted).** §3 is M=32 ⇒ preflight **SPLIT-DEFAULT** (escalated
   from SPLIT-RECOMMENDED as rounds 2-4 added step-level and `n/a` rows; scope unchanged), and
   round-2 review correctly noted the §3 appeal only refutes a split **by call shape**, leaving a
   **compiler/VM seam split** untested. Resolved against splitting, on the umbrella's own
   invariants:
   - **VM-seam-first (PR-A = VM, PR-B = compiler) violates I-4.** PR-A would land `CallSpread`,
     `NewSpread` and `CallMethodSpread` handlers with **zero compiler emit sites** — three dead
     opcodes, the exact "third state" I-4 forbids and the exact defect class §2.1 says this program
     exists to discharge. Converting `op_super_call_spread` to the shared helper gives *the helper* a
     live consumer but does nothing for the three handlers.
   - **Compiler-first is strictly worse** — it would emit opcodes whose handlers are still the
     `pop; pop; push undefined` stubs, i.e. ship a *new* silent-wrong path (I-1).
   - **Shape-split violates I-3** — half the call shapes migrated, half on the broken
     `compile_arguments`, which is the precise strangler shape that caused this bug (`super` correct,
     four callers broken).
   **✅ RESOLVED 2026-07-26 (user decision): adopt the 4th seam as a prereq PR.** Slice 1 splits into
   **1a (VM infrastructure — no call-shape change + two named semantic fixes, decs. 13a/10)** and **1b (compiler + opcodes + handlers)** — see
   §5. This answers SPLIT-DEFAULT by *actually splitting* rather than by overriding the gate at
   author altitude, and it halves a Slice-1 load that had grown across four review rounds. The
   history below is retained because it records why three earlier seams were rejected — those
   rejections still bind (in particular, splitting **by call shape** remains forbidden by I-3).

   ⚠ **Round 4 showed the earlier enumeration was incomplete.** Rounds 3 and 4 both identified a
   **fourth seam** that violates *none* of I-1/I-3/I-4: a **VM-infrastructure prereq PR** (no call-shape
   change; it carries two named semantic fixes, decs. 13a/10, each with its own edge row) containing `lay_out_call_args` + the `Empty` normalisation + converting
   `op_super_call_spread` to consume it + correcting its falsified docstring (the
   `expr_class.rs:145-152` producer is **spec-required** and is NOT folded — [§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) / I-3 carve-out) +
   the dec.-11 `Option<usize>` refactor of `ic_call`/`ic_call_method` + the two semantic fixes decs. 13a and 10. It lands **zero
   opcodes** (no I-4 third state), adds **zero
   emit paths** (no I-1 silent-wrong), and leaves every call shape on the unchanged
   `compile_arguments` (no I-3 strangler). `op_super_call_spread` is a live consumer today, so the
   helper is not dead on arrival. Decision 6's earlier text conceded the seam exists and then
   dismissed it on "does nothing for the three handlers" — a *completeness* criterion, not the split
   criterion. *(This decision originally cited §5's acceptance of the `vm/dispatch.rs` prereq split as
   precedent for the same shape. §5 now withdraws that split, so the precedent is gone; the seam
   argument below stands on its own and is what the decision rests on.)*

   Aggravating: Slice 1's load has grown across four rounds to helper + `emit_call` aggregation
   (dec. 2) + new opcode + 3-4 handlers + `lay_out_call_args` + `Empty` normalisation +
   `op_super_call_spread` conversion + docstring fix + IC refactor (dec. 11) +
   `op_array_spread` `return()` removal (dec. 13a) + GC rooting (dec. 10) + stack-depth guard
   (dec. 12) + the arity-selection rework + `(o.m)()` regression guard (dec. 14).

   The lens therefore stopped converging, and per
   `.claude/skills/elidex-plan-review/SKILL.md` the SPLIT-DEFAULT verdict was escalated to the user,
   who **adopted the 1a/1b split** (recorded at the head of this decision).

   ⚠ **And the generic path must not be collapsed onto the dense one (Codex R5).** Routing
   `Reflect.apply` through `collect_array_like`'s dense fast path is observably wrong for a sparse
   Array: that path reads `elements` directly and maps `Empty` to `undefined`, so it skips own indexed
   accessors and inherited numeric getters. ECMA-262 **§28.1.1** delegates the argument list to
   **§7.3.19 `CreateListFromArrayLike`**, which performs a real `Get` per index — a getter on
   `Array.prototype[0]` must run for `Reflect.apply(f, null, new Array(1))`. **Ratified as §9
   decision 17** (owners named there) rather than left as an either/or — an unratified either/or in
   §9 is invisible to the slice author reading §5, which is the reason dec. 11 was closed.

   What holds regardless: splitting **by call shape** is forbidden (I-3), and **the 1a/1b boundary this
   decision adopted stands** — nothing below reopens where the seam falls.

   ⚠ **AMENDED: both halves are themselves UMBRELLAs (§5's rows); neither 1a nor 1b is a terminal unit.** This decision
   settled the **breadth** axis — VM infrastructure against compiler + opcodes — and settled it
   correctly; the terminality criterion applied to **each** half runs on a **different axis**, over the
   mechanisms that half contains, and returns umbrella for both. §5's 1a row carries one derivation: an
   argument-layout and inline-cache contract, an *observable* iterator-closing correction (dec. 13a),
   and GC root lifetime across four call-entry windows (dec. 10), each with an assertion the other two
   pass — edge 32, edge 21, edges 27/28 respectively. §5's 1b row carries the other: five roots that
   intersect at one argument-emit helper and share no chokepoint below it, so following this decision
   as it stood would let all five ship as one PR. So the sentence this paragraph used to end on —
   *"each of 1a/1b remains a terminal unit under an approved umbrella (per-PR slices touching one
   subsystem do not re-trigger splitting, else infinite regress)"* — is corrected in **both** halves.
   ⚠ **The parenthetical it quoted from CLAUDE.md clause (c) is NOT weakened**: a slice does not become
   an umbrella by touching one subsystem, and a reading that made every per-PR slice re-trigger the
   split was implemented in this program, falsified against that very parenthetical, and reverted. What
   moves each half is the mechanism measurement on that half — three on 1a, five on 1b — and
   neither reaches any other row.
7. **`Function`/`eval` gate.** I-6 supplies **half** of it — ⚠ *this entry read "I-6 does not supply it
   (strict `eval` is core)", which is I-6's note with the word* direct *dropped, the same undivided
   reading §5's pointer row carried*: strict **direct** `eval` is core, while the **sloppy half** of each of the three dynamic-code forms — direct `eval`, indirect `eval` and the global `Function` constructor — is I-6's to keep out of core, the strict half of each being core: I-6 draws the line on the strictness of the **evaluation**, not on the spelling and — ⚠ **on the direct path** — not on the strictness of the parsed source either, which is what this entry said for one revision: **§19.2.1.1** PerformEval **step 12** makes a strict caller's `eval("var x = 1")` a strict eval and so core's however the source reads, while **step 1**'s assert leaves indirect `eval` and `Function` with no `strictCaller`, so on those two alone the criterion reduces to source strictness. So "indirect `eval` and `Function` are sloppy by construction" is superseded there and here. What
   I-6 does not supply is the remaining question, a security/CSP policy decision — ⚠ **and that is one of two questions, not the whole of it**: the
   ECMA-262 **§19.2.1** / **§20.2.1.1** algorithms are code, they are joined to this decision only at
   **§19.2.1.2**'s host hook, and §5's `Function`/`eval` pointer row carries that split and the security
   layer's home for the policy half. Recommendation: keep sequenced last; decide the policy in its
   own memo. R1 marked the row "decision first" but gave the decision no owner.
8. **`collect_array_like` sharing.** `vm/natives_function.rs:272` already unpacks an array to call
   args with the correct `Empty` normalisation, and is the natural home for the child umbrella 10a mints for
   `Reflect.apply`. Semantics differ (spread's array is compiler-guaranteed dense; `apply` is
   generic array-like per CreateListFromArrayLike). Share the dense fast path or keep separate with
   a stated reason? **Recommendation (added round 8 — this was the only §9 entry with none, and it
   sits inside the 1a half's deliverable — specifically the child of umbrella 1a that owns the argument-layout and inline-cache contract): share.** `collect_array_like` already has the correct
   `.or_undefined()` normalisation that child needs, `Reflect.apply` (that child of umbrella 10a) will home there, and
   `call_internal` — one of dec. 10's four unrooted windows — is already reached at N≫1 through it,
   so sharing puts all of that on one audited path. The array-like/dense distinction stays a
   *caller-side* precondition, not a second helper.
9. **Slice 0ca sweep method — RESOLVED (executed, not stipulated).** The three-pass sweep is now
   documented and **run** at the head of §2.2, and §2.2 is its output. It earned its keep
   immediately: it found 3 defects (`(x)++`, `(x)+=1`, `(a[0])++`) that the probe, R1, R2 and
   round-2 review had all missed, and it corrected a round-2 claim (`delete x` *is* parser-gated).
   **0ca's acceptance criteria**: (a) re-run **all three passes** — pass 3 at **both** variant and sub-arm-body granularity — and classify every hit, so the arm list is reproducible rather than inherited; (b) §7.2 carries **one row per §2.2 defect row** as its
   row-derivation rule — the table's completeness claim is then anchored to the sweep rather than to
   authoring instinct.
10. **Generator/async callee GC window — root it in the child of umbrella 1a that roots the four call-entry argument windows, via that umbrella's derivation, which mints it; carve withdrawn. ⚠ REMEDY CORRECTED
    (round 8): `gc_enabled` bracketing is WRONG for the async path.** Round 4 chose bracketing as
    "the only remedy-complete-by-construction option". Verified in round 8 that it cannot be applied
    at the site [§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) named: `interpreter.rs:893` is not an allocation but
    `make_async_coroutine_and_drive` (`vm/natives_generator.rs:545-571`), which allocates the wrapper
    Promise + Generator, back-links at `:562-566`, **then calls `drive_async_coroutine` (`:569`),
    running the async body until its first `await`**. Bracketing that region would hold
    `gc_enabled = false` across arbitrary user JS on every async call — and the in-tree contract says
    so explicitly (`natives_generator.rs:489-490`: the async driver step "does not save/restore
    `gc_enabled` (user JS inside the resumed body needs GC to keep running)";
    `interpreter.rs:1246-1250` calls a persisted `false` a hazard that "would silently disable GC").
    **The real unrooted window ends at `natives_generator.rs:566`** — inside the callee, before the
    drive. So the remedy is either (a) push the bracket down into `make_async_coroutine_and_drive`
    so it closes before `:569`, or (b) **root** `stack_slice` + `actual_args` rather than suppress
    GC. (b) is preferred: it is remedy-complete for both vectors without any GC-suppression window.
    Edges 27/28 must assert **both** vectors and must not pass against a partial fix.

    **⚠ The window has 4 sibling sites, not 2** (round 8): `call_internal`
    (`vm/interpreter.rs:629-659` async, `:664-700` generator) has the identical
    `stack_slice = self.stack.drain(base..).collect()` + `actual_args: Some(args.to_vec())` shape,
    and is **already reached at N≫1 today** via `Function.prototype.apply` → `collect_array_like`
    (uncapped for a real Array) — the very function dec. 8 proposes to share and the child umbrella 10a mints for
    `Reflect.apply` will home there. dec. 10's own justification (a documented-contract violation
    does not qualify for deferral) applies verbatim. Enumerate by command
    (`grep -n "drain(base\.\.)"` / `SuspendedFrame {`) and root all four windows in one unit.

11. **IC entry point** ([§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) IC): `Option<usize>` refactor of `ic_call`/`ic_call_method`
    (recommended, one SoT) vs. duplicating the three-way body. ⚠ **Round-8 caveat**: both callers
    (`dispatch.rs:719/730`) pass `Some`, so the `None` arm has **zero live producers until the child umbrella 1b mints for `CallSpread`** — a
    dead branch wherever the refactor lands inside the 1a half, which is the same I-4 "third state"
    argument dec. 6 used to reject VM-first,
    one layer down. Either move dec. 11 to the child umbrella **1b** mints for `CallSpread` (its producer), or pair it with a
    direct unit test on `ic_call(.., None)` so the branch is exercised where it lands.
    **RESOLVED (Codex R1 re-raised it): the second option.** Moving dec. 11 to that child is not free —
    §5's 1a row records `dispatch.rs`'s two callers as the *only* callers of `ic_call`/`ic_call_method`,
    and re-signaturing them is what lets the layout work compile at all. So the refactor stays with
    the child of umbrella 1a that owns the argument-layout and inline-cache contract, and **a direct
    unit test on `ic_call(.., None)` is a required acceptance item of that child**, not an optional one;
    without it that child ships exactly the dead third state I-4 rejects. ⚠ **The owner is the child and
    not `1a`, which is an umbrella from this revision and names nobody as an owner (§5)** — placing this
    item is a mandatory output of its derivation. Ratified rather than left open, because an
    unratified either/or in §9 is invisible to the slice author reading §5.
12. **Stack-depth guard — RESOLVED (a limit is required, and it belongs to the child umbrella **1b**'s derivation mints for the argument-emit helper).** Verified 2026-07-26:
    `grep -rn "MAX_STACK\|stack_limit\|call_depth\|MAX_FRAMES" crates/script/elidex-js/src/vm/` →
    **0 hits** — the crate has *no* stack or frame bound at all. `spread_iter_loop` caps the
    *array* at `DENSE_ARRAY_LEN_LIMIT` (1<<27, `vm/ops.rs:24`) but nothing caps the `vm.stack`
    push side, so once [§6.3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) routes >255 args through the Array path, `f(...arrOf10M)` becomes an
    unbounded user-driven push — **reintroducing a T0 in the fix for a T0**. The child umbrella 1b's derivation mints for the
    argument-emit helper must add a bound (and edge 29 must assert it, with an expected value), and
    placing both on that child is a mandatory output of the derivation — ⚠ *this read "Slice 1b must
    add a bound … a **1b blocker**", and an umbrella carries no obligation and blocks nothing.*
13. **[§6.2a](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) — the reused drain's `IteratorClose` divergence — RESOLVED as two units.** ⚠ **The "Round-8
    sequencing hazard" is withdrawn with the premise it rested on**: the child umbrella 0bc mints for the binding-pattern rest path owns [C39], whose rest form
    (`[a, ...rest] = it`) needs a drain-into-array and whose spec **requires no `IteratorClose` on
    that path** — **§13.15.5.5** IteratorDestructuringAssignmentEvaluation, production
    `AssignmentRestElement`, step 4, repeats *while* `iteratorRecord.[[Done]] is false` and performs
    no close — so the child of umbrella 1a that removes `op_array_spread`'s `return()` **deletes** a
    call that child's rest path must not make either, and there is no tension between them to sequence
    around. ⚠ **This clause read "`op_array_spread`/`spread_iter_loop` is the only in-tree
    drain-into-array"; that is false and the instruction built on it is withdrawn.**
    `compiler/stmt_destructure.rs:70` emits `Op::IteratorRest` for an array rest element and
    `vm/dispatch_iter.rs:295-332` implements it as a drain-into-array. So that child **repairs and consumes
    `IteratorRest`** rather than hand-rolling a second drain or adding an explicit `iter_close` to the
    rest path — **§13.15.5.5** `AssignmentRestElement` repeats only while `[[Done]] is false` and
    performs no close, so a `return()` after normal rest exhaustion is a user-visible divergence and
    the no-`return()` regression in [§6.2a](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) is what pins it. Whatever `IteratorRest` shares with
    `ArraySpread` is factored out rather than duplicated (*One issue, one way*). ⚠ *An either/or stood here — "0bc routes through it … or 0bc hand-rolls a second
    drain" — and is withdrawn: it survived the correction one sentence above it and re-permitted
    exactly the second drain that correction forbids.* There is no second branch. That child repairs and
    consumes `IteratorRest`, and the 1a child's `return()` removal does not falsify that child's [C39]
    claim: [C39]'s close is the `[[Done]] is false` conditional around early termination and abrupt
    pattern evaluation (**§13.15.5.2**), which the exhausted-rest path never reaches. ⚠ *This clause opened "§5 lands 0bc before 1a"; the
    ordering half is deleted rather than re-pointed — no `Deps` cell carries that edge and ordering has
    one structured home (§5, `#11-plan-memo-spec-field-single-home-check`) — while the hazard it names
    is real and stated over the artifact instead.* State in §5/[§6.2a](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) that once that child lands, `op_array_spread` is
    **[C19]/[C22]-only**, have that child **repair and consume `Op::IteratorRest`** — the rest drain that
    already exists (`compiler/stmt_destructure.rs:70`, `vm/dispatch_iter.rs:295-332`) — **with no
    `iter_close` on the exhausted-rest path**, and pin both with a §7.2 row. ⚠ *This clause read
    "give 0bc's rest path its own explicit `iter_close` site"; it is withdrawn twice over — a second
    drain beside `IteratorRest`, and a `return()` after normal exhaustion that **§13.15.5.5** forbids.* (a) Removing
    `op_array_spread`'s `return()` call belongs to **the child of umbrella 1a that removes `op_array_spread`'s `return()`**, via that umbrella's derivation, which mints it — it is the reuse precondition and edge 21
    fails without it. (b) The **error-precedence inversion is a separate, crate-wide unit** ([§6.2a-2](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md):
    a cross-cutting site set — [§6.2a-3](2026-07-vm-p4-slice-1a-1b-call-spread-detail.md) is the authoritative figure and carries its
    derivation — incl. the canonical `iter_close` helper, whose docstring states the wrong rule as its
    contract; the correct behaviour is completion-kind-dependent so the signature must change) →
    carve **`#11-vm-iteratorclose-precedence-convention`**, sequenced **before the children umbrella 0bc mints**, whose
    [C39]→[C36] conformance claim otherwise inherits the inverted contract.
14. **`(o.m)(...)` `this`-loss — owner needed.** `compile_call_expr` (`expr_member.rs:92`) matches
    `ExprKind::Member` on the **raw** callee, and `ExprKind::Paren` is a real AST node
    (`ast.rs:299`), so `(o.m)()` takes the plain-call branch and loses `this` — a live T1
    silent-wrong sharing the non-normalised-Paren root, inside the very function 1b rewrites.
    Coupling: §5 puts Paren normalisation in **0bb's** scope, so if 0bb normalises
    globally this is fixed as an **unrecorded side effect** of a slice scoped to assign/update
    targets; if 0bb scopes narrowly, the children umbrella 1b mints ship the bug intact. Recommendation: one `peel_paren`
    chokepoint owned by **0bb**, with `(o.m)(...)` an explicit 0bb deliverable + §2.2 row, and
    edge 31 as the regression guard of the child umbrella 1b mints for the call-emit shape. ⚠ **Made operative (Codex R1)**: as written, edge 31 was
    allocated to 1b alone, so 0bb could land the receiver-binding fix it owns with **no test asserting
    `this === o`**, and the regression would surface only in a later slice that rewrites the same call
    compiler. `(o.m)()` asserting `this === o` is therefore **part of 0bb's own acceptance suite**; that child
    keeps edge 31 as its regression guard.
15. **`expr.rs:186-189` prefix `Spread` — layer mismatch.** §2.2 originally assigned it to the I-1 discharge, whose rule (§9
    dec. 5) is *runtime throw*; but `var y = ...x` is an **early SyntaxError** per spec, so a runtime
    throw lets preceding side effects run and lets `if (false) { var y = ...x }` execute the whole
    script. Root is parser-layer: `parser/expr.rs:256-263`'s `Ellipsis` arm is **ungated** (its own
    comment says "in arguments/array context" but no context check exists). Recommendation: reassign
    to **0ba** (which owns `parser/expr.rs`) as a parse-time rejection, not 0ca.
16. **§7.2's conformance-row outcome type — RESOLVED (crash-aware outcome), owner Slice 0cb.** The
    table's outcome type expresses `Panics` / `Throws` alongside a value, so `f(a×256)` — a process
    abort owned by the child umbrella 1b mints for the argument-emit helper — has a writable row before that child lands. The alternative (sequence the arity fix
    ahead of the table) is excluded by this document's own ordering rather than by taste: the fix is
    the responsibility of the children umbrella **1b** mints, and §5 puts the edge on the child umbrella 1b mints, consuming the child of umbrella 1a that produces
    `lay_out_call_args`, so hoisting it inverts that edge. ⚠ **This sentence also cited "1a `Deps: 0cb`" until PR-B; that edge was deleted
    at `b3eb07c1` under §5's rule** — the children umbrella 1a mints consume no artifact 0cb delivers, and the reason offered for
    it ("every later slice inherits a baseline") is one **no** Deps cell in §5's table carries:
    `0cb` occurs in no cell of that column (control, same splitter, same run: `0ca` occurs in one and
    `1a` in one). The decision is unaffected, and ⚠ **the "0cb-before-1a is a ship-order fact, not a Deps
    edge" framing is withdrawn with it** — it reinstated the deleted edge in prose. Ordering has **one**
    structured home, the `Deps` column (`#11-plan-memo-spec-field-single-home-check`, §8), and the
    sequencing paragraph in §5 defers to it by name rather than carrying a second copy, so an ordering
    assertion outside the column is neither enforceable nor readable: nothing consults it. And none is
    needed here. **The children umbrella 1a mints landing first breaks nothing**: their acceptance is stated over their own artifacts —
    the existing suite plus edges 21/27/28/32, which umbrella 1a's derivation places one per child —
    and turns true whether or not the conformance table
    exists, which is the same measurement that deleted the edge. What 0cb buys later slices is a
    baseline they are *measured against once it exists*, not a precondition they wait on. Was an unowned either/or in §7.2; ratified here so 0cb's author
    reads a decision, not a fork.
17. **Dense vs generic argument unpacking — RESOLVED (keep them separate), owners the child of umbrella 1a that owns the argument-layout and inline-cache contract and the child umbrella 10a mints for `Reflect.apply` (§28.1.1).**
    **Umbrella 1a's child** shares `collect_array_like`'s **dense** fast path (dec. 8), and may, because the arrays it
    unpacks are compiler-emitted and dense by construction. **The child umbrella 10a mints for `Reflect.apply` must NOT reuse that fast path**: ECMA-262 **§28.1.1** `Reflect.apply` delegates the argument list to
    **§7.3.19** `CreateListFromArrayLike`, which performs a real `Get` per index, so a getter on
    `Array.prototype[0]` must run for `Reflect.apply(f, null, new Array(1))` while the dense path
    reads `elements` directly and maps `Empty` to `undefined`. Making the generic path do property
    lookup is therefore **that child's deliverable**, settled at that child's own mandatory plan-review; the
    array-like-vs-dense distinction stays a **caller-side precondition** (dec. 8), not a second
    helper. §-numbers verified via `webref heading ecma262 28.1.1` /
    `webref aoid ecma262 CreateListFromArrayLike`. Was an unowned either/or inside dec. 6.

---

## §§10-15. Round-2 … round-7 review dispositions + the convergence call → **carved out**

Moved **verbatim** to `docs/plans/2026-07-vm-p4-umbrella-review-rounds.md` by this document's
touch-time prereq split. `§10` (round 2), `§11` (round 3), `§13` (round 5), `§12` (round 4 +
convergence state), `§14` (rounds 6-7 + the convergence series) and `§15` (the convergence call) keep
their numbers there — including the out-of-order `§13`-before-`§12` the archive was written in — and
every reference to them in this document links to that file.

---

## §§16-18. Slice 0a — landing record and its two review gates → **carved out**

Moved **verbatim** to `docs/plans/2026-07-vm-p4-slice-0a-landing-record.md` by this document's
touch-time prereq split. `§16` (implemented and merged as `658cc302`), `§17` (the pre-push
`/elidex-review` findings) and `§18` (the post-push `/external-converge` and the design re-gate it
triggered, with `§18.1` the carve and `§18.2` the converge loop on the carved head) keep their
numbers there, and every reference to them in this document links to that file.
