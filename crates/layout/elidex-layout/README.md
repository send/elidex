# elidex-layout

Layout algorithms (block, inline, flexbox) for the elidex browser engine.

Implements CSS box layout, including block formatting contexts, inline layout,
and the flexbox algorithm, operating on the ECS-based DOM tree.

See the [workspace README](../../README.md) for project-wide details.
