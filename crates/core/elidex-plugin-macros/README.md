# elidex-plugin-macros

Procedural macros for the elidex plugin system.

Provides derive macros (e.g., `SpecLevel`) to reduce boilerplate when
implementing elidex plugin traits.

See the [workspace README](../../README.md) for project-wide details.
