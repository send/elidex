# elidex-parser

HTML/XML parser for the elidex browser engine.

Wraps html5ever to provide a streaming, spec-compliant HTML parser that
integrates with the elidex plugin system.

See the [workspace README](../../README.md) for project-wide details.
