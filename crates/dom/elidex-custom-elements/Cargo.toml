[package]
name = "elidex-custom-elements"
description = "Custom Elements v1 registry and reaction queue for elidex"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true
keywords = ["browser-engine", "web-components", "custom-elements"]
categories = ["web-programming"]

[dependencies]
elidex-ecs.workspace = true
# `collect_upgrade_candidates` queries the hecs world directly so the
# define()-time candidate walk covers detached entities — the per-entity
# `CustomElementState` component is the only "awaiting upgrade" record.
hecs.workspace = true

[lints]
workspace = true
