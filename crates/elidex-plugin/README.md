# elidex-plugin

Plugin system traits and registry for the elidex browser engine.

Defines the core extension points (`CssPropertyHandler`, `HtmlElementHandler`,
`LayoutModel`, `NetworkMiddleware`), specification-level enums, and a generic
`PluginRegistry` for static and dynamic handler dispatch.

See the [workspace README](../../README.md) for project-wide details.
