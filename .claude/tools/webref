#!/usr/bin/env python3
"""webref CLI — see _webref/ package for implementation.

Backs the Axis 4 (Spec citation discipline) verification recipe in
.claude/skills/elidex-review/axes.md. Resolves section numbers, anchor IDs,
WebIDL fragments, concept dfn anchors, element-to-interface mappings, CSS
property metadata, and section prose against w3c/webref + tc39 biblio.
"""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
from _webref.cli import main  # noqa: E402

main()
