# GENERATED by `python3 .claude/tools/plan-memo-umbrella-check.py --write-manifest` -- do not edit by hand.
# One line per mutation row, control and case of the self-test; `--self-test`
# fails on ANY difference.  Adding, removing or changing a row, control or case --
# a control's body, docstring or comments included -- requires regenerating this
# file and committing its diff (the rule's home is the module docstring of
# plan_memo_selftest_manifest.py).
CASE	KNOWN-MISS	purely numeric id with no row noun	0f6997fb798c0545
CASE	KNOWN-MISS	undecorated single letter with no row noun	bccebc3d20eb5e87
CASE	NEGATIVE	(R23 kind) a field that spells the phrase CLEANLY and straddles a blank elsewhere is no miss: both readings say undetermined, so the census is not in doubt -- the DISAGREEMENT is the other conjunct	964bcc91b46cf5f4
CASE	NEGATIVE	(R23 kind) a kind phrase QUOTED WHOLE is no miss: the two readings differ about it, and that difference is I-A's disposition (a quoted phrase declares nothing), not a could-not-scan -- the STRADDLE is the conjunct that tells them apart	a9d8a18d63c9394d
CASE	NEGATIVE	(R23 memo) exactly ONE U+FEFF is dropped: a second is an ordinary character of the document and the table is lost to it again -- a strip that is a `lstrip` rewrites the document instead of reading its encoding	746415a0d32fd812
CASE	NEGATIVE	(R23 render) `Slice Q&ast;x owns it` names NO row `Qx`: the reference renders a character, so the span is a BLANK and bounds the two sides -- dropping it (the disposition for a construct that renders nothing) would join them into an id the document does not spell	7ecd61037008e023
CASE	NEGATIVE	(R23 render) `The &ast; marks it` names NO row `ast`: the reference renders `*`, so the letters `a` `s` `t` are nowhere in the document a reader reads -- leaving the entity's SOURCE spelling in the stream fabricated the site	1855b8af79d8f3cd
CASE	NEGATIVE	(R23 seed) the same phrase spelled CLEANLY in prose seeds nothing -- the discriminating half: the residue is the disagreement, never the phrase	e7bce729184ae742
CASE	NEGATIVE	(R24 render) `9z-notes.md owns it` names nobody: no whitespace renders anywhere in the run, so both readings agree that the file name swallows the id -- the file-name disposition is not weakened by moving it onto the rendered text	69832c54120bb0df
CASE	NEGATIVE	(R24 render) ``**`x` 9z**7z owns it`` names nobody: a READER sees the document bolding `x 9z`, which is prose and no decorated id, so the delimiters drop and the sides join.  This is the control over WHICH rendering the exception is asked of -- the checker's own disposed stream would show `9z` beside blanks, whitespace-separate into an id-only run, and keep a decoration the document does not have	204a14c61dbfa1fc
CASE	NEGATIVE	(R24 width) a field that merely MENTIONS a sibling and then declares itself attributes nothing: the discrimination is the DASH between the id and the marker, never the distance -- so removing the window does not widen the attribution	923bd0682c37a3d6
CASE	NEGATIVE	(R24 width) a row noun standing between the licensing phrase and the id does not hide the phrase (`the child of Slice 9z`), and NOT because the rule has a clause for one: the ANCHORED reading of that site starts AT the noun and wins the dedup, so the text before the mention is `the child of` either way.  This is the control the deleted `_TRAILING_NOUN` substitution was believed to be needed for	2d0065a737d3a999
CASE	NEGATIVE	(R24 width) the same sentence with the phrase actually AT a word boundary is licensed -- the discriminating half: only the character in front of `derivation` differs, and it is the one the lookbehind exists to read	e8e30b8fdf5363c3
CASE	NEGATIVE	(R25 sibling) `ax%22notes.md` is no sibling: the decoded `ax"notes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	13ff46e1b9577cd8
CASE	NEGATIVE	(R25 sibling) `ax%2Anotes.md` is no sibling: the decoded `ax*notes.md` holds a character Microsoft's "Naming Files, Paths, and Namespaces" lists among those a file name may not use.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	ee32b2e02f28e9c6
CASE	NEGATIVE	(R25 sibling) `ax%3Anotes.md` is no sibling: the decoded `ax:notes.md` opens an NTFS ALTERNATE DATA STREAM there -- `notes:child.md` is the stream `child.md` of the file `notes` -- so where the stream exists reading it SUCCEEDS and yields another file's content, the very class the DOS devices were rejected for.  This is the member R25 reported, and the one that falsified the `OSError` argument the other six rested on.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	0f95bc558a0c26fd
CASE	NEGATIVE	(R25 sibling) `ax%3Cnotes.md` is no sibling: the decoded `ax<notes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	86d4fe7dfb414913
CASE	NEGATIVE	(R25 sibling) `ax%3Enotes.md` is no sibling: the decoded `ax>notes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	28e3de6aa55dc611
CASE	NEGATIVE	(R25 sibling) `ax%3Fnotes.md` is no sibling: the decoded `ax?notes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	0f9b52c642d6937c
CASE	NEGATIVE	(R25 sibling) `ax%7Cnotes.md` is no sibling: the decoded `ax\pnotes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	73ddafc10bf276aa
CASE	NEGATIVE	(R25 sibling) the character rule is read per PART, as the device rule is: `ax%3Ab%2Fchild.md` (`ax:b/child.md`) puts the colon in a NON-FINAL component -- with a final-component-only reading it passes while `ax:b.md` is refused.  ⚠ The colon must sit in the DIRECTORY: `sub/ax:b.md` puts it in the last part, where that reading refuses it too and R22's per-part mutant SURVIVES (measured)	6c2fe423c9f70f68
CASE	NEGATIVE	(R25 §2.4) `Slice\\\\` + a line ending is a LITERAL backslash (the first escapes the second) and THEN a soft break, so a rendered character stands between the noun and the id and no row is named -- the discriminating half: a fix that widened the §2.4 escape to swallow any backslash before a line ending, or that dropped the backslash without asking whether the escape above had already consumed it, would pass the R25 control and fail this one	706f9ba957f81faf
CASE	NEGATIVE	(R26 file) `(m.md(9z)md).md`: the run is balanced ACROSS its groups, so it is one name and not two tokens with the id exposed between them -- the second shape the flat arm could not read, and the one that leaks an id rather than a parenthesis	e7e41e9e16963508
CASE	NEGATIVE	(R26 file) `a.md+9z+b.md` is ONE name ending at the LAST suffix, not the first: leftmost-LONGEST, as a pattern would have read it.  Stopping at the first `.md` would leave `9z` standing outside the token.  ⚠ The first fixture written for this clause was `a.md.9z.md`, and it was NOT discriminating -- an id directly before `.md` is a DOTTED NUMBER to the id grammar and no site either way, so both readings reported 0 and the mutant survived.  The id has to be separated from the second suffix	df6d50678aba6347
CASE	NEGATIVE	(R26 file) `foo((9z)).md` is ONE file name: nested balanced parentheses are §6.3's rule at any depth, so the id inside the name is no naming site.  Until R26 the token arm matched a FLAT chunk only, could reach no further left than the bare `.md`, and reported `9z`	aae3f35c65167bbe
CASE	NEGATIVE	(R26 file) `foo(9z).md`, the FLAT partner, green before the fix: a single balanced pair always read as one name, which is what says the two controls above are about NESTING and not about parentheses as such	bb1f30aabd31c05e
CASE	NEGATIVE	(R26 §6.3) a destination nested 33 deep is NOT a link, so `[x](...)` is literal text and no memo is looked up: rc 0.  ⚠ This is a DIVERGENCE from commonmark.js, which reads it as a link, and an agreement with cmark; the spec permits both and the vendored corpus reaches depth 2, so nothing but this control says where the boundary is	ce811881d09543f6
CASE	NEGATIVE	(R27 noun) `Unlike Slot `#11-zz-alpha`, **UMBRELLA, …**` does NOT attribute -- the new noun inherits the DASH discrimination rather than widening what a mention is. The discriminating partner: a fix that anchored on the noun alone would pass the row above and fail this one	527ae34f48f6eade
CASE	NEGATIVE	(R30 id) an id cell `**—**` is STILL a deliberate blank: here the `**` pair closes (§6.2), and paired decoration around a blank is what the strip was right about.  The partner that bounds the fix's reach: refusing decoration outright would pass all six above and fail this one	128da236b8d5b5fa
CASE	NEGATIVE	(R30 id) an id cell `` `—` `` is a deliberate blank: the code span renders its content and a reader sees the em dash -- the same partner for the other `DECOR_MARK`	b75582e467defd29
CASE	NEGATIVE	(R31-1 §2.5) a header cell rendering `$` (`&#36;`) is NOT the `slice` schema: the reading moved and the COMPARISON did not loosen, so the table stays non-schema and declares nothing.  The partner that bounds the fix -- a match that normalised the header instead of rendering it would pass the three above and fail this one -- and the witness is the R42-10 gate naming the unbound claim, which a bound table would not produce	520efd54d2893884
CASE	NEGATIVE	(R33-1) `Subslice 9z — **UMBRELLA, …**` attributes NOTHING: `Subslice` is one word and the row noun inside it is not a row noun, so the field is the containing row's OWN declaration.  The reported shape -- read without a left boundary the search started mid-word, the row became a pointer and the run FABRICATED an UMBRELLA-MARK	b1b42709254e1cdd
CASE	NEGATIVE	(R33-1) `xSlice 9z — …` likewise: the boundary is a fact of the GRAMMAR (an ASCII alphanumeric may not stand before the noun), not a list of words that happen to end in one -- so a prefix nobody thought of is covered by the same clause	8800676c623b6331
CASE	NEGATIVE	(R33-2) `KIND / UNDETERMINED` declares NOTHING: the set admits the three dashes these documents use and did not become 'any punctuation' -- the partner that bounds the fix	8f7c55a5567fa351
CASE	NEGATIVE	(R34-1) `9z+notes.md owns it` reports NOTHING -- the discriminating half: the same prose with the run ENDING at the suffix is a file name, which is what makes the case above a claim about the run's end and not about the `+`	724f21c9d22b098b
CASE	NEGATIVE	(R34-1) a FRAGMENT tail still ends a name: `See slice-9z-sib.md#acceptance` masks the whole name.  This is the direction the correspondence forbids outright -- the resolver follows that run, stripping the fragment, so a reader that broke it into pieces and reported the id would be the one failure mode the property names	7268b842df04dce9
CASE	NEGATIVE	(R34-1) a TRAILING-PUNCTUATION tail still ends a name: `See slice-9z-sib.md.` masks the name and reports nothing.  Here the two readers differ LEGITIMATELY -- the resolver rejects the run with the period, because it is handed a destination the link grammar already bounded, while this reader must find the boundary itself and a sentence-final period is prose	e9e67c7c8edba284
CASE	NEGATIVE	(R34-2) an umbrella row whose `Deps` cell is a hyphen carries NO edge: the reading moved and `is_empty` still decides by SHAPE, so a deliberate blank is still blank.  The partner that bounds the fix -- reading the cell as 'anything the lexer did not blank' would pass the four above and fail these three	f97ef822e678eebc
CASE	NEGATIVE	(R34-2) an umbrella row whose `Deps` cell is an em dash carries NO edge: the reading moved and `is_empty` still decides by SHAPE, so a deliberate blank is still blank.  The partner that bounds the fix -- reading the cell as 'anything the lexer did not blank' would pass the four above and fail these three	5fbe9147cace49a4
CASE	NEGATIVE	(R34-2) an umbrella row whose `Deps` cell is an en dash carries NO edge: the reading moved and `is_empty` still decides by SHAPE, so a deliberate blank is still blank.  The partner that bounds the fix -- reading the cell as 'anything the lexer did not blank' would pass the four above and fail these three	f3503a8a4860d398
CASE	NEGATIVE	(R35) `See notes.md#9z owns it` reports NOTHING: the resolver follows that whole run, stripping the fragment, so the lexer must span it whole.  The reported shape -- R34-1 stopped the span at the suffix and the id in the fragment became a site	c37a6c18b21beeed
CASE	NEGATIVE	(R35) the QUERY spelling too: `See notes.md?q=9z owns it` -- the resolver strips a query exactly as it strips a fragment, so one rule covers both and neither is a special case	6b0acd4d1546c8f2
CASE	NEGATIVE	(R38 seed) a row whose `Deps` cell holds only a bare `.md` name carries an edge, so ordering prose naming no other party reports NOTHING.  Read off the prose-scanning stream the cell was EMPTY and the seed fired -- printing a no-edge finding that quoted the very cell contents it had just called empty	deabfab42c213b4c
CASE	NEGATIVE	(R38 seed) a row whose `Deps` cell holds only a citation `[C1]` carries an edge, so ordering prose naming no other party reports NOTHING.  Read off the prose-scanning stream the cell was EMPTY and the seed fired -- printing a no-edge finding that quoted the very cell contents it had just called empty	e5c0d6fbe8c7412b
CASE	NEGATIVE	(R38 seed) a row whose `Deps` cell holds only a code span ``b.rs`` carries an edge, so ordering prose naming no other party reports NOTHING.  Read off the prose-scanning stream the cell was EMPTY and the seed fired -- printing a no-edge finding that quoted the very cell contents it had just called empty	62338276a6d2b61c
CASE	NEGATIVE	(R38 seed) a row whose `Deps` cell holds only an autolink carries an edge, so ordering prose naming no other party reports NOTHING.  Read off the prose-scanning stream the cell was EMPTY and the seed fired -- printing a no-edge finding that quoted the very cell contents it had just called empty	cdf0c5dc8912b434
CASE	NEGATIVE	(R42 §6.1) the partner that bounds the demotion: the SAME code span OUTSIDE an image is a QUOTATION and its prose is not read -- a kind marker quoted whole stays quoted (I-A), so the §6.4 demotion must not leak out of the description	87a29ce0b0e04714
CASE	NEGATIVE	(R42 §6.1/§6.4) the partner that bounds the whole demotion: the SAME padded span OUTSIDE an image is MASKED whole, so its content is not read, the slug never completes and NOTHING is reported -- the trim belongs to §6.4's reading of a description, never to a code span in ordinary prose	7d33c0b6bf9b5c32
CASE	NEGATIVE	(R42 §6.4) the baseline the cross-product is measured against: the id in BARE prose outside any image is one reported site	d4a3a911bc48bba8
CASE	NEGATIVE	(R42) and the POINTER phrase on a blank id cell is NOT the contradiction: it says another row owns this one, which is exactly what an unkeyed row is for -- the #506 memo's `Function`/`eval` row is this shape, and a rule over every kind phrase rather than the marker alone reported it	7a40731d8c7ce37f
CASE	NEGATIVE	(R42) the partner that bounds it: the SAME blank id cell with no kind phrase in its declaring field is the deliberate non-row the exemption is for, and stays silent -- the blank is not what is reported, the contradiction is	d7c4ea928bedb713
CASE	NEGATIVE	(R42-10) a documentation table whose first column is ID-SHAPED is silent: a landing record's review-round table (`obj`, `R1`...`R7`) keys its rows and declares nothing.  This is the control the REJECTED predicate fails -- it reports 151 such tables over the 141 plan memos on this disk	288c951cb828aeb1
CASE	NEGATIVE	(R42-10) the SAME unbound header with no kind phrase in it is silent -- the gate is keyed on the CLAIM, not on the header.  This is the control a header-near-miss predicate fails: `No.` differs from `#` there too, and that table declares nothing	68460d0ba400bf1b
CASE	NEGATIVE	(R42-10) the phrase is read off the RENDERED cell, as the census reads a declaring field: a marker inside a CODE SPAN in an unbound table is not a claim, exactly as it is not one in a bound table's declaring field	2071a4a3624b6f30
CASE	NEGATIVE	(R42-8 §6.6 seed) a raw HTML span DEMOTED into a resolved image description is not seeded -- the naming scan has already read it -- and a span crossing a LINE ENDING is not seeded per line either	ad5acd38813e249b
CASE	NEGATIVE	(R42-8 §6.6 seed) the same in a CELL, which is the other half of the seed's population and its own site: the paragraph control cannot see it	8689e016c99f0056
CASE	NEGATIVE	(R42-8 §6.6) a raw HTML span inside a resolved image description is the alt's own TEXT, so the kind marker is NOT read across it: `![UMBRELLA, not a <span>terminal unit](img.png)` has the alt text `UMBRELLA, not a <span>terminal unit` (cmark 0.31.2 and commonmark.js 0.31.2 both, the latter escaping the brackets -- they are content) and the marker phrase is not in it.  Masked as markup the span JOINED its two sides and the row exited 1 on an ownership claim nobody made: a FABRICATED finding, the opposite direction from every other §6.4 defect on this surface	95265d93c1edfa37
CASE	NEGATIVE	(R42-8 §6.6) the R17 reading in BARE PROSE is untouched: the same span outside any image is masked whole and its id is no naming site.  The arm that was refuted is "a raw HTML span renders nothing INSIDE A RESOLVED IMAGE DESCRIPTION", not "a raw HTML span renders nothing"	796c91fb125c49a2
CASE	NEGATIVE	(R42-9) and the arm that WAS refuted stays refuted: the POINTER phrase on a blank id is the legitimate shape (`declaring_rows` names the #506 memo's `Function`/`eval` row, `:1985`) -- another row owns this one, which is what an unkeyed row is for	a33baeb05c4c4abe
CASE	NEGATIVE	(R47-2) and the I-A arm survives BY CONSTRUCTION, not by an exemption: a phrase quoted WHOLE in an unbound table still declares nothing.  `kind_disagreements` requires a STRADDLE, so the quoted form is not a disagreement and needed no case carved out of the widened gate	21d02c2daa650169
CASE	NEGATIVE	(R47-4) a NON-whitespace character where a gap would be is not a gap: `UMBRELLA, not-a terminal unit` is not the marker.  The word boundaries cannot see this -- they guard the two ENDS of the phrase -- so it is the only arm that fails when the gap class is widened from whitespace to a wildcard	f5c9bcab47f34e36
CASE	NEGATIVE	(R47-4) and the WORD BOUNDARY still holds after the gap widened: `SUBUMBRELLA, not a terminal unit` is not the marker.  The gap is a character class between words, never a licence to match inside a longer one	fcbc67fd39050fe0
CASE	NEGATIVE	(R47-4) and the WORD BOUNDARY still holds after the gap widened: `UMBRELLA, not a terminal unitary claim` is not the marker.  The gap is a character class between words, never a licence to match inside a longer one	a910e9f2bb90fc8a
CASE	NEGATIVE	(R49-2) and a genuinely blank `Deps` cell is still empty: the links arm is an ADDITION to the shape rule, not a replacement -- `—` carries no edge and no link	41b138d1dc239ac0
CASE	NEGATIVE	(R51) the links arm reaches the cd-seed as well: a row with ordering vocabulary in its prose and a bare `[](sib.md)` in its `Deps` cell emits NO `ORDER-PROSE?` seed, because the cell is not empty.  Measured 1 -> 0 when the arm landed, and unstated until an audit asked.  ⚠ The alternative reading -- a label-less link names a FILE and never says which ROW the ordering is against -- is recorded in `deps_is_empty`'s docstring; this control is what makes flipping it a decision	451506d572d0c6c4
CASE	NEGATIVE	(R52) a marker OUTSIDE the declaring field, quoted WHOLE (I-A: a quotation certifies nothing, and still does not): assertion (a)'s outside-field check searched the disposed STREAM alone, so the straddled spelling was invisible and the row left at rc 0 with only a non-gating seed while the clean spelling exited 1.  Asked through the canonical `_claims` now -- which keeps the quoted case by construction, since a phrase quoted whole is not a straddle	1602be476e2a984d
CASE	NEGATIVE	(a) a self-declaring field that later says a sibling 'is not it' stays self-declaring	6c44484f8f81fcf8
CASE	NEGATIVE	(a) the same row with the marker in its declaring field ONLY: UMBRELLA-MARK 0 -- the scan reads the other cells, it does not report them	330b3d0d6ca7273c
CASE	NEGATIVE	(accept-vocab seed) a POINTER row is excluded from the population	959fc2afcf999c31
CASE	NEGATIVE	(accept-vocab seed) a row whose marker is ATTRIBUTED to another row is a pointer and owes no acceptance condition	b8e8dc8f9e3e57ae
CASE	NEGATIVE	(accept-vocab seed) a terminal row stating an acceptance condition	de063191e5b773f6
CASE	NEGATIVE	(anchor) `` `Slice `C owns it ``: the row noun is inside a code span, so on the disposed stream there is no `Slice C` to anchor on -- 0 sites (the bare `C` is a declared miss)	d24c0eed28cc63a4
CASE	NEGATIVE	(ascii) `١.` (an Arabic-Indic digit) is not a list marker (§5.2: ASCII digits)	4bf1657b7744e83f
CASE	NEGATIVE	(autolink) a backtick inside an autolink is consumed by it, not by a code span (the three delimiters are read left to right as met; commonmark.js agrees) -- 0 sites	745af3f1a59bc8c0
CASE	NEGATIVE	(autolink) a declared id inside a URI autolink is no naming site: the span is masked whole, exactly as a link's destination is -- 0 sites	f7e838981d4a27dc
CASE	NEGATIVE	(autolink) a declared id inside an EMAIL autolink is no naming site either (the second §6.5 arm, `mailto:` at the renderer) -- 0 sites.  The id is the whole local part: dropping the arm leaves `9z` bounded by `<` and `@`, so the mutant HAS a site to report (an id glued to a domain label -- `ops@9z.example.com` -- is no site either way and proves nothing)	7484449b29ec6d38
CASE	NEGATIVE	(autolink) an autolink holding a declared id and a `\p` is NOT a `[LEX-UNSUPPORTED?]` seed: the construct is fully lexed and hides nothing, unlike a raw HTML span	661f0cf3bbd7d70f
CASE	NEGATIVE	(autolink) an autolink's URL is never a sibling on disk (§6.5 admits an absolute URI or an email address only, and `sibling_path` stage (a) rejects a scheme): rc 0	7e4369049a016ed3
CASE	NEGATIVE	(autolink) backslash escapes do not work inside an autolink (§6.5 Example 603, `<https://example.com/\\[\\>`): the span is matched raw, so the `\\[` opens nothing and the id in it is masked -- 0 sites	2e450c1035b519bb
CASE	NEGATIVE	(autolink) the R21 reviewer's input `<https://example.com/[child](absent.md)>` is ONE §6.5 autolink: the brackets inside it are not link syntax, `absent.md` is no memo, rc 0	2eb98e3682668cde
CASE	NEGATIVE	(b) an umbrella row with an empty Deps cell	e1c25af2c8c6d509
CASE	NEGATIVE	(bare) a bare `.md` file name holding an id is a file token, not a site	1f81acc4c0cb3735
CASE	NEGATIVE	(bare) a dotted number is one token: `§6.9z` names no row	331bb34e85564109
CASE	NEGATIVE	(block) `[Slice 9z owns it]:\\n    code` IS a definition (§4.4: indented code cannot interrupt a paragraph; commonmark.js: destination `code`): a block of its own, not scanned	8a10b71aee84bc66
CASE	NEGATIVE	(c-seed) a Deps cell that CARRIES the prose's party is not reported	2e03f01db4d7702c
CASE	NEGATIVE	(c-seed) ordering vocabulary inside a code span is code, not prose	4287d6d1e0833fce
CASE	NEGATIVE	(c-seed) the same prose with `#11-zz-alpha` in the Deps cell too: the cell carries the party, ORDER-PROSE? 0	7e58473ba90c81e2
CASE	NEGATIVE	(cell) a `Deps` cell holding only an HTML comment is EMPTY: it renders nothing, so the umbrella row carries no Deps edge (`is_empty` reads the cell's stream, not its raw text)	b54bf83bfcaa22e8
CASE	NEGATIVE	(cite) `[C1]: ECMA-262 §1 says so, and the table cites it.` at a block start is prose (commonmark.js: a paragraph), and the citation shortcut stays exempt: rc 0	76fa7ab004ad41f3
CASE	NEGATIVE	(cite) `[c1]` beside a declared no-owner id `c1`: a lowercase citation is masked by the same predicate the reference walk exempts it by -- 0 sites	01bcfabb69b9a60f
CASE	NEGATIVE	(cite) adjacent lowercase citations `[c1][c2]` are not a full reference (the exemption is the grammar's either-case predicate): rc 0	84c864c1d81167b3
CASE	NEGATIVE	(cite) the same gloss mid-paragraph is prose: rc 0	0aae5c0ce899719b
CASE	NEGATIVE	(d) an ownership clause inside a code span is code, not a two-owner claim	82524d9e40db33df
CASE	NEGATIVE	(d) one owner is not two	b2e842f84f04c038
CASE	NEGATIVE	(def) `[sib]: child.md "title` whose title crosses a BLANK line is not a definition (commonmark.js: a paragraph): `[sib]` later is an exempt shortcut, rc 0, and the sibling is NOT walked -- its violation is not reported	b4f9a59f71e6dc8c
CASE	NEGATIVE	(def) `paragraph\\n[sib]: slice-9z-sib.md` alone: the orphan's own label bracket is exempt -- rc 0, nothing walked	8ee3db725c43a7b6
CASE	NEGATIVE	(def) `paragraph\\n[x]: #section\\n[x]`: the orphan names a section, never a memo -- the shortcut is prose, rc 0	ce713cf1701aaa14
CASE	NEGATIVE	(def) `paragraph\\n[x]: https://example.com/a\\n[x]`: the orphan names an external URL, never a memo -- rc 0	6d7c69f08b31b628
CASE	NEGATIVE	(def) a definition cannot interrupt a paragraph: the reference is unanswered, and reported ONCE (`[text][label]` re-scans `[label]`)	0c344a169442709e
CASE	NEGATIVE	(def) a label-and-colon line that is NOT a valid definition (junk after the destination) at a block start is prose, not an orphan: `[sib]` later is exempt, rc 0	9d7c35712c507d6b
CASE	NEGATIVE	(def) a next-line title holding `[x](missing.md)` is a title, not a link: rc 0	18084de948f94608
CASE	NEGATIVE	(def) a next-line title is part of the definition, not prose: an id in it is no site	dffcc56837092719
CASE	NEGATIVE	(def) text after the destination is not a definition, so the reference is unanswered: a schema miss	1d21e6e60baf2063
CASE	NEGATIVE	(fence) a `#11-` slug inside a fenced block is not a naming site	9c8e1a7720b1dbe2
CASE	NEGATIVE	(fence) a closer followed by text does not close	ae5d48476467643d
CASE	NEGATIVE	(fence) a closer of the OTHER character does not close	b3ec9edbe7dbf42f
CASE	NEGATIVE	(fence) a link inside a fence is not a link (A x B)	4bb8bff1ff5da3d9
CASE	NEGATIVE	(fence) a shorter closer does not close	79b74881faef79ff
CASE	NEGATIVE	(fence) a tilde fence masks too	ed4f014521a0bea5
CASE	NEGATIVE	(fence) an unclosed fence runs to the end of the document	76934254808bea40
CASE	NEGATIVE	(fence) text inside a fenced block is not prose	028495948a11ec3c
CASE	NEGATIVE	(fence) three spaces of indent is	5d69337737ba47c1
CASE	NEGATIVE	(file) `(9z).md` is one file name (a balanced parenthesis pair): no site	0c86839b6bc51d3a
CASE	NEGATIVE	(file) `.md` alone is a file name (the suffix-only name `sibling_path` accepts): beside a declared no-owner id `md`, `Read .md for details` reports 0 sites	e16c3a1786fbdbf2
CASE	NEGATIVE	(file) `9z+notes.md` is one file name: no site	382f65c881f2cd7a
CASE	NEGATIVE	(file) `9z@notes.md` is one file name: no site	034d0ab280bf9199
CASE	NEGATIVE	(file) `notes.md` beside a declared no-owner id `md` is still one file name: 0 sites	6e12c8f2f1960223
CASE	NEGATIVE	(html) `# h\\n<span>\\n9z owns it`: after a one-line block no paragraph is open, so the type-7 opener is a block start (commonmark.js: a heading and a raw block): no site	c51af710e3f847f2
CASE	NEGATIVE	(html) `<!-- Slice 9z -- owns it -->`: `--` inside a comment is allowed since 0.31 (§6.6: "a string of characters not including the string -->") -- the whole comment is masked	165a2393afcaf3e8
CASE	NEGATIVE	(html) `<!X Slice 9z owns it>`: a declaration is masked	c3e7ef36227ff354
CASE	NEGATIVE	(html) `<![CDATA[ Slice 9z owns it ]]>`: a CDATA section is masked	8387bf3e405371b1
CASE	NEGATIVE	(html) `<![CDATA[` opens a type-5 block to `]]>` (Example 182): raw, no site	31de448f534fc269
CASE	NEGATIVE	(html) `<!doctype` opens a type-4 block (§4.6 condition 4: `<!` + an ASCII letter, either case) to the `>` line: raw, no site	f8063bc1efa45422
CASE	NEGATIVE	(html) `<? Slice 9z owns it ?>`: a processing instruction is masked	787b401ec324fb50
CASE	NEGATIVE	(html) `<a title=9z-owns>x</a>`: an unquoted attribute value is masked too	8e5af13e02a08d91
CASE	NEGATIVE	(html) `<span title="Slice 9z owns it">x</span>`: an id inside an attribute value is no naming site -- the span is masked whole (kind `html`), as a raw HTML-block line is raw	18e00c6462bce267
CASE	NEGATIVE	(html) `<span>\\n9z owns it` at a block start IS a type-7 block to the blank line: raw	00df6abd3cf9641f
CASE	NEGATIVE	(html) `Heading\\n===\\n<span>\\n9z owns it`: after a setext heading no paragraph is open (commonmark.js: a heading and a raw block) -- the look-back at the previous line missed this: no site	eee95d17a74da775
CASE	NEGATIVE	(html) `text\\n<DIV>\\n9z owns it`: `<DIV>` opens a type-6 block (§4.6 condition 6 is case-insensitive), which interrupts the paragraph -- raw to the blank line, no site	05d91a768c96a37a
CASE	NEGATIVE	(html) `text\\n<PRE>\\n9z owns it\\n</pre>`: `<PRE>` opens a type-1 block (§4.6 condition 1 is case-insensitive), which interrupts the paragraph -- raw to `</pre>`, no site	36185ba13d8afbeb
CASE	NEGATIVE	(html) a CDATA section `<![CDATA[ [x](absent.md) ]]>` is raw: rc 0	aa03ffc10390cf1e
CASE	NEGATIVE	(html) a cell's `<span title="Slice 9z owns it">` is masked by the same inline pass: no site	7bf9aa3c2b92241d
CASE	NEGATIVE	(html) a comment may cross the paragraph's line ending (`<!--\\n[x](absent.md)\\n-->`, Example 625's shape): rc 0	87359ad0101317e2
CASE	NEGATIVE	(html) a declaration `<!X [x](absent.md)>` is raw: rc 0	4c027468ac889397
CASE	NEGATIVE	(html) a processing instruction `<? [x](absent.md) ?>` is raw: rc 0	c2398a2ec01077ff
CASE	NEGATIVE	(html) a single-quoted attribute value `<span title='[x](absent.md)'>` is a tag: rc 0	8a731ea704e98350
CASE	NEGATIVE	(html) an HTML comment `<!-- [x](absent.md) -->` is raw: rc 0	677c1bcff04b3b0a
CASE	NEGATIVE	(html) an UNQUOTED attribute value holding brackets `<span title=[x](absent.md)>` IS an open tag (§6.6: an unquoted value excludes only spaces, tabs, line endings, `"`, `'`, `=`, `<`, `>` and a backtick; commonmark.js agrees -- ⚠ the R17 brief listed this shape as a negative): rc 0	fff112bc613d4424
CASE	NEGATIVE	(html) the R17 reviewer's shape `<span title="[child](absent.md)">text</span>`: a bracket inside a double-quoted attribute value is no link -- nothing is walked, rc 0	011976c3c8b99586
CASE	NEGATIVE	(id) an id cell `-` is empty: not declared, not a schema miss	f44121ff491aae48
CASE	NEGATIVE	(id) an id cell `-` is not an id	38478bdb45d94e41
CASE	NEGATIVE	(id) an id cell `–` (en dash) is a literal blank: a non-row	f13dede9d7d72acf
CASE	NEGATIVE	(id) an id cell `—` is a literal blank: a deliberate non-row, rc 0	bc7e8ad8366c0e25
CASE	NEGATIVE	(id) the `Intl`-shaped cell never mints the whole cell as an id	dcb48d3836575b1e
CASE	NEGATIVE	(id) the id cell's own id is not a site: `**7z** — MERGED` reports nothing	f4e5853e2ab5dee6
CASE	NEGATIVE	(image) `![a ![b [c](child.md)](i.png)][missing]`: the OUTER image fails but the INNER one resolves, and the link inside the inner description is demoted (commonmark.js: `<img alt="b c">`) -- `child.md` is not walked	3fc9c8b2c9ea079a
CASE	NEGATIVE	(image) `![alt [b](9z)](i.png)`: the demoted link's tail stays masked -- the `9z` in its destination is not prose, 0 sites	c353875dc304c9a5
CASE	NEGATIVE	(image) `![alt [docs](child.md)](absent-image.md)`: neither the demoted link nor the image is a memo link -- `child.md` is not walked (its violation is unreported) and the image's `.md` destination is not probed	df8f5a2fcb635444
CASE	NEGATIVE	(image) `![alt [x][missing]](img.png)`: a failed reference inside a resolved image's description names no lost memo (resolved, it would have been demoted; commonmark.js: `alt="alt [x][missing]"`) -- rc 0	82fa3ce61779c327
CASE	NEGATIVE	(image) `![alt][img]` with a definition is consumed whole: `[img]` is not re-read as a shortcut, and the image destination is not a memo	bb08c3e056d6b1f1
CASE	NEGATIVE	(image) a nested image in a resolved image `![a ![b [c](child.md)](i.png)](j.png)`: the inner image stays masked and the link inside it is demoted -- `child.md` is not walked	131735e62bde3c14
CASE	NEGATIVE	(image) an undefined reference image `![diagram][missing-image]` is literal syntax, not an unresolved memo reference: rc 0	54e88f12919a7784
CASE	NEGATIVE	(image) the R19 reviewer's input `![alt [docs](absent-file.md)](image.png)`: the image resolves, so its description is plain text (§6.4; commonmark.js: `<img alt="alt docs">`) -- the link inside it is no memo link, nothing is walked, rc 0	21b166a8857fa03e
CASE	NEGATIVE	(image) the reviewer's input `![alt][missing]` + an orphan `[missing]: image.md` in the same paragraph: the re-scanned `[missing]` is the same failed site, not a shortcut for the orphan rule -- rc 0, `image.md` never walked	3ddffd3f1c17a2a0
CASE	NEGATIVE	(indented) `    Slice 9z owns it` at a block start is a raw extent (§4.4), like a fence: no site	688b263a1c3c791d
CASE	NEGATIVE	(indented) ` \\tSlice 9z owns it`: a space then a tab is four columns (§2.2) -- raw, no site	8c78a0dbc643cc09
CASE	NEGATIVE	(indented) `\\tSlice 9z owns it`: a tab is four columns (§2.2), so the line is indented code at a block start -- a raw extent, no site	7cda38a63662dacb
CASE	NEGATIVE	(item) `- item\\n\\n      Slice 9z owns it`: six columns are four inside the item -- indented code IN the item (commonmark.js), raw: no site	ac8b9db8993eef1b
CASE	NEGATIVE	(item) `- item\\n\\n    Slice 9z owns it` is no raw line: the `item` seed reading is gone with the flat reading -- 0 seeds	d8509b9c3d110ed8
CASE	NEGATIVE	(item) `- item\\n\\n  para\\n\\n# h\\n\\n    Slice 9z owns it`: a 0-column heading is a lazy candidate that is a block start -- the item ends, the heading and the code block are outside it: no site	3691b8e0f9d5df22
CASE	NEGATIVE	(item) `- item\\n\\n para\\n\\n    Slice 9z owns it`: a 1-column line after a blank is short of the content indentation with no paragraph open, so the item ENDS before it (commonmark.js: a paragraph and a code block outside the list) -- the indented line is §4.4 raw, no site	62d1b85f8c121ccd
CASE	NEGATIVE	(item) `- open `x\\nSlice 9z owns it` end`: the line short of the content indentation is the item paragraph's lazy continuation text (§5.2 rule 5), so the span crosses it: no site	2c4f08e8f34db4a5
CASE	NEGATIVE	(item) `open `x\\n*\\n9z owns it` end`: an EMPTY item cannot interrupt a paragraph (§5.2; `*`, not `-`, which would be a setext underline) -- one paragraph, the span masks the site	bfae0f741f04a8f4
CASE	NEGATIVE	(item) `open `x\\n2. 9z owns it` end`: an ordered item not starting at 1 cannot interrupt a paragraph (§5.2) -- one paragraph, the span masks the site	02c744893d7b06a7
CASE	NEGATIVE	(kind) `SUBUMBRELLA, not a terminal unit.` carries no marker: the row is terminal, out of the no-owner census, and a prose mention of it is no site	160701336f510d60
CASE	NEGATIVE	(kind) `The KIND UNDETERMINEDNESS metric must be recorded` declares no unsettled kind: with a nonempty `Deps` cell the unbounded reading made the row no-owner and forced `UMBRELLA-CELL`, rc 1	2a828393333ce056
CASE	NEGATIVE	(kind) `is a pointer rather than a slice` still IS one, and a pointer row owes no acceptance condition -- the discriminating half	0f49ac5fe6d079d5
CASE	NEGATIVE	(kind) `not a terminal unitary claim` is not the kind vocabulary either: the seed half of assertion (a) reads the same bounded phrases the marker does	6a3fc4ca4b6a4e27
CASE	NEGATIVE	(kind) a declaring field holding a code span and NO marker is no miss: rc 0 -- the residue is a marker the reader reads ACROSS a blanked span, never the presence of one	fb6aa860917d6180
CASE	NEGATIVE	(kind) nor does `MANKIND UNDETERMINED by the probe` -- the boundary is on BOTH sides, and the left one is the half a right-only fix would leave authoritative	2dae31c0a5e0e800
CASE	NEGATIVE	(kind) nor does `UMBRELLA, not a terminal unitary claim.` -- the right-hand edge	ee37271cf32a23b5
CASE	NEGATIVE	(kind) nor is a field carrying the marker WHOLE beside a code span: rc 0	b2aa15b7ec3a8304
CASE	NEGATIVE	(kind) the POINTER spelling read across a comment: the row is a POINTER, and a pointer row owes no acceptance condition -- so the ACCEPT-VOCAB? seed stays silent where reading the comment as text would have made the row an active terminal owing one	af18eae3916635ab
CASE	NEGATIVE	(kind) the marker QUOTED WHOLE in a code span declares nothing (I-A: a quoted marker is not a declaration) -- the row is terminal and its mention is no site	2fa0947b099b11bb
CASE	NEGATIVE	(label) `[foo&auml;]: child.md` then `[fooä]`: §6.3 label matching is on the RAW label (case fold, strip, collapse -- no character-reference decoding; commonmark.js: no link) -- the shortcut is prose, 0 sites, `child.md` not walked	6c0a06704866b1c8
CASE	NEGATIVE	(lex-seed) `a<br>b` / `<sub>2</sub>`: tags holding neither a `\p` nor a declared id are no seed	fca1fbc74e68fa75
CASE	NEGATIVE	(lex-seed) a type-6 block ends at a blank line: the paragraph after it is not seeded	ae33e586c7dc6342
CASE	NEGATIVE	(lex-seed) an HTML-block line with neither a `\p` nor a declared id is no seed	031402f751452099
CASE	NEGATIVE	(lex-seed) an indented-code line with neither a `\p` nor a declared id is no seed	932330b3e25d4729
CASE	NEGATIVE	(licence) `9z's memo` IS it and is licensed -- the discriminating half	a41b00be88be3711
CASE	NEGATIVE	(licence) `The child of 9z` IS the phrase and is licensed -- the discriminating half	fefdc032dfbd114e
CASE	NEGATIVE	(link) `<dest>` may not contain an unescaped `<`	97e127f56b0df936
CASE	NEGATIVE	(link) `[a ![b [c](child.md)](i.png)](parent.md)`: the inner link deactivated the outer `[` as it closed, before the image demoted it (§6.3 "links may not contain links"; commonmark.js: `[a <img alt="b c">](parent.md)`) -- neither `child.md` nor the absent `parent.md` is linked: 0 sites, not rc 2	e9551dca32cd05a8
CASE	NEGATIVE	(link) `[label][undefined]` is neither a full reference nor a shortcut (§6.3 Example 571: a shortcut is not followed by a link label) -- the unanswered label is a schema miss, not a link to the sibling	27725f92d044df1c
CASE	NEGATIVE	(link) `[x](child&#46md)`: `&#46` without `;` is no reference -- the destination is the literal `child&#46md`, which names no memo: 0 sites, `child.md` not walked	6880c6b137d4c9b1
CASE	NEGATIVE	(link) `[x](child&#x26;#46;md)`: a decoded `&` is a character, never the start of a second reference (one pass, no re-scan) -- literal `child&#46;md`, 0 sites	325157e44dae1145
CASE	NEGATIVE	(link) `[x](child&MadeUpEntity;md)`: a name not on the HTML5 list is literal text (§2.5 Example 30) -- no memo, 0 sites	46eefd652eeef9ea
CASE	NEGATIVE	(link) `[x](child\\&#46;md)`: a backslash-escaped `&` opens no reference (ONE pass: §2.4 and §2.5 meet at the character once) -- literal `child&#46;md`, no memo, 0 sites	04fd10595e322b77
CASE	NEGATIVE	(link) `notes%3Achild.md` is NOT a sibling.  It has no scheme -- WHATWG URL §4.4 #scheme-start-state / #scheme-state read the input as written and `%` is in neither class, #string-percent-decode being a later, separate operation -- so stage (a) admits it; stage (c) then refuses it, because the decoded `notes:child.md` is an NTFS alternate data stream on Windows and a plain file name on POSIX, which is two readings where that stage allows one.  ⚠ THIS EXPECTATION IS A REVERSAL of PR #510 R8, decided at R25-1, and it costs a POSIX memo genuinely named `notes:child.md` -- dropped without a report, as `sub\\child.md` and `NUL.md` already are	7da1fb0f49748623
CASE	NEGATIVE	(link) a `#11-` slug in a link DESTINATION is not a naming site	0e3ca330b2e422be
CASE	NEGATIVE	(link) a `(…)` title may not contain an unescaped `(`	aed3a1879ff61a46
CASE	NEGATIVE	(link) a `[C19]` citation is a shortcut with no definition anywhere: no miss	02571a96572a6caa
CASE	NEGATIVE	(link) a collapsed-shaped citation `[C19][]` is not a reference: rc 0	f598145b98f4078c
CASE	NEGATIVE	(link) a link inside a code span is not a link (A x B)	02190380fc0ae9c6
CASE	NEGATIVE	(link) adjacent citations `[C19][C20]` are not a full reference: rc 0	245a9a4ae4478c59
CASE	NEGATIVE	(link) an ASCII control character (0x7F) ends a bare destination	0c455aae778db17f
CASE	NEGATIVE	(link) an escaped `\\[` opens nothing: `\\[x](absent-file.md)` is not a link, rc 0	cae6d8ccf610a155
CASE	NEGATIVE	(link) bracket text holding unescaped brackets is not a label (§6.3), so `[the [x] walk][]` is no collapsed reference: rc 0	6d1a04e0474b8169
CASE	NEGATIVE	(link) label matching collapses spaces, tabs and line endings only: a non-breaking space is not a space, so the reference is unanswered	6814c214385e9f4f
CASE	NEGATIVE	(quote) `> Heading `open\\n===\\nSlice 9z owns it` here`: a lazy `===` is the quote paragraph's text, not an underline (§5.1, Example 93) -- one paragraph, the span masks the site	ffde4ec7aad7fb8d
CASE	NEGATIVE	(quote) `> [a]: /u\\n>` then the lazy schema header: the blank quote line closes the run, so the quote ENDS and the header is a paragraph outside it (cmark-gfm) -- no id declared	1b221872e0dce3e7
CASE	NEGATIVE	(quote) `> open `here\\nSlice 9z owns it` there`: the marker-less line is lazy continuation text of the quote's paragraph (§5.1), so the span crosses it: no site	1946b0e65ba53e49
CASE	NEGATIVE	(quote) a fenced block then the lazy schema header: no run is open, the quote ends (cmark-gfm) -- no id declared	85557b92db10171f
CASE	NEGATIVE	(quote) a slot table inside a block quote whose DELIMITER row is lazy is one paragraph (cmark-gfm: the delimiter arrives in an unmatched container): no id declared	d7b5f47a09fa3010
CASE	NEGATIVE	(rc) `n%3Achild.md`: a ONE-letter name before `:` is a Windows drive letter (URL `#path-state` step 1.4.1, platform-independent) -- drive-relative, rejected at stage (c) by the ANCHOR, rc 0.  ⚠ Since R25-1 the multi-letter `notes%3Achild.md` is refused at the same stage by the CHARACTER rule, so this no longer discriminates one-letter from multi-letter; what it still says is that the drive reading is applied on every platform	8c15f0a636fbb187
CASE	NEGATIVE	(rc) a code-quoted link to an absent file is not a link: rc 0	1fdd2ebd7a35b11c
CASE	NEGATIVE	(rc) a link cycle (A -> B -> A) is walked once and is not an error	37619be091879a55
CASE	NEGATIVE	(rc) a memo whose every row is terminal is rc 0, not a schema miss	87747b7c2a6cc783
CASE	NEGATIVE	(rc) a percent-encoded ABSOLUTE destination `%2Ftmp%2Fchild.md` is rejected after decoding (never probes `/tmp/child.md`): rc 0	8d033f21824546e6
CASE	NEGATIVE	(rc) a percent-encoded UNC `%5C%5Cserver%5Cshare%5Cx.md` is rejected (stage c: a UNC prefix anchors): rc 0	9320e05d48bcff8c
CASE	NEGATIVE	(rc) a percent-encoded Windows drive-absolute `C%3A%5Ctemp%5Cchild.md` (`C:\\temp\\child.md`) is rejected after decoding on every platform (stage c: a drive anchors): rc 0	c99455fc89a57bc5
CASE	NEGATIVE	(rc) a percent-encoded backslash-rooted `%5Cchild.md` (`\\child.md`) is rejected (stage c: a root anchors): rc 0	3f2ad63c7b3e534f
CASE	NEGATIVE	(rc) a protocol-relative `//host/x.md` is not a sibling on disk: rc 0	3edcd374264e7f19
CASE	NEGATIVE	(rc) a raw `\\\\server\\share\\x.md` destination decodes (§2.4: `\\\\` is one backslash, `\\s` is literal) to the backslash-rooted `\\server\\share\\x.md` (commonmark.js: href `%5Cserver%5Cshare%5Cx.md`) and is rejected: rc 0	f13a89256798da70
CASE	NEGATIVE	(rc) a root-relative `/guide.md` is not a sibling on disk (nothing probed): rc 0	e769a4a8949e875f
CASE	NEGATIVE	(rc) a slice header over a one-cell delimiter row is not a table, so its wide body row is not a width miss	79a53571e3f3d478
CASE	NEGATIVE	(rc) an absolute URL ending in `.md` is not a sibling on disk: rc 0	d7d99bb7462604ba
CASE	NEGATIVE	(rc) an all-terminal memo reports a zero census	47952daee205a99a
CASE	NEGATIVE	(rc) an excess body cell of a non-schema table holding `[x](absent-file.md)` is ignored (GFM §4.10): no link, nothing walked, rc 0	05846f454c6f938b
CASE	NEGATIVE	(rc) an indented slot row after a table's rows is raw, so its id is not declared and the run is rc 0 (the seed, not the census, carries it)	49dd2519c9308d30
CASE	NEGATIVE	(rc) drive-relative `C:child.md`: raw, it is a URL of scheme `c` (stage a); percent-encoded `C%3Achild.md` decodes to a drive-anchored name (stage c) -- both rejected, rc 0	eddb5af99178cb50
CASE	NEGATIVE	(rc) two MARKED rows spelling it the SAME way: KIND-SPELLING 0	e4394b146c7ca90d
CASE	NEGATIVE	(render) GFM strikethrough `W~~z~~` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	3a46076ec8e458ff
CASE	NEGATIVE	(render) `&#42;&#42;C&#42;&#42;` is the same rule for §2.5: a reference that would spell a decoration stands as written	3f2759c10228d506
CASE	NEGATIVE	(render) `**9z**'s children`: the licensing rule needs no decoration spelling of its own -- the mention's token consumes the `**` it carries, so the licensed noun is what follows it	4b33af6fa0e15a0f
CASE	NEGATIVE	(render) `*9z*7z` names nothing: a SINGLE `*` is not this document's decoration (`plan_memo_ids.DECOR` is `**` and a backtick), so the pair renders away and `9z7z` is one token -- the exception is the decoration's, not emphasis's	c37afb660aa8810f
CASE	NEGATIVE	(render) `9z is an *umbrella* row`: same, for the kind-statement arm of the licence	2ff1cf702b552d7e
CASE	NEGATIVE	(render) `9z's **charter**`: the licensed noun carries the decoration, and the phrase is licensed all the same	4e849cbd48556ef5
CASE	NEGATIVE	(render) `Slice W**z** owns it` seeds NOTHING: the construct renders no character, so the two readings agree and there is no residue to report	a2132cc6f5441fb9
CASE	NEGATIVE	(render) `Slice W*z owns it`: an UNMATCHED `*` run is literal text (§6.2 -- commonmark.js renders `9*z` verbatim), so it BOUNDS the token and `Wz` is not named -- dropping a run that pairs with nothing would fabricate the row	d5ea9b39a047a607
CASE	NEGATIVE	(render) `Slice W_z_ owns it`: an intraword `_` run can neither open nor close (§6.2 rules 5-6, the `snake_case` rule), so it is literal and bounds the token	28f1fe0c44801258
CASE	NEGATIVE	(render) `Slice W`z` owns it`: a code span RENDERS TEXT this checker refuses to read (I-A), so it blanks and bounds the token -- the residue the `[LEX-SPLIT?]` seed reports rather than decides	6e964027cbc66557
CASE	NEGATIVE	(render) `Slice W~~~z~~~ owns it`: three tildes are no strikethrough (GFM: a matching pair of ONE OR TWO), so the run is literal text and bounds the token	b5c9b256eb3cb2bd
CASE	NEGATIVE	(render) `\\*\\*C\\*\\* is how the row is written`: an ESCAPED decoration character stands as written -- substituting it would spell a `**C**` bold the document does not have, and the undecorated single letter is the declared miss	5f1e75fa6c76c626
CASE	NEGATIVE	(render) `each of *9z's* children`: the licensing phrase is read as the document renders it, so the emphasis no longer hides a licensed construction	89d58481201f9092
CASE	NEGATIVE	(render) §2.5 a character reference `W&#122;` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	c356197100bbceb8
CASE	NEGATIVE	(render) §6.2 emphasis `W*z*` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	de7bdc37ba614e14
CASE	NEGATIVE	(render) §6.2 strong emphasis `W**z**` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	e1c92dd09790ab07
CASE	NEGATIVE	(render) §6.3 a link's brackets `W[z](slice-9z-sib.md)` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	385d5f1db161970a
CASE	NEGATIVE	(render) §6.6 a raw HTML tag pair `W<b>z</b>` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	7cfae3df1ccc33c2
CASE	NEGATIVE	(render) §6.6 an HTML comment `W<!-- c -->z` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	d769ee2b65ee8623
CASE	NEGATIVE	(row) `a\\\pb` is one cell (the odd backslash escapes the pipe): rc 0	3599d045668016b4
CASE	NEGATIVE	(row) a trailing `\\\\\p` is a literal backslash then the trailing pipe: rc 0	f7053010cd7b74b8
CASE	NEGATIVE	(schema) a citation id in the citation table is admitted: rc 0 -- the three controls above have a live subject	e1a9929e1029a634
CASE	NEGATIVE	(seed) a raw HTML-block line whose only declared id sits INSIDE a `.md` file name seeds nothing: the same name in prose is masked and reports nothing, and a seed that fires where prose would not is reporting a reading the document has no way to hold	3b564b968cb8132c
CASE	NEGATIVE	(seed) a raw line whose only declared id is a CITATION seeds nothing -- the shared spans SUBSUME the seed's former hand-written `kind != cite` filter, so the citation half has one spelling and not two	c85f2d267cff23dd
CASE	NEGATIVE	(seed) the same for an INDENTED-CODE line: one disposition, both readings	3863c1ac06d5e47c
CASE	NEGATIVE	(setext) `==` after a list item is NOT an underline (§4.3 Examples 92-94): the item's paragraph continues and a code span crosses it	ac5b9e93639e199d
CASE	NEGATIVE	(sibling) `NUL.md` is a DOS device, not a memo beside this one: the destination is no sibling even though a file of that name is there to read	0b4a24fca5e19e8e
CASE	NEGATIVE	(sibling) `NUL/child.md`: the device is read per PART of the parsed path -- a device DIRECTORY rejects the destination too.  The device sits in a NON-FINAL component on purpose: with it in the last one, a final-component-only reading passes this control and the mutant survives (measured -- the probe would have had another subject)	a5d321292fde95a6
CASE	NEGATIVE	(sibling) `com1.md`: the device names fold case (`ntpath._isreservedname` upper-cases the stem), so the lower-case spelling is the same device	78632d204d90d7cc
CASE	NEGATIVE	(sibling) `dir%20/child.md`: a component ending in a SPACE is reserved too -- Windows strips the trailing run, so that component names a different directory there than here	5c1f5da475fa475a
CASE	NEGATIVE	(sibling) `prn%20.md`: the stem's trailing spaces are stripped before the device lookup, so `prn .md` is `PRN` (Microsoft, "Naming Files, Paths, and Namespaces")	7e4478c469940f69
CASE	NEGATIVE	(slug) `#11-zz-alphaZZ` in prose is not the id `#11-zz-alpha`: the slug is bounded on its right as on its left -- 0 sites	e136c33b82285ddb
CASE	NEGATIVE	(slug) `` `tool #11-zz-alpha_extra` ``: the kept-slug exception inside a code span reads the grammar's boundary, so nothing is excepted and the span stays code -- 0 sites	e0dfd2c1b8e635cd
CASE	NEGATIVE	(span) `` `[c](child&#46;md)` `` is a code span: §2.5's first exception -- nothing in it is a destination and nothing is decoded; 0 sites, `child.md` not walked	441a1260df4659e5
CASE	NEGATIVE	(span) a backtick BEFORE the `]` opens a code span that swallows it: `[not a `link](absent.md)`` is code, no link, rc 0	79532b32a5f6f0aa
CASE	NEGATIVE	(span) a code span may cross a line ending	ab1efc690950d595
CASE	NEGATIVE	(span) a quoted kind marker is not a declaration (A x E)	e510460c13567048
CASE	NEGATIVE	(span) an NBSP-only line is NOT blank (§4.9: spaces or tabs only), so it does not end the paragraph and the code span crosses it	72cdec0b4bc7832b
CASE	NEGATIVE	(span) backtick strings pair by EQUAL length	82eed6ad14a34ef1
CASE	NEGATIVE	(stream) ordering vocabulary in a link TITLE is the link's tail, not prose	b611c61fc519d315
CASE	NEGATIVE	(table) `---` right after a schema table is a thematic break (§4.1: no paragraph is open so §4.3 does not apply; GFM §4.10: another block-level structure ends the table): rc 0	879f5148f839352f
CASE	NEGATIVE	(table) `\\t\p Slot \p ... \p` over `\\t\p---\p...\p`: a tab-indented header and delimiter row are indented code (§4.4, four columns), not a table -- the wide row under them is no width miss: rc 0	404500d66c6c9c3d
CASE	NEGATIVE	(table) a delimiter cell is >=1 hyphen with optional colons; `:` alone is not	398355b59f0de719
CASE	NEGATIVE	(table) a list item right after a schema table ends it (a block start), so it is not a 1-cell body row: rc 0	5b3a8beb99e31411
CASE	NEGATIVE	(table) a row opening with an NBSP before its `\p` is not edge-piped: the NBSP is a cell, the header is 7 wide over a 6-cell delimiter, no table	3fbd98040a7d96ab
CASE	NEGATIVE	(table) a type-7 HTML opener right after a schema table ENDS it (a table is not a paragraph): `<span>` + prose are raw lines, not one-cell rows -- no width miss, rc 0	203b4ee870935bd4
CASE	NEGATIVE	(table) an excess body cell of a non-schema table is ignored (GFM §4.10): its `9z owns it` is never lexed -- no site	5a9f727f1e434a46
CASE	NEGATIVE	(table) an indented line right after a schema table opens an indented code block (a block-level structure; no paragraph is open), not a one-cell row: rc 0	6245b7275ed86448
CASE	NEGATIVE	(table) header and delimiter of unequal width are not a table	6c3818446c2a2726
CASE	NEGATIVE	(table) the prose inside the type-7 block after a schema table is raw, not a site	7c8f59f91486a21f
CASE	NEGATIVE	(vocab) acceptance vocabulary split by emphasis is acceptance vocabulary: a terminal row writing `m*us*t` owes no ACCEPT-VOCAB? seed	7a010fd56c66bc2a
CASE	NEGATIVE	a bold sentence opener is not row A	027a2161b7b492f4
CASE	NEGATIVE	a row naming itself in its own cell	14e6e375c43585ff
CASE	NEGATIVE	a self-declaring row that MENTIONS a sibling stays in the population	969de0f6c944cba4
CASE	NEGATIVE	an id in a titled link's destination is not a naming site	1e797266dd37c47b
CASE	NEGATIVE	an id-looking token inside a file name	328542cb30189770
CASE	NEGATIVE	an id-looking token inside inline code	21239b72f14a93f8
CASE	NEGATIVE	possessor of a charter	37f2950938e15cfc
CASE	NEGATIVE	possessor of children	345184355dd00e69
CASE	NEGATIVE	statement about the row's kind	41c9089bedb12d8b
CASE	NEGATIVE	the child-of construction	8b108951f0d9b7fb
CASE	NEGATIVE	the derivation-mints construction	5e62ca8398bda4a4
CASE	POSITIVE	(R23 kind) `KIND UNDETER`MINED`` in a declaring field is the schema miss: a reader reads the undetermined kind (§6.1 -- the code span contributes `MINED` as plain text), the disposed stream reads none, and the row silently left the census as an active terminal at rc 0 -- the marker was gated, every OTHER kind phrase was not	a3dfcc3ba8b6c22f
CASE	POSITIVE	(R23 kind) `KIND `x` UNDETERMINED` is the SAME miss from the other side: the blank stands as spaces, so the STREAM reads the undetermined kind and a reader (`KIND x UNDETERMINED`) reads none -- a gate stated over one direction leaves the other authoritative	2e76ee642cdf0397
CASE	POSITIVE	(R23 kind) `KIND&ast;UNDETERMINED` is that same stream-side miss through a BLANKED decoration spelling: the reader sees `KIND*UNDETERMINED` and no kind at all.  It is the one control over what a reader SEES where such a span is blanked -- render it as spaces and the two readings agree on a kind neither should read	73f8680203cd40c7
CASE	POSITIVE	(R23 kind) the POINTER phrase straddling a code span is the miss too -- the third member, gated by arriving in `KIND_PHRASES` and not by being named here	2315cdb96735e467
CASE	POSITIVE	(R23 memo) a linked memo whose FIRST block is a slice table still declares its rows when the file opens with a BOM: the signature is dropped where the text becomes lines, or the header row is not a header row and the census silently shrinks at rc 0	b9bdbe900b4b88ec
CASE	POSITIVE	(R23 memo) the same sibling WITHOUT a BOM declares the row -- the discriminating half: the table, the schema and the link are the control's constants	0cde759607909d5d
CASE	POSITIVE	(R23 render) `Slice Qx owns it` IS the site -- the discriminating half of the blank: the row is an umbrella and the joined spelling is what would be reported	36ebb8000b5619e0
CASE	POSITIVE	(R23 render) `The ast marks it` IS the site -- the discriminating half: the row is declared, the scan reaches that prose, and only the SPELLING differs	37067260ed90b60d
CASE	POSITIVE	(R23 seed) the `[LEX-SPLIT?]` residue seed reads every kind phrase too, not only the marker: `KIND UNDETER`MINED`` in PROSE (no declaring field, so no census to gate) is the reported disagreement	4e3bf9a4ed3766ed
CASE	POSITIVE	(R24 render) `**&#57;z**7z owns it` names `9z`: the pair's content RENDERS `9z`, so the `**` decorate an id and stand as the boundary they are.  Asked of the source, `id_only` read `&#57;z`, dropped the delimiters, and joined the two sides into the token `9z7z` a reader never sees -- the mirror of the fabrication design re-gate 4 closed, one spelling further out	40f9c5c5e6791161
CASE	POSITIVE	(R24 render) `**9z**7z owns it` names `9z` -- the discriminating half of the decoration exception, green on both sides of the fix	406eb0779c92c420
CASE	POSITIVE	(R24 render) `9z notes.md owns it` names `9z` -- the discriminating half: the same document with the space spelled as a space, green before the fix and after it, so the pair differs only in the SPELLING of a character both render	fe60f7b149f91d3a
CASE	POSITIVE	(R24 render) `9z&#32;notes.md owns it` names `9z`: §2.5 renders the reference as a SPACE, so the file name begins at `notes` and the id stands beside it -- the raw reading saw one unbroken run ending in `.md`, masked the whole of it, and the ownership claim left the census at rc 0	72506ce804573eeb
CASE	POSITIVE	(R24 width) `the grandchild of 9z` is still REPORTED -- the left edge the trailing noun clause must not loosen	d139331096484443
CASE	POSITIVE	(R24 width) `xderivation … that the 9z` is REPORTED: the licensing phrase is 40 characters, exactly the width of the slice the backward look was given, so its lookbehind fell off the start of that slice and succeeded against nothing -- the document says `xderivation`, which is not the licensed phrase	1c0838b748843c04
CASE	POSITIVE	(R24 width) a pointer row whose appositive names a 76-character slug attributes the marker to that row: the appositive ends where the marker begins, which is a GRAMMAR fact and not a character count.  Read through a 70-character window the appositive fell outside it, the field was taken as the row's OWN declaration, no UMBRELLA-MARK was emitted, and the census carried a pointer row as an umbrella at rc 0	dbea29eb6f9cc9d5
CASE	POSITIVE	(R24 width) the same shape with a SHORT slug attributes too -- the discriminating half: the row, the marker and the appositive are the control's constants, and only the length of the named id differs	2c30e0d4040fe509
CASE	POSITIVE	(R24 §2) a link destination holding a literal U+0000 names the file the document renders: §2 replaces the NUL with U+FFFD before parsing, so `[x](child .md)` links `child<U+FFFD>.md`, that memo is walked and its rows are declared.  Left in, the NUL is an ASCII control, `link_destination` (§6.3) refuses the destination, and the memo -- with every violation in it -- left the census while the run could still exit 0	a972112daa69c513
CASE	POSITIVE	(R24 §2) the same link with the REPLACEMENT CHARACTER written out is walked -- the discriminating half: the file, the table and the link are the control's constants, and only the SPELLING of the destination's one character differs	e98ce5ac69a4395b
CASE	POSITIVE	(R24 §2.1) a memo whose lines end with a bare CARRIAGE RETURN is one document of many lines: §2.1's line ending is `\\n`, `\\r\\n`, or `\\r` not followed by `\\n`, so the tables parse and the rows are declared.  Read without that rule the whole file is one line, no table is admitted, and no schema miss says so	a59d0719712cb2e7
CASE	POSITIVE	(R24 §2.1) the same memo with LINE FEED endings declares the same row -- the discriminating half, and the reason the CR case is a claim about the ENDING rather than about the fixture	536e4bfc0cdea30d
CASE	POSITIVE	(R25 §6.7) a HARD line break spelled with a BACKSLASH names the row across it: `Slice\\` + a line ending + `C` reads `Slice C` (§2.4, vendored Example 16: `foo\\` + an ending renders `<p>foo<br />` + `bar</p>`), so the umbrella row `C` is a naming site.  Until R25 the backslash stood in the stream, `NOUN_ANCHOR` could not reach the id, and the run exited 0 with no site at all	75630a7113b58ba3
CASE	POSITIVE	(R25 §6.7) the TWO-SPACE hard spelling, green before the fix too: its spaces render as whitespace, which is what the ending contributes anyway, so this spelling never held the defect and the three must agree	d35f671c757cb570
CASE	POSITIVE	(R25 §6.8) the SOFT-break partner, green before the fix: the same two words across a plain line ending name the same row -- this is the control that says the backslash case is about the BACKSLASH and not about the adjacency	406ad5aae9587942
CASE	POSITIVE	(R26 file) `9z(x.md)` reports `9z`: the run ending at the suffix may not hold the parenthesis still OPEN there, so it starts one past it and the name is `x.md` -- the half that says the start is the innermost open parenthesis and not the segment	84f388dbde53b1cf
CASE	POSITIVE	(R26 file) `9z)foo.md` still reports `9z`: a `)` that closes nothing may be held by no run and crossed by none, so the name is `foo.md` and the id before it is outside -- the second discriminating half, against a scan that balanced parentheses by IGNORING the ones it could not match	fb088511d95c4428
CASE	POSITIVE	(R26 file) `foo((9z)) .md` -- the same characters with a space before the suffix -- still REPORTS `9z`: a run ends at whitespace, so this is not a file name at all.  The discriminating half: a fix that widened the token until every parenthesis was swallowed would pass the three above and fail this one	e82965518a5bc9bb
CASE	POSITIVE	(R26 §6.3) a destination nested 32 deep IS a link, so the memo it names is a sibling this run could not read: rc 2.  Green before the limit as after it -- the half that says the limit is a LIMIT and not a refusal of nested destinations	9bd0906c7c1c7583
CASE	POSITIVE	(R27 noun) ``Slot `#11-zz-alpha` — **UMBRELLA, …**`` attributes the marker to the named row: the containing row is a POINTER and the attribution finding is emitted. The schema noun the §8 table's own id column is headed with, and the one spelling the hand-written alternation left out	6107cc1c941aed51
CASE	POSITIVE	(R30 id) an id cell `***` renders literally too: the strip took the whole run whatever its length, so a longer run was as blank as a shorter one -- unkeyed, rc 2	46027ca45fdbbadb
CASE	POSITIVE	(R30 id) an id cell `**` is an unmatched strong-emphasis run: §6.2 pairs nothing with it and a reader sees `**`, so the row is UNKEYED -- a schema miss, rc 2.  The reported shape: before R30 the unconditional strip made it the empty-string blank and this row -- marker in its declaring field, a `Deps` edge -- exited 0	c26f004bdf55c704
CASE	POSITIVE	(R30 id) an id cell `*` is a single unmatched delimiter: the same run one character short, and not a `DECOR_MARK` at all -- unkeyed, rc 2	6801b8c8c625171e
CASE	POSITIVE	(R30 id) an id cell `` ` `` is one backtick that opens no code span (§6.1 wants a closing backtick string): a reader sees the backtick, so the row is unkeyed, rc 2	98ea2fdbb7c07052
CASE	POSITIVE	(R30 id) an id cell `` `?` `` is unkeyed: a code span RENDERS its content (§6.1) and a reader sees `?`.  The discriminating case for WHICH rendering: the disposed stream -- what every other predicate over a block reads -- blanks a code span, so under it this cell is empty and the row is a blank, which is the silent-skip class re-opened	7e69702752168790
CASE	POSITIVE	(R30 id) an id cell ``` `` ``` is ONE backtick string of length 2 that closes nothing (§6.1: the closer must be of EQUAL length), so it renders literally.  The case that says the pairing authority is the inline LEXER and not the id grammar's decoration walk: that walk reads two `DECOR_MARKS`, so a hand-rolled peel of matched pairs -- the other shape offered for this fix -- strips it to nothing and calls the row a blank	62a0ef161cedef9e
CASE	POSITIVE	(R30 id) the keyed partner, end to end: `**7z**` keys the row, so the marker and the `Deps` edge ARE asserted -- UMBRELLA-CELL, rc 1.  This is the run the six unkeyed cells above silently left, and it must not be reached by asking the blank question of a row that declared an id	cc4da0c368d1cbfb
CASE	POSITIVE	(R30-3 §6.4) a kind phrase crossing a LINK inside a resolved image description is read: `![KIND [UNDETERMINED](x)](img.png)` renders the alt text `KIND UNDETERMINED` (Example 575), so the row is kind-undetermined and its `Deps` edge is asserted.  The reviewer's shape: the demoted link's `[` was popped off the opener list, stood in the stream, and the row left the census as terminal at rc 0	ac9ad5416778e302
CASE	POSITIVE	(R30-3 §6.4) a resolved image's own `![` is a BLANK where it is NOT demoted, and the difference is loud: in `KIND ![UNDETERMINED](img.png)` the phrase STRADDLES the image, and the text around an image and the text of its description are two runs (a reader of the first sees a picture where the second stands), so the row is not kind-undetermined -- but the near-miss is reported as a schema miss at rc 2 rather than passed over.  The discriminating half of the clause above: recording the opener as a blank is what makes the phrase visibly CROSS something, and without the record the `![` is literal text, nothing is straddled, and the run exits 0 saying nothing	186e8c49aafb40c6
CASE	POSITIVE	(R30-3 §6.4) and the phrase crossing a nested IMAGE's markup: `![KIND ![](i.png)UNDETERMINED](img.png)` renders `KIND UNDETERMINED` (Example 574: a nested image's alt text is folded into the enclosing one's).  The inner `![` is the third piece of markup, and it was recorded nowhere at all -- neither clause above reaches it, since a link's `[` is recorded and a tail is recorded	da9ff4020590ccac
CASE	POSITIVE	(R30-3 §6.4) the phrase with the inner markup simply REMOVED -- `![KIND UNDETERMINED](img.png)` -- is the three positives' CONSTANT: a description whose plain string content is the phrase outright, read as prose exactly as §6.4 reduces it.  Green before R30-3 and after, which is what makes the three above claims about the MARKUP a description encloses rather than about descriptions	7435a748a505caf1
CASE	POSITIVE	(R30-3 §6.4) the same phrase crossing a demoted link's TAIL: `![KIND [](x)UNDETERMINED](img.png)` renders `KIND UNDETERMINED` too, because an empty label contributes an empty string and the tail contributes nothing.  This is the clause the opener case cannot see -- there the whole phrase stands before the tail -- and a tail disposed as a BLANK puts a run boundary through the middle of the phrase	90550e2a48671362
CASE	POSITIVE	(R31-1 §2.5) a linked memo whose `slice` header spells `#` as `&#35;` IS that schema: the character reference renders `#`, so the table is bound, `8z` is declared an umbrella and the terminal row's `Deps` edge is a naming site.  The reported shape -- before R31 the raw comparison made it a non-schema table and every declaration and assertion in the memo left the run at rc 0	5942f321265e0d6c
CASE	POSITIVE	(R31-1 §2.5) the same sibling with the header spelled `#` outright reports the same one site -- the discriminating half: the memo, the rows and the edge are the control's constants and only the SPELLING of one header cell differs, so the case above is a claim about the reading and not about the fixture	f3373959facc6741
CASE	POSITIVE	(R32 §2.5/GFM) a `&#124;` inside a cell does NOT split it: the row keeps its six cells, so it is no schema miss and the `Deps` edge is still read.  Split at the reference, the row would have seven cells and the run would be rc 2 -- which is why the measure here is the SITE and not merely the exit status	1c0002a150e59171
CASE	POSITIVE	(R32 §2.5/GFM) a delimiter row spelled `&#45;&#45;&#45;` is NO delimiter row: a table's shape is settled over RAW text before any inline construct exists, so the character reference never becomes a hyphen, the table never forms, and the linked memo declares nothing.  The clause `admit_table` states and nothing probed	b937393e0e10be11
CASE	POSITIVE	(R32 §2.5/GFM) the same sibling with a plain `---` delimiter row DOES form the table and names the umbrella -- the discriminating half: the memo, the rows and the edge are the control's constants and only the delimiter row's spelling differs	8d491f704f2e9bce
CASE	POSITIVE	(R33-1) `Slice 9z — **UMBRELLA, …**` attributes the marker to the named row: the appositive reading, unchanged.  The control's constant -- what R33-1 narrows is where the search may BEGIN, never what a real row noun means	26e57c0c70f39712
CASE	POSITIVE	(R33-2) and the HYPHEN spelling, also green before -- three spellings of one separator, which is why the set is spelled once and composed rather than written out at each reader	b577e99af069b557
CASE	POSITIVE	(R33-2) the EM DASH spelling, green before the fix -- the discriminating half: the row, the phrase and the edge are the control's constants and only the dash differs	2605e4d41c29146d
CASE	POSITIVE	(R34-1) `9z+notes.md_tail owns it` REPORTS `9z`: the run continues into more name, `sibling_path` follows no such file, and so the run is no file name and the id in it is prose.  The reported shape -- read as 'the suffix is not followed by an alphanumeric', `9z+notes.md` was masked, the ownership claim produced no site and the run exited 0	b70a666425716399
CASE	POSITIVE	(R34-2) an umbrella row whose `Deps` cell holds only a bare `.md` name carries an edge: the construct RENDERS something a reader sees, so the cell is not empty and UMBRELLA-CELL is emitted.  Read off the prose-scanning stream -- which blanks exactly the constructs a prose scanner must not read as prose -- the cell was EMPTY and the row's forbidden dependency left the run at rc 0	d0ed4ccd0b98e13c
CASE	POSITIVE	(R34-2) an umbrella row whose `Deps` cell holds only a citation `[C1]` carries an edge: the construct RENDERS something a reader sees, so the cell is not empty and UMBRELLA-CELL is emitted.  Read off the prose-scanning stream -- which blanks exactly the constructs a prose scanner must not read as prose -- the cell was EMPTY and the row's forbidden dependency left the run at rc 0	c46d04d214887fcf
CASE	POSITIVE	(R34-2) an umbrella row whose `Deps` cell holds only a code span ``b.rs`` carries an edge: the construct RENDERS something a reader sees, so the cell is not empty and UMBRELLA-CELL is emitted.  Read off the prose-scanning stream -- which blanks exactly the constructs a prose scanner must not read as prose -- the cell was EMPTY and the row's forbidden dependency left the run at rc 0	63ce0aff4a939ea7
CASE	POSITIVE	(R34-2) an umbrella row whose `Deps` cell holds only an autolink `<https://x.example>` carries an edge: the construct RENDERS something a reader sees, so the cell is not empty and UMBRELLA-CELL is emitted.  Read off the prose-scanning stream -- which blanks exactly the constructs a prose scanner must not read as prose -- the cell was EMPTY and the row's forbidden dependency left the run at rc 0	f5ac609788423fb1
CASE	POSITIVE	(R35) the discriminating half: with the SAME id one space away from the same name, `See notes.md 9z owns it` REPORTS it -- what the tail rule covers is the run, and a new run is prose again	cdb8d3fba92b5f5c
CASE	POSITIVE	(R38 seed) the partner that bounds it: a `Deps` cell that IS a deliberate blank (an em dash) with the same ordering prose still reports the no-edge finding -- the reading moved and `is_empty` still decides by SHAPE	f2724ef18c9a54ae
CASE	POSITIVE	(R42 §6.1/§6.4) a PADDED code span inside a resolved image description renders its TRIMMED content: §6.1 removes one leading and one trailing space when the content begins and ends with one, and §6.4 uses that string -- so `` ![Slot #11-zz-alph` a ` owns it](img.png) `` has the alt text `Slot #11-zz-alpha owns it` and the slot is ONE reported site.  Read verbatim it splits into `#11-zz-alph` and `a`, which are declared nowhere: no site, rc 0, and no residue to say the checker could not read it	337d4b66a9b17c61
CASE	POSITIVE	(R42 §6.1/§6.4) the arm the spec spells out, and the one an unconditional trim breaks: a span that consists ENTIRELY of spaces is NOT trimmed ("but does not consist entirely of space characters"), so `` ![Slot #11-zz-alpha`  `owns it](img.png) `` keeps the separator and the slug stands alone -- trimmed, it would fuse with what follows and the site would vanish.  ⚠ The span is the ONLY separator here, deliberately: written with a space after the closing backtick the control stayed green under the mutant, because that space did the separating and the claim was never tested	b00a6c49fc3f38b6
CASE	POSITIVE	(R42 §6.1/§6.4) §6.1's TWO steps run in ORDER: a code span padded with a LINE ENDING inside a resolved image description is normalised to a space FIRST and only then trimmed, so `` ![Slot #11-zz-alph`<NL>a ` owns it](img.png) `` has the alt text `Slot #11-zz-alpha owns it` and the slot is ONE reported site.  Written against the RAW content the trim declines -- `\\n` is not a space -- the slug splits into `#11-zz-alph` and `a`, declared nowhere, and the run exits 0 saying nothing.  ⚠ This is the half of the R42-7 fix that shipped UNPINNED: both R42-6 mutants patch the trim, so step one could be made a no-op with every control green	c7512c47c883af37
CASE	POSITIVE	(R42 §6.4) a declared id inside a §6.1 code span inside a RESOLVED image description is one reported site, the same as in bare prose: §6.4 reduces the description to the plain string content of its inline children, so every row of §3.0b's closed list contributes its text and none of its markup.  §6.1 and §6.5 were the two rows the image-close branch never demoted -- a code span there BLANKED a kind marker and an autolink contributed nothing at all	0f8bfd7323123371
CASE	POSITIVE	(R42 §6.4) a declared id inside a §6.2 emphasis inside a RESOLVED image description is one reported site, the same as in bare prose: §6.4 reduces the description to the plain string content of its inline children, so every row of §3.0b's closed list contributes its text and none of its markup.  §6.1 and §6.5 were the two rows the image-close branch never demoted -- a code span there BLANKED a kind marker and an autolink contributed nothing at all	447a168b3ff04ce5
CASE	POSITIVE	(R42 §6.4) a declared id inside a §6.3 link text inside a RESOLVED image description is one reported site, the same as in bare prose: §6.4 reduces the description to the plain string content of its inline children, so every row of §3.0b's closed list contributes its text and none of its markup.  §6.1 and §6.5 were the two rows the image-close branch never demoted -- a code span there BLANKED a kind marker and an autolink contributed nothing at all	8c9075d2a776cb0b
CASE	POSITIVE	(R42 §6.4) a declared id inside a §6.4 nested image alt inside a RESOLVED image description is one reported site, the same as in bare prose: §6.4 reduces the description to the plain string content of its inline children, so every row of §3.0b's closed list contributes its text and none of its markup.  §6.1 and §6.5 were the two rows the image-close branch never demoted -- a code span there BLANKED a kind marker and an autolink contributed nothing at all	0b4ccee3af02621a
CASE	POSITIVE	(R42 §6.4) a declared id inside a §6.5 autolink inside a RESOLVED image description is one reported site, the same as in bare prose: §6.4 reduces the description to the plain string content of its inline children, so every row of §3.0b's closed list contributes its text and none of its markup.  §6.1 and §6.5 were the two rows the image-close branch never demoted -- a code span there BLANKED a kind marker and an autolink contributed nothing at all	8a2d1b105ea53398
CASE	POSITIVE	(R42 §6.4/§6.1) a DECORATED id in bare prose (the same reading) reports the one unlicensed id: the id-only exception runs before the demotion, so the delimiters stand and the two ids do not glue -- the precedent `dispose` already sets for the `**` pair, and the reason a demoted code span is routed through `id_only` rather than blanked	12e276622746f186
CASE	POSITIVE	(R42 §6.4/§6.1) a DECORATED id inside a resolved image description reports the one unlicensed id: the id-only exception runs before the demotion, so the delimiters stand and the two ids do not glue -- the precedent `dispose` already sets for the `**` pair, and the reason a demoted code span is routed through `id_only` rather than blanked	bb9ef11d112b263c
CASE	POSITIVE	(R42 §6.4/§6.1) a DECORATED id the `**` twin in bare prose reports the one unlicensed id: the id-only exception runs before the demotion, so the delimiters stand and the two ids do not glue -- the precedent `dispose` already sets for the `**` pair, and the reason a demoted code span is routed through `id_only` rather than blanked	ee4a3106aac13f91
CASE	POSITIVE	(R42 §6.4/§6.1) a DECORATED id the `**` twin inside an image (the precedent) reports the one unlicensed id: the id-only exception runs before the demotion, so the delimiters stand and the two ids do not glue -- the precedent `dispose` already sets for the `**` pair, and the reason a demoted code span is routed through `id_only` rather than blanked	563d76df59c2e1ff
CASE	POSITIVE	(R42 §6.4/§6.1) a kind marker wholly inside a CODE SPAN inside a resolved image description is read: §6.4 reduces the description to the plain string content of its inline children, so the alt text holds the marker and the row's `Deps` edge is asserted.  The reported shape: the span stayed in `lx.code`, the disposition blanked the whole marker, the row read TERMINAL and the run exited 0 -- while the identical marker in a LINK inside the same image exited 1	c37e9295a9d96b9c
CASE	POSITIVE	(R42) a DELIBERATE blank id cell whose declaring field claims the row is an umbrella is a contradiction, not a non-row: keyed by nothing it is absent from `ids`, so assertion (b) never reads its `Deps` edge -- reported as a schema miss at rc 2 rather than passed over at rc 0	7550801618b8f421
CASE	POSITIVE	(R42-10) a LINKED memo whose slice table binds to no schema -- its header reads `No.` where the schema reads `#` -- but carries an umbrella row with a `Deps` edge: the claim is made and the whole table leaves the census.  It exited 0, with no gate reporting it, because the schema-miss gate asks only of `main`	bd5b7604d042c2bb
CASE	POSITIVE	(R42-8 §6.6) an id inside a raw HTML ATTRIBUTE inside a resolved image description is a reported naming site: the alt text spells the tag out, so `9z owns it` is document text there.  This is what a blank mask cannot give -- it would not join the sides either, and would still hide the id	b53758ddb362f6dc
CASE	POSITIVE	(R42-8 §6.6) the BASELINE that control is measured against -- the identical cell with the span taken out reads the marker and exits 1, so the silence above is the span's and not the fixture's	69972d94918169c4
CASE	POSITIVE	(R42-9) a blank id cell whose declaring field spells KIND UNDETERMINED is the same contradiction as the marker: §5 gives an undetermined row the no-owner obligation an umbrella has, and keyed by nothing it is absent from `ids`, so assertion (b) never reads its `Deps` edge -- rc 2, not the silent 0 the marker-only predicate gave	053b0dcf7719f17c
CASE	POSITIVE	(R45 GFM §4.10) a delimiter cell of centred admits the table exactly as `---` does -- "cells whose only content are hyphens (-), and optionally, a leading or trailing colon (:)" sets no minimum length.  Verified against cmark-gfm 0.29.0.gfm.13, which makes a `<table>` from all four; a review finding asking for a three-hyphen minimum is the FP this pins	36da346ad031bd2a
CASE	POSITIVE	(R45 GFM §4.10) a delimiter cell of left-aligned admits the table exactly as `---` does -- "cells whose only content are hyphens (-), and optionally, a leading or trailing colon (:)" sets no minimum length.  Verified against cmark-gfm 0.29.0.gfm.13, which makes a `<table>` from all four; a review finding asking for a three-hyphen minimum is the FP this pins	76976f1d967e4806
CASE	POSITIVE	(R45 GFM §4.10) a delimiter cell of one hyphen admits the table exactly as `---` does -- "cells whose only content are hyphens (-), and optionally, a leading or trailing colon (:)" sets no minimum length.  Verified against cmark-gfm 0.29.0.gfm.13, which makes a `<table>` from all four; a review finding asking for a three-hyphen minimum is the FP this pins	21bfee3ca65c3b4b
CASE	POSITIVE	(R45 GFM §4.10) a delimiter cell of right-aligned admits the table exactly as `---` does -- "cells whose only content are hyphens (-), and optionally, a leading or trailing colon (:)" sets no minimum length.  Verified against cmark-gfm 0.29.0.gfm.13, which makes a `<table>` from all four; a review finding asking for a three-hyphen minimum is the FP this pins	d43da6e1fd3bdc81
CASE	POSITIVE	(R45 scope) a LINKED memo's UNKEYED row is reported: `_unkeyed` asks of the whole population, not of `main`.  Scoped to `main` the sibling's `xxxxWz` id cell declares nothing, the row is unscanned and the run exits 0 -- the I-C class, in the memo the census exists to reach	773976579e5c42ab
CASE	POSITIVE	(R45 scope) a LINKED memo's table WIDTH miss is reported: the table-miss loop asks of the whole population too.  Scoped to `main` the sibling's 7-cell row under a 6-cell header leaves the run at 0 with nothing saying the row was skipped	a151d75fc8dba6cc
CASE	POSITIVE	(R47-1) a numeric id introduced by a row noun and a plain space is one reported site: the separator composes `plan_memo_ids.DASH`, which is the ONE home of the three spellings, rather than re-spelling a narrower set.  With the ASCII hyphen alone, `Slice — 9 owns the close rule.` matched no appositive, the bare pass deliberately ignores a numeric id, and the ownership claim produced no site at rc 0	0819e13dc727603d
CASE	POSITIVE	(R47-1) a numeric id introduced by a row noun and an ASCII hyphen is one reported site: the separator composes `plan_memo_ids.DASH`, which is the ONE home of the three spellings, rather than re-spelling a narrower set.  With the ASCII hyphen alone, `Slice — 9 owns the close rule.` matched no appositive, the bare pass deliberately ignores a numeric id, and the ownership claim produced no site at rc 0	f05ef76329e7e7e7
CASE	POSITIVE	(R47-1) a numeric id introduced by a row noun and an EM dash is one reported site: the separator composes `plan_memo_ids.DASH`, which is the ONE home of the three spellings, rather than re-spelling a narrower set.  With the ASCII hyphen alone, `Slice — 9 owns the close rule.` matched no appositive, the bare pass deliberately ignores a numeric id, and the ownership claim produced no site at rc 0	c4cc4a47da9b6a18
CASE	POSITIVE	(R47-1) a numeric id introduced by a row noun and an EN dash is one reported site: the separator composes `plan_memo_ids.DASH`, which is the ONE home of the three spellings, rather than re-spelling a narrower set.  With the ASCII hyphen alone, `Slice — 9 owns the close rule.` matched no appositive, the bare pass deliberately ignores a numeric id, and the ownership claim produced no site at rc 0	1bf253b072eb37f1
CASE	POSITIVE	(R47-2) a LINKED memo's unbound table whose kind claim STRADDLES a masked span is the same miss as a plain one: `` **UMBRELLA, not a `terminal` unit.** `` is what a reader sees and what the disposed stream does not, so a stream-only search found nothing and the run exited 0 with the table's declarations and `Deps` gone.  Asked through `kind_disagreements`, the CANONICAL question `_kind_residue` already gates a bound row with -- not a second rule	5e0dd606037092c1
CASE	POSITIVE	(R47-4) the UNDETERMINED phrase has its own spelling of the gap and the same defect: under `re.ASCII` its `\\s` is ASCII whitespace, so a U+00A0 between `KIND` and `UNDETERMINED` read as no kind at all.  The scope `(?u:...)` widens the GAP alone and leaves the ASCII case folding, which is why the phrase is still not matched by a long-s spelling	f5ae910ccc3df449
CASE	POSITIVE	(R47-4) the marker phrase written with `&#10;` (a line ending, which a reader reads as a gap) is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0	c5828635e41f6ab4
CASE	POSITIVE	(R47-4) the marker phrase written with `&#32;` (decodes to U+0020 -- the arm the defect never touched) is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0	2cdb828e0e05ddf5
CASE	POSITIVE	(R47-4) the marker phrase written with `&#9;` (a tab) is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0	a99d3c8a2f3574a8
CASE	POSITIVE	(R47-4) the marker phrase written with `&nbsp;` (U+00A0 -- the spelling a human actually types) is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0	7c2adc1638998faf
CASE	POSITIVE	(R47-4) the marker phrase written with a LITERAL U+00A0 typed into the cell is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0	fb0c76e7dd07013b
CASE	POSITIVE	(R47-4) the marker phrase written with a plain space (the baseline arm: this one ALWAYS worked) is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0	110a7e7f420f706c
CASE	POSITIVE	(R47-5 scope) EVERY declared id is given a kind, not just the first: the loop over `self.ids` walks the whole map.  Truncated, only one row is classified and the census under-counts the no-owner rows it exists to take	0396d83c9bf53a7b
CASE	POSITIVE	(R47-5 scope) TWO kind phrases straddling masked spans in ONE declaring field are both reported: `_kind_residue` walks every phrase `kind_disagreements` returns.  Truncated, the row's second undecidable kind is silent	ea95b26083adfd6b
CASE	POSITIVE	(R47-5 scope) TWO table misses in one memo are both reported: the loop over a table's misses walks all of them.  Truncated, the second row is unscanned at rc 2 with only the first named -- a reader fixes one and believes the table is clean	81639cf7910a4e0c
CASE	POSITIVE	(R47-5 scope) TWO unresolved references are both reported: the loop over a memo's unanswered references walks all of them.  Truncated it reports the first and the second reference's memo is missing from the population with nothing saying so	e410b8ee81ef2580
CASE	POSITIVE	(R47-5 scope) a memo's SECOND schema row is read for the unkeyed miss too: the sibling's first row is keyed and its second is not.  Truncated to the first row the unkeyed one declares nothing and leaves the census at rc 0	1df9a4579f49dac1
CASE	POSITIVE	(R47-5 scope) a memo's SECOND table is asked for an unbound claim too: the sibling carries a bound table and then an unbound one whose row carries the marker.  Truncated to the first table the claim is silent and the run exits 0	c86714f3dfbd7d51
CASE	POSITIVE	(R48-2) TWO undetermined spellings in ONE declaring field are both collected: the KIND-SPELLING gate is about the DOCUMENT's spellings, so a field using both reports exactly as two rows using one each do.  Reading only the first match, the set had size one and the run exited 0 on the condition that gate exists for	06c2f09f357d10f4
CASE	POSITIVE	(R48-2) the discriminating twin -- the SAME two spellings split across two rows -- was always reported, which is why the one-field case looked covered	63636a2cc638880f
CASE	POSITIVE	(R49-1) a BLANK-id row whose declaring field spells a kind ACROSS a masked span is the same contradiction as the clean spelling: the blank-id check asked the disposed stream alone, so `` **UMBRELLA, not a `terminal` unit.** `` on a blank-id row exited 0 while the clean form exited 2.  Asked through the ONE contradiction site now, so a caller cannot forget the arm	6862a1abdb611192
CASE	POSITIVE	(R49-1) the clean twin, which was ALWAYS reported -- so the case above is a claim about the READING and not about the fixture	258c8a73ca3f760d
CASE	POSITIVE	(R49-2) an umbrella's `Deps` cell whose whole content is a resolved link with a punctuation-only label carries an edge: the reader rendering drops a link's TAIL, so the cell had no alphanumeric character and read as EMPTY -- while the population walker was following that very sibling.  A resolved link is an edge whatever its label renders as	b2fc2623fc42f25b
CASE	POSITIVE	(R49-2) an umbrella's `Deps` cell whose whole content is a resolved link with an EMPTY label carries an edge: the reader rendering drops a link's TAIL, so the cell had no alphanumeric character and read as EMPTY -- while the population walker was following that very sibling.  A resolved link is an edge whatever its label renders as	a625ae486efd1bb0
CASE	POSITIVE	(R51) a blank-id row whose field straddles a masked span with TWO kind phrases names BOTH: `_claims` marks every disagreement the canonical question returns, not the first.  Truncated, the miss reads one kind and a reader deleting that one believes the row is settled	fe2c91c0ea45461f
CASE	POSITIVE	(R52) a marker OUTSIDE the declaring field, STRADDLING a masked construct: assertion (a)'s outside-field check searched the disposed STREAM alone, so the straddled spelling was invisible and the row left at rc 0 with only a non-gating seed while the clean spelling exited 1.  Asked through the canonical `_claims` now -- which keeps the quoted case by construction, since a phrase quoted whole is not a straddle	0119a99b1fe4bbed
CASE	POSITIVE	(R52) a marker OUTSIDE the declaring field, spelled cleanly (the twin that was ALWAYS reported): assertion (a)'s outside-field check searched the disposed STREAM alone, so the straddled spelling was invisible and the row left at rc 0 with only a non-gating seed while the clean spelling exited 1.  Asked through the canonical `_claims` now -- which keeps the quoted case by construction, since a phrase quoted whole is not a straddle	4175ad766b46ca6e
CASE	POSITIVE	(a) a marker that names ANOTHER row is not a self-declaration	a08b855182c7a379
CASE	POSITIVE	(a) a row carrying the marker in another cell and NOT in its declaring field is the same finding (the arm that already worked)	6127067e93c97637
CASE	POSITIVE	(a) a row carrying the marker in its declaring field AND again in another cell is the double marker the assertion forbids: UMBRELLA-MARK 1	3a5bc701f5522dee
CASE	POSITIVE	(a) the R20 reviewer's declaring field `Slice `#11-zz-alpha` — **UMBRELLA, …**` attributes the marker to a SLUG row: the row is a pointer and the UMBRELLA-MARK attribution finding is emitted	150c6f29682206c0
CASE	POSITIVE	(a) the bold slug form `Slice **#11-zz-alpha** — **UMBRELLA, …**` attributes the marker too	bd219081f1830967
CASE	POSITIVE	(a) the marker outside the declaring field certifies nothing	c31e4091e26a8714
CASE	POSITIVE	(a) the short form `Slice **9z** — **UMBRELLA, …**` in the same position still attributes (the alternation lost nothing)	731c651922d1a128
CASE	POSITIVE	(a-seed) a row declaring the kind in words carries no marker	f6681cb9b17a0da1
CASE	POSITIVE	(a-seed) fires on a cell QUOTING the criterion too -- a measured false positive, kept visible rather than filtered	15a9468879d0d7de
CASE	POSITIVE	(accept-vocab seed) a retirement word inside a code span does not retire the row	55dc91847869e66e
CASE	POSITIVE	(accept-vocab seed) a terminal row with neither `must` nor `acceptance`	279a2487f7930e98
CASE	POSITIVE	(anchor) `` Slice `C` owns it ``: an id-only code span stands in the stream, so the anchored `Slice C` is still a site	7cbde2f0e0f08522
CASE	POSITIVE	(autolink) the same URL WITHOUT the angle brackets is prose (§6.5 Example 611: a bare URL is no autolink), so the id in it IS a site -- the control above has a live subject	16be83f9f3b9660c
CASE	POSITIVE	(autolink) the same brackets OUTSIDE an autolink are a link to a memo that is not there: rc 2 -- the control above has a live subject	7af3f083bef03670
CASE	POSITIVE	(autolink) the same id in a raw HTML SPAN is a seed -- the two dispositions differ, and the control above has a live subject	0de672b0d4e94035
CASE	POSITIVE	(b) a Deps cell `nil` -- a word outside the lexical exceptions -- is NOT empty: the stated polarity is a reported edge (false rc 1), never a silent skip	38c832e94abde516
CASE	POSITIVE	(b) a KIND-UNDETERMINED row carrying a Deps edge is checked too	4f092ac6961f748b
CASE	POSITIVE	(b) an umbrella row carrying a Deps edge	4966eb430958fb56
CASE	POSITIVE	(bare) a decorated side is bounded by its decoration: `` `9z`-`7z` `` is two ids	17b85a277522a557
CASE	POSITIVE	(bare) a full stop after an id is a boundary	6f8a97e59a7cebab
CASE	POSITIVE	(bare) the decoration closes the token even against an id character: `**9z**7z`	a467d209030a4566
CASE	POSITIVE	(block) `[Slice 9z owns it]:\\n#` is a paragraph and an ATX heading, not a definition	596c82cfee155e01
CASE	POSITIVE	(block) `[Slice 9z owns it]:\\n***` is a paragraph and a thematic break, not a definition	9ffa045e1c35fff4
CASE	POSITIVE	(block) `[Slice 9z owns it]:\\n---` is a setext heading (`<h2>…</h2>`), not a definition: the label line is paragraph text and its site is reported	03e2f47b3f4c35f4
CASE	POSITIVE	(block) `[Slice 9z owns it]:\\n<div>` is a paragraph and an HTML block (type 6 interrupts a paragraph), not a definition	4326f04b912eef96
CASE	POSITIVE	(block) `[Slice 9z owns it]:\\n>` is a paragraph and a block quote, not a definition	22912a975f7bd51a
CASE	POSITIVE	(block) `[Slice 9z owns it]:\\n\pa\pb\p\\n\p--\p--\p` -- the table header ends the run (local policy over pure CommonMark, which has no tables): a paragraph and a table, not a definition with the header row as its destination	14a62d01f800f1de
CASE	POSITIVE	(block) `[foo]:\\n- item`: the list item interrupts, so `[foo]:` is a paragraph and a code span opened before the marker does not cross it	c2845bc8eba68caf
CASE	POSITIVE	(c-seed) `Lands after Slice `#11-zz-alpha`` in a Slice cell whose Deps cell names only **9z**: the anchored reading admits the slug, so the prose names a party the cell does not carry -- ORDER-PROSE? 1	1911c0f0fce00fd9
CASE	POSITIVE	(c-seed) a Deps cell `--` is empty by shape	da6c3ce8ef6a25ba
CASE	POSITIVE	(c-seed) a Deps cell `N/A` is empty (the lexical exception is case-insensitive)	59ea319de78598a2
CASE	POSITIVE	(c-seed) a Deps cell `n/a` is empty, so ordering prose is reported	eba93f429e30e981
CASE	POSITIVE	(c-seed) a Deps cell `xxxxC` does not carry the id `C`	f08efb6659234ebc
CASE	POSITIVE	(c-seed) a Deps cell `–` (en dash) is empty by shape: no alphanumeric	4d4da739b7dd0bb5
CASE	POSITIVE	(c-seed) a Deps cell naming a FILE whose name holds the id does not carry the id	33da10d624b42a14
CASE	POSITIVE	(c-seed) a sibling of the SAME basename in another directory, whose Deps cell at the same line names the party, does not discharge the main memo's row	bd5cddd5a6ae9ecf
CASE	POSITIVE	(c-seed) ordering vocabulary in prose against an empty Deps cell	5d31022edb140032
CASE	POSITIVE	(c-seed) prose names a SECOND party the non-empty Deps cell omits	87198139b15ba874
CASE	POSITIVE	(cell) a `Deps` cell naming a row is still an edge: the control above has a live subject	20d3d25c40148cc2
CASE	POSITIVE	(d) `owned by `#11-zz-alpha` and **Qx**`: a SLUG owner and a short owner are a two-owner clause (`OWNS_TWO` composes `ROW_ID`)	c2cd629f6d6bed15
CASE	POSITIVE	(d) one clause assigning a deliverable to two owners	ee97b6f2272f6662
CASE	POSITIVE	(def) `paragraph\\n[x]: child.md\\n[x]`: the orphan names a sibling on disk -- the documented miss, rc 2 (unchanged)	5a9f2924a4c13446
CASE	POSITIVE	(def) a cell shaped like a definition is inline content and is scanned	6ab687c60df25f41
CASE	POSITIVE	(def) a next line that is NOT a valid title is prose (the definition ends at the destination)	fa0788dc8352dff2
CASE	POSITIVE	(def) a would-be MULTILINE definition that interrupts a paragraph is an orphan: the shortcut naming it is a schema miss, not an exempt citation-style shortcut	e8c68b199c8e9e92
CASE	POSITIVE	(fence) a backtick fence whose info string holds a backtick is not a fence	396716c813d41820
CASE	POSITIVE	(fence) four spaces of indent is not a fence	114acd5770ad40e5
CASE	POSITIVE	(file) a link's visible text is not swallowed into the destination token: `[Slice 9z](slice-9z-sib.md)` still reports `9z`	2d80846d1c5e0327
CASE	POSITIVE	(file) bare `md` (no suffix) beside a declared no-owner id `md` IS a site -- the subject of the two controls above is live	027488628ed3cb3c
CASE	POSITIVE	(html) `- item\\n<span>\\n9z owns it`: the item's paragraph is open (lazy continuation), so type 7 does not open a block: paragraph text, the site is reported	daee22f9b56e7e8c
CASE	POSITIVE	(html) `< span title="[x](absent.md)">`: nothing may stand between `<` and the tag name -- literal, the link is read, rc 2	453ae09fbf266356
CASE	POSITIVE	(html) `<! Slice 9z owns it>`: no ASCII letter after `<!`, no declaration -- the site is reported	be9f42e7c23ca066
CASE	POSITIVE	(html) `<! [x](absent.md)>`: a declaration needs an ASCII letter right after `<!` -- literal, rc 2	9f4082a139dde346
CASE	POSITIVE	(html) `<!-- Slice 9z owns it --` is not closed: prose, the site is reported	4945c78ba0d94a67
CASE	POSITIVE	(html) `<!-- [x](absent.md) --` is not closed: no comment, the link is read, rc 2	570ff8c50fe2a8a6
CASE	POSITIVE	(html) `<![cdata[ [x](absent.md) ]]>`: `<![CDATA[` is exact case -- literal, rc 2	3bb5f68c1511e9ab
CASE	POSITIVE	(html) `<![cdata[` is no CDATA opener (§4.6 condition 5 is exact; commonmark.js: a paragraph): the next line is prose and the site is reported	b1b532fc7b3c1a1c
CASE	POSITIVE	(html) `</ span [x](absent.md)>`: a closing tag's name follows `</` directly -- literal, rc 2	e93213c940e07a77
CASE	POSITIVE	(html) `<3 [x](absent.md)`: a tag name begins with an ASCII letter, so the `<` is literal and the link is read -- the absent memo is the rc-2 miss	f881071cac7ff942
CASE	POSITIVE	(html) `<a b="c"d="[x](absent.md)">`: an attribute needs whitespace before it (Example 622) -- literal, the link is read, rc 2	b9ded51801b1834a
CASE	POSITIVE	(html) `<a b="c"d=9z>x</a>`: no whitespace before `d` (Example 622), no tag -- `9z` is prose	ad533ff76709cf36
CASE	POSITIVE	(html) `<a href="x" [x](absent.md)>`: `[x](…)` is no attribute, so the `<` is literal and the link is read, rc 2	e05bd7b80632d72d
CASE	POSITIVE	(html) `<div>\\nx\\n</div>\\ny` -- a type-6 block runs to the BLANK line, not to `</div>`: `y` is raw, the paragraph after the blank is prose	55e22b4837b9afdb
CASE	POSITIVE	(html) `<pre></pre>\\n9z owns it` -- the opener meets the end condition itself, so the block is that one line and the next line is prose	95d0a83066b5c0ba
CASE	POSITIVE	(html) `<pre>\\n`\\n</pre>\\nSlice 9z` owns it`: the backtick inside the raw HTML block does not pair with the prose one -- the site is reported	0cc7bc5613215ed6
CASE	POSITIVE	(html) `<span title="`">Slice 9z owns it` x`: the tag is read first, left to right, so the backtick inside it opens no code span -- the site is reported (commonmark.js)	cc55a3a16b7d1b9a
CASE	POSITIVE	(html) `[<span>x</span>](slice-9z-sib.md)`: a link may wrap a tag -- the sibling is walked	55b6988fdd126d91
CASE	POSITIVE	(html) `[sib]: slice-9z-sib.md\\n<span>\\n9z owns it`: a definition keeps the paragraph open (§4.7; commonmark.js: `<span>` is paragraph text), so the site is reported	28d63b78341529da
CASE	POSITIVE	(html) `\\<span title="[x](absent.md)">`: an escaped `<` opens no tag (§2.4), the link is read, rc 2	86081844fe750fc4
CASE	POSITIVE	(html) `` `x <span title="`9z">y ``: the code span is read first and swallows the `<`, so no tag masks `9z` -- the site is reported (commonmark.js)	2277968390495997
CASE	POSITIVE	(html) `text\\n<span>\\n9z owns it` -- a type-7 opener cannot interrupt a paragraph (commonmark.js): the lines stay paragraph text and the site is reported	d8a70caf9c97d31e
CASE	POSITIVE	(html) the reviewer's input `<pre>\\n`\\n</pre>\\nSlice `9z` owns it` reports the site	9cf544e404538fbe
CASE	POSITIVE	(id) a backticked slug with trailing prose declares the slug	2a9e66f2c2986a04
CASE	POSITIVE	(id) a cell that does not start with an id declares nothing: the row is unkeyed (its Deps edge would go unasserted), so the run is a schema miss	030505d5dd8322bc
CASE	POSITIVE	(id) a citation id declared TWICE, the same spelling, is the duplicate miss: rc 2	b253828e387b728c
CASE	POSITIVE	(id) an id cell `**?**` is not a blank: decoration does not blank it, rc 2	2cc3fe1aeff4ed79
CASE	POSITIVE	(id) an id cell `?` is not a blank: unkeyed, rc 2	7bc804a60a9b49be
CASE	POSITIVE	(id) an id cell `…` is not a blank: unkeyed, rc 2 (the shape rule would skip it)	cb2fada272fb9661
CASE	POSITIVE	(id) an id cell with trailing prose declares the id at its start	6caddf020075bdd5
CASE	POSITIVE	(id) the `Intl`-shaped cell declares the 4-character id at its start	292b8833c472d689
CASE	POSITIVE	(id) the id cell's trailing prose is scanned: `**7z** — Slice 9z lands first` reports `9z` (the row's own `7z` is suppressed)	07ab9cf17f8b2de7
CASE	POSITIVE	(indented) `   Slice 9z owns it`: three spaces are three columns -- paragraph text, the site is reported	873581a4de2b205c
CASE	POSITIVE	(indented) `text\\n    Slice 9z owns it`: an indented line cannot interrupt a paragraph (§4.4; commonmark.js: one paragraph) -- paragraph text, the site is reported	834ee4678dfb315b
CASE	POSITIVE	(item) `- item\\n\\n    Slice 9z owns it`: the item's second paragraph is prose -- the site is reported (until R15 the line was indented code, seeded and never scanned)	60d658d8a7619116
CASE	POSITIVE	(item) `- item\\n\\n  > q\\n\\n    Slice 9z owns it`: a block quote inside the item, then the item's paragraph -- the site is reported	5797224e756bb736
CASE	POSITIVE	(item) `- item\\n\\n  para\\n\\n    Slice 9z owns it`: the item's THIRD paragraph (its second, at the content indentation, keeps it open) -- the site is reported	db1a463dcbdfb46a
CASE	POSITIVE	(item) `1. item\\n\\n     Slice 9z owns it`: an ordered item's content indentation is 3 (`1. `), so five columns are two inside it -- the item's second paragraph, the site is reported	446deb455e3b3ac7
CASE	POSITIVE	(item) `open `x\\n1. 9z owns it` end`: an ordered item starting at 1 interrupts -- the item's paragraph holds one literal backtick and the site	27c01915f73ff9b7
CASE	POSITIVE	(item) … and that raw line inside the item is seeded (the one raw-line rule reaches into a container)	d23f3c600ae18f39
CASE	POSITIVE	(item) … and that raw line is seeded under the one raw-line rule, with the §4.4 reading	e25961adc63406ca
CASE	POSITIVE	(kind) `KIND — UNDETERMINED` still declares it, and the same nonempty `Deps` cell is still `UMBRELLA-CELL`, rc 1 -- the discriminating half: the boundary must not have silenced the phrase it bounds	4cb4e03049e8028d
CASE	POSITIVE	(kind) `is a pointer rather than a slicer of work` is no pointer row: it is an active terminal, and a terminal row stating no acceptance condition owes the ACCEPT-VOCAB? seed -- which the unbounded reading suppressed	3cd0b00155e6dcc8
CASE	POSITIVE	(kind) `is an umbrella` in a declaring field without the marker still seeds it -- the discriminating half	8c7350873d0532e3
CASE	POSITIVE	(kind) a declaring field spelling the marker ACROSS a code span is the schema miss, rc 2: a reader reads the marker, the disposed stream does not, and §1 forbids a clean exit for a could-not-scan over the census	a6319e8d90b70845
CASE	POSITIVE	(kind) a declaring field spelling the marker with GFM strikethrough inside it DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	d17c79e7213db376
CASE	POSITIVE	(kind) a declaring field spelling the marker with plain DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	7b05b6182e764324
CASE	POSITIVE	(kind) a declaring field spelling the marker with §2.4 an escaped comma DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	2c96ac883978801e
CASE	POSITIVE	(kind) a declaring field spelling the marker with §2.5 a character reference for the comma DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	4a5195670c134453
CASE	POSITIVE	(kind) a declaring field spelling the marker with §6.2 emphasis inside it (`not a *terminal* unit`) DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	a8979290d68d36d5
CASE	POSITIVE	(kind) a declaring field spelling the marker with §6.6 a comment inside it DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	613895895f1e8c74
CASE	POSITIVE	(kind) a declaring field spelling the marker with §6.6 a tag pair inside it DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	3603393cba12c0f2
CASE	POSITIVE	(kind) the UNDETERMINED spelling read across emphasis: `KIND — UNDE*TER*MINED` puts the row in the no-owner census, so its mention is a site	e6e43fe7774255d3
CASE	POSITIVE	(kind) the `DECLARES` near-miss backstop is the same reading: `is an *umbrella*` in a declaring field without the marker is the seed	2f90107d9e934769
CASE	POSITIVE	(kind) the appositive attribution reads the rendered field too: `Slice **9z** — **UMBRELLA, not a *terminal* unit.**` is the UMBRELLA-MARK finding, so the pointer row stays out of the census	67a842ad485bc4df
CASE	POSITIVE	(kind) the marker itself still declares the umbrella and the mention IS a site -- the discriminating half of both controls above	d705516f71ef8e77
CASE	POSITIVE	(lex-seed) `<!-- c\\n\p9z\p\\n-->`: a `\p` line inside a comment block (type 2 ends at `-->`) is seeded	35635ee8130f8acb
CASE	POSITIVE	(lex-seed) `<pre>\\nSlice 9z owns it\\n</pre>`: the inner line holding the id is seeded (type 1 ends at `</pre>`)	2ca6a1c306c5050d
CASE	POSITIVE	(lex-seed) `<span title="Slice 9z owns it">`: an inline span holding a declared id is seeded under the one raw-line rule	6de55df33f21bc26
CASE	POSITIVE	(lex-seed) `para\\n\\n    Slice 9z owns it`: indented code after a plain paragraph is raw under CommonMark too, but holds a declared id -- seeded by the one raw-line rule	3d6fd0d5c19c4131
CASE	POSITIVE	(lex-seed) a 4-space-indented slot row after a table's rows is raw (cmark-gfm: `<pre><code>`) and is SEEDED -- it holds a `\p` -- never a silent skip (I-C)	98aa66d996d3c8d6
CASE	POSITIVE	(lex-seed) a tab-indented slot row after a table's rows is raw (§2.2: four columns) and is SEEDED	3474be0974e865b7
CASE	POSITIVE	(lex-seed) an HTML-block opener holding a declared id is a seed	74900982b258266c
CASE	POSITIVE	(lex-seed) the raw line holding a declared id inside the type-7 block after a table is seeded	6a53b428d54ea4a1
CASE	POSITIVE	(lex-seed) … and that seed carries the `inline` reading	04d6c09594be73ed
CASE	POSITIVE	(licence) `9z's memorandum` is not the licensed possession `memo`: the right-hand edge of the forward look, reported	f3016565833d1bcc
CASE	POSITIVE	(licence) `The grandchild of 9z` is not the licensing phrase `child of`: a licence SUPPRESSES a report, so an unbounded edge there is the dangerous polarity -- the mention is reported	73a1d8a83411a092
CASE	POSITIVE	(licence) a licensing phrase inside a code span licenses nothing	1951a8f3b76429c5
CASE	POSITIVE	(link) `[foo\\]][missing]` is a FULL reference (the `]` is escaped): a schema miss, not an exempt shortcut	10352848a1b894dd
CASE	POSITIVE	(link) a full reference no definition answers is a schema miss	df862c9901aeecad
CASE	POSITIVE	(link) a shortcut whose only definition sits mid-paragraph is a schema miss	64385b11c55615d1
CASE	POSITIVE	(link) …and its discriminating half: the UNESCAPED `[x](absent-file.md)` IS a link, so the memo it names is an unavailable sibling -- 1 schema miss, rc 2.  Without this the control above cannot tell a working escape from a needle that never matches	edfdc1ec638aef62
CASE	POSITIVE	(quote) `> Slice 9z owns it`: the quote's paragraph is scanned at its real line and the marker is not content -- the site is reported	5ffb489fa01ac102
CASE	POSITIVE	(quote) `> open `x\\n# Slice 9z owns it` there`: an ATX heading is never lazy -- the quote ends, the heading is a block of its own and its site is reported	0c7e9a1caeb4156e
CASE	POSITIVE	(rc) a full reference no definition answers is rc 2, never clean	e9fd0d88fca55098
CASE	POSITIVE	(rc) a link to an absent memo inside a table CELL is rc 2	f62fc7c013559aa6
CASE	POSITIVE	(rc) a linked memo that is not on disk is rc 2, never clean	7e3883a74b8d21ec
CASE	POSITIVE	(rc) a row carrying the marker AND one undetermined spelling, beside another row's other spelling, is KIND-SPELLING rc 1	a59cc96e3376ae05
CASE	POSITIVE	(rc) a schema body row whose width differs from its header is rc 2	a5542e8bc47bdcd4
CASE	POSITIVE	(rc) a schema with no matching table is rc 2	0b09cef05fca42b2
CASE	POSITIVE	(rc) that divergence is a mechanical finding: rc 1	c19e45562a1ff79a
CASE	POSITIVE	(rc) the same id declared in two memos is rc 2	f5227ea61c08837b
CASE	POSITIVE	(rc) the undetermined kind written two ways is KIND-SPELLING, rc 1	35d8166db0b9fb2b
CASE	POSITIVE	(rc) two MARKED rows spelling the undetermined kind two ways is KIND-SPELLING: the undetermined set is empty and the divergence is still real	6fa368cf52e00804
CASE	POSITIVE	(render) GFM strikethrough `W~~z~~` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	feba66392a57f539
CASE	POSITIVE	(render) `**9z**7z` still names `9z`: a `**` pair whose content is only declared ids is the document DECORATING an id, so the delimiters stand and bound it -- dropping them would leave the single token `9z7z`, which no row declares	cf64ee6ae094d3c8
CASE	POSITIVE	(render) `Slice [W](slice-9z-sib.md)z owns it` names `Wz`: the link's tail renders nothing, and the `.md` file token INSIDE it is dropped with it -- where a drop and a blank overlap the drop wins, or the tail's two sides stay apart	b0e4fa15e60d539e
CASE	POSITIVE	(render) an id-only code span is still the document SPELLING an id: `` `9z` `` is a naming site, decoration and all	d1fc1b13ed33f9dd
CASE	POSITIVE	(render) that same code span IS the `[LEX-SPLIT?]` residue seed: the reader reads `Wz` across a span the disposition blanks, and the seed says so	32fd75835cd44c5f
CASE	POSITIVE	(render) §2.5 a character reference `W&#122;` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	ef57e15a52d0cb91
CASE	POSITIVE	(render) §6.2 emphasis `W*z*` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	ac167d98fe11ca50
CASE	POSITIVE	(render) §6.2 strong emphasis `W**z**` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	5e19566320cda457
CASE	POSITIVE	(render) §6.3 a link's brackets `W[z](slice-9z-sib.md)` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	648babde8cf6a99f
CASE	POSITIVE	(render) §6.6 a raw HTML tag pair `W<b>z</b>` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	e9732bae88fc3872
CASE	POSITIVE	(render) §6.6 an HTML comment `W<!-- c -->z` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	5ee3f963a9c1004d
CASE	POSITIVE	(row) `\\\p` is `\p` in the cell, so `9z \\\p 7z` is an id-only run	1c20d901ce2a06ce
CASE	POSITIVE	(row) `a\\\\\pb` holds an UNESCAPED pipe (§2.4: `\\\\` is a literal backslash): 5 cells under a 4-cell header is a width miss, rc 2	115f06abc5ebc479
CASE	POSITIVE	(row) a table without leading and trailing pipes declares its rows	dc48c6d3cb468483
CASE	POSITIVE	(row) an unescaped `\p` inside backticks SPLITS the row: each half is prose with a literal backtick, so the id on each side is a mention	2a396af3ac4c391f
CASE	POSITIVE	(schema) a SHORT id in the citation table keys no citation row (the schema keys cite): rc 2 -- the inverse direction, which would otherwise pollute the keep-set	a3d23b10b9ca5a4a
CASE	POSITIVE	(schema) a `#11-` SLUG in the citation table is the same miss -- rc 2	cd7841f7a08b7022
CASE	POSITIVE	(schema) a citation-shaped id in the §5 SLICE table keys no slice row (the schema keys short + slug): the row is unkeyed, so its cells would go unasserted -- rc 2	5ec39e86609b2a5c
CASE	POSITIVE	(schema) a citation-shaped id in the §8 SLOT table is the same miss -- rc 2	e0bf0dfd2671d228
CASE	POSITIVE	(seed) `\p 9z \p notes.md \p` on a raw line seeds a `\p` AND `9z`: a file run is bounded by whitespace, so the id beside the name is outside every masked span	46b9d8f456fbb876
CASE	POSITIVE	(seed) a raw HTML-block line holding a BARE declared id still seeds it -- the discriminating half of the pair above: a fix that stopped reading ids off raw lines at all would pass that control and fail this one	43a6bcd9276e0c35
CASE	POSITIVE	(setext) a `---` after paragraph text is the heading's underline (§4.3 over §4.1, Example 59) and ends the paragraph like the thematic break it is not	4f0d012ecffc7077
CASE	POSITIVE	(setext) a bare `===` IS a paragraph, so `===\\n---` is the heading `<h2>===</h2>` (commonmark.js) and the code span opened in it does not reach the next site	b8101d1658a8517b
CASE	POSITIVE	(setext) the reviewer's case: `Heading\\n===\\nSlice `9z` owns it` reports the site	31266243bc794b0f
CASE	POSITIVE	(sibling) `NULX.md` is an ordinary sibling and IS walked -- the discriminating half: a device is the STEM of a component, never a prefix of one, and a guard that rejected every name holding `NUL` would drop a memo the author linked	95b63a7907003c09
CASE	POSITIVE	(slug) `` `tool #11-zz-alpha` `` inside a code span IS the kept slug, bounded by the closing backtick -- 1 site (the exception the control above must not widen)	4b5c0d609cc4140c
CASE	POSITIVE	(span) a `#11-` slug is ATOMIC in an id-only run: `` `#11-zz-alpha / 9z` `` is the document spelling two ids, both reported	0b17dc6b081d03b7
CASE	POSITIVE	(span) a backtick behind a backslash is literal and opens no span	6641de77adf1c118
CASE	POSITIVE	(span) a kept slug inside a command-line code span is a naming site	359658794fefbbf4
CASE	POSITIVE	(span) a paragraph ends at a `>` line	2347c0992b423e16
CASE	POSITIVE	(span) a paragraph ends at a blank line	98bfc5c07898ffa3
CASE	POSITIVE	(span) a paragraph ends at a list item: a backtick open in one item and closed in the next is literal	db10ee9c3e64d79e
CASE	POSITIVE	(span) a paragraph ends at a table row	612c5664878fcddd
CASE	POSITIVE	(span) a paragraph ends at an ATX heading	25570e7497c55005
CASE	POSITIVE	(span) an unmatched backtick string is literal, not a mask to end of line	77c24941b3dfad21
CASE	POSITIVE	(table) `===` right after a schema table is a one-cell body row (§4.3: no paragraph to underline; GFM §4.10 Example 202: a pipe-less line is a row): width miss, rc 2	3b5aa629a42f2ee8
CASE	POSITIVE	(table) a reference definition right after a schema table is a ROW of it (GFM Example 202: a pipe-less line after the rows is a row; §4.7: a definition cannot interrupt a block) -- one cell under a 4-cell header, width miss rc 2	3a93140eaafbd06e
CASE	POSITIVE	(table) the cells within the header's width of a non-schema row are scanned: `9z owns it` in the second of three cells under a two-cell header is a site	9e98dc6b913dc701
CASE	POSITIVE	(vocab) the ownership connective is read the same way: `own*ed by* **9z** and **Qx**` is the TWO-OWNERS? clause	387485df6459ff1c
CASE	POSITIVE	(§2.5) a character reference in PROSE renders its character, so an id SPELLED as one (`Slice &#57;z` renders `Slice 9z`) IS a naming site: the reference and the §2.4 escape are the two spellings of one substitution, at the one site the stream builds (design re-gate 4; until then §2.5 was decoded in a link DESTINATION only and this measured 0)	a4f35f1a2d00c92d
CASE	POSITIVE	(§6.2) a link WRAPPED in emphasis is still the memo link: emphasis-as-prose costs the population nothing (commonmark.js: `<em><a href="child.md">x</a></em>`)	08d4fd3a922ac7e8
CASE	POSITIVE	(§6.2) emphasis is LEXED (design re-gate 4): a declared id inside `*...*` IS a naming site, because the delimiters render no character at all -- the `**9z**` DECORATION a row id may carry is the exception `dispose` makes for it, not this pass's	51c67d1df2f3f0ae
CASE	POSITIVE	(§6.7) a HARD line break inside a link's text does not break the link: the inline pass reads the block's joined text, so a construct may span the lines of its paragraph (§6.8 soft breaks likewise)	2e7cc56fbc82bf4f
CASE	POSITIVE	(§6.7) a hard line break's backslash renders NOTHING and the break renders a line ending, which bounds every unit the scanners read, so `Slice W\\` + `z` names no `Wz` on the probe table where `Wz` is the umbrella.  ⚠ THIS CONTROL COULD NOT SEE R25-2: until then the backslash STOOD in the stream, and a standing backslash bounds the two sides exactly as the break does -- the wrong reading and the right one agree in the direction that BOUNDS and part only in the direction that JOINS, which is the R25 control below	081cee023b899711
CASE	POSITIVE	(§6.8) a SOFT line break is the same measurement: `Slice W` then `z` on the next line renders two words and names no `Wz`	9b5567db2a8a6102
CASE	POSITIVE	Deps cell of a terminal row names an umbrella	d678f31304e68d3f
CASE	POSITIVE	a KIND-UNDETERMINED row named as an owner is reported	1e235bfc959c89c1
CASE	POSITIVE	a backticked BARE id is the document spelling an id, not code	a033292a4a449926
CASE	POSITIVE	a backticked run of ids and separators is a Deps-shaped edge, not code	9a7ad521c114c862
CASE	POSITIVE	a multi-character id inside a bold PHRASE, not bold itself	7ee8def499aedbfc
CASE	POSITIVE	a visible link LABEL is prose and is scanned	28a44bb48cc5f1f8
CASE	POSITIVE	prose names an umbrella bare, with no row noun	a7be88fb1e881440
CASE	POSITIVE	prose orders an umbrella	6808e50a18067758
CASE	POSITIVE	trigger cell names another row's umbrella slot as a co-occasion	28f4851bba19b512
CASE	POSITIVE	used-by cell names an umbrella outright	cf39fb55673559fc
CASE	POSITIVE-NOVEL	(R25 sibling) `ax%2Bnotes.md` (`ax+notes.md`) IS a sibling and IS walked -- the discriminating half of the seven above: `+` is an ordinary name character on every platform and outside `_RESERVED_CHARS`, so a fix that refused ASCII punctuation wholesale would pass all seven and fail this one	f4a60e301c35bd78
CASE	POSITIVE-NOVEL	(R30 id) an id cell `&#8212;` is a deliberate blank: §2.5 renders the em dash, which the RAW reading could not see -- this cell was a schema miss before R30 and is a non-row after it, the one verdict the fix reverses in the other direction	0ddf7e4060921e94
CASE	POSITIVE-NOVEL	(R31-1 §2.4) a header cell spelling `#` as the ESCAPE `\\#` binds too: §2.4 renders a backslash escape as the character itself, and this is the spelling the reviewer did not name -- the fix reads the rendering rather than enumerating the two syntaxes that reach it	07efd4e37319f14d
CASE	POSITIVE-NOVEL	(R33-2) `KIND – UNDETERMINED` with an EN DASH declares the unsettled kind, so the row's nonempty `Deps` edge is asserted.  The reported shape: this one spelling was missing from the phrase's character class while the row grammar beside it accepted it, and the row left the run at rc 0	e384aeb3753c86ba
CASE	POSITIVE-NOVEL	(ascii) `次のSlice Cが所有する` reaches the naming worklist: the row-noun anchor is not `\\b` (no Unicode word boundary before `Slice`)	4e3bbe02736c32a6
CASE	POSITIVE-NOVEL	(ascii) `次は#11-zz-alphaが所有する` reaches the naming worklist: the slug anchor is an ASCII class, not `\\w`	cb0be7762ea52d18
CASE	POSITIVE-NOVEL	(autolink) `<foo.bar.baz>` has neither a scheme nor an `@` (§6.5 Example 610) and is no tag: the `<` is literal and the link after it is read	7c96ad87217db1de
CASE	POSITIVE-NOVEL	(autolink) `<m:abc…>` is a ONE-character scheme and no autolink (§6.5 Example 609, 2-32 characters): the `<` is literal and the link INSIDE the would-be span is read (commonmark.js: `&lt;m:abc<a href="child.md">x</a>&gt;`).  The link sits inside the span so that widening the scheme to one character masks it and the control goes red	946501dc24e4f958
CASE	POSITIVE-NOVEL	(autolink) a SPACE inside the absolute URI ends it (§6.5 Example 608), so `<https://foo.bar/ …>` is no autolink and no tag either -- the `<` is literal and the link INSIDE the would-be span is read (commonmark.js: `&lt;https://foo.bar/ <a href="child.md">x</a>&gt;`).  The space sits in the URI TAIL, which is the class the grammar excludes it from: a space right after `<` fails at the SCHEME instead, so that shape leaves the tail-widening mutant alive and proves nothing	07f2ac44d7381db2
CASE	POSITIVE-NOVEL	(autolink) an autolink inside a LINK's text: the autolink is masked, the link is still the memo link (commonmark.js nests the two `<a>`s)	b6c355d5551353b2
CASE	POSITIVE-NOVEL	(b) a sibling umbrella's Deps edge is asserted	01fe328b59258b1f
CASE	POSITIVE-NOVEL	(bare) `9z.é` bounds the id (a dotted number is ASCII on both sides)	3a38214f3ee44240
CASE	POSITIVE-NOVEL	(bare) `9z.次の工程` bounds the id: the far side of the `.` is not an ASCII id character, so the site is reported	247e4e2b32bd2bfe
CASE	POSITIVE-NOVEL	(bare) a hyphen bounds a short id: `after 9z-7z` names 9z	e33165f16801f620
CASE	POSITIVE-NOVEL	(bare) an id before `!` is bounded	e99f147b75396d7d
CASE	POSITIVE-NOVEL	(bare) an id before `?` is bounded	f3e61fd54ce8bf17
CASE	POSITIVE-NOVEL	(bare) an id inside ASCII double quotes is bounded	b8700cd04e98c1fb
CASE	POSITIVE-NOVEL	(bare) an id inside curly double quotes is bounded	11909b61f3b65182
CASE	POSITIVE-NOVEL	(block) `[foo]:\\nslice-9z-sib.md` IS a definition (destination on the next line): the sibling is walked	b3c2bca6c65ffc30
CASE	POSITIVE-NOVEL	(def) `[sib]: child&#46;md`: a reference definition's destination is decoded by the SAME normalisation -- the sibling is walked	eb76c6c8de5e8879
CASE	POSITIVE-NOVEL	(def) a definition is read from RAW lines at a block start: `[sib]: slice`x`.md` keeps its backticks in the destination and the sibling is scanned	f8c71968e69261f4
CASE	POSITIVE-NOVEL	(def) a label spanning FIVE lines is a definition (§4.7 / §6.3: a label may span lines); the later shortcut resolves and the sibling is scanned	4041504d6972e751
CASE	POSITIVE-NOVEL	(def) one line ending is allowed before the destination	0fa996188d7d982a
CASE	POSITIVE-NOVEL	(def) the FIRST definition of a label wins	a80391f2c3a8e740
CASE	POSITIVE-NOVEL	(id) a citation id declared twice differing only in CASE is the same miss -- `[C1]` and `[c1]` are one §6.3 label, which is how the link resolver already reads them: rc 2	3734b449b76d3057
CASE	POSITIVE-NOVEL	(image) `![a ![b](i.png) [c](child.md)][missing]`: the link stands OUTSIDE the inner image's description and the outer image fails -- it is a link, `child.md` is walked	a14f5e8e1012d673
CASE	POSITIVE-NOVEL	(image) `![alt [docs](child.md)][missing]`: the image does NOT resolve, so `![alt` is literal text and the link inside it IS a link (commonmark.js) -- `child.md` is walked and its violation reported	bcab31fc47dec614
CASE	POSITIVE-NOVEL	(image) `![alt][missing][Slice 9z]` with `[Slice 9z]` defined: the failed image's label is re-scanned and `[missing][Slice 9z]` is a link (§6.3 Example 571; commonmark.js) -- its tail is masked, the sibling is walked: 1 site, not 2	2ccba7101896b4d6
CASE	POSITIVE-NOVEL	(item) `- > [sib]: slice-9z-sib.md`: a definition inside a block quote inside an item registers -- the sibling is walked	9f6152dbb4d9267b
CASE	POSITIVE-NOVEL	(item) `- [a]: /u` then the lazy schema header over an indented delimiter row: the table is the item's (cmark-gfm, measured) -- the id declared	089c0f066258fa0c
CASE	POSITIVE-NOVEL	(item) `> - [sib]: slice-9z-sib.md`: a definition inside an item inside a block quote registers -- the sibling is walked and its violation reported	8dcfe429ec01f28f
CASE	POSITIVE-NOVEL	(item) a slot table inside a list item is a table (cmark-gfm, measured): its row declares its id	ee03a0e47a37f409
CASE	POSITIVE-NOVEL	(item) the reviewer's input `- item\\n\\n    [child](child.md)`: the indented line is the item's SECOND paragraph (§5.2, Example 108: the content indentation is 2), not indented code -- the link is found, `child.md` is walked and its violation reported	17ac27726f688770
CASE	POSITIVE-NOVEL	(lex-seed) a raw HTML line `9z-owner`: a hyphen bounds the short id on the raw line as in prose, so the line is seeded holding `9z`	8ffa1b8c5fe24c0c
CASE	POSITIVE-NOVEL	(lex-seed) a raw HTML line `owner-9z`: the hyphen bounds on the left too -- seeded holding `9z`	4ee53ec9ab0dc7ba
CASE	POSITIVE-NOVEL	(link) `<dest>` may contain an escaped `>`	30d79197bcf034f7
CASE	POSITIVE-NOVEL	(link) `[child](<child&#46;md>)`: the angle-bracket destination decodes by the same rule	61ee21cd7d595fb6
CASE	POSITIVE-NOVEL	(link) `[child](child&#46;md)`: a decimal character reference in the destination is decoded (§2.5 / §6.3) -- `child.md` is walked and its violation reported	a432337119c57fa0
CASE	POSITIVE-NOVEL	(link) `[child](child&#x2E;md)`: a hexadecimal character reference decodes	793e6406e6f283c5
CASE	POSITIVE-NOVEL	(link) `[child](child&period;md)`: a named entity reference (HTML5 `&period;`) decodes	c4402ff8c60a01e3
CASE	POSITIVE-NOVEL	(link) `[x](.md)` links the sibling file named `.md`: `sibling_path` stage (d) and the lexer's file token read the ONE `FILE_SUFFIX`, so the suffix-only name is a file on both sides	3caf893ecd773d6f
CASE	POSITIVE-NOVEL	(link) `[x](child&#0;.md)`: U+0000 is replaced by U+FFFD (§2.5, "for security reasons") -- the memo named `child\\ufffd.md` is walked (a raw U+0000 would be rejected as a C0 control)	0020cf7dfaf02c75
CASE	POSITIVE-NOVEL	(link) `[x](child&#x110000;.md)`: an invalid code point is U+FFFD (§2.5) -- `child\\ufffd.md` is walked	cd7e6583b1e1adc3
CASE	POSITIVE-NOVEL	(link) `[x](child&copy.md)`: HTML's legacy semicolon-less `&copy` is NOT a reference in CommonMark (§2.5 Example 29) -- the destination is the literal `child&copy.md`, and THAT file is walked (`html.unescape` would have named `child©.md`)	6185d2754a2de941
CASE	POSITIVE-NOVEL	(link) `[x](slice&#37;20sib.md)`: §2.5 decodes `&#37;` to `%`, then `sibling_path` percent-decodes `%20` (stage b) -- the two decoders run in spec order, `slice sib.md` is walked	939de044befc3505
CASE	POSITIVE-NOVEL	(link) `sub%5Cchild.md`: a backslash is a path separator on every platform (WHATWG URL `#path-state` step 1: for a special scheme -- `file` is one -- `\\` ends a segment as `/` does; `PureWindowsPath` is that syntax) -- the file `sub/child.md` is walked	7c9f40b8b1250771
CASE	POSITIVE-NOVEL	(link) a `"…"` title may contain an escaped `"`	1de29db76e66042f
CASE	POSITIVE-NOVEL	(link) a label holding only a non-breaking space is a label (§6.3: at least one character that is not a space, tab, or line ending)	ffb850250f22a175
CASE	POSITIVE-NOVEL	(link) a link wrapping a REFERENCE image `[![alt][img]](child.md)`: the image does not deactivate the outer opener, so `child.md` is scanned	5d77ee97ace552d7
CASE	POSITIVE-NOVEL	(link) a link wrapping an IMAGE `[![alt](img.png)](sib.md)` links the sibling; `img.png` is never a memo	c9618d4543efff45
CASE	POSITIVE-NOVEL	(link) a non-ASCII space is destination content (only ASCII space and controls end it)	d586fe4523892f97
CASE	POSITIVE-NOVEL	(link) a percent-encoded destination `slice%20sib.md` links the file `slice sib.md`, as `<slice sib.md>` does	679cf9048486c7a8
CASE	POSITIVE-NOVEL	(link) a reference link nested in inline brackets: the inner reference is the link, the outer tail is text	b59d902f3800360b
CASE	POSITIVE-NOVEL	(link) bare destination with a balanced parenthesis pair	da06b5fffec0263c
CASE	POSITIVE-NOVEL	(link) bare destination with an escaped parenthesis	02796fc683306650
CASE	POSITIVE-NOVEL	(link) collapsed reference `[label][]`	6459f7bcd213eadb
CASE	POSITIVE-NOVEL	(link) full reference whose text holds nested brackets; the label is the link's tail, not prose, and so is the definition	0638e2a7bea0ff8b
CASE	POSITIVE-NOVEL	(link) label matching collapses internal whitespace	1b3994b9863cd3de
CASE	POSITIVE-NOVEL	(link) label matching is a Unicode case FOLD, not lower()	477c9e02ecf3e4c3
CASE	POSITIVE-NOVEL	(link) nested inline links: the INNER link is the link, the outer tail is text -- `child.md` joins the population, absent `parent.md` is not linked	c243ee4764022ba7
CASE	POSITIVE-NOVEL	(noun) `ROW 9 lands first` names the row (a bare `9` is the declared numeric miss)	259675e05ee02d12
CASE	POSITIVE-NOVEL	(noun) `SLICE C owns it` names the row: the row noun folds case (a bare `C` is the declared single-letter miss, so only the anchor can reach it)	3cb3f25f78503d86
CASE	POSITIVE-NOVEL	(noun) `UMBRELLA C owns it` names the row	470925c03ab25e67
CASE	POSITIVE-NOVEL	(population) a terminal id declared in a linked memo is in the keep-set, so `Tq / 9z` is an id-only run, not code	89f6e9c9cffc6169
CASE	POSITIVE-NOVEL	(population) a violation in a sibling linked ONLY from a cell is reported	0856759e274add51
CASE	POSITIVE-NOVEL	(population) an umbrella declared in a linked memo is in the census	88e74c50a65b98c7
CASE	POSITIVE-NOVEL	(population) the population is transitive: a memo linked from a linked memo is scanned	2ee94f44675dd8c2
CASE	POSITIVE-NOVEL	(quote) `> [sib]: slice-9z-sib.md`: a definition inside a block quote registers (§5.1 container, Example 218) -- the sibling is walked and its violation reported	3cc75418bf3d45a5
CASE	POSITIVE-NOVEL	(quote) a slot table inside a block quote is a table: its row declares its id	9193ac0fa732f673
CASE	POSITIVE-NOVEL	(quote) a slot table inside a block quote whose HEADER row is lazy is a table (cmark-gfm: the header is read out of the quote paragraph's last line): its row declares its id	a842f2f07de1f746
CASE	POSITIVE-NOVEL	(quote) the R17 reviewer's shape: `> [a]: /u` then an UNQUOTED schema header over a QUOTED delimiter row is a table in the quote (cmark-gfm: a definition is paragraph text until the paragraph ends, and the header is read out of its last line) -- the row declares its id	cee4780dea767b88
CASE	POSITIVE-NOVEL	(quote) two definitions then the lazy schema header: still the table's header, the id declared	6bce743ef62091a2
CASE	POSITIVE-NOVEL	(setext) `Heading\\n===` is a heading; the `===` underline ends the paragraph, so a code span opened in the heading does not reach the next paragraph's site	7c5482da1beb75c5
CASE	POSITIVE-NOVEL	(span) a backtick inside a link DESTINATION is consumed by the link, not a code span: `[sib](slice`x`.md)` links the sibling	4c1fbcc46b72076a
CASE	POSITIVE-NOVEL	a violation in a carved sibling the memo links	0bbbb43bdf829f7e
CASE	POSITIVE-NOVEL	novel acceptance idiom: 'signs off'	935e883174370df7
CASE	POSITIVE-NOVEL	novel ordering idiom: 'downstream of'	6d5957ddc535afab
CASE	POSITIVE-NOVEL	novel role noun: an umbrella as 'the integrator'	ff9658e35e82b59d
CASE	POSITIVE-NOVEL	novel spelling in a cell: an em-dash hand-off	1f67ae892e8416f8
CASE	POSITIVE-NOVEL	novel verb: an umbrella is 'chartered to deliver'	7bdcf90375d49d51
CASE	POSITIVE-NOVEL	prose names a slot umbrella bare	3c57572af08abef5
CASE	POSITIVE-NOVEL	prose names a slot umbrella in bold	0765d90e63ca10e9
CASE	POSITIVE-NOVEL	the reference definition has an angle-bracket destination	ef5d0114d0208e39
CASE	POSITIVE-NOVEL	the sibling is linked by a shortcut reference	72e9c484808879c5
CASE	POSITIVE-NOVEL	the sibling is linked by reference	5c1bd9cbf84fc5fe
CASE	POSITIVE-NOVEL	the sibling link carries a section fragment	752c477f094c9e24
CASE	POSITIVE-NOVEL	the sibling link carries a title	03a99b3b599ca647
CASE	POSITIVE-NOVEL	the sibling link uses an angle-bracket destination	f0b63cf86cbe7efa
CASE	POSITIVE-NOVEL	umbrella id is the LAST token of a link label	96b19d778091463a
CONTROL	CONTROL	CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html	plan_memo_selftest_controls.spec_examples_control#f2440adaff1d3e91
CONTROL	CONTROL	CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html	plan_memo_selftest_controls.inline_examples_control#15579fef651e6ef5
CONTROL	CONTROL	CommonMark 0.31.2 §6.1: a code span READS as the text the spec's own html puts inside `<code>` (line endings converted, then the one-space trim)	plan_memo_selftest_controls.code_span_reading_control#8b249e6deb96b49b
CONTROL	CONTROL	CommonMark 0.31.2 §6.4: Phase 2's inline claim agrees with the spec's own html for every §3.0b family DEMOTED into a resolved image description (the cross-product the corpus cannot reach)	plan_memo_selftest_controls.demoted_agreement_control#94f421028b9de63d
CONTROL	CONTROL	PROPERTY: Population._kind reads every kind phrase from plan_memo_stream.KIND_PHRASES, the tuple the residue gate iterates (a fourth phrase cannot decide a kind without being gated)	plan_memo_selftest_properties.kind_phrase_gate_control#09889c889601af3a
CONTROL	CONTROL	PROPERTY: _straddles answers its own definition (a character inside a blank and a character outside every blank), over every blank layout of eight positions and every extent inside it	plan_memo_selftest_invariants.straddle_definition_control#47d49ffa53d88ee3
CONTROL	CONTROL	PROPERTY: a run the sibling resolver FOLLOWS is one whole file span to the lexer -- never a prefix with the remainder left for the naming scan (the direction the correspondence forbids)	plan_memo_selftest_invariants.file_token_run_agreement_control#6b706acece33d2f9
CONTROL	CONTROL	PROPERTY: every criterion the two ratchets state names an existing killing row (the table is data in plan_memo_selftest_mutants_ratchets.CRITERIA)	plan_memo_selftest_ratchets.criteria_rows_control#7280ce14de42741c
CONTROL	CONTROL	PROPERTY: every import-graph seam this suite states in prose is true of the imports (an "only importer of X" is a claim about the COMPLEMENT)	plan_memo_selftest_records.import_seam_control#d5b8cf7893caa4b7
CONTROL	CONTROL	PROPERTY: every line the run REPORTS goes through the escape -- measured over the EMIT SITES, the subject the escape function's own control cannot reach	plan_memo_selftest_records.report_channel_control#72a36e2ca7f39d6c
CONTROL	CONTROL	PROPERTY: every loop in the census module that walks a POPULATION is pinned by a mutant that truncates THAT loop (the ratchet is derived from the code, not a list somebody extends when a reviewer names one)	plan_memo_selftest_ratchets.population_scope_control#863fbb8244f048d5
CONTROL	CONTROL	PROPERTY: every module of this checker is NAMED in the entry point's MODULES map (the map is checked, not asked to be kept)	plan_memo_selftest_records.module_map_completeness_control#bc107cc0248400c0
CONTROL	CONTROL	PROPERTY: every name the entry point's MODULES map spells is a file that exists (the rename half the completeness direction cannot see)	plan_memo_selftest_records.module_map_existence_control#55127904783664b0
CONTROL	CONTROL	PROPERTY: every name the sibling resolver accepts, standing alone in prose, is ONE file token to the lexer (the correspondence FILE_SUFFIX's comment asserts)	plan_memo_selftest_invariants.file_token_resolver_agreement_control#f502d4335930506e
CONTROL	CONTROL	PROPERTY: every row-id composer admits every row kind of plan_memo_ids.ROW_KINDS (the kind half of the spelling sweep)	plan_memo_selftest_invariants.row_kind_coverage_control#92129dee410fa3fb
CONTROL	CONTROL	PROPERTY: every row-keyed schema's NAME is a row noun and no other schema's is (the nouns are derived from SCHEMAS, so the next schema's is covered by default)	plan_memo_selftest_invariants.row_noun_schema_control#6f17332ff6208502
CONTROL	CONTROL	PROPERTY: every source of this checker compiles with SyntaxWarning as an error (an invalid escape in a docstring is a future SyntaxError, and this PR shipped one)	plan_memo_selftest_properties.syntax_warning_control#9d319e1cb1669234
CONTROL	CONTROL	PROPERTY: every source of this checker is under the 1000-line touch-time bound (the invariant this PR broke, as a mechanism instead of a sentence)	plan_memo_selftest_properties.line_bound_control#29bc720489f02451
CONTROL	CONTROL	PROPERTY: every written `module.symbol` attribution names the module that DEFINES that symbol (the class five touch-time splits left with no detector)	plan_memo_selftest_records.symbol_attribution_control#3d55808640d8ee7d
CONTROL	CONTROL	PROPERTY: no ANCHORED pattern in the module set is handed a subject truncated by a number (a width window is a second statement of what the anchor already says)	plan_memo_selftest_properties.anchored_matcher_width_control#334980b99d2805f8
CONTROL	CONTROL	PROPERTY: no C0 control character or DEL from a MEMO reaches stdout through the checker's own report (the property the emit-site sweep is a proxy for, asked by running the program over a hostile memo)	plan_memo_selftest_invariants.report_bytes_control#64e61538a6288065
CONTROL	CONTROL	PROPERTY: no SCANNING pattern-method call in the module set applies a pattern that BEGINS with an unbounded repeat (a run the engine re-enters at every position inside it -- a cost no witness in this suite can count)	plan_memo_selftest_properties.leading_run_scan_control#abbe6fb263d1f60c
CONTROL	CONTROL	PROPERTY: no module can leave a run unnoticed -- every file related to the population is in it, and every module in it is reached from a root	plan_memo_selftest_population.registry_membership_control#035cacde524ed3b6
CONTROL	CONTROL	PROPERTY: no source of this checker performs text I/O without naming its encoding (the checker set and the self-test both, globbed)	plan_memo_selftest_properties.encoding_sweep_control#72fe489a1156727c
CONTROL	CONTROL	PROPERTY: no source of this checker removes an element from the FRONT of a list (the O(1) half of the population walk's drain, which no work witness here can measure)	plan_memo_selftest_properties.front_drain_sweep_control#fc133fb7010989b5
CONTROL	CONTROL	PROPERTY: plan_memo_ids.tokens reads exactly the language plan_memo_ids.decorated_id spells, over an EXHAUSTIVE corpus of decoration and id characters (the core-first scan against the grammar's own composition)	plan_memo_selftest_invariants.id_scan_grammar_agreement_control#d7e96de2bb373a68
CONTROL	CONTROL	PROPERTY: the census is the same under each of the three line endings CommonMark §2.1 recognises (LF, CRLF, a bare CR), written as bytes	plan_memo_selftest_invariants.line_ending_control#dbcf18f409ab17b3
CONTROL	CONTROL	PROPERTY: the entry point sets BOTH output streams to UTF-8 -- the absence a call-site sweep cannot report	plan_memo_selftest_invariants.stream_encoding_control#c12540544cc1b739
CONTROL	CONTROL	PROPERTY: the entry point's CLI contract is per MODE -- a closed option set whose complement is refused, and for each mode the flags it accepts and the positional count it takes (a known flag in the wrong mode returned 0 for the wrong operation)	plan_memo_selftest_records.option_set_control#861802aa9cdfd7ec
CONTROL	CONTROL	PROPERTY: the harness DERIVES the population from a directory (glob, self-test partition, registry-by-content, import order, cycle) -- asked of a planted directory	plan_memo_selftest_population.population_partner_control#a6fdf5d39beb3c7f
CONTROL	CONTROL	PROPERTY: the id character classes are spelled once, in plan_memo_ids.py (a source-text sweep)	plan_memo_selftest_properties.id_spelling_sweep_control#f5e945fd44e0cd0f
CONTROL	CONTROL	PROPERTY: the kind-phrase questions are asked at their sanctioned sites only -- "which kind does this field DECLARE" and "does this cell CLAIM one, under either reading" are two questions with one site each, and a third caller asking either one directly is the shape three rounds of findings had	plan_memo_selftest_ratchets.kind_question_site_control#6e95def10c00bec8
CONTROL	CONTROL	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere	plan_memo_selftest_ratchets.kind_question_partner_control#6e510ccee8a24481
CONTROL	CONTROL	PROPERTY: the licensing rule's backward index decides what the whole preceding text decides, at every position of a generated corpus -- and every phrase opens with a literal the index holds	plan_memo_selftest_invariants.licence_index_control#3cc7fdfa6c415f29
CONTROL	CONTROL	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)	plan_memo_selftest_population.manifest_control#eb01db11dad14c81
CONTROL	CONTROL	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other	plan_memo_selftest_ratchets.population_scope_partner_control#2056ab765c6fed79
CONTROL	CONTROL	PROPERTY: the self-test's IMPORT DIRECTION rule holds of the imports (no cycle, the harness imports nothing of the set, no checker module imports the self-test but the entry point's dispatch) -- a rule over the AST, not an inventory of edges	plan_memo_selftest_records.import_direction_control#92a672c170ccfd8d
CONTROL	CONTROL	PROPERTY: the separator dash set is spelled once, in plan_memo_ids.py (the class three readers spelled three ways, disagreeing)	plan_memo_selftest_properties.dash_spelling_sweep_control#461f100a503a762a
CONTROL	CONTROL	PROPERTY: the verdict is invariant under a §2.5 re-spelling of any prose character the document renders the same (the rendered-text rule, swept position by position)	plan_memo_selftest_invariants.render_equivalence_control#1c48eb29036ac81c
CONTROL	CONTROL	PROPERTY: the verdict is invariant under re-spelling any ONE line break as each of CommonMark's three (the §6.8 soft break, §6.7's two-space and backslash hard breaks) -- the render-equivalence family's second guard, for the class its first one excludes by construction	plan_memo_selftest_invariants.break_equivalence_control#7dc0d6ca166f64ca
CONTROL	CONTROL	PROPERTY: the §4.6 start-condition-6 tag list in the code is the list CommonMark 0.31.2 spells, both directions, against the vendored extraction (source, version and sha256 recorded)	plan_memo_selftest_properties.html_block_tag_names_control#beaad93440bec0f4
CONTROL	CONTROL	Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js	plan_memo_selftest_controls.sequence_control#d60883f484c65e18
CONTROL	CONTROL	Phase-1 orphan detection is linear: <= 4 link_label calls per line	plan_memo_selftest_pipeline.linear_orphans_control#ac19780ceaf6fdb4
CONTROL	CONTROL	a RuntimeError raised while PARSING a memo is a crash out of check(), never the unavailable-memo miss	plan_memo_selftest_controls.parse_runtime_error_control#5b524dae9a6b12e8
CONTROL	CONTROL	a decoded destination with a C0 control character is rejected, never resolved	plan_memo_selftest_controls.control_char_destination_control#c3a26128b41497b0
CONTROL	CONTROL	a destination the sibling resolver REFUSES leaves no finding, seed or note naming it -- the stated policy, pinned rather than merely current	plan_memo_selftest_controls.refused_destination_silence_control#8fe7cad78bce0f44
CONTROL	CONTROL	a lazy schema header after a definition in a linked memo's quote is a table: id declared, kind umbrella, census +1	plan_memo_selftest_controls.lazy_header_after_definition_control#bf3299f7bc551025
CONTROL	CONTROL	a linked `.md` whose target is not a regular file (a device, a FIFO, a socket) is the unavailable-memo miss at rc 2, refused before it is read	plan_memo_selftest_controls.non_regular_memo_control#6ef13acc463ae85b
CONTROL	CONTROL	a marker naming another row does not enter the count	plan_memo_selftest_controls.attribution_control#0d80adcebf8683d3
CONTROL	CONTROL	a memo path with a lone surrogate (a non-UTF-8 POSIX filename byte) is reported escaped at rc 2, never a UnicodeEncodeError from the strict UTF-8 channel	plan_memo_selftest_controls.surrogate_report_control#b546a61fc3e98a88
CONTROL	CONTROL	a resolved image's demotion is linear: N nested images demote their descendants once, not once per enclosing image	plan_memo_selftest_work.linear_image_demotion_control#d1bb3ab53ef6324c
CONTROL	CONTROL	a row whose id cell declares no id is named by its declaring locator (`row <no id> at :LINE (token)`), never `row None`	plan_memo_selftest_controls.empty_id_row_name_control#f6f39e9e4182fd1b
CONTROL	CONTROL	a site after an escaped pipe is reported at its raw column	plan_memo_selftest_controls.raw_offset_control#b1a7a806c957ef28
CONTROL	CONTROL	a site read across a construct that renders nothing is reported at its raw column (the stream map)	plan_memo_selftest_controls.split_locator_control#8fc480b6f675fe2c
CONTROL	CONTROL	a table with and without edge pipes reads the same	plan_memo_selftest_controls.pipe_shape_control#1ab4886ce0b2a79f
CONTROL	CONTROL	a §6.1 code span's closer is found by an index of the runs BY LENGTH, not by walking them (0.27 x L^1.5 before)	plan_memo_selftest_work.linear_code_closer_control#cdf777473e91499e
CONTROL	CONTROL	a §6.6 span crossing a line ending seeds each of its lines at ITS line, not all of them at the opener's	plan_memo_selftest_controls.multiline_span_locator_control#925c97381d36ad9f
CONTROL	CONTROL	an OSError from resolve() is the unavailable-sibling schema miss, never an exception	plan_memo_selftest_controls.unavailable_sibling_control#0ccf63fef537e0df
CONTROL	CONTROL	an empty control or mutant registry is a FAIL, never green	plan_memo_selftest_controls.empty_registry_control#1b5b989fabff530b
CONTROL	CONTROL	an orphan definition exempts its OWN bracket only: `[sib]: child.md "[sib]"` is the documented miss, rc 2, child.md not walked	plan_memo_selftest_controls.orphan_offset_control#c6a8a4beb4015575
CONTROL	CONTROL	an undecodable sibling is the unavailable-linked-memo schema miss, never an exception	plan_memo_selftest_controls.undecodable_sibling_control#571f728dd29b14b6
CONTROL	CONTROL	block quotes are linear: N quotes cost <= 4N quote_content calls	plan_memo_selftest_pipeline.scaling_quotes_control#9eeefbc15ab31427
CONTROL	CONTROL	container nesting is off the call stack: 1,000 nested quotes / items parse as commonmark.js nests them	plan_memo_selftest_controls.deep_nesting_control#326431ebbe9d7e0a
CONTROL	CONTROL	declaring-field parse and whole-line marker grep differ	plan_memo_selftest_controls.degenerate_control#c8f1c52ccab77659
CONTROL	CONTROL	diagnostics name a memo relative to the root memo's directory: `a/child.md` and `b/child.md` are two files, and a memo outside that directory is named by its absolute path	plan_memo_selftest_controls.display_path_control#a5a305bee8c94bee
CONTROL	CONTROL	emphasis matching is linear: N unmatched delimiter runs cost O(N) work (the Appendix's openers_bottom)	plan_memo_selftest_work.linear_emphasis_control#91ad7aa5c99751ad
CONTROL	CONTROL	file_and_cite_spans is linear: N parenthesis groups are one pass, not a re-scan from every start position	plan_memo_selftest_work.linear_file_token_control#bf9bb21bb782b4ab
CONTROL	CONTROL	inline_pass does not RUN the §6.6 grammar at a `<` whose closer stands nowhere ahead (the one cost the Python-level witnesses cannot see)	plan_memo_selftest_work.linear_html_attempts_control#84c45c55ebadc1c6
CONTROL	CONTROL	inline_pass is linear over a malformed inline-link tail: `[`xN + `](`xN is O(N), bounded by §6.3's permitted nesting limit	plan_memo_selftest_work.linear_inline_tail_control#9ef8de9269843eb9
CONTROL	CONTROL	linked_files is linear: <= N Path.__eq__ calls over N distinct siblings (a set dedup hashes, a list compares)	plan_memo_selftest_pipeline.scaling_linked_files_control#a4f5f9e2d3961826
CONTROL	CONTROL	links() is linear: 30 nested brackets are one inline_pass call	plan_memo_selftest_work.linear_links_control#1e546983e5c6029e
CONTROL	CONTROL	split_row is linear: <= 64 source lines per character and per cell (breaks partitioned in the one scan)	plan_memo_selftest_work.scaling_split_row_control#bf6192b398a976b1
CONTROL	CONTROL	the always-run raw-content seed is linear: N id tokens and N file names on one raw line are one pass, not a span sum per token	plan_memo_selftest_pipeline.linear_raw_seed_control#b639a7754aa5cfbb
CONTROL	CONTROL	the closer index carries a cursor: 600 openers with no closer start at most one search per literal, not one per opener	plan_memo_selftest_work.linear_closer_index_control#27879689cc721219
CONTROL	CONTROL	the entries named `PROPERTY: ...` are exactly the property family's fragments (`_FAMILY`), both directions	plan_memo_selftest_controls.property_family_control#3bd14582d227e1dd
CONTROL	CONTROL	the id scan walks a decoration run ONCE per id and never at all where no id follows it (an exact count, not a ratio -- the half of R29-2 a Python witness can see)	plan_memo_selftest_work.linear_id_scan_control#21772033a2220ca8
CONTROL	CONTROL	the licensing rule's backward look is linear: N mentions hand LICENSE_BEFORE O(N) characters in all, not one growing prefix each	plan_memo_selftest_pipeline.linear_licence_scan_control#1768f16e91bc2a1f
CONTROL	CONTROL	the population walk queues each memo at most once, over a corpus GENERATED from the definition of a memo family: every digraph on three memos, each also with one memo absent	plan_memo_selftest_growth.population_walk_once_control#66db61621773a003
CONTROL	CONTROL	the runner's report channel escapes every C0 control character and DEL, so a run line a control names with one is still greppable	plan_memo_selftest_controls.printable_output_control#d5ababbd95258a94
CONTROL	CONTROL	the scans are linear over a corpus GENERATED from the grammar: every branch character, delimiter, HTML opener, bracket construct and id kind, each repeated and each PAIR of them interleaved, and no source line grows worse than its input	plan_memo_selftest_growth.generated_growth_control#baf961042891c18e
CONTROL	CONTROL	the §5.1 marker test builds no content: quote_content is called once per line a quote's gather examines and never to answer whether a line carries a marker	plan_memo_selftest_pipeline.quote_build_once_control#4be90d4bbba1ef42
CONTROL	CONTROL	the §6.4 demotion of code spans and autolinks is LINEAR in the nesting depth (a retag loop per image close re-walks every descendant)	plan_memo_selftest_work.image_demotion_linear_control#9281ee8fab15c1b5
CONTROL	CONTROL	unresolved_references is linear: <= N*(log2 N + 2) reads of the line-offset table (a bisect per site, not a scan)	plan_memo_selftest_pipeline.scaling_unresolved_control#459d21b77f697ccd
CONTROL	KNOWN-MISS	purely numeric id with no row noun	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	KNOWN-MISS	undecorated single letter with no row noun	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R23 kind) a field that spells the phrase CLEANLY and straddles a blank elsewhere is no miss: both readings say undetermined, so the census is not in doubt -- the DISAGREEMENT is the other conjunct	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R23 kind) a kind phrase QUOTED WHOLE is no miss: the two readings differ about it, and that difference is I-A's disposition (a quoted phrase declares nothing), not a could-not-scan -- the STRADDLE is the conjunct that tells them apart	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R23 memo) exactly ONE U+FEFF is dropped: a second is an ordinary character of the document and the table is lost to it again -- a strip that is a `lstrip` rewrites the document instead of reading its encoding	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R23 render) `Slice Q&ast;x owns it` names NO row `Qx`: the reference renders a character, so the span is a BLANK and bounds the two sides -- dropping it (the disposition for a construct that renders nothing) would join them into an id the document does not spell	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R23 render) `The &ast; marks it` names NO row `ast`: the reference renders `*`, so the letters `a` `s` `t` are nowhere in the document a reader reads -- leaving the entity's SOURCE spelling in the stream fabricated the site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R23 seed) the same phrase spelled CLEANLY in prose seeds nothing -- the discriminating half: the residue is the disagreement, never the phrase	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R24 render) `9z-notes.md owns it` names nobody: no whitespace renders anywhere in the run, so both readings agree that the file name swallows the id -- the file-name disposition is not weakened by moving it onto the rendered text	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R24 render) ``**`x` 9z**7z owns it`` names nobody: a READER sees the document bolding `x 9z`, which is prose and no decorated id, so the delimiters drop and the sides join.  This is the control over WHICH rendering the exception is asked of -- the checker's own disposed stream would show `9z` beside blanks, whitespace-separate into an id-only run, and keep a decoration the document does not have	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R24 width) a field that merely MENTIONS a sibling and then declares itself attributes nothing: the discrimination is the DASH between the id and the marker, never the distance -- so removing the window does not widen the attribution	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R24 width) a row noun standing between the licensing phrase and the id does not hide the phrase (`the child of Slice 9z`), and NOT because the rule has a clause for one: the ANCHORED reading of that site starts AT the noun and wins the dedup, so the text before the mention is `the child of` either way.  This is the control the deleted `_TRAILING_NOUN` substitution was believed to be needed for	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R24 width) the same sentence with the phrase actually AT a word boundary is licensed -- the discriminating half: only the character in front of `derivation` differs, and it is the one the lookbehind exists to read	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R25 sibling) `ax%22notes.md` is no sibling: the decoded `ax"notes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R25 sibling) `ax%2Anotes.md` is no sibling: the decoded `ax*notes.md` holds a character Microsoft's "Naming Files, Paths, and Namespaces" lists among those a file name may not use.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R25 sibling) `ax%3Anotes.md` is no sibling: the decoded `ax:notes.md` opens an NTFS ALTERNATE DATA STREAM there -- `notes:child.md` is the stream `child.md` of the file `notes` -- so where the stream exists reading it SUCCEEDS and yields another file's content, the very class the DOS devices were rejected for.  This is the member R25 reported, and the one that falsified the `OSError` argument the other six rested on.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R25 sibling) `ax%3Cnotes.md` is no sibling: the decoded `ax<notes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R25 sibling) `ax%3Enotes.md` is no sibling: the decoded `ax>notes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R25 sibling) `ax%3Fnotes.md` is no sibling: the decoded `ax?notes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R25 sibling) `ax%7Cnotes.md` is no sibling: the decoded `ax\pnotes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R25 sibling) the character rule is read per PART, as the device rule is: `ax%3Ab%2Fchild.md` (`ax:b/child.md`) puts the colon in a NON-FINAL component -- with a final-component-only reading it passes while `ax:b.md` is refused.  ⚠ The colon must sit in the DIRECTORY: `sub/ax:b.md` puts it in the last part, where that reading refuses it too and R22's per-part mutant SURVIVES (measured)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R25 §2.4) `Slice\\\\` + a line ending is a LITERAL backslash (the first escapes the second) and THEN a soft break, so a rendered character stands between the noun and the id and no row is named -- the discriminating half: a fix that widened the §2.4 escape to swallow any backslash before a line ending, or that dropped the backslash without asking whether the escape above had already consumed it, would pass the R25 control and fail this one	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R26 file) `(m.md(9z)md).md`: the run is balanced ACROSS its groups, so it is one name and not two tokens with the id exposed between them -- the second shape the flat arm could not read, and the one that leaks an id rather than a parenthesis	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R26 file) `a.md+9z+b.md` is ONE name ending at the LAST suffix, not the first: leftmost-LONGEST, as a pattern would have read it.  Stopping at the first `.md` would leave `9z` standing outside the token.  ⚠ The first fixture written for this clause was `a.md.9z.md`, and it was NOT discriminating -- an id directly before `.md` is a DOTTED NUMBER to the id grammar and no site either way, so both readings reported 0 and the mutant survived.  The id has to be separated from the second suffix	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R26 file) `foo((9z)).md` is ONE file name: nested balanced parentheses are §6.3's rule at any depth, so the id inside the name is no naming site.  Until R26 the token arm matched a FLAT chunk only, could reach no further left than the bare `.md`, and reported `9z`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R26 file) `foo(9z).md`, the FLAT partner, green before the fix: a single balanced pair always read as one name, which is what says the two controls above are about NESTING and not about parentheses as such	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R26 §6.3) a destination nested 33 deep is NOT a link, so `[x](...)` is literal text and no memo is looked up: rc 0.  ⚠ This is a DIVERGENCE from commonmark.js, which reads it as a link, and an agreement with cmark; the spec permits both and the vendored corpus reaches depth 2, so nothing but this control says where the boundary is	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R27 noun) `Unlike Slot `#11-zz-alpha`, **UMBRELLA, …**` does NOT attribute -- the new noun inherits the DASH discrimination rather than widening what a mention is. The discriminating partner: a fix that anchored on the noun alone would pass the row above and fail this one	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R30 id) an id cell `**—**` is STILL a deliberate blank: here the `**` pair closes (§6.2), and paired decoration around a blank is what the strip was right about.  The partner that bounds the fix's reach: refusing decoration outright would pass all six above and fail this one	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R30 id) an id cell `` `—` `` is a deliberate blank: the code span renders its content and a reader sees the em dash -- the same partner for the other `DECOR_MARK`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R31-1 §2.5) a header cell rendering `$` (`&#36;`) is NOT the `slice` schema: the reading moved and the COMPARISON did not loosen, so the table stays non-schema and declares nothing.  The partner that bounds the fix -- a match that normalised the header instead of rendering it would pass the three above and fail this one -- and the witness is the R42-10 gate naming the unbound claim, which a bound table would not produce	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R33-1) `Subslice 9z — **UMBRELLA, …**` attributes NOTHING: `Subslice` is one word and the row noun inside it is not a row noun, so the field is the containing row's OWN declaration.  The reported shape -- read without a left boundary the search started mid-word, the row became a pointer and the run FABRICATED an UMBRELLA-MARK	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R33-1) `xSlice 9z — …` likewise: the boundary is a fact of the GRAMMAR (an ASCII alphanumeric may not stand before the noun), not a list of words that happen to end in one -- so a prefix nobody thought of is covered by the same clause	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R33-2) `KIND / UNDETERMINED` declares NOTHING: the set admits the three dashes these documents use and did not become 'any punctuation' -- the partner that bounds the fix	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R34-1) `9z+notes.md owns it` reports NOTHING -- the discriminating half: the same prose with the run ENDING at the suffix is a file name, which is what makes the case above a claim about the run's end and not about the `+`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R34-1) a FRAGMENT tail still ends a name: `See slice-9z-sib.md#acceptance` masks the whole name.  This is the direction the correspondence forbids outright -- the resolver follows that run, stripping the fragment, so a reader that broke it into pieces and reported the id would be the one failure mode the property names	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R34-1) a TRAILING-PUNCTUATION tail still ends a name: `See slice-9z-sib.md.` masks the name and reports nothing.  Here the two readers differ LEGITIMATELY -- the resolver rejects the run with the period, because it is handed a destination the link grammar already bounded, while this reader must find the boundary itself and a sentence-final period is prose	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R34-2) an umbrella row whose `Deps` cell is a hyphen carries NO edge: the reading moved and `is_empty` still decides by SHAPE, so a deliberate blank is still blank.  The partner that bounds the fix -- reading the cell as 'anything the lexer did not blank' would pass the four above and fail these three	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R34-2) an umbrella row whose `Deps` cell is an em dash carries NO edge: the reading moved and `is_empty` still decides by SHAPE, so a deliberate blank is still blank.  The partner that bounds the fix -- reading the cell as 'anything the lexer did not blank' would pass the four above and fail these three	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R34-2) an umbrella row whose `Deps` cell is an en dash carries NO edge: the reading moved and `is_empty` still decides by SHAPE, so a deliberate blank is still blank.  The partner that bounds the fix -- reading the cell as 'anything the lexer did not blank' would pass the four above and fail these three	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R35) `See notes.md#9z owns it` reports NOTHING: the resolver follows that whole run, stripping the fragment, so the lexer must span it whole.  The reported shape -- R34-1 stopped the span at the suffix and the id in the fragment became a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R35) the QUERY spelling too: `See notes.md?q=9z owns it` -- the resolver strips a query exactly as it strips a fragment, so one rule covers both and neither is a special case	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R38 seed) a row whose `Deps` cell holds only a bare `.md` name carries an edge, so ordering prose naming no other party reports NOTHING.  Read off the prose-scanning stream the cell was EMPTY and the seed fired -- printing a no-edge finding that quoted the very cell contents it had just called empty	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R38 seed) a row whose `Deps` cell holds only a citation `[C1]` carries an edge, so ordering prose naming no other party reports NOTHING.  Read off the prose-scanning stream the cell was EMPTY and the seed fired -- printing a no-edge finding that quoted the very cell contents it had just called empty	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R38 seed) a row whose `Deps` cell holds only a code span ``b.rs`` carries an edge, so ordering prose naming no other party reports NOTHING.  Read off the prose-scanning stream the cell was EMPTY and the seed fired -- printing a no-edge finding that quoted the very cell contents it had just called empty	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R38 seed) a row whose `Deps` cell holds only an autolink carries an edge, so ordering prose naming no other party reports NOTHING.  Read off the prose-scanning stream the cell was EMPTY and the seed fired -- printing a no-edge finding that quoted the very cell contents it had just called empty	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42 §6.1) the partner that bounds the demotion: the SAME code span OUTSIDE an image is a QUOTATION and its prose is not read -- a kind marker quoted whole stays quoted (I-A), so the §6.4 demotion must not leak out of the description	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42 §6.1/§6.4) the partner that bounds the whole demotion: the SAME padded span OUTSIDE an image is MASKED whole, so its content is not read, the slug never completes and NOTHING is reported -- the trim belongs to §6.4's reading of a description, never to a code span in ordinary prose	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42 §6.4) the baseline the cross-product is measured against: the id in BARE prose outside any image is one reported site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42) and the POINTER phrase on a blank id cell is NOT the contradiction: it says another row owns this one, which is exactly what an unkeyed row is for -- the #506 memo's `Function`/`eval` row is this shape, and a rule over every kind phrase rather than the marker alone reported it	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42) the partner that bounds it: the SAME blank id cell with no kind phrase in its declaring field is the deliberate non-row the exemption is for, and stays silent -- the blank is not what is reported, the contradiction is	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42-10) a documentation table whose first column is ID-SHAPED is silent: a landing record's review-round table (`obj`, `R1`...`R7`) keys its rows and declares nothing.  This is the control the REJECTED predicate fails -- it reports 151 such tables over the 141 plan memos on this disk	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42-10) the SAME unbound header with no kind phrase in it is silent -- the gate is keyed on the CLAIM, not on the header.  This is the control a header-near-miss predicate fails: `No.` differs from `#` there too, and that table declares nothing	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42-10) the phrase is read off the RENDERED cell, as the census reads a declaring field: a marker inside a CODE SPAN in an unbound table is not a claim, exactly as it is not one in a bound table's declaring field	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42-8 §6.6 seed) a raw HTML span DEMOTED into a resolved image description is not seeded -- the naming scan has already read it -- and a span crossing a LINE ENDING is not seeded per line either	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42-8 §6.6 seed) the same in a CELL, which is the other half of the seed's population and its own site: the paragraph control cannot see it	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42-8 §6.6) a raw HTML span inside a resolved image description is the alt's own TEXT, so the kind marker is NOT read across it: `![UMBRELLA, not a <span>terminal unit](img.png)` has the alt text `UMBRELLA, not a <span>terminal unit` (cmark 0.31.2 and commonmark.js 0.31.2 both, the latter escaping the brackets -- they are content) and the marker phrase is not in it.  Masked as markup the span JOINED its two sides and the row exited 1 on an ownership claim nobody made: a FABRICATED finding, the opposite direction from every other §6.4 defect on this surface	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42-8 §6.6) the R17 reading in BARE PROSE is untouched: the same span outside any image is masked whole and its id is no naming site.  The arm that was refuted is "a raw HTML span renders nothing INSIDE A RESOLVED IMAGE DESCRIPTION", not "a raw HTML span renders nothing"	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R42-9) and the arm that WAS refuted stays refuted: the POINTER phrase on a blank id is the legitimate shape (`declaring_rows` names the #506 memo's `Function`/`eval` row, `:1985`) -- another row owns this one, which is what an unkeyed row is for	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R47-2) and the I-A arm survives BY CONSTRUCTION, not by an exemption: a phrase quoted WHOLE in an unbound table still declares nothing.  `kind_disagreements` requires a STRADDLE, so the quoted form is not a disagreement and needed no case carved out of the widened gate	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R47-4) a NON-whitespace character where a gap would be is not a gap: `UMBRELLA, not-a terminal unit` is not the marker.  The word boundaries cannot see this -- they guard the two ENDS of the phrase -- so it is the only arm that fails when the gap class is widened from whitespace to a wildcard	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R47-4) and the WORD BOUNDARY still holds after the gap widened: `SUBUMBRELLA, not a terminal unit` is not the marker.  The gap is a character class between words, never a licence to match inside a longer one	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R47-4) and the WORD BOUNDARY still holds after the gap widened: `UMBRELLA, not a terminal unitary claim` is not the marker.  The gap is a character class between words, never a licence to match inside a longer one	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R49-2) and a genuinely blank `Deps` cell is still empty: the links arm is an ADDITION to the shape rule, not a replacement -- `—` carries no edge and no link	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R51) the links arm reaches the cd-seed as well: a row with ordering vocabulary in its prose and a bare `[](sib.md)` in its `Deps` cell emits NO `ORDER-PROSE?` seed, because the cell is not empty.  Measured 1 -> 0 when the arm landed, and unstated until an audit asked.  ⚠ The alternative reading -- a label-less link names a FILE and never says which ROW the ordering is against -- is recorded in `deps_is_empty`'s docstring; this control is what makes flipping it a decision	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(R52) a marker OUTSIDE the declaring field, quoted WHOLE (I-A: a quotation certifies nothing, and still does not): assertion (a)'s outside-field check searched the disposed STREAM alone, so the straddled spelling was invisible and the row left at rc 0 with only a non-gating seed while the clean spelling exited 1.  Asked through the canonical `_claims` now -- which keeps the quoted case by construction, since a phrase quoted whole is not a straddle	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(a) a self-declaring field that later says a sibling 'is not it' stays self-declaring	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(a) the same row with the marker in its declaring field ONLY: UMBRELLA-MARK 0 -- the scan reads the other cells, it does not report them	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(accept-vocab seed) a POINTER row is excluded from the population	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(accept-vocab seed) a row whose marker is ATTRIBUTED to another row is a pointer and owes no acceptance condition	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(accept-vocab seed) a terminal row stating an acceptance condition	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(anchor) `` `Slice `C owns it ``: the row noun is inside a code span, so on the disposed stream there is no `Slice C` to anchor on -- 0 sites (the bare `C` is a declared miss)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(ascii) `١.` (an Arabic-Indic digit) is not a list marker (§5.2: ASCII digits)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(autolink) a backtick inside an autolink is consumed by it, not by a code span (the three delimiters are read left to right as met; commonmark.js agrees) -- 0 sites	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(autolink) a declared id inside a URI autolink is no naming site: the span is masked whole, exactly as a link's destination is -- 0 sites	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(autolink) a declared id inside an EMAIL autolink is no naming site either (the second §6.5 arm, `mailto:` at the renderer) -- 0 sites.  The id is the whole local part: dropping the arm leaves `9z` bounded by `<` and `@`, so the mutant HAS a site to report (an id glued to a domain label -- `ops@9z.example.com` -- is no site either way and proves nothing)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(autolink) an autolink holding a declared id and a `\p` is NOT a `[LEX-UNSUPPORTED?]` seed: the construct is fully lexed and hides nothing, unlike a raw HTML span	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(autolink) an autolink's URL is never a sibling on disk (§6.5 admits an absolute URI or an email address only, and `sibling_path` stage (a) rejects a scheme): rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(autolink) backslash escapes do not work inside an autolink (§6.5 Example 603, `<https://example.com/\\[\\>`): the span is matched raw, so the `\\[` opens nothing and the id in it is masked -- 0 sites	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(autolink) the R21 reviewer's input `<https://example.com/[child](absent.md)>` is ONE §6.5 autolink: the brackets inside it are not link syntax, `absent.md` is no memo, rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(b) an umbrella row with an empty Deps cell	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(bare) a bare `.md` file name holding an id is a file token, not a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(bare) a dotted number is one token: `§6.9z` names no row	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(block) `[Slice 9z owns it]:\\n    code` IS a definition (§4.4: indented code cannot interrupt a paragraph; commonmark.js: destination `code`): a block of its own, not scanned	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(c-seed) a Deps cell that CARRIES the prose's party is not reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(c-seed) ordering vocabulary inside a code span is code, not prose	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(c-seed) the same prose with `#11-zz-alpha` in the Deps cell too: the cell carries the party, ORDER-PROSE? 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(cell) a `Deps` cell holding only an HTML comment is EMPTY: it renders nothing, so the umbrella row carries no Deps edge (`is_empty` reads the cell's stream, not its raw text)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(cite) `[C1]: ECMA-262 §1 says so, and the table cites it.` at a block start is prose (commonmark.js: a paragraph), and the citation shortcut stays exempt: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(cite) `[c1]` beside a declared no-owner id `c1`: a lowercase citation is masked by the same predicate the reference walk exempts it by -- 0 sites	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(cite) adjacent lowercase citations `[c1][c2]` are not a full reference (the exemption is the grammar's either-case predicate): rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(cite) the same gloss mid-paragraph is prose: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(d) an ownership clause inside a code span is code, not a two-owner claim	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(d) one owner is not two	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(def) `[sib]: child.md "title` whose title crosses a BLANK line is not a definition (commonmark.js: a paragraph): `[sib]` later is an exempt shortcut, rc 0, and the sibling is NOT walked -- its violation is not reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(def) `paragraph\\n[sib]: slice-9z-sib.md` alone: the orphan's own label bracket is exempt -- rc 0, nothing walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(def) `paragraph\\n[x]: #section\\n[x]`: the orphan names a section, never a memo -- the shortcut is prose, rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(def) `paragraph\\n[x]: https://example.com/a\\n[x]`: the orphan names an external URL, never a memo -- rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(def) a definition cannot interrupt a paragraph: the reference is unanswered, and reported ONCE (`[text][label]` re-scans `[label]`)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(def) a label-and-colon line that is NOT a valid definition (junk after the destination) at a block start is prose, not an orphan: `[sib]` later is exempt, rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(def) a next-line title holding `[x](missing.md)` is a title, not a link: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(def) a next-line title is part of the definition, not prose: an id in it is no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(def) text after the destination is not a definition, so the reference is unanswered: a schema miss	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(fence) a `#11-` slug inside a fenced block is not a naming site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(fence) a closer followed by text does not close	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(fence) a closer of the OTHER character does not close	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(fence) a link inside a fence is not a link (A x B)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(fence) a shorter closer does not close	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(fence) a tilde fence masks too	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(fence) an unclosed fence runs to the end of the document	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(fence) text inside a fenced block is not prose	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(fence) three spaces of indent is	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(file) `(9z).md` is one file name (a balanced parenthesis pair): no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(file) `.md` alone is a file name (the suffix-only name `sibling_path` accepts): beside a declared no-owner id `md`, `Read .md for details` reports 0 sites	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(file) `9z+notes.md` is one file name: no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(file) `9z@notes.md` is one file name: no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(file) `notes.md` beside a declared no-owner id `md` is still one file name: 0 sites	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `# h\\n<span>\\n9z owns it`: after a one-line block no paragraph is open, so the type-7 opener is a block start (commonmark.js: a heading and a raw block): no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `<!-- Slice 9z -- owns it -->`: `--` inside a comment is allowed since 0.31 (§6.6: "a string of characters not including the string -->") -- the whole comment is masked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `<!X Slice 9z owns it>`: a declaration is masked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `<![CDATA[ Slice 9z owns it ]]>`: a CDATA section is masked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `<![CDATA[` opens a type-5 block to `]]>` (Example 182): raw, no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `<!doctype` opens a type-4 block (§4.6 condition 4: `<!` + an ASCII letter, either case) to the `>` line: raw, no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `<? Slice 9z owns it ?>`: a processing instruction is masked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `<a title=9z-owns>x</a>`: an unquoted attribute value is masked too	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `<span title="Slice 9z owns it">x</span>`: an id inside an attribute value is no naming site -- the span is masked whole (kind `html`), as a raw HTML-block line is raw	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `<span>\\n9z owns it` at a block start IS a type-7 block to the blank line: raw	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `Heading\\n===\\n<span>\\n9z owns it`: after a setext heading no paragraph is open (commonmark.js: a heading and a raw block) -- the look-back at the previous line missed this: no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `text\\n<DIV>\\n9z owns it`: `<DIV>` opens a type-6 block (§4.6 condition 6 is case-insensitive), which interrupts the paragraph -- raw to the blank line, no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) `text\\n<PRE>\\n9z owns it\\n</pre>`: `<PRE>` opens a type-1 block (§4.6 condition 1 is case-insensitive), which interrupts the paragraph -- raw to `</pre>`, no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) a CDATA section `<![CDATA[ [x](absent.md) ]]>` is raw: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) a cell's `<span title="Slice 9z owns it">` is masked by the same inline pass: no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) a comment may cross the paragraph's line ending (`<!--\\n[x](absent.md)\\n-->`, Example 625's shape): rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) a declaration `<!X [x](absent.md)>` is raw: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) a processing instruction `<? [x](absent.md) ?>` is raw: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) a single-quoted attribute value `<span title='[x](absent.md)'>` is a tag: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) an HTML comment `<!-- [x](absent.md) -->` is raw: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) an UNQUOTED attribute value holding brackets `<span title=[x](absent.md)>` IS an open tag (§6.6: an unquoted value excludes only spaces, tabs, line endings, `"`, `'`, `=`, `<`, `>` and a backtick; commonmark.js agrees -- ⚠ the R17 brief listed this shape as a negative): rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(html) the R17 reviewer's shape `<span title="[child](absent.md)">text</span>`: a bracket inside a double-quoted attribute value is no link -- nothing is walked, rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(id) an id cell `-` is empty: not declared, not a schema miss	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(id) an id cell `-` is not an id	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(id) an id cell `–` (en dash) is a literal blank: a non-row	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(id) an id cell `—` is a literal blank: a deliberate non-row, rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(id) the `Intl`-shaped cell never mints the whole cell as an id	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(id) the id cell's own id is not a site: `**7z** — MERGED` reports nothing	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(image) `![a ![b [c](child.md)](i.png)][missing]`: the OUTER image fails but the INNER one resolves, and the link inside the inner description is demoted (commonmark.js: `<img alt="b c">`) -- `child.md` is not walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(image) `![alt [b](9z)](i.png)`: the demoted link's tail stays masked -- the `9z` in its destination is not prose, 0 sites	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(image) `![alt [docs](child.md)](absent-image.md)`: neither the demoted link nor the image is a memo link -- `child.md` is not walked (its violation is unreported) and the image's `.md` destination is not probed	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(image) `![alt [x][missing]](img.png)`: a failed reference inside a resolved image's description names no lost memo (resolved, it would have been demoted; commonmark.js: `alt="alt [x][missing]"`) -- rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(image) `![alt][img]` with a definition is consumed whole: `[img]` is not re-read as a shortcut, and the image destination is not a memo	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(image) a nested image in a resolved image `![a ![b [c](child.md)](i.png)](j.png)`: the inner image stays masked and the link inside it is demoted -- `child.md` is not walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(image) an undefined reference image `![diagram][missing-image]` is literal syntax, not an unresolved memo reference: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(image) the R19 reviewer's input `![alt [docs](absent-file.md)](image.png)`: the image resolves, so its description is plain text (§6.4; commonmark.js: `<img alt="alt docs">`) -- the link inside it is no memo link, nothing is walked, rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(image) the reviewer's input `![alt][missing]` + an orphan `[missing]: image.md` in the same paragraph: the re-scanned `[missing]` is the same failed site, not a shortcut for the orphan rule -- rc 0, `image.md` never walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(indented) `    Slice 9z owns it` at a block start is a raw extent (§4.4), like a fence: no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(indented) ` \\tSlice 9z owns it`: a space then a tab is four columns (§2.2) -- raw, no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(indented) `\\tSlice 9z owns it`: a tab is four columns (§2.2), so the line is indented code at a block start -- a raw extent, no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(item) `- item\\n\\n      Slice 9z owns it`: six columns are four inside the item -- indented code IN the item (commonmark.js), raw: no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(item) `- item\\n\\n    Slice 9z owns it` is no raw line: the `item` seed reading is gone with the flat reading -- 0 seeds	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(item) `- item\\n\\n  para\\n\\n# h\\n\\n    Slice 9z owns it`: a 0-column heading is a lazy candidate that is a block start -- the item ends, the heading and the code block are outside it: no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(item) `- item\\n\\n para\\n\\n    Slice 9z owns it`: a 1-column line after a blank is short of the content indentation with no paragraph open, so the item ENDS before it (commonmark.js: a paragraph and a code block outside the list) -- the indented line is §4.4 raw, no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(item) `- open `x\\nSlice 9z owns it` end`: the line short of the content indentation is the item paragraph's lazy continuation text (§5.2 rule 5), so the span crosses it: no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(item) `open `x\\n*\\n9z owns it` end`: an EMPTY item cannot interrupt a paragraph (§5.2; `*`, not `-`, which would be a setext underline) -- one paragraph, the span masks the site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(item) `open `x\\n2. 9z owns it` end`: an ordered item not starting at 1 cannot interrupt a paragraph (§5.2) -- one paragraph, the span masks the site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(kind) `SUBUMBRELLA, not a terminal unit.` carries no marker: the row is terminal, out of the no-owner census, and a prose mention of it is no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(kind) `The KIND UNDETERMINEDNESS metric must be recorded` declares no unsettled kind: with a nonempty `Deps` cell the unbounded reading made the row no-owner and forced `UMBRELLA-CELL`, rc 1	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(kind) `is a pointer rather than a slice` still IS one, and a pointer row owes no acceptance condition -- the discriminating half	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(kind) `not a terminal unitary claim` is not the kind vocabulary either: the seed half of assertion (a) reads the same bounded phrases the marker does	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(kind) a declaring field holding a code span and NO marker is no miss: rc 0 -- the residue is a marker the reader reads ACROSS a blanked span, never the presence of one	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(kind) nor does `MANKIND UNDETERMINED by the probe` -- the boundary is on BOTH sides, and the left one is the half a right-only fix would leave authoritative	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(kind) nor does `UMBRELLA, not a terminal unitary claim.` -- the right-hand edge	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(kind) nor is a field carrying the marker WHOLE beside a code span: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(kind) the POINTER spelling read across a comment: the row is a POINTER, and a pointer row owes no acceptance condition -- so the ACCEPT-VOCAB? seed stays silent where reading the comment as text would have made the row an active terminal owing one	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(kind) the marker QUOTED WHOLE in a code span declares nothing (I-A: a quoted marker is not a declaration) -- the row is terminal and its mention is no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(label) `[foo&auml;]: child.md` then `[fooä]`: §6.3 label matching is on the RAW label (case fold, strip, collapse -- no character-reference decoding; commonmark.js: no link) -- the shortcut is prose, 0 sites, `child.md` not walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(lex-seed) `a<br>b` / `<sub>2</sub>`: tags holding neither a `\p` nor a declared id are no seed	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(lex-seed) a type-6 block ends at a blank line: the paragraph after it is not seeded	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(lex-seed) an HTML-block line with neither a `\p` nor a declared id is no seed	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(lex-seed) an indented-code line with neither a `\p` nor a declared id is no seed	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(licence) `9z's memo` IS it and is licensed -- the discriminating half	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(licence) `The child of 9z` IS the phrase and is licensed -- the discriminating half	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) `<dest>` may not contain an unescaped `<`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) `[a ![b [c](child.md)](i.png)](parent.md)`: the inner link deactivated the outer `[` as it closed, before the image demoted it (§6.3 "links may not contain links"; commonmark.js: `[a <img alt="b c">](parent.md)`) -- neither `child.md` nor the absent `parent.md` is linked: 0 sites, not rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) `[label][undefined]` is neither a full reference nor a shortcut (§6.3 Example 571: a shortcut is not followed by a link label) -- the unanswered label is a schema miss, not a link to the sibling	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) `[x](child&#46md)`: `&#46` without `;` is no reference -- the destination is the literal `child&#46md`, which names no memo: 0 sites, `child.md` not walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) `[x](child&#x26;#46;md)`: a decoded `&` is a character, never the start of a second reference (one pass, no re-scan) -- literal `child&#46;md`, 0 sites	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) `[x](child&MadeUpEntity;md)`: a name not on the HTML5 list is literal text (§2.5 Example 30) -- no memo, 0 sites	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) `[x](child\\&#46;md)`: a backslash-escaped `&` opens no reference (ONE pass: §2.4 and §2.5 meet at the character once) -- literal `child&#46;md`, no memo, 0 sites	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) `notes%3Achild.md` is NOT a sibling.  It has no scheme -- WHATWG URL §4.4 #scheme-start-state / #scheme-state read the input as written and `%` is in neither class, #string-percent-decode being a later, separate operation -- so stage (a) admits it; stage (c) then refuses it, because the decoded `notes:child.md` is an NTFS alternate data stream on Windows and a plain file name on POSIX, which is two readings where that stage allows one.  ⚠ THIS EXPECTATION IS A REVERSAL of PR #510 R8, decided at R25-1, and it costs a POSIX memo genuinely named `notes:child.md` -- dropped without a report, as `sub\\child.md` and `NUL.md` already are	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) a `#11-` slug in a link DESTINATION is not a naming site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) a `(…)` title may not contain an unescaped `(`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) a `[C19]` citation is a shortcut with no definition anywhere: no miss	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) a collapsed-shaped citation `[C19][]` is not a reference: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) a link inside a code span is not a link (A x B)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) adjacent citations `[C19][C20]` are not a full reference: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) an ASCII control character (0x7F) ends a bare destination	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) an escaped `\\[` opens nothing: `\\[x](absent-file.md)` is not a link, rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) bracket text holding unescaped brackets is not a label (§6.3), so `[the [x] walk][]` is no collapsed reference: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(link) label matching collapses spaces, tabs and line endings only: a non-breaking space is not a space, so the reference is unanswered	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(quote) `> Heading `open\\n===\\nSlice 9z owns it` here`: a lazy `===` is the quote paragraph's text, not an underline (§5.1, Example 93) -- one paragraph, the span masks the site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(quote) `> [a]: /u\\n>` then the lazy schema header: the blank quote line closes the run, so the quote ENDS and the header is a paragraph outside it (cmark-gfm) -- no id declared	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(quote) `> open `here\\nSlice 9z owns it` there`: the marker-less line is lazy continuation text of the quote's paragraph (§5.1), so the span crosses it: no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(quote) a fenced block then the lazy schema header: no run is open, the quote ends (cmark-gfm) -- no id declared	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(quote) a slot table inside a block quote whose DELIMITER row is lazy is one paragraph (cmark-gfm: the delimiter arrives in an unmatched container): no id declared	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) `n%3Achild.md`: a ONE-letter name before `:` is a Windows drive letter (URL `#path-state` step 1.4.1, platform-independent) -- drive-relative, rejected at stage (c) by the ANCHOR, rc 0.  ⚠ Since R25-1 the multi-letter `notes%3Achild.md` is refused at the same stage by the CHARACTER rule, so this no longer discriminates one-letter from multi-letter; what it still says is that the drive reading is applied on every platform	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) a code-quoted link to an absent file is not a link: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) a link cycle (A -> B -> A) is walked once and is not an error	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) a memo whose every row is terminal is rc 0, not a schema miss	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) a percent-encoded ABSOLUTE destination `%2Ftmp%2Fchild.md` is rejected after decoding (never probes `/tmp/child.md`): rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) a percent-encoded UNC `%5C%5Cserver%5Cshare%5Cx.md` is rejected (stage c: a UNC prefix anchors): rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) a percent-encoded Windows drive-absolute `C%3A%5Ctemp%5Cchild.md` (`C:\\temp\\child.md`) is rejected after decoding on every platform (stage c: a drive anchors): rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) a percent-encoded backslash-rooted `%5Cchild.md` (`\\child.md`) is rejected (stage c: a root anchors): rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) a protocol-relative `//host/x.md` is not a sibling on disk: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) a raw `\\\\server\\share\\x.md` destination decodes (§2.4: `\\\\` is one backslash, `\\s` is literal) to the backslash-rooted `\\server\\share\\x.md` (commonmark.js: href `%5Cserver%5Cshare%5Cx.md`) and is rejected: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) a root-relative `/guide.md` is not a sibling on disk (nothing probed): rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) a slice header over a one-cell delimiter row is not a table, so its wide body row is not a width miss	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) an absolute URL ending in `.md` is not a sibling on disk: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) an all-terminal memo reports a zero census	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) an excess body cell of a non-schema table holding `[x](absent-file.md)` is ignored (GFM §4.10): no link, nothing walked, rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) an indented slot row after a table's rows is raw, so its id is not declared and the run is rc 0 (the seed, not the census, carries it)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) drive-relative `C:child.md`: raw, it is a URL of scheme `c` (stage a); percent-encoded `C%3Achild.md` decodes to a drive-anchored name (stage c) -- both rejected, rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(rc) two MARKED rows spelling it the SAME way: KIND-SPELLING 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) GFM strikethrough `W~~z~~` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) `&#42;&#42;C&#42;&#42;` is the same rule for §2.5: a reference that would spell a decoration stands as written	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) `**9z**'s children`: the licensing rule needs no decoration spelling of its own -- the mention's token consumes the `**` it carries, so the licensed noun is what follows it	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) `*9z*7z` names nothing: a SINGLE `*` is not this document's decoration (`plan_memo_ids.DECOR` is `**` and a backtick), so the pair renders away and `9z7z` is one token -- the exception is the decoration's, not emphasis's	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) `9z is an *umbrella* row`: same, for the kind-statement arm of the licence	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) `9z's **charter**`: the licensed noun carries the decoration, and the phrase is licensed all the same	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) `Slice W**z** owns it` seeds NOTHING: the construct renders no character, so the two readings agree and there is no residue to report	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) `Slice W*z owns it`: an UNMATCHED `*` run is literal text (§6.2 -- commonmark.js renders `9*z` verbatim), so it BOUNDS the token and `Wz` is not named -- dropping a run that pairs with nothing would fabricate the row	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) `Slice W_z_ owns it`: an intraword `_` run can neither open nor close (§6.2 rules 5-6, the `snake_case` rule), so it is literal and bounds the token	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) `Slice W`z` owns it`: a code span RENDERS TEXT this checker refuses to read (I-A), so it blanks and bounds the token -- the residue the `[LEX-SPLIT?]` seed reports rather than decides	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) `Slice W~~~z~~~ owns it`: three tildes are no strikethrough (GFM: a matching pair of ONE OR TWO), so the run is literal text and bounds the token	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) `\\*\\*C\\*\\* is how the row is written`: an ESCAPED decoration character stands as written -- substituting it would spell a `**C**` bold the document does not have, and the undecorated single letter is the declared miss	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) `each of *9z's* children`: the licensing phrase is read as the document renders it, so the emphasis no longer hides a licensed construction	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) §2.5 a character reference `W&#122;` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) §6.2 emphasis `W*z*` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) §6.2 strong emphasis `W**z**` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) §6.3 a link's brackets `W[z](slice-9z-sib.md)` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) §6.6 a raw HTML tag pair `W<b>z</b>` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(render) §6.6 an HTML comment `W<!-- c -->z` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(row) `a\\\pb` is one cell (the odd backslash escapes the pipe): rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(row) a trailing `\\\\\p` is a literal backslash then the trailing pipe: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(schema) a citation id in the citation table is admitted: rc 0 -- the three controls above have a live subject	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(seed) a raw HTML-block line whose only declared id sits INSIDE a `.md` file name seeds nothing: the same name in prose is masked and reports nothing, and a seed that fires where prose would not is reporting a reading the document has no way to hold	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(seed) a raw line whose only declared id is a CITATION seeds nothing -- the shared spans SUBSUME the seed's former hand-written `kind != cite` filter, so the citation half has one spelling and not two	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(seed) the same for an INDENTED-CODE line: one disposition, both readings	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(setext) `==` after a list item is NOT an underline (§4.3 Examples 92-94): the item's paragraph continues and a code span crosses it	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(sibling) `NUL.md` is a DOS device, not a memo beside this one: the destination is no sibling even though a file of that name is there to read	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(sibling) `NUL/child.md`: the device is read per PART of the parsed path -- a device DIRECTORY rejects the destination too.  The device sits in a NON-FINAL component on purpose: with it in the last one, a final-component-only reading passes this control and the mutant survives (measured -- the probe would have had another subject)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(sibling) `com1.md`: the device names fold case (`ntpath._isreservedname` upper-cases the stem), so the lower-case spelling is the same device	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(sibling) `dir%20/child.md`: a component ending in a SPACE is reserved too -- Windows strips the trailing run, so that component names a different directory there than here	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(sibling) `prn%20.md`: the stem's trailing spaces are stripped before the device lookup, so `prn .md` is `PRN` (Microsoft, "Naming Files, Paths, and Namespaces")	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(slug) `#11-zz-alphaZZ` in prose is not the id `#11-zz-alpha`: the slug is bounded on its right as on its left -- 0 sites	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(slug) `` `tool #11-zz-alpha_extra` ``: the kept-slug exception inside a code span reads the grammar's boundary, so nothing is excepted and the span stays code -- 0 sites	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(span) `` `[c](child&#46;md)` `` is a code span: §2.5's first exception -- nothing in it is a destination and nothing is decoded; 0 sites, `child.md` not walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(span) a backtick BEFORE the `]` opens a code span that swallows it: `[not a `link](absent.md)`` is code, no link, rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(span) a code span may cross a line ending	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(span) a quoted kind marker is not a declaration (A x E)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(span) an NBSP-only line is NOT blank (§4.9: spaces or tabs only), so it does not end the paragraph and the code span crosses it	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(span) backtick strings pair by EQUAL length	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(stream) ordering vocabulary in a link TITLE is the link's tail, not prose	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(table) `---` right after a schema table is a thematic break (§4.1: no paragraph is open so §4.3 does not apply; GFM §4.10: another block-level structure ends the table): rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(table) `\\t\p Slot \p ... \p` over `\\t\p---\p...\p`: a tab-indented header and delimiter row are indented code (§4.4, four columns), not a table -- the wide row under them is no width miss: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(table) a delimiter cell is >=1 hyphen with optional colons; `:` alone is not	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(table) a list item right after a schema table ends it (a block start), so it is not a 1-cell body row: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(table) a row opening with an NBSP before its `\p` is not edge-piped: the NBSP is a cell, the header is 7 wide over a 6-cell delimiter, no table	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(table) a type-7 HTML opener right after a schema table ENDS it (a table is not a paragraph): `<span>` + prose are raw lines, not one-cell rows -- no width miss, rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(table) an excess body cell of a non-schema table is ignored (GFM §4.10): its `9z owns it` is never lexed -- no site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(table) an indented line right after a schema table opens an indented code block (a block-level structure; no paragraph is open), not a one-cell row: rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(table) header and delimiter of unequal width are not a table	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(table) the prose inside the type-7 block after a schema table is raw, not a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	(vocab) acceptance vocabulary split by emphasis is acceptance vocabulary: a terminal row writing `m*us*t` owes no ACCEPT-VOCAB? seed	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	a bold sentence opener is not row A	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	a row naming itself in its own cell	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	a self-declaring row that MENTIONS a sibling stays in the population	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	an id in a titled link's destination is not a naming site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	an id-looking token inside a file name	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	an id-looking token inside inline code	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	possessor of a charter	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	possessor of children	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	statement about the row's kind	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	the child-of construction	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	NEGATIVE	the derivation-mints construction	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R23 kind) `KIND UNDETER`MINED`` in a declaring field is the schema miss: a reader reads the undetermined kind (§6.1 -- the code span contributes `MINED` as plain text), the disposed stream reads none, and the row silently left the census as an active terminal at rc 0 -- the marker was gated, every OTHER kind phrase was not	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R23 kind) `KIND `x` UNDETERMINED` is the SAME miss from the other side: the blank stands as spaces, so the STREAM reads the undetermined kind and a reader (`KIND x UNDETERMINED`) reads none -- a gate stated over one direction leaves the other authoritative	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R23 kind) `KIND&ast;UNDETERMINED` is that same stream-side miss through a BLANKED decoration spelling: the reader sees `KIND*UNDETERMINED` and no kind at all.  It is the one control over what a reader SEES where such a span is blanked -- render it as spaces and the two readings agree on a kind neither should read	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R23 kind) the POINTER phrase straddling a code span is the miss too -- the third member, gated by arriving in `KIND_PHRASES` and not by being named here	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R23 memo) a linked memo whose FIRST block is a slice table still declares its rows when the file opens with a BOM: the signature is dropped where the text becomes lines, or the header row is not a header row and the census silently shrinks at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R23 memo) the same sibling WITHOUT a BOM declares the row -- the discriminating half: the table, the schema and the link are the control's constants	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R23 render) `Slice Qx owns it` IS the site -- the discriminating half of the blank: the row is an umbrella and the joined spelling is what would be reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R23 render) `The ast marks it` IS the site -- the discriminating half: the row is declared, the scan reaches that prose, and only the SPELLING differs	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R23 seed) the `[LEX-SPLIT?]` residue seed reads every kind phrase too, not only the marker: `KIND UNDETER`MINED`` in PROSE (no declaring field, so no census to gate) is the reported disagreement	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R24 render) `**&#57;z**7z owns it` names `9z`: the pair's content RENDERS `9z`, so the `**` decorate an id and stand as the boundary they are.  Asked of the source, `id_only` read `&#57;z`, dropped the delimiters, and joined the two sides into the token `9z7z` a reader never sees -- the mirror of the fabrication design re-gate 4 closed, one spelling further out	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R24 render) `**9z**7z owns it` names `9z` -- the discriminating half of the decoration exception, green on both sides of the fix	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R24 render) `9z notes.md owns it` names `9z` -- the discriminating half: the same document with the space spelled as a space, green before the fix and after it, so the pair differs only in the SPELLING of a character both render	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R24 render) `9z&#32;notes.md owns it` names `9z`: §2.5 renders the reference as a SPACE, so the file name begins at `notes` and the id stands beside it -- the raw reading saw one unbroken run ending in `.md`, masked the whole of it, and the ownership claim left the census at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R24 width) `the grandchild of 9z` is still REPORTED -- the left edge the trailing noun clause must not loosen	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R24 width) `xderivation … that the 9z` is REPORTED: the licensing phrase is 40 characters, exactly the width of the slice the backward look was given, so its lookbehind fell off the start of that slice and succeeded against nothing -- the document says `xderivation`, which is not the licensed phrase	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R24 width) a pointer row whose appositive names a 76-character slug attributes the marker to that row: the appositive ends where the marker begins, which is a GRAMMAR fact and not a character count.  Read through a 70-character window the appositive fell outside it, the field was taken as the row's OWN declaration, no UMBRELLA-MARK was emitted, and the census carried a pointer row as an umbrella at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R24 width) the same shape with a SHORT slug attributes too -- the discriminating half: the row, the marker and the appositive are the control's constants, and only the length of the named id differs	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R24 §2) a link destination holding a literal U+0000 names the file the document renders: §2 replaces the NUL with U+FFFD before parsing, so `[x](child .md)` links `child<U+FFFD>.md`, that memo is walked and its rows are declared.  Left in, the NUL is an ASCII control, `link_destination` (§6.3) refuses the destination, and the memo -- with every violation in it -- left the census while the run could still exit 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R24 §2) the same link with the REPLACEMENT CHARACTER written out is walked -- the discriminating half: the file, the table and the link are the control's constants, and only the SPELLING of the destination's one character differs	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R24 §2.1) a memo whose lines end with a bare CARRIAGE RETURN is one document of many lines: §2.1's line ending is `\\n`, `\\r\\n`, or `\\r` not followed by `\\n`, so the tables parse and the rows are declared.  Read without that rule the whole file is one line, no table is admitted, and no schema miss says so	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R24 §2.1) the same memo with LINE FEED endings declares the same row -- the discriminating half, and the reason the CR case is a claim about the ENDING rather than about the fixture	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R25 §6.7) a HARD line break spelled with a BACKSLASH names the row across it: `Slice\\` + a line ending + `C` reads `Slice C` (§2.4, vendored Example 16: `foo\\` + an ending renders `<p>foo<br />` + `bar</p>`), so the umbrella row `C` is a naming site.  Until R25 the backslash stood in the stream, `NOUN_ANCHOR` could not reach the id, and the run exited 0 with no site at all	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R25 §6.7) the TWO-SPACE hard spelling, green before the fix too: its spaces render as whitespace, which is what the ending contributes anyway, so this spelling never held the defect and the three must agree	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R25 §6.8) the SOFT-break partner, green before the fix: the same two words across a plain line ending name the same row -- this is the control that says the backslash case is about the BACKSLASH and not about the adjacency	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R26 file) `9z(x.md)` reports `9z`: the run ending at the suffix may not hold the parenthesis still OPEN there, so it starts one past it and the name is `x.md` -- the half that says the start is the innermost open parenthesis and not the segment	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R26 file) `9z)foo.md` still reports `9z`: a `)` that closes nothing may be held by no run and crossed by none, so the name is `foo.md` and the id before it is outside -- the second discriminating half, against a scan that balanced parentheses by IGNORING the ones it could not match	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R26 file) `foo((9z)) .md` -- the same characters with a space before the suffix -- still REPORTS `9z`: a run ends at whitespace, so this is not a file name at all.  The discriminating half: a fix that widened the token until every parenthesis was swallowed would pass the three above and fail this one	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R26 §6.3) a destination nested 32 deep IS a link, so the memo it names is a sibling this run could not read: rc 2.  Green before the limit as after it -- the half that says the limit is a LIMIT and not a refusal of nested destinations	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R27 noun) ``Slot `#11-zz-alpha` — **UMBRELLA, …**`` attributes the marker to the named row: the containing row is a POINTER and the attribution finding is emitted. The schema noun the §8 table's own id column is headed with, and the one spelling the hand-written alternation left out	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R30 id) an id cell `***` renders literally too: the strip took the whole run whatever its length, so a longer run was as blank as a shorter one -- unkeyed, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R30 id) an id cell `**` is an unmatched strong-emphasis run: §6.2 pairs nothing with it and a reader sees `**`, so the row is UNKEYED -- a schema miss, rc 2.  The reported shape: before R30 the unconditional strip made it the empty-string blank and this row -- marker in its declaring field, a `Deps` edge -- exited 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R30 id) an id cell `*` is a single unmatched delimiter: the same run one character short, and not a `DECOR_MARK` at all -- unkeyed, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R30 id) an id cell `` ` `` is one backtick that opens no code span (§6.1 wants a closing backtick string): a reader sees the backtick, so the row is unkeyed, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R30 id) an id cell `` `?` `` is unkeyed: a code span RENDERS its content (§6.1) and a reader sees `?`.  The discriminating case for WHICH rendering: the disposed stream -- what every other predicate over a block reads -- blanks a code span, so under it this cell is empty and the row is a blank, which is the silent-skip class re-opened	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R30 id) an id cell ``` `` ``` is ONE backtick string of length 2 that closes nothing (§6.1: the closer must be of EQUAL length), so it renders literally.  The case that says the pairing authority is the inline LEXER and not the id grammar's decoration walk: that walk reads two `DECOR_MARKS`, so a hand-rolled peel of matched pairs -- the other shape offered for this fix -- strips it to nothing and calls the row a blank	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R30 id) the keyed partner, end to end: `**7z**` keys the row, so the marker and the `Deps` edge ARE asserted -- UMBRELLA-CELL, rc 1.  This is the run the six unkeyed cells above silently left, and it must not be reached by asking the blank question of a row that declared an id	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R30-3 §6.4) a kind phrase crossing a LINK inside a resolved image description is read: `![KIND [UNDETERMINED](x)](img.png)` renders the alt text `KIND UNDETERMINED` (Example 575), so the row is kind-undetermined and its `Deps` edge is asserted.  The reviewer's shape: the demoted link's `[` was popped off the opener list, stood in the stream, and the row left the census as terminal at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R30-3 §6.4) a resolved image's own `![` is a BLANK where it is NOT demoted, and the difference is loud: in `KIND ![UNDETERMINED](img.png)` the phrase STRADDLES the image, and the text around an image and the text of its description are two runs (a reader of the first sees a picture where the second stands), so the row is not kind-undetermined -- but the near-miss is reported as a schema miss at rc 2 rather than passed over.  The discriminating half of the clause above: recording the opener as a blank is what makes the phrase visibly CROSS something, and without the record the `![` is literal text, nothing is straddled, and the run exits 0 saying nothing	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R30-3 §6.4) and the phrase crossing a nested IMAGE's markup: `![KIND ![](i.png)UNDETERMINED](img.png)` renders `KIND UNDETERMINED` (Example 574: a nested image's alt text is folded into the enclosing one's).  The inner `![` is the third piece of markup, and it was recorded nowhere at all -- neither clause above reaches it, since a link's `[` is recorded and a tail is recorded	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R30-3 §6.4) the phrase with the inner markup simply REMOVED -- `![KIND UNDETERMINED](img.png)` -- is the three positives' CONSTANT: a description whose plain string content is the phrase outright, read as prose exactly as §6.4 reduces it.  Green before R30-3 and after, which is what makes the three above claims about the MARKUP a description encloses rather than about descriptions	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R30-3 §6.4) the same phrase crossing a demoted link's TAIL: `![KIND [](x)UNDETERMINED](img.png)` renders `KIND UNDETERMINED` too, because an empty label contributes an empty string and the tail contributes nothing.  This is the clause the opener case cannot see -- there the whole phrase stands before the tail -- and a tail disposed as a BLANK puts a run boundary through the middle of the phrase	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R31-1 §2.5) a linked memo whose `slice` header spells `#` as `&#35;` IS that schema: the character reference renders `#`, so the table is bound, `8z` is declared an umbrella and the terminal row's `Deps` edge is a naming site.  The reported shape -- before R31 the raw comparison made it a non-schema table and every declaration and assertion in the memo left the run at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R31-1 §2.5) the same sibling with the header spelled `#` outright reports the same one site -- the discriminating half: the memo, the rows and the edge are the control's constants and only the SPELLING of one header cell differs, so the case above is a claim about the reading and not about the fixture	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R32 §2.5/GFM) a `&#124;` inside a cell does NOT split it: the row keeps its six cells, so it is no schema miss and the `Deps` edge is still read.  Split at the reference, the row would have seven cells and the run would be rc 2 -- which is why the measure here is the SITE and not merely the exit status	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R32 §2.5/GFM) a delimiter row spelled `&#45;&#45;&#45;` is NO delimiter row: a table's shape is settled over RAW text before any inline construct exists, so the character reference never becomes a hyphen, the table never forms, and the linked memo declares nothing.  The clause `admit_table` states and nothing probed	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R32 §2.5/GFM) the same sibling with a plain `---` delimiter row DOES form the table and names the umbrella -- the discriminating half: the memo, the rows and the edge are the control's constants and only the delimiter row's spelling differs	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R33-1) `Slice 9z — **UMBRELLA, …**` attributes the marker to the named row: the appositive reading, unchanged.  The control's constant -- what R33-1 narrows is where the search may BEGIN, never what a real row noun means	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R33-2) and the HYPHEN spelling, also green before -- three spellings of one separator, which is why the set is spelled once and composed rather than written out at each reader	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R33-2) the EM DASH spelling, green before the fix -- the discriminating half: the row, the phrase and the edge are the control's constants and only the dash differs	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R34-1) `9z+notes.md_tail owns it` REPORTS `9z`: the run continues into more name, `sibling_path` follows no such file, and so the run is no file name and the id in it is prose.  The reported shape -- read as 'the suffix is not followed by an alphanumeric', `9z+notes.md` was masked, the ownership claim produced no site and the run exited 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R34-2) an umbrella row whose `Deps` cell holds only a bare `.md` name carries an edge: the construct RENDERS something a reader sees, so the cell is not empty and UMBRELLA-CELL is emitted.  Read off the prose-scanning stream -- which blanks exactly the constructs a prose scanner must not read as prose -- the cell was EMPTY and the row's forbidden dependency left the run at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R34-2) an umbrella row whose `Deps` cell holds only a citation `[C1]` carries an edge: the construct RENDERS something a reader sees, so the cell is not empty and UMBRELLA-CELL is emitted.  Read off the prose-scanning stream -- which blanks exactly the constructs a prose scanner must not read as prose -- the cell was EMPTY and the row's forbidden dependency left the run at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R34-2) an umbrella row whose `Deps` cell holds only a code span ``b.rs`` carries an edge: the construct RENDERS something a reader sees, so the cell is not empty and UMBRELLA-CELL is emitted.  Read off the prose-scanning stream -- which blanks exactly the constructs a prose scanner must not read as prose -- the cell was EMPTY and the row's forbidden dependency left the run at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R34-2) an umbrella row whose `Deps` cell holds only an autolink `<https://x.example>` carries an edge: the construct RENDERS something a reader sees, so the cell is not empty and UMBRELLA-CELL is emitted.  Read off the prose-scanning stream -- which blanks exactly the constructs a prose scanner must not read as prose -- the cell was EMPTY and the row's forbidden dependency left the run at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R35) the discriminating half: with the SAME id one space away from the same name, `See notes.md 9z owns it` REPORTS it -- what the tail rule covers is the run, and a new run is prose again	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R38 seed) the partner that bounds it: a `Deps` cell that IS a deliberate blank (an em dash) with the same ordering prose still reports the no-edge finding -- the reading moved and `is_empty` still decides by SHAPE	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.1/§6.4) a PADDED code span inside a resolved image description renders its TRIMMED content: §6.1 removes one leading and one trailing space when the content begins and ends with one, and §6.4 uses that string -- so `` ![Slot #11-zz-alph` a ` owns it](img.png) `` has the alt text `Slot #11-zz-alpha owns it` and the slot is ONE reported site.  Read verbatim it splits into `#11-zz-alph` and `a`, which are declared nowhere: no site, rc 0, and no residue to say the checker could not read it	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.1/§6.4) the arm the spec spells out, and the one an unconditional trim breaks: a span that consists ENTIRELY of spaces is NOT trimmed ("but does not consist entirely of space characters"), so `` ![Slot #11-zz-alpha`  `owns it](img.png) `` keeps the separator and the slug stands alone -- trimmed, it would fuse with what follows and the site would vanish.  ⚠ The span is the ONLY separator here, deliberately: written with a space after the closing backtick the control stayed green under the mutant, because that space did the separating and the claim was never tested	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.1/§6.4) §6.1's TWO steps run in ORDER: a code span padded with a LINE ENDING inside a resolved image description is normalised to a space FIRST and only then trimmed, so `` ![Slot #11-zz-alph`<NL>a ` owns it](img.png) `` has the alt text `Slot #11-zz-alpha owns it` and the slot is ONE reported site.  Written against the RAW content the trim declines -- `\\n` is not a space -- the slug splits into `#11-zz-alph` and `a`, declared nowhere, and the run exits 0 saying nothing.  ⚠ This is the half of the R42-7 fix that shipped UNPINNED: both R42-6 mutants patch the trim, so step one could be made a no-op with every control green	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.4) a declared id inside a §6.1 code span inside a RESOLVED image description is one reported site, the same as in bare prose: §6.4 reduces the description to the plain string content of its inline children, so every row of §3.0b's closed list contributes its text and none of its markup.  §6.1 and §6.5 were the two rows the image-close branch never demoted -- a code span there BLANKED a kind marker and an autolink contributed nothing at all	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.4) a declared id inside a §6.2 emphasis inside a RESOLVED image description is one reported site, the same as in bare prose: §6.4 reduces the description to the plain string content of its inline children, so every row of §3.0b's closed list contributes its text and none of its markup.  §6.1 and §6.5 were the two rows the image-close branch never demoted -- a code span there BLANKED a kind marker and an autolink contributed nothing at all	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.4) a declared id inside a §6.3 link text inside a RESOLVED image description is one reported site, the same as in bare prose: §6.4 reduces the description to the plain string content of its inline children, so every row of §3.0b's closed list contributes its text and none of its markup.  §6.1 and §6.5 were the two rows the image-close branch never demoted -- a code span there BLANKED a kind marker and an autolink contributed nothing at all	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.4) a declared id inside a §6.4 nested image alt inside a RESOLVED image description is one reported site, the same as in bare prose: §6.4 reduces the description to the plain string content of its inline children, so every row of §3.0b's closed list contributes its text and none of its markup.  §6.1 and §6.5 were the two rows the image-close branch never demoted -- a code span there BLANKED a kind marker and an autolink contributed nothing at all	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.4) a declared id inside a §6.5 autolink inside a RESOLVED image description is one reported site, the same as in bare prose: §6.4 reduces the description to the plain string content of its inline children, so every row of §3.0b's closed list contributes its text and none of its markup.  §6.1 and §6.5 were the two rows the image-close branch never demoted -- a code span there BLANKED a kind marker and an autolink contributed nothing at all	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.4/§6.1) a DECORATED id in bare prose (the same reading) reports the one unlicensed id: the id-only exception runs before the demotion, so the delimiters stand and the two ids do not glue -- the precedent `dispose` already sets for the `**` pair, and the reason a demoted code span is routed through `id_only` rather than blanked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.4/§6.1) a DECORATED id inside a resolved image description reports the one unlicensed id: the id-only exception runs before the demotion, so the delimiters stand and the two ids do not glue -- the precedent `dispose` already sets for the `**` pair, and the reason a demoted code span is routed through `id_only` rather than blanked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.4/§6.1) a DECORATED id the `**` twin in bare prose reports the one unlicensed id: the id-only exception runs before the demotion, so the delimiters stand and the two ids do not glue -- the precedent `dispose` already sets for the `**` pair, and the reason a demoted code span is routed through `id_only` rather than blanked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.4/§6.1) a DECORATED id the `**` twin inside an image (the precedent) reports the one unlicensed id: the id-only exception runs before the demotion, so the delimiters stand and the two ids do not glue -- the precedent `dispose` already sets for the `**` pair, and the reason a demoted code span is routed through `id_only` rather than blanked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42 §6.4/§6.1) a kind marker wholly inside a CODE SPAN inside a resolved image description is read: §6.4 reduces the description to the plain string content of its inline children, so the alt text holds the marker and the row's `Deps` edge is asserted.  The reported shape: the span stayed in `lx.code`, the disposition blanked the whole marker, the row read TERMINAL and the run exited 0 -- while the identical marker in a LINK inside the same image exited 1	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42) a DELIBERATE blank id cell whose declaring field claims the row is an umbrella is a contradiction, not a non-row: keyed by nothing it is absent from `ids`, so assertion (b) never reads its `Deps` edge -- reported as a schema miss at rc 2 rather than passed over at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42-10) a LINKED memo whose slice table binds to no schema -- its header reads `No.` where the schema reads `#` -- but carries an umbrella row with a `Deps` edge: the claim is made and the whole table leaves the census.  It exited 0, with no gate reporting it, because the schema-miss gate asks only of `main`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42-8 §6.6) an id inside a raw HTML ATTRIBUTE inside a resolved image description is a reported naming site: the alt text spells the tag out, so `9z owns it` is document text there.  This is what a blank mask cannot give -- it would not join the sides either, and would still hide the id	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42-8 §6.6) the BASELINE that control is measured against -- the identical cell with the span taken out reads the marker and exits 1, so the silence above is the span's and not the fixture's	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R42-9) a blank id cell whose declaring field spells KIND UNDETERMINED is the same contradiction as the marker: §5 gives an undetermined row the no-owner obligation an umbrella has, and keyed by nothing it is absent from `ids`, so assertion (b) never reads its `Deps` edge -- rc 2, not the silent 0 the marker-only predicate gave	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R45 GFM §4.10) a delimiter cell of centred admits the table exactly as `---` does -- "cells whose only content are hyphens (-), and optionally, a leading or trailing colon (:)" sets no minimum length.  Verified against cmark-gfm 0.29.0.gfm.13, which makes a `<table>` from all four; a review finding asking for a three-hyphen minimum is the FP this pins	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R45 GFM §4.10) a delimiter cell of left-aligned admits the table exactly as `---` does -- "cells whose only content are hyphens (-), and optionally, a leading or trailing colon (:)" sets no minimum length.  Verified against cmark-gfm 0.29.0.gfm.13, which makes a `<table>` from all four; a review finding asking for a three-hyphen minimum is the FP this pins	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R45 GFM §4.10) a delimiter cell of one hyphen admits the table exactly as `---` does -- "cells whose only content are hyphens (-), and optionally, a leading or trailing colon (:)" sets no minimum length.  Verified against cmark-gfm 0.29.0.gfm.13, which makes a `<table>` from all four; a review finding asking for a three-hyphen minimum is the FP this pins	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R45 GFM §4.10) a delimiter cell of right-aligned admits the table exactly as `---` does -- "cells whose only content are hyphens (-), and optionally, a leading or trailing colon (:)" sets no minimum length.  Verified against cmark-gfm 0.29.0.gfm.13, which makes a `<table>` from all four; a review finding asking for a three-hyphen minimum is the FP this pins	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R45 scope) a LINKED memo's UNKEYED row is reported: `_unkeyed` asks of the whole population, not of `main`.  Scoped to `main` the sibling's `xxxxWz` id cell declares nothing, the row is unscanned and the run exits 0 -- the I-C class, in the memo the census exists to reach	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R45 scope) a LINKED memo's table WIDTH miss is reported: the table-miss loop asks of the whole population too.  Scoped to `main` the sibling's 7-cell row under a 6-cell header leaves the run at 0 with nothing saying the row was skipped	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-1) a numeric id introduced by a row noun and a plain space is one reported site: the separator composes `plan_memo_ids.DASH`, which is the ONE home of the three spellings, rather than re-spelling a narrower set.  With the ASCII hyphen alone, `Slice — 9 owns the close rule.` matched no appositive, the bare pass deliberately ignores a numeric id, and the ownership claim produced no site at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-1) a numeric id introduced by a row noun and an ASCII hyphen is one reported site: the separator composes `plan_memo_ids.DASH`, which is the ONE home of the three spellings, rather than re-spelling a narrower set.  With the ASCII hyphen alone, `Slice — 9 owns the close rule.` matched no appositive, the bare pass deliberately ignores a numeric id, and the ownership claim produced no site at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-1) a numeric id introduced by a row noun and an EM dash is one reported site: the separator composes `plan_memo_ids.DASH`, which is the ONE home of the three spellings, rather than re-spelling a narrower set.  With the ASCII hyphen alone, `Slice — 9 owns the close rule.` matched no appositive, the bare pass deliberately ignores a numeric id, and the ownership claim produced no site at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-1) a numeric id introduced by a row noun and an EN dash is one reported site: the separator composes `plan_memo_ids.DASH`, which is the ONE home of the three spellings, rather than re-spelling a narrower set.  With the ASCII hyphen alone, `Slice — 9 owns the close rule.` matched no appositive, the bare pass deliberately ignores a numeric id, and the ownership claim produced no site at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-2) a LINKED memo's unbound table whose kind claim STRADDLES a masked span is the same miss as a plain one: `` **UMBRELLA, not a `terminal` unit.** `` is what a reader sees and what the disposed stream does not, so a stream-only search found nothing and the run exited 0 with the table's declarations and `Deps` gone.  Asked through `kind_disagreements`, the CANONICAL question `_kind_residue` already gates a bound row with -- not a second rule	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-4) the UNDETERMINED phrase has its own spelling of the gap and the same defect: under `re.ASCII` its `\\s` is ASCII whitespace, so a U+00A0 between `KIND` and `UNDETERMINED` read as no kind at all.  The scope `(?u:...)` widens the GAP alone and leaves the ASCII case folding, which is why the phrase is still not matched by a long-s spelling	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-4) the marker phrase written with `&#10;` (a line ending, which a reader reads as a gap) is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-4) the marker phrase written with `&#32;` (decodes to U+0020 -- the arm the defect never touched) is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-4) the marker phrase written with `&#9;` (a tab) is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-4) the marker phrase written with `&nbsp;` (U+00A0 -- the spelling a human actually types) is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-4) the marker phrase written with a LITERAL U+00A0 typed into the cell is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-4) the marker phrase written with a plain space (the baseline arm: this one ALWAYS worked) is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-5 scope) EVERY declared id is given a kind, not just the first: the loop over `self.ids` walks the whole map.  Truncated, only one row is classified and the census under-counts the no-owner rows it exists to take	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-5 scope) TWO kind phrases straddling masked spans in ONE declaring field are both reported: `_kind_residue` walks every phrase `kind_disagreements` returns.  Truncated, the row's second undecidable kind is silent	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-5 scope) TWO table misses in one memo are both reported: the loop over a table's misses walks all of them.  Truncated, the second row is unscanned at rc 2 with only the first named -- a reader fixes one and believes the table is clean	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-5 scope) TWO unresolved references are both reported: the loop over a memo's unanswered references walks all of them.  Truncated it reports the first and the second reference's memo is missing from the population with nothing saying so	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-5 scope) a memo's SECOND schema row is read for the unkeyed miss too: the sibling's first row is keyed and its second is not.  Truncated to the first row the unkeyed one declares nothing and leaves the census at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R47-5 scope) a memo's SECOND table is asked for an unbound claim too: the sibling carries a bound table and then an unbound one whose row carries the marker.  Truncated to the first table the claim is silent and the run exits 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R48-2) TWO undetermined spellings in ONE declaring field are both collected: the KIND-SPELLING gate is about the DOCUMENT's spellings, so a field using both reports exactly as two rows using one each do.  Reading only the first match, the set had size one and the run exited 0 on the condition that gate exists for	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R48-2) the discriminating twin -- the SAME two spellings split across two rows -- was always reported, which is why the one-field case looked covered	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R49-1) a BLANK-id row whose declaring field spells a kind ACROSS a masked span is the same contradiction as the clean spelling: the blank-id check asked the disposed stream alone, so `` **UMBRELLA, not a `terminal` unit.** `` on a blank-id row exited 0 while the clean form exited 2.  Asked through the ONE contradiction site now, so a caller cannot forget the arm	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R49-1) the clean twin, which was ALWAYS reported -- so the case above is a claim about the READING and not about the fixture	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R49-2) an umbrella's `Deps` cell whose whole content is a resolved link with a punctuation-only label carries an edge: the reader rendering drops a link's TAIL, so the cell had no alphanumeric character and read as EMPTY -- while the population walker was following that very sibling.  A resolved link is an edge whatever its label renders as	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R49-2) an umbrella's `Deps` cell whose whole content is a resolved link with an EMPTY label carries an edge: the reader rendering drops a link's TAIL, so the cell had no alphanumeric character and read as EMPTY -- while the population walker was following that very sibling.  A resolved link is an edge whatever its label renders as	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R51) a blank-id row whose field straddles a masked span with TWO kind phrases names BOTH: `_claims` marks every disagreement the canonical question returns, not the first.  Truncated, the miss reads one kind and a reader deleting that one believes the row is settled	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R52) a marker OUTSIDE the declaring field, STRADDLING a masked construct: assertion (a)'s outside-field check searched the disposed STREAM alone, so the straddled spelling was invisible and the row left at rc 0 with only a non-gating seed while the clean spelling exited 1.  Asked through the canonical `_claims` now -- which keeps the quoted case by construction, since a phrase quoted whole is not a straddle	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(R52) a marker OUTSIDE the declaring field, spelled cleanly (the twin that was ALWAYS reported): assertion (a)'s outside-field check searched the disposed STREAM alone, so the straddled spelling was invisible and the row left at rc 0 with only a non-gating seed while the clean spelling exited 1.  Asked through the canonical `_claims` now -- which keeps the quoted case by construction, since a phrase quoted whole is not a straddle	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(a) a marker that names ANOTHER row is not a self-declaration	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(a) a row carrying the marker in another cell and NOT in its declaring field is the same finding (the arm that already worked)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(a) a row carrying the marker in its declaring field AND again in another cell is the double marker the assertion forbids: UMBRELLA-MARK 1	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(a) the R20 reviewer's declaring field `Slice `#11-zz-alpha` — **UMBRELLA, …**` attributes the marker to a SLUG row: the row is a pointer and the UMBRELLA-MARK attribution finding is emitted	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(a) the bold slug form `Slice **#11-zz-alpha** — **UMBRELLA, …**` attributes the marker too	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(a) the marker outside the declaring field certifies nothing	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(a) the short form `Slice **9z** — **UMBRELLA, …**` in the same position still attributes (the alternation lost nothing)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(a-seed) a row declaring the kind in words carries no marker	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(a-seed) fires on a cell QUOTING the criterion too -- a measured false positive, kept visible rather than filtered	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(accept-vocab seed) a retirement word inside a code span does not retire the row	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(accept-vocab seed) a terminal row with neither `must` nor `acceptance`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(anchor) `` Slice `C` owns it ``: an id-only code span stands in the stream, so the anchored `Slice C` is still a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(autolink) the same URL WITHOUT the angle brackets is prose (§6.5 Example 611: a bare URL is no autolink), so the id in it IS a site -- the control above has a live subject	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(autolink) the same brackets OUTSIDE an autolink are a link to a memo that is not there: rc 2 -- the control above has a live subject	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(autolink) the same id in a raw HTML SPAN is a seed -- the two dispositions differ, and the control above has a live subject	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(b) a Deps cell `nil` -- a word outside the lexical exceptions -- is NOT empty: the stated polarity is a reported edge (false rc 1), never a silent skip	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(b) a KIND-UNDETERMINED row carrying a Deps edge is checked too	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(b) an umbrella row carrying a Deps edge	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(bare) a decorated side is bounded by its decoration: `` `9z`-`7z` `` is two ids	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(bare) a full stop after an id is a boundary	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(bare) the decoration closes the token even against an id character: `**9z**7z`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(block) `[Slice 9z owns it]:\\n#` is a paragraph and an ATX heading, not a definition	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(block) `[Slice 9z owns it]:\\n***` is a paragraph and a thematic break, not a definition	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(block) `[Slice 9z owns it]:\\n---` is a setext heading (`<h2>…</h2>`), not a definition: the label line is paragraph text and its site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(block) `[Slice 9z owns it]:\\n<div>` is a paragraph and an HTML block (type 6 interrupts a paragraph), not a definition	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(block) `[Slice 9z owns it]:\\n>` is a paragraph and a block quote, not a definition	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(block) `[Slice 9z owns it]:\\n\pa\pb\p\\n\p--\p--\p` -- the table header ends the run (local policy over pure CommonMark, which has no tables): a paragraph and a table, not a definition with the header row as its destination	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(block) `[foo]:\\n- item`: the list item interrupts, so `[foo]:` is a paragraph and a code span opened before the marker does not cross it	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(c-seed) `Lands after Slice `#11-zz-alpha`` in a Slice cell whose Deps cell names only **9z**: the anchored reading admits the slug, so the prose names a party the cell does not carry -- ORDER-PROSE? 1	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(c-seed) a Deps cell `--` is empty by shape	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(c-seed) a Deps cell `N/A` is empty (the lexical exception is case-insensitive)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(c-seed) a Deps cell `n/a` is empty, so ordering prose is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(c-seed) a Deps cell `xxxxC` does not carry the id `C`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(c-seed) a Deps cell `–` (en dash) is empty by shape: no alphanumeric	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(c-seed) a Deps cell naming a FILE whose name holds the id does not carry the id	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(c-seed) a sibling of the SAME basename in another directory, whose Deps cell at the same line names the party, does not discharge the main memo's row	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(c-seed) ordering vocabulary in prose against an empty Deps cell	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(c-seed) prose names a SECOND party the non-empty Deps cell omits	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(cell) a `Deps` cell naming a row is still an edge: the control above has a live subject	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(d) `owned by `#11-zz-alpha` and **Qx**`: a SLUG owner and a short owner are a two-owner clause (`OWNS_TWO` composes `ROW_ID`)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(d) one clause assigning a deliverable to two owners	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(def) `paragraph\\n[x]: child.md\\n[x]`: the orphan names a sibling on disk -- the documented miss, rc 2 (unchanged)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(def) a cell shaped like a definition is inline content and is scanned	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(def) a next line that is NOT a valid title is prose (the definition ends at the destination)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(def) a would-be MULTILINE definition that interrupts a paragraph is an orphan: the shortcut naming it is a schema miss, not an exempt citation-style shortcut	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(fence) a backtick fence whose info string holds a backtick is not a fence	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(fence) four spaces of indent is not a fence	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(file) a link's visible text is not swallowed into the destination token: `[Slice 9z](slice-9z-sib.md)` still reports `9z`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(file) bare `md` (no suffix) beside a declared no-owner id `md` IS a site -- the subject of the two controls above is live	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `- item\\n<span>\\n9z owns it`: the item's paragraph is open (lazy continuation), so type 7 does not open a block: paragraph text, the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `< span title="[x](absent.md)">`: nothing may stand between `<` and the tag name -- literal, the link is read, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<! Slice 9z owns it>`: no ASCII letter after `<!`, no declaration -- the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<! [x](absent.md)>`: a declaration needs an ASCII letter right after `<!` -- literal, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<!-- Slice 9z owns it --` is not closed: prose, the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<!-- [x](absent.md) --` is not closed: no comment, the link is read, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<![cdata[ [x](absent.md) ]]>`: `<![CDATA[` is exact case -- literal, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<![cdata[` is no CDATA opener (§4.6 condition 5 is exact; commonmark.js: a paragraph): the next line is prose and the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `</ span [x](absent.md)>`: a closing tag's name follows `</` directly -- literal, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<3 [x](absent.md)`: a tag name begins with an ASCII letter, so the `<` is literal and the link is read -- the absent memo is the rc-2 miss	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<a b="c"d="[x](absent.md)">`: an attribute needs whitespace before it (Example 622) -- literal, the link is read, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<a b="c"d=9z>x</a>`: no whitespace before `d` (Example 622), no tag -- `9z` is prose	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<a href="x" [x](absent.md)>`: `[x](…)` is no attribute, so the `<` is literal and the link is read, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<div>\\nx\\n</div>\\ny` -- a type-6 block runs to the BLANK line, not to `</div>`: `y` is raw, the paragraph after the blank is prose	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<pre></pre>\\n9z owns it` -- the opener meets the end condition itself, so the block is that one line and the next line is prose	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<pre>\\n`\\n</pre>\\nSlice 9z` owns it`: the backtick inside the raw HTML block does not pair with the prose one -- the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `<span title="`">Slice 9z owns it` x`: the tag is read first, left to right, so the backtick inside it opens no code span -- the site is reported (commonmark.js)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `[<span>x</span>](slice-9z-sib.md)`: a link may wrap a tag -- the sibling is walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `[sib]: slice-9z-sib.md\\n<span>\\n9z owns it`: a definition keeps the paragraph open (§4.7; commonmark.js: `<span>` is paragraph text), so the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `\\<span title="[x](absent.md)">`: an escaped `<` opens no tag (§2.4), the link is read, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `` `x <span title="`9z">y ``: the code span is read first and swallows the `<`, so no tag masks `9z` -- the site is reported (commonmark.js)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) `text\\n<span>\\n9z owns it` -- a type-7 opener cannot interrupt a paragraph (commonmark.js): the lines stay paragraph text and the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(html) the reviewer's input `<pre>\\n`\\n</pre>\\nSlice `9z` owns it` reports the site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(id) a backticked slug with trailing prose declares the slug	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(id) a cell that does not start with an id declares nothing: the row is unkeyed (its Deps edge would go unasserted), so the run is a schema miss	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(id) a citation id declared TWICE, the same spelling, is the duplicate miss: rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(id) an id cell `**?**` is not a blank: decoration does not blank it, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(id) an id cell `?` is not a blank: unkeyed, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(id) an id cell `…` is not a blank: unkeyed, rc 2 (the shape rule would skip it)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(id) an id cell with trailing prose declares the id at its start	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(id) the `Intl`-shaped cell declares the 4-character id at its start	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(id) the id cell's trailing prose is scanned: `**7z** — Slice 9z lands first` reports `9z` (the row's own `7z` is suppressed)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(indented) `   Slice 9z owns it`: three spaces are three columns -- paragraph text, the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(indented) `text\\n    Slice 9z owns it`: an indented line cannot interrupt a paragraph (§4.4; commonmark.js: one paragraph) -- paragraph text, the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(item) `- item\\n\\n    Slice 9z owns it`: the item's second paragraph is prose -- the site is reported (until R15 the line was indented code, seeded and never scanned)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(item) `- item\\n\\n  > q\\n\\n    Slice 9z owns it`: a block quote inside the item, then the item's paragraph -- the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(item) `- item\\n\\n  para\\n\\n    Slice 9z owns it`: the item's THIRD paragraph (its second, at the content indentation, keeps it open) -- the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(item) `1. item\\n\\n     Slice 9z owns it`: an ordered item's content indentation is 3 (`1. `), so five columns are two inside it -- the item's second paragraph, the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(item) `open `x\\n1. 9z owns it` end`: an ordered item starting at 1 interrupts -- the item's paragraph holds one literal backtick and the site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(item) … and that raw line inside the item is seeded (the one raw-line rule reaches into a container)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(item) … and that raw line is seeded under the one raw-line rule, with the §4.4 reading	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) `KIND — UNDETERMINED` still declares it, and the same nonempty `Deps` cell is still `UMBRELLA-CELL`, rc 1 -- the discriminating half: the boundary must not have silenced the phrase it bounds	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) `is a pointer rather than a slicer of work` is no pointer row: it is an active terminal, and a terminal row stating no acceptance condition owes the ACCEPT-VOCAB? seed -- which the unbounded reading suppressed	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) `is an umbrella` in a declaring field without the marker still seeds it -- the discriminating half	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) a declaring field spelling the marker ACROSS a code span is the schema miss, rc 2: a reader reads the marker, the disposed stream does not, and §1 forbids a clean exit for a could-not-scan over the census	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) a declaring field spelling the marker with GFM strikethrough inside it DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) a declaring field spelling the marker with plain DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) a declaring field spelling the marker with §2.4 an escaped comma DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) a declaring field spelling the marker with §2.5 a character reference for the comma DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) a declaring field spelling the marker with §6.2 emphasis inside it (`not a *terminal* unit`) DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) a declaring field spelling the marker with §6.6 a comment inside it DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) a declaring field spelling the marker with §6.6 a tag pair inside it DECLARES the umbrella: the row is in the census, so a prose mention of it is a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) the UNDETERMINED spelling read across emphasis: `KIND — UNDE*TER*MINED` puts the row in the no-owner census, so its mention is a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) the `DECLARES` near-miss backstop is the same reading: `is an *umbrella*` in a declaring field without the marker is the seed	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) the appositive attribution reads the rendered field too: `Slice **9z** — **UMBRELLA, not a *terminal* unit.**` is the UMBRELLA-MARK finding, so the pointer row stays out of the census	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(kind) the marker itself still declares the umbrella and the mention IS a site -- the discriminating half of both controls above	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(lex-seed) `<!-- c\\n\p9z\p\\n-->`: a `\p` line inside a comment block (type 2 ends at `-->`) is seeded	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(lex-seed) `<pre>\\nSlice 9z owns it\\n</pre>`: the inner line holding the id is seeded (type 1 ends at `</pre>`)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(lex-seed) `<span title="Slice 9z owns it">`: an inline span holding a declared id is seeded under the one raw-line rule	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(lex-seed) `para\\n\\n    Slice 9z owns it`: indented code after a plain paragraph is raw under CommonMark too, but holds a declared id -- seeded by the one raw-line rule	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(lex-seed) a 4-space-indented slot row after a table's rows is raw (cmark-gfm: `<pre><code>`) and is SEEDED -- it holds a `\p` -- never a silent skip (I-C)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(lex-seed) a tab-indented slot row after a table's rows is raw (§2.2: four columns) and is SEEDED	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(lex-seed) an HTML-block opener holding a declared id is a seed	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(lex-seed) the raw line holding a declared id inside the type-7 block after a table is seeded	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(lex-seed) … and that seed carries the `inline` reading	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(licence) `9z's memorandum` is not the licensed possession `memo`: the right-hand edge of the forward look, reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(licence) `The grandchild of 9z` is not the licensing phrase `child of`: a licence SUPPRESSES a report, so an unbounded edge there is the dangerous polarity -- the mention is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(licence) a licensing phrase inside a code span licenses nothing	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(link) `[foo\\]][missing]` is a FULL reference (the `]` is escaped): a schema miss, not an exempt shortcut	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(link) a full reference no definition answers is a schema miss	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(link) a shortcut whose only definition sits mid-paragraph is a schema miss	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(link) …and its discriminating half: the UNESCAPED `[x](absent-file.md)` IS a link, so the memo it names is an unavailable sibling -- 1 schema miss, rc 2.  Without this the control above cannot tell a working escape from a needle that never matches	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(quote) `> Slice 9z owns it`: the quote's paragraph is scanned at its real line and the marker is not content -- the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(quote) `> open `x\\n# Slice 9z owns it` there`: an ATX heading is never lazy -- the quote ends, the heading is a block of its own and its site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(rc) a full reference no definition answers is rc 2, never clean	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(rc) a link to an absent memo inside a table CELL is rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(rc) a linked memo that is not on disk is rc 2, never clean	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(rc) a row carrying the marker AND one undetermined spelling, beside another row's other spelling, is KIND-SPELLING rc 1	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(rc) a schema body row whose width differs from its header is rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(rc) a schema with no matching table is rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(rc) that divergence is a mechanical finding: rc 1	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(rc) the same id declared in two memos is rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(rc) the undetermined kind written two ways is KIND-SPELLING, rc 1	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(rc) two MARKED rows spelling the undetermined kind two ways is KIND-SPELLING: the undetermined set is empty and the divergence is still real	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(render) GFM strikethrough `W~~z~~` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(render) `**9z**7z` still names `9z`: a `**` pair whose content is only declared ids is the document DECORATING an id, so the delimiters stand and bound it -- dropping them would leave the single token `9z7z`, which no row declares	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(render) `Slice [W](slice-9z-sib.md)z owns it` names `Wz`: the link's tail renders nothing, and the `.md` file token INSIDE it is dropped with it -- where a drop and a blank overlap the drop wins, or the tail's two sides stay apart	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(render) an id-only code span is still the document SPELLING an id: `` `9z` `` is a naming site, decoration and all	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(render) that same code span IS the `[LEX-SPLIT?]` residue seed: the reader reads `Wz` across a span the disposition blanks, and the seed says so	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(render) §2.5 a character reference `W&#122;` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(render) §6.2 emphasis `W*z*` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(render) §6.2 strong emphasis `W**z**` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(render) §6.3 a link's brackets `W[z](slice-9z-sib.md)` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(render) §6.6 a raw HTML tag pair `W<b>z</b>` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(render) §6.6 an HTML comment `W<!-- c -->z` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(row) `\\\p` is `\p` in the cell, so `9z \\\p 7z` is an id-only run	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(row) `a\\\\\pb` holds an UNESCAPED pipe (§2.4: `\\\\` is a literal backslash): 5 cells under a 4-cell header is a width miss, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(row) a table without leading and trailing pipes declares its rows	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(row) an unescaped `\p` inside backticks SPLITS the row: each half is prose with a literal backtick, so the id on each side is a mention	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(schema) a SHORT id in the citation table keys no citation row (the schema keys cite): rc 2 -- the inverse direction, which would otherwise pollute the keep-set	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(schema) a `#11-` SLUG in the citation table is the same miss -- rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(schema) a citation-shaped id in the §5 SLICE table keys no slice row (the schema keys short + slug): the row is unkeyed, so its cells would go unasserted -- rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(schema) a citation-shaped id in the §8 SLOT table is the same miss -- rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(seed) `\p 9z \p notes.md \p` on a raw line seeds a `\p` AND `9z`: a file run is bounded by whitespace, so the id beside the name is outside every masked span	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(seed) a raw HTML-block line holding a BARE declared id still seeds it -- the discriminating half of the pair above: a fix that stopped reading ids off raw lines at all would pass that control and fail this one	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(setext) a `---` after paragraph text is the heading's underline (§4.3 over §4.1, Example 59) and ends the paragraph like the thematic break it is not	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(setext) a bare `===` IS a paragraph, so `===\\n---` is the heading `<h2>===</h2>` (commonmark.js) and the code span opened in it does not reach the next site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(setext) the reviewer's case: `Heading\\n===\\nSlice `9z` owns it` reports the site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(sibling) `NULX.md` is an ordinary sibling and IS walked -- the discriminating half: a device is the STEM of a component, never a prefix of one, and a guard that rejected every name holding `NUL` would drop a memo the author linked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(slug) `` `tool #11-zz-alpha` `` inside a code span IS the kept slug, bounded by the closing backtick -- 1 site (the exception the control above must not widen)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(span) a `#11-` slug is ATOMIC in an id-only run: `` `#11-zz-alpha / 9z` `` is the document spelling two ids, both reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(span) a backtick behind a backslash is literal and opens no span	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(span) a kept slug inside a command-line code span is a naming site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(span) a paragraph ends at a `>` line	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(span) a paragraph ends at a blank line	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(span) a paragraph ends at a list item: a backtick open in one item and closed in the next is literal	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(span) a paragraph ends at a table row	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(span) a paragraph ends at an ATX heading	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(span) an unmatched backtick string is literal, not a mask to end of line	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(table) `===` right after a schema table is a one-cell body row (§4.3: no paragraph to underline; GFM §4.10 Example 202: a pipe-less line is a row): width miss, rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(table) a reference definition right after a schema table is a ROW of it (GFM Example 202: a pipe-less line after the rows is a row; §4.7: a definition cannot interrupt a block) -- one cell under a 4-cell header, width miss rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(table) the cells within the header's width of a non-schema row are scanned: `9z owns it` in the second of three cells under a two-cell header is a site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(vocab) the ownership connective is read the same way: `own*ed by* **9z** and **Qx**` is the TWO-OWNERS? clause	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(§2.5) a character reference in PROSE renders its character, so an id SPELLED as one (`Slice &#57;z` renders `Slice 9z`) IS a naming site: the reference and the §2.4 escape are the two spellings of one substitution, at the one site the stream builds (design re-gate 4; until then §2.5 was decoded in a link DESTINATION only and this measured 0)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(§6.2) a link WRAPPED in emphasis is still the memo link: emphasis-as-prose costs the population nothing (commonmark.js: `<em><a href="child.md">x</a></em>`)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(§6.2) emphasis is LEXED (design re-gate 4): a declared id inside `*...*` IS a naming site, because the delimiters render no character at all -- the `**9z**` DECORATION a row id may carry is the exception `dispose` makes for it, not this pass's	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(§6.7) a HARD line break inside a link's text does not break the link: the inline pass reads the block's joined text, so a construct may span the lines of its paragraph (§6.8 soft breaks likewise)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(§6.7) a hard line break's backslash renders NOTHING and the break renders a line ending, which bounds every unit the scanners read, so `Slice W\\` + `z` names no `Wz` on the probe table where `Wz` is the umbrella.  ⚠ THIS CONTROL COULD NOT SEE R25-2: until then the backslash STOOD in the stream, and a standing backslash bounds the two sides exactly as the break does -- the wrong reading and the right one agree in the direction that BOUNDS and part only in the direction that JOINS, which is the R25 control below	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	(§6.8) a SOFT line break is the same measurement: `Slice W` then `z` on the next line renders two words and names no `Wz`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	Deps cell of a terminal row names an umbrella	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	a KIND-UNDETERMINED row named as an owner is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	a backticked BARE id is the document spelling an id, not code	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	a backticked run of ids and separators is a Deps-shaped edge, not code	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	a multi-character id inside a bold PHRASE, not bold itself	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	a visible link LABEL is prose and is scanned	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	prose names an umbrella bare, with no row noun	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	prose orders an umbrella	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	trigger cell names another row's umbrella slot as a co-occasion	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE	used-by cell names an umbrella outright	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(R25 sibling) `ax%2Bnotes.md` (`ax+notes.md`) IS a sibling and IS walked -- the discriminating half of the seven above: `+` is an ordinary name character on every platform and outside `_RESERVED_CHARS`, so a fix that refused ASCII punctuation wholesale would pass all seven and fail this one	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(R30 id) an id cell `&#8212;` is a deliberate blank: §2.5 renders the em dash, which the RAW reading could not see -- this cell was a schema miss before R30 and is a non-row after it, the one verdict the fix reverses in the other direction	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(R31-1 §2.4) a header cell spelling `#` as the ESCAPE `\\#` binds too: §2.4 renders a backslash escape as the character itself, and this is the spelling the reviewer did not name -- the fix reads the rendering rather than enumerating the two syntaxes that reach it	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(R33-2) `KIND – UNDETERMINED` with an EN DASH declares the unsettled kind, so the row's nonempty `Deps` edge is asserted.  The reported shape: this one spelling was missing from the phrase's character class while the row grammar beside it accepted it, and the row left the run at rc 0	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(ascii) `次のSlice Cが所有する` reaches the naming worklist: the row-noun anchor is not `\\b` (no Unicode word boundary before `Slice`)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(ascii) `次は#11-zz-alphaが所有する` reaches the naming worklist: the slug anchor is an ASCII class, not `\\w`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(autolink) `<foo.bar.baz>` has neither a scheme nor an `@` (§6.5 Example 610) and is no tag: the `<` is literal and the link after it is read	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(autolink) `<m:abc…>` is a ONE-character scheme and no autolink (§6.5 Example 609, 2-32 characters): the `<` is literal and the link INSIDE the would-be span is read (commonmark.js: `&lt;m:abc<a href="child.md">x</a>&gt;`).  The link sits inside the span so that widening the scheme to one character masks it and the control goes red	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(autolink) a SPACE inside the absolute URI ends it (§6.5 Example 608), so `<https://foo.bar/ …>` is no autolink and no tag either -- the `<` is literal and the link INSIDE the would-be span is read (commonmark.js: `&lt;https://foo.bar/ <a href="child.md">x</a>&gt;`).  The space sits in the URI TAIL, which is the class the grammar excludes it from: a space right after `<` fails at the SCHEME instead, so that shape leaves the tail-widening mutant alive and proves nothing	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(autolink) an autolink inside a LINK's text: the autolink is masked, the link is still the memo link (commonmark.js nests the two `<a>`s)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(b) a sibling umbrella's Deps edge is asserted	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(bare) `9z.é` bounds the id (a dotted number is ASCII on both sides)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(bare) `9z.次の工程` bounds the id: the far side of the `.` is not an ASCII id character, so the site is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(bare) a hyphen bounds a short id: `after 9z-7z` names 9z	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(bare) an id before `!` is bounded	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(bare) an id before `?` is bounded	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(bare) an id inside ASCII double quotes is bounded	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(bare) an id inside curly double quotes is bounded	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(block) `[foo]:\\nslice-9z-sib.md` IS a definition (destination on the next line): the sibling is walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(def) `[sib]: child&#46;md`: a reference definition's destination is decoded by the SAME normalisation -- the sibling is walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(def) a definition is read from RAW lines at a block start: `[sib]: slice`x`.md` keeps its backticks in the destination and the sibling is scanned	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(def) a label spanning FIVE lines is a definition (§4.7 / §6.3: a label may span lines); the later shortcut resolves and the sibling is scanned	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(def) one line ending is allowed before the destination	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(def) the FIRST definition of a label wins	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(id) a citation id declared twice differing only in CASE is the same miss -- `[C1]` and `[c1]` are one §6.3 label, which is how the link resolver already reads them: rc 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(image) `![a ![b](i.png) [c](child.md)][missing]`: the link stands OUTSIDE the inner image's description and the outer image fails -- it is a link, `child.md` is walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(image) `![alt [docs](child.md)][missing]`: the image does NOT resolve, so `![alt` is literal text and the link inside it IS a link (commonmark.js) -- `child.md` is walked and its violation reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(image) `![alt][missing][Slice 9z]` with `[Slice 9z]` defined: the failed image's label is re-scanned and `[missing][Slice 9z]` is a link (§6.3 Example 571; commonmark.js) -- its tail is masked, the sibling is walked: 1 site, not 2	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(item) `- > [sib]: slice-9z-sib.md`: a definition inside a block quote inside an item registers -- the sibling is walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(item) `- [a]: /u` then the lazy schema header over an indented delimiter row: the table is the item's (cmark-gfm, measured) -- the id declared	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(item) `> - [sib]: slice-9z-sib.md`: a definition inside an item inside a block quote registers -- the sibling is walked and its violation reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(item) a slot table inside a list item is a table (cmark-gfm, measured): its row declares its id	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(item) the reviewer's input `- item\\n\\n    [child](child.md)`: the indented line is the item's SECOND paragraph (§5.2, Example 108: the content indentation is 2), not indented code -- the link is found, `child.md` is walked and its violation reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(lex-seed) a raw HTML line `9z-owner`: a hyphen bounds the short id on the raw line as in prose, so the line is seeded holding `9z`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(lex-seed) a raw HTML line `owner-9z`: the hyphen bounds on the left too -- seeded holding `9z`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) `<dest>` may contain an escaped `>`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) `[child](<child&#46;md>)`: the angle-bracket destination decodes by the same rule	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) `[child](child&#46;md)`: a decimal character reference in the destination is decoded (§2.5 / §6.3) -- `child.md` is walked and its violation reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) `[child](child&#x2E;md)`: a hexadecimal character reference decodes	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) `[child](child&period;md)`: a named entity reference (HTML5 `&period;`) decodes	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) `[x](.md)` links the sibling file named `.md`: `sibling_path` stage (d) and the lexer's file token read the ONE `FILE_SUFFIX`, so the suffix-only name is a file on both sides	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) `[x](child&#0;.md)`: U+0000 is replaced by U+FFFD (§2.5, "for security reasons") -- the memo named `child\\ufffd.md` is walked (a raw U+0000 would be rejected as a C0 control)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) `[x](child&#x110000;.md)`: an invalid code point is U+FFFD (§2.5) -- `child\\ufffd.md` is walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) `[x](child&copy.md)`: HTML's legacy semicolon-less `&copy` is NOT a reference in CommonMark (§2.5 Example 29) -- the destination is the literal `child&copy.md`, and THAT file is walked (`html.unescape` would have named `child©.md`)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) `[x](slice&#37;20sib.md)`: §2.5 decodes `&#37;` to `%`, then `sibling_path` percent-decodes `%20` (stage b) -- the two decoders run in spec order, `slice sib.md` is walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) `sub%5Cchild.md`: a backslash is a path separator on every platform (WHATWG URL `#path-state` step 1: for a special scheme -- `file` is one -- `\\` ends a segment as `/` does; `PureWindowsPath` is that syntax) -- the file `sub/child.md` is walked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) a `"…"` title may contain an escaped `"`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) a label holding only a non-breaking space is a label (§6.3: at least one character that is not a space, tab, or line ending)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) a link wrapping a REFERENCE image `[![alt][img]](child.md)`: the image does not deactivate the outer opener, so `child.md` is scanned	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) a link wrapping an IMAGE `[![alt](img.png)](sib.md)` links the sibling; `img.png` is never a memo	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) a non-ASCII space is destination content (only ASCII space and controls end it)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) a percent-encoded destination `slice%20sib.md` links the file `slice sib.md`, as `<slice sib.md>` does	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) a reference link nested in inline brackets: the inner reference is the link, the outer tail is text	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) bare destination with a balanced parenthesis pair	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) bare destination with an escaped parenthesis	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) collapsed reference `[label][]`	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) full reference whose text holds nested brackets; the label is the link's tail, not prose, and so is the definition	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) label matching collapses internal whitespace	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) label matching is a Unicode case FOLD, not lower()	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(link) nested inline links: the INNER link is the link, the outer tail is text -- `child.md` joins the population, absent `parent.md` is not linked	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(noun) `ROW 9 lands first` names the row (a bare `9` is the declared numeric miss)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(noun) `SLICE C owns it` names the row: the row noun folds case (a bare `C` is the declared single-letter miss, so only the anchor can reach it)	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(noun) `UMBRELLA C owns it` names the row	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(population) a terminal id declared in a linked memo is in the keep-set, so `Tq / 9z` is an id-only run, not code	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(population) a violation in a sibling linked ONLY from a cell is reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(population) an umbrella declared in a linked memo is in the census	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(population) the population is transitive: a memo linked from a linked memo is scanned	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(quote) `> [sib]: slice-9z-sib.md`: a definition inside a block quote registers (§5.1 container, Example 218) -- the sibling is walked and its violation reported	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(quote) a slot table inside a block quote is a table: its row declares its id	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(quote) a slot table inside a block quote whose HEADER row is lazy is a table (cmark-gfm: the header is read out of the quote paragraph's last line): its row declares its id	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(quote) the R17 reviewer's shape: `> [a]: /u` then an UNQUOTED schema header over a QUOTED delimiter row is a table in the quote (cmark-gfm: a definition is paragraph text until the paragraph ends, and the header is read out of its last line) -- the row declares its id	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(quote) two definitions then the lazy schema header: still the table's header, the id declared	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(setext) `Heading\\n===` is a heading; the `===` underline ends the paragraph, so a code span opened in the heading does not reach the next paragraph's site	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	(span) a backtick inside a link DESTINATION is consumed by the link, not a code span: `[sib](slice`x`.md)` links the sibling	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	a violation in a carved sibling the memo links	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	novel acceptance idiom: 'signs off'	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	novel ordering idiom: 'downstream of'	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	novel role noun: an umbrella as 'the integrator'	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	novel spelling in a cell: an em-dash hand-off	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	novel verb: an umbrella is 'chartered to deliver'	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	prose names a slot umbrella bare	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	prose names a slot umbrella in bold	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	the reference definition has an angle-bracket destination	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	the sibling is linked by a shortcut reference	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	the sibling is linked by reference	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	the sibling link carries a section fragment	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	the sibling link carries a title	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	the sibling link uses an angle-bracket destination	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
CONTROL	POSITIVE-NOVEL	umbrella id is the LAST token of a link label	plan_memo_selftest_harness.control.<locals>.run#607315692c631059
MUTANT	#1 gate: an unresolved reference is a schema miss (rc 2), not a note	plan_memo_population.py	0274b1d1f0a9ddd7	(link) a full reference no definition answers is a schema miss|(rc) a full reference no definition answers is rc 2, never clean
MUTANT	#11 identity: per-memo maps are keyed on the resolved path, not the basename	plan_memo_memo.py	34a54f3a5c920d21	(c-seed) a sibling of the SAME basename in another directory, whose Deps cell at the same line names the party, does not discharge the main memo's row
MUTANT	#2 gate: an unkeyed schema row is a schema miss (not a note, not a silent drop)	plan_memo_population.py	0043901ef8b642cf	(id) a cell that does not start with an id declares nothing: the row is unkeyed (its Deps edge would go unasserted), so the run is a schema miss
MUTANT	#3 / R14-1 bare id: a hyphen bounds a short id, in prose and on a raw line (re-inject it into the class)	plan_memo_ids.py	2c84edca660101ff	(bare) a hyphen bounds a short id: `after 9z-7z` names 9z|(lex-seed) a raw HTML line `9z-owner`: a hyphen bounds the short id on the raw line as in prose, so the line is seeded holding `9z`|(lex-seed) a raw HTML line `owner-9z`: the hyphen bounds on the left too -- seeded holding `9z`
MUTANT	#3 bare id: a decorated side is bounded by its decoration	plan_memo_ids.py	84c14e5e9369deb4	(bare) the decoration closes the token even against an id character: `**9z**7z`
MUTANT	#3 bare id: a dotted number is one token	plan_memo_ids.py	9b3c9c4b939d6284	(bare) a dotted number is one token: `§6.9z` names no row
MUTANT	#3 bare id: bounded by the complement of the id-continuation class (re-inject a list)	plan_memo_ids.py	76a4a7b615fa5b5b	(bare) an id before `!` is bounded|(bare) an id before `?` is bounded|(bare) an id inside ASCII double quotes is bounded|(bare) an id inside curly double quotes is bounded
MUTANT	#3 file token: a bare `.md` file name is masked before the bare scan (drop the arm: no run ending in the suffix is ever a token)	plan_memo_tokens.py	197d6ef4b6407af7	(bare) a bare `.md` file name holding an id is a file token, not a site
MUTANT	#4 empty cell: a word outside the lexical exceptions is NOT empty	plan_memo_tables.py	14fe62f6fcbdb5df	(b) a Deps cell `nil` -- a word outside the lexical exceptions -- is NOT empty: the stated polarity is a reported edge (false rc 1), never a silent skip
MUTANT	#4 empty cell: emptiness is decided by SHAPE (no alphanumeric), re-injecting the old list	plan_memo_tables.py	44f79ec51538dcb3	(c-seed) a Deps cell `--` is empty by shape|(c-seed) a Deps cell `–` (en dash) is empty by shape: no alphanumeric
MUTANT	#8 stream: every span of the mask is blanked, not only code	plan_memo_stream.py	c3d4769c86942709	(stream) ordering vocabulary in a link TITLE is the link's tail, not prose
MUTANT	#8 stream: the (c) seed reads the Slice cell's disposed stream	plan_memo_roles.py	bf92ceea4d4e14e0	(c-seed) ordering vocabulary inside a code span is code, not prose
MUTANT	#8 stream: the (d) seed reads the disposed stream	plan_memo_roles.py	38b8b06dfcce93c3	(d) an ownership clause inside a code span is code, not a two-owner claim
MUTANT	#8 stream: the acceptance seed and RETIRED read the disposed stream	plan_memo_roles.py	a54756ca30a92351	(accept-vocab seed) a retirement word inside a code span does not retire the row
MUTANT	4.5 id cell: `—` is a literal blank	plan_memo_tables.py	c56fc0a234b299de	(id) an id cell `—` is a literal blank: a deliberate non-row, rc 0
MUTANT	4.5 id cell: blanks are LITERAL, not the shape rule (re-inject `is_empty`)	plan_memo_population.py	6d9ca1326b8b1746	(id) an id cell `**?**` is not a blank: decoration does not blank it, rc 2|(id) an id cell `?` is not a blank: unkeyed, rc 2|(id) an id cell `…` is not a blank: unkeyed, rc 2 (the shape rule would skip it)
MUTANT	4.5 link: a citation-grammar label is exempt in every reference form	plan_memo_memo.py	b16b95c162d33754	(link) a collapsed-shaped citation `[C19][]` is not a reference: rc 0|(link) adjacent citations `[C19][C20]` are not a full reference: rc 0
MUTANT	A x E: kind markers are read from the MASKED declaring field	plan_memo_population.py	2a6434e3ececb175	(span) a quoted kind marker is not a declaration (A x E)
MUTANT	Axis 5 channel: unwrap the FAIL block's print (the other line a reader sees -- the per-control listing and the failure summary are two sites, and a fix to one says nothing about the other)	plan_memo_umbrella_selftest.py	1b2a7d9d9278b940	PROPERTY: every line the run REPORTS goes through the escape -- measured over the EMIT SITES, the subject the escape function's own control cannot reach
MUTANT	Axis 5 channel: unwrap the control runner's per-control line (the exact attack that stayed green before this control existed: the escape function is untouched, only its caller drops it)	plan_memo_umbrella_selftest.py	2d5062d6610ce8cf	PROPERTY: every line the run REPORTS goes through the escape -- measured over the EMIT SITES, the subject the escape function's own control cannot reach
MUTANT	Axis 5 line bound: a source crosses 1000 lines (grow the lexer's docstring by 400 line endings, whatever its current size: the debt the touch-time rule exists to stop)	plan_memo_lexer.py	8fdd38fe3a00b420	PROPERTY: every source of this checker is under the 1000-line touch-time bound (the invariant this PR broke, as a mechanism instead of a sentence)
MUTANT	Axis 5 report channel: drop the C0 arm of the escape (only DEL is escaped: the NUL a control name carries reaches the wire's command substitution and is stripped there)	plan-memo-umbrella-check.py	3e1b57cd9c3a27a2	the runner's report channel escapes every C0 control character and DEL, so a run line a control names with one is still greppable
MUTANT	Axis 5 report channel: escape EVERYTHING (a printable line is mangled too: a green run over an unreadable report is the other direction of the same defect)	plan-memo-umbrella-check.py	c09ed7825a87ed54	the runner's report channel escapes every C0 control character and DEL, so a run line a control names with one is still greppable
MUTANT	C7 kind: the population's pointer kind excludes the row from the acceptance seed	plan_memo_roles.py	b548ae311466b113	(accept-vocab seed) a POINTER row is excluded from the population|(accept-vocab seed) a row whose marker is ATTRIBUTED to another row is a pointer and owes no acceptance condition
MUTANT	C8 disposition: a kept slug inside a code span is visible	plan_memo_stream.py	d379a32e9acaa559	(span) a kept slug inside a command-line code span is a naming site
MUTANT	F1 id cell: a cell not starting with an id declares nothing (no fallback to the cell)	plan_memo_tables.py	5fae87fc7f445c8f	(id) a cell that does not start with an id declares nothing: the row is unkeyed (its Deps edge would go unasserted), so the run is a schema miss
MUTANT	F1 id cell: the id is followed by a non-id character, not the cell end	plan_memo_tables.py	50f37af0d9d88546	(id) a backticked slug with trailing prose declares the slug|(id) an id cell with trailing prose declares the id at its start
MUTANT	F12 link: the link tail masks a slug	plan_memo_stream.py	549dfb47b5837949	(link) a `#11-` slug in a link DESTINATION is not a naming site
MUTANT	F13 link: a shortcut with an orphan definition is reported	plan_memo_lexer.py	f29c656911b01550	(link) a shortcut whose only definition sits mid-paragraph is a schema miss
MUTANT	F13 link: an unanswered full reference is reported	plan_memo_lexer.py	715e9e49c6ef07ea	(link) a full reference no definition answers is a schema miss
MUTANT	F2 population: links in CELLS join the population	plan_memo_memo.py	72e4f116b27a4579	(population) a violation in a sibling linked ONLY from a cell is reported|(rc) a link to an absent memo inside a table CELL is rc 2
MUTANT	F4 attribution: the FIRST marker occurrence decides	plan_memo_tables.py	92dba2e448827bf1	(a) a self-declaring field that later says a sibling 'is not it' stays self-declaring
MUTANT	F5 kind: the undetermined spelling is collected beside the marker	plan_memo_population.py	2d3c6b84c87821b1	(rc) a row carrying the marker AND one undetermined spelling, beside another row's other spelling, is KIND-SPELLING rc 1
MUTANT	F6 (c): the Deps cell's ids are the population's mentions, not a raw tokenisation	plan_memo_roles.py	b820d27c67ab5fdc	(c-seed) a Deps cell `xxxxC` does not carry the id `C`|(c-seed) a Deps cell naming a FILE whose name holds the id does not carry the id
MUTANT	F7 check: an empty no-owner census is clean, not a schema miss	plan-memo-umbrella-check.py	f247f690e1f1aefe	(rc) a memo whose every row is terminal is rc 0, not a schema miss|(rc) an all-terminal memo reports a zero census
MUTANT	F8 / #4 empty cell: `n/a` is a lexical exception (case-insensitive)	plan_memo_tables.py	b1c674e2982d90e1	(c-seed) a Deps cell `N/A` is empty (the lexical exception is case-insensitive)|(c-seed) a Deps cell `n/a` is empty, so ordering prose is reported
MUTANT	F9 span: an escaped backtick opens no span	plan_memo_lexer.py	657a6e8a4462f95d	(span) a backtick behind a backslash is literal and opens no span
MUTANT	R1-1 row: only an ODD backslash run escapes `\p` (even run = literal backslash + pipe)	plan_memo_blocks.py	5fae8a8e252f7087	(row) `a\\\\\pb` holds an UNESCAPED pipe (§2.4: `\\\\` is a literal backslash): 5 cells under a 4-cell header is a width miss, rc 2
MUTANT	R1-1 row: the trailing-pipe check uses the same parity	plan_memo_blocks.py	abaaac5224671568	(row) a trailing `\\\\\p` is a literal backslash then the trailing pipe: rc 0
MUTANT	R1-2 runner: the emptiness guard fires on zero controls / zero mutants	plan_memo_selftest_controls.py	ecc6fb44c9078cf5	an empty control or mutant registry is a FAIL, never green
MUTANT	R1-3 link: a link deactivates every `[` opener before it (links may not contain links)	plan_memo_lexer.py	e9fbeada54a01918	(link) a reference link nested in inline brackets: the inner reference is the link, the outer tail is text|(link) nested inline links: the INNER link is the link, the outer tail is text -- `child.md` joins the population, absent `parent.md` is not linked
MUTANT	R1-4 def: an orphan candidate is parsed with its continuation line, not per line	plan_memo_memo.py	e21b39da5b3dbc56	(def) a would-be MULTILINE definition that interrupts a paragraph is an orphan: the shortcut naming it is a schema miss, not an exempt citation-style shortcut
MUTANT	R10-1 raw: HTML-block lines are raw extents (re-inject them into the paragraph)	plan_memo_blocks.py	43c401b16d8fc84e	(html) `<pre>\\n`\\n</pre>\\nSlice 9z` owns it`: the backtick inside the raw HTML block does not pair with the prose one -- the site is reported|(lex-seed) an HTML-block opener holding a declared id is a seed
MUTANT	R10-1 raw: a type-7 opener counts only where no paragraph is open (make it interrupt)	plan_memo_blocks.py	5ce182f77cef639a	(html) `text\\n<span>\\n9z owns it` -- a type-7 opener cannot interrupt a paragraph (commonmark.js): the lines stay paragraph text and the site is reported
MUTANT	R10-1 raw: the end condition on the opener line closes the block there	plan_memo_blocks.py	47d07c7eea264758	(html) `<pre></pre>\\n9z owns it` -- the opener meets the end condition itself, so the block is that one line and the next line is prose
MUTANT	R10-2 blocks: the id cell is scanned with its own id suppressed (re-inject the skip)	plan-memo-umbrella-check.py	c32a09f3a12ea8e9	(id) the id cell's trailing prose is scanned: `**7z** — Slice 9z lands first` reports `9z` (the row's own `7z` is suppressed)
MUTANT	R10-3 file: a bare `.md` name is a path-syntax run (re-inject a narrow class: `+`, `@` and `(` bound the run, so a path character becomes a boundary)	plan_memo_tokens.py	588a43a2d002240e	(file) `(9z).md` is one file name (a balanced parenthesis pair): no site|(file) `9z+notes.md` is one file name: no site|(file) `9z@notes.md` is one file name: no site
MUTANT	R11-1 driver: block start from the block STATE (re-inject the look-back at the previous raw line: blank / one-line block before it)	plan_memo_memo.py	c7def12fdd9518cb	(html) `Heading\\n===\\n<span>\\n9z owns it`: after a setext heading no paragraph is open (commonmark.js: a heading and a raw block) -- the look-back at the previous line missed this: no site|(table) the prose inside the type-7 block after a schema table is raw, not a site
MUTANT	R11-1 raw: a type-7 opener opens where no paragraph is open (re-inject 'never at a block start')	plan_memo_blocks.py	f0f72df6e06ed9f4	(html) `# h\\n<span>\\n9z owns it`: after a one-line block no paragraph is open, so the type-7 opener is a block start (commonmark.js: a heading and a raw block): no site|(table) a type-7 HTML opener right after a schema table ENDS it (a table is not a paragraph): `<span>` + prose are raw lines, not one-cell rows -- no width miss, rc 0
MUTANT	R11-1 table: no paragraph is open inside a table (re-inject the paragraph's interruption rule)	plan_memo_tables.py	a670893fd99ffd4b	(table) a type-7 HTML opener right after a schema table ENDS it (a table is not a paragraph): `<span>` + prose are raw lines, not one-cell rows -- no width miss, rc 0
MUTANT	R11-2 link: the re-scanned label of a failed full reference is the same site (re-inject the second record)	plan_memo_lexer.py	47bd9ac18ce2e0d4	(def) a definition cannot interrupt a paragraph: the reference is unanswered, and reported ONCE (`[text][label]` re-scans `[label]`)|(image) the reviewer's input `![alt][missing]` + an orphan `[missing]: image.md` in the same paragraph: the re-scanned `[missing]` is the same failed site, not a shortcut for the orphan rule -- rc 0, `image.md` never walked
MUTANT	R11-2 link: the tail of a failed reference is NOT consumed (re-inject the advance to `end`)	plan_memo_lexer.py	f9ba1c8e541dc271	(image) `![alt][missing][Slice 9z]` with `[Slice 9z]` defined: the failed image's label is re-scanned and `[missing][Slice 9z]` is a link (§6.3 Example 571; commonmark.js) -- its tail is masked, the sibling is walked: 1 site, not 2
MUTANT	R12-A setext: an underline is a boundary only where a paragraph is open (re-inject the unconditional arm)	plan_memo_blocks.py	b1098ff021bc3d23	(table) `===` right after a schema table is a one-cell body row (§4.3: no paragraph to underline; GFM §4.10 Example 202: a pipe-less line is a row): width miss, rc 2
MUTANT	R12-B tabs: a block start allows at most three columns through the one measure (re-inject 'any number of spaces' in `unindented`, which every block start reads)	plan_memo_blocks.py	625cc6bd43e9c943	CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html
MUTANT	R12-B tabs: indentation is measured in columns, a tab to the next multiple of 4 (re-inject the four-space literal)	plan_memo_blocks.py	bc7e92c2775bb943	(indented) ` \\tSlice 9z owns it`: a space then a tab is four columns (§2.2) -- raw, no site|(indented) `\\tSlice 9z owns it`: a tab is four columns (§2.2), so the line is indented code at a block start -- a raw extent, no site|CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html
MUTANT	R12-C anchors: the anchored pass reads the disposed stream (re-inject the raw text, for the anchor and the token map alike)	plan-memo-umbrella-check.py	ee44912ae0361298	(anchor) `` `Slice `C owns it ``: the row noun is inside a code span, so on the disposed stream there is no `Slice C` to anchor on -- 0 sites (the bare `C` is a declared miss)
MUTANT	R12-D witness: Phase 1's `link_label` calls are counted where Phase 1 makes them (re-bind the counter to the lexer's binding, which sees only Phase 2)	plan_memo_selftest_pipeline.py	1fa4fba06fa92ce8	Phase-1 orphan detection is linear: <= 4 link_label calls per line
MUTANT	R12-E conformance: the type-6 tag list is the spec's (drop `div`; `search`, the brief's example, has no spec example to exercise it)	plan_memo_blocks.py	0d31f1a94a50f88f	CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html
MUTANT	R13 GFM §4.10: the excess cells of a NON-schema row are ignored before lexing (re-inject them)	plan_memo_tables.py	995dc35cfc98e2ad	(rc) an excess body cell of a non-schema table holding `[x](absent-file.md)` is ignored (GFM §4.10): no link, nothing walked, rc 0|(table) an excess body cell of a non-schema table is ignored (GFM §4.10): its `9z owns it` is never lexed -- no site
MUTANT	R13 §4.4: a blank line inside the chunk stays in it when an indented line follows (re-inject 'a blank line ends it')	plan_memo_blocks.py	c78b673c8e0dde11	CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R13 §4.4: an indented line at a block start opens a raw extent (drop the opener arm)	plan_memo_blocks.py	1c62bac5761e17bf	(indented) `    Slice 9z owns it` at a block start is a raw extent (§4.4), like a fence: no site|(table) an indented line right after a schema table opens an indented code block (a block-level structure; no paragraph is open), not a one-cell row: rc 0|CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html
MUTANT	R13 §4.4: the trailing blank lines are not part of the block (re-inject them)	plan_memo_blocks.py	6cb0ceca19e599ef	Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R13 §4.6: condition 1's tags are case-insensitive (drop the fold: `<PRE>` is type 7, which cannot interrupt)	plan_memo_blocks.py	0ddda38f95a15fd3	(html) `text\\n<PRE>\\n9z owns it\\n</pre>`: `<PRE>` opens a type-1 block (§4.6 condition 1 is case-insensitive), which interrupts the paragraph -- raw to `</pre>`, no site
MUTANT	R13 §4.6: condition 4 is `<!` + an ASCII letter of EITHER case (re-inject upper only)	plan_memo_blocks.py	7eb247e62a42e0f4	(html) `<!doctype` opens a type-4 block (§4.6 condition 4: `<!` + an ASCII letter, either case) to the `>` line: raw, no site
MUTANT	R13 §4.6: condition 5 `<![CDATA[` is exact (re-inject case folding)	plan_memo_blocks.py	f066f138c07a490e	(html) `<![cdata[` is no CDATA opener (§4.6 condition 5 is exact; commonmark.js: a paragraph): the next line is prose and the site is reported
MUTANT	R13 §4.6: condition 6's tag list is case-insensitive (drop the fold: `<DIV>` is type 7, which cannot interrupt)	plan_memo_blocks.py	44d3b3161fe76b4d	(html) `text\\n<DIV>\\n9z owns it`: `<DIV>` opens a type-6 block (§4.6 condition 6 is case-insensitive), which interrupts the paragraph -- raw to the blank line, no site
MUTANT	R13 §5.1 / RG3 MIN-9: a lazy DELIMITER row opens no table (re-inject it)	plan_memo_blocks.py	e376d958401c305e	(quote) a slot table inside a block quote whose DELIMITER row is lazy is one paragraph (cmark-gfm: the delimiter arrives in an unmatched container): no id declared|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R13 §5.1: a `>` line opens the container (drop the branch: the marker line heads a paragraph)	plan_memo_memo.py	b27dd727b9a4dbe0	(quote) `> [sib]: slice-9z-sib.md`: a definition inside a block quote registers (§5.1 container, Example 218) -- the sibling is walked and its violation reported|(quote) a slot table inside a block quote is a table: its row declares its id|CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html
MUTANT	R13 §5.1: a lazy candidate ends a fence inside the quote (re-inject 'the fence runs on')	plan_memo_blocks.py	150e1d7ab9823757	Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R13 §5.1: a lazy candidate is a boundary wherever no paragraph is open (drop the arm: a table's or a raw extent's next line)	plan_memo_blocks.py	8a7ff3c17923e065	Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R13 §5.1: a lazy candidate where no paragraph is open ends the quote (drop the stop: it is parsed inside; Example 237 is the spec's instance)	plan_memo_memo.py	d527ba3c2f14d9c3	CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R13 §5.1: a quote's lazy candidates are gathered once, up to the first boundary (drop the bound: every quote re-scans the rest of the document, quadratic -- the result is the same, the cost is not)	plan_memo_memo.py	6c3c359fafc9658a	block quotes are linear: N quotes cost <= 4N quote_content calls
MUTANT	R13 §5.1: a setext underline is never a lazy continuation line -- but a lazy `===` is paragraph text, Example 93 (re-inject the underline on a lazy line)	plan_memo_blocks.py	de9d978f55c2b276	(quote) `> Heading `open\\n===\\nSlice 9z owns it` here`: a lazy `===` is the quote paragraph's text, not an underline (§5.1, Example 93) -- one paragraph, the span masks the site|(setext) `==` after a list item is NOT an underline (§4.3 Examples 92-94): the item's paragraph continues and a code span crosses it|CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html
MUTANT	R13 §5.1: lazy continuation (drop it: a marker-less line never joins the quote)	plan_memo_memo.py	a4a523a1627a71ff	(quote) `> open `here\\nSlice 9z owns it` there`: the marker-less line is lazy continuation text of the quote's paragraph (§5.1), so the span crosses it: no site|CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R13 §5.1: the marker's tab is one column of marker space and the rest content indentation (re-inject one column per tab in the content's leading whitespace)	plan_memo_blocks.py	f13e745dd150b93f	CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R14 property: a second spelling of an id class outside the grammar module is red (re-inject one in the row-noun anchor, behaviour unchanged)	plan_memo_roles.py	15dec8b05d2820c6	PROPERTY: the id character classes are spelled once, in plan_memo_ids.py (a source-text sweep)
MUTANT	R14-1 seed: the raw-line seed reads the grammar (re-inject the seed's own boundary regex -- the former `_BARE_TOKEN`, a second spelling that rejected a hyphen on either side)	plan-memo-umbrella-check.py	b62a5b2569a7da32	(lex-seed) a raw HTML line `9z-owner`: a hyphen bounds the short id on the raw line as in prose, so the line is seeded holding `9z`|PROPERTY: the id character classes are spelled once, in plan_memo_ids.py (a source-text sweep)
MUTANT	R14-2 slug: bounded on its RIGHT side too (re-inject the left-only boundary)	plan_memo_ids.py	26395a78426b0e35	(slug) `#11-zz-alphaZZ` in prose is not the id `#11-zz-alpha`: the slug is bounded on its right as on its left -- 0 sites|(slug) `` `tool #11-zz-alpha_extra` ``: the kept-slug exception inside a code span reads the grammar's boundary, so nothing is excepted and the span stays code -- 0 sites
MUTANT	R14-3 def: the orphan exemption is by the orphan's exact bracket (re-inject the line-number test)	plan_memo_memo.py	f01189bd0b7b2cf7	an orphan definition exempts its OWN bracket only: `[sib]: child.md "[sib]"` is the documented miss, rc 2, child.md not walked
MUTANT	R14-4 cite: the citation label is either case, ONE predicate for the mask and the exemption (re-inject upper-case only)	plan_memo_ids.py	c9bdb952eae32e5e	(cite) `[c1]` beside a declared no-owner id `c1`: a lowercase citation is masked by the same predicate the reference walk exempts it by -- 0 sites|(cite) adjacent lowercase citations `[c1][c2]` are not a full reference (the exemption is the grammar's either-case predicate): rc 0
MUTANT	R15 #2 def: an orphan's destination decides the miss (re-inject the drop: every orphan blocks the shortcut)	plan_memo_memo.py	12e11ddeebfae8e3	(def) `paragraph\\n[x]: #section\\n[x]`: the orphan names a section, never a memo -- the shortcut is prose, rc 0|(def) `paragraph\\n[x]: https://example.com/a\\n[x]`: the orphan names an external URL, never a memo -- rc 0
MUTANT	R15 #3 noun: the row noun folds ASCII case in ONE place (re-inject the Title/lower enumeration)	plan_memo_tables.py	f24a4dd22d04e36a	(noun) `ROW 9 lands first` names the row (a bare `9` is the declared numeric miss)|(noun) `SLICE C owns it` names the row: the row noun folds case (a bare `C` is the declared single-letter miss, so only the anchor can reach it)|(noun) `UMBRELLA C owns it` names the row
MUTANT	R15 §4.1: a thematic break takes precedence over a list marker (drop it: `* * *` is an item)	plan_memo_blocks.py	4247e28e54405fe8	CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R15 §5.2: a list item is a container (re-inject the flat reading: the marker line heads a paragraph, its content indentation tracked by nothing)	plan_memo_memo.py	18bc8492af1b822c	(item) `> - [sib]: slice-9z-sib.md`: a definition inside an item inside a block quote registers -- the sibling is walked and its violation reported|(item) a slot table inside a list item is a table (cmark-gfm, measured): its row declares its id|(item) the reviewer's input `- item\\n\\n    [child](child.md)`: the indented line is the item's SECOND paragraph (§5.2, Example 108: the content indentation is 2), not indented code -- the link is found, `child.md` is walked and its violation reported|CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R15 §5.2: an item can begin with at most one blank line (drop the rule: Example 280's `foo` joins the empty item)	plan_memo_memo.py	2277a0b78c30ca29	CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R15 §5.2: an ordered item interrupts only when it starts at 1 (re-inject 'any number')	plan_memo_blocks.py	52a1889c0189e690	(item) `open `x\\n2. 9z owns it` end`: an ordered item not starting at 1 cannot interrupt a paragraph (§5.2) -- one paragraph, the span masks the site|CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html
MUTANT	R15 §5.2: lazy continuation (drop it: a line short of the content indentation never joins the item; 20 columns is the item gather's line)	plan_memo_memo.py	8afded92bab28157	(item) `- open `x\\nSlice 9z owns it` end`: the line short of the content indentation is the item paragraph's lazy continuation text (§5.2 rule 5), so the span crosses it: no site|CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html
MUTANT	R15 §5.2: the content indentation is W + N in LINE columns -- a tab past it leaves its remaining columns (re-inject 'a tab is one column' in the strip)	plan_memo_blocks.py	4e334717698bc842	Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R15 §5.2: the content indentation is stripped from every continuation line (drop the strip: the item's second paragraph is indented code again)	plan_memo_memo.py	da68b2351f3e672b	(item) `1. item\\n\\n     Slice 9z owns it`: an ordered item's content indentation is 3 (`1. `), so five columns are two inside it -- the item's second paragraph, the site is reported|(item) the reviewer's input `- item\\n\\n    [child](child.md)`: the indented line is the item's SECOND paragraph (§5.2, Example 108: the content indentation is 2), not indented code -- the link is found, `child.md` is walked and its violation reported|CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html
MUTANT	R15 §5.2: the enclosing container's lazy candidate stays lazy and whole inside the item (re-inject 'strip it when indented enough')	plan_memo_memo.py	ae4d9f8b62150a09	Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R15 §5.2: the interruption rule -- an EMPTY item cannot interrupt a paragraph, nor an ordered item not starting at 1 (re-inject 'any marker line interrupts', the pre-R15 local policy)	plan_memo_blocks.py	199db042d7331962	(item) `open `x\\n*\\n9z owns it` end`: an EMPTY item cannot interrupt a paragraph (§5.2; `*`, not `-`, which would be a setext underline) -- one paragraph, the span masks the site|(item) `open `x\\n2. 9z owns it` end`: an ordered item not starting at 1 cannot interrupt a paragraph (§5.2) -- one paragraph, the span masks the site|CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html
MUTANT	R15 §5.3: a gap opens only after a block at its level (re-inject 'any blank line': an item's blank first line would loosen its list)	plan_memo_memo.py	532523d6fd892f46	CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R15 §5.3: a list is loose when a non-final item ends with a blank line (drop the arm)	plan_memo_memo.py	48ab2208ef3dbed2	CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R15 §5.3: a list is loose when an item opens a block across a gap (drop the arm)	plan_memo_memo.py	6a495fa30721991f	CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R15 §5.3: sibling items form one list only when of the same type (re-inject 'any marker continues the list')	plan_memo_blocks.py	ae75f4981ee9fab7	CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	R16 #2 §2.5: U+0000 is replaced by U+FFFD (drop the rule: `&#0;` yields a C0 control the resolver rejects)	plan_memo_links.py	0c4ef33269ebfad8	(link) `[x](child&#0;.md)`: U+0000 is replaced by U+FFFD (§2.5, "for security reasons") -- the memo named `child\\ufffd.md` is walked (a raw U+0000 would be rejected as a C0 control)
MUTANT	R16 #2 §2.5: a reference needs its `;` (re-inject an optional semicolon)	plan_memo_links.py	f45a04dfbd569bbd	(link) `[x](child&#46md)`: `&#46` without `;` is no reference -- the destination is the literal `child&#46md`, which names no memo: 0 sites, `child.md` not walked
MUTANT	R16 #2 §2.5: character references in a destination are decoded (re-inject backslash-only unescaping: no reference is ever matched)	plan_memo_links.py	576dc721761425c6	(def) `[sib]: child&#46;md`: a reference definition's destination is decoded by the SAME normalisation -- the sibling is walked|(link) `[child](<child&#46;md>)`: the angle-bracket destination decodes by the same rule|(link) `[child](child&#46;md)`: a decimal character reference in the destination is decoded (§2.5 / §6.3) -- `child.md` is walked and its violation reported|(link) `[child](child&#x2E;md)`: a hexadecimal character reference decodes|(link) `[child](child&period;md)`: a named entity reference (HTML5 `&period;`) decodes|(link) `[x](slice&#37;20sib.md)`: §2.5 decodes `&#37;` to `%`, then `sibling_path` percent-decodes `%20` (stage b) -- the two decoders run in spec order, `slice sib.md` is walked
MUTANT	R16 #2 §2.5: the decoder is ONE pass of the spec's grammar, not `html.unescape` (re-inject it as a second pass: HTML's legacy semicolon-less `&copy`, and a decoded `&` / an escaped `&` re-read as a reference)	plan_memo_links.py	35a2dbb9db83f24d	(link) `[x](child&#x26;#46;md)`: a decoded `&` is a character, never the start of a second reference (one pass, no re-scan) -- literal `child&#46;md`, 0 sites|(link) `[x](child&copy.md)`: HTML's legacy semicolon-less `&copy` is NOT a reference in CommonMark (§2.5 Example 29) -- the destination is the literal `child&copy.md`, and THAT file is walked (`html.unescape` would have named `child©.md`)|(link) `[x](child\\&#46;md)`: a backslash-escaped `&` opens no reference (ONE pass: §2.4 and §2.5 meet at the character once) -- literal `child&#46;md`, no memo, 0 sites
MUTANT	R16 #2 §6.3: label matching is on the RAW label (re-inject character-reference decoding in `normalize_label`)	plan_memo_links.py	6b2ac19319f15c0d	(label) `[foo&auml;]: child.md` then `[fooä]`: §6.3 label matching is on the RAW label (case fold, strip, collapse -- no character-reference decoding; commonmark.js: no link) -- the shortcut is prose, 0 sites, `child.md` not walked
MUTANT	R16 #3 chokepoint: only I/O is the unavailable-memo miss (re-inject `RuntimeError` in the chokepoint's except: a parser exception becomes rc 2)	plan_memo_population.py	4839479820cb96cb	a RuntimeError raised while PARSING a memo is a crash out of check(), never the unavailable-memo miss
MUTANT	R16 #3 nesting: the frame stack has no depth cap (re-inject one of 200 -- a `RecursionError` at the 201st nested container, as the call stack gave at ~500)	plan_memo_memo.py	6f99be41ea177110	container nesting is off the call stack: 1,000 nested quotes / items parse as commonmark.js nests them
MUTANT	R17 #1 lazy header: a definition keeps the run open (re-inject the paragraph-only rule -- `cur` alone -- so the quote ends before the header is asked)	plan_memo_memo.py	503025503b44ce69	(quote) the R17 reviewer's shape: `> [a]: /u` then an UNQUOTED schema header over a QUOTED delimiter row is a table in the quote (cmark-gfm: a definition is paragraph text until the paragraph ends, and the header is read out of its last line) -- the row declares its id|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js|a lazy schema header after a definition in a linked memo's quote is a table: id declared, kind umbrella, census +1
MUTANT	R17 #2 disposition: the raw HTML span is masked (drop the `html` kind from the disposition)	plan_memo_stream.py	b077e9e323fac8d2	(html) `<span title="Slice 9z owns it">x</span>`: an id inside an attribute value is no naming site -- the span is masked whole (kind `html`), as a raw HTML-block line is raw|(html) a cell's `<span title="Slice 9z owns it">` is masked by the same inline pass: no site
MUTANT	R17 #2 seed: an inline raw HTML span is recorded for the LEX-UNSUPPORTED? seed (drop the record)	plan_memo_memo.py	b6194718fd4ca9ea	(lex-seed) `<span title="Slice 9z owns it">`: an inline span holding a declared id is seeded under the one raw-line rule|(lex-seed) … and that seed carries the `inline` reading
MUTANT	R17 #2 §6.6: a CDATA section is a raw span (drop the arm)	plan_memo_html.py	3b39760b3c6ee660	(html) `<![CDATA[ Slice 9z owns it ]]>`: a CDATA section is masked|(html) a CDATA section `<![CDATA[ [x](absent.md) ]]>` is raw: rc 0|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R17 #2 §6.6: a declaration needs an ASCII letter after `<!` (re-inject `<!` + anything)	plan_memo_html.py	39b27523a68ee480	(html) `<! Slice 9z owns it>`: no ASCII letter after `<!`, no declaration -- the site is reported|(html) `<! [x](absent.md)>`: a declaration needs an ASCII letter right after `<!` -- literal, rc 2
MUTANT	R17 #2 §6.6: a processing instruction is a raw span (drop the arm)	plan_memo_html.py	122b4817064109dd	(html) `<? Slice 9z owns it ?>`: a processing instruction is masked|(html) a processing instruction `<? [x](absent.md) ?>` is raw: rc 0|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R17 #2 §6.6: an HTML comment is a raw span (drop the comment arm)	plan_memo_html.py	ca30f836d7a24396	(html) `<!-- Slice 9z -- owns it -->`: `--` inside a comment is allowed since 0.31 (§6.6: "a string of characters not including the string -->") -- the whole comment is masked|(html) an HTML comment `<!-- [x](absent.md) -->` is raw: rc 0|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R17 #2 §6.6: an attribute is preceded by at least one space, tab or line ending (re-inject optional whitespace: Example 622's `<a href='bar'title=title>` becomes a tag)	plan_memo_html.py	aa4067dd7c0dc534	(html) `<a b="c"d="[x](absent.md)">`: an attribute needs whitespace before it (Example 622) -- literal, the link is read, rc 2|(html) `<a b="c"d=9z>x</a>`: no whitespace before `d` (Example 622), no tag -- `9z` is prose|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R17 #2 §6.6: an attribute value may be single- or double-quoted (drop the quoted arms: unquoted only)	plan_memo_html.py	f151424d77b6aaad	(html) `<span title="Slice 9z owns it">x</span>`: an id inside an attribute value is no naming site -- the span is masked whole (kind `html`), as a raw HTML-block line is raw|(html) a single-quoted attribute value `<span title='[x](absent.md)'>` is a tag: rc 0|(html) the R17 reviewer's shape `<span title="[child](absent.md)">text</span>`: a bracket inside a double-quoted attribute value is no link -- nothing is walked, rc 0|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R17 #2 §6.6: raw HTML is a span of the one inline pass (drop the `<` arm: a tag is text and its brackets are delimiters)	plan_memo_lexer.py	6a53bc58406572b2	(html) `<span title="Slice 9z owns it">x</span>`: an id inside an attribute value is no naming site -- the span is masked whole (kind `html`), as a raw HTML-block line is raw|(html) the R17 reviewer's shape `<span title="[child](absent.md)">text</span>`: a bracket inside a double-quoted attribute value is no link -- nothing is walked, rc 0|(lex-seed) `<span title="Slice 9z owns it">`: an inline span holding a declared id is seeded under the one raw-line rule|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R17 #2 §6.6: the 0.31 comment grammar admits `--` inside (re-inject 0.30's exclusion)	plan_memo_html.py	a2b5a4afdf04f93c	(html) `<!-- Slice 9z -- owns it -->`: `--` inside a comment is allowed since 0.31 (§6.6: "a string of characters not including the string -->") -- the whole comment is masked|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R19 #1 §6.4: a failed reference inside a resolved image's description names no lost memo (drop the rule)	plan_memo_lexer.py	a239a35db1c0e1de	(image) `![alt [x][missing]](img.png)`: a failed reference inside a resolved image's description names no lost memo (resolved, it would have been demoted; commonmark.js: `alt="alt [x][missing]"`) -- rc 0
MUTANT	R19 #1 §6.4: a resolved image's description is plain text -- a link recorded inside it is demoted (re-inject the immediate add: the link stays a link)	plan_memo_lexer.py	a41c648454275378	(image) `![a ![b [c](child.md)](i.png)][missing]`: the OUTER image fails but the INNER one resolves, and the link inside the inner description is demoted (commonmark.js: `<img alt="b c">`) -- `child.md` is not walked|(image) `![alt [docs](child.md)](absent-image.md)`: neither the demoted link nor the image is a memo link -- `child.md` is not walked (its violation is unreported) and the image's `.md` destination is not probed|(image) a nested image in a resolved image `![a ![b [c](child.md)](i.png)](j.png)`: the inner image stays masked and the link inside it is demoted -- `child.md` is not walked|(image) the R19 reviewer's input `![alt [docs](absent-file.md)](image.png)`: the image resolves, so its description is plain text (§6.4; commonmark.js: `<img alt="alt docs">`) -- the link inside it is no memo link, nothing is walked, rc 0|(link) `[a ![b [c](child.md)](i.png)](parent.md)`: the inner link deactivated the outer `[` as it closed, before the image demoted it (§6.3 "links may not contain links"; commonmark.js: `[a <img alt="b c">](parent.md)`) -- neither `child.md` nor the absent `parent.md` is linked: 0 sites, not rc 2
MUTANT	R19 #1 §6.4: demotion is the RESOLVED image's (re-inject it on the failed image too: `![alt` literal, yet the link inside is stripped)	plan_memo_lexer.py	ff232b3053733dbc	(image) `![a ![b](i.png) [c](child.md)][missing]`: the link stands OUTSIDE the inner image's description and the outer image fails -- it is a link, `child.md` is walked|(image) `![alt [docs](child.md)][missing]`: the image does NOT resolve, so `![alt` is literal text and the link inside it IS a link (commonmark.js) -- `child.md` is walked and its violation reported
MUTANT	R19 #1 §6.4: the demoted link's tail stays masked (re-inject a plain drop: the tail is prose)	plan_memo_lexer.py	f2cb8801bc60284a	(image) `![alt [b](9z)](i.png)`: the demoted link's tail stays masked -- the `9z` in its destination is not prose, 0 sites
MUTANT	R19 #2 display: a memo is named by its path relative to the root memo's directory (re-inject the basename)	plan_memo_population.py	b1872d5fecf21ac3	diagnostics name a memo relative to the root memo's directory: `a/child.md` and `b/child.md` are two files, and a memo outside that directory is named by its absolute path
MUTANT	R19 #3 sibling: a backslash separates on every platform (re-inject the POSIX reading: `\\` a name character)	plan_memo_sibling.py	8cb9c9f3f13ed044	(link) `sub%5Cchild.md`: a backslash is a path separator on every platform (WHATWG URL `#path-state` step 1: for a special scheme -- `file` is one -- `\\` ends a segment as `/` does; `PureWindowsPath` is that syntax) -- the file `sub/child.md` is walked
MUTANT	R19 #3 sibling: the decoded name must be relative under Windows path syntax on every platform (re-inject the `/`-only test)	plan_memo_sibling.py	cc1e1dc40a2bf9af	(rc) `n%3Achild.md`: a ONE-letter name before `:` is a Windows drive letter (URL `#path-state` step 1.4.1, platform-independent) -- drive-relative, rejected at stage (c) by the ANCHOR, rc 0.  ⚠ Since R25-1 the multi-letter `notes%3Achild.md` is refused at the same stage by the CHARACTER rule, so this no longer discriminates one-letter from multi-letter; what it still says is that the drive reading is applied on every platform|(rc) a percent-encoded UNC `%5C%5Cserver%5Cshare%5Cx.md` is rejected (stage c: a UNC prefix anchors): rc 0|(rc) a percent-encoded Windows drive-absolute `C%3A%5Ctemp%5Cchild.md` (`C:\\temp\\child.md`) is rejected after decoding on every platform (stage c: a drive anchors): rc 0|(rc) a percent-encoded backslash-rooted `%5Cchild.md` (`\\child.md`) is rejected (stage c: a root anchors): rc 0|(rc) a raw `\\\\server\\share\\x.md` destination decodes (§2.4: `\\\\` is one backslash, `\\s` is literal) to the backslash-rooted `\\server\\share\\x.md` (commonmark.js: href `%5Cserver%5Cshare%5Cx.md`) and is rejected: rc 0|(rc) drive-relative `C:child.md`: raw, it is a URL of scheme `c` (stage a); percent-encoded `C%3Achild.md` decodes to a drive-anchored name (stage c) -- both rejected, rc 0
MUTANT	R2-1 def: the next-line title is tried before the destination-only ending	plan_memo_blocks.py	98409eb4cedd4d96	(def) a next-line title holding `[x](missing.md)` is a title, not a link: rc 0|(def) a next-line title is part of the definition, not prose: an id in it is no site
MUTANT	R2-2 link: `![` opens an image, which is not a link	plan_memo_links.py	a1614c8f7b7017ad	(link) a link wrapping an IMAGE `[![alt](img.png)](sib.md)` links the sibling; `img.png` is never a memo
MUTANT	R2-3 link: the reference form is the lexer's (re-inject a raw bracket walk)	plan_memo_memo.py	d877d5d6370a53b1	(link) `[foo\\]][missing]` is a FULL reference (the `]` is escaped): a schema miss, not an exempt shortcut
MUTANT	R20 #1 file: the stem of a bare `.md` file name may be EMPTY (re-inject the >=1-character stem)	plan_memo_tokens.py	e9bec701e638a728	(file) `.md` alone is a file name (the suffix-only name `sibling_path` accepts): beside a declared no-owner id `md`, `Read .md for details` reports 0 sites
MUTANT	R20 #1 sibling: stage (d) is the lexer's FILE_SUFFIX test alone (re-inject a stem requirement on the resolver side)	plan_memo_sibling.py	f6ae5f7b58d6edea	(link) `[x](.md)` links the sibling file named `.md`: `sibling_path` stage (d) and the lexer's file token read the ONE `FILE_SUFFIX`, so the suffix-only name is a file on both sides
MUTANT	R20 #2 anchored: the anchored reading admits every row kind (re-inject the short-only test)	plan-memo-umbrella-check.py	5f93eb6fef54ee6d	(c-seed) `Lands after Slice `#11-zz-alpha`` in a Slice cell whose Deps cell names only **9z**: the anchored reading admits the slug, so the prose names a party the cell does not carry -- ORDER-PROSE? 1|PROPERTY: every row-id composer admits every row kind of plan_memo_ids.ROW_KINDS (the kind half of the spelling sweep)
MUTANT	R20 #2 appositive: ROW_NOUN_ID composes ROW_ID (re-inject decorated_id(SHORT_ID) at the one composer)	plan_memo_tables.py	0f37cda0e3fd6271	(a) the R20 reviewer's declaring field `Slice `#11-zz-alpha` — **UMBRELLA, …**` attributes the marker to a SLUG row: the row is a pointer and the UMBRELLA-MARK attribution finding is emitted|PROPERTY: every row-id composer admits every row kind of plan_memo_ids.ROW_KINDS (the kind half of the spelling sweep)
MUTANT	R20 #2 grammar: ROW_ID is every row kind (re-inject SHORT_ID only -- the appositive, OWNS_TWO and the anchored reading all lose the slug at once, since they compose the one alternation)	plan_memo_ids.py	7500933147b4bba4	(a) the R20 reviewer's declaring field `Slice `#11-zz-alpha` — **UMBRELLA, …**` attributes the marker to a SLUG row: the row is a pointer and the UMBRELLA-MARK attribution finding is emitted|(a) the bold slug form `Slice **#11-zz-alpha** — **UMBRELLA, …**` attributes the marker too|(d) `owned by `#11-zz-alpha` and **Qx**`: a SLUG owner and a short owner are a two-owner clause (`OWNS_TWO` composes `ROW_ID`)|PROPERTY: every row-id composer admits every row kind of plan_memo_ids.ROW_KINDS (the kind half of the spelling sweep)|a marker naming another row does not enter the count
MUTANT	R20 #3 naming: a row with no id is named by its declaring locator (re-inject `%r` of the id: `row None`)	plan_memo_tables.py	ebab2527cd01bb36	a row whose id cell declares no id is named by its declaring locator (`row <no id> at :LINE (token)`), never `row None`
MUTANT	R21 #1 §6.4: a construct demoted into a resolved image's description renders no tag of its own (re-tag it `image`: the resolved-image count over-claims)	plan_memo_lexer.py	cdda420c3bd38b32	CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R21 #1 §6.5: a scheme is 2-32 characters (widen to 1: `<m:abc>` becomes an autolink)	plan_memo_html.py	13bd696b29850483	(autolink) `<m:abc…>` is a ONE-character scheme and no autolink (§6.5 Example 609, 2-32 characters): the `<` is literal and the link INSIDE the would-be span is read (commonmark.js: `&lt;m:abc<a href="child.md">x</a>&gt;`).  The link sits inside the span so that widening the scheme to one character masks it and the control goes red|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R21 #1 §6.5: a space ends the absolute URI (admit it: `< https://foo.bar >` becomes an autolink)	plan_memo_html.py	b5686d638076ef93	(autolink) a SPACE inside the absolute URI ends it (§6.5 Example 608), so `<https://foo.bar/ …>` is no autolink and no tag either -- the `<` is literal and the link INSIDE the would-be span is read (commonmark.js: `&lt;https://foo.bar/ <a href="child.md">x</a>&gt;`).  The space sits in the URI TAIL, which is the class the grammar excludes it from: a space right after `<` fails at the SCHEME instead, so that shape leaves the tail-widening mutant alive and proves nothing|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R21 #1 §6.5: an autolink is one masked token of the inline pass (drop the arm: its brackets are delimiters again)	plan_memo_lexer.py	b76b3c8c3608a648	(autolink) a backtick inside an autolink is consumed by it, not by a code span (the three delimiters are read left to right as met; commonmark.js agrees) -- 0 sites|(autolink) a declared id inside a URI autolink is no naming site: the span is masked whole, exactly as a link's destination is -- 0 sites|(autolink) a declared id inside an EMAIL autolink is no naming site either (the second §6.5 arm, `mailto:` at the renderer) -- 0 sites.  The id is the whole local part: dropping the arm leaves `9z` bounded by `<` and `@`, so the mutant HAS a site to report (an id glued to a domain label -- `ops@9z.example.com` -- is no site either way and proves nothing)|(autolink) the R21 reviewer's input `<https://example.com/[child](absent.md)>` is ONE §6.5 autolink: the brackets inside it are not link syntax, `absent.md` is no memo, rc 0|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R21 #1 §6.5: the autolink span is MASKED (drop the disposition: its text is prose again)	plan_memo_stream.py	0a4a3531ba3cb299	(autolink) a backtick inside an autolink is consumed by it, not by a code span (the three delimiters are read left to right as met; commonmark.js agrees) -- 0 sites|(autolink) a declared id inside a URI autolink is no naming site: the span is masked whole, exactly as a link's destination is -- 0 sites|(autolink) a declared id inside an EMAIL autolink is no naming site either (the second §6.5 arm, `mailto:` at the renderer) -- 0 sites.  The id is the whole local part: dropping the arm leaves `9z` bounded by `<` and `@`, so the mutant HAS a site to report (an id glued to a domain label -- `ops@9z.example.com` -- is no site either way and proves nothing)
MUTANT	R21 #1 §6.5: the email arm is an autolink too (drop it: the URI arm alone)	plan_memo_html.py	3bbfcc03155206ef	(autolink) a declared id inside an EMAIL autolink is no naming site either (the second §6.5 arm, `mailto:` at the renderer) -- 0 sites.  The id is the whole local part: dropping the arm leaves `9z` bounded by `<` and `@`, so the mutant HAS a site to report (an id glued to a domain label -- `ops@9z.example.com` -- is no site either way and proves nothing)|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R21 #2 (a): the marker in the declaring field short-circuits the SEED only (re-inject the `continue` that skipped the out-of-field scan)	plan_memo_roles.py	5549ea86383bd9a4	(a) a row carrying the marker in its declaring field AND again in another cell is the double marker the assertion forbids: UMBRELLA-MARK 1
MUTANT	R21 #3: KIND-SPELLING is gated on the spellings, not on the winning kind (re-nest it under a non-empty undetermined set)	plan-memo-umbrella-check.py	35b491b46007a78a	(rc) that divergence is a mechanical finding: rc 1|(rc) two MARKED rows spelling the undetermined kind two ways is KIND-SPELLING: the undetermined set is empty and the divergence is still real
MUTANT	R21 #4: an id cell declares an id of a kind ITS schema keys (drop the kind test)	plan_memo_tables.py	34f85f25a10f2280	(schema) a SHORT id in the citation table keys no citation row (the schema keys cite): rc 2 -- the inverse direction, which would otherwise pollute the keep-set|(schema) a `#11-` SLUG in the citation table is the same miss -- rc 2|(schema) a citation-shaped id in the §5 SLICE table keys no slice row (the schema keys short + slug): the row is unkeyed, so its cells would go unasserted -- rc 2|(schema) a citation-shaped id in the §8 SLOT table is the same miss -- rc 2
MUTANT	R21 #4: the citation table keys citation ids (widen it to every kind)	plan_memo_tables.py	4cbbe3217b84d541	(schema) a SHORT id in the citation table keys no citation row (the schema keys cite): rc 2 -- the inverse direction, which would otherwise pollute the keep-set|(schema) a `#11-` SLUG in the citation table is the same miss -- rc 2
MUTANT	R21 #4: the §5 slice table keys short + slug (widen it to every kind)	plan_memo_tables.py	a56796e215b9845e	(schema) a citation-shaped id in the §5 SLICE table keys no slice row (the schema keys short + slug): the row is unkeyed, so its cells would go unasserted -- rc 2
MUTANT	R22 #1 seed: the mask is BOTH kinds (drop the citation half -- the hand-written filter the shared spans subsume)	plan-memo-umbrella-check.py	14e8a25ac0b19331	(seed) a raw line whose only declared id is a CITATION seeds nothing -- the shared spans SUBSUME the seed's former hand-written `kind != cite` filter, so the citation half has one spelling and not two
MUTANT	R22 #1 seed: the raw line is masked by the file / citation spans the DISPOSITION reads (drop the mask: the seed reads the raw line again)	plan-memo-umbrella-check.py	fbe9013d9e9c1a49	(seed) a raw HTML-block line whose only declared id sits INSIDE a `.md` file name seeds nothing: the same name in prose is masked and reports nothing, and a seed that fires where prose would not is reporting a reading the document has no way to hold|(seed) a raw line whose only declared id is a CITATION seeds nothing -- the shared spans SUBSUME the seed's former hand-written `kind != cite` filter, so the citation half has one spelling and not two|(seed) the same for an INDENTED-CODE line: one disposition, both readings
MUTANT	R22 #2 sibling: EVERY part, not just the last (re-inject a final-component-only reading)	plan_memo_sibling.py	93462cabc4d71497	(R25 sibling) the character rule is read per PART, as the device rule is: `ax%3Ab%2Fchild.md` (`ax:b/child.md`) puts the colon in a NON-FINAL component -- with a final-component-only reading it passes while `ax:b.md` is refused.  ⚠ The colon must sit in the DIRECTORY: `sub/ax:b.md` puts it in the last part, where that reading refuses it too and R22's per-part mutant SURVIVES (measured)|(sibling) `NUL/child.md`: the device is read per PART of the parsed path -- a device DIRECTORY rejects the destination too.  The device sits in a NON-FINAL component on purpose: with it in the last one, a final-component-only reading passes this control and the mutant survives (measured -- the probe would have had another subject)|(sibling) `dir%20/child.md`: a component ending in a SPACE is reserved too -- Windows strips the trailing run, so that component names a different directory there than here
MUTANT	R22 #2 sibling: a component ending in a dot or a space is reserved as well (drop that half)	plan_memo_sibling.py	857a7b208130ead8	(sibling) `dir%20/child.md`: a component ending in a SPACE is reserved too -- Windows strips the trailing run, so that component names a different directory there than here
MUTANT	R22 #2 sibling: stage (c) rejects a reserved COMPONENT (drop the clause -- an empty anchor was the whole test until R22)	plan_memo_sibling.py	3c19aee473f957d0	(sibling) `NUL.md` is a DOS device, not a memo beside this one: the destination is no sibling even though a file of that name is there to read|(sibling) `NUL/child.md`: the device is read per PART of the parsed path -- a device DIRECTORY rejects the destination too.  The device sits in a NON-FINAL component on purpose: with it in the last one, a final-component-only reading passes this control and the mutant survives (measured -- the probe would have had another subject)|(sibling) `com1.md`: the device names fold case (`ntpath._isreservedname` upper-cases the stem), so the lower-case spelling is the same device|(sibling) `dir%20/child.md`: a component ending in a SPACE is reserved too -- Windows strips the trailing run, so that component names a different directory there than here|(sibling) `prn%20.md`: the stem's trailing spaces are stripped before the device lookup, so `prn .md` is `PRN` (Microsoft, "Naming Files, Paths, and Namespaces")
MUTANT	R22 #2 sibling: the device is the STEM before the first dot, case-folded, trailing spaces stripped (re-inject the whole component)	plan_memo_sibling.py	a241cec3770a115d	(sibling) `NUL.md` is a DOS device, not a memo beside this one: the destination is no sibling even though a file of that name is there to read|(sibling) `com1.md`: the device names fold case (`ntpath._isreservedname` upper-cases the stem), so the lower-case spelling is the same device|(sibling) `prn%20.md`: the stem's trailing spaces are stripped before the device lookup, so `prn .md` is `PRN` (Microsoft, "Naming Files, Paths, and Namespaces")
MUTANT	R22 #2 sibling: the stem's case is folded (drop the fold)	plan_memo_sibling.py	d1d301400d9f5822	(sibling) `com1.md`: the device names fold case (`ntpath._isreservedname` upper-cases the stem), so the lower-case spelling is the same device|(sibling) `prn%20.md`: the stem's trailing spaces are stripped before the device lookup, so `prn .md` is `PRN` (Microsoft, "Naming Files, Paths, and Namespaces")
MUTANT	R22 #3 phrase: POINTER is bounded (re-inject the bare phrase)	plan_memo_stream.py	3ede46b938300b46	(kind) `is a pointer rather than a slicer of work` is no pointer row: it is an active terminal, and a terminal row stating no acceptance condition owes the ACCEPT-VOCAB? seed -- which the unbounded reading suppressed
MUTANT	R22 #3 phrase: UNDETERMINED is bounded (re-inject the bare phrase)	plan_memo_stream.py	8bdeab4291912c4e	(kind) `The KIND UNDETERMINEDNESS metric must be recorded` declares no unsettled kind: with a nonempty `Deps` cell the unbounded reading made the row no-owner and forced `UMBRELLA-CELL`, rc 1|(kind) nor does `MANKIND UNDETERMINED by the probe` -- the boundary is on BOTH sides, and the left one is the half a right-only fix would leave authoritative
MUTANT	R22 #3 phrase: the BACKWARD licensing look is bounded on its left (drop the edge)	plan_memo_roles.py	962834d83becb089	(licence) `The grandchild of 9z` is not the licensing phrase `child of`: a licence SUPPRESSES a report, so an unbounded edge there is the dangerous polarity -- the mention is reported
MUTANT	R22 #3 phrase: the DECLARES vocabulary is bounded (re-inject the bare alternation)	plan_memo_roles.py	32b15f7667f79629	(kind) `not a terminal unitary claim` is not the kind vocabulary either: the seed half of assertion (a) reads the same bounded phrases the marker does
MUTANT	R22 #3 phrase: the FORWARD licensing look is bounded on its right (drop the edge)	plan_memo_roles.py	e929acc2825eba1d	(licence) `9z's memorandum` is not the licensed possession `memo`: the right-hand edge of the forward look, reported
MUTANT	R22 #3 phrase: the MARKER is bounded (re-inject the bare phrase)	plan_memo_stream.py	4a095c6b048db7c4	(kind) `SUBUMBRELLA, not a terminal unit.` carries no marker: the row is terminal, out of the no-owner census, and a prose mention of it is no site|(kind) nor does `UMBRELLA, not a terminal unitary claim.` -- the right-hand edge
MUTANT	R22 #3 phrase: the shared boundary composer is load-bearing (drop both lookarounds from `bounded`)	plan_memo_ids.py	6bdc506df0f67b50	(kind) `SUBUMBRELLA, not a terminal unit.` carries no marker: the row is terminal, out of the no-owner census, and a prose mention of it is no site|(kind) `The KIND UNDETERMINEDNESS metric must be recorded` declares no unsettled kind: with a nonempty `Deps` cell the unbounded reading made the row no-owner and forced `UMBRELLA-CELL`, rc 1|(kind) `is a pointer rather than a slicer of work` is no pointer row: it is an active terminal, and a terminal row stating no acceptance condition owes the ACCEPT-VOCAB? seed -- which the unbounded reading suppressed|(kind) `not a terminal unitary claim` is not the kind vocabulary either: the seed half of assertion (a) reads the same bounded phrases the marker does|(kind) nor does `MANKIND UNDETERMINED by the probe` -- the boundary is on BOTH sides, and the left one is the half a right-only fix would leave authoritative|(kind) nor does `UMBRELLA, not a terminal unitary claim.` -- the right-hand edge
MUTANT	R23 #1 stream: a §2.5 / §2.4 spelling of a DECORATION character is BLANKED (leave it standing as written: the entity's SOURCE letters are back where the id scanner reads them)	plan_memo_stream.py	155e1dd38b3257fb	(R23 render) `The &ast; marks it` names NO row `ast`: the reference renders `*`, so the letters `a` `s` `t` are nowhere in the document a reader reads -- leaving the entity's SOURCE spelling in the stream fabricated the site
MUTANT	R23 #1 stream: it is a BLANK and not a DROP (the character IS rendered, so its two sides are not one word: dropping it joins them into an id no reader reads)	plan_memo_stream.py	0cc8e50103690a03	(R23 render) `Slice Q&ast;x owns it` names NO row `Qx`: the reference renders a character, so the span is a BLANK and bounds the two sides -- dropping it (the disposition for a construct that renders nothing) would join them into an id the document does not spell
MUTANT	R23 #1 stream: what a READER sees where a decoration spelling is blanked is the character it renders (leave it as spaces: the two readings agree on a kind neither of them should read)	plan_memo_stream.py	8fead8c3764d4ef1	(R23 kind) `KIND&ast;UNDETERMINED` is that same stream-side miss through a BLANKED decoration spelling: the reader sees `KIND*UNDETERMINED` and no kind at all.  It is the one control over what a reader SEES where such a span is blanked -- render it as spaces and the two readings agree on a kind neither should read
MUTANT	R23 #2 _kind: every phrase it reads comes from KIND_PHRASES (re-inject a direct read: a phrase decides a kind without the tuple -- and so without the gate -- ever seeing it)	plan_memo_population.py	d63c23558d4e0d52	PROPERTY: Population._kind reads every kind phrase from plan_memo_stream.KIND_PHRASES, the tuple the residue gate iterates (a fourth phrase cannot decide a kind without being gated)
MUTANT	R23 #2 gate: the READER's rendering is consulted (drop it: a phrase the reader reads across a blank is no longer a miss)	plan_memo_stream.py	d4ad3cc330c24f47	(R23 kind) `KIND UNDETER`MINED`` in a declaring field is the schema miss: a reader reads the undetermined kind (§6.1 -- the code span contributes `MINED` as plain text), the disposed stream reads none, and the row silently left the census as an active terminal at rc 0 -- the marker was gated, every OTHER kind phrase was not|(kind) a declaring field spelling the marker ACROSS a code span is the schema miss, rc 2: a reader reads the marker, the disposed stream does not, and §1 forbids a clean exit for a could-not-scan over the census
MUTANT	R23 #2 gate: the STREAM's rendering is consulted too (drop it: a phrase only the stream reads -- a blank standing as spaces -- is no longer a miss)	plan_memo_stream.py	1e70b67f606dfa33	(R23 kind) `KIND `x` UNDETERMINED` is the SAME miss from the other side: the blank stands as spaces, so the STREAM reads the undetermined kind and a reader (`KIND x UNDETERMINED`) reads none -- a gate stated over one direction leaves the other authoritative|(R23 kind) `KIND&ast;UNDETERMINED` is that same stream-side miss through a BLANKED decoration spelling: the reader sees `KIND*UNDETERMINED` and no kind at all.  It is the one control over what a reader SEES where such a span is blanked -- render it as spaces and the two readings agree on a kind neither should read
MUTANT	R23 #2 gate: the disagreement must come from a STRADDLE (drop the conjunct: a phrase quoted WHOLE becomes the miss I-A exists to prevent)	plan_memo_stream.py	1f53969b58e95b6b	(R23 kind) a kind phrase QUOTED WHOLE is no miss: the two readings differ about it, and that difference is I-A's disposition (a quoted phrase declares nothing), not a could-not-scan -- the STRADDLE is the conjunct that tells them apart
MUTANT	R23 #2 gate: the residue gate iterates EVERY member of KIND_PHRASES (truncate it to the first: the marker stays gated and every other phrase decides a kind unwatched again -- the R23 defect exactly)	plan_memo_stream.py	dd4c041e092b52ae	(R23 kind) `KIND UNDETER`MINED`` in a declaring field is the schema miss: a reader reads the undetermined kind (§6.1 -- the code span contributes `MINED` as plain text), the disposed stream reads none, and the row silently left the census as an active terminal at rc 0 -- the marker was gated, every OTHER kind phrase was not|(R23 kind) `KIND `x` UNDETERMINED` is the SAME miss from the other side: the blank stands as spaces, so the STREAM reads the undetermined kind and a reader (`KIND x UNDETERMINED`) reads none -- a gate stated over one direction leaves the other authoritative|(R23 kind) the POINTER phrase straddling a code span is the miss too -- the third member, gated by arriving in `KIND_PHRASES` and not by being named here
MUTANT	R23 #2 gate: the two readings must DISAGREE about the phrase (drop the conjunct: a field that spells the phrase cleanly and straddles a blank elsewhere becomes a miss)	plan_memo_stream.py	abd643ca5ff5c17e	(R23 kind) a field that spells the phrase CLEANLY and straddles a blank elsewhere is no miss: both readings say undetermined, so the census is not in doubt -- the DISAGREEMENT is the other conjunct
MUTANT	R23 #2 seed: the residue SEED reads every member too (truncate it to the first: a kind phrase read across a blank outside a declaring field is silent again)	plan_memo_stream.py	641afe911bbe01fc	(R23 seed) the `[LEX-SPLIT?]` residue seed reads every kind phrase too, not only the marker: `KIND UNDETER`MINED`` in PROSE (no declaring field, so no census to gate) is the reported disagreement
MUTANT	R23 #3 memo: a leading U+FEFF is dropped where the text becomes lines (keep it: the first block's header row is not a header row, and the table nobody saw is no schema miss)	plan_memo_memo.py	c5daf9349515f10b	(R23 memo) a linked memo whose FIRST block is a slice table still declares its rows when the file opens with a BOM: the signature is dropped where the text becomes lines, or the header row is not a header row and the census silently shrinks at rc 0
MUTANT	R23 #3 memo: exactly ONE is dropped (strip every leading one: a second U+FEFF is an ordinary character of the document and stripping it rewrites the document)	plan_memo_memo.py	e1599b125c234cf6	(R23 memo) exactly ONE U+FEFF is dropped: a second is an ordinary character of the document and the table is lost to it again -- a strip that is a `lstrip` rewrites the document instead of reading its encoding
MUTANT	R23 #4 lexer: the demotion ranges are applied as a UNION, once (re-inject the per-close walk: N nested images re-tag one descendant N times -- the same OUTPUT, quadratic work, which is why only a work witness can see it)	plan_memo_lexer.py	dfbcb68f3a2c6d74	a resolved image's demotion is linear: N nested images demote their descendants once, not once per enclosing image
MUTANT	R23 #4 lexer: the image range is recorded at the close (drop it: a nested image is a resolved image of its own and the tag count over-claims)	plan_memo_lexer.py	52c13da71dfeefe8	CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R24 F1 tables: the decoration exception is asked of the RENDERED content (re-inject the raw content: `**&#57;z**` decorates an id the source does not spell)	plan_memo_stream.py	4052ea262c8ca56f	(R24 render) `**&#57;z**7z owns it` names `9z`: the pair's content RENDERS `9z`, so the `**` decorate an id and stand as the boundary they are.  Asked of the source, `id_only` read `&#57;z`, dropped the delimiters, and joined the two sides into the token `9z7z` a reader never sees -- the mirror of the fabrication design re-gate 4 closed, one spelling further out|PROPERTY: the verdict is invariant under a §2.5 re-spelling of any prose character the document renders the same (the rendered-text rule, swept position by position)
MUTANT	R24 F1 tables: the exception reads what a READER sees, not the disposed stream (hand it the stream: a code span's blanks whitespace-separate into an id-only run)	plan_memo_stream.py	7cb75706c655be0f	(R24 render) ``**`x` 9z**7z owns it`` names nobody: a READER sees the document bolding `x 9z`, which is prose and no decorated id, so the delimiters drop and the sides join.  This is the control over WHICH rendering the exception is asked of -- the checker's own disposed stream would show `9z` beside blanks, whitespace-separate into an id-only run, and keep a decoration the document does not have
MUTANT	R24 F1 tables: the file/cite reading is taken over the block's RENDERING (re-inject the raw reading: a §2.5 reference that renders whitespace no longer bounds the file name)	plan_memo_stream.py	433a9cfde74f767f	(R24 render) `9z&#32;notes.md owns it` names `9z`: §2.5 renders the reference as a SPACE, so the file name begins at `notes` and the id stands beside it -- the raw reading saw one unbroken run ending in `.md`, masked the whole of it, and the ownership claim left the census at rc 0|PROPERTY: the verdict is invariant under a §2.5 re-spelling of any prose character the document renders the same (the rendered-text rule, swept position by position)
MUTANT	R24 F2 memo: a CR NOT followed by an LF is its own ending (drop that arm: `\\r\\n` folds and a bare `\\r` does not)	plan_memo_memo.py	28f93daa7c0a4f01	(R24 §2.1) a memo whose lines end with a bare CARRIAGE RETURN is one document of many lines: §2.1's line ending is `\\n`, `\\r\\n`, or `\\r` not followed by `\\n`, so the tables parse and the rows are declared.  Read without that rule the whole file is one line, no table is admitted, and no schema miss says so|PROPERTY: the census is the same under each of the three line endings CommonMark §2.1 recognises (LF, CRLF, a bare CR), written as bytes
MUTANT	R24 F2 memo: §2 replaces U+0000 with U+FFFD before parsing (drop it: the NUL is an ASCII control, the link destination is refused, and the memo it named leaves the census)	plan_memo_memo.py	dae9ed926b55d406	(R24 §2) a link destination holding a literal U+0000 names the file the document renders: §2 replaces the NUL with U+FFFD before parsing, so `[x](child .md)` links `child<U+FFFD>.md`, that memo is walked and its rows are declared.  Left in, the NUL is an ASCII control, `link_destination` (§6.3) refuses the destination, and the memo -- with every violation in it -- left the census while the run could still exit 0
MUTANT	R24 F2 memo: §2.1's OTHER two line endings become U+000A (drop the normalisation: with the reader's own translation switched off, a CR-ended memo is one line and holds no table)	plan_memo_memo.py	fb1d8b2488b901d0	(R24 §2.1) a memo whose lines end with a bare CARRIAGE RETURN is one document of many lines: §2.1's line ending is `\\n`, `\\r\\n`, or `\\r` not followed by `\\n`, so the tables parse and the rows are declared.  Read without that rule the whole file is one line, no table is admitted, and no schema miss says so|PROPERTY: the census is the same under each of the three line endings CommonMark §2.1 recognises (LF, CRLF, a bare CR), written as bytes
MUTANT	R24 F3 check: the ANCHORED reading wins the mention dedup (let the bare one win: a licensing phrase separated from the id by a row noun no longer stands immediately before the mention)	plan-memo-umbrella-check.py	3e09b6052e84b0ff	(R24 width) a row noun standing between the licensing phrase and the id does not hide the phrase (`the child of Slice 9z`), and NOT because the rule has a clause for one: the ANCHORED reading of that site starts AT the noun and wins the dedup, so the text before the mention is `the child of` either way.  This is the control the deleted `_TRAILING_NOUN` substitution was believed to be needed for
MUTANT	R24 F3 roles: the backward look reads the text before the mention, bounded by `endpos` where `$` matches (re-inject the 40-character slice: a phrase that long starts at index 0 and the lookbehind succeeds against nothing)	plan_memo_roles.py	7df956615244df6c	(R24 width) `xderivation … that the 9z` is REPORTED: the licensing phrase is 40 characters, exactly the width of the slice the backward look was given, so its lookbehind fell off the start of that slice and succeeded against nothing -- the document says `xderivation`, which is not the licensed phrase|PROPERTY: no ANCHORED pattern in the module set is handed a subject truncated by a number (a width window is a second statement of what the anchor already says)
MUTANT	R24 F3 tables: the appositive is bounded by where the MARKER begins, not by a width (re-inject the 70-character slice: a longer declared slug pushes the appositive out of the window)	plan_memo_tables.py	c3bc0d78d8dde902	(R24 width) a pointer row whose appositive names a 76-character slug attributes the marker to that row: the appositive ends where the marker begins, which is a GRAMMAR fact and not a character count.  Read through a 70-character window the appositive fell outside it, the field was taken as the row's OWN declaration, no UMBRELLA-MARK was emitted, and the census carried a pointer row as an umbrella at rc 0|PROPERTY: no ANCHORED pattern in the module set is handed a subject truncated by a number (a width window is a second statement of what the anchor already says)
MUTANT	R24 F3 tables: the appositive still requires the DASH (drop it: a field that merely mentions a sibling attributes to it, which is what the window was believed to prevent)	plan_memo_tables.py	619404a8e7149411	(R24 width) a field that merely MENTIONS a sibling and then declares itself attributes nothing: the discrimination is the DASH between the id and the marker, never the distance -- so removing the window does not widen the attribution
MUTANT	R24 check: the BARE pass admits every ROW_KINDS kind, not one of them (narrow it to `short`: a slug named with no row noun before it stops being a naming site, while the anchored reading still reports it)	plan-memo-umbrella-check.py	1d59f23e2fdf505b	PROPERTY: every row-id composer admits every row kind of plan_memo_ids.ROW_KINDS (the kind half of the spelling sweep)
MUTANT	R24 memo: a multiline §6.6 span's pieces are located at their OWN offsets (key them on the span's start instead: the seed sends the reader to the line the comment opened on, where the id it names is not)	plan_memo_memo.py	8236776d3cac3624	a §6.6 span crossing a line ending seeds each of its lines at ITS line, not all of them at the opener's
MUTANT	R25-1 sibling: stage (c) refuses a component holding a character Windows does not read as a letter of a name (drop the clause -- the reading that let an NTFS alternate data stream through)	plan_memo_sibling.py	bcc06251edeb402d	(R25 sibling) `ax%22notes.md` is no sibling: the decoded `ax"notes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not|(R25 sibling) `ax%2Anotes.md` is no sibling: the decoded `ax*notes.md` holds a character Microsoft's "Naming Files, Paths, and Namespaces" lists among those a file name may not use.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not|(R25 sibling) `ax%3Anotes.md` is no sibling: the decoded `ax:notes.md` opens an NTFS ALTERNATE DATA STREAM there -- `notes:child.md` is the stream `child.md` of the file `notes` -- so where the stream exists reading it SUCCEEDS and yields another file's content, the very class the DOS devices were rejected for.  This is the member R25 reported, and the one that falsified the `OSError` argument the other six rested on.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not|(R25 sibling) `ax%3Cnotes.md` is no sibling: the decoded `ax<notes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not|(R25 sibling) `ax%3Enotes.md` is no sibling: the decoded `ax>notes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not|(R25 sibling) `ax%3Fnotes.md` is no sibling: the decoded `ax?notes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not|(R25 sibling) `ax%7Cnotes.md` is no sibling: the decoded `ax\pnotes.md` holds a character that same list excludes.  ⚠ Whether that platform RAISES is NOT the test and was not determined here (no Windows is reachable from this tree); the test is stage (c)'s policy -- the name must denote a file beside this memo on every platform, and this one does not|(link) `notes%3Achild.md` is NOT a sibling.  It has no scheme -- WHATWG URL §4.4 #scheme-start-state / #scheme-state read the input as written and `%` is in neither class, #string-percent-decode being a later, separate operation -- so stage (a) admits it; stage (c) then refuses it, because the decoded `notes:child.md` is an NTFS alternate data stream on Windows and a plain file name on POSIX, which is two readings where that stage allows one.  ⚠ THIS EXPECTATION IS A REVERSAL of PR #510 R8, decided at R25-1, and it costs a POSIX memo genuinely named `notes:child.md` -- dropped without a report, as `sub\\child.md` and `NUL.md` already are
MUTANT	R25-2 lexer: a §6.7 hard line break's backslash renders NOTHING (drop the clause -- the literal backslash that stood in the stream until R25)	plan_memo_lexer.py	0946905c76bf5126	(R25 §6.7) a HARD line break spelled with a BACKSLASH names the row across it: `Slice\\` + a line ending + `C` reads `Slice C` (§2.4, vendored Example 16: `foo\\` + an ending renders `<p>foo<br />` + `bar</p>`), so the umbrella row `C` is a naming site.  Until R25 the backslash stood in the stream, `NOUN_ANCHOR` could not reach the id, and the run exited 0 with no site at all|PROPERTY: the verdict is invariant under re-spelling any ONE line break as each of CommonMark's three (the §6.8 soft break, §6.7's two-space and backslash hard breaks) -- the render-equivalence family's second guard, for the class its first one excludes by construction
MUTANT	R25-2 lexer: the hard break's mark is the BACKSLASH alone, not the backslash AND the line ending (swallow the ending too: the break stops separating and the two lines become one word)	plan_memo_lexer.py	9644de7bef1f02f7	(R25 §6.7) a HARD line break spelled with a BACKSLASH names the row across it: `Slice\\` + a line ending + `C` reads `Slice C` (§2.4, vendored Example 16: `foo\\` + an ending renders `<p>foo<br />` + `bar</p>`), so the umbrella row `C` is a naming site.  Until R25 the backslash stood in the stream, `NOUN_ANCHOR` could not reach the id, and the run exited 0 with no site at all|PROPERTY: the verdict is invariant under re-spelling any ONE line break as each of CommonMark's three (the §6.8 soft break, §6.7's two-space and backslash hard breaks) -- the render-equivalence family's second guard, for the class its first one excludes by construction
MUTANT	R26-2 agreement: the sweep's corpus must reach nesting depth 2 (make the resolver refuse every parenthesised name: the sweep then agrees about flat names only, and says so)	plan_memo_sibling.py	7ca1e896d2003578	PROPERTY: every name the sibling resolver accepts, standing alone in prose, is ONE file token to the lexer (the correspondence FILE_SUFFIX's comment asserts)
MUTANT	R26-2 file token: a parenthesis NESTS (drop the push: the stack never deepens, so a nested pair reads as an unmatchable close and cuts the run -- the flat arm's own defect, re-injected)	plan_memo_tokens.py	f6250319b03d7d52	(R26 file) `(m.md(9z)md).md`: the run is balanced ACROSS its groups, so it is one name and not two tokens with the id exposed between them -- the second shape the flat arm could not read, and the one that leaks an id rather than a parenthesis|(R26 file) `foo((9z)).md` is ONE file name: nested balanced parentheses are §6.3's rule at any depth, so the id inside the name is no naming site.  Until R26 the token arm matched a FLAT chunk only, could reach no further left than the bare `.md`, and reported `9z`|PROPERTY: every name the sibling resolver accepts, standing alone in prose, is ONE file token to the lexer (the correspondence FILE_SUFFIX's comment asserts)
MUTANT	R26-2 file token: a run starts one past the INNERMOST parenthesis still open (re-inject the segment start: the token holds an unclosed `(` and everything before it)	plan_memo_tokens.py	991a9b9738f071f4	(R26 file) `9z(x.md)` reports `9z`: the run ending at the suffix may not hold the parenthesis still OPEN there, so it starts one past it and the name is `x.md` -- the half that says the start is the innermost open parenthesis and not the segment
MUTANT	R26-2 file token: an unmatchable `)` ends the segment (drop it: a run holds a close that opens nothing, and the id before it is swallowed)	plan_memo_tokens.py	901fb525bdee3d18	(R26 file) `9z)foo.md` still reports `9z`: a `)` that closes nothing may be held by no run and crossed by none, so the name is `foo.md` and the id before it is outside -- the second discriminating half, against a scan that balanced parentheses by IGNORING the ones it could not match
MUTANT	R26-2 work: the token scan is ONE pass (re-inject a walk back to the segment start at every candidate -- the same answer, quadratically)	plan_memo_tokens.py	d075296554830f25	file_and_cite_spans is linear: N parenthesis groups are one pass, not a re-scan from every start position
MUTANT	R26-3 §6.1: a code span's closer is found by the index BY LENGTH (re-inject the walk over every run: the same answer, 0.27 x L^1.5)	plan_memo_lexer.py	8e372a215f8a537c	a §6.1 code span's closer is found by an index of the runs BY LENGTH, not by walking them (0.27 x L^1.5 before)
MUTANT	R26-3 §6.3: the bound is above what the spec requires and the corpus uses (drop it to 1: Example 496's two levels stop being a link)	plan_memo_links.py	15e3034530306bf9	(R26 §6.3) a destination nested 32 deep IS a link, so the memo it names is a sibling this run could not read: rc 2.  Green before the limit as after it -- the half that says the limit is a LIMIT and not a refusal of nested destinations|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	R26-3 §6.3: the destination's parenthesis nesting is BOUNDED (lift the bound: 33 levels are a link again, which is both the reported false rc-2 miss and the quadratic tail scan)	plan_memo_links.py	988798a6202e4ba0	(R26 §6.3) a destination nested 33 deep is NOT a link, so `[x](...)` is literal text and no memo is looked up: rc 0.  ⚠ This is a DIVERGENCE from commonmark.js, which reads it as a link, and an agreement with cmark; the spec permits both and the vendored corpus reaches depth 2, so nothing but this control says where the boundary is|inline_pass is linear over a malformed inline-link tail: `[`xN + `](`xN is O(N), bounded by §6.3's permitted nesting limit
MUTANT	R26-3 §6.6: a CDATA section needs a `]]>`, not merely a `>` (drop its row: the same defect one alternative over)	plan_memo_html.py	8f1e88ddb6e5fa15	inline_pass does not RUN the §6.6 grammar at a `<` whose closer stands nowhere ahead (the one cost the Python-level witnesses cannot see)
MUTANT	R26-3 §6.6: a processing instruction needs a `?>`, not merely a `>` (drop its row: a text of `<?x` openers ending in a bare `>` re-runs the lazy scan at every one)	plan_memo_html.py	dd86aa571309a739	inline_pass does not RUN the §6.6 grammar at a `<` whose closer stands nowhere ahead (the one cost the Python-level witnesses cannot see)
MUTANT	R26-3 §6.6: the closer index answers YES when the closer is there (refuse always: no raw HTML span is ever recognised)	plan_memo_lexer.py	fa38b54d3db1b740	CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html|inline_pass does not RUN the §6.6 grammar at a `<` whose closer stands nowhere ahead (the one cost the Python-level witnesses cannot see)
MUTANT	R26-3 §6.6: the closer index carries a CURSOR (re-search at every query: the same answers, one scan of the text per opener)	plan_memo_lexer.py	8f3f113a98e24c32	the closer index carries a cursor: 600 openers with no closer start at most one search per literal, not one per opener
MUTANT	R26-3 §6.6: the tag grammar is not RUN at a `<` whose closer stands nowhere ahead (drop the gate: every opener re-scans to the end of the text, as it did before)	plan_memo_lexer.py	56e5e285a32fba48	inline_pass does not RUN the §6.6 grammar at a `<` whose closer stands nowhere ahead (the one cost the Python-level witnesses cannot see)
MUTANT	R26-4 encoding: BOTH streams, not just stdout (reconfigure stdout alone -- stderr keeps the locale's encoding and every diagnostic written there dies on a non-ASCII host)	plan-memo-umbrella-check.py	7fc39b55d03f9f3a	PROPERTY: the entry point sets BOTH output streams to UTF-8 -- the absence a call-site sweep cannot report
MUTANT	R26-4 encoding: a self-test fixture write names its encoding (drop it -- the SELF-TEST half of the sweep's population, which `load()` alone would not show)	plan_memo_selftest_controls.py	3d304245eca4a17b	PROPERTY: no source of this checker performs text I/O without naming its encoding (the checker set and the self-test both, globbed)
MUTANT	R26-4 encoding: the entry point reconfigures its output streams (drop the encoding -- a `reconfigure` naming none leaves the locale's in place)	plan-memo-umbrella-check.py	ad739d8709b74518	PROPERTY: no source of this checker performs text I/O without naming its encoding (the checker set and the self-test both, globbed)|PROPERTY: the entry point sets BOTH output streams to UTF-8 -- the absence a call-site sweep cannot report
MUTANT	R26-4 encoding: the production memo read names its encoding (drop it -- the checker set half of the sweep's population)	plan_memo_memo.py	f5467e5335680599	PROPERTY: no source of this checker performs text I/O without naming its encoding (the checker set and the self-test both, globbed)
MUTANT	R27 sweep: every branch character is an ATOM of the corpus (drop them: the pairs that hold a character no construct spells stop being generated, and every probe left still passes)	plan_memo_selftest_growth.py	2d21ee76b5dd9ea4	the scans are linear over a corpus GENERATED from the grammar: every branch character, delimiter, HTML opener, bracket construct and id kind, each repeated and each PAIR of them interleaved, and no source line grows worse than its input
MUTANT	R27 sweep: every id KIND the grammar declares has a document spelling in the corpus (drop one: the sweep would silently stop pairing that kind's token with anything)	plan_memo_selftest_growth.py	bb464a45152d7fdb	the scans are linear over a corpus GENERATED from the grammar: every branch character, delimiter, HTML opener, bracket construct and id kind, each repeated and each PAIR of them interleaved, and no source line grows worse than its input
MUTANT	R27 sweep: the confirmation runs at a size where a CONSTANT-bounded scan has plateaued (confirm at the sweep's sizes instead: §6.3's destination limit reads as growth)	plan_memo_selftest_growth.py	972b10a3ad703f2c	the scans are linear over a corpus GENERATED from the grammar: every branch character, delimiter, HTML opener, bracket construct and id kind, each repeated and each PAIR of them interleaved, and no source line grows worse than its input
MUTANT	R27-1 Appendix: a link's deactivation of the openers below it is O(1) (re-inject the walk over the whole stack at every close: the same answers, quadratic over `![`xN then N resolved links)	plan_memo_lexer.py	4f141a4672010e08	the scans are linear over a corpus GENERATED from the grammar: every branch character, delimiter, HTML opener, bracket construct and id kind, each repeated and each PAIR of them interleaved, and no source line grows worse than its input
MUTANT	R27-2: only ROW-KEYED schemas name rows (drop the kind filter: a citation table's name becomes a row noun)	plan_memo_tables.py	6abc44ec61f9c53e	PROPERTY: every row-keyed schema's NAME is a row noun and no other schema's is (the nouns are derived from SCHEMAS, so the next schema's is covered by default)
MUTANT	R27-2: the generic nouns survive the derivation (drop them: `row` and `umbrella` belong to no schema and would leave with the literal)	plan_memo_tables.py	43ef00c9c997bf54	PROPERTY: every row-keyed schema's NAME is a row noun and no other schema's is (the nouns are derived from SCHEMAS, so the next schema's is covered by default)
MUTANT	R27-2: the row nouns are derived from SCHEMAS (re-inject the hand-written alternation: `Slot` names no row again, and a slot row that attributes a marker declares itself an umbrella)	plan_memo_tables.py	6c458c9645c30564	(R27 noun) ``Slot `#11-zz-alpha` — **UMBRELLA, …**`` attributes the marker to the named row: the containing row is a POINTER and the attribution finding is emitted. The schema noun the §8 table's own id column is headed with, and the one spelling the hand-written alternation left out|PROPERTY: every row-keyed schema's NAME is a row noun and no other schema's is (the nouns are derived from SCHEMAS, so the next schema's is covered by default)
MUTANT	R27-3 residue: `_straddles` reads only the OVERLAPPING blanks (re-inject the sum over the whole list: the same answers, quadratic in a block of N blanked spans)	plan_memo_stream.py	df0bf25ab324247f	the scans are linear over a corpus GENERATED from the grammar: every branch character, delimiter, HTML opener, bracket construct and id kind, each repeated and each PAIR of them interleaved, and no source line grows worse than its input
MUTANT	R27-3 residue: an extent WHOLLY inside the blanks does not straddle them (flip the early exit: a quoted marker becomes a disagreement)	plan_memo_stream.py	3163849b52607745	PROPERTY: _straddles answers its own definition (a character inside a blank and a character outside every blank), over every blank layout of eight positions and every extent inside it
MUTANT	R27-3 residue: the blank BEFORE the extent may reach into it (drop the step back: a unit whose left end is inside a blank that started earlier reads as wholly outside)	plan_memo_stream.py	0422864afa1ba5c8	PROPERTY: _straddles answers its own definition (a character inside a blank and a character outside every blank), over every blank layout of eight positions and every extent inside it
MUTANT	R28-1 walk: a memo enters the queue at most once (re-inject a duplicate pending entry -- the shape N memos linking the same K produced N*K times over)	plan_memo_population.py	aa679044c61ec884	the population walk queues each memo at most once, over a corpus GENERATED from the definition of a memo family: every digraph on three memos, each also with one memo absent
MUTANT	R28-1 walk: the pending queue is drained from its front in O(1) (re-inject `list.pop(0)` behind the deque's name: the same answers, quadratic in the queue's length)	plan_memo_population.py	acf2bfd771b1feb1	PROPERTY: no source of this checker removes an element from the FRONT of a list (the O(1) half of the population walk's drain, which no work witness here can measure)
MUTANT	R28-2 map: every module is NAMED in the map (drop one line: the module map's own drift, which the prose warning it replaced did not prevent)	plan-memo-umbrella-check.py	e2c22544a9022fcd	PROPERTY: every module of this checker is NAMED in the entry point's MODULES map (the map is checked, not asked to be kept)
MUTANT	R28-2 map: every name in the map IS a module (add one that is not: a rename leaves the completeness direction green and the map wrong)	plan-memo-umbrella-check.py	3c019460a125d168	PROPERTY: every name the entry point's MODULES map spells is a file that exists (the rename half the completeness direction cannot see)
MUTANT	R29-1 quote: `starts_block` asks the marker test, not the content build (restore the build: the arm a gather reaches only at a lazy candidate, which the nested shape never has)	plan_memo_blocks.py	45ffb10e8da70263	the §5.1 marker test builds no content: quote_content is called once per line a quote's gather examines and never to answer whether a line carries a marker
MUTANT	R29-1 quote: the `_parse` arm that OPENS a quote asks the marker test, not the content build (restore the build: the same answer, a copy of the rest of the line thrown away)	plan_memo_memo.py	71920122e4889abd	the §5.1 marker test builds no content: quote_content is called once per line a quote's gather examines and never to answer whether a line carries a marker
MUTANT	R29-2 id scan: a token's left decoration stops at the previous token's end (widen the clamp to `pos`: the marks a token already carries as its right decoration become the next one's left)	plan_memo_ids.py	61633019cf084517	PROPERTY: plan_memo_ids.tokens reads exactly the language plan_memo_ids.decorated_id spells, over an EXHAUSTIVE corpus of decoration and id characters (the core-first scan against the grammar's own composition)
MUTANT	R29-2 id scan: the decoration is walked from an ID, not from every position (re-inject the per-position walk -- the retired re-entry, in Python where a witness can count it)	plan_memo_ids.py	a7c983a34271295d	the id scan walks a decoration run ONCE per id and never at all where no id follows it (an exact count, not a ratio -- the half of R29-2 a Python witness can see)
MUTANT	R29-2 id scan: the scan searches for the id CORE, not for a pattern that opens with an unbounded decoration run (restore the retired `decorated_id` composition at the same `finditer`)	plan_memo_ids.py	5538b285365f52f2	PROPERTY: no SCANNING pattern-method call in the module set applies a pattern that BEGINS with an unbounded repeat (a run the engine re-enters at every position inside it -- a cost no witness in this suite can count)
MUTANT	R29-3 §4.6: the type-6 tag list holds every name the spec does (drop `search`, the one 0.31 added -- the direction an added-name check cannot see)	plan_memo_blocks.py	5600581670c0a6e1	PROPERTY: the §4.6 start-condition-6 tag list in the code is the list CommonMark 0.31.2 spells, both directions, against the vendored extraction (source, version and sha256 recorded)
MUTANT	R29-3 §4.6: the type-6 tag list holds no name the spec does not (add `hgroup`, which 0.31 removed when it added `search` -- the change this round was asked for)	plan_memo_blocks.py	03f49ba4b680d0c5	PROPERTY: the §4.6 start-condition-6 tag list in the code is the list CommonMark 0.31.2 spells, both directions, against the vendored extraction (source, version and sha256 recorded)
MUTANT	R3-1 link: a consumed image tail is masked and not re-read	plan_memo_lexer.py	c4824627460f00cc	(image) `![alt][img]` with a definition is consumed whole: `[img]` is not re-read as a shortcut, and the image destination is not a memo
MUTANT	R3-1 link: an IMAGE does not deactivate the openers before it (deactivate on image)	plan_memo_lexer.py	cc1b15579e96f014	(link) a link wrapping a REFERENCE image `[![alt][img]](child.md)`: the image does not deactivate the outer opener, so `child.md` is scanned|(link) a link wrapping an IMAGE `[![alt](img.png)](sib.md)` links the sibling; `img.png` is never a memo
MUTANT	R3-1 link: an escaped `[` is not an opener	plan_memo_lexer.py	7f43dda5ecd3b42a	(link) an escaped `\\[` opens nothing: `\\[x](absent-file.md)` is not a link, rc 0
MUTANT	R3-1 link: one pass, no recursive inner re-parse (re-inject one: exponential)	plan_memo_lexer.py	668d03da689f3821	links() is linear: 30 nested brackets are one inline_pass call
MUTANT	R3-2 / R5-2 population: a `/`-leading path -- raw `/x.md`, `//host/x.md`, or DECODED `%2Ftmp%2Fx.md` -- is not a sibling (drop the anchor test)	plan_memo_sibling.py	a28dc084faf2dd75	(rc) a percent-encoded ABSOLUTE destination `%2Ftmp%2Fchild.md` is rejected after decoding (never probes `/tmp/child.md`): rc 0|(rc) a protocol-relative `//host/x.md` is not a sibling on disk: rc 0|(rc) a root-relative `/guide.md` is not a sibling on disk (nothing probed): rc 0
MUTANT	R30 id cell: `_unkeyed` judges only the rows that declared nothing (drop the guard: every keyed row is asked the blank question too)	plan_memo_population.py	911b854bb4776649	(R30 id) the keyed partner, end to end: `**7z**` keys the row, so the marker and the `Deps` edge ARE asserted -- UMBRELLA-CELL, rc 1.  This is the run the six unkeyed cells above silently left, and it must not be reached by asking the blank question of a row that declared an id
MUTANT	R30 id cell: the blank question is asked of a RENDERING, not of the raw cell (the direction the strip hid: a construct that RENDERS a blank)	plan_memo_population.py	b80cada8e316ee28	(R30 id) an id cell `&#8212;` is a deliberate blank: §2.5 renders the em dash, which the RAW reading could not see -- this cell was a schema miss before R30 and is a non-row after it, the one verdict the fix reverses in the other direction|(R30 id) an id cell `**—**` is STILL a deliberate blank: here the `**` pair closes (§6.2), and paired decoration around a blank is what the strip was right about.  The partner that bounds the fix's reach: refusing decoration outright would pass all six above and fail this one|(R30 id) an id cell `` `—` `` is a deliberate blank: the code span renders its content and a reader sees the em dash -- the same partner for the other `DECOR_MARK`
MUTANT	R30 id cell: the decoration strip is GONE (re-inject it on the reading -- the reported defect, which the reading alone does not fix)	plan_memo_tables.py	8b3a898e4eb0209e	(R30 id) an id cell `***` renders literally too: the strip took the whole run whatever its length, so a longer run was as blank as a shorter one -- unkeyed, rc 2|(R30 id) an id cell `**` is an unmatched strong-emphasis run: §6.2 pairs nothing with it and a reader sees `**`, so the row is UNKEYED -- a schema miss, rc 2.  The reported shape: before R30 the unconditional strip made it the empty-string blank and this row -- marker in its declaring field, a `Deps` edge -- exited 0|(R30 id) an id cell `*` is a single unmatched delimiter: the same run one character short, and not a `DECOR_MARK` at all -- unkeyed, rc 2|(R30 id) an id cell `` ` `` is one backtick that opens no code span (§6.1 wants a closing backtick string): a reader sees the backtick, so the row is unkeyed, rc 2|(R30 id) an id cell ``` `` ``` is ONE backtick string of length 2 that closes nothing (§6.1: the closer must be of EQUAL length), so it renders literally.  The case that says the pairing authority is the inline LEXER and not the id grammar's decoration walk: that walk reads two `DECOR_MARKS`, so a hand-rolled peel of matched pairs -- the other shape offered for this fix -- strips it to nothing and calls the row a blank
MUTANT	R30 id cell: the rendering is the READER's, not the disposed stream (which blanks a code span, so `` `?` `` would read as empty)	plan_memo_population.py	682d85053c7074e8	(R30 id) an id cell `` `?` `` is unkeyed: a code span RENDERS its content (§6.1) and a reader sees `?`.  The discriminating case for WHICH rendering: the disposed stream -- what every other predicate over a block reads -- blanks a code span, so under it this cell is empty and the row is a blank, which is the silent-skip class re-opened
MUTANT	R30-3 §6.4: a DEMOTED construct's extent renders NOTHING, so it disposes as a mark and not as a blank (put the blank back: the two sides of a demoted tail become two runs)	plan_memo_stream.py	db932c0c28325fad	(R30-3 §6.4) the same phrase crossing a demoted link's TAIL: `![KIND [](x)UNDETERMINED](img.png)` renders `KIND UNDETERMINED` too, because an empty label contributes an empty string and the tail contributes nothing.  This is the clause the opener case cannot see -- there the whole phrase stands before the tail -- and a tail disposed as a BLANK puts a run boundary through the middle of the phrase
MUTANT	R30-3 §6.4: a RESOLVED image's own `![` is recorded (drop the record: it stands in the stream as literal text, where a reader sees either a picture or nothing at all)	plan_memo_lexer.py	44e6fbd1f1748a96	(R30-3 §6.4) a resolved image's own `![` is a BLANK where it is NOT demoted, and the difference is loud: in `KIND ![UNDETERMINED](img.png)` the phrase STRADDLES the image, and the text around an image and the text of its description are two runs (a reader of the first sees a picture where the second stands), so the row is not kind-undetermined -- but the near-miss is reported as a schema miss at rc 2 rather than passed over.  The discriminating half of the clause above: recording the opener as a blank is what makes the phrase visibly CROSS something, and without the record the `![` is literal text, nothing is straddled, and the run exits 0 saying nothing|(R30-3 §6.4) and the phrase crossing a nested IMAGE's markup: `![KIND ![](i.png)UNDETERMINED](img.png)` renders `KIND UNDETERMINED` (Example 574: a nested image's alt text is folded into the enclosing one's).  The inner `![` is the third piece of markup, and it was recorded nowhere at all -- neither clause above reaches it, since a link's `[` is recorded and a tail is recorded
MUTANT	R30-3 §6.4: a demoted link's `[` stays a mark (re-inject the pop: the bracket stands in the stream and a phrase crossing it is not there)	plan_memo_lexer.py	e1b73b5ed4a7eba0	(R30-3 §6.4) a kind phrase crossing a LINK inside a resolved image description is read: `![KIND [UNDETERMINED](x)](img.png)` renders the alt text `KIND UNDETERMINED` (Example 575), so the row is kind-undetermined and its `Deps` edge is asserted.  The reviewer's shape: the demoted link's `[` was popped off the opener list, stood in the stream, and the row left the census as terminal at rc 0
MUTANT	R31-1 phase order: the header cells are lexed BEFORE the tables bind (drop the pre-resolve: `rendered` then reads a cell that has not been through the inline pass, which is its raw text again -- the same silent skip by the other clause)	plan_memo_memo.py	515987fcac594298	(R31-1 §2.5) a linked memo whose `slice` header spells `#` as `&#35;` IS that schema: the character reference renders `#`, so the table is bound, `8z` is declared an umbrella and the terminal row's `Deps` edge is a naming site.  The reported shape -- before R31 the raw comparison made it a non-schema table and every declaration and assertion in the memo left the run at rc 0
MUTANT	R31-1 §2.5: a table's schema is matched on what its header RENDERS (compare the RAW cell text: a `&#35;` header is no schema and the memo's rows leave the run at rc 0)	plan_memo_tables.py	fa8dba503a06a7d4	(R31-1 §2.5) a linked memo whose `slice` header spells `#` as `&#35;` IS that schema: the character reference renders `#`, so the table is bound, `8z` is declared an umbrella and the terminal row's `Deps` edge is a naming site.  The reported shape -- before R31 the raw comparison made it a non-schema table and every declaration and assertion in the memo left the run at rc 0
MUTANT	R31-2 §6.2: a cleared delimiter is REMOVED from the stack, not flagged in place (re-inject the in-place mark: every later search walks the dead ones again)	plan_memo_emphasis.py	da08f7860c048239	emphasis matching is linear: N unmatched delimiter runs cost O(N) work (the Appendix's openers_bottom)
MUTANT	R31-3 raw seed: a token asks the ONE span that can reach it (re-inject the sum over every span: N tokens x N file names on one line)	plan-memo-umbrella-check.py	7ee99762d43091e3	the always-run raw-content seed is linear: N id tokens and N file names on one raw line are one pass, not a span sum per token
MUTANT	R31-4 index: every branch's literal is IN the index (drop one keyword: that phrase's sites leave the index silently and stop being licensable)	plan_memo_roles.py	890a76172f27b2f4	PROPERTY: the licensing rule's backward index decides what the whole preceding text decides, at every position of a generated corpus -- and every phrase opens with a literal the index holds
MUTANT	R31-4 index: no phrase quantifies the last character of its own literal (re-spell `mint(?:s\ped\ping)?` as `mints?(?:ed\ping)?`: the index keys on `mints` and `mint ` stops being licensable)	plan_memo_roles.py	02b7e642fa6a7825	PROPERTY: the licensing rule's backward index decides what the whole preceding text decides, at every position of a generated corpus -- and every phrase opens with a literal the index holds
MUTANT	R31-4 index: the LAST candidate before the mention is the one applied (take the first: a match beginning earlier cannot reach the mention, which is the whole argument)	plan_memo_roles.py	07efaed302fcc318	PROPERTY: the licensing rule's backward index decides what the whole preceding text decides, at every position of a generated corpus -- and every phrase opens with a literal the index holds
MUTANT	R31-4 index: the index keys on a branch's WHOLE literal prefix (key on the first four letters: `deri` is a superset that still matches, so only the structural half reports it)	plan_memo_roles.py	0ca7085b3449cfa3	PROPERTY: the licensing rule's backward index decides what the whole preceding text decides, at every position of a generated corpus -- and every phrase opens with a literal the index holds
MUTANT	R31-4 licensing: the backward look starts at the last offset a phrase may begin at (re-inject R24's search from the block's start: one growing prefix per mention)	plan_memo_roles.py	0281dfb6dba8f0df	the licensing rule's backward look is linear: N mentions hand LICENSE_BEFORE O(N) characters in all, not one growing prefix each
MUTANT	R31-4 licensing: the index offers the candidates rather than none (return an empty index: the cheapest possible bound, which licenses nothing and passes every upper bound)	plan_memo_roles.py	5c7f992a64ae437a	the licensing rule's backward look is linear: N mentions hand LICENSE_BEFORE O(N) characters in all, not one growing prefix each
MUTANT	R32 attribution: a stale `module.symbol` is found (put back the pre-split spelling of the disposition, which moved to `plan_memo_stream.py` at `e7b49ed4`)	plan_memo_tokens.py	52b33d174a29bc75	PROPERTY: every written `module.symbol` attribution names the module that DEFINES that symbol (the class five touch-time splits left with no detector)
MUTANT	R32 index: the keyword set is derived per BRANCH, not per tuple entry (collapse the split: an entry holding two alternatives indexes only the first)	plan_memo_roles.py	131822d9956d59c7	PROPERTY: the licensing rule's backward index decides what the whole preceding text decides, at every position of a generated corpus -- and every phrase opens with a literal the index holds
MUTANT	R32 seams: a seam whose names NOTHING imports is red (rename one to a name no module imports: the allow-list is satisfied vacuously)	plan_memo_selftest_records.py	49c21aec3a34301f	PROPERTY: every import-graph seam this suite states in prose is true of the imports (an "only importer of X" is a claim about the COMPLEMENT)
MUTANT	R32 seams: a seam's allow-list is the set that may import it (widen one to every module: an "only importer" nobody can violate is a sentence about nothing)	plan_memo_selftest_records.py	2359b18336500f74	PROPERTY: every import-graph seam this suite states in prose is true of the imports (an "only importer of X" is a claim about the COMPLEMENT)
MUTANT	R32 §6.1: a code span's line endings are converted to spaces BEFORE the one-space trim (drop the conversion: the raw line ending stands in the reader's text and the trim never fires)	plan_memo_stream.py	19e7e42685b4957a	CommonMark 0.31.2 §6.1: a code span READS as the text the spec's own html puts inside `<code>` (line endings converted, then the one-space trim)
MUTANT	R33-1 appositive: the boundary admits a REAL row noun (refuse every appositive: the two negatives stay green and the attribution stops happening)	plan_memo_tables.py	81d6b6baa7cd7b44	(R33-1) `Slice 9z — **UMBRELLA, …**` attributes the marker to the named row: the appositive reading, unchanged.  The control's constant -- what R33-1 narrows is where the search may BEGIN, never what a real row noun means
MUTANT	R33-1 appositive: the row noun carries a LEFT boundary (drop `BEFORE`: the search starts inside a longer word and the containing row is read as a pointer)	plan_memo_tables.py	0a37f77c3ed0667c	(R33-1) `Subslice 9z — **UMBRELLA, …**` attributes NOTHING: `Subslice` is one word and the row noun inside it is not a row noun, so the field is the containing row's OWN declaration.  The reported shape -- read without a left boundary the search started mid-word, the row became a pointer and the run FABRICATED an UMBRELLA-MARK|(R33-1) `xSlice 9z — …` likewise: the boundary is a fact of the GRAMMAR (an ASCII alphanumeric may not stand before the noun), not a list of words that happen to end in one -- so a prefix nobody thought of is covered by the same clause
MUTANT	R33-2 dashes: the set holds the EN dash (drop it: `KIND – UNDETERMINED` reads as terminal and the row's `Deps` is never asserted)	plan_memo_ids.py	9823784e01e82572	(R33-2) `KIND – UNDETERMINED` with an EN DASH declares the unsettled kind, so the row's nonempty `Deps` edge is asserted.  The reported shape: this one spelling was missing from the phrase's character class while the row grammar beside it accepted it, and the row left the run at rc 0
MUTANT	R33-2 dashes: the set is the three these documents use, not any punctuation (widen it: a `/` separator would declare a kind)	plan_memo_ids.py	485626b0e8b7a741	(R33-2) `KIND / UNDETERMINED` declares NOTHING: the set admits the three dashes these documents use and did not become 'any punctuation' -- the partner that bounds the fix
MUTANT	R33-2 sweep: a second dash class in a checker module is found (re-inject one at the reader that had it)	plan_memo_stream.py	cc9926562335c6ea	PROPERTY: the separator dash set is spelled once, in plan_memo_ids.py (the class three readers spelled three ways, disagreeing)
MUTANT	R34-1 file token: a FRAGMENT tail is still a name (drop the `#?` arm: the resolver follows that run and the lexer breaks it into pieces -- the one direction the correspondence forbids)	plan_memo_tokens.py	b78b09fad11bc1fd	(R34-1) a FRAGMENT tail still ends a name: `See slice-9z-sib.md#acceptance` masks the whole name.  This is the direction the correspondence forbids outright -- the resolver follows that run, stripping the fragment, so a reader that broke it into pieces and reported the id would be the one failure mode the property names
MUTANT	R34-1 file token: a TRAILING-PUNCTUATION tail is still a name (empty the set: a sentence-final period stops ending a file name)	plan_memo_tokens.py	577efd2aad63fa2b	(R34-1) a TRAILING-PUNCTUATION tail still ends a name: `See slice-9z-sib.md.` masks the name and reports nothing.  Here the two readers differ LEGITIMATELY -- the resolver rejects the run with the period, because it is handed a destination the link grammar already bounded, while this reader must find the boundary itself and a sentence-final period is prose
MUTANT	R34-1 file token: the suffix TERMINATES the run (re-inject the alphanumeric test: a prefix of a longer run is masked and the ids in it are hidden)	plan_memo_tokens.py	00b8ba64c0a9de97	(R34-1) `9z+notes.md_tail owns it` REPORTS `9z`: the run continues into more name, `sibling_path` follows no such file, and so the run is no file name and the id in it is prose.  The reported shape -- read as 'the suffix is not followed by an alphanumeric', `9z+notes.md` was masked, the ownership claim produced no site and the run exited 0
MUTANT	R34-2/R38 Deps: emptiness is asked of the READER's rendering (re-inject the prose-scanning stream: a cell of only masked syntax reads empty and the row's edge is never reported -- at BOTH sites at once, since R49-2 gave the question one home)	plan_memo_roles.py	aae5a61e406a3a3e	(R34-2) an umbrella row whose `Deps` cell holds only a bare `.md` name carries an edge: the construct RENDERS something a reader sees, so the cell is not empty and UMBRELLA-CELL is emitted.  Read off the prose-scanning stream -- which blanks exactly the constructs a prose scanner must not read as prose -- the cell was EMPTY and the row's forbidden dependency left the run at rc 0|(R34-2) an umbrella row whose `Deps` cell holds only a citation `[C1]` carries an edge: the construct RENDERS something a reader sees, so the cell is not empty and UMBRELLA-CELL is emitted.  Read off the prose-scanning stream -- which blanks exactly the constructs a prose scanner must not read as prose -- the cell was EMPTY and the row's forbidden dependency left the run at rc 0|(R34-2) an umbrella row whose `Deps` cell holds only a code span ``b.rs`` carries an edge: the construct RENDERS something a reader sees, so the cell is not empty and UMBRELLA-CELL is emitted.  Read off the prose-scanning stream -- which blanks exactly the constructs a prose scanner must not read as prose -- the cell was EMPTY and the row's forbidden dependency left the run at rc 0|(R34-2) an umbrella row whose `Deps` cell holds only an autolink `<https://x.example>` carries an edge: the construct RENDERS something a reader sees, so the cell is not empty and UMBRELLA-CELL is emitted.  Read off the prose-scanning stream -- which blanks exactly the constructs a prose scanner must not read as prose -- the cell was EMPTY and the row's forbidden dependency left the run at rc 0|(R38 seed) a row whose `Deps` cell holds only a bare `.md` name carries an edge, so ordering prose naming no other party reports NOTHING.  Read off the prose-scanning stream the cell was EMPTY and the seed fired -- printing a no-edge finding that quoted the very cell contents it had just called empty|(R38 seed) a row whose `Deps` cell holds only a citation `[C1]` carries an edge, so ordering prose naming no other party reports NOTHING.  Read off the prose-scanning stream the cell was EMPTY and the seed fired -- printing a no-edge finding that quoted the very cell contents it had just called empty|(R38 seed) a row whose `Deps` cell holds only a code span ``b.rs`` carries an edge, so ordering prose naming no other party reports NOTHING.  Read off the prose-scanning stream the cell was EMPTY and the seed fired -- printing a no-edge finding that quoted the very cell contents it had just called empty|(R38 seed) a row whose `Deps` cell holds only an autolink carries an edge, so ordering prose naming no other party reports NOTHING.  Read off the prose-scanning stream the cell was EMPTY and the seed fired -- printing a no-edge finding that quoted the very cell contents it had just called empty
MUTANT	R34-2/R38 Deps: emptiness is judged by SHAPE, not by 'the reading left something' (drop the shape rule: a cell a reader sees a dash in is reported as carrying an edge)	plan_memo_roles.py	67b6d23a3a4a2729	(R34-2) an umbrella row whose `Deps` cell is a hyphen carries NO edge: the reading moved and `is_empty` still decides by SHAPE, so a deliberate blank is still blank.  The partner that bounds the fix -- reading the cell as 'anything the lexer did not blank' would pass the four above and fail these three|(R34-2) an umbrella row whose `Deps` cell is an em dash carries NO edge: the reading moved and `is_empty` still decides by SHAPE, so a deliberate blank is still blank.  The partner that bounds the fix -- reading the cell as 'anything the lexer did not blank' would pass the four above and fail these three|(R34-2) an umbrella row whose `Deps` cell is an en dash carries NO edge: the reading moved and `is_empty` still decides by SHAPE, so a deliberate blank is still blank.  The partner that bounds the fix -- reading the cell as 'anything the lexer did not blank' would pass the four above and fail these three|(R38 seed) the partner that bounds it: a `Deps` cell that IS a deliberate blank (an em dash) with the same ordering prose still reports the no-edge finding -- the reading moved and `is_empty` still decides by SHAPE
MUTANT	R35 file token: a fragment/query tail EXTENDS the span (stop at the suffix instead: the id in the tail is left for the naming scan)	plan_memo_tokens.py	2f9e66fd7a784a21	(R35) `See notes.md#9z owns it` reports NOTHING: the resolver follows that whole run, stripping the fragment, so the lexer must span it whole.  The reported shape -- R34-1 stopped the span at the suffix and the id in the fragment became a site|(R35) the QUERY spelling too: `See notes.md?q=9z owns it` -- the resolver strips a query exactly as it strips a fragment, so one rule covers both and neither is a special case|PROPERTY: a run the sibling resolver FOLLOWS is one whole file span to the lexer -- never a prefix with the remainder left for the naming scan (the direction the correspondence forbids)
MUTANT	R36-2 file token: the run boundary is REUSED across the suffixes of one run (pass 0 as the cache: every suffix re-walks to the same boundary)	plan_memo_tokens.py	0ea5b324579ec62c	file_and_cite_spans is linear: N parenthesis groups are one pass, not a re-scan from every start position
MUTANT	R4-1 gate: an unresolved IMAGE reference is not a memo miss	plan_memo_memo.py	e1946b154b23ae93	(image) an undefined reference image `![diagram][missing-image]` is literal syntax, not an unresolved memo reference: rc 0
MUTANT	R4-2 population: the destination path is percent-decoded	plan_memo_sibling.py	9968ee07b4109888	(link) a percent-encoded destination `slice%20sib.md` links the file `slice sib.md`, as `<slice sib.md>` does
MUTANT	R4-3 pass: a code span swallows a `]` (brackets inside it are not delimiters)	plan_memo_lexer.py	10de1d65bfa2b356	(link) a link inside a code span is not a link (A x B)|(rc) a code-quoted link to an absent file is not a link: rc 0|(span) a backtick BEFORE the `]` opens a code span that swallows it: `[not a `link](absent.md)`` is code, no link, rc 0
MUTANT	R4-3 pass: one inline pass over the RAW text (re-introduce the code pre-mask)	plan_memo_lexer.py	28ac532a9148375f	(span) a backtick inside a link DESTINATION is consumed by the link, not a code span: `[sib](slice`x`.md)` links the sibling
MUTANT	R42 §6.4: blank a demoted code span outright instead of routing it through `id_only` (the decoration exception stops surviving into alt text: `` `9z`7z `` glues)	plan_memo_stream.py	2826685162305bcf	(R42 §6.4/§6.1) a kind marker wholly inside a CODE SPAN inside a resolved image description is read: §6.4 reduces the description to the plain string content of its inline children, so the alt text holds the marker and the row's `Deps` edge is asserted.  The reported shape: the span stayed in `lx.code`, the disposition blanked the whole marker, the row read TERMINAL and the run exited 0 -- while the identical marker in a LINK inside the same image exited 1
MUTANT	R42 §6.4: stop demoting AUTOLINKS into the description (§6.5 back to masked: the URI §6.5 makes the link text contributes nothing and the id in it is reported nowhere)	plan_memo_lexer.py	6067f348fdcdbed4	(R42 §6.4) a declared id inside a §6.5 autolink inside a RESOLVED image description is one reported site, the same as in bare prose: §6.4 reduces the description to the plain string content of its inline children, so every row of §3.0b's closed list contributes its text and none of its markup.  §6.1 and §6.5 were the two rows the image-close branch never demoted -- a code span there BLANKED a kind marker and an autolink contributed nothing at all
MUTANT	R42 §6.4: stop demoting CODE SPANS into the description (§6.1 back to masked: a kind marker there is blanked and the row leaves the census at rc 0)	plan_memo_lexer.py	3ca816e5f532b4b1	(R42 §6.4/§6.1) a kind marker wholly inside a CODE SPAN inside a resolved image description is read: §6.4 reduces the description to the plain string content of its inline children, so the alt text holds the marker and the row's `Deps` edge is asserted.  The reported shape: the span stayed in `lx.code`, the disposition blanked the whole marker, the row read TERMINAL and the run exited 0 -- while the identical marker in a LINK inside the same image exited 1
MUTANT	R42-10: the REJECTED header-near-miss predicate (re-inject it: a renamed header is reported whether or not the table declares anything)	plan_memo_population.py	b42b4f438b8391b9	(R42-10) the SAME unbound header with no kind phrase in it is silent -- the gate is keyed on the CLAIM, not on the header.  This is the control a header-near-miss predicate fails: `No.` differs from `#` there too, and that table declares nothing
MUTANT	R42-10: the REJECTED id-shape predicate (re-inject it: every unbound table whose EVERY BODY ROW starts with a row id -- 151 of them over the corpus §8 names)	plan_memo_population.py	6249128f1c3bcbee	(R42-10) a documentation table whose first column is ID-SHAPED is silent: a landing record's review-round table (`obj`, `R1`...`R7`) keys its rows and declares nothing.  This is the control the REJECTED predicate fails -- it reports 151 such tables over the 141 plan memos on this disk
MUTANT	R42-10: the gate asks of the whole POPULATION (narrow it to `main`, where the schema-miss gate already asks -- the linked half is the whole defect)	plan_memo_population.py	b72dd935d31227f7	(R31-1 §2.5) a header cell rendering `$` (`&#36;`) is NOT the `slice` schema: the reading moved and the COMPARISON did not loosen, so the table stays non-schema and declares nothing.  The partner that bounds the fix -- a match that normalised the header instead of rendering it would pass the three above and fail this one -- and the witness is the R42-10 gate naming the unbound claim, which a bound table would not produce|(R42-10) a LINKED memo whose slice table binds to no schema -- its header reads `No.` where the schema reads `#` -- but carries an umbrella row with a `Deps` edge: the claim is made and the whole table leaves the census.  It exited 0, with no gate reporting it, because the schema-miss gate asks only of `main`
MUTANT	R42-10: the phrase is read off the RENDERED cell (read the raw text -- a marker inside a code span becomes a claim)	plan_memo_population.py	5429999ea68c8443	(R42-10) the phrase is read off the RENDERED cell, as the census reads a declaring field: a marker inside a CODE SPAN in an unbound table is not a claim, exactly as it is not one in a bound table's declaring field
MUTANT	R42-10: the unbound-claim gate exists (drop the call -- a linked memo's unbound table makes its census claim and leaves the population at rc 0)	plan_memo_population.py	0e05e6027e1a158e	(R31-1 §2.5) a header cell rendering `$` (`&#36;`) is NOT the `slice` schema: the reading moved and the COMPARISON did not loosen, so the table stays non-schema and declares nothing.  The partner that bounds the fix -- a match that normalised the header instead of rendering it would pass the three above and fail this one -- and the witness is the R42-10 gate naming the unbound claim, which a bound table would not produce|(R42-10) a LINKED memo whose slice table binds to no schema -- its header reads `No.` where the schema reads `#` -- but carries an umbrella row with a `Deps` edge: the claim is made and the whole table leaves the census.  It exited 0, with no gate reporting it, because the schema-miss gate asks only of `main`
MUTANT	R42-4 modes: accept a known flag in any mode (drop the per-mode flag check: `--mutants memo.md` runs the memo report and returns 0 without executing one mutant)	plan-memo-umbrella-check.py	8258b51f77051970	PROPERTY: the entry point's CLI contract is per MODE -- a closed option set whose complement is refused, and for each mode the flags it accepts and the positional count it takes (a known flag in the wrong mode returned 0 for the wrong operation)
MUTANT	R42-4 modes: drop the positional-count half (two memo paths, or none, run whichever mode was selected and say nothing -- the direction the flag check alone cannot see)	plan-memo-umbrella-check.py	5dfb87518711bd08	PROPERTY: the entry point's CLI contract is per MODE -- a closed option set whose complement is refused, and for each mode the flags it accepts and the positional count it takes (a known flag in the wrong mode returned 0 for the wrong operation)
MUTANT	R42-4 report bytes: stop escaping the checker's OWN report (a memo-controlled ESC reaches the default report and can clear a terminal or forge a line in a captured log)	plan-memo-umbrella-check.py	c1d4fc758949e300	PROPERTY: every line the run REPORTS goes through the escape -- measured over the EMIT SITES, the subject the escape function's own control cannot reach|PROPERTY: no C0 control character or DEL from a MEMO reaches stdout through the checker's own report (the property the emit-site sweep is a proxy for, asked by running the program over a hostile memo)
MUTANT	R42-6 §6.1: drop the trim on a demoted span (the padding stands in the alt text and a declared id split by it is two tokens: `Slice 9` + `z`, no site, rc 0)	plan_memo_stream.py	1102153605b48681	(R42 §6.1/§6.4) a PADDED code span inside a resolved image description renders its TRIMMED content: §6.1 removes one leading and one trailing space when the content begins and ends with one, and §6.4 uses that string -- so `` ![Slot #11-zz-alph` a ` owns it](img.png) `` has the alt text `Slot #11-zz-alpha owns it` and the slot is ONE reported site.  Read verbatim it splits into `#11-zz-alph` and `a`, which are declared nowhere: no site, rc 0, and no residue to say the checker could not read it
MUTANT	R42-6 §6.1: trim unconditionally (drop the spec's own "not entirely spaces" arm: an all-space span is trimmed, which JOINS what the reader sees separated)	plan_memo_stream.py	f44cf7511621c14e	(R42 §6.1/§6.4) the arm the spec spells out, and the one an unconditional trim breaks: a span that consists ENTIRELY of spaces is NOT trimmed ("but does not consist entirely of space characters"), so `` ![Slot #11-zz-alpha`  `owns it](img.png) `` keeps the separator and the slug stands alone -- trimmed, it would fuse with what follows and the site would vanish.  ⚠ The span is the ONLY separator here, deliberately: written with a space after the closing backtick the control stayed green under the mutant, because that space did the separating and the claim was never tested
MUTANT	R42-8 channel: narrow the predicate back to a `%` over a literal (the list of shapes it was written against: every other way of building a line goes unread)	plan_memo_selftest_records.py	778ad6f9603c0cc7	PROPERTY: every line the run REPORTS goes through the escape -- measured over the EMIT SITES, the subject the escape function's own control cannot reach
MUTANT	R42-8 channel: unwrap the population summary (an `IfExp`, memo-controlled through `pop.display` -- the exact shape the %-only predicate could not see)	plan-memo-umbrella-check.py	204a46f198e941bc	PROPERTY: every line the run REPORTS goes through the escape -- measured over the EMIT SITES, the subject the escape function's own control cannot reach
MUTANT	R42-8 §6.6 seed: ... and not from a CELL either -- its own site, which the paragraph mutant leaves standing	plan_memo_memo.py	792e95a295ef2989	(R42-8 §6.6 seed) the same in a CELL, which is the other half of the seed's population and its own site: the paragraph control cannot see it
MUTANT	R42-8 §6.6 seed: a demoted span is not seeded from a PARAGRAPH (re-inject the seed)	plan_memo_memo.py	a16e8b6a65c78c07	(R42-8 §6.6 seed) a raw HTML span DEMOTED into a resolved image description is not seeded -- the naming scan has already read it -- and a span crossing a LINE ENDING is not seeded per line either
MUTANT	R42-8 §6.6: the disposition READS that record (re-mask the demoted span -- the tag is still recorded, so the seed still skips it and only the READING moves)	plan_memo_stream.py	95a55b7273db0c48	(R42-8 §6.6) a raw HTML span inside a resolved image description is the alt's own TEXT, so the kind marker is NOT read across it: `![UMBRELLA, not a <span>terminal unit](img.png)` has the alt text `UMBRELLA, not a <span>terminal unit` (cmark 0.31.2 and commonmark.js 0.31.2 both, the latter escaping the brackets -- they are content) and the marker phrase is not in it.  Masked as markup the span JOINED its two sides and the row exited 1 on an ownership claim nobody made: a FABRICATED finding, the opposite direction from every other §6.4 defect on this surface|(R42-8 §6.6) an id inside a raw HTML ATTRIBUTE inside a resolved image description is a reported naming site: the alt text spells the tag out, so `9z owns it` is document text there.  This is what a blank mask cannot give -- it would not join the sides either, and would still hide the id
MUTANT	R42-8 §6.6: the lexer records a raw HTML span demoted into a resolved image description (drop the range -- the span stays markup, masked and seeded as in bare prose)	plan_memo_lexer.py	5a7d121c5b049f38	(R42-8 §6.6 seed) a raw HTML span DEMOTED into a resolved image description is not seeded -- the naming scan has already read it -- and a span crossing a LINE ENDING is not seeded per line either|(R42-8 §6.6 seed) the same in a CELL, which is the other half of the seed's population and its own site: the paragraph control cannot see it|(R42-8 §6.6) a raw HTML span inside a resolved image description is the alt's own TEXT, so the kind marker is NOT read across it: `![UMBRELLA, not a <span>terminal unit](img.png)` has the alt text `UMBRELLA, not a <span>terminal unit` (cmark 0.31.2 and commonmark.js 0.31.2 both, the latter escaping the brackets -- they are content) and the marker phrase is not in it.  Masked as markup the span JOINED its two sides and the row exited 1 on an ownership claim nobody made: a FABRICATED finding, the opposite direction from every other §6.4 defect on this surface|(R42-8 §6.6) an id inside a raw HTML ATTRIBUTE inside a resolved image description is a reported naming site: the alt text spells the tag out, so `9z owns it` is document text there.  This is what a blank mask cannot give -- it would not join the sides either, and would still hide the id
MUTANT	R42-9: narrow the blank-id contradiction back to the MARKER alone (the over-narrowing a review round reported: an UNDETERMINED row with a `Deps` cell exits 0 and no gate reports it)	plan_memo_population.py	08b51715c5037283	(R42-9) a blank id cell whose declaring field spells KIND UNDETERMINED is the same contradiction as the marker: §5 gives an undetermined row the no-owner obligation an umbrella has, and keyed by nothing it is absent from `ids`, so assertion (b) never reads its `Deps` edge -- rc 2, not the silent 0 the marker-only predicate gave
MUTANT	R42-9: widen it to every kind phrase (the POINTER arm back: the #506 memo's `Function`/`eval` row at `:1985` is a legitimate empty-id row and would become a FATAL)	plan_memo_population.py	5e35e1eb2c5edcf7	(R42-9) and the arm that WAS refuted stays refuted: the POINTER phrase on a blank id is the legitimate shape (`declaring_rows` names the #506 memo's `Function`/`eval` row, `:1985`) -- another row owns this one, which is what an unkeyed row is for
MUTANT	R42: accept the blank-and-marked row again (drop the contradiction clause: the row is keyed by nothing, assertion (b) never reads its Deps edge, rc 0)	plan_memo_population.py	1e9b8d0ba2a0d9c2	(R42) a DELIBERATE blank id cell whose declaring field claims the row is an umbrella is a contradiction, not a non-row: keyed by nothing it is absent from `ids`, so assertion (b) never reads its `Deps` edge -- reported as a schema miss at rc 2 rather than passed over at rc 0
MUTANT	R42: read the declaring field from the wrong moment (`row.field`, which this pass runs BEFORE -- it is None here, so every blank row reads as terminal and the clause is vacuous)	plan_memo_population.py	ff5102c36bc80061	(R42) a DELIBERATE blank id cell whose declaring field claims the row is an umbrella is a contradiction, not a non-row: keyed by nothing it is absent from `ids`, so assertion (b) never reads its `Deps` edge -- reported as a schema miss at rc 2 rather than passed over at rc 0
MUTANT	R45 GFM §4.10: a delimiter cell is >=1 hyphen (re-inject a three-hyphen minimum -- the dialect the review finding asked for, which cmark-gfm refutes)	plan_memo_blocks.py	04a266de122df1a8	(R45 GFM §4.10) a delimiter cell of centred admits the table exactly as `---` does -- "cells whose only content are hyphens (-), and optionally, a leading or trailing colon (:)" sets no minimum length.  Verified against cmark-gfm 0.29.0.gfm.13, which makes a `<table>` from all four; a review finding asking for a three-hyphen minimum is the FP this pins|(R45 GFM §4.10) a delimiter cell of left-aligned admits the table exactly as `---` does -- "cells whose only content are hyphens (-), and optionally, a leading or trailing colon (:)" sets no minimum length.  Verified against cmark-gfm 0.29.0.gfm.13, which makes a `<table>` from all four; a review finding asking for a three-hyphen minimum is the FP this pins|(R45 GFM §4.10) a delimiter cell of one hyphen admits the table exactly as `---` does -- "cells whose only content are hyphens (-), and optionally, a leading or trailing colon (:)" sets no minimum length.  Verified against cmark-gfm 0.29.0.gfm.13, which makes a `<table>` from all four; a review finding asking for a three-hyphen minimum is the FP this pins|(R45 GFM §4.10) a delimiter cell of right-aligned admits the table exactly as `---` does -- "cells whose only content are hyphens (-), and optionally, a leading or trailing colon (:)" sets no minimum length.  Verified against cmark-gfm 0.29.0.gfm.13, which makes a `<table>` from all four; a review finding asking for a three-hyphen minimum is the FP this pins
MUTANT	R45 scope: `_unkeyed` asks of the whole POPULATION (scope it to `main` -- a linked memo's unkeyed row goes unreported at rc 0)	plan_memo_population.py	818a083f2b193e74	(R45 scope) a LINKED memo's UNKEYED row is reported: `_unkeyed` asks of the whole population, not of `main`.  Scoped to `main` the sibling's `xxxxWz` id cell declares nothing, the row is unscanned and the run exits 0 -- the I-C class, in the memo the census exists to reach
MUTANT	R45 scope: the table-miss loop asks of the whole POPULATION (scope it to `main` -- a linked memo's width miss goes unreported at rc 0)	plan_memo_population.py	54c96d66cf11145d	(R45 scope) a LINKED memo's table WIDTH miss is reported: the table-miss loop asks of the whole population too.  Scoped to `main` the sibling's 7-cell row under a 6-cell header leaves the run at 0 with nothing saying the row was skipped
MUTANT	R45 §6.1: line endings become spaces BEFORE the trim asks (neuter step one -- the raw test then declines the trim on a span padded with a line ending, and the slug splits in silence)	plan_memo_stream.py	9164f4acbc951383	(R42 §6.1/§6.4) §6.1's TWO steps run in ORDER: a code span padded with a LINE ENDING inside a resolved image description is normalised to a space FIRST and only then trimmed, so `` ![Slot #11-zz-alph`<NL>a ` owns it](img.png) `` has the alt text `Slot #11-zz-alpha owns it` and the slot is ONE reported site.  Written against the RAW content the trim declines -- `\\n` is not a space -- the slug splits into `#11-zz-alph` and `a`, declared nowhere, and the run exits 0 saying nothing.  ⚠ This is the half of the R42-7 fix that shipped UNPINNED: both R42-6 mutants patch the trim, so step one could be made a no-op with every control green
MUTANT	R45 §6.4 falsifier: a DEMOTED autolink emits no `<a href=` (un-filter the count)	plan_memo_selftest_conformance.py	e17e4041e9a9e837	CommonMark 0.31.2 §6.4: Phase 2's inline claim agrees with the spec's own html for every §3.0b family DEMOTED into a resolved image description (the cross-product the corpus cannot reach)
MUTANT	R45 §6.4 falsifier: a DEMOTED code span emits no `<code>` (un-filter the count -- the shape that shipped: `![a `b` c](img.png)` reported "the html emits 0 `<code>`, Phase 2 claims 1" against cmark's own `alt="a b c"`)	plan_memo_selftest_conformance.py	0008135e5a059e13	CommonMark 0.31.2 §6.4: Phase 2's inline claim agrees with the spec's own html for every §3.0b family DEMOTED into a resolved image description (the cross-product the corpus cannot reach)
MUTANT	R45 §6.4 falsifier: a DEMOTED raw HTML span is not masked verbatim (un-filter the span list -- the R42-8 fix, now pinned by a control the corpus can reach)	plan_memo_selftest_conformance.py	cf7be9124f532b10	CommonMark 0.31.2 §6.4: Phase 2's inline claim agrees with the spec's own html for every §3.0b family DEMOTED into a resolved image description (the cross-product the corpus cannot reach)
MUTANT	R45 §6.4 falsifier: the filter must not be applied EVERYWHERE -- invert it, so the count keeps only the DEMOTED spans and a bare code span claims none.  The direction the demoted arm alone cannot see: a filter that simply stopped counting passes it	plan_memo_selftest_conformance.py	62fe4c8a980b6b7a	CommonMark 0.31.2 §6.4: Phase 2's inline claim agrees with the spec's own html for every §3.0b family DEMOTED into a resolved image description (the cross-product the corpus cannot reach)
MUTANT	R47-1 separator: the row-noun separator COMPOSES `DASH` (re-spell the narrower ASCII-only set -- an en/em-dash claim matches no appositive and the numeric id goes unreported at rc 0)	plan_memo_tables.py	2869baefa63d76af	(R47-1) a numeric id introduced by a row noun and an EM dash is one reported site: the separator composes `plan_memo_ids.DASH`, which is the ONE home of the three spellings, rather than re-spelling a narrower set.  With the ASCII hyphen alone, `Slice — 9 owns the close rule.` matched no appositive, the bare pass deliberately ignores a numeric id, and the ownership claim produced no site at rc 0|(R47-1) a numeric id introduced by a row noun and an EN dash is one reported site: the separator composes `plan_memo_ids.DASH`, which is the ONE home of the three spellings, rather than re-spelling a narrower set.  With the ASCII hyphen alone, `Slice — 9 owns the close rule.` matched no appositive, the bare pass deliberately ignores a numeric id, and the ownership claim produced no site at rc 0
MUTANT	R47-2 gate: the disagreement arm requires a STRADDLE (widen it to every disagreement -- a phrase quoted WHOLE becomes a claim, against I-A)	plan_memo_stream.py	04379274e38a0e65	(R47-2) and the I-A arm survives BY CONSTRUCTION, not by an exemption: a phrase quoted WHOLE in an unbound table still declares nothing.  `kind_disagreements` requires a STRADDLE, so the quoted form is not a disagreement and needed no case carved out of the widened gate
MUTANT	R47-2 gate: the unbound-claim gate asks BOTH readings (drop the disagreement arm -- a claim straddling a masked span is invisible to the stream and the table leaves the census at rc 0)	plan_memo_population.py	d8133126055aa573	(R47-2) a LINKED memo's unbound table whose kind claim STRADDLES a masked span is the same miss as a plain one: `` **UMBRELLA, not a `terminal` unit.** `` is what a reader sees and what the disposed stream does not, so a stream-only search found nothing and the run exited 0 with the table's declarations and `Deps` gone.  Asked through `kind_disagreements`, the CANONICAL question `_kind_residue` already gates a bound row with -- not a second rule
MUTANT	R47-4 gap: a word gap is what a READER sees (re-spell the marker as a `re.escape`d literal -- U+0020 and nothing else, so `&nbsp;` drops the claim at rc 0)	plan_memo_stream.py	badd43691eb3cb4d	(R47-4) the marker phrase written with `&#9;` (a tab) is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0|(R47-4) the marker phrase written with `&nbsp;` (U+00A0 -- the spelling a human actually types) is the marker: a word GAP is what a READER sees between two words, not U+0020 in particular.  cmark renders every one of these as whitespace, so a census that reads only U+0020 drops the claim and the row leaves as an active terminal at rc 0
MUTANT	R47-4 gap: the UNDETERMINED phrase composes the SAME gap (re-spell its `\\s` under `re.ASCII`, which is ASCII whitespace and not U+00A0)	plan_memo_stream.py	af9cf17207fecace	(R47-4) the UNDETERMINED phrase has its own spelling of the gap and the same defect: under `re.ASCII` its `\\s` is ASCII whitespace, so a U+00A0 between `KIND` and `UNDETERMINED` read as no kind at all.  The scope `(?u:...)` widens the GAP alone and leaves the ASCII case folding, which is why the phrase is still not matched by a long-s spelling
MUTANT	R47-4 gap: the gap is a WHITESPACE class, not a wildcard (widen it to `.` -- every arm above still passes, and only the word-boundary negative catches it)	plan_memo_stream.py	279f9763c71614fa	(R47-4) a NON-whitespace character where a gap would be is not a gap: `UMBRELLA, not-a terminal unit` is not the marker.  The word boundaries cannot see this -- they guard the two ENDS of the phrase -- so it is the only arm that fails when the gap class is widened from whitespace to a wildcard
MUTANT	R47-5 scope: every MISS of a table is reported (truncate the loop at line ~107)	plan_memo_population.py	f7b21549bf2409e8	(R47-5 scope) TWO table misses in one memo are both reported: the loop over a table's misses walks all of them.  Truncated, the second row is unscanned at rc 2 with only the first named -- a reader fixes one and believes the table is clean
MUTANT	R47-5 scope: every ROW of an unbound table is asked (truncate the loop at line ~214)	plan_memo_population.py	8394d4b502b26c64	(R42-10) a LINKED memo whose slice table binds to no schema -- its header reads `No.` where the schema reads `#` -- but carries an umbrella row with a `Deps` edge: the claim is made and the whole table leaves the census.  It exited 0, with no gate reporting it, because the schema-miss gate asks only of `main`
MUTANT	R47-5 scope: every SCHEMA ROW is read for the unkeyed miss (truncate the loop at line ~265)	plan_memo_population.py	457a84fd27dfab0f	(R47-5 scope) a memo's SECOND schema row is read for the unkeyed miss too: the sibling's first row is keyed and its second is not.  Truncated to the first row the unkeyed one declares nothing and leaves the census at rc 0
MUTANT	R47-5 scope: every SCHEMA ROW of a memo is declared (truncate the loop at line ~152)	plan_memo_population.py	f07b7282a30fe6d7	a self-declaring row that MENTIONS a sibling stays in the population
MUTANT	R47-5 scope: every TABLE of a memo is asked for an unbound claim (truncate the loop at line ~211)	plan_memo_population.py	65ebca88b187a254	(R47-5 scope) a memo's SECOND table is asked for an unbound claim too: the sibling carries a bound table and then an unbound one whose row carries the marker.  Truncated to the first table the claim is silent and the run exits 0
MUTANT	R47-5 scope: every TABLE of a memo is bound (truncate the loop at line ~106)	plan_memo_population.py	1bc4e933d2444677	(rc) a schema body row whose width differs from its header is rc 2
MUTANT	R47-5 scope: every UNANSWERED reference of a memo is reported (truncate the loop at line ~94)	plan_memo_population.py	1ed406c0491374ae	(R47-5 scope) TWO unresolved references are both reported: the loop over a memo's unanswered references walks all of them.  Truncated it reports the first and the second reference's memo is missing from the population with nothing saying so
MUTANT	R47-5 scope: every declared id is given a KIND (truncate the loop at line ~123)	plan_memo_population.py	a3aa6f44f91959a5	(R47-5 scope) EVERY declared id is given a kind, not just the first: the loop over `self.ids` walks the whole map.  Truncated, only one row is classified and the census under-counts the no-owner rows it exists to take
MUTANT	R47-5 scope: every kind phrase the residue names is reported (truncate the loop at line ~388)	plan_memo_population.py	4c6e62d901adae08	(R47-5 scope) TWO kind phrases straddling masked spans in ONE declaring field are both reported: `_kind_residue` walks every phrase `kind_disagreements` returns.  Truncated, the row's second undecidable kind is silent
MUTANT	R47-5 scope: the population walk queues EVERY link of a memo (truncate the loop at line ~88)	plan_memo_population.py	d40c4571a6a34cde	diagnostics name a memo relative to the root memo's directory: `a/child.md` and `b/child.md` are two files, and a memo outside that directory is named by its absolute path
MUTANT	R47-6 refusal: ... and the ORDINARY sibling IS walked (drop every link -- the half that says "nothing names these" is not also true of a checker that reads no destinations)	plan_memo_memo.py	96669ed26c491362	a destination the sibling resolver REFUSES leaves no finding, seed or note naming it -- the stated policy, pinned rather than merely current
MUTANT	R47-6 refusal: a REFUSED destination stays silent (reverse the polarity -- report it, the reading §1's could-not-scan rule would ask for and the one §8 carries as open)	plan_memo_memo.py	5084c19cde17b408	a destination the sibling resolver REFUSES leaves no finding, seed or note naming it -- the stated policy, pinned rather than merely current
MUTANT	R48-2 spellings: `_phrases` returns EVERY occurrence (re-inject `search` -- one Match per phrase, so a field using both undetermined spellings contributes one and the consistency gate sees a set of size one)	plan_memo_population.py	da2edca7e2b08e35	(R48-2) TWO undetermined spellings in ONE declaring field are both collected: the KIND-SPELLING gate is about the DOCUMENT's spellings, so a field using both reports exactly as two rows using one each do.  Reading only the first match, the set had size one and the run exited 0 on the condition that gate exists for
MUTANT	R48-2 spellings: every collected occurrence reaches `spellings` (truncate the loop -- the shape the derived scope ratchet demanded a row for the moment the loop was written)	plan_memo_population.py	b5f7e6617152a18d	(R48-2) TWO undetermined spellings in ONE declaring field are both collected: the KIND-SPELLING gate is about the DOCUMENT's spellings, so a field using both reports exactly as two rows using one each do.  Reading only the first match, the set had size one and the run exited 0 on the condition that gate exists for
MUTANT	R49-2 Deps: a resolved LINK is an edge whatever its label renders as (drop the links arm -- `[](slice-9z-sib.md)` reads as an empty cell while the walker follows that very sibling)	plan_memo_roles.py	fe0a2af711a3e9b0	(R49-2) an umbrella's `Deps` cell whose whole content is a resolved link with a punctuation-only label carries an edge: the reader rendering drops a link's TAIL, so the cell had no alphanumeric character and read as EMPTY -- while the population walker was following that very sibling.  A resolved link is an edge whatever its label renders as|(R49-2) an umbrella's `Deps` cell whose whole content is a resolved link with an EMPTY label carries an edge: the reader rendering drops a link's TAIL, so the cell had no alphanumeric character and read as EMPTY -- while the population walker was following that very sibling.  A resolved link is an edge whatever its label renders as|(R51) the links arm reaches the cd-seed as well: a row with ordering vocabulary in its prose and a bare `[](sib.md)` in its `Deps` cell emits NO `ORDER-PROSE?` seed, because the cell is not empty.  Measured 1 -> 0 when the arm landed, and unstated until an audit asked.  ⚠ The alternative reading -- a label-less link names a FILE and never says which ROW the ordering is against -- is recorded in `deps_is_empty`'s docstring; this control is what makes flipping it a decision
MUTANT	R5-1 phase 1: definitions are read from RAW lines (re-inject the inline pre-mask)	plan_memo_blocks.py	001a13af73b97843	(def) a definition is read from RAW lines at a block start: `[sib]: slice`x`.md` keeps its backticks in the destination and the sibling is scanned
MUTANT	R5-3 disposition: a slug is atomic in an id-only run (re-inject the hyphen split)	plan_memo_stream.py	3757f08bcd1ecc75	(span) a `#11-` slug is ATOMIC in an id-only run: `` `#11-zz-alpha / 9z` `` is the document spelling two ids, both reported
MUTANT	R51 claims: EVERY disagreement the canonical question returns is marked (truncate the loop inside `_claims` -- the one the ratchet credited to a mutant that never touched it, because it matched a target PREFIX)	plan_memo_population.py	61254c0f1485eb27	(R51) a blank-id row whose field straddles a masked span with TWO kind phrases names BOTH: `_claims` marks every disagreement the canonical question returns, not the first.  Truncated, the miss reads one kind and a reader deleting that one believes the row is settled
MUTANT	R52 outside-field: the marker-outside-the-declaring-field check asks the CANONICAL question (re-inject the stream-only search -- a marker split across a masked construct certifies nothing and nothing says so)	plan_memo_roles.py	f4615d22b11894ba	(R52) a marker OUTSIDE the declaring field, STRADDLING a masked construct: assertion (a)'s outside-field check searched the disposed STREAM alone, so the straddled spelling was invisible and the row left at rc 0 with only a non-gating seed while the clean spelling exited 1.  Asked through the canonical `_claims` now -- which keeps the quoted case by construction, since a phrase quoted whole is not a straddle
MUTANT	R6-2 locate: a bisect over the line offsets, not a linear scan per site	plan_memo_memo.py	7e2f68d3bf19a882	unresolved_references is linear: <= N*(log2 N + 2) reads of the line-offset table (a bisect per site, not a scan)
MUTANT	R7-1 def: the definition is parsed over the rest of the block (re-inject a 3-line window)	plan_memo_memo.py	49a8a49963c65103	(def) a label spanning FIVE lines is a definition (§4.7 / §6.3: a label may span lines); the later shortcut resolves and the sibling is scanned
MUTANT	R7-2 population: a C0 control character in a decoded destination is rejected	plan_memo_sibling.py	53f7b5d84f0de4ef	a decoded destination with a C0 control character is rejected, never resolved
MUTANT	R7-3 def: a title may not cross a blank line (re-inject the blank into the run)	plan_memo_blocks.py	4753fb73831d3512	(def) `[sib]: child.md "title` whose title crosses a BLANK line is not a definition (commonmark.js: a paragraph): `[sib]` later is an exempt shortcut, rc 0, and the sibling is NOT walked -- its violation is not reported
MUTANT	R8 sweep: a blank line is spaces or tabs only (§2.1)	plan_memo_blocks.py	2cde0641d60aaae5	(span) an NBSP-only line is NOT blank (§4.9: spaces or tabs only), so it does not end the paragraph and the code span crosses it
MUTANT	R8-2 sibling: an OSError from resolve() is the unavailable-sibling miss (unguard it)	plan_memo_sibling.py	f756b22fe4a34424	an OSError from resolve() is the unavailable-sibling schema miss, never an exception
MUTANT	R8-3 bare id: the far side of a `.` is the ASCII id class, not `str.isalnum`	plan_memo_ids.py	ad5c83d3903a50be	(bare) `9z.é` bounds the id (a dotted number is ASCII on both sides)|(bare) `9z.次の工程` bounds the id: the far side of the `.` is not an ASCII id character, so the site is reported
MUTANT	R8-4 row: edge pipes are detected with the space/tab class	plan_memo_blocks.py	7cc0ba119e66af2e	(table) a row opening with an NBSP before its `\p` is not edge-piped: the NBSP is a cell, the header is 7 wide over a 6-cell delimiter, no table
MUTANT	R8-5 sibling: the dedup is a set (re-inject the list membership test)	plan_memo_memo.py	3bbeca326d81a325	linked_files is linear: <= N Path.__eq__ calls over N distinct siblings (a set dedup hashes, a list compares)
MUTANT	R9 #3 row: breaks are partitioned in the one scan (re-inject the per-cell filter)	plan_memo_blocks.py	857f705d663310b3	split_row is linear: <= 64 source lines per character and per cell (breaks partitioned in the one scan)
MUTANT	R9 F1 / R13 seed: every raw HTML-block line is recorded for the seed (re-inject 'indented only')	plan_memo_memo.py	c99093d8f20620fa	(lex-seed) `<pre>\\nSlice 9z owns it\\n</pre>`: the inner line holding the id is seeded (type 1 ends at `</pre>`)|(lex-seed) an HTML-block opener holding a declared id is a seed
MUTANT	R9 F1 seed: only a line holding a `\p` or a declared id is reported	plan-memo-umbrella-check.py	c496f85fe5296353	(lex-seed) an HTML-block line with neither a `\p` nor a declared id is no seed
MUTANT	R9 F1 setext: the underline closes the paragraph (re-inject the join: drop the `block_end` arm, so the run reads on through `===`)	plan_memo_blocks.py	766f385be4b6f019	(setext) `Heading\\n===` is a heading; the `===` underline ends the paragraph, so a code span opened in the heading does not reach the next paragraph's site
MUTANT	R9 F2 I/O: a decode error is the unavailable-memo miss (unguard it)	plan_memo_population.py	4d57ed2f6535d973	an undecodable sibling is the unavailable-linked-memo schema miss, never an exception
MUTANT	R9 F3 ascii: list markers are ASCII digits (re-inject `\\d`)	plan_memo_blocks.py	ebeb1d179851b547	(ascii) `١.` (an Arabic-Indic digit) is not a list marker (§5.2: ASCII digits)
MUTANT	R9 F3 ascii: the row-noun anchor is an ASCII class (re-inject `\\b`)	plan_memo_roles.py	5e09c1da09e786b8	(ascii) `次のSlice Cが所有する` reaches the naming worklist: the row-noun anchor is not `\\b` (no Unicode word boundary before `Slice`)
MUTANT	R9 F3 ascii: the slug boundary is an ASCII class (re-inject `\\w`)	plan_memo_ids.py	4fc9980b82e67f74	(ascii) `次は#11-zz-alphaが所有する` reaches the naming worklist: the slug anchor is an ASCII class, not `\\w`
MUTANT	RG-1 def: orphan detection joins each run ONCE (re-inject a per-line join of the rest: quadratic)	plan_memo_memo.py	c70cf1fb5bc18b4f	Phase-1 orphan detection is linear: <= 4 link_label calls per line
MUTANT	RG-3 link: one label grammar -- a collapsed / shortcut text is a label iff `link_label` reads it from the opener	plan_memo_links.py	0657512bbed03139	(link) bracket text holding unescaped brackets is not a label (§6.3), so `[the [x] walk][]` is no collapsed reference: rc 0
MUTANT	RG2 IMP-1 / R13 §4.4: indented code cannot interrupt a paragraph (re-inject it as an opener with one open)	plan_memo_blocks.py	2bef5515042b31c2	(block) `[Slice 9z owns it]:\\n    code` IS a definition (§4.4: indented code cannot interrupt a paragraph; commonmark.js: destination `code`): a block of its own, not scanned|(indented) `text\\n    Slice 9z owns it`: an indented line cannot interrupt a paragraph (§4.4; commonmark.js: one paragraph) -- paragraph text, the site is reported|CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html
MUTANT	RG2 IMP-1: a table header is a block end (local policy; re-inject the pure-CommonMark reading)	plan_memo_blocks.py	758e92c99085042f	(block) `[Slice 9z owns it]:\\n\pa\pb\p\\n\p--\p--\p` -- the table header ends the run (local policy over pure CommonMark, which has no tables): a paragraph and a table, not a definition with the header row as its destination
MUTANT	RG2 IMP-1: admit_table reads the ONE predicate (re-inject a third boundary)	plan_memo_tables.py	0f3b20d9ea9d5ce8	(table) a list item right after a schema table ends it (a block start), so it is not a 1-cell body row: rc 0
MUTANT	RG2 IMP-1: run_end reads the ONE predicate (re-inject raw/blank-only run ends)	plan_memo_blocks.py	9c14b289e1a21c02	(block) `[Slice 9z owns it]:\\n#` is a paragraph and an ATX heading, not a definition|(block) `[Slice 9z owns it]:\\n***` is a paragraph and a thematic break, not a definition|(block) `[Slice 9z owns it]:\\n---` is a setext heading (`<h2>…</h2>`), not a definition: the label line is paragraph text and its site is reported|(block) `[Slice 9z owns it]:\\n>` is a paragraph and a block quote, not a definition
MUTANT	RG2 IMP-2: an orphan is a VALID definition off a block start (re-inject the label-colon shape, which blocked the shortcut by shape alone -- as if it named a sibling)	plan_memo_memo.py	9a3227630e382055	(cite) `[C1]: ECMA-262 §1 says so, and the table cites it.` at a block start is prose (commonmark.js: a paragraph), and the citation shortcut stays exempt: rc 0|(def) a label-and-colon line that is NOT a valid definition (junk after the destination) at a block start is prose, not an orphan: `[sib]` later is exempt, rc 0
MUTANT	RG2 IMP-3: RuntimeError from resolve() is guarded with OSError	plan_memo_sibling.py	3ce37a5289020bfd	an OSError from resolve() is the unavailable-sibling schema miss, never an exception
MUTANT	RG2 MIN-1: a type-6 block ends at a blank line	plan_memo_blocks.py	ebf30a126ac19eb6	(lex-seed) a type-6 block ends at a blank line: the paragraph after it is not seeded
MUTANT	RG2 MIN-1: every line of an HTML block is seeded to its end condition	plan_memo_blocks.py	ecfce2ca40253e83	(lex-seed) `<!-- c\\n\p9z\p\\n-->`: a `\p` line inside a comment block (type 2 ends at `-->`) is seeded|(lex-seed) `<pre>\\nSlice 9z owns it\\n</pre>`: the inner line holding the id is seeded (type 1 ends at `</pre>`)
MUTANT	RG3 IMP-2: every indented-code line is recorded for the seed (re-inject 'HTML lines only')	plan_memo_memo.py	c7def79f4dbb801e	(item) … and that raw line inside the item is seeded (the one raw-line rule reaches into a container)|(lex-seed) `para\\n\\n    Slice 9z owns it`: indented code after a plain paragraph is raw under CommonMark too, but holds a declared id -- seeded by the one raw-line rule|(lex-seed) a 4-space-indented slot row after a table's rows is raw (cmark-gfm: `<pre><code>`) and is SEEDED -- it holds a `\p` -- never a silent skip (I-C)|(lex-seed) a tab-indented slot row after a table's rows is raw (§2.2: four columns) and is SEEDED
MUTANT	RG3 MIN-5: the tab after the marker gives ONE column to the marker's space (re-inject none: Example 6 would hold seven spaces)	plan_memo_blocks.py	e00d568ad3657ecc	Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	RG3 MIN-9: a lazy HEADER row is the table's header where a paragraph is open (re-inject the header arm: 'neither row may be lazy')	plan_memo_blocks.py	4637d55fdccd94ae	(quote) a slot table inside a block quote whose HEADER row is lazy is a table (cmark-gfm: the header is read out of the quote paragraph's last line): its row declares its id|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	RG3 MIN-9: the driver hands a lazy header to the table instead of ending the quote (re-inject the unconditional stop)	plan_memo_memo.py	0855554f210dd739	(quote) a slot table inside a block quote whose HEADER row is lazy is a table (cmark-gfm: the header is read out of the quote paragraph's last line): its row declares its id|Phase 1's block sequence over the §4.4 chunk and the §5.1 / §5.2 container shapes matches commonmark.js
MUTANT	RG4 cell: `is_empty` reads the cell's disposed stream (re-inject the raw text: an HTML comment fills the cell)	plan_memo_roles.py	19bbf1e0023c36d3	(cell) a `Deps` cell holding only an HTML comment is EMPTY: it renders nothing, so the umbrella row carries no Deps edge (`is_empty` reads the cell's stream, not its raw text)
MUTANT	RG4 gate: a kind phrase straddling a blanked span in a DECLARING field is a schema miss (drop it: the row leaves the census at rc 0 -- §1's clean exit for a could-not-scan)	plan_memo_population.py	7b147b77dba6c565	(R23 kind) `KIND UNDETER`MINED`` in a declaring field is the schema miss: a reader reads the undetermined kind (§6.1 -- the code span contributes `MINED` as plain text), the disposed stream reads none, and the row silently left the census as an active terminal at rc 0 -- the marker was gated, every OTHER kind phrase was not|(R23 kind) `KIND `x` UNDETERMINED` is the SAME miss from the other side: the blank stands as spaces, so the STREAM reads the undetermined kind and a reader (`KIND x UNDETERMINED`) reads none -- a gate stated over one direction leaves the other authoritative|(kind) a declaring field spelling the marker ACROSS a code span is the schema miss, rc 2: a reader reads the marker, the disposed stream does not, and §1 forbids a clean exit for a could-not-scan over the census
MUTANT	RG4 gate: the miss is the STRADDLE, not the presence of a span (re-inject the coarse test: any declaring field holding a code span becomes a schema miss)	plan_memo_population.py	571d67a83521e408	(kind) a declaring field holding a code span and NO marker is no miss: rc 0 -- the residue is a marker the reader reads ACROSS a blanked span, never the presence of one
MUTANT	RG4 lexer: a link's `[` renders nothing (drop the mark: the opener splits the token)	plan_memo_lexer.py	d9fbb22a8cda943d	(render) §6.3 a link's brackets `W[z](slice-9z-sib.md)` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token
MUTANT	RG4 lexer: a §2.5 reference in PROSE is a substitution (drop the record: only a destination decodes, as before R16's sibling rule was generalised)	plan_memo_lexer.py	6fc14f2f0e81a3bc	(kind) a declaring field spelling the marker with §2.5 a character reference for the comma DECLARES the umbrella: the row is in the census, so a prose mention of it is a site|(render) §2.5 a character reference `W&#122;` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token
MUTANT	RG4 lexer: the §2.4 escape is a substitution (re-inject the bare skip: the backslash stands in the stream)	plan_memo_lexer.py	845cd288e394a285	(kind) a declaring field spelling the marker with §2.4 an escaped comma DECLARES the umbrella: the row is in the census, so a prose mention of it is a site
MUTANT	RG4 report: a reporting coordinate goes through the stream map (drop it: the column is off by the characters the stream dropped)	plan-memo-umbrella-check.py	bf261d5affa8c4b2	a site read across a construct that renders nothing is reported at its raw column (the stream map)
MUTANT	RG4 residue: a unit WHOLLY inside a blanked span is no straddle (widen the predicate: a quoted marker becomes the schema miss I-A exists to prevent)	plan_memo_stream.py	2d3c6a87593a8898	(kind) the marker QUOTED WHOLE in a code span declares nothing (I-A: a quoted marker is not a declaration) -- the row is terminal and its mention is no site
MUTANT	RG4 seed: the `[LEX-SPLIT?]` residue is reported (drop the loop: a unit read across a blanked span is silent again)	plan-memo-umbrella-check.py	0364be178555beee	(render) that same code span IS the `[LEX-SPLIT?]` residue seed: the reader reads `Wz` across a span the disposition blanks, and the seed says so
MUTANT	RG4 stream: a link's TAIL renders no character (re-inject it as blanks)	plan_memo_stream.py	2e2703d8f6e900d8	(render) `Slice [W](slice-9z-sib.md)z owns it` names `Wz`: the link's tail renders nothing, and the `.md` file token INSIDE it is dropped with it -- where a drop and a blank overlap the drop wins, or the tail's two sides stay apart
MUTANT	RG4 stream: a matched §6.2 / GFM delimiter run renders NOTHING (drop the marks: the delimiters stand in the stream and split the token again -- the defect this round fixed)	plan_memo_stream.py	e8d317a3fdefdfe1	(kind) a declaring field spelling the marker with §6.2 emphasis inside it (`not a *terminal* unit`) DECLARES the umbrella: the row is in the census, so a prose mention of it is a site|(render) GFM strikethrough `W~~z~~` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token|(render) §6.2 emphasis `W*z*` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token|(render) §6.2 strong emphasis `W**z**` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`|(render) §6.2 strong emphasis `W**z**` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token
MUTANT	RG4 stream: a raw HTML span renders no character (re-inject it as text the checker refuses to read: it blanks and splits the token again)	plan_memo_stream.py	0481e3f5e9ce4f88	(kind) a declaring field spelling the marker with §6.6 a comment inside it DECLARES the umbrella: the row is in the census, so a prose mention of it is a site|(render) §6.6 a raw HTML tag pair `W<b>z</b>` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token|(render) §6.6 an HTML comment `W<!-- c -->z` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token
MUTANT	RG4 stream: a substitution that would spell a DECORATION does not substitute (drop the carve: `\\*\\*C\\*\\*` becomes the bold `**C**` no reader sees)	plan_memo_stream.py	952e4968b4315afd	(render) `&#42;&#42;C&#42;&#42;` is the same rule for §2.5: a reference that would spell a decoration stands as written|(render) `\\*\\*C\\*\\* is how the row is written`: an ESCAPED decoration character stands as written -- substituting it would spell a `**C**` bold the document does not have, and the undecorated single letter is the declared miss
MUTANT	RG4 stream: a §2.4 escape and a §2.5 reference SUBSTITUTE their character (drop both: the reference and the backslash are read as written again)	plan_memo_stream.py	bb63d4afa6e48d0c	(kind) a declaring field spelling the marker with §2.4 an escaped comma DECLARES the umbrella: the row is in the census, so a prose mention of it is a site|(kind) a declaring field spelling the marker with §2.5 a character reference for the comma DECLARES the umbrella: the row is in the census, so a prose mention of it is a site|(render) §2.5 a character reference `W&#122;` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token
MUTANT	RG4 stream: the DECORATION exception -- a `**` pair whose content is only declared ids stands (drop it: `**9z**` renders away and `**9z**7z` is one token)	plan_memo_stream.py	170f34fb1bd07a5b	(render) `**9z**7z` still names `9z`: a `**` pair whose content is only declared ids is the document DECORATING an id, so the delimiters stand and bound it -- dropping them would leave the single token `9z7z`, which no row declares
MUTANT	RG4 stream: the exception is `id_only`'s (widen it to every `**` pair: `W**z**` is decorated again and the split id comes back)	plan_memo_stream.py	90cbc3781a22dead	(render) §6.2 strong emphasis `W**z**` names no row on the mirror table, where `W` is the umbrella and `Wz` terminal: the old reading fabricated a site on `W`|(render) §6.2 strong emphasis `W**z**` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token
MUTANT	RG4 stream: the exception is the DECORATION's, not emphasis's (widen it to a single `*`: `*9z*7z` names `9z`)	plan_memo_stream.py	f45a71a53172b0ca	(render) `*9z*7z` names nothing: a SINGLE `*` is not this document's decoration (`plan_memo_ids.DECOR` is `**` and a backtick), so the pair renders away and `9z7z` is one token -- the exception is the decoration's, not emphasis's
MUTANT	RG4 §6.2: `_` opens only where §6.2 rules 5-6 allow (drop the arm: intraword `_` becomes emphasis and `snake_case` breaks)	plan_memo_emphasis.py	fb1bfe582aa3fe99	(render) `Slice W_z_ owns it`: an intraword `_` run can neither open nor close (§6.2 rules 5-6, the `snake_case` rule), so it is literal and bounds the token|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	RG4 §6.2: `openers_bottom` memoises a failed search (drop it: a paragraph of unmatched runs is quadratic)	plan_memo_emphasis.py	1a842f677eec5294	the scans are linear over a corpus GENERATED from the grammar: every branch character, delimiter, HTML opener, bracket construct and id kind, each repeated and each PAIR of them interleaved, and no source line grows worse than its input
MUTANT	RG4 §6.2: a `~` run of three or more is no delimiter (drop the GFM bound: `W~~~z~~~` strikes)	plan_memo_emphasis.py	915568d414578b84	(render) `Slice W~~~z~~~ owns it`: three tildes are no strikethrough (GFM: a matching pair of ONE OR TWO), so the run is literal text and bounds the token
MUTANT	RG4 §6.2: the flanking rules read the character BEFORE the run (drop it: every run reads a line start)	plan_memo_emphasis.py	eaafb4a6295f63e6	(render) §6.2 emphasis `W*z*` renders `Wz` and names the UMBRELLA row `Wz`: a construct that contributes no character does not split a token|CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	RG4 §6.2: the rule of three (drop it: a `can_open` closer takes an opener the spec forbids it)	plan_memo_emphasis.py	554bd03956878a48	CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	RG4 §6.4: emphasis inside a RESOLVED image's description is demoted -- plain string content, no `<em>` (drop the demotion: the tag count over-claims)	plan_memo_lexer.py	32f6bbdbd07219da	CommonMark 0.31.2 spec examples (§2.4, §2.5, §6.1-§6.6): Phase 2's inline claim aligns with the html
MUTANT	cases: a module that writes a case but holds no list is REFUSED (ignore it -- its cases go nowhere, and a case that was never collected is what the manifest cannot compare)	plan_memo_selftest_cases.py	2319782e5c5b82c6	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	def: a definition cannot interrupt a paragraph (Phase 1: only at a block start)	plan_memo_memo.py	d7893847c8de4cc9	(def) a definition cannot interrupt a paragraph: the reference is unanswered, and reported ONCE (`[text][label]` re-scans `[label]`)
MUTANT	def: nothing but whitespace after the destination/title	plan_memo_blocks.py	177e439d052db109	(def) text after the destination is not a definition, so the reference is unanswered: a schema miss
MUTANT	def: the first definition of a label wins	plan_memo_memo.py	e52518fee4973e36	(def) the FIRST definition of a label wins
MUTANT	def: up to one line ending before the destination	plan_memo_blocks.py	8cf420302e8ae035	(def) one line ending is allowed before the destination
MUTANT	direction: a FUNCTION-LOCAL import is an edge (read module-level statements only -- the form a hidden cycle takes)	plan_memo_selftest_records.py	22d1819c8b358dfb	PROPERTY: the self-test's IMPORT DIRECTION rule holds of the imports (no cycle, the harness imports nothing of the set, no checker module imports the self-test but the entry point's dispatch) -- a rule over the AST, not an inventory of edges
MUTANT	direction: a cycle is a violation (drop the report)	plan_memo_selftest_records.py	a4aa6663a662aee6	PROPERTY: the self-test's IMPORT DIRECTION rule holds of the imports (no cycle, the harness imports nothing of the set, no checker module imports the self-test but the entry point's dispatch) -- a rule over the AST, not an inventory of edges
MUTANT	direction: no checker module imports the self-test (drop the clause)	plan_memo_selftest_records.py	a492e921910f2357	PROPERTY: the self-test's IMPORT DIRECTION rule holds of the imports (no cycle, the harness imports nothing of the set, no checker module imports the self-test but the entry point's dispatch) -- a rule over the AST, not an inventory of edges
MUTANT	direction: the entry point's dispatch is the ONE exception (drop the exception -- the rule then forbids the runner's own entry)	plan_memo_selftest_records.py	f82d2dda8ec456d5	PROPERTY: the self-test's IMPORT DIRECTION rule holds of the imports (no cycle, the harness imports nothing of the set, no checker module imports the self-test but the entry point's dispatch) -- a rule over the AST, not an inventory of edges
MUTANT	direction: the harness imports nothing of the population (drop the clause)	plan_memo_selftest_records.py	ecb754250a8ed8d7	PROPERTY: the self-test's IMPORT DIRECTION rule holds of the imports (no cycle, the harness imports nothing of the set, no checker module imports the self-test but the entry point's dispatch) -- a rule over the AST, not an inventory of edges
MUTANT	family: the STRAY direction is reported (drop it -- a PROPERTY name from outside the family passes)	plan_memo_selftest_controls.py	f8f724b998bd07a2	the entries named `PROPERTY: ...` are exactly the property family's fragments (`_FAMILY`), both directions
MUTANT	family: the ratchets module IS in the property family (re-inject the three-module family the docstrings stated -- every ratchet entry is then a PROPERTY name from outside it)	plan_memo_selftest_controls.py	3dd399265dacbfa4	the entries named `PROPERTY: ...` are exactly the property family's fragments (`_FAMILY`), both directions
MUTANT	fence: backtick info string may not hold a backtick	plan_memo_blocks.py	13f7f74b56d34f67	(fence) a backtick fence whose info string holds a backtick is not a fence
MUTANT	fence: closer at least as long as the opener	plan_memo_blocks.py	8d23151f2e15c5ab	(fence) a shorter closer does not close
MUTANT	fence: closer followed only by spaces/tabs	plan_memo_blocks.py	5c617451d216e876	(fence) a closer followed by text does not close
MUTANT	fence: closer uses the opener's character	plan_memo_blocks.py	0e3de15083481ddf	(fence) a closer of the OTHER character does not close
MUTANT	fence: fenced lines are not paragraph lines (A x B)	plan_memo_blocks.py	ff0864658271ba8b	(fence) a link inside a fence is not a link (A x B)
MUTANT	fence: opener indent <=3 columns (re-inject any indent)	plan_memo_blocks.py	e486475116531abf	(fence) four spaces of indent is not a fence
MUTANT	fence: opener needs >=3 fence characters	plan_memo_blocks.py	164c2e3ca61a93e4	(fence) a tilde fence masks too
MUTANT	gate: KIND-SPELLING is a mechanical finding	plan-memo-umbrella-check.py	ceda0e9136ff1560	(rc) the undetermined kind written two ways is KIND-SPELLING, rc 1
MUTANT	gate: a duplicate declaration is a schema miss	plan_memo_population.py	44772071ab290148	(rc) the same id declared in two memos is rc 2
MUTANT	gate: a mechanical finding is exit 1	plan-memo-umbrella-check.py	363851e7927f2917	(rc) the undetermined kind written two ways is KIND-SPELLING, rc 1
MUTANT	gate: an absent linked memo is a schema miss	plan_memo_population.py	ba7411c2f5db61a7	(rc) a linked memo that is not on disk is rc 2, never clean
MUTANT	gate: an unmatched schema is a schema miss	plan_memo_population.py	eaead5c30661ff4b	(rc) a schema with no matching table is rc 2
MUTANT	id: a citation id is keyed by its canonical §6.3 LABEL (key the raw spelling -- two rows differing only in case declare one id twice and nothing says so)	plan_memo_tables.py	7e3c51e9833a5531	(id) a citation id declared twice differing only in CASE is the same miss -- `[C1]` and `[c1]` are one §6.3 label, which is how the link resolver already reads them: rc 2
MUTANT	link: 0x7F is an ASCII control character	plan_memo_links.py	873a62c5c7093730	(link) an ASCII control character (0x7F) ends a bare destination
MUTANT	link: `<dest>` holds no unescaped `<`	plan_memo_links.py	b078ea8ab868fa34	(link) `<dest>` may not contain an unescaped `<`
MUTANT	link: `[text]` followed by a link label is not a shortcut	plan_memo_links.py	6d48cf1a057a4f39	(link) `[label][undefined]` is neither a full reference nor a shortcut (§6.3 Example 571: a shortcut is not followed by a link label) -- the unanswered label is a schema miss, not a link to the sibling
MUTANT	link: a `(…)` title holds no unescaped `(`	plan_memo_links.py	3b747ac9287472d4	(link) a `(…)` title may not contain an unescaped `(`
MUTANT	link: backslash escapes ASCII punctuation (destination, `<dest>`, title)	plan_memo_links.py	feb07a84dd6e4e2d	(link) `<dest>` may contain an escaped `>`|(link) a `"…"` title may contain an escaped `"`|(link) bare destination with an escaped parenthesis
MUTANT	link: bare destination balances parentheses	plan_memo_links.py	1135412871d1379f	(link) bare destination with a balanced parenthesis pair
MUTANT	link: bracket text nests (full reference with inner brackets): an inactive inner opener is popped and its `]` is literal, not a failure of the outer	plan_memo_lexer.py	e5ada022c1de58ee	(link) full reference whose text holds nested brackets; the label is the link's tail, not prose, and so is the definition
MUTANT	link: collapsed reference resolves the text as label	plan_memo_links.py	a95b5175cd1625ba	(link) collapsed reference `[label][]`
MUTANT	link: label content = a character that is not a space, tab, or line ending	plan_memo_links.py	f6459d646ec485ee	(link) a label holding only a non-breaking space is a label (§6.3: at least one character that is not a space, tab, or line ending)
MUTANT	link: label matching collapses internal whitespace	plan_memo_links.py	2bce2bdf2d5ed372	(link) label matching collapses internal whitespace
MUTANT	link: label matching folds spaces, tabs, line endings -- not Unicode whitespace	plan_memo_links.py	67c513ab866c9c8f	(link) label matching collapses spaces, tabs and line endings only: a non-breaking space is not a space, so the reference is unanswered
MUTANT	link: label matching is a case FOLD	plan_memo_links.py	b81b0ee0d042f0a3	(link) label matching is a Unicode case FOLD, not lower()
MUTANT	link: only ASCII space ends a bare destination, not Unicode whitespace	plan_memo_links.py	665b35f125e60ab7	(link) a non-ASCII space is destination content (only ASCII space and controls end it)
MUTANT	manifest: `finish` refuses a run that did not execute the verified table (drop the audit)	plan_memo_selftest_manifest.py	5ac2299b794aa774	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: `finish` refuses a table this module did not make (accept any)	plan_memo_selftest_manifest.py	b5c3ce78fc8f28eb	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: `lines` keeps DUPLICATES (drop them -- a row collected twice reads as one)	plan_memo_selftest_manifest.py	af171b015f930e40	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: `read` reads the file it is given (read the committed one always)	plan_memo_selftest_manifest.py	3751bb52e506398e	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: `take` RAISES on a difference (return the table anyway -- the runner would execute a collection it had not compared)	plan_memo_selftest_manifest.py	1b03cb89d86300d1	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: `take` hands back the VERIFIED rows (hand back fewer -- the mutation proof then runs a smaller registry and says nothing)	plan_memo_selftest_manifest.py	5c80158a35b6e524	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: a CHANGED line is reported once, as changed (leave it in removed and added too)	plan_memo_selftest_manifest.py	0e4b2ed6bd9521fb	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: a REMOVED line is a difference (drop that half)	plan_memo_selftest_manifest.py	da25e310aae59cd0	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: a case name used twice is refused at generation (drop the check)	plan_memo_selftest_manifest.py	819de60583357d51	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: a control's line carries its SOURCE digest (name and qualname alone -- a control re-pointed to a stub, or re-bodied, reads the same)	plan_memo_selftest_manifest.py	a853da14a030f9c8	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: a dict is frozen into sorted pairs (leave it a dict -- a case's `files`)	plan_memo_selftest_registry.py	f1675553083f7f9d	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: a difference is reported (skip the comparison)	plan_memo_selftest_manifest.py	602d8e982c9350ea	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: a mutation label used twice is refused at generation (drop the check)	plan_memo_selftest_manifest.py	d6313d09a886e5cf	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: a row naming NO control is refused at generation (drop the check)	plan_memo_selftest_manifest.py	f64a2d7a88e00897	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: a row naming a MISSING control is refused at generation (drop the check)	plan_memo_selftest_manifest.py	fa967b8f9d89c9aa	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: a row's line carries its EDIT digest (drop it -- two rows' replacements can be swapped)	plan_memo_selftest_manifest.py	56f15a8b0251c59e	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: an ADDED line alone is a difference (read removals and changes only)	plan_memo_selftest_manifest.py	f1a15ff8735b1343	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: an EMPTY manifest is the one error (accept it -- every line then reads as removed)	plan_memo_selftest_manifest.py	799e0b41b35b18b7	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: an UNDECODABLE manifest is the one error (let it escape as a traceback)	plan_memo_selftest_manifest.py	92d22b80a4fd56f2	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: an unreachable source is an ERROR (fall back to a constant -- every such control reads the same)	plan_memo_selftest_manifest.py	22aa26fbf8e09989	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the ESCAPE does not depend on load state (make it the identity -- the runner unloads before it reports, and a control name's BEL then reaches stderr raw)	plan_memo_selftest_manifest.py	214c5e91fd05befc	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the `\p` between control names is ESCAPED (leave it -- a row naming `a\pb` and one naming `a` and `b` read the same)	plan_memo_selftest_manifest.py	0c736a4c741bed8f	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the audit is EXACTLY once (accept any number of runs)	plan_memo_selftest_manifest.py	1144f955fd150273	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the audit reports a control that ran and is not in the table (drop that half)	plan_memo_selftest_manifest.py	10c195772c902b6c	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the collection FREEZES what it reads (return it as it is)	plan_memo_selftest_registry.py	c3d7046a747a9b33	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the collection replaces each module's list by its frozen copy (leave the list)	plan_memo_selftest_registry.py	4d415bda3cdeca1c	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the collection step RECORDS its calls (drop the record -- the one-enumeration arm sees nothing)	plan_memo_selftest_registry.py	6e34309c3f6e15f7	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the counts are of what RAN (count the verified table instead)	plan_memo_selftest_manifest.py	64f2317cf57e0c76	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the digest is of the DEFINITION BLOCK (digest the whole module text instead -- every control in one file then reads the same)	plan_memo_selftest_manifest.py	c4a8db1e2a447953	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the digest reads the text the module was EXEC'D from (read the file on disk always -- a patched control digests the unpatched text)	plan_memo_selftest_manifest.py	f351a492a9e08e07	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the generator writes the RUNNER's collection (write a different one -- the rows dropped)	plan_memo_selftest_manifest.py	81ece6f8d81b93f5	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the snapshot enumerates the cases ONCE (take them twice -- the table collects its own)	plan_memo_selftest_manifest.py	cbcebdda11d39b7d	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the snapshot is built ONCE per process (build it per call)	plan_memo_selftest_manifest.py	810b1405619ea8b3	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: the wrapper COUNTS the control that ran (drop the record -- every control then reads as never executed)	plan_memo_selftest_manifest.py	2abf7acbec21b67a	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	manifest: two rows repeating ONE edit are refused at generation (drop the check)	plan_memo_selftest_manifest.py	3740931c8b3310cd	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	membership: a FUNCTION-LOCAL import is an edge (read top-level statements only -- the runner, imported inside the entry point's dispatch, is then reached by nothing)	plan_memo_selftest_population.py	a903c4a78a68ffc0	PROPERTY: no module can leave a run unnoticed -- every file related to the population is in it, and every module in it is reached from a root
MUTANT	membership: a file importing the population must be in it (drop the clause)	plan_memo_selftest_population.py	9bdc88626c673f46	PROPERTY: no module can leave a run unnoticed -- every file related to the population is in it, and every module in it is reached from a root
MUTANT	membership: a file the population imports must be in it (drop the clause)	plan_memo_selftest_population.py	d1658ccf1ca8c334	PROPERTY: no module can leave a run unnoticed -- every file related to the population is in it, and every module in it is reached from a root
MUTANT	membership: a root outside the population reaches nothing (take every root)	plan_memo_selftest_population.py	a8d832b1dc3ab653	PROPERTY: no module can leave a run unnoticed -- every file related to the population is in it, and every module in it is reached from a root
MUTANT	membership: every module is reached from a root (drop the clause -- a module on disk never runs)	plan_memo_selftest_population.py	40807ef7fcd51a48	PROPERTY: no module can leave a run unnoticed -- every file related to the population is in it, and every module in it is reached from a root
MUTANT	membership: reachability follows the import graph (stop after the roots)	plan_memo_selftest_population.py	ff50a2f58b28e88f	PROPERTY: no module can leave a run unnoticed -- every file related to the population is in it, and every module in it is reached from a root
MUTANT	membership: the disk is EVERY `.py` beside the checker (read the population glob alone -- a helper outside it is never seen)	plan_memo_selftest_population.py	5127cbdddac1e4c3	PROPERTY: no module can leave a run unnoticed -- every file related to the population is in it, and every module in it is reached from a root
MUTANT	memo: a memo is a REGULAR file, asked before the read (drop the guard -- a `.md` link to a device is read to EOF: `/dev/zero` never ends)	plan_memo_memo.py	f2e0ac4f2f90eb75	a linked `.md` whose target is not a regular file (a device, a FIFO, a socket) is the unavailable-memo miss at rc 2, refused before it is read
MUTANT	memo: the guard asks REGULAR, not one non-regular kind (narrow it to a char device -- a FIFO then blocks the run again)	plan_memo_memo.py	4c16c4191c7c4452	a linked `.md` whose target is not a regular file (a device, a FIFO, a socket) is the unavailable-memo miss at rc 2, refused before it is read
MUTANT	merge: `Registry.update` goes through the refusal (bypass it)	plan_memo_selftest_harness.py	2af426f78e399ed3	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	merge: `Registry` refuses a name it holds (drop the refusal)	plan_memo_selftest_harness.py	3f3d30f4b04554ba	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	population: a `from X import` is an order edge (drop it)	plan_memo_selftest_harness.py	45130719a134dddc	PROPERTY: the harness DERIVES the population from a directory (glob, self-test partition, registry-by-content, import order, cycle) -- asked of a planted directory
MUTANT	population: a registry module binds its list at TOP LEVEL (read every assignment -- a list bound inside a function makes a module a registry)	plan_memo_selftest_harness.py	2f6416c1c1a4fbd4	PROPERTY: the harness DERIVES the population from a directory (glob, self-test partition, registry-by-content, import order, cycle) -- asked of a planted directory
MUTANT	population: a registry module is a SELF-TEST module (drop it -- a checker module holding a `MUTANTS` becomes one)	plan_memo_selftest_harness.py	d5184b583c994228	PROPERTY: the harness DERIVES the population from a directory (glob, self-test partition, registry-by-content, import order, cycle) -- asked of a planted directory
MUTANT	population: a registry module is one that HOLDS the list (re-inject the name rule -- a renamed module's rows leave in silence)	plan_memo_selftest_harness.py	71f63db51dafbfb3	PROPERTY: the harness DERIVES the population from a directory (glob, self-test partition, registry-by-content, import order, cycle) -- asked of a planted directory
MUTANT	population: an ANNOTATED assignment binds the list (read `Assign` alone)	plan_memo_selftest_harness.py	647e44c700ec18a0	PROPERTY: the harness DERIVES the population from a directory (glob, self-test partition, registry-by-content, import order, cycle) -- asked of a planted directory
MUTANT	population: an `import X` is an order edge (read `from X import` alone)	plan_memo_selftest_harness.py	bd3db7c0ecf8d607	PROPERTY: the harness DERIVES the population from a directory (glob, self-test partition, registry-by-content, import order, cycle) -- asked of a planted directory
MUTANT	population: an import cycle is an error (break out of it -- a misordered load in silence)	plan_memo_selftest_harness.py	203b44ebdcb46cc7	PROPERTY: the harness DERIVES the population from a directory (glob, self-test partition, registry-by-content, import order, cycle) -- asked of a planted directory
MUTANT	population: every memo's ids are in the keep-set	plan_memo_population.py	f809708577494a93	(population) a terminal id declared in a linked memo is in the keep-set, so `Tq / 9z` is an id-only run, not code
MUTANT	population: every memo's rows are asserted	plan_memo_population.py	360ef29c953ded85	(b) a sibling umbrella's Deps edge is asserted
MUTANT	population: every memo's rows are declared (census)	plan_memo_population.py	e6ac51b866df7376	(population) an umbrella declared in a linked memo is in the census
MUTANT	population: the entry point is in it (drop it from `files()`)	plan_memo_selftest_harness.py	dc7c6840eb6a302f	PROPERTY: the harness DERIVES the population from a directory (glob, self-test partition, registry-by-content, import order, cycle) -- asked of a planted directory
MUTANT	population: the glob is `plan_memo*.py` (re-inject the narrowed `plan_memo_*.py` -- `plan_memo.py` leaves in silence)	plan_memo_selftest_harness.py	d0dea25731badac1	PROPERTY: the harness DERIVES the population from a directory (glob, self-test partition, registry-by-content, import order, cycle) -- asked of a planted directory
MUTANT	population: the link walk is transitive	plan_memo_population.py	af7271ff34811976	(population) the population is transitive: a memo linked from a linked memo is scanned
MUTANT	population: the self-test partition reads the whole name (read a prefix -- the runner `plan_memo_umbrella_selftest.py` becomes a checker module)	plan_memo_selftest_harness.py	0a90b30b50d32fb1	PROPERTY: the harness DERIVES the population from a directory (glob, self-test partition, registry-by-content, import order, cycle) -- asked of a planted directory
MUTANT	ratchets: EXEMPT is decided first (count an exempt-and-credited loop as pinned too)	plan_memo_selftest_ratchets.py	03c824c4c04f9265	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: `list(...)[:1]` is a truncation (drop the unwrap -- every row in that form, most of the census rows, credits nothing)	plan_memo_selftest_ratchets.py	6a2e5a61168ebb67	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: a MODULE-LEVEL call is a call, named `<module>` (drop it)	plan_memo_selftest_ratchets.py	4450cc710d6e2a61	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: a call by bare NAME is a call (read attribute calls only -- `kind_disagreements(...)` leaves the population and its sanctions read as stale)	plan_memo_selftest_ratchets.py	6be7f3062ff197c0	PROPERTY: the kind-phrase questions are asked at their sanctioned sites only -- "which kind does this field DECLARE" and "does this cell CLAIM one, under either reading" are two questions with one site each, and a third caller asking either one directly is the shape three rounds of findings had
MUTANT	ratchets: a caller's identity carries its CLASS (keep the innermost def alone -- `Population._kind` becomes `_kind`, which is what a same-named function elsewhere is)	plan_memo_selftest_ratchets.py	03a0a5206136cc15	PROPERTY: the kind-phrase questions are asked at their sanctioned sites only -- "which kind does this field DECLARE" and "does this cell CLAIM one, under either reading" are two questions with one site each, and a third caller asking either one directly is the shape three rounds of findings had
MUTANT	ratchets: a loop is credited by POSITION (re-inject the exact-header-LINE keying of 799349db -- one row pins every loop spelled like its target)	plan_memo_selftest_ratchets.py	ed3c5b74932866c5	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: a loop is credited by POSITION (re-inject the target-PREFIX keying 799349db replaced -- `for x in ` pins every loop over any iterable)	plan_memo_selftest_ratchets.py	653af82a0c60f922	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: a loop is credited by the row's EDIT, not its find (re-inject the first version's find-text keying -- a loop the find merely contains reads as pinned)	plan_memo_selftest_ratchets.py	9cc7da9fea402832	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: a loop's position is WITHIN its def or class (stop separating bodies -- a method's loop takes an ordinal in the enclosing body and every later ordinal shifts)	plan_memo_selftest_ratchets.py	f302267f10a0d746	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: a row's find must be UNIQUE to apply (accept a repeated find -- one row pins every occurrence)	plan_memo_selftest_ratchets.py	008bbe8e43deddc0	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: a sanction is keyed on (module, qualified function) (re-inject the bare-name keying -- a `_kind` in another module asks with `Population._kind`'s sanction)	plan_memo_selftest_ratchets.py	65cf5e71e98baa24	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: a sanction names the CALLER, not only the module (key it on the module alone)	plan_memo_selftest_ratchets.py	efc1fd361afac68c	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: a sanction names the MODULE, not only the caller (key it on the caller alone -- an `a` in another module asks with this `a`'s sanction)	plan_memo_selftest_ratchets.py	377d5ff87557bec6	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: a sanctioned site that makes no call is red (drop the direction -- a sanction outlives the caller it was written for)	plan_memo_selftest_ratchets.py	d7e58f5278740887	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: a stale sanction is judged PER MODULE (drop the module from the seen key -- a caller of the same name elsewhere hides it)	plan_memo_selftest_ratchets.py	7b425720703ba83d	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: a stale sanction is judged PER QUESTION (drop the question from the seen key -- a site seen for `_claims` hides its stale `kind_disagreements` sanction)	plan_memo_selftest_ratchets.py	ca6eea650924df43	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: a truncating INTENT needs ` in ` (drop it -- `for e[:1]:` becomes an orphan)	plan_memo_selftest_ratchets.py	9eacc018e3c83381	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: a truncating INTENT needs a `for` header (any `[:1]` line intends -- a non-loop line becomes an orphan)	plan_memo_selftest_ratchets.py	9201b915aaa65f56	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: a truncating row that credits NO loop is an orphan (drop the direction -- a row whose edit truncates nothing the module has is a silent no-op)	plan_memo_selftest_ratchets.py	a9f2380a1724be20	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: a truncation has NO lower bound (drop the check -- `[1:1]` is credited)	plan_memo_selftest_ratchets.py	e6a895da3dcb3ff7	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: a truncation has NO step (drop the check -- `[:1:2]` is credited)	plan_memo_selftest_ratchets.py	f382106766170d94	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: a truncation is recognised on the TREE (make `_is_truncation` answer True -- every loop a row's patched text still has reads as truncated)	plan_memo_selftest_ratchets.py	eece69704b78d86c	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: a truncation keeps ONE element (accept any constant upper bound -- `[:2]` pins a loop it does not truncate to its first element)	plan_memo_selftest_ratchets.py	dac11fc20cd89ee8	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: all THREE question names are read (drop `_phrases` -- a caller asking which kind a field declares is not seen)	plan_memo_selftest_ratchets.py	b83b754db43cac7a	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: an `async for` is a loop (read `ast.For` alone)	plan_memo_selftest_ratchets.py	74aa1a28242580a5	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: an exempt entry naming no loop is red (drop the check -- a stale complement exempts nothing and says so to nobody)	plan_memo_selftest_ratchets.py	8fab8803bad39897	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: an unpinned loop is red (drop the report -- the buckets still add up and the verdict is clean)	plan_memo_selftest_ratchets.py	f142d357c199595c	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: each question's sanction is ITS OWN (union the sites over every question -- a site sanctioned for `_claims` may ask `_phrases`)	plan_memo_selftest_ratchets.py	b8942e2311650334	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: every criterion of `CRITERIA` names a row that exists (rename one -- the table then points at nothing)	plan_memo_selftest_ratchets.py	93ea3c9f9029ee87	PROPERTY: every criterion the two ratchets state names an existing killing row (the table is data in plan_memo_selftest_mutants_ratchets.CRITERIA)
MUTANT	ratchets: only `list(...)` is unwrapped (unwrap any call -- `sorted(xs)[:1]` is credited)	plan_memo_selftest_ratchets.py	e33901f376de343c	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: only a ONE-argument `list(...)` is unwrapped (drop the count -- `list(zs, 0)[:1]` is credited)	plan_memo_selftest_ratchets.py	d4782e363f0ea6e0	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: only a `list` NAME is unwrapped (accept any callee -- `m.list(zs)[:1]` raises on `.id`)	plan_memo_selftest_ratchets.py	c7522a20861b03ef	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: only a keyword-free `list(...)` is unwrapped (drop the check -- `list(zs, key=0)[:1]` is credited)	plan_memo_selftest_ratchets.py	2c59781467ac3473	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: the exempt complement is guarded by its header (drop the guard -- an edit that shifts the ordinals exempts whichever loop moved into the position)	plan_memo_selftest_ratchets.py	cbc56e0aa1d34f20	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: the kind population includes the SELF-TEST half (re-inject the exclusion whose stated reason measured false -- a self-test-named helper calling `_claims` is unread)	plan_memo_selftest_ratchets.py	d0843eefa7119ee4	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: the kind-question population is DERIVED from the module set (re-inject 799349db's hand-written five-tuple -- a caller in the sibling resolver or the entry point is outside it)	plan_memo_selftest_ratchets.py	e1c82d6e2b1a159c	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: the kind-question population is read off the DISK (re-inject a hand list complete today -- a new checker module is outside it)	plan_memo_selftest_ratchets.py	4991f600fdb7c492	PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: the kind-question population is the WHOLE checker set (re-inject the census module alone -- the population before 799349db)	plan_memo_selftest_ratchets.py	f225136cf4b5801a	PROPERTY: the kind-phrase questions are asked at their sanctioned sites only -- "which kind does this field DECLARE" and "does this cell CLAIM one, under either reading" are two questions with one site each, and a third caller asking either one directly is the shape three rounds of findings had|PROPERTY: the kind-question ratchet reports a caller in ANY module of the checker set, judged by (module, qualified function) -- not a caller outside a listed subset, not one whose bare name is sanctioned elsewhere
MUTANT	ratchets: the loop ratchet reads the WHOLE registry (read the base module's list alone -- every loop a later module's row pins reads as unpinned)	plan_memo_selftest_ratchets.py	a7b69d62d2646688	PROPERTY: every loop in the census module that walks a POPULATION is pinned by a mutant that truncates THAT loop (the ratchet is derived from the code, not a list somebody extends when a reviewer names one)
MUTANT	ratchets: the population is EVERY `for`, nested ones too (stop descending into a loop's body -- an inner loop leaves the population)	plan_memo_selftest_ratchets.py	983ef5c9e6901991	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: the truncated iterable must BE the original (drop the comparison -- a re-point to another iterable's `[:1]` pins a loop whose population it never touches)	plan_memo_selftest_ratchets.py	ef646a54b2eabbf2	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	ratchets: the upper bound must be a CONSTANT (drop the type check -- `zs[:n]` raises on `.value`)	plan_memo_selftest_ratchets.py	ea7189ed1d67d371	PROPERTY: the loop ratchet credits a loop by POSITION: one row truncating one of two loops that share a header pins that loop and no other
MUTANT	registry: a module holding an EMPTY list is refused (drop the check -- a list emptied elsewhere, or a name collision, contributes nothing and says nothing)	plan_memo_selftest_registry.py	df3d6ccfca361769	PROPERTY: the live collection is exactly the committed golden manifest, and the manifest mechanism holds (the one door, the comparison, one source, deep immutability, the generation-time validations and the file's fault shapes)
MUTANT	report channel: a raised CRASH is not a report channel (take every raise -- the population then holds developer-facing invariant messages)	plan_memo_selftest_records.py	c3a8cdf442979b8c	PROPERTY: every line the run REPORTS goes through the escape -- measured over the EMIT SITES, the subject the escape function's own control cannot reach
MUTANT	report channel: a raised `ManifestError` is a report channel (drop it -- the manifest diff reaches stderr unescaped, which is where three raw NULs went)	plan_memo_selftest_records.py	826a30725cda4fc2	PROPERTY: every line the run REPORTS goes through the escape -- measured over the EMIT SITES, the subject the escape function's own control cannot reach
MUTANT	report: a line's embedded NEWLINE is escaped (leave line endings alone -- a finding whose text carries one then breaks a line-oriented consumer)	plan-memo-umbrella-check.py	e35cd66b553ddd5a	PROPERTY: every line the run REPORTS goes through the escape -- measured over the EMIT SITES, the subject the escape function's own control cannot reach
MUTANT	report: every LONE SURROGATE is escaped (drop the arm -- a non-UTF-8 POSIX filename then raises UnicodeEncodeError from the strict UTF-8 channel, a traceback at the findings code)	plan-memo-umbrella-check.py	19690db89a24be2b	a memo path with a lone surrogate (a non-UTF-8 POSIX filename byte) is reported escaped at rc 2, never a UnicodeEncodeError from the strict UTF-8 channel
MUTANT	report: only the surrogates are escaped above DEL (escape all non-ASCII -- every Japanese line becomes <U+XXXX> soup)	plan-memo-umbrella-check.py	3f67c77f53b174cc	a memo path with a lone surrogate (a non-UTF-8 POSIX filename byte) is reported escaped at rc 2, never a UnicodeEncodeError from the strict UTF-8 channel
MUTANT	report: the surrogate arm stops at U+D800 (widen it down one -- U+D7FF, the code point just below the range, is then mangled)	plan-memo-umbrella-check.py	072ea40d57887ffe	a memo path with a lone surrogate (a non-UTF-8 POSIX filename byte) is reported escaped at rc 2, never a UnicodeEncodeError from the strict UTF-8 channel
MUTANT	report: the surrogate arm stops at U+DFFF (drop the upper bound -- U+E000 and everything above it, the fullwidth forms included, is then mangled)	plan-memo-umbrella-check.py	0b7a3183657ec4ea	a memo path with a lone surrogate (a non-UTF-8 POSIX filename byte) is reported escaped at rc 2, never a UnicodeEncodeError from the strict UTF-8 channel
MUTANT	row: `\\\p` becomes `\p` (the backslash is consumed)	plan_memo_blocks.py	e6f6e66841b45ba4	(row) `\\\p` is `\p` in the cell, so `9z \\\p 7z` is an id-only run
MUTANT	row: an unescaped `\p` splits even inside backticks (Example 200)	plan_memo_blocks.py	609f6a1329bacbb1	(row) an unescaped `\p` inside backticks SPLITS the row: each half is prose with a literal backtick, so the id on each side is a mention
MUTANT	row: offsets map through each cell's raw segments	plan_memo_blocks.py	a381889c8b864d09	a site after an escaped pipe is reported at its raw column
MUTANT	row: the leading pipe is optional	plan_memo_blocks.py	3b0cfac6c1ff3aa9	(row) a table without leading and trailing pipes declares its rows
MUTANT	row: the trailing pipe is optional	plan_memo_blocks.py	3dc0ebb031ff06f0	a table with and without edge pipes reads the same
MUTANT	span: a `>` line starts a block	plan_memo_blocks.py	85e88b5eb2849ed6	(span) a paragraph ends at a `>` line
MUTANT	span: a list item starts a block (drop the arm: a marker line never ends a run, nor a sibling item's lazy gather)	plan_memo_blocks.py	0fc2a5e8e3999bef	(span) a paragraph ends at a list item: a backtick open in one item and closed in the next is literal|CommonMark 0.31.2 spec examples (Tabs, §4.1-§4.9, §5.1-§5.3): Phase 1's block sequence aligns with the html
MUTANT	span: an ATX heading is a block	plan_memo_blocks.py	3a2b76e631f443f6	(span) a paragraph ends at an ATX heading
MUTANT	span: an unmatched backtick string is literal	plan_memo_lexer.py	300f097c0a3e8400	(span) an unmatched backtick string is literal, not a mask to end of line
MUTANT	span: lexed over the paragraph, not the line	plan_memo_memo.py	448605eda4a956c8	(span) a code span may cross a line ending
MUTANT	span: opener and closer are backtick strings of EQUAL length (index every run under ONE key, so the closer is whichever run comes next)	plan_memo_lexer.py	44753eda9fe1e5b4	(span) backtick strings pair by EQUAL length
MUTANT	table: a schema body row of the wrong width is exit 2 (the width miss gates)	plan_memo_population.py	b9a74ec20fb95776	(rc) a schema body row whose width differs from its header is rc 2
MUTANT	table: delimiter cell = >=1 hyphen with optional colons	plan_memo_blocks.py	11cc721e6894dd25	(table) a delimiter cell is >=1 hyphen with optional colons; `:` alone is not
MUTANT	table: header and delimiter must have equal width	plan_memo_blocks.py	7fef0b814614f83d	(rc) a slice header over a one-cell delimiter row is not a table, so its wide body row is not a width miss
MUTANT	usage: the help text is NOT routed through the report escape (re-inject it -- the 13 KB usage prints as one physical line)	plan-memo-umbrella-check.py	ae8b69d9e8c5ff1b	PROPERTY: every line the run REPORTS goes through the escape -- measured over the EMIT SITES, the subject the escape function's own control cannot reach
